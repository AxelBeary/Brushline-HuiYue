# 待修复问题清单

> 维护者：四号（记录）/ 五号（排查）/ 一号（派工）
> 旧版归档：`docs/archive/待修复问题清单.md`

---
## P0 审计修复（3 项）— ✅ 已修复（v0.24，`e03cabc`）

- **修复人**：五号
- **内容**：reset 禁止活跃订单 + 价格加减法修复 + 看板队列过滤 + 标签 UI
- **测试**：508 个用例全绿

---
## P1 审计修复（4 项）— ✅ 已修复（v0.24）

- **修复人**：五号
- **内容**：send-code 防枚举 / health promises / Q4 收入 / like 限流
- **测试**：512 个用例全绿

---
## PERF-1：字体跳变（霞鹜文楷未拆分 + 无预加载）— ✅ 已修复（v0.24，霞鹜文楷子集拆分 3.3MB→1.7MB）

- **发现日期**：2026-08-01
- **发现人**：外部用户反馈（经奚怡熊转达）
- **现象**：打开网页时字体有明显跳变（先用系统字体显示，1~3 秒后突然切换为自定义字体）
- **修复**：霞鹜文楷子集拆分为 6 个 woff2 按需加载（3.3MB → 1.7MB），v0.24 合入

---
## v0.28 审计修复（3 项）— ✅ 已修复

| 项 | 修复 | 版本 |
|---|---|---|
| OD1 订单详情 #undefined 拼入 UI | 条件表达式 null guard | v0.28 |
| T3 TierManage 14 处硬编码中文 | i18n 化（36 键全覆盖） | v0.28 |
| app.js Windows 路径分隔符 | `startsWith(WEB_DIST + sep)` | v0.28 |

---
## v0.28 审计遗留（视觉类，待视觉统一批次）

- 详见 `docs/audit-画师后台现状.md`（60+ 项）
- 设计书：`docs/画师后台设计书-完整版.md`
- 视觉规范：`docs/画师后台视觉规范-v1.md`
- 状态：等用户带设计稿回来后定稿实施

---
## UX-20260804：四号交互直觉审计（4 项，均已转一号）

> 来源：用户要求"排查哪些操作不够直觉"，四号读代码逐项核实（行号为证）。详细分析见 `docs/comms/04-to-01-UX审计-20260804.md`。

| # | 问题 | 证据 | 归属 |
|---|------|------|------|
| 1 | 画风/尺寸**无法排序**（后端 sort_order API 已有，前端无拖拽） | ArtStyleManager.vue 无 draggable | 波 1 顺带补 |
| 2 | 已有画风**无法追加导入增项**（importAddons 只在新建弹窗） | ArtStyleManager.vue L158 `v-if="!editingStyleId"` | 波 1 增项区补"＋导入" |
| 3 | 增项保存心智不统一（勾选/改价要点"保存增项"，尺寸覆盖每行独立保存） | ArtStyleManager.vue L97/L125 | 波 1 统一即时保存 |
| 4 | 订单筛选缺"修改中(revision)"档（DB 有 7 态，筛选条只 6 档） | OrderList.vue L20-28 vs L128 | 小修，随下批 |

---
## AUDIT-20260804：五号全项目深度审计（安全/质量/交互）

> 来源：五号只读审计（server 44 文件 + web 95 文件，含 2 个子代理并行，发现已逐条核对源码）。
> 总体结论：**安全基线显著高于预期，无 🔴 严重漏洞**（SQL 注入/XSS/越权/路径穿越/价格完整性/状态机/限流/部署配置全部确认正确）。
> M3（登录码剩余次数 off-by-one）经一号验算为**误报**——公式 `4-attempts` 恰等于本次失败后剩余次数，语义正确，不改。

| # | 问题 | 级别 | 归属 |
|---|------|------|------|
| H1 | 生产容器 `AUTH_DEV_MODE=true`（知 QQ 号即可拿登录码登录） | 🟡 上线前必做 | 见下方清单 |
| L4 | QQ Bot 未接入（登录码无真实通知渠道，全项目唯一 TODO） | 🔵 上线前必做 | 见下方清单 |
| M1 | 图片路径归属校验缺口：createTier/updateTier 的 example_image、createArtStyle/updateArtStyle 的 cover_image 无路径校验（已核实低危：公开目录渲染+GC 保护，最坏是热链他人公开图） | 🟢 | v0.36 清账（对照 avatar 写法补齐） |
| M2 | focus-image 路由层缺路径校验（服务层兜底无越权，纵深防御不一致） | 🟢 | v0.36 清账（与 M1 一次改齐） |
| M4 | demo-data.ts 绕过 createOrder 字段完整性（已两次事故：deadline/width-height） | 🟢 | v0.36 清账（加列清单注释或改走服务函数） |
| L1 | api/index.js 5 处静默吞错（签名刷新/下载/二维码/embed/myOrders） | 🔵 | v0.36 清账（至少 console.warn） |
| L2 | QuickActions 拖拽无禁用态竞态防护（概率极低） | 🔵 | 观察项 |
| L3 | 支付金额前端无范围前置校验（后端 schema 兜底 400） | 🔵 | 随手修 |
| L5 | i18n 空字符串值 3 处（zh-CN.js:438/475/738） | 🔵 | 随手修 |

---
## BUGBATCH-20260804：五号挖 bug 报告（8 项 + 子代理 22 条）

> 来源：五号按挖 bug 派工五方向只读审计（基线 593d8ef），子代理 2 个（task-0 前端交互 22 条 / task-1 质量+授权）。关键根因链一号已抽查实锤。

| # | 问题 | 级别 | 处置 |
|---|------|------|------|
| BUG-1 | 分期状态与收款池双模型分叉（getOrderInstallments 节点模型 vs computeInstallmentStatuses 池子模型；通用收款不传 installmentId → 钱进池节点不动；撤销不回冲） | 🟡 | **待用户拍板**：a) addPayment 无 installmentId 按序自动分摊 + revoke 透传 b) getOrderInstallments 改用池子推算（死代码转正，改动最小前端零改）——五号推荐 b |
| BUG-2 | i18n 缺 56 键（errors.ts 122 码 vs locales 68 键，脚本实锤） | 🟡 | 批次 A 已派五号 |
| BUG-3 | 公开路由 hidden 过滤缺口 ×4（calculate-price/validate-discount/like 缺过滤；calculate-style-price 波 1 域缓） | 🟡 | 批次 A 已派五号（3 处） |
| BUG-4 | 分期舍入漂移（尾差不归末节点 ±1~2 分） | 🟢 | 批次 A 已派五号 |
| BUG-5 | 死代码 ×7（TplCoverShowcase/copyTemplateToArtist/resolveSpeechTemplate/computeInstallmentStatuses/nowSqlite/TERMINAL+COMPLETED_STATUSES） | 🟢 | v0.36 清账统一删（邻波 1 域等合入后） |
| BUG-6 | 时间条整条平移可拖进过去（QueueBoard.vue:817-821 钳制只在开工日已在过去时生效） | 🟡 | v0.35 合入后复查（波 2 域） |
| BUG-7 | 设置页须知/profile 加载失败 → 保存覆盖真实数据（静默吞错不可逆） | 🟡 | 批次 A 已派五号（错误态+禁保存） |
| BUG-8 | admin 存在性校验不一致 ×3（PUT rules/GET greetings/GET workflow 不存在返回 200）；status enum 缺 hidden | 🟢 | 前两条批次 A 已派；**第三条待用户拍板**（admin 端能否设 hidden 状态） |

**task-0 子代理其余发现**（已核实行号）：QueueBoard 平移两 PUT 非原子（半平移风险）、OrderDetail picker 保存失败不回滚、客户 QQ 校验只有 required、备注 Enter 可重复提交、状态推进按钮无防连点、滑块确认缺 pointercancel——均归 v0.36 清账/随手修池，波 1/2 域项合入后复查。

## ⚠️ 上线前必做清单（真实画师使用前）

| # | 项 | 操作 |
|---|-----|------|
| 1 | AUTH_DEV_MODE 关闭（H1） | `.env` 改 `AUTH_DEV_MODE=false` + 重建容器——演示期保留，上线前必改 |
| 2 | QQ Bot 接入（L4） | 登录码真实通知渠道，auth.service.ts:63 TODO |
