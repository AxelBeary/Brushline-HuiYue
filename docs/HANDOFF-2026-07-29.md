# 交接状态文档 — 2026-07-29 收工

> 一号（主理人）交接记录。下次开工时读此文件即可恢复上下文。

---

## 当前 master

- **HEAD**：`9865a11`，与 origin 同步
- **测试**：118/118 通过（7 文件）
- **构建**：通过
- **容器**：commission-web 运行中，http://localhost:3000 可访问（ports 3000 默认开放）
- **npm audit**：0 vulnerabilities

---

## 今日完成

| 提交 | 内容 |
|------|------|
| `b2efc7a` | plan-*.md 状态标注更新（五号） |
| `0fe5391` | v0.11.x 四连：R17 分段按钮 + R14 计价渐进展开 + R16 列表缩略图 + R20 焦点模式移看板（二号） |
| `931ac7d` | UI-1 参考图删除按钮（五号） |
| `9ddba18` | 签名 URL 三处补全：focus-image / queue / orders（一号，三号发现） |
| `3afc28d` | R3 快速录单补全（cherry-pick 恢复，原 commit 因历史重写丢失） |
| `dc8a374` | ENV-1 启动时确保 uploads 目录存在（五号代码，一号提取入库） |
| `5dc626c` | UI-2 看板大图模式左图右文固定尺寸（五号代码，一号提取入库） |
| 文档提交 | v0.11 技术文档全量更新（changelog / 开发自参考 / 维护说明书 / 切换指南） |
| 文档提交 | REQ-003 体验走查反馈批次（四号） |
| 文档提交 | REQ-004 画师端侧边栏可折叠（四号） |
| 文档提交 | SPEC-001 v0.12 详细设计（四号） |

---

## 进行中（未合入 master）

| 角色 | 任务 | 分支 | 状态 |
|------|------|------|------|
| 二号 | R21 侧边栏折叠 | `feat/client-frontend-r21-sidebar`（worktree: artist-commission-client） | 本地 commit `bedcad8`，**未提交审核** |
| 四号 | v0.12 设计文档 | SPEC-001 已在 master | 已提交，**待一号审核** |

---

## 待安排（下次开工）

| 优先级 | 内容 | 分配 |
|--------|------|------|
| P1 | ~~审核 SPEC-001（v0.12 详细设计）~~ | ✅ 已通过（`1021ec8`），方案 A 接受，GC 硬性前置条件确认 |
| P1 | 审核二号 R21（侧边栏折叠） | 一号（等二号提交） |
| P1 | 迁移 v12 实施（R18/R15/R19） | 三号（需用户确认高风险操作） |
| P2 | R18 订单图库前端 | 二号（迁移 v12 后） |
| P2 | R15 外链列表前端 | 二号（迁移 v12 后） |
| P2 | R19 备注附图前端 | 二号（迁移 v12 后） |

---

## 已知问题 / 注意事项

### 1. master 历史被重写（已发生两次）
- 第一次：P0 + R7 合并丢失，cherry-pick 恢复
- 第二次：R3 合并丢失，cherry-pick 恢复（`3afc28d`）
- **根因不明**。可能是某个 Agent 的 worktree 操作触发了 force push
- **应对**：合并后立即 `git push origin master`，发现丢失时从 reflog / 对象库 cherry-pick 恢复
- **教训**：`git log --grep` 搜不到 ≠ 没做过，需查 `git reflog --all` 和 `git cat-file`

### 2. 五号分支两次混入越权文档变更
- 第一次（ENV-1 分支）：混入 R3 状态回退 + PROC-1 回退 + REQ-004 未授权删除
- 第二次（UI-2 分支）：混入 SPEC-001（v0.12 设计文档）未授权删除
- **处理**：两次均未合并分支，代码修复由一号手动提取入库
- **已告知五号**：只改授权文件，不碰文档。下次再犯需升级处理

### 3. 签名 URL 机制
- `images/` 公开，`references/` / `deliverables/` / `notes/`（v0.12）需签名
- 规则：service 层存裸路径，routes 层签名后返回
- `signOrderUrls()` 辅助函数（order.routes.js:15-22）统一处理 references + deliverables
- v0.12 的 notes 附图**必须**加入 signOrderUrls（SPEC-001 已标注）

### 4. 四号两次事实误判（已纠正）
- R3 状态：误判"未实施"（实际是历史重写导致搜不到）
- PROC-1：误判"v0.11.x 未合入"（实际已在 master）
- 四号已复盘并留档，根因是"历史重写场景下 git log 搜不到 ≠ 没做过"

---

## Worktree 状态

```
D:/Hermes Agent CN Desktop/workspace/artist-commission         5dc626c [master]
D:/Hermes Agent CN Desktop/workspace/artist-commission-client  dc8a374 [feat/client-frontend-r21-sidebar]
```

二号 R21 worktree 保留（有本地 commit `bedcad8`）。下次开工时二号继续提交或一号审核。

---

## 需求进度总览

| 需求 | 版本 | 状态 |
|------|------|------|
| R1~R11 | v0.11 | ✅ 全部合入 |
| R3 录单补全 | v0.11 | ✅ cherry-pick 恢复 |
| R14/R16/R17/R20 | v0.11.x | ✅ 合入 |
| R21 侧边栏折叠 | v0.11.x | 🔵 二号本地 commit，待提交审核 |
| UI-1 参考图删除 | — | ✅ 合入 |
| UI-2 大图视觉 | — | ✅ 合入 |
| ENV-1 uploads 目录 | — | ✅ 合入 |
| R15 外链列表 | v0.12 | 📋 SPEC-001 待审核 |
| R18 订单图库 | v0.12 | 📋 SPEC-001 待审核 |
| R19 备注附图 | v0.12 | 📋 SPEC-001 待审核 |
| R12/R13 | v0.12 | 📋 备案 |

---

## 快速恢复命令

```powershell
cd "D:\Hermes Agent CN Desktop\workspace\artist-commission"
git pull origin master
cd server && npm install && npx vitest run && cd ..
cd web && npm install && npm run build && cd ..
cmd.exe /c "docker compose up -d --build web"
# 访问 http://localhost:3000
```
