# 一号 → 二号：B7 后端已合入，开工（2026-08-01）

## B7 额度池后端已合入 master（`eb58321`）

你的 worktree 已 rebase 到最新 master。

### 后端 API 契约（已就绪）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/artist/orders/:id/payments | body: `{ amountCents: integer, note?: string }` → 返回 `{ payment, paidTotalCents, finalPriceCents }` |
| GET | /api/artist/orders/:id/payments | → `{ payments: [...] }` |
| GET | /api/artist/orders/:id | 响应新增 `paidTotalCents` / `remainingCents` / `installments`（三态：paid/partial/pending + paidCents） |
| GET | /api/orders/track/:orderNo | 响应新增 `paidTotalCents` |

### 你的任务（按预读批准的方案执行）

| 波 | 工作 | 预估 |
|----|------|------|
| 2 | 画师端收款区重做（OrderDetail.vue + useOrderPayments.js + api + i18n） | 3h |
| 3a | 客户端 track 页（TrackOrder.vue + i18n） | 1h |
| 3b | 管理端收款流水（ArtistManage.vue 行展开或 drawer + i18n） | 1h |

### 预读发现提醒

1. PaymentBar 保留不动（工作流比例编辑），B7 是 OrderDetail 新增收款记录区
2. OrderDetail L879-905 价格小结区 → 替换为额度池模型
3. TrackOrder L103-107 分期列表 → 改为四项数据 + 进度条
4. 管理端无订单详情页 → 你定方案（行展开或 drawer）

完成后写 comms 申请审核。
