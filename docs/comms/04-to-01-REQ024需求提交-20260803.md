# 四号 → 一号：REQ-024 需求整理完成（画风档位统一），申请审核排期

> 日期：2026-08-03
> 交付物：`docs/requirements/REQ-024-画风档位统一.md`（待审状态）

## 结论

用户口头需求"主页只显示画风图，不能显示我自己上传的图？画风和档位两个页面应该放在一起"经三轮拷问对齐，产出 REQ-024，含 5 项功能（F1 档位带图、F2 后台合并+多画风开关、F3 点档位切大图、F4 下单页预选、F5 旧模型迁移）。用户关键拍板（均逐字记录在文档"用户原声"节）：

- 多画风开关**一律默认关**，接受 alice 第二画风迁移后暂时不可见
- 默认画风 = 排序最前的启用画风（动态顺延）
- 旧画师迁移：默认画风名"默认"，图丢弃重传，描述/天数重写
- 预选（a）方案：预选齐画风+档位直接跳第三步
- 无图档位：兜底显示画风封面，不留空白

## ⚠️ 与 v0.34 在途任务的重叠（重点，请一号排期时注意）

| 重叠点 | v0.34 在途 | REQ-024 | 建议 |
|--------|-----------|---------|------|
| 尺寸行可点/预选跳转 | 二号任务 B（goOrder 带 `?styleId=&sizeId=` + OrderForm 读 query 预选） | F4 下单页预选 | **任务 B 是 F4 的子集**。建议 B 照常做，REQ-024 F4 在其上补"入口区分"（展示柜带预选/约稿按钮不带）与"跳第三步" |
| 封面图裁切/尺寸行纯展示 | 二号任务 C（contain 不裁切）+G（空间） | F3 改展示柜交互 | C/G 是局部修补，F3 是交互重构。**建议 v0.34 合入后 F3 再排**，避免二号同区域返工 |
| 档位图上传 | 三号任务 2（封面上传即时保存，画风级） | F1 尺寸级带图（新字段+上传/选作品） | 不冲突但相邻。F1 需 v37 迁移加字段，三号任务 2 修完画风级 UX 后可顺带沿用同一模式 |
| 迁移脚本幂等 | — | F5 | v0.33 教训已写入文档：清理与种子区分归属 |

**总原则建议**：REQ-024 整体排 v0.35 或之后，不与 v0.34 并行抢同一批文件。

## 待确认问题（仅剩 1 项，不阻塞审核）

- ~~历史订单引用旧档位的显示~~ → ✅ 四号已内部定案（代码核实：tier_id 外键 ON DELETE SET NULL + 查进度页已有"定制"兜底 + 价格存 snapshot 不依赖档位表，迁移无需特殊处理）。已写入 REQ-024 第五节。
- 旧"展示专用"（showcase）档位迁移后变普通尺寸，三选一（直接迁不提示 / 迁+后台提示一次 / 不迁丢弃）——**待用户拍板**。四号核实：demo 数据 showcase 未被使用（carol 三档均 visible），真实环境无法保证。用户拍板后四号自行更新文档，不打扰一号。

## 申请

请一号审核 REQ-024 是否可进入开发。审核通过后由一号拆解排期（建议避开 v0.34 在途区域）。
