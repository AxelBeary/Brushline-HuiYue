# 三号交付：v0.35 补漏（任务 A 5 条）+ 波 3 F6 后台 UI

> 日期：2026-08-04
> 分支：`feat/v035-style-unify-backend`（worktree wt-03，已 merge master 至 `6ec792d`，fast-forward 无冲突）
> 派工：`01-to-03-v035补漏加波3-20260804.md`

## 任务 A：波 1 补漏（5 条）

| # | 项 | 结果 |
|---|-----|------|
| A1 | 删旧「增项」tab | ✅ TierManage 删 tab + import；`AddonManager.vue` 已删（唯一消费端核实过，旧增项后端 API 按派工保留归 v0.36） |
| A2 | 菜单文案 | ⚠️ **no-op**：merge master 后核实 `menu.tiers` 和 `tiers.title` 已是「价格管理/Pricing」（master 上已改好），无需动作 |
| A3 | 画风/尺寸拖拽排序 | ✅ 双层 vuedraggable：画风卡片间 + 尺寸行间；grid/flex class 放 `<draggable>` 自身（v0.26 教训）；拖拽后逐条 PUT sort_order |
| A4 | 「＋导入增项」入口 | ✅ 画风卡片增项区按钮（有未导入项才显示）→ 弹窗列增项库未导入项勾选 → 确认导入（现有项原状 + 新项默认启用，整体 PUT setStyleAddons） |
| A5 | 增项保存统一即时 | ✅ 勾选立即 PUT；改价防抖 500ms 后 PUT；覆盖改价防抖 PUT、隐藏勾选即时 PUT；失败 ElMessage + 回滚（勾选/隐藏单格回滚，改价拉服务端实际值）；「保存增项」按钮和覆盖行内保存按钮已删 |

**A3 实现说明**：后端无批量 reorder 端点，前端拖拽后对 `sort_order !== index` 的行逐条 PUT（画风/尺寸通常 ≤10 条，请求量可控）。尺寸区从 el-table 改为自绘行列表（el-table 不支持行拖拽），信息密度不变：缩略图/名称/价格/描述/工期/操作。

## 任务 B：波 3 —— F6 后台 UI

ArtworkManage.vue 作品编辑弹窗（主图区和网格区各一个入口按钮）：
- 标题（≤100）+ 自由描述（≤2000 textarea）
- 档位标注多选（el-select multiple，选项 = 启用画风×尺寸展平；**多画风时「画风 · 尺寸」防歧义**，单画风只显示尺寸名）
- 保存串行两 PUT：`PUT /api/artist/artworks/:id` + `PUT .../tags`（后端无合并端点），成功提示 + 刷新回显
- 档位选项加载失败不阻塞页面（静默，编辑弹窗打开时选项为空）

api/index.js 补两个封装（授权范围内）：`updateArtwork(id, data)`、`setArtworkTags(id, sizeIds)`。

## 改动文件

- 删：`web/src/components/artist/AddonManager.vue`
- 改：`web/src/views/artist/TierManage.vue`、`web/src/components/artist/ArtStyleManager.vue`、`web/src/views/artist/ArtworkManage.vue`、`web/src/api/index.js`、`web/src/locales/{zh-CN,en}.js`（仅 styleManage/tiers/artworks 命名空间，menu 未动）

## 验证

- server：`tsc --noEmit` 0 错；`vitest run` **703/703**（本波未动后端，确认合并后无回归）
- web：`eslint .` 0 错 0 警；`vite build` ✅；`vitest run` **153/153**
- merge master：fast-forward 无冲突（locales 无重叠——五号 errors 键 vs 我 styleManage/tiers/artworks）

## 遗留说明

- 旧增项后端 API（/api/artist/addons 等）保留未删，归 v0.36 清账
- ArtStyleManager 加载时多取一个 `getAddonTemplates`（A4 候选列表），请求 +1
