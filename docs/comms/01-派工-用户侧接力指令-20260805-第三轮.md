# 01 派工 — 用户侧接力指令（2026-08-05 第三轮）

> 用法：每个角色开一个独立 Hermes 聊天窗口，把对应段落整段粘贴。四路全开，互不阻塞。
> 一号会话（本窗口）继续守 master 门禁：审核 → 合并 → 记账。

---

## 窗口一：二号（F1+F2 前端批，主链，预计最重）

你是二号（客户页面前端）。开工：先读 `docs/comms/STATUS.md` 恢复全局状态，再读你的派工 `docs/comms/01-to-02-F1F2前端批-20260805.md` 逐条执行。worktree 已建好：`../artist-commission-f1f2`（分支 f1f2-frontend），第一步 cd 进去 npm install、git merge master、git merge f2-social-backend 联调。遵守身份自检规则：只动自己分支，不碰 master，不推送不合并，交付写报告。F1+F2 验收清单在 `docs/REQ022-F1F2验收清单.md`，交付前逐条自检。

---

## 窗口二：三号（addons 清理批）

你是三号（后端）。开工：先读 `docs/comms/STATUS.md`，再读派工 `docs/comms/01-to-03-addons清理批-20260805.md`。worktree 已建好：`../artist-commission-addons`（分支 addons-cleanup）。本批执行你自己评估报告 §Q1.5 的子集（算价读路径 + 测试清理），POST /api/orders schema 的 addons 字段本批不删（等前端同批）。只动 server，测试基线 864 全绿才算交付。

---

## 窗口三：四号（A 测执行手册）

你是四号（需求整理）。开工：先读 `docs/comms/STATUS.md`，再读派工 `docs/comms/01-to-04-A测准备批-20260805.md`。worktree 已建好：`../artist-commission-v038acc`（分支 atest-docs）。纯文档批：产出 `docs/A测执行手册.md`（测试剧本 + AUTH_DEV_MODE 关闭检查单 + 反馈收集模板 + 已知限制清单），零代码改动，现状断言逐条实锤。

---

## 窗口四：五号（打磨第二批）

你是五号（QA/打磨）。开工：先读 `docs/comms/STATUS.md`，再读派工 `docs/comms/01-to-05-打磨第二批-20260805.md`。worktree 已建好：`../artist-commission-polish`（分支 polish-leftover）。三项：回收站插画偏亮修复 + 验证风格统一（required vs JS 守卫）+ 画廊空态（可选）。D 项（需求描述可空过）待用户拍板，不碰。

---

## 用户待拍板项（不阻塞以上四路）

1. **打磨批 D：约稿需求描述可空过**——保持选填 / 加软提示 / 加最低校验，三选一（五号第三方报告原问题）。
