# 03→01：addons 冻结表清理第一批（算价读路径）交付

> 日期：2026-08-05 · 执行：三号 · 分支：`addons-cleanup` · commit：`3e0d190`（基于 master `a8ef3e5`）
> 派工：`docs/comms/01-to-03-addons清理批-20260805.md` · 范围：只动 server，web 零触碰，不推送不合并

## 做了什么

按派工 Q1.5 第 1 条（算价链路）+ 第 3 条（测试清理）执行完毕：

1. **`pricing.service.ts`**
   - `getPublicPricing`：移除 price_addons/addon_tiers 读取与「档位+适用增项」组装；tiers 保留空 `addons: []` 字段维持响应结构兼容（前端 `useOrderForm.js` 兜底 `tier?.addons || []` 下批同清）
   - `calculatePrice`：移除 addon_tiers 校验 / price_addons 取价 / 去重 / max_qty / inquiry 全部分支；改为从 opts 解构（addons 留在 opts 类型里不接收，避免 TS excess property check 报错——调用方 createOrder/pricing.routes 仍传 addons，下批删 schema 时同步清理）；`addonTotal` 恒 0（PriceResult 字段保留，响应契约不删字段）
   - 档位基础价（price_tiers）零改动；`Addon` import 移除；文件头注释同步更新
2. **测试清理**
   - `pricing.service.test.js`：删 seedAddon 辅助 + 6 个增项行为测试（TC-P-18/19/20/21/27）；TC-P-16/17 改写为回归「传 addons 不影响档位算价」（含不存在 ID / 重复 ID 均等价忽略）；TC-P-25 改为基础+倍率+分期；TC-P-29 断言 tiers.addons 恒空数组
   - `style.test.js`：删 seedPriceAddon + TC-MIG-02 两行调用（迁移测试验证的是 addon_templates 新模型，不受影响）
   - `setup.js`：cleanDb 移除 addon_tiers/price_addons 两行 DELETE（本批后测试无插入路径，成死代码）
3. **明确未做（派工红线）**：POST /api/orders schema addons 字段、createOrder addons 参数、DROP 表迁移（v43 留给下批）

## 测试基线对比

| 项 | 基线 | 本批 | 说明 |
|----|------|------|------|
| server 测试 | 864/864（51 文件） | **859/859（51 文件）** | 减少 5 = 删除的增项行为测试（18/19/20/21/27），符合「受影响断言改写」 |
| lint | 0 | 0 | `npm run lint` 零输出 |
| tsc | 0 | 0 | `npm run typecheck` 零输出 |

新增回归 2 例（TC-P-16/17），断言 addons 传参不影响档位算价（basePrice 200 / addonTotal 0 / totalPrice 200），覆盖不存在 ID 与重复 ID 两个旧拒绝路径。

## 剩余清理项（下批，勿越界）

1. POST /api/orders 与手动录单 schema 的 addons 字段删除——与前端旧模型兜底分支（useOrderForm.js / ManualOrder.vue）耦合，等二号前后端同批
2. createOrder addons 参数删除（同上耦合）
3. pricing.routes.ts POST /api/pricing/calculate schema addons 字段（算价公开接口，客户端同批）
4. DROP price_addons/addon_tiers 迁移——版本号 **v43**（v42 已被 F2 平台表占用）；守 v38 事故规则：事务外 + 显式关 FK + 迁移前备份
5. errors.ts ADDON_NOT_FOUND/ADDON_MAX_QTY/ADDON_NOT_FOR_TIER 错误码、entities.ts Addon interface、fastify.d.ts requireOwnAddon 死注释——随 DROP 批一并清

## 验证证据

- `npx vitest run`：51 文件 859/859 通过，40.44s
- `npm run lint`：exit 0 零输出
- `npm run typecheck`：exit 0 零输出
- commit `3e0d190` 已落分支，未推送未合并

—— 三号
