# 04-派工-代码健康巡检-20260807

> 派工：一号 → 四号（requirements）
> 工作方式：**纯只读审计，零代码改动，不需要 worktree**——在主仓目录只读（不许 git 写操作），输出报告落 `docs/comms/`。
> 目的：用户要求「找个人检查有没有新隐患和 bug，代码要保持优雅」——对 master 当前状态（`64f1dc8`）做一轮**只读健康巡检**。

## 一、背景

近期合入批次多（v0.40 三修 + 埋点看板前后端 + OrderDetail 拆分 + repowiki 报告），用户担心累积新隐患。**不做功能测试**（那是门禁职责），只做静态健康巡检。

## 二、巡检范围（精确）

### 1. 后端 `server/src`（TS，重点）
- **any 黑洞**：全库约 193 处 `: any` / `as any` / `<any>`——分类：哪些是合理逃生舱（如 JSON.parse 结果、第三方无类型），哪些是**偷懒 any**（应补类型）——列出 top 文件 + 典型位置，**不要求全清**（strict 全开批正在做，重复活），重点是找"any 掩盖的隐患"
- **TODO/FIXME/HACK**：62 处——分类：过时注释（代码已实现但注释没删）/ 真实遗留（未实现功能/已知缺陷）——列出值得转工单的
- **console.log/debug**：2 处——确认是否生产环境会打印敏感信息
- **新批次风险点**（重点查，刚合入）：
  - `features/tracking/tracking.service.ts` + `tracking.routes.ts`：SQL 拼接（`datetime('now', '-${days} days')`）、白名单校验、限流、权限（requireAdmin/requireAuth）是否严密
  - `app.ts` 静态服务/缓存头/路径穿越防御（近期改过）
- **资源/并发**：db.prepare 是否都在函数内（避免 prepare 泄漏）、有无明显 N+1

### 2. 前端 `web/src`（JS+Vue）
- **新批次风险点**（重点查，刚合入）：
  - `views/admin/TrackingAnalytics.vue`（新页面）：v-loading、错误处理、开关回滚逻辑
  - `views/artist/Dashboard.vue`（加了门面统计区块）：独立 try/catch 是否真的不阻塞其他模块
  - `components/artist/order/PaymentPanel.vue` + `GalleryPanel.vue`（新拆分组件）：props/emit 是否完整、有无事件丢失
- **xss 面**：`v-html` 使用点（应极少数且白名单）；用户输入直接进 innerHTML 的
- **i18n 完整性**：zh-CN/en 键是否同步（新批次的键）
- **死代码**：新组件是否被引用、有无重复实现

### 3. 优雅度（用户红线：代码要保持优雅）
- 明显重复代码块（同逻辑写两遍）
- 魔法数字/魔法字符串（应常量）
- 超长函数（>100 行单函数，新批次内）
- 命名不当/注释误导

## 三、输出格式

- 报告写 `docs/comms/04-to-01-代码健康巡检-交付-20260807.md`：
  - **🔴 真实隐患**（会导致 bug/安全问题，列文件:行号 + 问题 + 建议）——最高优先
  - 🟡 改进建议（优雅度/可维护性，不紧急）
  - ✅ 已确认健康项（重点批次查了没问题的，给用户安心）
  - 每个问题标注：文件:行号 + 严重度（高/中/低）+ 建议处置（派工/记待办/可忽略）
- 零代码改动，不 commit；报告由一号合入

## 四、纪律

- **只读**：不 git add/commit/push/merge、不碰 master、不建 worktree
- 判断以 master 代码为准；**不夸大不编造**（用户会逐条核实，之前外部报告约 1/3 不实被拒）
- 新批次（tracking/Dashboard 统计/拆分组件）是重点，旧代码扫到明显的也记
- 20 分钟无产出停下报卡点
