# 05E → 01 交付：巡检修复批（05D 发现的真问题）

> 执行人：五号（bugfix）· 日期：2026-08-08
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w17`
> 分支：`beta/fix-inspect-findings`（master 已 merge，无冲突）
> 依据：施工图 docs/comms/05E-派工-巡检修复-20260808.md
> 授权范围：施工图授权清单 10 项，本批全部落在授权文件内

## 〇、执行概要

- 05D 巡检 Top5 低成本高收益 + 顺手小项全部完成，另按「如顺手」完成 W1/P1 订单下拉 100 条上限。
- 门禁四项全绿：lint / test:web（262 passed）/ check:i18n / build。
- 零后端改动、零新依赖、零超授权文件；关键逻辑（Q1 完成区刷新、O1 复合筛选不漏单）已做针对性验证。

## 一、逐任务改动说明

### 任务 1：🟡-Q1 排期看板完成区刷新（QueueBoard.vue，+3/-1）
- `onDeliveredFromBoard`：交付成功后除 `loadQueue()` 外补 `await loadCompletedQueue()`——刚交付的订单立即出现在最近 7 天完成区，不再要求整页刷新。
- `refreshAll`：`Promise.all([loadQueue(), loadBufferQueue()])` → 加入 `loadCompletedQueue()`，三区（正式/缓冲/完成）全量刷新。
- 验证：正则抽取两函数体确认 `loadCompletedQueue()` 均在调用（见 §三）。

### 任务 2：🟡-SE1 Settings 切 tab 未保存修改静默丢失（Settings.vue，+54/-1；zh/en locales 各 +2 键）
- 采用施工图方案 A（切 tab 前脏检测 + 确认拦截），el-tabs 挂 `:before-leave="beforeTabLeave"`。
- 基线快照机制：`loadProfile` 成功 / `save` / `saveRules` / `loadRules` 成功后更新对应 tab 基线；切换前 `snapshotTab` 对比，有未保存修改弹 `ElMessageBox.confirm`（确认离开 / 取消留在原 tab）。
- 脏检测按各 tab 保存字段精确圈定：
  - profile → name/bio/artistCode/contactQq（avatar 即时上传不参与）
  - showcase → customLinks.url（忽略本地 `__k`）/ inspirationTags / announcement / announcementExpiresAt / rulesContent（须知独立保存一并纳入）
  - template → templateId/paletteId/accentColor
- 基线未加载（加载失败/懒加载未发生）不拦截，避免误报；保存成功后基线同步，不重复弹窗。
- 新增 i18n：settings.unsavedLeaveTitle / unsavedLeaveTip。

### 任务 3：🟡-A1 触屏按钮 + 🟡-A2 上传大小检查（ArtworkManage.vue，+14）
- **A1**：`.artwork-actions` 加 `@media (hover: none) { opacity: 1 }`——触屏设备操作层常显；桌面 hover 展开保持；主图区与网格区共用该 class 一处覆盖。遵守克制动效纪律：只改可见性、无 hover 位移。
- **A2**：`handleUpload` 开头补与粘贴路径一致的前端校验——非 `image/*` 拒绝、>10MB 拒绝，超限 `ElMessage.warning` + return 不发送。
- 新增 i18n：upload.fileNotImage / upload.fileTooBig（不带「粘贴」字样，与粘贴文案语义一致）。

### 任务 4：🟡-O1 分页漏单 + 🟢-O2 row-click（OrderList.vue，+45/-4）
- 查证后端 `order.routes.ts` L348-351 + `order.service.ts`：`status` 仅支持单值枚举（`WHERE o.status = ?`），无 active/completed 复合语义 → 按施工图走方案 B（前端拉全量再过滤）。
- **O1**：复合筛选（active/completed）路径改为 `fetchAllOrders()`（pageSize 200 循环拉全量）→ 客户端过滤 → `orders.value` 即过滤结果，`total` 为过滤后长度（不再误导）；`displayedOrders` 复合分支改为前端分页切片。
  - 分页事件分发：`onPageChange`/`onSizeChange` 在复合筛选时只切片不重拉（数据已在内存）；普通单值筛选维持原服务端分页行为。
  - 竞态保护 `loadSeq` 保留，慢请求不覆盖新结果。
- **O2**：桌面 `el-table` 加 `@row-click="onRowClick"` 进详情；详情按钮加 `.stop` 防 row-click 双触发；行 hover `cursor: pointer` 提示可点。移动端卡片原本整卡可点，行为不变。

### 任务 5：🟡-I1/E1 裸 fetch 收口（api/index.js +3 方法 +1 拦截器增强；StandaloneIncome.vue -37；ToolsExport.vue -20）
- `artistApi` 新增：`getStandaloneIncomes(params)` / `createStandaloneIncome(data)` / `deleteStandaloneIncome(id)`——401 自动登出 / 15s 超时 / i18n 错误翻译全部走统一 axios 拦截器。
- 拦截器错误对象附加 `status`/`code`（纯增量，不改变既有消息行为），供调用方特判 404 等场景；StandaloneIncome 删除的 404「越权/已删 → 提示 + 刷新列表」语义完整保留。
- **ToolsExport.loadOverview** 收口为 `getStandaloneIncomes({ from, to })`。
- **ToolsExport.doExport**：CSV blob 下载按施工图保留 fetch（不经 JSON 拦截器），补 15s `AbortController` 超时（对齐 `API_TIMEOUT_MS`）→ 超时专用提示；`res.status === 401` 时清认证 + 跳登录（对齐拦截器行为）。
- 新增 i18n：toolsExport.timeout。

### 任务 6：顺手小项
- **🟢-T2**（DiscountCodeManager.vue，+30）：折扣码行内「复制」按钮，clipboard API + execCommand 兜底（照 ScheduleSharePage 模式）；新增 i18n discount.copyCode / copied / copyFailed。
- **🟢-E2**（ToolsExport.vue）：隐藏「总收入」「订单收入」两个永久「不可用」占位格（后端区间汇总端点缺口），保留 incomeNote 注脚说明；后端端点预留不动。
- **🟢-O2**：见任务 4。

### 可选追加：🟡-W1/P1 订单下拉 100 条上限（api/index.js +1 方法；Watermark.vue +2；PuzzlePage.vue +2）
- `artistApi.getAllOrders(q)`：pageSize 200 循环拉全量（逻辑集中一处，两页零重复代码）。
- Watermark.loadOrders / PuzzlePage.loadOrders 改为调 `getAllOrders()`——订单 >100 时可选到任意早期订单，filterable 客户端搜索覆盖全量。

## 二、门禁结果（全部在 web/ 目录执行）

| 门禁 | 结果 |
|------|------|
| `npm run lint` | ✅ 通过（eslint 0 错误） |
| `npm run test:web` | ✅ 17 files / 262 tests passed |
| `npm run check:i18n` | ✅ 存量豁免 9 条，无新增硬编码中文 |
| `npm run build` | ✅ built in 6.27s |

## 三、关键验证

- **Q1 完成区刷新**：正则抽取 `onDeliveredFromBoard` / `refreshAll` 函数体，确认两处均调用 `loadCompletedQueue()`（`loadCompletedQueue` 定义唯一存在）。
- **O1 分页不漏单**：node 脚本模拟 130 条混合状态订单 → active 过滤得 94 条（=期望值）、completed 37 条（=期望值）；50/页前端切片后 ID 去重 94 个 = total，无重复无遗漏；`total` 为过滤后长度不误导。
- **O1 后端语义查证**：`order.service.ts getArtistOrders` 为 `AND o.status = ?` 单值枚举，复合值确实需前端方案——方案 B 依据成立。
- **授权合规**：12 个改动文件全部 ∈ 施工图授权清单；`git status --short` 仅 12 个 M，无未跟踪残留（临时验证脚本已删；dist 为构建产物被 .gitignore 忽略）。

## 四、未做与说明

- 🟡-M1 / 🟡-G1 / 🟡-I2 / 🟢 系列等其余巡检发现不在本批授权范围（05D 共 32 条，本批承接 Top5 + 顺手项 + W1/P1），如需后续批次请一号再派。
- T1 倍率语义、I3 金额上色为「需验证」项，未在本批处理（待产品语义确认）。
- 无后端改动、无 schema 迁移、无新依赖；失败回滚 = `git revert` 本分支提交（纯前端改动，无数据面影响）。

— 五号 2026-08-08
