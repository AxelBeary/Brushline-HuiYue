# 三号→一号：v0.24-misc 完成（2026-08-02）

## 做了什么

### 任务 A：快捷按钮 DB 字段（迁移 v26）

- 迁移 v26：`artists ADD COLUMN quick_actions TEXT DEFAULT NULL`（幂等，PRAGMA 检测）
- `updateArtist` allowed 列表加 `quick_actions`
- `GET /api/artist/profile` 已含（`...artist` 展开），无需改路由
- 测试：TC-S5-16/17/18（默认 NULL / 设置 JSON / 清除）

### 任务 B：#16 getMonthlyUsage UTC 修复

- 原：`getUTCFullYear/getUTCMonth` 手动拼 UTC 月初 → UTC+8 画师每月 1 号 08:00 才重置
- 新：复用 `utils/date.ts` 的 `localMonthStartSqlite()`（与 dashboard/order-stats 一致）
- 测试：TC-S5-19

### 任务 C：容器重建

- `docker compose down && docker compose up -d --build`
- 迁移 v24/v25 在容器启动时自动执行
- 验证：health 端点返回迁移版本

## 改了哪些文件

| 文件 | 变更 |
|------|------|
| `server/src/db/init.js` | 迁移 v26 |
| `server/src/features/artist/artist.service.ts` | +import localMonthStartSqlite / allowed 加 quick_actions / getMonthlyUsage 修复 |
| `server/tests/monthly-quota.test.js` | +4 用例（TC-S5-16~19） |

## 分支

`fix/v024-misc`，worktree `../artist-commission-misc`，commit `8745ed5`

## 验证结果

- 全量测试 **512/512 通过**（29 文件）
- 容器重建完成，health 端点验证通过
