# 补丁派工：v0.35 波 2 联调——删 mock 换真实 API（二号）

> 日期：2026-08-04 · 分支：`feat/v035-client-gallery-link`（新建）· Worktree：`artist-commission-wt-02`（重建）
> 你的波 2（F3/F4/F6 mock-first）已合入 master `8598729`（含一号审核补的 hover 点击死区 `6bca653`）
> 三号波 1 API 已合入 master `3e2a1af`——可以联调了

## 第一步：建 worktree

```
git worktree add -b feat/v035-client-gallery-link D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02 master
```

## 任务：删两处 mock 注入点，换三号真实 API

你交付报告里标注的两个唯一注入点：

1. **`useArtistData.js` 的 `applyV035MockFields()`**：整函数删除（含 ⚠️ 注释块）
2. **`ArtistHome.vue` getPublicStyles 回调内的包装块**：删 mock 包装，恢复 `styles.value = res || []` 直读

**三号真实 API 契约**（已合入 master，与派工预判有 3 处差异，注意适配）：

### GET /api/public/styles/:subdomain（扩展，数组结构不变）
sizes 每行新增：
- `image`: string|null — 独立上传图路径
- `image_artwork_id`: number|null — 作品集引用 id
- `artwork_image_path`: string|null — 引用作品的实时图路径
- `description`: string|null、`work_days`: number|null
- **⚠️ 差异 1**：三号后端「传一清一」互斥语义（设作品集图时独立图清空），且**直接给了 `artwork_image_path` 解析好的路径**——你的 `resolveSizeImagePath(size, artworks)` 纯函数可简化为：`size.artwork_image_path || size.image || ''`（不再需要 artworks 参数查 id）。TplStyleGrid 的 artworks prop 可保留（其他用途）或按实际使用裁剪

开关行为后端已做：`multi_style_enabled=0` 只返回默认画风，你无需过滤

### GET /api/public/gallery/:subdomain（新端点）
- **⚠️ 差异 2**：这是**独立新端点**，不塞在 styles 里（你 mock 时是从 artworks prop 读的）。返回：
```
{
  artworks: [{ id, image_path, title, description, like_count, is_cover, width, height,
               size_tags: [{ style_size_id, size_name, style_id, style_name }] }],
  filterSizes: [{ id, name, style_id, style_name, sort_order }]
}
```
- **⚠️ 差异 3**：字段名与你预判不同——你的 `tags`（尺寸 id 数组）实际是 `size_tags`（对象数组，含 style_size_id/size_name/style_id/style_name）；`deriveGalleryFilters` 可直接用 `filterSizes`（后端已给好），`filterArtworksBySize` 按 `size_tags[].style_size_id` 匹配
- 开关门控后端已做：关闭画风的标注不出现在 size_tags/filterSizes
- 删尺寸标注 CASCADE 自动清理

### 适配要点
- `web/src/api/index.js` 加 `getPublicGallery: (sub) => api.get(`/public/gallery/${sub}`)`（若无现成方法）
- 多画风筛选标签格式：后端 `filterSizes` 只给 name，多画师的「画风 · 尺寸」前缀你前端拼（用 style_name 字段）
- TplGallery 的 styles prop 改传 gallery 数据（或改组件签名吃新端点数据，你定，交付说明）
- 你的 22 个 useArtistData 测试用例需同步改（mock 函数删了，纯函数签名可能变）

## 验证标准

1. `cd web && npx eslint . && npm run build && npx vitest run`（153+，改测试后数字会变，交付写明）
2. 浏览器实测（vite 5173 + Docker 后端）：F3 尺寸切图真实生效（alice 有迁移数据）+ F6 画廊筛选真实标签 + 大图标签跳下单
3. locales 只动 artistHome/orderForm/gallery 命名空间（三号并行改 styleManage/tiers/menu）
4. 交付 comms：`docs/comms/02-to-01-v035联调-{日期}.md`

## 授权文件

- `web/src/composables/useArtistData.js` + `__tests__/useArtistData.test.js`
- `web/src/views/client/ArtistHome.vue`、`web/src/components/templates/TplGallery.vue`、`TplStyleGrid.vue`
- `web/src/api/index.js`（仅加 getPublicGallery）
- 4 模板传参文件（如签名变化）
