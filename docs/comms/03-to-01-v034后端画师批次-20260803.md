# 三号交付：v0.34 后端+画师后台批次

> 分支：`fix/v034-demo-dims-cover-ux`（worktree wt-03，基于 master `c7b41f8`）
> 日期：2026-08-03
> 状态：✅ 三任务全部完成，申请审核

## 做了什么

### 任务 1：演示作品补 width/height（画廊「顶位置」复发根因修复）
- `demo-data.ts`：`insertArtworks` 改为 async，用 sharp 逐张读种子图实际尺寸写入 width/height（读失败打警告不阻塞）
- 新增 `backfillMissingDims()` 幂等回填段：只碰 width/height 为 NULL 的存量行，挂主流程末尾
- `backfill-artwork-dimensions.js`：删 `pragma journal_mode=WAL` 硬编码（容器是 DELETE 模式），头注释加说明
- **容器内已执行**：新脚本 docker cp + `docker exec -w /app/server npx tsx` 重跑成功（sharp 在 /app/server/node_modules，脚本必须放 /app/server 下跑，放 /tmp 会 MODULE_NOT_FOUND）

### 任务 2：画风封面上传即时保存（用户 2026-08-03 踩的 P0 陷阱）
- `ArtStyleManager.vue` `uploadCover`：编辑已有画风时上传成功**立即 PUT** `updateArtStyle(id, { cover_image })`（对齐 R48 头像模式）；PUT 失败回滚表单预览；新建画风无 id，改 warning 弹 5 秒提示「点保存后生效」
- PUT schema 无 required 字段、service 层部分更新，单字段 PUT 合法（已核实 style.routes.ts / style.service.ts）
- **同模式审计结果**：
  - `TierManage.vue` 弹窗例图：**同陷阱，已同修**——点击上传 + 粘贴上传两处走新 `applyExampleImage()`（编辑→立即 PUT；新建→提示），复用现有 key `tiers.exampleUploaded`
  - `TierManage.vue` 卡片直传（R55）：已是即时保存，无需改
  - `Settings.vue` 头像（R48）：已是即时保存（本次修复的参照模式）
  - `ArtworkManage.vue`：上传即 createArtwork 建档，即时生效
  - `OrderDetail.vue` 交付弹窗 / `ManualOrder.vue` 参考图：属"整体提交"设计（无独立 id / 需确认交付），保留，不修

### 任务 3：画师/管理后台 emoji 清理（11 个文件）
**判定原则**：真 emoji（彩色图形字符）删除或换 SVG；**功能性文本符号（✓✕★↑↓→↩◐○⠿）与 SVG 图标同角色，保留**——它们是删除/勾选/排序按钮的图标本体，删了按钮变空白。
- 直接删除（纯装饰/标题前缀）：OrderDetail 11 处、QueueBoard 视图切换 3 处、MultiplierManager 2 处、AddonManager 标题 2 处 + 分类 label 5 处、StageListView 1 处、ManualOrder 分组 icon 6 处、Settings 通用链接 1 处、GreetingHero 时段 icon 5 处（连 span + computed 一起删）
- 换 `@element-plus/icons-vue` SVG（图标位不能空）：
  - QuickActions 快捷按钮 9 个（TrendCharts→Odometer 统一描边风格）
  - Settings 模板卡 preview 4 组 8 个
  - admin/HealthCheck 状态 4 个（CircleCheck/Warning/CircleClose/QuestionFilled，保留 ok/warn/fail 语义色 CSS）
  - 组件对象入常量数组用 `markRaw()` 防 reactive 代理告警
- **🔒（WorkflowOverviewStrip.vue L7）未动**：共享组件 + 客户端渲染，归二号任务 F 范围

### i18n 说明（locales 归二号，未新增 key）
任务 2 的两条新文案复用现有 key：保存成功用 `common.saved`，新建提示用 `tiers.exampleUploaded`（"例图已上传，点保存后生效"，画风封面即示例图语义吻合）。若一号认为需要专属文案，请让二号补 2 个 key 后我再换。

## 改了哪些文件

| 任务 | 文件 |
|------|------|
| 1 | `server/scripts/demo-data.ts`、`server/scripts/backfill-artwork-dimensions.js` |
| 2 | `web/src/components/artist/ArtStyleManager.vue`、`web/src/views/artist/TierManage.vue` |
| 3 | `web/src/views/artist/`：OrderDetail、QueueBoard、Settings、ManualOrder、Preferences |
| 3 | `web/src/components/artist/`：AddonManager、MultiplierManager、StageListView、dashboard/QuickActions、dashboard/GreetingHero |
| 3 | `web/src/views/admin/HealthCheck.vue` |

## Commits（4 个）

| hash | 内容 |
|------|------|
| `c59115e` | fix(backend): 演示作品补 width/height |
| `2f3c0cc` | fix(artist): 封面/例图上传即时保存 |
| `5d64819` | fix(artist): 画师/管理后台 emoji 清理 |
| `000929f` | fix(artist): 仪表盘图标 TrendCharts→Odometer 统一描边 |

## 验证结果

| 项 | 结果 |
|----|------|
| server typecheck（tsc --noEmit） | ✅ 0 错误 |
| server 测试 | ✅ 666/666（39 文件） |
| web eslint | ✅ 0 错误 0 警告 |
| web build | ✅ 通过（5.3s） |
| web 测试 | ✅ 118/118（6 文件） |
| 任务1 API | ✅ 容器内 GET /api/artists/alice|bob|carol，16 张作品全部带 width/height，missing: 0 |
| 任务1 前端 | ✅ 浏览器 DOM：`aspect-ratio: 500/728` 等占位与 API 值一致，el-image lazy 正常加载 |
| 任务2 端到端 | ✅ dev(5173)+容器后端：编辑默认画风→注入测试图→**不点确定**→DB 已写入新封面→刷新仍在→测试数据已清理（封面恢复 alice-p01.jpg、测试图已删） |
| 任务3 视觉 | ✅ 仪表盘 6 个快捷按钮 SVG 渲染正常（无空白/豆腐块），颜色大小协调 |

## 风险提示

- 容器内演示数据已被我重跑脚本刷新（幂等，结果一致）；用户 22:14 上传的 3 张孤儿图片（q5PLdYJyI_hZ.jpg 等）仍在磁盘但无 DB 引用，下次 gcUploads 会清理，未手动删（不碰非授权数据）
- 任务 2 改变了保存时序（上传即 PUT），若画师编辑画风时上传了封面但随后点「取消」关弹窗，封面**不会回滚**——与 R48 头像行为一致，属预期；如需"取消回滚"是另一个设计决策，请一号拍板
