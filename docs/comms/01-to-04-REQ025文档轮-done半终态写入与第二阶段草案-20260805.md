# 派工：四号 REQ-025 文档轮（低风险）

> 一号派工 · 2026-08-05 · 风险等级：低（纯需求文档，零代码）
> 工作目录：`D:\Hermes Agent CN Desktop\workspace\artist-commission-req025`（worktree，分支 `v037-req025-spec`，已建好）
> **开工第一步：`git merge master` 再读本文件 + `docs/requirements/REQ-025-动态节点计价模型.md`**

## 任务 1：done 半终态规则写入 REQ-025 文档

用户 2026-08-05 拍板，写入 REQ-025 作为正式规则（建议新增 R13 或独立小节「done 半终态」）：

- **done = 半终态，双向留痕**：
  - ✅ 允许：加附加项（加钱）、负条目（减钱/退）、收款、负数收款
  - ❌ 禁止：无痕改总价（updateFinalPrice 对 done 关闭）、绕过条目直接删改价格结构、工作流回退
- **钱的去向引擎自动判定**：客户未付全 → delta 冲抵未付节点（R5/R9）；已付全 → 进 `extra_charge_after_close` / `extra_refund_after_close`（R10）
- **delivered 保持完全终态**
- 用户场景原文（写进规则出典）：① 画完后客户想要多版本光影，画师额外收钱——退回修改太浪费操作；② 客户原本十张突然只要一张，画师想退一点
- **同步修订边界案例**：补一个 done 状态的加减价案例（可用案例 1 的订单走到 done 后继续：加 50 进额外应收、减 200 进额外应退）
- **同步修订第五节实施映射**：注明 `addExtraItem` 的 schema 现状 `priceCents minimum: 0` 是第二阶段要放开的点（负条目入口），`updateFinalPrice` 需补 done/delivered/cancelled 后端守卫（现状零校验）

## 任务 2：REQ-025 第二阶段需求草案（供一号终审，不直接实施）

在 REQ-025 文档末尾新增「第二阶段实施方案（草案）」章节，覆盖：

1. **端点接入清单**：逐个列出现有需接引擎的入口——createOrder（base 条目 + 分期生成去重，现状两处内联 INSERT 分期，行号自查）、updateFinalPrice（转为 manual_adjust 条目 + done/delivered/cancelled 守卫）、addExtraItem（extra_item 条目 + schema 放开负数）、deleteExtraItem（冲正条目）、addPayment（收款填充推导）、removePayment/撤销（镜像回冲）、promoteOrder/generateInstallmentsForOrder（分期生成统一走引擎）、工作流推进（完成即锁触发点）
2. **切流方案**：R12 不留双轨——演示数据随版本重建的具体步骤（demo-data 脚本改造点）
3. **前端交互稿**（文字描述即可，不用画图）：PaymentBar 新展示要素（已锁节点锁定价 / 当前待收 / 超付抵扣标签 / 额外应收 / 额外应退）；done 订单的加减价操作入口布局；错误提示文案（守恒断言失败、终态拦截）
4. **测试基准**：验收标准 1~12 条与第二阶段的映射（第一阶段已覆盖的纯函数部分标注）

**边界**：草案只写「做什么」，不写具体代码实现；三号引擎第一阶段的函数签名尚未交付，草案中引擎接口描述用占位符，待引擎合入后一号终审时补实。

## 授权文件

- `docs/requirements/REQ-025-动态节点计价模型.md`（唯一）

其他文件一律不碰。完成后 commit（`docs(req): ...`）+ 交付报告写本 worktree 的 docs/comms/，转交一号。

## 验收

1. done 半终态规则完整写入，用户场景原文保留
2. 第二阶段草案覆盖上述四节，端点清单与代码现状一致（行号自查实锤，不凭印象）
3. 交付报告列明：新增章节清单 + 端点清单的行号依据
