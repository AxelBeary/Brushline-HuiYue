# 一号 → 五号：文档同步批（TOTP 后漂移修复）

> 日期：2026-08-05 · 分支：`docs-sync-totp` · worktree：`../artist-commission-docs` · 风险：低
> **授权范围：仅 `docs/` 目录**。代码文件一律不碰（发现代码问题写进交付报告，不动手）。

## 背景

REQ-027 TOTP 上线后旧登录码机制（`login_codes` 表、send-code 接口、`_dev_code`）已整体移除，但以下文档仍在描述旧机制，产生漂移。逐条修复，**每条修复前先 grep 代码确认现状**（不许凭文档自洽改文档）。

## 漂移清单（已预查，行号可能随版本漂移）

### 1. docs/维护说明书.md
- L378 附近「认证接口有速率限制（send-code 5次/分钟，verify 5次/分钟）」——send-code 已删除；verify 实际限流以 `server/src/features/auth/auth.routes.ts` 的 `guardRateLimit` 调用为准（grep 确认次数/窗口后改）。TOTP 登录限流描述按代码现状写。

### 2. docs/开发自参考.md
- L80 附近目录树「auth.routes.js # send-code / verify / me」——文件已是 .ts，接口只剩 verify / me / logout（grep auth.routes.ts 确认后改）
- L403 附近接口表「POST /api/auth/send-code 获取登录码」——删行；按 auth.routes.ts 现状补 TOTP 相关接口行（verify/me/logout + 管理端 totp bind/reset 段，grep admin.routes.ts 确认）
- L587 附近 TODO「登录码通过 Bot 发送（删除 _dev_code）」——该项已过时（QQ Bot 推迟为 REQ-028 备案，旧码已删），删除或改写为指向 REQ-028

### 3. docs/开发→生产切换指南.md
- L62 已由三号更新（_dev_secret 新语义），不动
- L136-140 附近「验证 _dev_code 已关闭」整节（curl send-code）——send-code 不存在，该验证方法作废；改写为「验证 AUTH_DEV_MODE=false：bind-init 响应不含 _dev_secret」（grep admin.routes.ts bind-init 确认字段名）
- L177 附近对照表「登录码 | AUTH_DEV_MODE=true 时返回 _dev_code | …」行——按 TOTP 新语义改写（对照表里 TOTP 绑定明文辅助的开关效果）
- L203 附近「切回后 _dev_code 恢复」——改 _dev_secret 语义
- L213 附近「curl -X POST .../send-code 查看 _dev_code」——删除或改为新链路

### 4. docs/待修复问题清单.md
- L17 附近「send-code 防枚举」条目——send-code 已删，按代码现状核实该项是否已消解（TOTP 的防枚举在 verifyTotpLogin 统一响应，grep auth.service.ts 确认），已消解则标记/移除，措辞与清单现有体例一致

## 验证

- 改完 grep 复查：`send-code|_dev_code|login_codes` 在 docs/ 下应只剩历史 changelog 与 archive（changelog.md 是历史账本不改；archive/ 不动）
- 交付报告 docs/comms/05-to-01-文档同步批-交付-20260805.md，逐条列「改前→改后→代码依据（文件:行）」
- commit 单行 `docs: TOTP上线后文档漂移修复——…`，禁止 add -A，**不推送不合并**
