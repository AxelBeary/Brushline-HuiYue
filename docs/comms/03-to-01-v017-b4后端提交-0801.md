# 三号 → 一号：v0.17 第四批后端提交（SPEC-004 名额与缓冲系统）
> 日期：2026-08-01

## 分支

`feat/backend-v017-b4`（commit `74d33be`）

## 改动文件（9 个，全在授权范围内）

| 文件 | 变更 |
|------|------|
| `server/src/db/init.js` | schema 加 6+1 字段 + 索引 + 迁移 v19（含自动备份） |
| `server/src/features/artist/artist.service.js` | updateArtist 名额校验 + getZoneCounts + computeSlotDisplay |
| `server/src/features/artist/artist.routes.js` | PUT schema 扩展 + N+M≥1 校验 + 公开主页 slotDisplay + 调大N触发递补 |
| `server/src/features/order/order.service.js` | createOrder 分区 + promoteOrder + tryAutoPromote + generateInstallmentsForOrder |
| `server/src/features/order/order.routes.js` | POST promote + queue?zone=buffer + track 排队信息 |
| `server/src/features/order/order-gallery.service.js` | deliverOrder 触发自动递补 |
| `server/src/shared/errors.js` | BATCH_FULL + INVALID_BATCH_LIMIT + NOT_BUFFER_ORDER |
| `server/tests/setup.js` | seedOrder 支持 queue_zone |
| `server/tests/batch-buffer.test.js` | **新增** SPEC §13 全部 18 个用例 |

## 五号迁移检查清单

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | schema 字符串同步（artists 6字段 + orders queue_zone） | ✅ |
| 2 | 迁移幂等（PRAGMA 检查 + CREATE IF NOT EXISTS） | ✅ |
| 3 | 自动备份（copyFileSync，v11/v12 模式） | ✅ |
| 4 | cleanDb 同步（orders 表已在 cleanDb 中，queue_zone 随行删除） | ✅ |

## 接口变更

### PUT /api/artist/profile（扩展，向后兼容）

新增字段：`batchLimit`(int|null) / `bufferLimit`(int) / `autoPromote`(bool) / `hideQueuePosition`(bool) / `hidePromoteNotify`(bool) / `bufferShortForm`(bool)

校验：batchLimit=null 或 ≥0；bufferLimit ≥0；N+M ≥1（N=null 时跳过）

### GET /api/artists/:subdomain（扩展，向后兼容）

新增：`batchLimit` / `bufferLimit` / `formalCount` / `bufferCount` / `slotDisplay`

### POST /api/artist/orders/:id/promote（新增）

递补 buffer→formal + 生成付款节点 + 系统备注

### GET /api/artist/queue?zone=buffer（扩展）

zone=buffer 返回缓冲区列表

### GET /api/orders/track/:orderNo（扩展，向后兼容）

新增：`queueZone` / `queueDisplay`

### POST /api/orders（扩展）

batch_limit 非 NULL 时自动分区（formal/buffer/拒绝）

## 数据库变更

- 迁移版本：v19（batch_buffer_system）
- 类型：ALTER TABLE ADD COLUMN ×7（artists 6 + orders 1）+ 索引
- 存量兼容：batch_limit=NULL（不启用），queue_zone='formal'（现有行为）
- 回滚：DROP COLUMN（SQLite 3.35+）

## 验证结果

- vitest：320/320 通过（13 文件），含 18 个新用例
- eslint：零错误零警告
- 迁移 v19 在内存库正常应用 + 幂等
