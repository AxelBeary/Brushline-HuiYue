# 02-派工-巡检B-画师后台-20260807

> 派工：一号 → 二号B（client-frontend，全页面巡检第 B 路）
> 工作方式：**只读巡检 + 浏览器实测**，零代码改动、不建 worktree、不 git 写操作。输出报告落主仓 `docs/comms/`。
> 目的：用户要求「全页面巡检，找布局/美观/操作逻辑的问题」。本路负责**画师后台**。

## 一、巡检范围（本路专属）

画师后台（登录后）全部页面：
1. **Dashboard 仪表盘**（views/artist/Dashboard.vue + components/artist/dashboard/*）
2. **QueueBoard 排期看板**（views/artist/QueueBoard.vue——⚠️ 五号正在拆分中，**只看 master 当前版**，不评价其行数/结构）
3. **OrderDetail 订单详情**（views/artist/OrderDetail.vue + components/artist/order/*——PaymentPanel/GalleryPanel 刚拆分）
4. **ManualOrder 手动录单**（views/artist/ManualOrder.vue）
5. **OrderList 订单列表**（views/artist/OrderList.vue）
6. **其余**：ArtworkManage 图库 / SlotManage 名额 / TierManage 档位 / GuestbookManage 留言 / Preferences 偏好 / Settings 设置 / ArtistLayout 布局框架

## 二、检查点（逐页过）

1. **布局**：错位/重叠/溢出/空白异常/窄屏（375px）/宽屏（1440px）；纸墨双主题（宣纸/墨黑）都看
2. **美观**：对照拍板设计系统（纸墨颜料盘 7 色语义：花青=进行中/朱砂=逾期/石绿=完成/藤黄=待确认/赭石=客户；霞鹜文楷标题/思源黑体正文）；色板一致性；间距/字号/圆角统一
3. **操作逻辑**：按钮可点性/禁用态/加载态/空态/错误态；表单校验反馈；跳转链路；数据刷新（loadOrder 链路）；状态机交互（订单流转）
4. **响应式**：断点行为
5. **i18n**：中英键完整性

## 三、方法

1. **静态分析**（先做）：读代码找结构问题
2. **浏览器实测**（后做）：起本地 vite dev **端口 5175**（`cd web && $env:PORT=5175; npm run dev`），访问各页面**截图留证**
   - 需登录：可用开发态/AUTH_DEV_MODE 或直接构造会话（参考 api 层）；演示数据可用 seed 或手工造
   - 每发现一个问题：截图 + 文件:行号 + 描述
3. 产出报告，不修代码

## 四、输出

- 报告写 `docs/comms/02-to-01-巡检B-画师后台-交付-20260807.md`：
  - 🔴 真实问题（页面 + 截图路径 + 文件:行号 + 描述 + 建议修法）
  - 🟡 改进建议 / ✅ 健康项
  - 严重度标注：高/中/低
- 截图存 `workspace/temp/inspect-b/`（若实测）

## 五、纪律

- 只读：不 git add/commit/push/merge、不碰 master
- **不夸大不编造**：截图为准；判断以用户拍板过的设计规范为准
- 五号正在拆分 QueueBoard，**你的任务是找问题不是改代码**，发现的问题写进报告即可
- 20 分钟无产出停下报卡点
