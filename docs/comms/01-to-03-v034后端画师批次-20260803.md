# 派工：v0.34 后端+画师后台批次（三号）

> 分支：`fix/v034-demo-dims-cover-ux`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-03`
> 风险等级：低（脚本+画师后台组件，不碰订单/支付/权限）
> 先做任务 1（用户正在等体验），2、3 随后。

## 任务 1：演示作品缺 width/height → 客户端画廊「顶位置」复发（用户已两次反馈）

**一号已核实的代码现状**：
- API 实测：`GET /api/artists/alice` 返回 6 个 artworks **全部 width:null height:null**（carol 同理）
- `web/src/components/templates/TplGallery.vue` L57-59：`ratioStyle()` 只在有 width/height 时生成 aspect-ratio 占位，缺失时图片加载完才撑开高度 → 布局跳动/顶位置（#15/#50 当年修的就是这个，demo 数据绕过了）
- 根因：`server/scripts/demo-data.ts` L48-55 `insertArtworks` 的 INSERT **没写 width/height 列**
- 已有工具：`server/scripts/backfill-artwork-dimensions.js`（用 sharp 读尺寸回填，但它硬编码 `journal_mode = WAL`，**容器内是 DELETE 模式，别直接在容器里跑这个脚本**）；sharp 已在 server/package.json dependencies

**要求**：
1. 改 `demo-data.ts`：insertArtworks 用 sharp 读每张种子图的实际尺寸写入 width/height
2. 对已存在的演示行做一次性回填（在 demo-data.ts 里加回填段或写个容器内小脚本，复用运行中 DB 连接的老路子：`docker cp` + `docker exec commission-web npx tsx`）
3. 容器内验证：`GET /api/artists/alice` 的 artworks 全部带 width/height
4. 顺手修 `backfill-artwork-dimensions.js` 的 WAL 硬编码问题（改成探测当前模式或删掉那行），避免下次有人在容器里跑它踩坑

## 任务 2：画风封面上传 UX 陷阱（用户今晚刚踩，P0 体验）

**一号已查实的证据链**（不用再排查）：
- 用户 22:14 在画师后台上传了 3 张图（`uploads/images/2158/q5PLdYJyI_hZ.jpg`、`VhscD7-DW_CJ.jpg`、`C0vKS7wpD9V1.png`，磁盘文件在）
- 服务器日志：**没有任何 PUT /api/artist/art-styles 请求**——图片传上去了，「关联到画风」从未发生
- 数据库 art_styles.cover_image 仍是 demo-data 写死的 `alice-p01.jpg`
- 代码现状：`web/src/components/artist/ArtStyleManager.vue` L209-219 `uploadCover` 只把路径写进表单 `styleForm.cover_image`，**必须再点弹窗「确定」（L221 saveStyle）才发 PUT**。上传成功无任何保存动作也无强提示 → 用户以为传完就生效了

**要求**：
1. 编辑已有画风时：上传成功**立即保存** cover_image（直接调 `artistApi.updateArtStyle(styleId, { cover_image })`），与 R48 头像上传「即时保存」模式一致；成功后 ElMessage 提示
2. 新建画风时（还没有 id，无法即时保存）：上传成功后给出醒目文案提示「确定后生效」
3. **审计同模式陷阱**：档位示例图（TierManage.vue）、头像（Settings.vue）、其他 el-upload + 弹窗表单组合，凡「上传了但要点确定才关联」的，按同一模式修或在交付 comms 说明为何保留

## 任务 3：画师/管理后台 emoji 清理（用户拍板：删所有 emoji，SVG 无所谓）

**分工边界**：locales 两个文件归二号，你不要碰。你负责：
- `web/src/views/artist/**.vue`（OrderDetail 26 处、Settings 12、ManualOrder 7、ArtworkManage 5、QueueBoard 4、MultiplierManager 4、StageListView 2 等）
- `web/src/views/admin/**.vue`（HealthCheck 4、ArtistManage、ArtistDetailDrawer 2）
- `web/src/components/artist/**.vue`（QuickActions 9、AddonManager 8、GreetingHero 6、AddonTemplateManager 1 等）

**要求**：删后文案通顺；纯装饰 emoji 图标位连 span 一起删；状态语义（如 🔒=尾款锁定）若删了影响理解，用文字替代。测试文件里的 emoji 保留。

## 授权文件范围

- `server/scripts/demo-data.ts` / `backfill-artwork-dimensions.js`
- `web/src/components/artist/**.vue`、`web/src/views/artist/**.vue`、`web/src/views/admin/**.vue`（仅 emoji 与任务 2 相关改动）
- 容器内操作：docker cp / docker exec（只碰演示数据，不碰生产逻辑）

## 验证标准

1. 任务 1：API 返回 artworks 带尺寸；客户端画廊加载不跳动（可用浏览器看一眼 localhost:3000/artist/alice）
2. 任务 2：编辑画风上传封面 → 不点确定 → 刷新页面封面仍在；`cd web && npx eslint .` + `npm run build` 通过
3. 任务 3：`npx eslint` 无新增错误，build 通过

## 交付

comms：`docs/comms/03-to-01-v034后端画师批次-{日期}.md`。完成后申请审核。
