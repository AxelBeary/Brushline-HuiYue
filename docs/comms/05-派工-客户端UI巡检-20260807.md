# 05-派工-客户端UI巡检-20260807.md

> 派工：一号 → 五号（bugfix）
> worktree：`../artist-commission-w5`（分支 `beta/ui-audit-client`）
> 背景：用户拍板「派海量的人去大面积测网页丑陋/交互问题」——本批=**客户端全页面巡检**（只读，出问题清单）
> 基线：master `9974be5`。**开工第一步 `git merge master`**。
> 方法：隔离环境（参考 artist-commission-local-dev-env skill）+ Playwright 逐页截图 + DOM 量化检查 + **curl VL 看图**（脚本：`D:\Hermes Agent CN Desktop\data\hermes-home\skills\software-development\huiyue-visual-verification\scripts\vl-curl.mjs`）

## 任务：客户端全页面视觉/交互巡检

**巡检范围**（公开访客态 + 下单流程）：
1. Landing（画师列表）
2. 画师主页 4 模板（Classic / Gallery / Folio / Atelier）——桌面 + 移动
3. OrderForm（三步骤：选档位/画风模式、写需求、联系方式——提交到小票）
4. TrackOrder（查询表单/我的订单/时间线/付款进度）
5. DeliveryPage（交付下载）
6. NotFound 404
7. 客户端共享组件（ThemePicker 悬浮、留言板、点赞）

**每页检查项**：
- 布局：横向溢出（`scrollWidth > innerWidth`）、元素重叠、大段空白、移动端适配（375px）
- 视觉：错位、颜色不协调（截图 + curl VL 描述）
- 交互：按钮反馈、步骤流转、弹窗、加载态
- 控制台：JS 错误、请求失败
- **重点**：OrderForm 三步完整流程点击链路（画风模式 + 旧模型都要跑）

**交付**：
- 截图存 `workspace/temp/ui-audit-client/`（每页桌面 + 移动）
- 问题清单 `docs/comms/05-to-01-客户端UI巡检-交付-20260807.md`：按严重度分级（🔴 明显错位/交互失效 / 🟡 观感差 / 🟢 建议），每条附页面 + 截图 + 描述 + 初步定位
- 只读巡检：**零代码改动**，commit 报告到分支

## 红线

- 只读：不改任何源码，不建数据（用现有 seed 或生产 https://localhost）
- 报告「不夸大不编造」：每条注明「截图确认 / DOM 量化 / 静态分析」

## 门禁

- 报告落盘 Test-Path 确认存在
- 截图数量 ≥ 页面数
