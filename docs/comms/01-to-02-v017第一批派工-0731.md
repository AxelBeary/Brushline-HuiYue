# 一号 → 二号：v0.17 第一批派工（R24 + R25 + BUG-5 看板闪烁）
> 日期：2026-07-31（更新：加入 BUG-5）

## 预读确认

你的预读结论全部正确：R26 ✅ / R31 ✅ / BUG-1/2/4 ✅，第一批只剩 R24 + R25。

## 新增：BUG-5 排期看板焦点图闪烁（五号研判，低风险）

根因：一张图 error → 全量刷新所有签名 URL → 所有图 src 变化 → 循环闪烁。v0.16 防抖只治标。

**修复要求（方案 A，~30 行）**：
1. `refreshNow(path)` 接受出错图片的 path 参数
2. `collect` 只收集传入的 path（Set 去重后批量）
3. `errorRetries` 改为 `Map<path, count>`，按图独立计数
4. `apply` 只更新出错图片的 URL，不动其他图
5. `QueueBoard.vue` 改为 `@error="() => refreshNow(element.focus_image_path)"`

## R25 授权裁定

**在授权范围内，可以做。** R25 只改 4 个模板文件里 ThemePicker 的挂载位置（DOM 移动 + CSS），不动 ThemePicker.vue 组件内部逻辑。

**红线**：`web/src/components/ThemePicker.vue` 不在授权列表内，不许碰。

## 分支与 worktree

- 分支：`feat/client-frontend-v017-b1`（从 master 最新切出）
- worktree：独立建，禁止在主 worktree 操作

## 授权文件列表（只许改这些）

| # | 文件 | 用途 |
|---|------|------|
| 1 | `web/src/views/client/OrderForm.vue` | R24：校验弹窗 + 滚动定位 |
| 2 | `web/src/composables/useOrderForm.js` | R24：校验逻辑调整（如需要） |
| 3 | `web/src/locales/zh-CN.js` | R24：弹窗文案 i18n 键 |
| 4 | `web/src/locales/en.js` | R24：弹窗文案 i18n 键 |
| 5 | `web/src/views/client/templates/ArtistHomeClassic.vue` | R25：ThemePicker 移至右下角 |
| 6 | `web/src/views/client/templates/ArtistHomeGallery.vue` | R25：ThemePicker 移至右下角 |
| 7 | `web/src/views/client/templates/ArtistHomeFolio.vue` | R25：ThemePicker 移至右下角 |
| 8 | `web/src/views/client/templates/ArtistHomeAtelier.vue` | R25：ThemePicker 移至右下角 |
| 9 | `web/src/composables/useSignatureRefresh.js` | BUG-5：按图刷新 + 按图计数 |
| 10 | `web/src/views/artist/QueueBoard.vue` | BUG-5：@error 传递出错图片 path |

## R24 校验弹窗——要求

1. 校验失败时弹 `ElMessageBox`，列出所有未通过项（不是一条一条弹）
2. 弹窗关闭后自动滚动到第一个未通过字段
3. 须知（disclaimer）未勾选时也要列出
4. i18n 中英同步，键名走 `order.validation.*` 命名空间
5. 不引入新依赖

## R25 ThemePicker 位置——要求

1. 4 个模板统一：ThemePicker 从 CTA 按钮下方移到**右下角固定悬浮**（用户拍板 C37）
2. `position: fixed; right: 16px; bottom: 16px; z-index` 合理（不遮挡弹窗）
3. 4 模板视觉一致，响应式下不溢出屏幕
4. 不改 ThemePicker.vue 组件本身

## 验证标准

- `cd web && npx eslint . && npm run build` 零错误零警告
- 4 个模板页面 ThemePicker 均在右下角
- OrderForm 校验失败弹窗正常、滚动定位正常
- QueueBoard 首次进入：单图 error 只刷新该图，其他图不动，无循环闪烁
- 提交前 `git diff --stat` 确认只有授权文件

## 交付

完成后写 `docs/comms/02-to-01-v017-b1提交-0731.md`，含分支名 + 改动文件 + 验证结果。
