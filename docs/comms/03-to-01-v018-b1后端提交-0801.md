# 三号 → 一号：v0.18 第一批后端提交（节点话术）
> 日期：2026-08-01

## 做了什么

plan-node-speech 后端全部完成（A/B/C/D 四项）。

## 分支

`feat/backend-artist-v018-b1`（worktree: `../artist-commission-03`）

## 改了哪些文件

| 文件 | 变更 |
|------|------|
| `server/src/db/init.js` | 迁移 v20（speech_template 字段，幂等+备份+存量回填）；管理员自举种子显式传话术 |
| `server/src/features/artist/workflow.service.js` | toCamel/listCamel 返回 speechTemplate；updateStage 接受 speechTemplate；addStage/seedArtistStages/resetArtistStages 显式传默认话术 |
| `server/src/features/artist/artist.routes.js` | PUT workflow/:id schema 加 speechTemplate（string\|null, maxLength 500） |
| `server/src/features/order/order-workflow.service.js` | 新增 replaceSpeechVars（9变量替换）+ getSpeechInfo（话术+客户沟通数据） |
| `server/src/features/order/order.routes.js` | GET /api/artist/orders/:id 返回 speechText/clientQq/totalPriceCents/paidCents/unpaidCents |
| `server/tests/speech.test.js` | 新增 15 用例（TC-SP-01~15） |

## 接口变更

| 接口 | 变更 | 兼容性 |
|------|------|--------|
| PUT /api/artist/workflow/:id | body 新增可选 speechTemplate | 向后兼容（不传不改） |
| GET /api/artist/workflow | 响应新增 speechTemplate 字段 | 向后兼容（只增） |
| GET /api/artist/orders/:id | 响应新增 speechText/clientQq/totalPriceCents/paidCents/unpaidCents | 向后兼容（只增） |

## 数据库变更

- 版本：v20（stage_speech_template）
- 类型：ALTER TABLE ADD COLUMN（带 DEFAULT）+ UPDATE 存量回填
- 回滚：`ALTER TABLE artist_workflow_stages DROP COLUMN speech_template`（SQLite 3.35+）
- 影响现有数据：存量节点回填默认话术 `{客户名}，你的订单已{节点名}。`

## 验证结果

- 335/335 测试全绿（14 文件，含新增 speech.test.js 15 用例）
- ESLint 零错误零警告
- 迁移 v20 幂等验证通过（测试环境每次全量跑 v1~v20）

## 变量替换规则（9 个）

| 变量 | 来源 | 空值处理 |
|------|------|----------|
| {客户名} | orders.client_name | 空串 |
| {客户QQ} | orders.client_qq | 空串 |
| {订单号} | orders.order_no | 空串 |
| {档位名} | price_tiers.name（JOIN） | 空串 |
| {节点名} | 当前节点 name | 空串 |
| {截稿日} | orders.deadline → X月X日 | 空串 |
| {总价} | final_price_cents/100 → ¥X | 空串 |
| {已付} | paid 分期合计/100 → ¥X | 空串 |
| {待付} | (总价-已付)/100 → ¥X | 空串 |
