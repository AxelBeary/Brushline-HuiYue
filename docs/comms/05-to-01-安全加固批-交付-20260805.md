# 05→01：安全加固批交付报告

> 日期：2026-08-05 · 执行：五号 · 分支：`security-hardening`（基于 master `3d4286c`）
> 状态：4 任务全完成（5 commits），未推送未合并，请一号审核

## 交付摘要

| # | 任务 | commit | 验证 |
|---|------|--------|------|
| 1 | F1 TOTP 密钥 DTO 投影（补全版） | `0e990de` + `0dd27d7`（tsc 类型修复） | 4 端点 + 4 例回归测试 |
| 2 | F4 AUTH_DEV_MODE 生产 fail-fast | `bcb936a` | 3 例测试 |
| 3 | F3 去 unsafe-eval + F9 补安全头 | `fcebf79` | 隔离实例 7 页实测零 violation |
| 4 | F6 容器非 root + entrypoint 修复 | `cec1c1c` | docker build + 容器实测 |

**基线**：server 897 → **904**（+7 例）全绿 · eslint 0 · tsc 0 · web build 过（F3 实测时构建）

## 任务明细

### 1. F1 TOTP 密钥不泄露（本批核心，中风险）
- 新增 `server/src/shared/dto.ts`：`publicArtistDTO` 剔除 `totp_secret/totp_failed_attempts/totp_locked_until/token_version/deleted_at/weibo_url/bilibili_url/platform_urls`
- 应用到 **4 端点**（对方 patch 只覆盖 3 处，补上第 4 处）：
  - `GET /api/auth/me`（auth.routes.ts）
  - `GET /api/admin/artists`（admin.routes.ts，双重防御：getAllArtists 已显式列 + DTO）
  - `GET /api/admin/artists/:id/profile`（admin.routes.ts）
  - `GET /api/artist/profile`（artist.routes.ts——**对方 patch 漏修**）
- `getAllArtists` 改显式列：剔除密钥列，**保留 `totp_verified`**（管理后台 ArtistManage.vue 据此显示「绑定/重绑」按钮，搜证）**+ `quick_actions`**（Preferences.vue/QuickActions.vue 消费）
- `getArtistById` 保留完整行（认证中间件需 token_version）——内部使用不出网
- 回归测试 `security-dto.test.js` 4 例：4 端点响应断言不含敏感列 + quick_actions/totp_verified 不缺失

### 2. F4 AUTH_DEV_MODE 生产 fail-fast
- `auth.service.ts` 模块级守卫：`AUTH_DEV_MODE=true && NODE_ENV=production` → 启动即抛错（误配会泄露 TOTP 密钥明文）
- 参照三号 ADMIN_QQ fail-fast 同模式（P1-4 先例）
- 测试 `security-failfast.test.js` 3 例（vi.resetModules 破缓存重载）：production+dev 抛错 / production+false 正常 / development+true 正常

### 3. F3 CSP 去 unsafe-eval + F9 安全头
- `app.js` CSP `script-src 'self'`（移除 unsafe-eval）
- **实测**（隔离实例 + Playwright）：7 个主要页面（登录/公开主页/下单/查询/管理后台/画师后台/订单列表）CSP violations **0**、console errors **0**——Vue 3 生产构建无 eval 依赖，删除安全
- `app.js` onRequest 补 `Cross-Origin-Opener-Policy: same-origin` / `Cross-Origin-Resource-Policy: same-origin` / `X-Permitted-Cross-Domain-Policies: none`（图片/API 均同源，CORP 无副作用）

### 4. F6 容器非 root
- `Dockerfile`：`RUN mkdir -p /app/data /app/uploads && chown -R node:node ...` + `USER node`
- **容器实测**（docker build + run 挂临时卷）：
  - `id` → uid=1000(node) ✓ 非 root
  - /app/data /app/uploads 属主 node 且**可写**（touch 测试通过）✓
  - 应用启动正常：迁移 v41/v42 + 管理员自举 + listening + 容器内 health OK ✓
- **发现并修复关联问题**：`entrypoint.sh` 从 `/app` 起跑 `npx tsx`，npx 找不到 `/app/server/node_modules/.bin/tsx` 会**在线下载**（无网络/慢环境卡启动）——改为 `cd /app/server` 再 npx
- 注：外部 3101 访问超时为 Windows Docker Desktop 端口转发环境问题（容器内 self-test 通过证明改动无破坏）

## 修改文件清单

| 文件 | 修改类型 |
|------|---------|
| server/src/shared/dto.ts | 新增 |
| server/src/features/auth/auth.routes.ts | 1 端点 |
| server/src/features/admin/admin.routes.ts | 2 端点 |
| server/src/features/artist/artist.routes.ts | 1 端点 |
| server/src/features/artist/artist.service.ts | getAllArtists 显式列 |
| server/src/features/auth/auth.service.ts | F4 fail-fast |
| server/src/app.js | F3 + F9 |
| Dockerfile | F6 |
| entrypoint.sh | F6 关联修复 |
| server/tests/security-dto.test.js | 新增 4 例 |
| server/tests/security-failfast.test.js | 新增 3 例 |

## 验证结果

- [x] server 904/904（897 + 7 新增）全绿 · eslint 0 · tsc 0
- [x] F1 4 端点响应实测（security-dto 4 例）
- [x] F4 三分支实测（security-failfast 3 例）
- [x] F3 隔离实例 7 页零 CSP violation 零 console 错误
- [x] F6 docker build + 容器实测（uid/uploads/data/health 全过）
- [x] git status 干净（临时 playwright/测试库/容器/镜像/脚本全清理）

## 备注

1. F14（lookup 返回 adminQq）按派工未动——产品决策项，待用户拍板
2. F2/F5/F7/F8/F10/F13 未入本批（P1/P2 排期）
3. entrypoint.sh 修复是 F6 验证中发现的环境关联缺陷（此前 root 用户 + 有网络时 npx 在线装 tsx 能跑通，改 USER node 后暴露）——建议合入后观察生产容器启动日志确认
