# 04→01：A 测执行手册交付

> 日期：2026-08-05 · 交付：四号 · 接收：一号
> worktree：`../artist-commission-v038acc` · 分支：`atest-docs`（基于 master `a8ef3e5`）

## 交付物

`docs/A测执行手册.md`（单文件，纯文档，零代码零配置改动）

## 内容结构

1. **测试剧本**：9 个场景 40+ 步骤，真实画师 0→1 全链路——建号+TOTP 绑定+登录 → 资料设置 → 开稿/价格 → 接单（自助+手动）→ 工作流推进 → 收款（REQ-025 动态计价重点）→ 交付 → 客户侧查单/留言 → 视觉反馈。每步「操作→预期→记录位」。
2. **AUTH_DEV_MODE 关闭检查单**：前置 3 项（管理员已绑定 ✅ 2026-08-05 实跑 / 测试画师先绑好 / 登录链路通）+ 关闭步骤 4 步（含验证 bind-init 无 `_dev_secret` 的 DevTools 操作）+ 回滚路径。STATUS 规则 9 已落成可执行步骤。
3. **反馈收集模板**：画师反馈单四段格式（操作场景/发生了什么/期望/严重度）+ 归档流程（🔴🟡→五号排查→Bug 进待修复清单/缺失进 REQ；🟢💡→四号收集并对照 REQ-031）。
4. **已知限制清单**：11 条（L1~L11），逐条标来源（git log / STATUS / REQ / 代码行号），测前告知画师防误报。

## 实锤纪律

- 所有现状断言均读代码核实：TOTP 登录（`auth.routes.ts` + `auth.service.ts`，5 次锁 15 分钟）、管理员建号（`admin.routes.ts`）、TOTP 绑定弹窗（`ArtistManage.vue`）、外链旧版编辑器（`Settings.vue` R15）、收款接口（`order.routes.ts` payments，正数/负数）、done 半终态（REQ-025 R13 + STATUS 规则 1）、手动录单新画风模式（REQ-029，`styleSizeId`/`styleAddons`）。
- **F1/F2 合入状态已核实**：F1 后端（publish-artwork API）已合入 master（`9851ad5`），但 F1 前端入口 + F2 全部在 `f1f2-frontend` 分支未入 master——手册中 7-4/7-5 步和 L1/L9 标注「若已合入」条件，未合入则跳过并注明「未测」。
- **画师使用说明书已发现过时**（v0.36 版：登录码机制、手动录单旧档位模型描述均已过时）——本手册按代码现状重写，不引用过时描述。该说明书更新建议转一号排期。

## 涉及角色

管理员（建号/绑 TOTP/关 AUTH_DEV_MODE）/ 画师（全流程）/ 客户（下单/查单/留言）/ 一号（A 测启动与验收）

## 验收标准

1. 当 A 测执行人打开手册时，应能按场景 0→9 顺序完成真实画师全流程测试，每步有操作、预期、记录位。
2. 当 A 测启动需要关 AUTH_DEV_MODE 时，应能按第二章检查单逐项核对并执行，含验证和回滚路径。
3. 当画师反馈问题时，应按第三章模板记录并归档到正确位置（Bug→待修复清单 / 需求→REQ 池）。
4. 当画师测前阅读已知限制时，应能了解 L1~L11 各项（含 F2 未合入、QQ 通知无推送、描述可空过等），不会误报。

## 优先级

P0（A 测启动前置）

## 待确认问题

无（派工范围内已全部落实；「画师使用说明书过时」作为建议转排期）

## 申请

请一号审核 A 测执行手册是否可交付 A 测使用。

—— 四号
