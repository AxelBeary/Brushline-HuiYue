# 补丁派工：v0.35 波 1 补漏 + 波 3 F6 后台 UI（三号）

> 日期：2026-08-04 · 分支：`feat/v035-style-unify-backend`（原分支继续）· Worktree：`artist-commission-wt-03`
> 波 1 已合入 master `3e2a1af`，审核通过（迁移 v37 实跑验证+测试全绿，质量好）

## 背景：为什么有补漏

派工文件发出后（00:08），一号在 master 上追加了任务 3 第 4/5/7/8/9 条（四号 UX 审计并入），但你 worktree 里的派工文件是发出时的旧版本（worktree 分支基于旧 master，未拉到更新）。你交付的波 1 本体全部合格，以下 5 条是遗漏的追加项，本轮补齐。

## 第一步：同步 master

`git merge master`（无冲突应直接过；locales 若有冲突保留双方——五号批次 A 补了 56 个 errors 键，与你动的 styleManage/tiers 命名空间不重叠）

## 任务 A：波 1 补漏（5 条，都在 ArtStyleManager/TierManage 域）

1. **删旧「增项」tab**（用户拍板退役）：TierManage.vue 删 el-tab-pane(name="addons") + AddonManager import；删 `web/src/components/artist/AddonManager.vue`（唯一消费端已核实）。旧增项后端 API 保留不删（归 v0.36）
2. **菜单文案**：`menu.tiers`「档位管理」→「价格管理」（zh-CN/en 同步；页面标题 tiers.title 一并改「价格管理」）
3. **画风/尺寸拖拽排序**（F2「默认画风=排序最前」强依赖）：画风卡片之间 + 尺寸行之间双层拖拽（vuedraggable，后端 sort_order 更新 API 已有）。⚠️ vuedraggable 陷阱：grid/flex class 放 `<draggable>` 自身不放外层容器（v0.26 教训）
4. **增项区补「＋导入增项」入口**：importAddons 勾选目前只在新建弹窗（L187 `v-if="!editingStyleId"`），已有画风无法追加导入库里新增项——与 SPEC-025 动线 5 矛盾。画风卡片增项区加入口（弹窗列增项库未导入项供勾选）
5. **增项保存统一即时保存**（v0.34 原则）：删「保存增项」按钮和尺寸覆盖行内保存按钮，勾选/改价/覆盖改即时 PUT（失败 ElMessage 提示回滚）——同封面/例图模式

## 任务 B：波 3 —— F6 后台 UI（作品管理档位标注 + 自由描述）

API 已在波 1 就绪（你自己写的）：
- `GET /api/artist/artworks` — 每作品带 `size_tag_ids: number[]`
- `PUT /api/artist/artworks/:id` — body `{ title?, description? }`
- `PUT /api/artist/artworks/:id/tags` — body `{ sizeIds: number[] }`（替换语义）

作品管理页（ArtworkManage 或现有作品列表组件，你确认文件名）加：
- 每作品「档位标注」多选（列该画师全部启用画风×尺寸，多画风显示「画风·尺寸」防歧义）——保存调 PUT tags
- 「自由描述」编辑（textarea，maxLength 2000）——保存调 PUT artworks/:id
- 交互模式沿用即时保存（改完 PUT + ElMessage 成功/失败）

## 验证标准

1. `cd server && npx tsc --noEmit` 0 错 + `npx vitest run` 全绿（703+）
2. `cd web && npx eslint . && npm run build` + `npx vitest run`（153+）
3. locales 只动 styleManage/tiers/menu/artworks 命名空间（二号在并行联调改 artistHome/orderForm/gallery）
4. 交付 comms：`docs/comms/03-to-01-v035补漏加波3-{日期}.md`

## 授权文件

- `web/src/views/artist/TierManage.vue`、`web/src/components/artist/ArtStyleManager.vue`、`web/src/components/artist/AddonManager.vue`（删）
- `web/src/views/artist/` 作品管理页（你确认文件名）+ `web/src/api/index.js`（如需补 updateArtwork/setArtworkTags 封装，仅限这几个方法）
- `web/src/locales/zh-CN.js`、`en.js`（限上述命名空间）
