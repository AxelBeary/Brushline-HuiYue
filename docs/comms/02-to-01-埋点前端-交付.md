# 02-to-01-埋点前端-交付

> **二号转交一号，文件：docs/comms/02-to-01-埋点前端-交付.md**
> 派工：2026-08-07 · 分支：beta/tracking-frontend · commit：9af9e2b（二号迭代预算用尽，一号补 commit）
> 状态：✅ 代码+门禁完成

## 一、改动摘要（12 文件，+216/-12）

| 文件 | 改动 |
|------|------|
| `web/src/utils/track.js` | 核心改造：匿名凭证（POST /api/anon-token，localStorage 缓存）+ 攒批（5 秒/10 条）+ sendBeacon（pagehide 带走剩余）+ 失败策略（网络错误回队 / 400 重取重试 / 其余丢弃）+ 白名单事件 |
| `web/src/views/client/OrderForm.vue` | 下单漏斗全挂接：start / step_view / step_leave / step_back / submit_attempt / submit_success / submit_fail / abandon（payload 带 pricing_model/total_steps） |
| `web/src/views/client/ArtistHome.vue` | 换色事件真实上报（theme_accent_change，替代 console.debug） |
| `web/src/components/ArtistLayout.vue` | 后台路由级 watch：进入各页 track dashboard_view/queue_view/orders_view/manual_view/artworks_view/settings_view/tiers_view/guestbook_view/preferences_view |
| `web/src/components/artist/dashboard/QuickActions.vue` | 快捷入口点击 track dashboard_quick_click（payload: action key） |
| `web/src/composables/useOrderWorkflow.js` | 工作流操作埋点 |
| `web/src/views/artist/ArtworkManage.vue` / `GuestbookManage.vue` / `ManualOrder.vue` / `OrderDetail.vue` / `Settings.vue` | 后台页面关键操作埋点 |
| 2 个测试文件 | OrderForm 测试适配（track.js mock） |

**事件名全部在白名单 23 项内**（后端 400 硬校验，越界会被拒）。

## 二、口径说明（二号核实）

施工图 §3.3 用 `dashboard_view` 系列、REQ-033 §4.4 验收提到 `artist_page_enter`——二号核实后端 tracking 模块白名单 23 项两者都在，**按施工图用 dashboard_view 系列**（派工文件为最终口径），artist_page_enter 保留白名单未发。

## 三、自测结果（一号独立复跑确认）

| 门禁 | 结果 |
|------|------|
| vitest（web） | **215/215 全绿**（13 文件） |
| build（web） | ✓ 5.70s |
| oxlint | 0 错误 0 警告 |

## 四、浏览器实测

未做浏览器实测（profile 会话无浏览器工具）。**待容器重建后联调**：打开客户端主页 → Network 可见 `/api/anon-token` + 切色后 `/api/events` 200；画师登录后台浏览 → events 表收到 dashboard_view 等。

## 五、卡点

1. profile 迭代预算（90 次）用尽，commit/报告由一号补。
2. 无其他卡点。

---
*二号转交一号，文件：docs/comms/02-to-01-埋点前端-交付.md*
