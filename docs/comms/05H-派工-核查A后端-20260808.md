# 05H-派工：全面核查「已做/漏做」（三路并行，只读盘点）

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：五号（bugfix）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w22`
> 分支：`beta/audit-coverage-1`
> 交付报告：`docs/comms/05H-to-01-核查A-后端需求覆盖-交付-20260808.md`
> 关联：另有 02E（前端需求覆盖，二号）、03D（STATUS/遗留对照，三号）两路并行

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w22'
git merge master --no-edit
```

## 任务：后端需求覆盖核查（只读）

**目标**：对照 `docs/requirements/` 全部 REQ 文档的功能点，核查 **server 后端**哪些已实现、哪些漏做、哪些部分实现。输出「已做 ✅ / 漏做 ❌ / 部分 ⚠️」对照表。

**核查范围**（REQ 文档里的后端相关功能点）：
- REQ-025 动态节点计价（引擎/守恒/locked）
- REQ-027 TOTP 登录
- REQ-029 手动录单接画风模式（后端部分）
- REQ-033 业务埋点（后端：事件落库/三态/统计接口）
- REQ-035 工具类目（后端：client_profiles/standalone_incomes/tools 路由）
- REQ-022 B 类低优先（后端相关）
- 各 REQ 里的**验收标准**逐条对照

**核查方法**：
1. 读每个 REQ 文档 → 提取后端功能点/验收标准清单
2. 对每个功能点：grep server/src 代码（路由/service/迁移/测试）→ 判定 ✅/❌/⚠️
3. 带证据：commit 或 文件:行号
4. ⚠️ 特别查：**需求写了但代码没做**（漏做）或**代码做了但文档没更新**（已做未记）

**输出格式**：
```
# 后端需求覆盖核查
## 汇总
| REQ | 功能点 | 后端实现 | 状态 |
## ✅ 已实现（带证据）
## ❌ 漏做（需求有但代码无）——最重点
## ⚠️ 部分实现（缺口说明）
## 已做未记录（代码有但文档/STATUS 没标）
```

## 铁律

- 只读：改代码 = 打回
- 每条带证据（commit/文件:行号）
- 拿不准标「需验证」不硬判
- 漏做项**先核实不是已合入但文档过时**（git log --grep 查）——08-08 多次教训：STATUS/REQ 状态行过时 ≠ 漏做

## 交付要求

1. commit 描述中文（如 `docs(audit): 后端需求覆盖核查`）
2. 交付报告写 `docs/comms/05H-to-01-核查A-后端需求覆盖-交付-20260808.md`
3. 结束前：`git status --short` 确认干净
4. **不推送、不合并、不改 STATUS**
