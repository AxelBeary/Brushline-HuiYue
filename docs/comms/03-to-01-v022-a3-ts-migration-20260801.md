# 三号 → 一号：A3 TS 迁移第二批完成（2026-08-01）

## 变更

分支 `feat/v022-backend`，commit `dc28b57`。

29 个文件 JS→TS（git mv + 类型注解），覆盖：
- `features/` 全部 routes（9 个）+ services（13 个）
- `utils/`（4 个）
- `shared/middleware/`（2 个）
- 新增 `types/fastify.d.ts`（FastifyRequest.artist 类型扩展）

## 验证

| 项 | 结果 |
|----|------|
| `npx tsc --noEmit` | ✅ 零错误（迁移前 383→0） |
| `npx vitest run` | ✅ 469/469（27 文件） |
| `npx eslint server/src/` | ✅ 零错误零警告 |

## 迁移策略

- 渐进：`strict: false`，`allowJs: true`，不改运行时行为
- db 查询用 `as Type` 断言（与 v0.21 pricing.service.ts 模式一致）
- 类型来源：`types/entities.ts`（已有）+ 内联接口（entities 未覆盖的）
- 剩余 JS：`app.js`、`index.js`、`db/`（connection/init/seed）——入口和迁移脚本，下批或不动

## 未迁移文件（有意保留）

| 文件 | 原因 |
|------|------|
| `app.js` / `index.js` | 入口文件，依赖 Fastify 插件注册链，改动风险高收益低 |
| `db/connection.js` | 3 行代码，无类型价值 |
| `db/init.js` | 1077 行迁移脚本，含大量动态 SQL，类型化收益极低 |
| `db/seed.js` | 开发工具，非生产代码 |
