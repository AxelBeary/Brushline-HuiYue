# 04-to-01 repowiki 修补交付：仓库内补丁 + 外部 13 处 🟡 汇总

- 派工：`docs/comms/04-派工-repowiki修补批-20260807.md`（一号 → 四号）
- 交付日期：2026-08-07
- 核对基准：master HEAD `745aebb`（含 v44/v45 迁移、TS 100%、QueueBoard 拆分）
- 执行人：四号（需求整理者；纯文档修补，零代码改动，未触碰外部原文 C:\Users\qly19\Desktop\repowiki\）
- 结论口径：已修补 / 无需补 / 🟡 清单汇总

---

## 一、仓库内修补（`docs/external-wiki/`，2 份文档）

### 1. `wiki-数据库模式设计.md` —— 已补 v44/v45（10 处）

| # | 位置 | 改动 |
|---|------|------|
| 1 | cite init.js | 「最新 v43」→「最新 v45」 |
| 2 | cite 测试文件 | 补 `migration-v45.test.js`（v44 无独立测试文件，仅补 v45） |
| 3 | 人话总览 | 「共 29 张表」改为「schema 常量一次性建出 29 张表（v44 迁移另建 events / anon_tokens 两张埋点表，启动后实际 31 张）」；「当前最新版本 v43」→「v45」 |
| 4 | 表总览标题 | 「29 张表总览」→「29 张表总览（schema 常量建表）」（避免与 v44 新增 2 表混淆） |
| 5 | 目录 | 新增「11. 埋点域」，后续序号顺延（12-15） |
| 6 | 新增「埋点域」章节 | events 表（v44，REQ-033）+ anon_tokens 表（v44，REQ-033）字段表 + 两个索引说明 |
| 7 | 迁移里程碑表 | 补 v44 `tracking_events_anon_tokens`、v45 `tracking_events_artist_index` |
| 8 | 索引章节 | 开头注「埋点表两个索引在 v44/v45 迁移内创建」；列表补 `events(name, ts)`、`events(artist_id, ts)`（不在 schemaIndexes） |
| 9 | 常见问题 | 「新增迁移（version 44+）」→「version 46+」 |
| 10 | 迁移测试 | `migration-v38/v40/v41/v43.test.js` → 补 v45 |

**代码核实依据**（`server/src/db/init.js` 实测）：
- v44 迁移 `tracking_events_anon_tokens`：`CREATE TABLE events` + `CREATE TABLE anon_tokens` + `idx_events_name_ts`（纯 CREATE，事务内安全）
- v45 迁移 `tracking_events_artist_index`：`CREATE INDEX idx_events_artist_ts ON events(artist_id, ts)`（纯 CREATE INDEX，幂等）
- `schema` 常量仍是 29 张表（events/anon_tokens 不在 schema 常量内，由 v44 迁移创建）→ 文档保留「schema 常量 29 张」+ 明示 v44 另建 2 张，启动后实际 31 张

### 2. `wiki-认证接口.md` —— 无需补（已覆盖，核实通过）

派工要求确认是否需补「TOTP 失败锁定细节（remainingLockMs）」。**核实结论：文档已完整覆盖，无需补丁。**

- 「失败响应（401）」表 TOTP_LOCKED 行：已写明「连续错 5 次后锁定 15 分钟；detail.remainingLockMs 是剩余毫秒数」
- 「防爆破与失败锁定」章节：已写明「达到阈值后返回 TOTP_LOCKED，detail.remainingLockMs 告诉前端还剩多久」
- 代码对照（auth.service.ts:137/161、auth.routes.ts:49）：`remainingLockMs: artist.totp_locked_until - Date.now()`、`...(result.remainingLockMs != null ? { detail: { remainingLockMs } } : {})`、TOTP_MAX_ATTEMPTS=5 / TOTP_LOCK_DURATION_MS=15*60*1000 —— 全部与文档吻合

> 说明：上轮 P2 报告的 ⚪ 缺失项（「会话管理.md / 认证协议.md 可补一句 remainingLockMs」）指向的是**外部文档**，不是仓库内已重写版。仓库内重写版在 08-06/08-07 重写时已含此细节。

---

## 二、外部 13 处 🟡 清单（C:\Users\qly19\Desktop\repowiki\，原样未动，交一号决策）

### 认证系 6 处（来源：P2 报告第三节）

| # | 文档 | 过时段落 | 当前正确版本 |
|---|------|----------|--------------|
| A1 | `API 接口文档\画师管理接口.md` | cite 与正文引用 `server/src/app.js`、`server/src/index.js`（约 5 处，含 mermaid 图「应用启动 index.js / 应用构建 app.js」） | master 已 100% TS：`app.ts` / `index.ts`（已实测 .js 不存在） |
| A2 | `后端开发指南\认证授权系统\会话管理.md` | 图例 `Client->>Routes : "POST /login (用户名/密码/TOTP)"`；正文「暴露登录、登出、刷新等接口」 | 唯一登录端点为 `POST /api/auth/verify {qqNumber, code}`（TOTP）；无会话刷新接口，无用户名/密码登录 |
| A3 | `架构设计\前后端通信协议\认证协议.md` | 图例 `POST /login (用户名/密码)` + `POST /verify-totp (验证码)`；正文「自动续期策略」 | 同 A2；会话无自动续期，过期需重新 verify 登录 |
| A4 | `项目概述\核心功能特性\认证授权系统.md` | 「登录视图：处理用户名/密码与TOTP输入交互」 | 登录页仅 QQ 号 + 6 位 TOTP 码 |
| A5 | `架构设计\安全架构.md` | 「TOTP 双因素、JWT/HMAC Token」；「app.js 统一处理」 | 无 JWT，会话为 HMAC-SHA256 签名 token（auth.service.ts）；app.ts |
| A6 | `故障排除\错误码参考.md` | 「刷新 Token」字样 | 无刷新接口；登出=bumpTokenVersion+clearCookie |

### 非认证系 7 处（来源：非认证报告第三节 #1-#7）

| # | 文档 | 过时段落 | 当前正确版本 |
|---|------|----------|--------------|
| B1 | `部署运维\监控告警系统.md` | cite/图表引用 `server/src/app.js`（5 处） | app.ts |
| B2 | `数据库设计\数据迁移管理.md` | cite 引用 `connection.js`；演进列表止于 v43 | connection.ts；补 v44（events + anon_tokens 埋点，init.js 已含 version 44） |
| B3 | `数据库设计\数据访问模式.md` | cite 引用 index.js/app.js/connection.js/seed.js（4 处旧名） | index.ts/app.ts/connection.ts/seed.ts（init.js 仍为 .js） |
| B4 | `核心功能模块\仪表盘与分析.md` | 「实时数据更新机制：WebSocket」及故障排查「确认 WebSocket 连接状态与重连机制」 | 无 WebSocket（全库 grep 零命中）；数据更新为 REST 拉取 |
| B5 | `项目概述\项目结构说明.md` | cite 引用 index.js/app.js/connection.js（3 处旧名） | .ts |
| B6 | `故障排除\日志分析.md` | cite 引用 `server/src/app.js`；「根据 SENTRY_DSN 动态拼接 connect-src」 | app.ts；app.ts:145 实为 SENTRY_DSN_BACKEND \|\| SENTRY_DSN |
| B7 | `knowledge\zh\环境变量与运行时配置系统.md` | 「BOT_ENABLED、BOT_WS_URL」「LOGIN_CODE_TTL、LOGIN_CODE_MAX_ATTEMPTS」 | 均不存在（BOT=REQ-028 备案未实施；LOGIN_CODE 已随 v41 TOTP 移除）；「SESSION_SECRET（JWT/…）」措辞建议删 JWT |

**汇总：外部 13 处 🟡 = 认证系 6 + 非认证系 7，全部为 1-3 行/处的文件引用名（.js→.ts）、图例残留（旧登录流程）、虚构变量（BOT_*/LOGIN_CODE_*）级修补。**

---

## 三、建议（交一号决策）

1. **仓库内**：本次补丁已按派工完成（wiki-数据库模式设计.md 10 处 + wiki-认证接口.md 核实无需补）。已 commit 待一号审核合入。
2. **外部 13 处 🟡**：不涉及 🔴（无重写需求），均为轻量修补。两条路线供一号选择：
   - 路线 A：**转派「外部文档修订批」**——需用户贡献者配合修改 C:\Users\qly19\Desktop\repowiki\ 原文（13 处均明确，可直接给修改单）；
   - 路线 B：**仓库内维护替代版**——在 docs/external-wiki/ 下继续维护修订版（本次已修 2 篇，剩余 13 处涉及 13 篇外部文档，若走此路线需一号确认范围与优先级）。
3. **可选增强**（上轮 ⚪ 缺失项，不属本次派工范围，供参考）：管理员接口.md transfer 限流数值（IP 5 次/15 分钟 + 目标 QQ 3 次/15 分钟，admin.routes.ts:259-264）未写入。

---

## 四、纪律声明

- 外部原文（C:\Users\qly19\Desktop\repowiki\）**一字未动**
- 仓库内仅改 docs/external-wiki/ 下文档；零代码改动
- git 操作：仅 add/commit 本次 docs 改动，未推送（合入由一号）
- 本报告不 commit（交一号审核后由一号合入）
