# 03-派工-server lint清理-20260807.md

> 派工：一号 → 三号（backend）
> worktree：`../artist-commission-w3`（分支 `beta/server-lint-clean`）
> 背景：二号巡检A批交付时实测 server 6 个 oxlint warning（非 no-console 类，属后端域），建议派三号清理
> 基线：master `a7c8698`。**开工第一步 `git merge master`**。

## 任务：清理 server 6 个 oxlint warning

**实测清单**（二号报告 + 一号确认）：
- `server/src/features/pricing/pricing-engine.ts`：`no-new-array` ×2（L186/L232 附近）
- `server/src/features/order/order.service.ts`：`no-unused-vars`（deriveInstallmentProgress 未用导入）
- 其余 3 个（跑 `cd server && npx oxlint src` 看实际清单）

## 动作

1. `cd server && npx oxlint src 2>&1` 拿实际 warning 全清单（以实测为准，清单可能已变）
2. 逐条清理：no-new-array → 改 `[]` 字面量；no-unused-vars → 删未用导入/变量
3. **只清 warning，不改业务逻辑**；若某个 warning 是必要代码（如类型推导需要），加 eslint-disable 并注明原因（不滥用）

## 红线

- 只动 `server/src/` 下 warning 涉及的文件
- 不碰前端、不碰测试逻辑（若清理导致测试挂，先 stash 归因——预存问题不阻塞，自己的清理绿即可）
- i18n/硬编码中文不涉及后端（无此门禁）

## 门禁

1. `cd server && npx oxlint src` → **0 error 0 warning**
2. `cd server && npm run test`（或 vitest run）→ 全绿（基线 971/971）
3. `cd web && npm run test:web` → 215/215（如后端无前端影响可跳过，但跑一次保险）
4. `git diff --stat` 只动授权文件

## 交付

- commit 到分支 `beta/server-lint-clean`，不推送不合并
- 交付报告 `docs/comms/03-to-01-server lint清理-交付-20260807.md`：每处 warning 清理前/后 + 门禁跑分
