# 深度 Bug 挖掘检验报告——结构 / Bug / 技术债（只读）

> **性质**：只读深度挖掘。全程零产品代码修改（`server/src/**`、`web/src/**`、`e2e/**`、`scripts/**` 均未动一行）；仅写本报告、更新 STATUS.md、在 `temp/` 留下三份取证脚本与门禁日志（temp/ 属 gitignore，不入库）。
> **基准**：审计开始于 master `34a5edd5`（2026-08-23 00:13:47）；**审计进行中（00:32:23）有并行提交 `44728754`「chore: 版本号升 1.0.0（server/web package.json）」**——仅两行版本号 +1/-1，无任何业务代码，对本轮全部取证无影响；审计收工时工作区仅剩本批 docs 产物（git status 零产品污染）。
> **执行**：一号主理，三路子代理并行只读取证（后端数据层 / 后端路由鉴权 / 前端），主代理交叉复核关键项并容器内查库收口。
> **日期**：2026-08-23。
> **同日后续（用户拍板）**：F-1~F-7 静默空 catch 留痕小批已开工并提交——7 处各补一行 `console.warn` 失败留痕，保留全部降级回落逻辑，零行为变化；web 门禁四件套复跑全绿（eslint 0 警告、test:web 798/798 基线不变、check:i18n 无新增、build 8.66s）；防阀追认 ArtistLayout 995→997（+2 留痕行，check-file-size.mjs 出处注明在案）。

---

## 〇、一句话结论（人话版）

**本轮全仓深挖没找到任何“真的会出事”的 bug（🔴 = 0），门禁全绿，账实相符。** 唯一发现的 7 处“报错没留痕”小毛病（网络失败时静默吞掉不记日志，均有兜底不崩页面）已由用户拍板，同日一次小批修复完毕：各加一行 `console.warn` 留痕、零行为变化，web 门禁四件套复跑全绿。另有 4 项“设计上故意如此”的记录项，只提醒不立项。

---

## 一、冷启动基线实测（2026-08-23 凌晨，全部重新跑，未引用旧基线）

### server（跑于 00:33~00:36，日志 `temp/gate-audit-823-server-*.log`）

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| typecheck | `tsc --noEmit && tsc -p tsconfig.scripts.json --noEmit && tsc -p tsconfig.tests.json --noEmit` | ✅ 三配置全过，零错误 |
| lint | `eslint . && oxlint src tests` | ✅ eslint 零错；oxlint **0 warnings / 0 errors**（298 文件） |
| vitest | `npm test` | ✅ **Test Files 141 passed (141) / Tests 1641 passed (1641)**，198.9s |

### web（跑于 00:33~00:34，日志 `temp/gate-audit-823-web-*.log`）

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| eslint | `npx eslint .` | ✅ 0 问题（exit 0，独立复跑确认） |
| lint | `npm run lint`（= vue-tsc --noEmit + tsc scripts + eslint .） | ✅ 全过 |
| test:web | `npm run test:web` | ✅ **Test Files 113 passed (113) / Tests 798 passed (798)**，8.5s |
| check:i18n | `npm run check:i18n` | ✅ 存量违规 13 条豁免，**无新增硬编码中文** |
| build | `npm run build` | ✅ built in 8.32s（dist 产物正常，含 sentry/tesseract 分包） |

**基线对照**：实测 server 1641/1641、web 798/798，与 8-21 批登记的基线（1641/798）**逐位一致**。E2E 按计划书口径属可选项，本次未跑（用户指令的门禁清单未含）。

---

## 二、扫描清单逐项三态裁决表

> 三态：**安全**（有守卫/设计如此）/ **已修**（历史已修销账）/ **待修复**（当前 master 证据成立）。分级 🔴 真 bug / 🟡 技术债 / 🟢 记录级。

| # | 扫描项 | 取证结果（全仓实测） | 裁定 |
|---|---|---|---|
| 1 | SQL 注入（`db.prepare()` 内 `${}`） | 动态 SQL 拼接点 **18 处**（artist.service 白名单 UPDATE、greeting/style/invite/guestbook/activity-log/order-fields/dashboard 常量 SQL、IN 占位符、PRAGMA 元数据、VACUUM INTO、迁移列搬运）。**18/18 全部带守卫**：`allowed.includes(key)` 硬编码白名单、常量 SQL 片段（ACTIVE_ORDER_SQL 等）、`?` 参数化占位、`''`/`""` 转义。零用户输入直拼。 | 🟢 安全 |
| 2 | JSON.parse 无 try-catch | server **20 处**、web **17 处**，全部 try-catch + 降级默认值（`[]`/`{}`/null），含 HMAC 验签后豁免与构建期文件豁免。零裸解析。 | 🟢 安全 |
| 3 | v-html 漏消毒 | web 唯一渲染入口 `SanitizedRichText.vue`（契约：入参必须已 `sanitizeHtml`；DOMPurify 白名单标签/属性 + 禁 data 属性 + 链接强制 noopener）。实际消费点 4 处（报价须知/设置预览/模板页/下单页）数据源全部过 `sanitizeHtml`。契约测试在案。 | 🟢 安全 |
| 4 | 静默空 catch | server：严格空 catch **0**（15 处带注释清理/降级类，清单豁免）。web：`catch(() => {})` 8 处，其中 2 处为 ElMessageBox 关闭语义（安全）；**7 处静默吞错已同日小批补留痕修复**（见 §三）。 | 🟢 已修 |
| 5 | eval / new Function | server **0**、web **0**（含测试与脚本目录）。 | 🟢 安全 |
| 6 | `.toFixed()` 空值崩溃 | server 13 处、web 16 处。全部左值为数字证成（累加/stat 结果/除法算式）或经 `?? 0`/三元/集中工具 `money.ts toFiniteNumber` 归一。零裸调。 | 🟢 安全 |
| 7 | parseInt 无基数 | server：全带基数（10/8）。web：**全仓无 parseInt**（统一 `Number()` 归一）。 | 🟢 安全 |
| 8 | 路由鉴权覆盖 | 20 个 routes 文件全量枚举。`/api/admin/**` 全部 requireAdmin + `registerAdminStepUpHooks`（onRoute 批量追加 step-up，transfer 特化 60s reauth）；画师写路由全部 requireAuth（订单动作再挂 requireOwnOrder 归属校验）；30+ 公开路由逐条举证（anon-token / 查单令牌 / 限流）。**裸奔 0 条**。4 项设计权衡记录（见 §四）。 | 🟢 安全 |
| 9 | TODO/FIXME/HACK 残留 | server **0**、web **0**（"todo" 命中均为待办业务模块名，非维护标记）。 | 🟢 安全 |
| 10 | snake_case 字段映射断点 | 抽样 8 组 DTO 对照（订单列表/看板队列/收款/作品/工作流/收入图表/小票），前端 `api/types.ts` 读取键与后端实际下发**逐组一致**（混合命名约定忠实对齐，队列 camelCase 映射 currentStageId/startDate/focusImageUrl 已核对原文）。零断点。 | 🟢 安全 |
| 11 | seed/demo INSERT 与真实表结构 | 容器内实测（见 §四）：demo-data.ts + seed.ts + migrate.ts 三源 9 张表对照容器真实 schema——**幻影列 0、NOT NULL 漏写 0**，nullable 缺列全为 id/created_at 等默认列。e2e 面（fixtures/global-setup）零 INSERT（走 API 建数 + 预置 smoke 库）。 | 🟢 安全 |
| 12 | i18n 三轴（附带项） | 重跑 `audit-i18n-keydiff.mjs`：后端 148 错误码 ↔ zh/en 各 151 键，**缺键 0/0**，两处 `{count}` 占位符成对。 | 🟢 安全 |

---

## 三、已修复清单（F-1~F-7 静默空 catch 留痕批——2026-08-23 用户拍板，已修已提交）

**性质判定**：7 处全部是「静默空 catch」技术债——均**有注释说明 + 降级回落兜底**（页面不崩、流程不破），缺的只是失败留痕（日志/告警）。非真 bug。补留痕不改此定性。

| 编号 | 位置 | 现状 | 触发影响 |
|---|---|---|---|
| F-1 | `QuickActions.vue:195` | 发布成功后补拉 profile，失败静默 | 网络抖动时 profile 停留在旧值（quick_actions 判定），用户无感知；下次刷新自愈 |
| F-2 | `TrackOrder.vue:462` | 客户查单页拉画师信息失败静默 | 配色回落默认 paper，无提示；纯视觉降级 |
| F-3 | `DeliveryPage.vue:141` | 同上（交付页） | 同上 |
| F-4 | `useOrderForm.ts:679` | 下单页拉工作流失败静默 | 收款分期/阶段步骤静默缺失，客户照常下单（后端有默认流程兜底） |
| F-5 | `useOrderForm.ts:683` | 下单页拉报价元数据失败静默 | 折扣开关/分期比例交互静默失效 |
| F-6 | `useOrderForm.ts:693` | 画风列表失败静默 | 显示无定价空态（既设预期） |
| F-7 | `ArtistLayout.vue:493` | 后台骨架预拉 profile 失败静默 | 「统计/留言」导航开关在未知窗口停留稍久；fetchProfile 内部 401 已有登出守卫 |

**统一修复落地（一句话级，已于同日提交）**：7 处 catch 内各加一行留痕（`console.warn('[模块] xxx 失败', err)` + `eslint-disable-line no-console`，因项目 no-console=warn 要求零警告出品），**保留全部现有降级回落逻辑与注释**，零行为变化。附带：防阀 check-file-size.mjs 追认 ArtistLayout 995→997（+2 留痕行，出处注明在案）。复跑门禁：web lint（vue-tsc + tsc scripts + eslint **0 警告**）、test:web **798/798**（基线不变）、check:i18n 无新增、build 8.66s——四件套全绿。

### 误报/销账核对（对照历史账）

- v142 报告九项技术债中「snake 全链 / 守卫双写 / formatYuan / 幂等键 / 状态机 / comms 清理」销账声明 → 本次复核：**依然成立**（守卫仅读 store 注释在案、金额入口唯一 money.ts、字段映射逐组核对通过），无回潮。
- 历史审计 P1 清单（8-15 批）与本轮清单无重叠复发项。
- 本轮**零误报**：三条线子代理上报的候选问题由主代理逐条读上下文复核，全部去伪存真如上。

---

## 四、容器实测与数据类取证（2026-08-23）

**取证时刻与容器一致性**（防"旧容器验新代码"）：

| 事实 | 值 |
|---|---|
| 容器 | `commission-web`（build `2026-08-22T16:21:11Z` = 本地 08-23 00:21，晚于 HEAD 00:13）|
| 代码一致性证据 | 容器内 `/app/web/dist` 检出 `guestbook-card`（c206a503）与 `panel--stretch`（cb557ac4）→ **最新三笔提交已进容器**；容器内 server 包版本 1.0.0-beta.4（容器建于 00:21，早于 00:32 的 1.0.0 版本号提交 5 分钟——仅版本字样差异，代码一致）|
| 迁移版本 | `schema_migrations` MAX=71 / 共 71 条（迁移链完整，与 master 一致）|
| 表结构 | 44 张表（含 `events` 埋点表——迁移 v44/v45 的真名，探针里的 tracking_events 为笔误，已证伪非问题）|
| 生产数据 | artists 1 行、orders/artworks/art_styles/style_sizes 0 行（8-21 画师数据重置后口径）、invite_codes 45、invite_code_uses 0、greeting_templates 8 |
| 健康 | 容器内 `GET /api/health` → `{"status":"ok"}`（healthy）|

**seed/demo 列核对**（`temp/audit-seed-schema-823.cjs`，容器内只读连接 `readonly:true`）：对照 9 张表（artworks/art_styles/style_sizes/artists/commission_rules/orders/order_price_entries/order_payments/schema_migrations），**🔴 硬问题 0 处**。nullable 未写列逐一人工判定：id/created_at/updated_at 为默认生成列，version/customer_token_hash/focus_image_* 等为可空业务列（demo 单无客户链接属预期）——无 UI 依赖断裂（对照 B-07 前科三例消费面：画廊宽高、时间条 deadline、队列 queue_position 均已写入且列存在）。

### 记录级设计权衡（🟢，只提醒不立项）

| 编号 | 事项 | 现状 | 建议 |
|---|---|---|---|
| G-1 | 开箱口令 SETUP_TOKEN | 本机 `.env` 实测**未设置** → `validateSetupToken` 恒 true（"先到先得"建管理员）。已缓解：站点已初始化（410 兜底）+ IP 限流 5/min + 已建即拒。公网生产已初始化同理 | 全新部署前务必设置口令（.env.example 已有注释样板）；可考虑安装脚本强制生成强口令 |
| G-2 | 匿名点赞 | 纯 IP 限流、无凭证——产品显式匿名特性 | 保持；若未来刷赞成真再评估加 anon-token |
| G-3 | 交付下载令牌走 query 参数 | 浏览器历史/Referer 可暴露；已用 silent 日志 + 15min 签名 URL + 一次性下载缓解 | 保持现状 |
| G-4 | stepup-status 端点自守卫 | 首次访问即 401 STEP_UP_REQUIRED，前端按此分支弹二次验证窗 | 预期行为，保持 |

---

## 五、证据链与取证件索引

- 门禁日志：`temp/gate-audit-823-server-{typecheck,lint,test}.log`、`temp/gate-audit-823-web-{lint,test,i18n,build}.log`（eslint 因首跑日志文件写入异常已独立复跑取证，exit 0 实录在案）。
- 容器取证脚本：`temp/container-probe-823.cjs`（迁移/表/行数/版本）、`temp/audit-seed-schema-823.cjs`（seed 列核对扩面版，容器内 /tmp/audit-823/ 执行）。
- 三路子代理原始证据全文已由主代理归档于本次会话记录；关键样本（白名单门控、step-up 自动挂载、消毒链路、7 处 catch 周边、队列字段映射、admin 路由挂载）均经主代理逐条 Read 原文复核。

## 六、交付清单汇总

| 清单 | 内容 |
|---|---|
| 🔴 上线前必做 | **空（0 项）** |
| 🟡 待修复 | **已清零**——F-1~F-7 前端静默空 catch 留痕（2026-08-23 小批已修已提交） |
| 误报表 | 空（本轮无照单全收的假阳性；历史销账 6 项复核成立） |
| 🟢 可暂缓 | G-1~G-4 设计权衡记录项 |

---

> 本报告全文只读生效，无任何产品代码/数据库写操作。坑位与复核口径均遵循「深度分析计划书-结构Bug技术债-20260820.md」§2 证据规范。