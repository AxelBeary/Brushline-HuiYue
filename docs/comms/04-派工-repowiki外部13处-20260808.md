# 04-派工-repowiki外部13处-20260808.md

> 派工：一号 → 四号（requirements/docs）
> worktree：`../artist-commission-w4`（分支 `beta/repowiki-external-13`）
> 需求：`docs/comms/04-to-01-repowiki-P0重写-交付-20260807.md` §三（13 处 🟡 汇总表）+ §四（路线 B 已拍板：仓库内维护）
> 基线：master `8fd9015`。**开工第一步 `git merge master`**。
> 门禁：纯文档批——无代码测试；核对要点逐条引用代码证据 + README 更新 + 交付报告
> ⚠️ 本批为纯 docs（docs/external-wiki/ + docs/comms/），与前后端批零冲突，可满并行。

## 本批范围

按**路线 B（仓库内维护）**修复外部 repowiki 的 13 处 🟡 过时点：在 `docs/external-wiki/` 下维护修订版（与 P0 三篇同模式），**外部原文（C:\Users\qly19\Desktop\repowiki\）一字不动**。

## 施工步骤

### 1. 读清两份交付清单

- `docs/comms/04-to-01-repowiki-P0重写-交付-20260807.md` §三：
  - **认证系 6 处**（#1-6：画师管理接口/app.js 引用、会话管理、认证协议、认证授权系统、安全架构、错误码参考）
  - **非认证 7 处**（#7-13：监控告警、数据迁移管理、数据访问模式、仪表盘与分析、项目结构说明、日志分析、环境变量与运行时配置系统）
- `docs/comms/04-to-01-repowiki-P2-交付-20260807.md` + `04-to-01-repowiki非认证-交付-20260807.md`（两份原始核实报告的 §三，含每处细节）

### 2. 落盘修订版（与 P0 三篇同模式）

- 位置：`docs/external-wiki/`（README 已有目录表，新增 13 篇修订版文件名参照 P0 命名：`wiki-<主题>.md`）
- 每篇对应一个主题：从外部原文复制全文 → 按「当前正确版本」列逐处修正 → 落到 `docs/external-wiki/`
- 修正要点（从汇总表摘录，务必全部覆盖）：
  - #1/#7/#9/#11：`app.js`/`index.js`/`connection.js` → `app.ts`/`index.ts`/`connection.ts`（注意 #9 的 init.js 仍为 .js）
  - #2/#3/#6：登录端点改 `POST /api/auth/verify {qqNumber, code}`（TOTP 唯一）；删「会话刷新/自动续期」字样；登出 = bumpTokenVersion + clearCookie
  - #4：登录视图描述删「用户名/密码」（仅 QQ 号 + 6 位 TOTP 码）
  - #5：删「JWT/HMAC Token」→ HMAC-SHA256 签名 token（auth.service.ts）；文件名替换
  - #8：`connection.js`→`connection.ts`；迁移版本补 **v44**（events+anon_tokens 埋点）+ **v45**（events artist_id 索引，init.js MIGRATIONS 实为 v45）
  - #10：删「WebSocket」描述（全库零命中，REST 拉取）；可补埋点看板章节（⚪ 缺失项，v44 tracking）
  - #12：`app.js`→`app.ts`；SENTRY 变量名 `SENTRY_DSN_BACKEND || SENTRY_DSN`（app.ts:145）
  - #13：删 BOT_ENABLED/BOT_WS_URL/LOGIN_CODE_TTL/LOGIN_CODE_MAX_ATTEMPTS（均不存在）；`.env.example` 为权威清单；删 JWT 措辞

### 3. 验证（对照代码，不许抄旧文档）

- 每条修正**用 search_files/grep 实测验证后写入**：`server/src/app.ts` / `auth.routes.ts` / `auth.service.ts` / `.env.example` / `init.js` MIGRATIONS
- 参考已重写的 P0 三篇（`docs/external-wiki/`）——同主题内容已修正，**天然互补不重复**，照抄即可
- 特别注意 #8：**顺带确认 v45 迁移具体内容**（P0 交付 §五 存疑标注 2 提到 v45 内容未核查）——写 `wiki-数据迁移管理.md` 时核实 v45 = tracking_events_artist_index（init.js 行 1679-1686 已确认）

### 4. README 更新

- `docs/external-wiki/README.md` 归档内容表新增 13 行（文件名 + 对应外部原路径 + 修订日期 2026-08-08）

## 交付

- commit 到分支 `beta/repowiki-external-13`，不推送不合并
- 交付报告 `docs/comms/04-to-01-repowiki外部13处-交付-20260808.md`：13 处逐条 已修/存疑 + 每处引用代码证据（文件:行号）+ README 更新确认
- 若某处发现汇总表判定与代码不符 → **以代码为准**，交付报告写明差异（外部报告约 1/3 不实指控的历史教训）
