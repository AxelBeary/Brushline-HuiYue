# 二号 → 一号：v0.19 第 2 波前端交付（F3 公告 + F1 点赞）

> 日期：2026-08-01
> 分支：feat/v019-frontend-wave2

## 共享组件（逻辑不共享皮肤）

| 组件 | 内容 |
|------|------|
| TplAnnouncement.vue | 只输出 📢 + 文字，零样式。`v-if="artist?.announcement"` 短路，过期判断后端已做前端不重复 |
| ArtworkLikeButton.vue | 心形 SVG + 计数，点击调 POST/DELETE like API，localStorage 持久化（`huiyue_liked_${subdomain}`），填充过渡 + 弹跳微动画。T5：0 赞不显示数字。颜色继承 currentColor、大小跟随 1em（模板控制），组件自身无装饰样式 |

## F3 公告 4 模板适配（T3 位置表）

| 模板 | 位置 | 视觉 | 备注 |
|------|------|------|------|
| classic | 左栏信息卡：名字下方、状态徽章上方 | border-left 主色强调线 + 主色软底 | 桌面 sticky 常驻首屏，移动端信息卡置顶 |
| gallery | Hero 浮层**左上角** | 半透明毛玻璃 + 展签式细边线 | T3 原定左下角，但 Hero 展签（plaque）已在左下角，改为左上避免重叠 |
| folio | Hero 分屏左栏底部 | 内联文字块（无背景，dim 色） | 桌面 absolute 锚定左栏底部（内容垂直居中，底部恒有空隙）；移动端转 static 内联 |
| atelier | Hero 右下角便签卡 | 纸面卡片 + 赭橙顶边 + rotate(-1deg)，hover 回正 | 与纸面颗粒纹理/宋体语言一致，避开左下角展签 |

实现方式：TplHero 不在授权清单，gallery/folio/atelier 用 wrapper div（`position:relative`）包裹 TplHero + 公告 absolute 定位，**TplHero 零改动**。

## F1 点赞

- TplGallery 嵌入 ArtworkLikeButton（meta 区：标题左 + 点赞右），新增 subdomain prop（4 模板已传）
- 初始 liked 状态从 localStorage 读取（刷新保持）；换浏览器/清缓存 = 未赞（符合验收 3）
- API：`likeArtwork` / `unlikeArtwork` 加入 api/index.js（隐含授权，同第 1 波）

## Settings.vue 公告编辑

- profile tab 加公告区：textarea（500 字 + 字数统计）+ 可选 date-picker（YYYY-MM-DD）
- save 提交 `announcement`（空→null 清除）+ `announcementExpiresAt`（空→null 长期显示）
- 回显：`profile.announcement` + `announcement_expires_at` 取日期部分

## i18n

新增 4 键（中英双语，只加不改）：settings.announcementLabel / announcementPlaceholder / announcementHint / announcementExpiresLabel / announcementExpiresHint（实际 5 键，Expires 拆了 label+hint）

## 验证

- `npx eslint .`：零错误零警告
- `npx vitest run`：87/87 全绿
- `npm run build`：✓ built in 4.21s
- 浏览器实测待安排（公告 4 模板视觉 + 点赞交互，CSS 定位无法单测覆盖）

## 待一号决策

1. gallery 公告位置从 T3 原定"左下角"调整为"左上角"（展签冲突），是否认可
2. 浏览器走查安排（4 模板公告 + 点赞，建议合并前过一眼）
