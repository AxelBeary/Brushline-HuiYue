# 前端质量战役总结账（2026-08-13，逐波 before→after）

> 战役依据：用户四维要求（动画/布局/逻辑/代码质量）+ REQ-037 问题清单 + 纸墨设计语言提案 v1。
> 审计输入：三路只读审计 197 条（A 画师 81 / B 客户端 53 / C 管理+共享 63；P0×3、P1×60、P2×134）。
> 验收纪律：每波一号独立复跑门禁（不信 self-report）；E2E 终验 11/11；截图实证归档 workspace/temp/fq-batch/。

## 一、动画维度

| 项 | Before | After |
|---|---|---|
| 微交互时长 | 0.15~0.35s 硬编码遍地、ease/ease-out/内联 bezier 混用 | `--dur-fast/mid/slow` 三档 token（artist-tokens.css），87 文件统一（波2，仪式动画/linear/EP 内部 23 处豁免逐条列明） |
| 亮暗切换 | body background/color 0.3s 过渡 + @property 色值 0.55s 插值 → 灰沼中间态风险（前景/背景不同步致对比度塌陷） | **即时切换**（删全部颜色插值，灰沼教训注释在案） |
| 中英切换 | 无过渡，文案突变生硬 | 容器级 `.locale-swap` 单属性 opacity 淡入（--dur-mid），禁多变量插值，reduced-motion 豁免 |
| hover 位移 | 客户端 9 处 translateY(-1~-2px) + 百眼柜 active translateY+scale | 全清：背景/阴影加深替代，零位移（P2visual+波T） |
| 循环动画 | OrderTimeline tl-pulse 1.8s infinite 心跳 | 一次性脉冲/静态强调 + reduced-motion 豁免（波T） |
| 光晕 | TplStatusBadge box-shadow 0 0 8px 发光 | --sh-1/语义底替代（波T） |
| 列表增删 | 管理端问候语/画师删除、客户端订单列表瞬变 | TransitionGroup 克制淡入（波T） |

## 二、布局维度

| 项 | Before | After |
|---|---|---|
| Dashboard | SaaS 三件套骨架（问候/收入图/统计/待办 堆叠） | 原型 v0.9 纸墨构图：问候贴纸+统计｜挂牌 → 排期卷轴全宽 → 账本/留言/动态｜开张/快捷；收入走势移出（提案 §6.1） |
| 新器物组件 | 无 | GreetingNote（固定容器/逐字洇墨/每天一次演绎）/PlaqueStatus（Y 轴翻牌+绳随翻 rAF+钟摆）/LedgerTodo（一行一个动词+5s 墨线冷却+笔点沉底+撕页）/ScheduleScroll（纸签五色条+纸卷轴头+今日笔触线）——全 lang=ts |
| 模块开关 | 看板模块不可定制 | 偏好设置四开关（排期/留言/动态/开张）+ dashboard_modules 端点（键白名单+布尔硬校验双保险）+ Dashboard v-if 消费；浏览器实证 |
| 公告入口 | 仅顶栏小圆点 | 问候贴纸内淡墨一行，点开全文，看过即消（REQ-043 I4 零打扰） |
| 管理后台 | page-head/page-sub/stat-card 各页重复（7 页） | admin-common.css 公共类，AdminLayout 单点引入（+63/-138 像素级等价） |
| 断点/窄屏 | 管理后台 900/600 与 768/769 混用；360px/240px 操作列窄屏挤压 | 波 S 收尾中（统一两档+图标化/换行） |
| 响应式验证 | 无系统自审 | 三域三档视口截图自审（画师 4 帧/客户端+管理 9 帧/模块开关 3 帧），归档 fq-batch/ |

## 三、逻辑与交互维度

| 项 | Before | After |
|---|---|---|
| P0×3 | ①公告回显失败静默（空表单可覆盖现有公告）②画师删除失败被取消分支吞 ③OrderForm 加载失败只剩页头死路 | ①明示+重试+未加载禁发布 ②拆开必有反馈 ③loadError+retryLoad（草稿恢复仅首次） |
| 三态簇一批（14 文件） | 日志/收款/偏好/名额/统计/排期公示/稿价计算器/排期看板节点加载失败静默吞 | 全改明示错误态+重试（照 Settings profileLoadFailed 模式） |
| 三态簇二批（7 文件） | 水印/手动录单/分享对话框/画风编辑/流程编辑/增项模板/账号安全静默；水印双全量重复请求 | 明示+重试；水印按 sourceType 按需加载去重 |
| TOTP 重绑 Step1 | verifyWithCode 虚实现（不校验直接进步骤，错码拖到最终步） | 新增 verify-current 轻量校验端点+前端接线+4 断言用例（半实裁决：后端 confirm 强校验无安全洞，UX 属实） |
| 复制功能 | 7 处 execCommand 重复实现，失败路径各异 | utils/clipboard.ts 统一（lang=ts，剪贴板 API 优先+回退+失败不抛） |
| 死码 | GreetingHero/TodoList/SlotOverview/RevenueChart 零引用残留 | 全删（-719 行），RevenueChart 按铁律先核实测试引用、二批清退 |

## 四、代码质量维度

- **TS**：新组件/新工具一律 lang="ts"（GreetingNote/PlaqueStatus/LedgerTodo/ScheduleScroll/clipboard.ts）；触碰文件增量迁移（Dashboard.vue）
- **零 any/@ts-ignore**：store 未类型化处用轻量接口桥接并注释，全程无蒙混
- **去重**：clipboard 7 处、管理后台样式 7 页、收入走势归工具域
- **i18n**：新增键全部中英成对（greetSign/plaqueHint/ledger*/schedule*/dashModules*/annPrefix/quickAction.title 等 30+ 键），check-i18n 每波过
- **基线变化**：server 1351→**1367**（+16 新用例）；web 437→**436**（+5 新用例，-6 死码测试随组件清退，note 记明）；E2E 11/11 终验全绿（20.6s）

## 五、验证证据链

- 门禁：每波 worktree+合并态复跑 lint/vue-tsc/vitest/build/check-i18n（本档各波数字见 STATUS v94+）
- E2E：战役全量改动后 11/11（下单/查进度/推节点/管理端/金钱链路/登录旅程/Passkey）
- 浏览器实证：fq-batch/v94-dash-*.png（4 帧）、p2-*.png（5 帧）、ca-*.png（9 帧）
- 容器：每波合入 post-merge-deploy 重建（Healthy/迁移回读/登录页 200）

## 六、收尾在途

波 T（动效纪律清违）+ 波 S（断点/窄屏/共享层）并行施工 → 波 M（客户端三态散项+a11y）→ 容器重建 + 三域终局截图交用户终验。
