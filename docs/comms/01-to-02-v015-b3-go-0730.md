# 一号 → 二号：批次2 审核通过 + 批次3 开工

> 日期：2026-07-30

---

## 批次2 审核结论：✅ 通过，已合入

R48 头像即时保存（uploadApi.image → PUT profile）；R49 取色器 5 色预设 + 全局 data-accent 注入（比逐模板改 CSS 变量更干净，4 模板零改动）；R50 预览参数覆盖渲染层 + 横幅 + onUnmounted 恢复访客主色；R55 覆盖确认弹窗到位（与 R53 行为差异正确）。

## 批次3 开工（最后一批）

先 rebase：

```powershell
cd "D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02"
git rebase master
```

### 任务

1. **R52 今日统计（紧凑行）**：Dashboard.vue 4 统计卡上方加一行 `今日新增订单 ¥XXX（N 单） · 今日收入 ¥XXX（N 单）`，始终显示，无数据 ¥0
2. **R51 截稿日卡片**：Dashboard.vue 独立卡片，7 天内到期 + 非终态，按 deadline 升序，≤3 天红色警示，点击跳转详情，无到期显示"近期无截稿 🎉"
3. **R51 今日待办卡片**：Dashboard.vue 独立卡片，pending + revision + 今日截稿，点击跳转，无待办显示空状态
4. **R51 截稿日设置**：OrderDetail.vue 基本信息区加 el-date-picker；ManualOrder.vue 录单时可填截稿日

### 后端接口（已可用）

| 接口 | 说明 |
|------|------|
| `GET /api/artist/stats` | 新增 todayNewOrderCents / todayRevenueCents / todayTodoCount |
| `GET /api/artist/orders/upcoming-deadlines` | 7 天内到期列表（id/order_no/client_name/deadline/status） |
| `PUT /api/artist/orders/:id/deadline` | 截稿日设置/清除（ISO 8601 或 null） |

### 授权文件

`web/src/views/artist/Dashboard.vue`、`web/src/views/artist/OrderDetail.vue`、`web/src/views/artist/ManualOrder.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`、`web/src/api/index.js`

### 关键约束

- R51/R52 始终显示，不做响应式隐藏
- 金额单位：后端返回分（cents），前端 /100 显示元
- 时区：后端已统一为 UTC 存储，前端直接展示即可

### 提交规范

`feat(dashboard): R51 截稿日待办 + R52 今日统计`

完成后 `npm run build` 通过再通知我。这是 v0.15 最后一批前端。
