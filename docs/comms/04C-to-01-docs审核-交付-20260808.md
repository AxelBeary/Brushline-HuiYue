# 04C-to-01：docs 全面审核 + 部署须知更新（含 cron 备份）- 交付

> 执行人：四号（requirements）
> 日期：2026-08-09
> 分支：beta/docs-audit（worktree w32）
> 派工单：docs/comms/04C-派工-docs审核-20260808.md
> 基线：git merge master 后 Already up to date（master HEAD `2b0f9a8`，03E 备份已合入 `f1228b6`）

## 〇、审核结论一句话

docs 整体质量高（external-wiki 标注完整、REQ 状态行 90% 已更新、changelog v0.42-v0.44 对齐），但**部署类文档存在一批「照做必失败」的命令错误**：OPS.md 备份命令在容器内会找不到 package.json、维护说明书/切换指南仍有 seed.js/SIGN_SECRET/泛解析等 TS 迁移与 C48 决策前的旧表述。已按「明显过时/错误/矛盾」直接修正，全部有代码证据。

## 一、已修正清单（7 文件，每项带证据）

### 1. README.md（根）
- 测试数过时：`971 + 215 = 1186` → **1024 + 262 = 1286**（本批实跑：server 69 文件/1024 用例 + web 17 文件/262 用例全过，54.97s + 2.70s）
- 快速开始 `npm test # 971 个用例` → `# 1024 个用例`
- 其余（功能列表/技术栈/目录结构/安全说明）核对与代码现状一致，未动

### 2. docs/requirements/REQ-025（状态行过时）
- 原状态行只有「规则确认 + 排入 v0.37」，缺「已实施」
- 改为：`✅ 已实施（v0.37 动态计价引擎接线：locked 持久化 v40 + 七函数接引擎 + recalc 退役 + done 守卫 + 六处守恒挂载）`——证据：changelog v0.37 + STATUS v0.37 轮 A 路 `3aea46f`

### 3. docs/requirements/REQ-036（状态行过时）
- 原「📋 已拍板待开发（待一号拆施工图派工）」——但 master 已有施工图
- 改为：`📋 已拍板已拆批（2026-08-08 施工图已派：02G 批A 前端交互重构 + 批B 后端核心，执行中）`——证据：master `4a61158`（02G 派工）+ `2b0f9a8`（三路施工图含批B）

### 4. docs/OPS.md（部署须知，任务 2 主体）
- **备份命令修正（原命令照做必失败）**：`docker compose exec web npm run backup` → `docker compose exec -T web npm --prefix /app/server run backup`。证据：Dockerfile WORKDIR=/app，/app 下无 package.json（后端在 /app/server/），不带 --prefix 会报 ENOENT；backup-db.ts 通过脚本自身路径推导 ROOT 不依赖 cwd
- §2 cron 示例同步修正（同上 + 注释移到行尾，避免吞掉日志重定向）
- **新增 §5 部署/重建清单**：备份 → git pull → `docker compose up -d --build` → Caddy restart → 三层验证（health / anon-token 64hex / dist chunk），并注明 `up -d` 不应用 logging/mem_limit 等创建期选项
- **新增 §6 AUTH_DEV_MODE 生产必须 false**（bind-init 泄露 `_dev_secret` 风险 + 检查命令）
- §3 恢复/§5 验证的宿主 `curl 127.0.0.1:3000` 改为容器内 curl（v0.42 起 3000 端口已注释）

### 5. docs/维护说明书.md（过时点最多）
- seed 命令 ×2：`node server/src/db/seed.js` → `docker compose exec -T web npm --prefix /app/server run db:seed`（seed.ts 已改名，node 直跑必失败）
- §四 数据备份整节重写：弃「cp 直接复制」（WAL 不一致风险），对齐 OPS.md 的 backup-db.ts 体系（VACUUM INTO + 保留 7 份 + BACKUP_OK/FAILED），cron 示例同步
- 前置条件「已做泛解析」→ 路径访问模式（C48 决策，Caddyfile 单主域）；§五「泛解析 DNS 配置」小节改「DNS 解析（路径访问模式）」
- **SIGN_SECRET 虚构变量清理**（3 处）：环境变量表（删除行 + SESSION_SECRET 补「同时用于文件签名 URL HMAC」）、故障排查 #5、安全注意事项 #4。证据：`server/src` grep `SIGN_SECRET` 零引用，file-sign.ts 用 `process.env.SESSION_SECRET`
- TOTP rebind 容器命令 → `--prefix /app/server`（同 WORKDIR 问题）
- 安全注意事项 #6：3000 映射说法更新为「v0.42 已注释」
- node -e 诊断命令加 `-w /app/server`（require 解析），@fastify/static 版本检查去掉 `/app/node_modules` 前缀（依赖实际在 /app/server/node_modules）
- 本地访问 `localhost:3000` → 容器内 curl
- 最后更新日期 → 2026-08-09

### 6. docs/开发→生产切换指南.md
- SIGN_SECRET 对照表 ×2 → 标注虚构（文件签名 URL 用 SESSION_SECRET）
- 「泛解析 DNS」节 → 「域名解析（路径访问模式）」（A 记录即可，无需泛解析）
- `Caddyfile 已配置泛解析` → 单主域（C48）
- seed.js → `db:seed --prefix`（步骤 6）
- 检查清单 #2 SIGN_SECRET 虚构 / #16 迁移检查 v11 → 最新迁移（schema_migrations 查最新，当前 v48 totp_used_codes）/ #17 迁移备份 → 备份可生成（BACKUP_OK）
- 最后更新日期 → 2026-08-09

### 7. docs/开发自参考.md
- 测试数 3 处：`1000/64 + 215/13 = 1215/77` → **1024/69 + 262/17 = 1286/86**
- 目录树 `seed.js` → `seed.ts`

## 二、列给一号待定项（未直接改，含 STATUS 过时清单）

**STATUS.md（一号维护，只报告不改）：**
1. **commit 不一致**：头部「master `7d23b63` / 容器 `3b8a1cb`」vs master 状态段「HEAD `b926c74` / 容器 `b926c74`」vs 实际 master HEAD `2b0f9a8`——三处对不上，需刷新
2. **08-09 合入未记账**：03E 备份（`f1228b6`）、05J TOTP 重放+金额（`ab67f11`）、05I 用户点名bug（`3df4077`）、03D 核查（`2ac3cf5`）、02E 前端核查（`e81f9bd`）均已合入 master，STATUS 未记录
3. **测试基线过时**：STATUS v44 记 1014/256 → 实跑 **1024/262（69/17 文件）**（含 03E/05J 新增）
4. **「repowiki P2」遗留行过时**：external-wiki README 显示 P2 非认证主题 8 篇已修订完成（08-07），遗留表仍挂「P1 轻量修补 6 处 🟡 + P2 改派」——已闭环可销账
5. **各角色状态行滞后**：四号仍记「前端深度审计已合入（08-08）→ 空闲」（本派工执行中）、五号记「05C 执行中（w10）」（05C-to-01 交付已存在）
6. **版本收编**：03E/05J/05I 等 08-09 批次未进 changelog/版本号，待一号裁决（v0.45？）

**其他建议（非错误）：**
7. `.env.example` SESSION_SECRET 注释含「JWT 签名」表述——代码已无 JWT（HMAC 会话），但 .env.example 不在本次授权文件内，列给一号
8. archive 根文件无显式「已归档」标注（目录语义已表达，抽查发现，建议级可不改）
9. docs/ 根与 archive/ 各有一份「待修复问题清单.md」——根版头部已注明「旧版归档」，无矛盾，仅提示

## 三、部署须知改动说明（任务 2 验收对照）

- ✅ cron 每日备份命令（可直接复制）：OPS.md §2 `30 3 * * * cd /path/to/artist-commission && docker compose exec -T web npm --prefix /app/server run backup >> /var/log/commission-backup.log 2>&1`（维护说明书 §四同步）
- ✅ 恢复步骤：OPS.md §3（备份文件 cp 回 commission.db → up → health 验证）
- ✅ 保留策略：最近 7 份（backup-db.ts KEEP=7）
- ✅ 重建清单：OPS.md §5 新增（备份→build→up→Caddy restart→三层验证）
- ✅ AUTH_DEV_MODE 生产必须 false：OPS.md §6 新增

## 四、验证

| 项 | 结果 |
|----|------|
| server 测试（实跑） | ✅ 69 文件 / 1024 用例全过（54.97s） |
| web 测试（实跑） | ✅ 17 文件 / 262 用例全过（2.70s） |
| BOM / 控制字符扫描（7 文件） | ✅ 全干净（PS 写文档陷阱已防御） |
| 冲突标记残留 | ✅ 无 |
| git status | ✅ 仅 7 个授权文件改动，未推送、未合并、未改 STATUS |

## 五、交付状态

- [x] 门禁：改动全为 md 文档（无 lint），实跑测试全绿
- [x] 交付报告已写
- [x] git status 干净（见 §四）
- [x] 未推送、未合并、未改 STATUS
