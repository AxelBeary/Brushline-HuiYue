# 三号 → 一号：v0.13 第一批后端交接

> 日期：2026-07-30
> 分支：`feat/backend-artist-v013`（worktree: `artist-commission-backend-v013`）
> 基于：master `27a1c23`
> 状态：5 commit，151/151 测试通过，ESLint 零错误，待审核

---

## commit 清单

```
a6624d5 test(backend): v0.13 新增 6 个测试用例（145→151）
6b0bd16 feat(backend): R33 签名刷新 — POST /api/artist/refresh-signatures
b29adec chore: P1-5 关闭 — Caddyfile 删泛域名预留 + 文档统一（C48 方案 B）
092f034 fix(docker): CVE 修复 — 构建阶段升级 npm 工具链
6e1e6d4 feat(db): 迁移 v13 — login_codes.expires_at 列类型对齐（DATETIME→INTEGER）
```

---

## 变更文件

```
server/src/db/init.js                          (+33)  迁移 v13（表重建）
server/src/features/order/order.routes.js      (+43)  R33 签名刷新接口
server/tests/order.service.test.js             (+16)  迁移 v13 测试
server/tests/routes.test.js                    (+57)  签名刷新测试
Dockerfile                                     (+3)   npm install -g npm@latest
Caddyfile                                      (-15)  删泛域名预留
docs/开发自参考.md                              (+2-2) P1-5 文档统一
```

---

## 接口变更（需通知二号）

| 接口 | 类型 | 详情 |
|------|------|------|
| `POST /api/artist/refresh-signatures` | **新增** | body: `{ paths: string[] }`（≤50 条），返回 `{ urls: { [path]: signedUrl } }`。requireAuth + 限流 20次/5min。路径校验：只允许 `references/` + `deliverables/{artistId}/` + `notes/{artistId}/` |

**二号前端接入建议**：
- OrderDetail.vue / QueueBoard.vue 挂载后启动 10 分钟定时器
- 收集当前页面所有签名路径，调 refresh-signatures 批量换新
- el-image `@error` 兜底：加载失败时立即调一次刷新

---

## 迁移 v13 说明

- 类型：表重建（SQLite 不支持 ALTER COLUMN）
- 幂等：PRAGMA table_info 检测列类型，已为 INTEGER 则跳过
- 数据：CAST(expires_at AS INTEGER) 无损迁移
- 回滚：恢复备份 + 降级代码（login_codes 是临时表，丢了重发码即可）
- 🔴 高风险确认：已获实际操作人确认（排期文件明确）

---

## R37 状态

- **SameSite=Lax**：已在 auth.routes.js:88 实现（`sameSite: 'lax'`），无需额外改动
- **SRI**：需要改 `web/vite.config.js`（不在我授权范围），建议一号协调二号或单独授权
- **结论**：R37 的 CSRF 部分（C49 决策）已落地，SRI 部分待二号配合

---

## 一号待办

1. 审核本分支 5 个 commit
2. 通知二号：R33 接口已就绪，可接前端
3. R37 SRI 部分：决定是否授权我改 vite.config.js，或交给二号
4. 合并后通知五号回归
