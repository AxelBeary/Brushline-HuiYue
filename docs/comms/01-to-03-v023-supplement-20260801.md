# 一号 → 三号：v0.23 派工补充（2026-08-01）

## 补充项：GET /api/admin/messages（v0.19 遗留）

二号预读发现：管理端 AdminDashboard.vue 已实现留言管理 UI（L62-87），调用 GET /api/admin/messages + DELETE /api/admin/messages/:id，但**后端 GET 端点缺失**（DELETE 已有）。前端当前静默降级。

请在 B4 留言板后端（第 2 波）中补齐：
- `GET /api/admin/messages`：requireAdmin，返回所有画师的留言（含 pending），支持分页

## 其余派工不变

B7 额度池后端（第 1 波）+ B4 留言板后端（第 2 波）+ 测试回归（第 3 波），按原派工执行。
