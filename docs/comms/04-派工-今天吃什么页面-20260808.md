# 04-派工-今天吃什么页面-20260808.md

> 派工：一号 → 四号（requirements → 前端轻活）
> worktree：`../artist-commission-w4`（分支 `beta/food-menu-page`）
> 需求：REQ-035 §四A（今天吃什么四大类）+ 已拍板实现铁律
> 基线：master `6ff1dcd`。**开工第一步 `git merge master`**。
> 门禁：web 215/215 + oxlint 0 + build 过 + check:i18n 0
> ⚠️ 本批为纯前端轻活（数据文件 food-menu.js 已合入 master，只需做页面）

## 授权文件（只许动这些）

- **新建** `web/src/views/artist/FoodMenu.vue`（工具页）
- **修改** `web/src/components/ArtistLayout.vue`：BASE_MENU_ITEMS 加工具组菜单项（`{ index: '/tools/food', icon: ForkSpoon, labelKey: 'menu.foodMenu', group: 'tools' }`，参照既有 group 写法，tools 组已在 MENU_GROUPS）
- **修改** `web/src/router/index.js`：加路由 `/tools/food` → FoodMenu.vue
- **修改** `web/src/locales/zh-CN.js` + `en.js`：menu.foodMenu + 页面文案（zh/en 同步）
- **不碰**：food-menu.js（只读引用）、其他任何文件

## 需求要点（REQ-035 §四A）

### 功能
1. **四大类模式选择**：健康版 / 糖尿病版 / 痛风版 / 外卖版（顶部 tab 或 radio）
2. **随机推荐**：点「随机推荐」→ 从当前类随机出一条（名称/类别/标签/一句话点评）
3. **换一个**：不满意再点 → 换一条（同模式内）
4. **免责提示**：糖尿病/痛风版显示「本推荐仅供参考，具体饮食请遵医嘱。」（一行小字）

### 数据源（已合入 master，直接 import）
- `web/src/utils/food-menu.js`：先读文件了解导出结构（四类数组 + 标签字段），按实际结构消费
- 只读，不修改

### 视觉
- 对齐纸墨 token（--ink/--paper/--hq/--sp-*）；卡片式随机结果展示
- 双主题（亮/暗）自动适配（用现有 CSS 变量）

## 实现铁律（用户原话，违反打回）

> 「我们实现起来一定要避免屎山，禁止技术债，代码要优雅！」

1. 不做假修复；不引外部 API；不引新依赖
2. i18n 键 zh/en 同步；禁硬编码中文（check-i18n 门禁）
3. 新页面保持组件粒度合理（如随机卡片可抽小组件，别全塞一个 template）

## 验证门禁

1. `cd web && npm run test:web` → 215/215（不降）
2. `cd web && npm run lint` → 0 error
3. `cd web && npm run build` → 成功
4. `cd web && npm run check:i18n` → 0 新增硬编码中文
5. `git diff --stat` 只动授权文件

## 交付

- commit 到分支 `beta/food-menu-page`，不推送不合并
- 交付报告 `docs/comms/04-to-01-今天吃什么页面-交付-20260808.md`：页面结构 + 数据源引用方式 + 截图（可选，纯功能页可文字描述）
