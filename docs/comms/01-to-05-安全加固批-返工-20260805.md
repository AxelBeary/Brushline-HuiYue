# 01→05：安全加固批返工（F1 覆盖缺口）

> 日期：2026-08-05 · 发件：一号 · 分支：`security-hardening`（worktree `../artist-commission-sec`，继续用）
> 状态：批主体质量过关，**F1 有遗漏，暂不合入**。开工第一步 `git merge master`。

## 问题（一号 diff 审核发现）

`artist.service.ts` 的 `updateArtist()` 内部 `return getArtistById(id)` = **完整行（含 totp_secret）**，以下 3 个端点直接把它返回给客户端：

1. `PATCH /api/artist/profile`（artist.routes.ts，`return updated`）——**最关键**：画师改自己的资料，响应体带回自己的 TOTP 密钥。这与 F1 要堵的攻击面完全相同（XSS/抓包可偷密钥）。
2. `PUT /api/admin/artists/:id/status`（admin.routes.ts L129，`return artistService.updateArtist(...)`）
3. `PUT /api/admin/artists/:id/profile`（admin.routes.ts L570，同上）

`createArtist` 返回路径已核查无泄露；GET /api/artists 公开列表是显式子集投影，安全。

## 修复要求

1. 三个端点返回处包 `publicArtistDTO(...)`（**不要**改 updateArtist 本身的返回——它还有内部消费者需要完整行，如认证中间件 token_version）。
2. `security-dto.test.js` 补回归用例：画师 PATCH /api/artist/profile 改一个字段后，响应断言不含 SENSITIVE 列表字段（复用现有 setup/expectNoSensitive）。
3. 跑全量 server 测试 + eslint + tsc，更新交付报告（在原 `05-to-01-安全加固批-交付-20260805.md` 里补返工段）。

## 授权文件

- `server/src/features/artist/artist.routes.ts`
- `server/src/features/admin/admin.routes.ts`
- `server/tests/security-dto.test.js`
- `docs/comms/05-to-01-安全加固批-交付-20260805.md`

其他文件不动。不推送不合并，commit 到自己分支，交付写报告。
