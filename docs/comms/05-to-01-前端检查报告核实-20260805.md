# 05→01：第三方前端检查报告核实（亮点 6 项 + 问题 10 项）

> 日期：2026-08-05 · 执行：五号 · 模式：只读审计（未改代码）
> 结论速览：**亮点 4 全属实 + 1 部分属实(数据过时) + 1 属实**；问题 **5 属实 · 4 部分属实 · 1 部分属实(影响修正)**

## 一、亮点核实（建议保持）

| 项 | 结论 | 证据 |
|----|------|------|
| 依赖治理（EP 按需 + simple-icons + Sentry 零初始化） | ✅ **属实** | `vite.config.js` L16 `ElementPlusResolver({importStyle:'css'})`；实测主 chunk 仅命中 9/78 EP 组件名（el-button/el-dialog/el-message 等常驻+JS API 组件），tree-shake 生效非全量；`simpleIcons.js` L8-29 恰好 20 个具名导入（注释明确「避免 3453 图标全量入库」）；`main.js` L22-30 `if (sentryDsn)` 条件初始化 |
| 路由级代码分割 | ✅ **属实** | `router/index.js` 全部路由 `component: () => import(...)`，无静态组件 |
| 字体策略（woff2 + unicode-range + swap + subset_wencai.py） | ✅ **属实** | 36 个 woff2 子集文件（wencai 6 + noto 30）；`scripts/subset_wencai.py` 存在；font.css 引用 |
| 安全基线（httpOnly + HMAC + v-html 消毒 + errorHandler + 401 软登出） | ✅ **属实** | `auth.routes.ts` L56-62 httpOnly+sameSite:lax；v-html 恰好 6 处全部 `sanitizedRules/sanitizedRulesPreview`（DOMPurify，`utils/sanitize.js`）；`main.js` L33-41 全局 errorHandler；`api/index.js` L48-65 401 软登出（排除登录错误码） |
| 工程化（ESLint flat + Prettier + Vitest + Playwright + CI） | ⚠️ **部分属实（数据过时）** | 工具链 ✅；**Vitest 数字 106 过时——当前 web 实测 192/192**（12 文件）；Playwright 5 条 ✅（e2e.yml） |
| 性能意识（IO + reduced-motion + 上传 120s） | ✅ **属实** | `useScrollReveal.js` L30 IntersectionObserver + `templates.css` prefers-reduced-motion；`api/index.js` L243 `UPLOAD_TIMEOUT_MS = 120_000` |

## 二、问题核实（10 项）

| # | 报告项 | 结论 | 证据 / 修正 |
|---|--------|------|------------|
| P0-1 | 巨型组件 | ✅ **属实** | 实测：OrderDetail 1731 行 / QueueBoard 1530 / ManualOrder 1380 / OrderForm 1083（报告数字略高，数量级一致） |
| P0-2 | 生产层无压缩/缓存头 | ✅ **属实** | `Caddyfile` 仅 `reverse_proxy`（无 encode/Cache-Control/安全头）；`app.js` fastifyStatic 未配 compress，SPA 手动路由 createReadStream 无 gzip |
| P1-1 | 前端未类型化 | ✅ **属实** | `web/src` 下 `.ts` 文件 **0 个**；`api/index.js` 328 行无类型 |
| P1-2 | 无 manualChunks / 包分析 | ✅ **属实** | `vite.config.js` build.rollupOptions 仅 input，无 manualChunks、无 visualizer |
| P1-3 | 公共主页 SEO 弱 | ✅ **属实** | `/artist/:subdomain` 纯 CSR（SPA fallback），无预渲染/SSR/SSG |
| P1-4 | 图片懒加载/响应式 | ⚠️ **部分属实** | TplGallery 已用 `el-image lazy`（懒加载存在）但**无 srcset**；DeliveryPage **无 img/el-image 元素**（交付页只有文件列表，「交付页懒加载」不适用）；签名续期**是自动定时**（`useSignatureRefresh.js` 每 10 分钟 + @error 兜底按图刷新），非「手动」——但仅覆盖画师后台 OrderDetail/QueueBoard，**客户端交付页未接** |
| P2-1 | 无统一数据请求层 | ✅ **属实** | `api/index.js` axios 直连，无缓存/无 AbortController/无卸载取消 |
| P2-2 | useScrollReveal MutationObserver 回退 | ⚠️ **部分属实（有设计背景）** | L28 `containerRef?.value \|\| document` 回退存在；但注释写明因模板 defineAsyncComponent 异步插入 DOM 需动态补挂——建议强制传容器合理，需先解决异步组件挂载时机 |
| P2-3 | 错误路径动态 import 整个 EP | ⚠️ **属实但影响修正** | `main.js` L38 `import('element-plus')` 属实；但**实测 tree-shake 生效**（主 chunk 仅 9/78 EP 组件），动态 chunk 非全量；且 el-message 已在主 chunk（L7 手动引样式 + errorHandler 引用），动态 import 属冗余——改全局 `$message`/已引用 ElMessage 即可 |
| P2-4 | 字体总量偏大 | ⚠️ **属实但已缓解** | 实测 36 文件共 **9.36MB**（报告 9.8MB 接近）；但 unicode-range 按需下载，首屏实际传输远小于总量（报告自己也承认「实际更小」） |

## 三、新发现（报告未提）

1. **ci.yml 的 web job 未跑 vitest**（仅 eslint + build）——web 192 单测未进 CI，只本地跑（与上份后端报告核实结论一致，再次确认）
2. **README 过时多处**：L99 认证描述仍写「登录码」（REQ-027 已改 TOTP）；L100 测试数「666+106」已过时（server 864 + web 192）；L190 变更日志引用「v0.1~v0.27」（实际 v0.36+）
3. `main.js` L4 `import { ElLoading } from 'element-plus'` 走**根入口**——实测 tree-shake 生效（无全量），风险低，但改 `element-plus/es/components/loading` 子路径更稳（消除对 EP sideEffects 声明的依赖）

## 四、修复建议（按优先级，待一号派工授权）

- **P0（收益最大）**：Caddyfile 加 `encode zstd gzip` + `/assets/*` 缓存头；巨型组件拆分（可分波，先拆 OrderDetail 弹窗/表单块）
- **P1**：manualChunks 拆分 vendor + visualizer 基线；ci.yml web job 补 vitest；api/ 渐进 TS 化；客户端交付页接入 useSignatureRefresh；`/artist/:subdomain` 预渲染评估
- **P2**：main.js errorHandler 改已引用 ElMessage（去动态 import）；useScrollReveal 强制容器（先解决异步挂载时机）；README 过时内容更新；字体 preload 主字体首个子集

## 备注

- 本次只读核实，未改代码、未建分支；报告已含每条证据的文件:行号，可直接据此派工
- 巨型组件拆分与 SSR/预渲染属结构性改造，建议独立排期并配回归测试（web 192 基线）
