# 交付：手动录单日期批（二号）

> 日期：2026-08-06 · 交付人：二号 · 分支：`fix/manual-order-dates` · worktree：`../artist-commission-mo-fix`
> 提交：`2e1f5be`（基于 master `a4851a1`，3 files changed, +36/-4）· 未推送未合并

## 做了什么

| 项 | 内容 |
|----|------|
| B1 | `ManualOrder.vue` 两个 `el-date-picker` 的 `:disabled-date="(d) => d < new Date()"` → 归一化到当天 0 点比较（`today0` setup 期构造一次），今天可选、昨天起灰掉 |
| B2 | 新增 `disableDeadlineDate`/`disableStartDateDate` 双向灰掉（截稿日不可早于开稿日、开稿日不可晚于截稿日，边界用 `new Date(form.startDate + 'T00:00:00')`，对齐 OrderDetail 994-1005）；`submit()` 开头加冲突守卫：`startDate > deadline` → `ElMessage.error(dateConflict)` + return 不发请求（YYYY-MM-DD 定长字符串字典序比较） |
| B3 | `manualOrder.references` 文案补齐：`'参考图（可选，最多5张，每张≤10MB）'` / `'Reference images (optional, up to 5, ≤10MB each)'`（抄客户下单页 zh/en 第 293 行，两处统一） |

新增 i18n 键：`manualOrder.dateConflict` = `'开稿日不能晚于截稿日'` / `'Start date cannot be later than deadline'`（en 措辞与既有 `INVALID_START_DATE` 一致；中文用页内术语「开稿日」与 `manualOrder.startDate` 统一）。未改任何既有键措辞。

## 改了哪些文件（仅授权范围内）

- `web/src/views/artist/ManualOrder.vue`
- `web/src/locales/zh-CN.js`（仅 manualOrder 块）
- `web/src/locales/en.js`（仅 manualOrder 块）

## 验证结果

1. **web `npx vitest run`：215/215 全绿**（基线 215，无回退）
2. **eslint（3 个改动文件）：0 错**
3. **Playwright E2E 实测（临时 spec，已删除）：4/4 通过 × 2 次连续**（确认非 flaky）：
   - B1：截稿日+开稿日两个 picker 面板，今天可选、昨天灰掉
   - B2a：先选开稿日(今天+5) → 截稿日面板中今天（早于开稿日）灰掉
   - B2b：先选截稿日(今天+2) → 开稿日面板中今天+5（晚于截稿日）灰掉
   - B3：参考图标签显示完整限制文案
4. **B2 提交守卫：组件级 vitest 临时测试 2/2 通过**（已删除）：
   - 驱动 v-model 构造 startDate>deadline → `ElMessage.error` 调用 + `createManualOrder` 未调用
   - 合法日期（startDate<deadline）→ 正常创建，守卫不误伤

## 说明

- B2c 的提交拦截无法在真实 UI 层 E2E 验证：B2 双向灰掉后界面已选不出冲突日期（这正是修复目的），且 Element Plus 输入解析同样应用 disabled-date（键入 8/21 > 截稿日 8/16 会被拒绝）——故提交守卫走组件级 vitest 验证（见上）。
- E2E 调试中发现：EP 面板关闭有 transition，`:visible` 会命中关闭中的旧面板导致断言错位，验证 spec 中已用「等待面板完全关闭」规避（仅影响临时 spec，产品代码无此问题）。
- npm install 产生的 `allowScripts` 写入 package.json 已还原，未进入提交。

## 验收对照（派工验收标准）

- 日期面板今天可选，昨天及以前灰掉 ✅（E2E B1）
- 先选截稿日 → 开稿日面板晚于截稿日灰掉；反向同理 ✅（E2E B2a/B2b）
- 两日期冲突时提交被前端拦截（不发请求）✅（vitest B2 守卫）
- 参考图标签显示完整限制文案 ✅（E2E B3）
- 测试基线不回退（215/215），lint 0 ✅

请一号审核后转告用户。
