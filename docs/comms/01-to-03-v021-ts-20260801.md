# 派工：v0.21 TypeScript 渐进迁移

> 一号 → 三号 | 2026-08-01
> 分支：`feat/v021-ts`（三号 worktree，Sentry 合入后再开工）
> 规格：`docs/specs/plan-v021-engineering.md` §六

## 任务

| # | 内容 |
|---|------|
| T1 | server/tsconfig.json（allowJs: true, checkJs: false, strict: false, noEmit: true） |
| T2 | 后端构建适配（当前无构建步骤，tsc --noEmit 仅做类型检查，不改运行方式） |
| T3 | pricing 模块迁移：pricing.service.js → pricing.service.ts + 类型标注 |
| T4 | shared/ 模块迁移：validate.js / errors.js / file-sign.js → .ts |
| T5 | 核心实体类型定义：server/src/types/（Artist, Order, Tier, WorkflowStage） |

## 明确不做

- ❌ 不迁移路由文件、前端组件、测试文件
- ❌ 不启用 strict: true
- ❌ 不引入 typescript-eslint
- ❌ 不改运行逻辑（node 仍直接跑 .js，.ts 仅做编译期检查）

## 关键约束

- `npx tsc --noEmit` 已迁移模块零错误（T-AC1）
- 未迁移 JS 文件不受影响（T-AC2）
- 后端测试 469/469 通过（T-AC4）
- 可逆：删 tsconfig + .ts 恢复 .js 后正常（T-AC5）
- 前端暂不动（Vite 原生支持 TS，但 v0.21 不迁移前端）

## 授权文件

- `server/tsconfig.json`（新建）
- `server/src/types/`（新建目录）
- `server/src/features/artist/pricing.service.js` → `.ts`（重命名+类型）
- `server/src/shared/validate.js` → `.ts`
- `server/src/shared/errors.js` → `.ts`
- `server/src/shared/file-sign.js` → `.ts`
- `server/package.json`（加 typescript devDep + typecheck script）

## 注意

- 后端是 ESM（import/export），tsconfig 需 `"module": "ESNext"` + `"moduleResolution": "bundler"` 或 `"nodenext"`
- better-sqlite3 类型：`@types/better-sqlite3`（devDep）
- 迁移后原 .js 删除，import 路径不变（node 跑 .js 入口，.ts 仅 tsc 检查——或者用 tsx 跑？你定，但必须保持 `node src/index.js` 能启动）
