# 05-to-01 QueueBoard 拆分交付 — 20260807

> 交付：五号 → 一号（bugfix/重构）
> worktree：`artist-commission-w5`（分支 `beta/queueboard-split`）
> 派工依据：`docs/comms/05-派工-QueueBoard+ManualOrder拆分-20260807.md`
> 蓝本：OrderDetail 拆分（PaymentPanel/GalleryPanel，v0.40 已合入）——本批沿用 props 向下 / emit 向上模式
> 状态：未推送、未合并、未改 STATUS（合入由一号执行）

## 一、改动清单（授权文件，共 3 个）

| 文件 | 行数 | 说明 |
|------|------|------|
| `web/src/views/artist/QueueBoard.vue` | 1679 → **203** | 父组件瘦身；保留视图切换/队列加载保存/交付弹窗/签名刷新 composable |
| `web/src/components/artist/queue/QueueBoardList.vue` | **新增 613** | 列表视图（正式区 draggable 卡 + 缓冲区 + 完成区） |
| `web/src/components/artist/queue/QueueBoardCalendar.vue` | **新增 951** | 月历 + 时间条视图 |

> 行数口径说明：派工文档写 1530 行为较早快照；实测拆分前 `ReadAllLines` 统计为 1679 行。父组件按派工目标 ≤1100 大幅达标。

### QueueBoard.vue 保留（父组件职责）
- view-switch 三视图切换 + localStorage 默认视图（viewMode/saveViewMode）
- queue 数据加载/保存：loadQueue / loadBufferQueue / loadCompletedQueue / onDragEnd（拖拽排序 API）
- 交付弹窗：openDeliverFor / onDeliveredFromBoard / DeliverDialog
- 焦点图显示模式状态（focusDisplay + localStorage 持久化）
- R33 签名 URL 刷新 composable（useSignatureRefresh → refreshNow 传给列表子组件）
- 缓冲/完成区数据 refs（bufferQueue/completedQueue 等）

### QueueBoardList.vue（列表视图）
- 模板原样搬移：queue-toolbar + 正式区 draggable 列表 + 完成区 + 缓冲区 tab
- 交互逻辑随卡移入：焦点图上传链路（triggerFocusUpload/uploadAndSetFocus/handleFocusDrop）、滑块确认取消、手机左滑进详情、递补 promoteOrder、流程推进 advanceOrderStage/quickAction、workflow 加载
- props：queue/focusDisplay/activeTab/loading/bufferQueue/bufferLoading/completedQueue/completedLoading/refreshNow
- emit：update:queue / update:focus-display / update:active-tab / drag-end / open-deliver / refresh-queue / refresh-all
- 样式原样搬入 scoped

### QueueBoardCalendar.vue（月历 + 时间条）
- 月历：calCursor/changeMonth/手势翻月/calCells/色带函数原样搬入（派工推荐"calCursor 逻辑整体移入子组件"）
- 时间条：tlZoom 四档/今天线/拖拽改期/撤销 toast 全链路原样搬入
- props：queue/bufferQueue/loading/bufferLoading/viewMode
- emit：refresh-all（拖拽/撤销成功后父重拉队列）
- watch(viewMode) 加 `immediate: true`——从 board 直接切到 timeline 时子组件刚挂载也执行"滚到今天"，与原父级 watch 行为等价

## 二、零行为变化保障

- 模板 DOM 结构/class 原样搬移，未改 i18n 键、未改 API 契约、未改后端
- 事件 handler 等价：原 @click/@change/@drop/@end 等逐一对应（drag-end/open-deliver 上抛，其余随卡移入）
- vuedraggable v-model 经 computed 代理 emit update:queue，父组件接回，拖拽排序链路不变
- 纯重构，无顺手优化无关文件

## 三、测试结果（门禁全绿）

```bash
cd server && npx vitest run        # 944/944 passed
cd web && npm run test:web         # 215/215 passed
cd web && npm run lint             # 0 errors（4 warnings 均为既有 OrderDetail/OrderForm unused 警告，非本次引入）
cd web && npm run build            # ✓ built
```

> lint 对比：拆分前基线 242 warnings（其中 238 个来自旧 QueueBoard.vue 可自动修复项），拆分后全项目仅 4 个既有 unused 警告，新组件零警告。

## 四、截图对比（0% 像素差异铁证）

方法：隔离测试库（Alice 画师 + TOTP 登录）+ 造 9 单（正式 5：高优/进行中/逾期/完成/未设截稿；缓冲 2；完成 2）→ before 截图（HEAD 原版）→ 复用同一 DB 数据 → after 截图（拆分后）→ 逐像素对比。

| 视图 | before 路径 | after 路径 | 差异像素 | 差异率 |
|------|------------|-----------|---------|--------|
| board（列表） | workspace/temp/qbsplit-shots-before/board.png | workspace/temp/qbsplit-shots-after/board.png | 0 | 0.000000% |
| calendar（月历） | workspace/temp/qbsplit-shots-before/calendar.png | workspace/temp/qbsplit-shots-after/calendar.png | 0 | 0.000000% |
| timeline（时间条） | workspace/temp/qbsplit-shots-before/timeline.png | workspace/temp/qbsplit-shots-after/timeline.png | 0 | 0.000000% |

- 三视图 1280×900 逐像素对比均为 0 差异
- before/after 对应 PNG 字节数完全一致（board 97888 / calendar 173383 / timeline 78914）
- 截图产物已移至公共临时目录 `D:\Hermes Agent CN Desktop\workspace\temp\qbsplit-shots-*`（不入库，避免污染 git status）

## 五、浏览器交互冒烟（13 项，11 通过 + 2 拖拽项待复核）

通过：正式区卡片渲染(9) / 焦点图空态上传入口(7) / 缓冲区卡片(2) / 递补按钮(2) / 月历 42 格 / 月历翻月(8月→7月) / 月历订单带(7) / 时间条行(7) / 缩放按钮 / 切回列表缓冲 tab 状态保留。

待复核（2 项）：合成鼠标事件拖不动 vuedraggable（headless 下 handle 拖拽的已知难点），脚本层面 calls=0，未产生报错 toast。非拆分引入的证据：拖拽事件链（pointerdown→drag-end emit→reorder API）在代码上已完整接通；建议后续用真实 pointer 事件序列或 e2e 既有 vuedraggable 用例复核。可追加 `docs/待修复问题清单.md` 记录。

## 六、遗留说明

- ManualOrder.vue 拆分未做（本批派工为"先 QueueBoard，一号合入后再 ManualOrder"，遵守"禁止同时拆两个"）
- 临时脚本（temp-shot.mjs/temp-smoke.mjs）、隔离测试库（shot.db）已清理，git status 干净

## 七、提交

- 分支：`beta/queueboard-split`
- 提交内容：QueueBoard.vue 拆分 + 新增 queue/ 两个子组件
- 不推送、不合并、不改 STATUS