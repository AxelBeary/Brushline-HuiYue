# 二号 v0.17 开工准备报告

> **转交**：二号 → 一号
> **日期**：2026-07-31

---

## 环境就绪

| 项 | 状态 |
|---|---|
| master HEAD | `1219052`，与 origin 同步 |
| node_modules | ✅ 存在 |
| Vite build | ✅ 通过（8.51s） |
| 二号 worktree | 未创建，等派工后建 |

## 预读发现：第一批实际只剩 R24 + R25

STATUS.md 第一批列了 R26 + R24 + R25 + R31 + BUG-1/2/4，逐项核实：

| 需求 | 状态 | 依据 |
|---|---|---|
| R26 手动录单价格 | ✅ 已做完 | `ManualOrder.vue:92` "UI-5 修复：最终价格始终可见"，`v-if` 守卫已移除 |
| R31 焦点图小模式 | ✅ 已做完 | `QueueBoard.vue:173` "仅 无/大 两态，旧值 small 映射为 large" |
| BUG-1/2/4 | ✅ v0.16 已合入 | STATUS.md 明确列出 |
| **R24 校验弹窗** | ❌ 待做 | `OrderForm.vue:390` 仍是 `validate().catch(() => false); if (!valid) return`，无弹窗无滚动 |
| **R25 ThemePicker 位置** | ❌ 待做 | 4 模板 ThemePicker 仍在 CTA 按钮下方（如 `ArtistHomeClassic.vue:36-38`） |

**第一批纯前端、无后端依赖，可立即开工。**

## 待一号确认

1. **R25 授权边界**：ThemePicker.vue 在"需一号协调"清单里。R25 只改 4 个模板里的挂载位置（DOM 移动 + CSS），不动 ThemePicker 组件内部逻辑。是否视为客户前端授权范围内？
2. 派工确认后我建 worktree + 分支 `feat/client-frontend-v017-b1`，R24 + R25 一批做完。

## 预读摘要（降低实施风险）

- **R24**：改 `OrderForm.vue` 的 `openReceipt()` + 可能涉及 `useOrderForm.js`。需 `ElMessageBox` 列出未通过项 + `scrollTo` 定位。i18n 新增弹窗文案键（中英同步）。
- **R25**：4 模板（Classic/Gallery/Folio/Atelier）均引用 ThemePicker，STATUS.md 确认目标位置为右下角。需逐模板改挂载点 + 统一 CSS。
- **WorkflowOverviewStrip.vue**（R22，第五批）：39 行，6 处引用，本次不动。
