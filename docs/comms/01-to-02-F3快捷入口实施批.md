# 01-to-02：Beta 冲刺 F3 快捷入口实施批（施工图）

> 转交一号 · 派工时间：2026-08-07 · 执行角色：二号（前端） · worktree：`../artist-commission-w14` · 分支：`beta/f3-quickactions`
> **开工第一步：`git merge master` 再读本文件**（派工期间 master 可能前移）
> 只动本文件授权清单内文件；不推送、不合并、不改 STATUS。

## 〇、任务理解（人话）

用户拍板（STATUS v15 / REQ-034）把仪表盘「快捷入口」从**导航镜像**（默认 6 项 = 侧边栏同名项，内测吐槽"高度重复"）改为**动作型快捷通道**：

- 默认 5 项：新建订单 / 预览主页 / 约稿须知编辑 / 分享接稿页(复制链接) / 快捷入口设置
- 状态切换做**循环按钮**（点一下 open→full→break，展示当前状态）并**删下方 StatusSwitch 卡**
- 新增「快速发作品」= 画师**直接拖图/粘贴到块**（真正人类逻辑），默认含在快捷入口区
- 收入概览不做 / 未读留言不做（旁边已有收入大块、下方已有留言板块）
- 保留自定义能力（偏好页配置区照常工作，候选池全量可选）+ 最好自定义布局

## 一、授权文件（只准动这些）

| # | 文件 | 改动 |
|---|------|------|
| 1 | `web/src/components/artist/dashboard/QuickActions.vue` | 候选池加 type/action 字段 + 默认项改造 + go() 分发 + 状态循环 + 分享复制 + 快速发作品拖图块 |
| 2 | `web/src/views/artist/Dashboard.vue` | 删 StatusSwitch 引用 + 状态逻辑迁移 |
| 3 | `web/src/locales/zh-CN.js` | 新增 `quickAction.*` 文案键 |
| 4 | `web/src/locales/en.js` | 新增 `quickAction.*` 文案键（英文） |
| 5 | `web/src/views/artist/Preferences.vue` | 配置区候选列表：动作型条目加「⚡动作」标记（小改） |

**不要动**：`Settings.vue`（TAB_ALIASES rules→showcase 已存在，`/settings?tab=rules` 直接可用，勿改）、`ArtistLayout.vue`、`StatusSwitch.vue`（保留文件但不再被 Dashboard 引用——若编译报未使用可暂不删文件，只删引用）、后端任何文件、任何 `.css` 主题文件。

## 二、现状锚点（已核实，2026-08-07）

- `QuickActions.vue` L11-21：`QUICK_ACTION_POOL` 9 项（dashboard/queue/manual/orders/guestbook/tiers/artworks/settings/preview），每项 `{key, icon, labelKey, route}`；L24 `QUICK_ACTIONS_DEFAULT = ['queue','manual','orders','artworks','tiers','settings']`；L27-34 parseQuickActions；L37-39 readQuickActionsConfig；L66-73 activeActions computed；L75-82 go()（preview 特殊：`store.subdomain` 新窗；其他 router.push）。
- `Dashboard.vue` L29-31 右栏 `<div class="area area-status"><StatusSwitch :model-value="currentStatus" @pick="updateStatus" /></div>`；L109 import StatusSwitch；L116-129 currentStatus/lastKnownStatus/updateStatus（调 `artistApi.updateProfile({status: val})`，失败回滚）；L133-134 onMounted 初始化 currentStatus。
- `StatusSwitch.vue`：el-card + el-radio-group 三态，仅被 Dashboard 引用。
- `Preferences.vue` L111 `quickPoolOptions = QUICK_ACTION_POOL.filter(a => a.key !== 'dashboard')`；L112-133 quickSelected/saveQuickActions（3~9 项限制、DB+localStorage 双持久）；L162 `isDefault` 判断用 `QUICK_ACTIONS_DEFAULT` 比较。
- `Settings.vue` L285 `TAB_ALIASES = { rules: 'showcase' }` —— `/settings?tab=rules` 自动落到主页展示 tab（须知编辑卡就在该 tab，L141-163）。
- 剪贴板先例：`OrderForm.vue` L753 `await navigator.clipboard.writeText(text)` + L756 降级 `ElMessage.warning(text)`。
- 上传链路：`ArtworkManage.vue` L316-325 handleUpload = `uploadApi.image(file)` → `artistApi.createArtwork({imagePath, title: uploaded.originalName})`；`usePasteUpload` composable（`web/src/composables/usePasteUpload.js`，支持 `enabled` 参数、`maxCount`/`maxSizeMB`、仅拦图片、纯文本不拦）。
- API：`web/src/api/index.js` L108-109 `updateProfile`；L122-124 getArtworks/createArtwork；L245-249 `uploadApi.image`。
- store：`web/src/stores/artist.js` L21 getter `subdomain`；L38-50 fetchProfile；无直接改 profile 的 action（状态切换成功后需手动同步显示，参考现有 Dashboard 模式）。

## 三、改动点施工图

### 改动 1：`web/src/components/artist/dashboard/QuickActions.vue`

#### 1a. 候选池改造（L11-21 整体替换）

**before（L10-21）**：
```js
/** 候选池（~10 个，命名与侧边栏 menu.* 完全一致） */
export const QUICK_ACTION_POOL = [
  { key: 'dashboard', icon: markRaw(Odometer), labelKey: 'menu.dashboard', route: '/dashboard' },
  { key: 'queue', icon: markRaw(Tickets), labelKey: 'menu.queue', route: '/queue' },
  { key: 'manual', icon: markRaw(EditPen), labelKey: 'menu.manualOrder', route: '/orders?action=manual' },
  { key: 'orders', icon: markRaw(Box), labelKey: 'menu.orders', route: '/orders' },
  { key: 'guestbook', icon: markRaw(ChatDotRound), labelKey: 'menu.guestbook', route: '/guestbook' },
  { key: 'tiers', icon: markRaw(Money), labelKey: 'menu.tiers', route: '/tiers' },
  { key: 'artworks', icon: markRaw(Picture), labelKey: 'menu.artworks', route: '/artworks' },
  { key: 'settings', icon: markRaw(Setting), labelKey: 'menu.settings', route: '/settings' },
  { key: 'preview', icon: markRaw(View), labelKey: 'menu.preview', route: null }
]
```

**after**（type: 'route' 跳转 / 'action' 动作 / 'link' 新窗；action 字段给 go() 分发用）：
```js
/** 候选池：type=route 跳转 / action 执行动作 / link 新窗口；命名与侧边栏 menu.* 对齐 */
export const QUICK_ACTION_POOL = [
  { key: 'dashboard', type: 'route', icon: markRaw(Odometer), labelKey: 'menu.dashboard', route: '/dashboard' },
  { key: 'queue', type: 'route', icon: markRaw(Tickets), labelKey: 'menu.queue', route: '/queue' },
  { key: 'manual', type: 'route', icon: markRaw(EditPen), labelKey: 'menu.manualOrder', route: '/orders?action=manual' },
  { key: 'orders', type: 'route', icon: markRaw(Box), labelKey: 'menu.orders', route: '/orders' },
  { key: 'guestbook', type: 'route', icon: markRaw(ChatDotRound), labelKey: 'menu.guestbook', route: '/guestbook' },
  { key: 'tiers', type: 'route', icon: markRaw(Money), labelKey: 'menu.tiers', route: '/tiers' },
  { key: 'artworks', type: 'route', icon: markRaw(Picture), labelKey: 'menu.artworks', route: '/artworks' },
  { key: 'settings', type: 'route', icon: markRaw(Setting), labelKey: 'menu.settings', route: '/settings' },
  { key: 'preview', type: 'link', icon: markRaw(View), labelKey: 'menu.preview', route: null },
  // ── F3 新增动作（2026-08-07 用户拍板）──
  { key: 'rules', type: 'route', icon: markRaw(EditPen), labelKey: 'quickAction.rules', route: '/settings?tab=rules' },
  { key: 'share', type: 'action', icon: markRaw(Share), labelKey: 'quickAction.share', route: null, action: 'share' },
  { key: 'quickconfig', type: 'route', icon: markRaw(Setting), labelKey: 'quickAction.quickconfig', route: '/preferences' },
  { key: 'status', type: 'action', icon: markRaw(Refresh), labelKey: 'quickAction.status', route: null, action: 'status' },
  { key: 'publish', type: 'action', icon: markRaw(UploadFilled), labelKey: 'quickAction.publish', route: null, action: 'publish' }
]
```

**注意**：
- 新增图标导入：`Share`、`Refresh`、`UploadFilled` 需从 `@element-plus/icons-vue` 引入（改 L5 import 行，追加三个名字；`Refresh` 若与已导入冲突改名，图标库里有 `Refresh`/`RefreshLeft`/`Upload`/`UploadFilled`/`Share`/`Link`，选存在的）。
- 若 `EditPen` 已用于 manual，rules 可复用 `EditPen`（icon 允许重复）；`Setting` 复用 settings 的（允许重复）。
- `rules` 的 route 用 `/settings?tab=rules`（Settings 已有 TAB_ALIASES rules→showcase，直达须知编辑卡，**不要**改 Settings.vue）。

#### 1b. 默认项改造（L23-24）

**before**：
```js
/** 默认 6 个（与改版前一致） */
export const QUICK_ACTIONS_DEFAULT = ['queue', 'manual', 'orders', 'artworks', 'tiers', 'settings']
```

**after**（默认 5 个拍板项 + status 循环按钮 = 6 格；publish 拖图块固定展示不占格子，见 1e）：
```js
/** 默认（2026-08-07 用户拍板）：动作型为主，移除导航镜像冗余项 */
export const QUICK_ACTIONS_DEFAULT = ['manual', 'preview', 'rules', 'share', 'quickconfig', 'status']
```

#### 1c. 模板：状态循环按钮 + 快速发作品拖图块（L42-56 替换）

**before（L42-56）**：
```html
<template>
  <!-- #3: 快捷操作区（可自定义：候选池 + localStorage 配置，3 列网格自适应行数） -->
  <div class="quick-grid">
    <div
      v-for="action in activeActions" :key="action.key"
      class="quick-card" role="button" tabindex="0"
      @click="go(action)"
      @keydown.enter="go(action)"
      @keydown.space.prevent="go(action)"
    >
      <el-icon class="quick-icon"><component :is="action.icon" /></el-icon>
      <span class="quick-name">{{ $t(action.labelKey) }}</span>
    </div>
  </div>
</template>
```

**after**：
```html
<template>
  <div class="quick-actions-wrap">
    <!-- 快速发作品：拖图/粘贴即发（画师直接拖到块上，真正人类逻辑） -->
    <div
      class="quick-publish"
      :class="{ 'quick-publish--active': publishActive || publishUploading }"
      @dragenter.prevent="onDragEnter"
      @dragover.prevent
      @drop.prevent="onDropPublish"
      @click="publishActive = !publishActive"
      role="button" tabindex="0"
      @keydown.enter="publishActive = !publishActive"
    >
      <el-icon class="quick-publish-icon"><UploadFilled /></el-icon>
      <span class="quick-publish-name">{{ $t('quickAction.publishHint') }}</span>
      <span v-if="publishUploading" class="quick-publish-state">{{ $t('quickAction.uploading') }}</span>
    </div>
    <!-- 快捷入口网格：默认 5 项（manual/preview/rules/share/quickconfig），可自定义 -->
    <div class="quick-grid">
      <div
        v-for="action in activeActions" :key="action.key"
        class="quick-card" role="button" tabindex="0"
        :class="{ 'quick-card--status': action.key === 'status' }"
        @click="go(action)"
        @keydown.enter="go(action)"
        @keydown.space.prevent="go(action)"
      >
        <el-icon class="quick-icon"><component :is="action.icon" /></el-icon>
        <span class="quick-name">{{ $t(action.labelKey) }}</span>
        <span v-if="action.key === 'status'" class="quick-status-label">{{ statusText }}</span>
      </div>
    </div>
  </div>
</template>
```

> 状态卡底部展示当前状态文案（statusText：可约稿/已排满/休息中），点卡片循环切换，见 1d。

#### 1d. script setup：状态循环 + 分享复制 + 上传（L58-83 替换）

**before（L58-83）**：
```js
<script setup>
import { computed } from 'vue'
import { useRouter } from 'vue-router'
import { useArtistStore } from '../../../stores/artist.js'

const router = useRouter()
const store = useArtistStore()

// v0.25: DB 优先（profile.quick_actions），localStorage 回退，最终兜底默认值
const activeActions = computed(() => {
  const dbKeys = parseQuickActions(store.profile?.quick_actions)
  const keys = dbKeys || readQuickActionsConfig()
  return keys
    .map(k => QUICK_ACTION_POOL.find(a => a.key === k))
    .filter(Boolean)
})

function go(action) {
  // 主页预览：动态拼接 subdomain（新窗口，与 Settings 预览行为一致）
  if (action.key === 'preview') {
    if (store.subdomain) window.open(`/artist/${store.subdomain}`, '_blank', 'noopener')
    return
  }
  router.push(action.route)
}
</script>
```

**after**：
```js
<script setup>
import { ref, computed } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { useI18n } from 'vue-i18n'
import { useArtistStore } from '../../../stores/artist.js'
import { artistApi, uploadApi } from '../../../api/index.js'
import { usePasteUpload } from '../../../composables/usePasteUpload.js'

const router = useRouter()
const store = useArtistStore()
const { t } = useI18n()

// v0.25: DB 优先（profile.quick_actions），localStorage 回退，最终兜底默认值
const activeActions = computed(() => {
  const dbKeys = parseQuickActions(store.profile?.quick_actions)
  const keys = dbKeys || readQuickActionsConfig()
  return keys
    .map(k => QUICK_ACTION_POOL.find(a => a.key === k))
    .filter(Boolean)
})

// ─── F3 状态循环按钮（替代 Dashboard StatusSwitch，2026-08-07 拍板） ───
const STATUS_CYCLE = ['open', 'full', 'break']
const statusBusy = ref(false)
const statusText = computed(() => t(`dashboard.status${statusLabel(store.profile?.status || 'open')}`))
function statusLabel(s) {
  return { open: 'Open', full: 'Full', break: 'Break' }[s] || 'Open'
}
async function cycleStatus() {
  if (statusBusy.value) return
  const cur = store.profile?.status || 'open'
  const idx = STATUS_CYCLE.indexOf(cur)
  const next = STATUS_CYCLE[(idx + 1) % STATUS_CYCLE.length]
  statusBusy.value = true
  try {
    await artistApi.updateProfile({ status: next })
    store.profile = { ...store.profile, status: next }
    ElMessage.success(t('dashboard.statusUpdated'))
  } catch (err) {
    ElMessage.error(err.message)
  } finally {
    statusBusy.value = false
  }
}

// ─── F3 分享接稿页（复制链接） ───
async function shareLink() {
  if (!store.subdomain) {
    ElMessage.warning(t('quickAction.noSubdomain'))
    router.push('/settings')
    return
  }
  const url = `${window.location.origin}/artist/${store.subdomain}`
  try {
    await navigator.clipboard.writeText(url)
    ElMessage.success(t('quickAction.copied'))
  } catch {
    ElMessage.warning(url) // 剪贴板不可用：展示链接供手动复制
  }
}

// ─── F3 快速发作品：拖图/粘贴到块（真正人类逻辑） ───
const publishActive = ref(false)
const publishUploading = ref(false)
const publishFiles = ref([])
const { pasteError } = usePasteUpload({
  onFiles: async (files) => { await doPublish(files) },
  maxCount: 5,
  maxSizeMB: 10,
  enabled: true
})
watch(pasteError, (msg) => { if (msg) ElMessage.warning(msg) })

function onDragEnter() {
  publishActive.value = true
}
async function onDropPublish(e) {
  const files = Array.from(e.dataTransfer?.files || []).filter(f => f.type.startsWith('image/'))
  if (!files.length) { ElMessage.warning(t('quickAction.notImage')); return }
  await doPublish(files)
}
async function doPublish(files) {
  publishUploading.value = true
  try {
    for (const file of files) {
      const uploaded = await uploadApi.image(file)
      await artistApi.createArtwork({ imagePath: uploaded.filePath, title: uploaded.originalName })
    }
    ElMessage.success(t('quickAction.published'))
  } catch (err) {
    ElMessage.error(err.message || t('quickAction.publishFailed'))
  } finally {
    publishUploading.value = false
    publishActive.value = false
  }
}

// ─── go() 分发：route/action/link 三型 ───
function go(action) {
  if (action.type === 'link') {
    // 主页预览：动态拼接 subdomain（新窗口，与 Settings 预览行为一致）
    if (store.subdomain) window.open(`/artist/${store.subdomain}`, '_blank', 'noopener')
    else ElMessage.warning(t('quickAction.noSubdomain'))
    return
  }
  if (action.type === 'action') {
    if (action.action === 'status') cycleStatus()
    else if (action.action === 'share') shareLink()
    else if (action.action === 'publish') { publishActive.value = !publishActive.value }
    return
  }
  router.push(action.route)
}
</script>
```

**注意**：
- 需要 `import { watch } from 'vue'`（追加到 L59 import）。
- `dashboard.statusOpen/statusFull/statusBreak/statusUpdated` 键已存在（Dashboard.vue 用过），直接复用。
- store.profile 手动展开更新状态（store 无 setter，参考现有 Dashboard 模式——但 Dashboard 用 local ref；这里直接改 store 让侧边栏状态点联动，ArtistLayout 身份区读 `profile.status`）。
- upload 成功后不刷新作品列表（Dashboard 无作品网格）；如需即时反馈可 `store.fetchProfile()` 保持 profile 最新（状态/quick_actions 变了）——**可加一行** `store.fetchProfile().catch(() => {})` 在 doPublish 成功后。

#### 1e. 样式补充（L85-108 追加，不改现有）

在 `<style scoped>` 末尾追加：
```css
/* ─── F3 快速发作品拖图块（2026-08-07） ─── */
.quick-actions-wrap { display: flex; flex-direction: column; gap: 10px; }
.quick-publish {
  display: flex; align-items: center; justify-content: center; gap: 8px;
  padding: 14px 12px;
  border: 1.5px dashed color-mix(in srgb, var(--hq) 55%, transparent);
  border-radius: var(--r-l);
  background: color-mix(in srgb, var(--hq) 6%, var(--card));
  cursor: pointer; user-select: none;
  transition: border-color 0.2s, background 0.2s;
}
.quick-publish:hover, .quick-publish--active {
  border-color: var(--hq);
  background: color-mix(in srgb, var(--hq) 12%, var(--card));
}
.quick-publish-icon { font-size: 20px; color: var(--hq); }
.quick-publish-name { font-size: 13px; font-weight: 500; color: var(--ink); }
.quick-publish-state { font-size: 12px; color: var(--ink3); }
/* 状态卡当前状态文字 */
.quick-status-label { font-size: 11px; color: var(--ink3); margin-top: -2px; }
```

> 若 `var(--hq)`/`var(--card)`/`var(--ink)`/`var(--ink3)`/`var(--r-l)` 与文件现有 token 命名不一致，以本文件现有 L85-108 用到的 token 为准（现有已用 `--line/--card/--r-l/--hq/--sh-2/--ink`），缺失的 token 用现有等价类。

---

### 改动 2：`web/src/views/artist/Dashboard.vue`（删 StatusSwitch，状态逻辑迁 QuickActions）

#### 2a. 模板删状态区（L29-31）

**before**：
```html
        <div class="area area-status">
          <StatusSwitch :model-value="currentStatus" @pick="updateStatus" />
        </div>
```

**after**（删除整块，快捷入口区下面直接是活动流；grid 布局里 area-status 行顺延——见 2c）：
```html
```

#### 2b. 删 import（L109）

**before**：
```js
import StatusSwitch from '../../components/artist/dashboard/StatusSwitch.vue'
```

**after**（删除该行）。

#### 2c. 布局行号修正（L209-213）

**before**：
```css
  .area-slot     { grid-column: 2; grid-row: 1; }
  .area-quick    { grid-column: 2; grid-row: 2; }
  .area-status   { grid-column: 2; grid-row: 3; }
  .area-activity { grid-column: 2; grid-row: 4; }
  .area-guestbook { grid-column: 2; grid-row: 5; }
```

**after**（删掉 area-status 行，后面顺延）：
```css
  .area-slot     { grid-column: 2; grid-row: 1; }
  .area-quick    { grid-column: 2; grid-row: 2; }
  .area-activity { grid-column: 2; grid-row: 3; }
  .area-guestbook { grid-column: 2; grid-row: 4; }
```

#### 2d. 删状态逻辑（L116-129 + L133-134）

**before（L115-134 相关段）**：
```js
const stats = ref(null)
const currentStatus = ref('open')
const lastKnownStatus = ref('open') // P1-6: 回滚用

async function updateStatus(val) {
  try {
    await artistApi.updateProfile({ status: val })
    lastKnownStatus.value = val // P1-6: 成功后更新已知状态
    currentStatus.value = val
    ElMessage.success(t('dashboard.statusUpdated'))
  } catch (err) {
    currentStatus.value = lastKnownStatus.value // P1-6: 回滚到上次成功状态
    ElMessage.error(err.message)
  }
}

onMounted(async () => {
  await store.fetchProfile()
  currentStatus.value = store.profile?.status || 'open'
  lastKnownStatus.value = currentStatus.value // P1-6: 初始化已知状态
```

**after**：
```js
const stats = ref(null)

onMounted(async () => {
  await store.fetchProfile()
```

> 注意：`t` 仍被留言区使用（`t('dashboard.guestbookTitle')` 等），`ElMessage` 仍被留言区使用，`artistApi` 仍被留言区使用——**都保留**，只删 currentStatus/lastKnownStatus/updateStatus 三块。

### 改动 3：`web/src/locales/zh-CN.js`（新增 quickAction 键）

在 `menu` 块后（L252 `},` 之后）新增顶层 `quickAction` 块。**before/after 取 L250-253**：

**before**：
```js
    // REQ-016 C: 侧边栏分组标题
    groupWork: '工作', groupBiz: '经营', groupFront: '门面'
  },
```

**after**：
```js
    // REQ-016 C: 侧边栏分组标题
    groupWork: '工作', groupBiz: '经营', groupFront: '门面'
  },
  // F3 快捷入口动作（2026-08-07 用户拍板）
  quickAction: {
    rules: '约稿须知编辑',
    share: '分享接稿页',
    quickconfig: '快捷入口设置',
    status: '状态切换',
    publish: '快速发作品',
    publishHint: '拖图或粘贴，直接发布作品',
    uploading: '发布中…',
    published: '作品已发布',
    publishFailed: '发布失败',
    notImage: '仅支持图片',
    copied: '接稿页链接已复制',
    noSubdomain: '未设置主页域名，去设置'
  },
```

### 改动 4：`web/src/locales/en.js`（同键英文）

在对应 `menu` 块后加 `quickAction`：
```js
  quickAction: {
    rules: 'Edit Guidelines',
    share: 'Share Page',
    quickconfig: 'Quick Action Settings',
    status: 'Status',
    publish: 'Quick Publish',
    publishHint: 'Drag or paste to publish',
    uploading: 'Uploading…',
    published: 'Artwork published',
    publishFailed: 'Publish failed',
    notImage: 'Images only',
    copied: 'Page link copied',
    noSubdomain: 'No page domain set — go to settings'
  },
```

> en.js 的 menu 块结尾行号与 zh 不同，用 `groupFront` 或相邻键定位，在其后插入即可；**务必同时确认 en.js 也有 `menu.rules`/`menu.preview` 键**（rules 英文键若缺，补 `rules: 'Guidelines'`，preview 已有 `preview: 'Preview Page'`）。

### 改动 5：`web/src/views/artist/Preferences.vue`（配置区动作标记）

#### 5a. 候选列表加动作标记（L52-60 区域，找到 checkbox 模板）

**before（L52-60 附近，含 labelKey 渲染那行）**：
```html
            <el-checkbox
              v-for="opt in quickPoolOptions"
              :key="opt.key"
              :value="opt.key"
              class="quick-config-item"
            >
              <el-icon class="quick-config-icon"><component :is="opt.icon" /></el-icon> {{ $t(opt.labelKey) }}
            </el-checkbox>
```

**after**：
```html
            <el-checkbox
              v-for="opt in quickPoolOptions"
              :key="opt.key"
              :value="opt.key"
              class="quick-config-item"
            >
              <el-icon class="quick-config-icon"><component :is="opt.icon" /></el-icon>
              {{ $t(opt.labelKey) }}<template v-if="opt.type === 'action'"> <span class="quick-action-badge">⚡动作</span></template>
            </el-checkbox>
```

#### 5b. 样式（Preferences.vue `<style scoped>` 末尾追加）

```css
/* F3: 动作型候选标记 */
.quick-action-badge {
  display: inline-block; margin-left: 4px; padding: 0 5px;
  font-size: 10px; line-height: 16px;
  border-radius: 4px;
  color: var(--hq-d, #b45309);
  background: var(--hq-t, rgba(180, 83, 9, 0.12));
}
```

> `--hq-d/--hq-t` 若该文件未定义，用 fallback 值（如上写法自带 fallback）；或改为文件现有 token。

## 四、自测步骤（跑完才算完成）

```powershell
# 1. 语法门禁
cd D:\Hermes Agent CN Desktop\workspace\artist-commission-w14
npx oxlint web/src/components/artist/dashboard/QuickActions.vue web/src/views/artist/Dashboard.vue web/src/views/artist/Preferences.vue web/src/locales/zh-CN.js web/src/locales/en.js

# 2. web 单测（受影响套件）
npx vitest run --root web 2>&1 | Select-Object -Last 30

# 3. 构建验证（CSS/模板/import 全链路）
npm run build --prefix web 2>&1 | Select-Object -Last 20
```

**验证断言**：
- 构建无错误（Rollup 不报 missing import：Share/Refresh/UploadFilled 图标存在）。
- `Select-String -Path web/src -Pattern 'StatusSwitch' -Recurse` 除 `components/artist/dashboard/StatusSwitch.vue` 自身外无引用（Dashboard.vue 已删）。
- `Select-String -Path web/src/locales/zh-CN.js -Pattern 'quickAction'` 有块；en.js 同。
- 运行态浏览器实测（若环境允许）：登录画师 → Dashboard 快捷入口区显示拖图块 + 6 格（manual/preview/rules/share/quickconfig/status）；点 status 卡状态循环且侧边栏状态点联动；点 share 复制成功提示；点 rules 跳到设置页须知卡。

## 五、交付物

1. 改动全部 commit 到 `beta/f3-quickactions`（commit message：`beta: F3快捷入口改造——动作型候选池+默认5项+状态循环+拖图发作品+分享复制`，若拆多次 commit 每个都要有描述）。
2. 写交付报告 `docs/comms/02-to-01-F3快捷入口实施批-交付.md`（在你 worktree 的 docs/comms/ 下，commit 进分支）：
   - 改动摘要（每个文件改了什么，逐条）
   - 自修/纠偏说明（如有——读码发现与派工假设不符的地方，单独列出）
   - 自测结果（上述命令的真实输出摘要）
   - 浏览器实测截图（截图放 `workspace/temp/f3-quickactions/`，报告里写路径；若无法起浏览器，明确写"未浏览器实测"）
   - 卡点（如有）
3. 交付报告抬头写「**二号转交一号，文件：docs/comms/02-to-01-F3快捷入口实施批-交付.md**」。

## 六、红线

- 只动授权 5 文件；不改后端；不改 STATUS.md；不推送、不合并。
- 不自创 i18n 键以外的文案；文案一律走 locales（不硬编码中文）。
- 前端校验不收紧后端规则；不改 .env/配置文件。
- 遇 API 契约疑问（uploadApi.image 返回结构等）先读 `web/src/api/index.js` 对应行，按实际形状写，自修说明里注明。

