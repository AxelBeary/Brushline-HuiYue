# 二号 → 一号：v0.17 第二批前端提交（R58-8 平台链接 + 灵感标签）
> 日期：2026-07-31

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/client-frontend-v017-b2`（worktree: `artist-commission-02`，commit `aba7f0c`）

【任务编号】
R58-8（v0.17 第二批前端派工，任务 A/B/C/D）

【修改模块】
画师设置页 / 客户下单页 / 客户主页模板 / 共享 composable / i18n

【修改内容】
1. **任务 A**：设置页 profile tab 新增"平台链接"区域（外链区域下方）。URL 输入 + 平台下拉（"自动识别"默认 + 6 个已知平台手动选择），最多 10 条，留空行不保存。platform 为空时 PUT 请求省略该字段，由后端 `identifyPlatform()` 自动识别。
2. **任务 B**：设置页 profile tab 新增"灵感标签"区域。el-tag 回车添加 + closable 删除，最多 20 个 / 每个 ≤30 字符 / 去重校验（前端拦截 + 后端 schema 兜底）。保存走 PUT profile 的 `inspirationTags` 字段。
3. **任务 C**：OrderForm 灵感标签从硬编码 6 个中文标签改为 `computed(() => artist.value?.inspirationTags || [])`，画师未设置时 `v-if` 隐藏整个区域（不 fallback 硬编码），点击注入描述框逻辑不变。
4. **任务 D**：4 模板客户主页展示平台链接。`useArtistData` 新增 `platformLinks` computed（读 `artist.platformUrls`，追加 `PLATFORM_BADGE` 文字徽标），各模板在外链区域后追加平台链接块，复用各模板已有视觉语言（badge + label + 新窗口打开），无链接时 `v-if` 隐藏。

【涉及文件】
| # | 文件 | 改动 |
|---|------|------|
| 1 | `web/src/views/artist/Settings.vue` | 任务 A+B：模板区 + 脚本区 + 样式区（+126 行） |
| 2 | `web/src/views/client/OrderForm.vue` | 任务 C：inspireTags 改 computed + v-if |
| 3 | `web/src/composables/useArtistData.js` | 任务 D：platformLinks computed + PLATFORM_BADGE |
| 4 | `web/src/views/client/templates/ArtistHomeClassic.vue` | 任务 D：侧栏平台链接块 |
| 5 | `web/src/views/client/templates/ArtistHomeGallery.vue` | 任务 D：页脚平台链接块 |
| 6 | `web/src/views/client/templates/ArtistHomeFolio.vue` | 任务 D：CTA 区平台链接块 |
| 7 | `web/src/views/client/templates/ArtistHomeAtelier.vue` | 任务 D：页脚平台链接块 |
| 8 | `web/src/locales/zh-CN.js` | i18n：+9 键（settings.platform*/inspire*） |
| 9 | `web/src/locales/en.js` | i18n：+9 键（英文同步） |

【是否修改非客户前端文件】
是，均为隐含授权：
- `web/src/views/artist/Settings.vue`：派工授权列表 #1 明确列出
- `web/src/composables/useArtistData.js`：soul 永久授权文件
- `web/src/locales/zh-CN.js` + `en.js`：纯新增键（未修改任何既有键），隐含授权

【接口依赖】
- `PUT /api/artist/profile`：`platformUrls: [{url, platform?}]`（maxItems 10）+ `inspirationTags: [string]`（maxItems 20, ≤30 字符）——后端已合入 master
- `GET /api/artist/profile`：返回原始 DB 行，`platform_urls` / `inspiration_tags` 为 JSON 字符串——前端 onMounted 解析
- `GET /api/artists/:subdomain`：返回已解析的 `platformUrls: [{url, platform, label}]` + `inspirationTags: [string]`——客户页直接消费
- 无新增 API 调用（`artistApi.updateProfile` / `artistPublicApi.getProfile` 已有）

【自测情况】
| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过（9.14s） |
| Ad-hoc 静态验证 | ✅ 68/68 通过 |
| i18n 中英同步 | ✅ 9 键 × 2 语言 |
| 改动文件范围 | ✅ 仅授权文件（9 个） |
| console.log / TODO / v-html | ✅ 无新增残留 |

**需人工验证项**（无浏览器环境）：
- 设置页：平台链接添加/删除/保存后刷新回显
- 设置页：灵感标签回车添加/删除/保存后刷新回显
- 设置页：平台下拉选"自动识别"时保存，刷新后确认后端识别结果
- 客户下单页：画师设了标签 → 显示；未设 → 不显示
- 客户主页：4 模板平台链接展示 + 无链接时隐藏
- 客户主页：平台链接点击新窗口打开

【可能影响】
- 画师设置页：profile tab 新增两个表单区域（外链下方、联系 QQ 上方），不影响其他 tab
- 客户下单页：灵感标签区域条件渲染，画师未设置时消失（原硬编码标签不再显示）
- 客户主页：4 模板各增一个链接块，无链接时不渲染，不影响现有布局
- 管理后台：不受影响
- 嵌入脚本：不受影响

【待确认问题】
无

【申请】
申请一号审核合并。
