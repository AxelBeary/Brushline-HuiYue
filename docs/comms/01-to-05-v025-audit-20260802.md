# 五号 v0.25 派工：v0.24 测试覆盖审计

> 直接在 master 操作（只读审计 + 报告）
> 日期：2026-08-02

---

## 任务

v0.24 新增了大量功能（档位三态、统计卡、留言管理、快捷按钮、话术重构、月历、P0/P1/P2 修复、额度池），但测试是逐步补的。做一次测试覆盖审计：

### 审计范围

1. **后端**：对照 v0.24 新增/修改的 service 函数和 API 端点，检查 `server/tests/` 中是否有对应测试
   - 重点：收款 API（POST/GET payments）、封面 API（v0.25 新增，暂不查）、快捷按钮 API、留言审核 API
   - 方法：`git log --oneline v0.23..v0.24 --stat` 看改了哪些 service/routes，逐个搜测试文件

2. **前端**：对照 `web/src/` 中 v0.24 新增组件，检查 `web/tests/` 中是否有对应测试
   - 重点：QueueBoard 月历、QuickActions、GuestbookManage、SlotOverview

3. **E2E**：当前 5 个 spec 是否覆盖了 v0.24 关键路径（不扩展，只评估缺口）

### 输出格式

```markdown
| 功能 | 后端测试 | 前端测试 | E2E | 缺口风险 |
|------|----------|----------|-----|----------|
| 收款 API | ✅ payments.test.js | — | ❌ | 中 |
| 月历视图 | — | ❌ | ❌ | 低（纯前端） |
```

末尾给一句话结论：哪些缺口值得补（按风险排序），哪些可以不管。

### 约束

- **只审计不写测试**（用户已拍板不扩 E2E，测试补充等用户确认）
- 不改代码，不改测试
- 产出写入交付 comms

## 交付

comms：`05-to-01-v025-coverage-audit-{日期}.md`
