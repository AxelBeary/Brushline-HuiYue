# 三号交付：v0.31 Wave 3（2026-08-03）

> 分支：`feat/v0.31-backend-0803`
> Worktree：`../artist-commission-wt-be`
> 基于：rebase 后最新 master

---

## 提交清单（2 个 commit）

| commit | 内容 |
|--------|------|
| `0df038c` | F5 下一节点应收：操作条上方醒目提示 + 点击跳转收款区 |
| `d27c0f2` | 操作日志：迁移v35 + activity-log 服务 + 6 写入点 + GET logs 分页筛选 |

---

## F5 下一节点应收（REQ-020 F5）

- OrderDetail 操作条上方加"💰 下一节点应收：{name} ¥{amount}"醒目提示条
- 数据源：F4 的 `installmentRefs`，取第一个 `remainingCents > 0` 的节点
- 点击平滑滚动到收款区（`.pool-ref`）
- 终态（已交付/已取消）不显示
- 中英文翻译

---

## 操作日志后端（REQ-021 F1）

### 迁移 v35
- `order_activity_logs` 表：order_id / action_type / actor / detail_json / created_at
- action_type CHECK 约束：status_change / price_change / extra_item / payment / stage_advance / note_update
- 索引：order_id + created_at

### 服务层（activity-log.service.ts，新文件）
- `logActivity(orderId, actionType, actor, detail?)`：事务内写入，随主操作提交/回滚
- `getOrderLogs(orderId, { page, pageSize, type })`：分页 + 可选 type 筛选，detail_json 解析为对象

### 写入点（6 处）

| 函数 | action_type | detail |
|------|-------------|--------|
| updateOrderStatus | status_change | { from, to } |
| updateFinalPrice | price_change | { oldCents, newCents, reason } |
| addExtraItem | extra_item | { action: 'add', name, priceCents } |
| deleteExtraItem | extra_item | { action: 'delete', name, priceCents } |
| addPayment | payment | { amountCents, note, installmentId } |
| advanceStage | stage_advance | { action: 'advance', stageName, stageId } |
| rollbackStage | stage_advance | { action: 'rollback', from, to, stageId } |
| addNote | note_update | { action: 'add', hasImage }（仅画师备注） |
| deleteNote | note_update | { action: 'delete', noteId } |

### 查询路由
- `GET /api/artist/orders/:id/logs?page=&pageSize=&type=`
- preHandler: requireAuth + requireOwnOrder
- 永久保留，不清理

---

## 迁移汇总（Wave 3）

| 版本 | 名称 | 内容 |
|------|------|------|
| v35 | order_activity_logs | 操作日志表 + 索引 |

---

## 接口变更

| 路径 | 变更 | 兼容性 |
|------|------|--------|
| GET /api/artist/orders/:id/logs | 新增 | 新接口 |

---

## 验证

- order.service.test.js: 79/79 ✅
- extra-items.test.js: 13/13 ✅
- quota-pool.test.js: 13/13 ✅
- 合计 105/105 通过

---

## v0.31 三号全部完成

Wave 1（3 项）+ Wave 2（3 项）+ Wave 3（2 项）= 8 项全部交付。
迁移 v31-v35，共 5 个版本。
