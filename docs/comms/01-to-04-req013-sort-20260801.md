# 一号→四号：REQ-013 画师反馈整理（2026-08-01）

## 背景

REQ-013 共 10 条画师反馈。一号已完成代码核实，用户已拍板部分方向。以下按类型分配给你。

## 你的任务

### A. #4 稿位数字 + 仪表盘/主页设置逻辑规划（用户特别交代）

用户原话："我们得计划一下仪表盘和主页设置的逻辑仔细规划下。也丢四号去交流处理。"

现状：slotDisplay 4 模板已全部渲染，不是 Bug。画师问"怎么还没上线"可能是因为没在设置里开启名额/额度（batch_limit 和 monthly_quota 都为 null 时不显示）。

**你要做的**：
1. 和画师/用户交流，搞清楚画师真正困惑的是什么（是不知道在哪开？还是开了没显示？还是想要更多设置项？）
2. 梳理"仪表盘设置"和"客户主页设置"的逻辑关系，输出一份规划文档（哪些设置影响主页展示、哪些影响仪表盘、是否有重叠/遗漏）
3. 参考：`web/src/views/artist/Settings.vue`（设置页）、`web/src/components/artist/dashboard/SlotOverview.vue`（仪表盘名额卡）、`server/src/features/artist/artist.service.ts` L451 computeSlotDisplay

### B. #9 图片裁切问题（用户要和你直接交流）

用户原话："交四号和我交流"

现状（一号已核实）：
- TplTierGrid：档位示例图固定 180px 高 + cover → 竖图被裁
- TplGallery grid 模式：固定 200px 高 + cover → 竖图被裁
- TplGallery masonry 模式：height auto，不裁（正常）
- TplGallery editorial 模式：height auto，不裁（正常）
- TplHero（banner/fullscreen/split）：cover 裁切是背景图设计意图

**你要做的**：
1. 和用户交流确认：哪些场景要改、改成什么效果（auto-height / contain / 指定比例）
2. 输出验收标准（含具体模板+布局模式）
3. 整理完后交一号派工给二号/五号实施

### C. 其余需求条目整理（#1/#2/#3/#5/#6/#8/#10）

按反馈批次工作流：逐条核实代码现状 → 和用户对齐 → 验收标准 → 优先级排序。

参考文件：`docs/requirements/REQ-013-画师反馈批次-20260801.md`

## 产出

整理完的条目写入 `docs/requirements/REQ-013-整理.md`，每条含：现状、用户确认的方案、验收标准、优先级、预估工程量。

## 授权

只读代码 + 写 docs/。不改代码。
