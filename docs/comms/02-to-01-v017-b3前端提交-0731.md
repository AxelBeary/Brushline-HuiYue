# 二号 → 一号：v0.17 第四批前端提交（SPEC-003 前端 + UX-1）
> 日期：2026-07-31

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/client-frontend-v017-b3`（worktree: `artist-commission-02`，commit `8d32172`）

【任务编号】
SPEC-003 前端（任务 A/B）+ UX-1（任务 C，五号审计）

【修改模块】
画师订单详情 / 客户进度页 / 模板组件 / 画师作品管理 / 管理抽屉 / API 层 / i18n

【修改内容】
1. **任务 A**：OrderDetail 时间线卡下方新增"附加工作项"卡片。列表显示名称+说明+金额（分→元），删除按钮悬停显示（触屏常驻，C56 交互一致），终态隐藏操作。底部"添加附加项"按钮（上限 20 条禁用）→ 弹窗（名称必填 + 说明可选 + 金额 el-input-number 元输入）。添加/删除后后端返回完整订单直接替换 `order.value`（final_price_cents 已重算，与 deleteNote 模式一致）。有附加项时显示"最终价格 = 基础价格 + 附加项合计，由系统自动计算"提示。
2. **任务 B**：TrackOrder 时间线下方新增"价格与付款"区域。附加项明细仅显示 name + 金额（硬约束：不渲染 description/id/created_at），最终价格用 finalPriceCents，付款节点显示 name + amountCents + paid 状态标签（已付/未付）。区域整体条件渲染（三个数据源任一有值才显示）。
3. **任务 C**：4 个组件的 el-image 加 `preview-teleported`（一行 × 4 文件），防止图片预览被 transform 截断。

【涉及文件】
| # | 文件 | 改动 |
|---|------|------|
| 1 | `web/src/views/artist/OrderDetail.vue` | 任务 A：卡片 + 弹窗 + 脚本 + 样式（+132 行） |
| 2 | `web/src/views/client/TrackOrder.vue` | 任务 B：价格区域 + formatCents + 样式（+56 行） |
| 3 | `web/src/api/index.js` | addExtraItem / deleteExtraItem（+3 行） |
| 4 | `web/src/components/templates/TplGallery.vue` | 任务 C：preview-teleported（+1 行） |
| 5 | `web/src/components/templates/TplTierGrid.vue` | 任务 C：preview-teleported（+1 行） |
| 6 | `web/src/views/artist/ArtworkManage.vue` | 任务 C：preview-teleported（+1 行） |
| 7 | `web/src/views/admin/ArtistDetailDrawer.vue` | 任务 C：preview-teleported（+1 行） |
| 8 | `web/src/locales/zh-CN.js` | i18n：+18 键 |
| 9 | `web/src/locales/en.js` | i18n：+18 键（英文同步） |

【是否修改非客户前端文件】
是，均为派工授权或隐含授权：
- `web/src/views/artist/OrderDetail.vue` / `ArtworkManage.vue`：派工授权列表 #1/#7
- `web/src/views/admin/ArtistDetailDrawer.vue`：派工授权列表 #8（实际路径在 `views/admin/` 而非派工写的 `views/artist/`，"或等效"）
- `web/src/api/index.js`：隐含授权（新接口调用），comms 注明
- `web/src/locales/*.js`：纯新增键，隐含授权

【接口依赖】
- `POST /api/artist/orders/:id/extra-items`（body: {name, description?, priceCents?}）→ 返回完整订单
- `DELETE /api/artist/orders/:id/extra-items/:itemId` → 返回完整订单
- `GET /api/orders/track/:orderNo` 已返回 extraItems [{name, priceCents}] + finalPriceCents + installments [{name, amountCents, paid}]
- 后端均已合入 master（SPEC-003）

【自测情况】
| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过（7.97s） |
| Ad-hoc 静态验证 | ✅ 73/73 通过 |
| i18n 中英同步 | ✅ 18 键 × 2 语言 |
| 改动文件范围 | ✅ 仅授权文件（9 个） |
| console.log / TODO | ✅ 无残留 |
| API 方法存在性 | ✅ addExtraItem/deleteExtraItem 已定义 |

**需人工验证项**（无浏览器环境）：
- OrderDetail：附加项添加弹窗交互、删除确认、金额重算后最终价格刷新
- OrderDetail：终态订单不显示添加/删除按钮
- TrackOrder：附加项明细 + 最终价格 + 付款节点展示，无画师内部信息泄露
- 4 组件：图片预览不被 transform 截断（需实际点击预览确认）

【可能影响】
- 画师订单详情：新增一个卡片（时间线下方），不影响现有卡片
- 客户进度页：新增价格区域（时间线下方），无数据时不渲染
- 4 组件预览：preview-teleported 将预览层传送到 body，不影响其他交互
- 管理后台：ArtistDetailDrawer 仅加预览属性，无功能变化

【待确认问题】
无

【申请】
申请一号审核合并。
