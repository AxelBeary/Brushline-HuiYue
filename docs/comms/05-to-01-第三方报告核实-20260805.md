# 05→01：第三方报告核实（10 项逐条验证）

> 日期：2026-08-05 · 执行：五号 · 模式：只读审计（未改任何代码）
> 结论速览：**4 属实 · 2 部分属实 · 1 不准确 · 1 误报 · 1 部分属实+新发现 · 1 已知延后项**

## 逐条核实

| # | 报告项 | 核实结论 | 证据（文件:行） |
|---|--------|---------|----------------|
| P0-1 | order-workflow 多步写无事务 | ✅ **属实** | `order-workflow.service.ts` advanceStage L65+L69+L72+L75、rollbackStage L113+L117+L121 均多步写，全文件无 `db.transaction`。中间步骤（refreshInstallmentLocks/logActivity）抛错会留 orders 已更新但锁/日志未写的中间态 |
| P0-2 | /uploads/ 强制 attachment | ✅ **属实（影响面需收窄）** | `app.js` L179-184 setHeaders 全量 `Content-Disposition: attachment`；curl 实测公开图 `content-disposition: attachment`。**修正**：页面内嵌 `<img>` 不受 attachment 影响（浏览器忽略），仅用户直接访问图片 URL（新标签/分享）被强制下载。建议 images/ 公开路径改 inline、references/deliverables 签名路径保持 attachment |
| P1-1 | docker-compose 3000 暴露 | ⚠️ **部分属实（有文档化设计）** | `docker-compose.yml` L18-19 默认 `ports: 3000:3000`；但 L15-17 注释明确「生产部署注释掉 ports 走 Caddy」+ 引用 `docs/开发→生产切换指南.md`。风险=默认值本身不安全（忘了注释即绕过 TLS），建议加 profile 或 expose-only |
| P1-2 | 限流进程内 Map | ✅ **属实（已知延后项）** | `rate-limit.ts` L7 进程内 Map、L54 `buckets.clear()` 触顶全清。**补充**：该条 v0.15 待修复清单已评估「单实例部署可接受，延后」——非盲区；MAX_BUCKETS 全清 = 限流防护失效窗口（第三方「自 DOS」表述不准确，清空是放松不是攻击自己）。建议至少改 LRU 淘汰 |
| P1-3 | Cookie SameSite 未声明 | ❌ **误报** | `auth.routes.ts` L56-62：`sameSite: 'lax'` + `httpOnly` + `secure(NODE_ENV=production)` + `maxAge 7d` 全部显式声明 |
| P1-4 | 首启强依赖 ADMIN_QQ | ✅ **部分属实** | `init.js` L1763-1780 自举依赖 `ADMIN_QQ` 环境变量；漏配 = 无管理员账号 + 生产无 seed = 无人可登录（死锁）。.env.example 有必填标注，但**启动无 fail-fast 校验**，直到登录时才暴露 |
| P2-1 | SQLite 单连接文档边界 | ✅ **属实** | `docs/CONTEXT.md` L62 仅「单体，非微服务」，无数天花板/单实例边界说明 |
| P2-2 | E2E 未设合并门禁 | ❌ **不准确 + 新发现** | `.github/workflows/e2e.yml` 已存在（push/PR 触发，Playwright 5 路径），README L73/L100 声明有据。**新发现**：ci.yml web job 只跑 `eslint + build`，**未跑 vitest**（web 192 单测未进 CI，仅本地跑） |
| P2-3 | search_results.json 误提交 | ✅ **属实** | `git ls-files server/search_results.json` 已跟踪（v0.8 起），AI 搜索产物垃圾文件 |
| P2-4 | changelog 滞后 | ⚠️ **部分属实（数字不准确）** | `docs/changelog.md` 实际到 **v0.36**（非 v0.27）；README L190 引用描述停在 v0.27（更旧）；代码已 v0.38+/v0.39 进行中，滞后 3-4 个版本 |

## 修复建议（按风险分级，待一号派工授权）

**低风险（可直接派）**
- P2-3：`git rm server/search_results.json` + .gitignore 加 `server/search_results.json`
- P2-4：changelog 补 v0.37/v0.38 + README 版本引用更新
- P2-1：架构文档（CONTEXT.md/开发自参考）补 SQLite 单连接边界说明
- P2-2 新发现：ci.yml web job 加 `npx vitest run`

**中风险（方案确认后派）**
- P0-1：advanceStage/rollbackStage 用 `db.transaction()` 包裹多步写（含 refreshInstallmentLocks/logActivity），补事务测试
- P0-2：setHeaders 区分公开路径（`/uploads/images/` → inline）与签名路径（attachment），回归图片显示
- P1-4：启动时校验 ADMIN_QQ（生产 fail-fast）或首启强警告日志
- P1-1：docker-compose 3000 改 profile（`--profile dev`）或 expose-only

**高风险（架构决策，需用户/一号拍板）**
- P1-2：限流改共享存储（Redis）——仅多实例部署需要；单实例继续延后可接受，但 MAX_BUCKETS 全清改 LRU 淘汰应尽快

## 备注

- 本次为只读核实，未改代码、未建分支；测试基线未动
- 报告未覆盖项：P0-1 实际触发频率评估（中间步骤抛错概率低但 REQ-025 守恒逻辑不容中间态）、P1-4 的 seed 环境例外（开发环境有 alice/bob 可登录）
