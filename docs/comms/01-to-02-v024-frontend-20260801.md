# 一号→二号：v0.24-A 前端（2026-08-01）

## 前置条件

**等三号 #10 后端合入 master 后开工**（迁移 v25 + 三态 API）。一号会通知你。

## 任务（按顺序）

### 波 1：#9 档位展示柜（核心，~4-5h）

Spec：`docs/requirements/REQ-013-整理.md` #9 节。

**重做 TplTierGrid.vue**（4 模板共享组件）：

桌面端：
```
┌──────────────┬─────────────────────┐
│ 头像档  ¥50   │                     │
│ 半身档  ¥120 ◀│   [ 完整大图 ]       │
│ 全身档  ¥300  │   不裁切，原比例      │
│              │   工期 7 天           │
│  点哪个看哪个  │   [ 选择此档位 ]      │
└──────────────┴─────────────────────┘
```

移动端：档位标签横排上方，大图下方，左右滑动切换。

关键规则：
- 大图 **不裁切**（width: 100%, height: auto）
- 无示例图时展示区显示档位说明文字（不留空白框）
- showcase 档位：菜单灰色 + "暂不接单"标注，大图正常可看，"选择此档位"按钮 disabled
- hidden 档位：后端已过滤，前端不会收到
- 4 模板统一生效，不做模板级开关
- 配色跟随各模板 palette 变量（--pal-surface / --pal-border / --color-primary 等）

### 波 2：TplGallery grid 改 auto（~30min）

`TplGallery.vue` grid 模式 `.tpl-gallery-img` 从 `height: 200px` 改为 `height: auto`。

### 波 3：#10 管理端三态 UI（~1h）

档位管理页（TierManage.vue）每个档位加三态切换：
- 分段按钮：开 / 只展示 / 不展示
- 调 `PUT /api/artist/tiers/:id/visibility`
- hidden 档位在列表中灰色显示
- 新建档位默认 visible

### 波 4：#6 录单侧边栏入口（~30min）

ArtistLayout.vue 侧边栏"订单管理"下方加"手动录单"菜单项，点击打开录单表单（复用现有抽屉/路由）。

### 波 5：#2 统计卡可点击（~1h）

StatCards.vue 三张卡加 @click → router.push 到订单列表 + 对应状态筛选。加 cursor:pointer + hover 效果。

## 授权文件

- `web/src/components/templates/TplTierGrid.vue`（重做）
- `web/src/components/templates/TplGallery.vue`（grid auto）
- `web/src/views/artist/TierManage.vue`（三态 UI）
- `web/src/components/ArtistLayout.vue`（侧边栏）
- `web/src/components/artist/dashboard/StatCards.vue`（可点击）
- `web/src/api/index.js`（新增 visibility API 方法）
- `web/src/i18n/`
- `web/test/`

## 分支

`feat/v024-frontend`，worktree 用现有的 `../artist-commission-fe`（先切回 master 拉最新再建分支）。

## 风险等级

中（展示柜是组件重做，影响 4 模板客户主页）。完成后写 comms `02-to-01-v024-frontend-{日期}.md`。

## 关键提醒

- TplTierGrid 是共享组件，改完 4 个模板都要看（Classic/Gallery/Folio/Atelier）
- 展示柜的"选择此档位"按钮跳转下单流程，复用现有逻辑
- 移动端滑动切换注意 touch 事件不要和页面滚动冲突
