# 817-C2 画师工具页交互批剩余 13 项 — 交付报告（2026-08-17）

## 结论摘要

本批 13 项全部完成核实：**12 项为「已修销账」**（已被 815 审计六路修复中已合入的 `50449bec`（A 路）/ `0b449f0b`（K2 路）两个提交覆盖，当前分支代码即为终态），**1 项（11-17）核实为非真 bug 如实裁决不修**。本批未再新增任何代码改动，工作区仅保留前序进程的 3 项未提交改动（11-7 / 3-15 / 体验项 16，已确认完整）。

无假修复：全部销账结论均以当前代码 + git 历史 + i18n 键 + 既有回归测试四重证据支撑；11-17 未硬修，给出静态 CSS 变量证据链。

---

## 前序 3 项完整性确认（未重做，仅核查）

- **11-7 留言审核行级 pending 锁**：`GuestbookManage.vue` 已由提交 `50449bec` 的基础防连点（actionBusy）升级为工作区未提交版 `actionBusyId` 行级锁；`approve`/`reject` 函数体均有 `actionBusyId.value = null` finally 收尾与 `actionBusyId.value === msg.id` 重复触发守卫；模板 loading/disabled 均按行绑定。完整。
- **3-15 ManualOrderRight 幂等注释修正**：注释已按后端实际行为（`POST /api/artist/orders/manual` 消费 idempotency-key，scope = manual-order:{artistId}，同 key 重放返回首单结果）重写，含 I6-d 出处。完整。
- **体验项 16 手动录单昵称/QQ 同排**：`ManualOrderLeft.vue` 新增 `.mo-client-row`（grid 两列，`gap: 0 12px`），`@media (max-width: 599px)` 回退单列；参考图上传模块保持原位置（本项只做同排，未做模块换位）。完整。

---

## 13 项逐条核实结论

### 1. 11-2 埋点可见性开关切换失败回滚失效 — 已修销账

- **核实**：`TrackingAnalytics.vue`（约 L26/L120-134）当前为受控绑定 `:model-value="statsMode"` + `@change="onModeChange"`；函数体请求发出前取 `const prev = statsMode.value`（受控绑定下此时仍是旧值，即真正回滚目标），成功以 `res.statsMode` 为准，失败回滚 `prev`。模板注释 A2 明确记录该口径。
- **修法来源**：提交 `50449bec`（A 路，埋点开关回滚受控化）。
- **对照**：与 `PlatformManage.vue` L147-160 `toggleEnabled` 同一「请求前快照 + 失败回滚」模式。

### 2. 11-3 批量粘贴上传一败全停 — 已修销账

- **核实**：`ArtworkManage.vue` `handlePasteArtworkFiles`（L561-589）逐文件 try/catch：单个失败 push `artworks.pasteFailLine`（文件名+原因）不中断后续；成功照常入库并累计 `okCount`，结束后刷新 + 埋点；全成功 `artworks.uploaded`，部分/全败用 `pastePartial` / `pasteFailedAll` + `ElMessageBox.alert` 汇总。上游 `publishArtworkFile`（L361-380）API 失败会抛错，隔离成立。
- **修法来源**：提交 `0b449f0b`（K2 路，批量上传隔离）。
- **i18n**：`zh-CN.js` L1524-1527 / `en.js` L1525-1528 成对。

### 3. 11-4 开稿状态快速切换回滚基准错乱 — 已修销账

- **核实**：`SlotManage.vue`（L27-31/L127-137）`updateStatus`：`statusUpdating` 在途锁（切换期间 radio 禁用防连点）+ 受控绑定 `:model-value="currentStatus"` + 请求前 `const prev = currentStatus.value` 快照；失败回滚 `prev`，成功才更新 `lastKnownStatus`/`currentStatus`。
- **修法来源**：提交 `0b449f0b`（K2 路，SlotManage 回滚）。

### 4. 11-5 水印 logo 模式未传 logo 静默 — 已修销账

- **核实**：`Watermark.vue` `renderPreview` 与 `exportImage` 均在合成前判 `wmType.value === 'logo' && !logo`，未选 logo 时 `ElMessage.warning(t('watermark.logoRequired'))` 并 return，不静默产出/导出。
- **修法来源**：提交 `50449bec`（A 路，水印 logo 缺失明示）。
- **i18n**：`zh-CN.js` L620 / `en.js` L621 成对。

### 5. 11-6 算价器请求失败静默清空预览 — 已修销账

- **核实**：`PriceCalculator.vue` `doCalc`（L188-203）catch 分支保留上次 `preview.value` 不清空，并 `ElMessage.error(t('priceCalc.calcFailed'))` 明示；配合 calcSeq 竞态守卫只让最新请求写预览。
- **修法来源**：提交 `0b449f0b`（K2 路，算价器错误态）。
- **i18n**：`zh-CN.js` L340 / `en.js` L340 成对。

### 6. 11-8 修改次数计数器删除无二次确认 — 已修销账

- **核实**：`RevisionCount.vue`（L105-108）删除按钮已包 `el-popconfirm :title="$t('revisionCount.deleteConfirm')"`，注释 A4 说明不可恢复。
- **修法来源**：提交 `50449bec`（A 路，改稿删除二次确认）；回归测试 `RevisionCount.test.js` 已按 popconfirm 桩更新（点击即确认）。
- **i18n**：`zh-CN.js` L546 / `en.js` L547 成对。

### 7. 11-9 速记删除无二次确认 — 已修销账

- **核实**：`QuickNote.vue`（L93-96）删除按钮已包 `el-popconfirm :title="$t('note.deleteConfirm')"`，注释 A5。
- **修法来源**：提交 `50449bec`（A 路，速记删除二次确认）。
- **i18n**：`zh-CN.js` L369 / `en.js` L369 成对。

### 8. 11-11 老客召回/埋点天数切换无竞态保护 — 已修销账

- **核实**：`ReturningClients.vue`（L54-63）`clientsSeq` 与 `TrackingAnalytics.vue`（L109-118）`summarySeq` 均为项目 seq 模式：`mySeq = ++seq`，响应/错误/finally 全部以 `mySeq === seq` 判定，仅最新请求可写数据与 loading。
- **修法来源**：提交 `0b449f0b`（K2 路，竞态 seq）。

### 9. 11-12 PriceCard 两处绕过 yuanToCents — 已修销账

- **核实**：`PriceCard.vue` L239（copyText）与 L336（drawCard）均写作 `formatYuan(yuanToCents(tier.priceYuan))`，全文件 `rg priceYuan` 无第三处绕过。
- **修法来源**：提交 `0b449f0b`（K2 路，yuanToCents 收口）。

### 10. 11-13 作品编辑保存两请求串行半成功 — 已修销账

- **核实**：`ArtworkManage.vue` `saveArtworkEdit`（L530-551）：第一步（updateArtwork）失败 → `editInfoSaveFailed` 明示、弹窗保持；第一步成功但第二步（setArtworkTags）失败 → `editTagsSaveFailed` 明示 + `await loadArtworks()` 如实回显已保存信息 + return（不关弹窗）；注释已同步「两请求均成功才算成功、半成功刷新回显」真实语义。
- **修法来源**：提交 `0b449f0b`（K2 路，作品编辑原子化）。
- **i18n**：`zh-CN.js` L1521-1522 / `en.js` L1522-1523 成对。

### 11. 11-14 诊断包下载失败整页导航 — 已修销账

- **核实**：`HealthCheck.vue` `downloadReport`（L99-130）改 fetch + blob：同源带 credentials、AbortController 15s 超时、`!res.ok` 时仅 401 走清会话跳登录、其余失败页内 `ElMessage.error`（含 `downloadTimeout` 超时文案）；成功用 Content-Disposition 解析文件名 + ObjectURL 触发下载，模板按钮 `:loading="downloading"` 防重复。
- **修法来源**：提交 `0b449f0b`（K2 路，诊断包 blob 下载，参照 ToolsExport）。
- **i18n**：`zh-CN.js` L1799 / `en.js` L1802 成对（downloaded/downloadFailed/downloadTimeout）。

### 12. 11-22 平台删除确认后无删除中 loading — 已修销账

- **核实**：`PlatformManage.vue`（L201-216/L52/L74-76）：`deletingId` 行级在途锁，确认弹窗通过后才置位；按钮 `:loading="deletingId === row.id" :disabled="deletingId !== null"`；`remove` 内有 `if (deletingId.value !== null) return` 防重复触发，finally 收尾。
- **修法来源**：提交 `0b449f0b`（K2 路，平台删除 loading + 乐观锁核实）。

### 13. 11-17 ThemePicker 色板模块级一次性取值非响应式 — 核实为非真 bug，裁决不修

- **核实**：`ThemePicker.vue`（L62-71）`accents` 确为 setup 一次性读取 `--accent-1..5`（`accentColor()` 经 `getComputedStyle` 取值，非响应式）。
- **裁决依据**：`web/src/styles/theme.css` L98-102 中 `--accent-1..5` 仅在 `:root` 定义一次；全库 `rg --accent-N` 无任何运行时覆写（亮暗/`data-accent`/`data-artist-theme` 只覆写 `--color-primary` 提亮变体，不动 `--accent-N`）。色板展示的是规范色而非当前应用色，静态取值即正确且是预期行为。同模式已见于 `Settings.vue`（ACCENT_PRESETS）与 `ArtistHome.vue`（ACCENT_INDEX），属项目战役 S「五色单源」既有约定。当前不存在 CSS 变量运行时变化场景，无「响应式化」需求；若未来引入变量覆写再改，不属于本批范围。
- **结论**：不修（如实裁决，无硬修）。

---

## 门禁实测

| 门禁 | 结果 | 说明 |
| --- | --- | --- |
| `web npm run check:i18n` | ✅ 通过 | `[check-i18n] OK — 存量违规 13 条豁免，无新增硬编码中文` |
| `web npm run lint`（typecheck + eslint） | ✅ 通过 | `vue-tsc --noEmit` 零错误，eslint 零输出（零错误零警告） |
| `web npm run test:web` | ⛔ 沙箱拦截 | `Error: spawn EPERM`，esbuild 服务进程无法 spawn（node_modules 为 junction，栈指向 `artist-commission\web\node_modules\esbuild`）。重试一次同结果；按任务书未改 vitest 配置、不恋战 |
| `web npm run build` | ⛔ 沙箱拦截 | 同上，vite build 加载配置阶段 esbuild spawn EPERM |
| `web npm run dev` | ⛔ 沙箱拦截 | 尝试启动 dev server 供布局截图，同样在 vite 配置加载阶段 EPERM，无法起服务 |

说明：lint/typecheck/check-i18n 不依赖 esbuild 故全绿；test:web/build/dev 均被沙箱对 esbuild 子进程 spawn 的限制拦截，属任务书已知场景（«沙箱拦截（esbuild spawn EPERM 等）就在报告注明»）。本批无新增代码，未触及后端，未跑 server 门禁与 E2E。

---

## 布局审计自检记录（huiyue-layout-audit）

本批交付物含前序进程完成的体验项 16 布局改动（mo-client-row），按技能执行自检：

- **measure.mjs**（范围 = 3 个本批改动文件）：输出 `{"圆角族数":2,"野生字面圆角数":5,"离栅值数":8,"需人工核验":1,"阻塞":true}`。
- **阻塞项归属核实**：5 个野生圆角（`2px 1px 2px 1px` @ ManualOrderLeft L267、`999px` @ GuestbookManage L265 等）与 8 个离栅值（9/10/14/6/5/3/1px 等）全部位于本批 **未触碰的存量样式行**（`git diff` 逐 hunk 比对确认）；本批新增 `.mo-client-row` 为 grid 布局 + `gap: 0 12px`（12px 在 4px 栅格上），无野生圆角、无离栅间距。按技能护栏「不得为通过测量而删改任务范围外的文件」与任务书「严禁硬修」，不修存量值，如实记录。
- **需人工核验 1 处**：`padding-bottom: calc(10px + env(safe-area-inset-bottom))`（ManualOrderLeft，存量底部安全区适配）——语义正确，非本批引入，保留。
- **截图 + VL 评审**：无法执行。原因：(a) shot-before 无法补拍——布局改动发生于前序进程、先于本会话，无法回到修改前状态；(b) dev server 因 esbuild spawn EPERM 无法启动，screenshot.mjs 无可用页面。按技能要求如实写明，未静默跳过。

---

## 收口说明

- 本批**未新增任何代码改动**（13 项全部为核实销账/裁决），工作区保持前序进程的 3 个文件改动（GuestbookManage.vue / ManualOrderLeft.vue / ManualOrderRight.vue），未 commit，留待一号收口。
- 领地红线未触碰（ArtistLayout/QueueBoardList/AccountSecurity/增项库/OrderDetail/TrackOrder/order-form/greeting/money.js/STATUS.md）；未执行 npm install；无接口契约改动；无 i18n 新增键（复用既有成对键）。
