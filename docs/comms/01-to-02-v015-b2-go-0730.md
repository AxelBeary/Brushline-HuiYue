# 一号 → 二号：批次1 审核通过 + 批次2 开工

> 日期：2026-07-30

---

## 批次1 审核结论：✅ 通过，已合入

R46 删除交互正确（ElMessageBox + 系统备注不显示按钮 + 触屏常驻）；R40 时间线用 el-timeline 实现，类型标识清晰，操作条保持独立；R53 复用 uploadAndSetFocus 完整（上传→addReference→setFocusImage），dragleave 防闪烁处理到位。死键清理（notes/statusTitle）顺手做了，好。

## 批次2 开工

先 rebase：

```powershell
cd "D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02"
git rebase master
```

### 任务

1. **R48 头像上传**：Settings.vue 加 el-upload + 4 模板消费 avatar（无头像显示默认占位）
2. **R49 取色器前端**：Settings.vue el-color-picker（5 色预设）+ 4 模板 CSS 变量注入（`--accent-color`）+ 未设置回退默认色
3. **R50 预览**：Settings.vue "预览主页"按钮 + ArtistHome 参数覆盖 + 预览横幅
4. **R55 档位示例图拖拽**：TierManage.vue 示例图区域 dragover/drop/click，无图直传，有图 ElMessageBox.confirm 再覆盖

### ⚠️ R55 vs R53 行为差异

| | R53 看板焦点图 | R55 档位示例图 |
|---|---|---|
| 覆盖确认 | **不需要** | **需要**（旧图不可恢复） |

不要统一行为。

### 后端接口（已可用）

- `PUT /api/artist/profile` 支持 `accentColor`（5 色白名单 + null）
- `GET /api/artists/:subdomain` 返回 `accentColor`
- 头像上传走现有 `POST /api/upload/image` + `PUT /api/artist/profile` avatar 字段

### 授权文件

`web/src/views/artist/Settings.vue`、`web/src/views/artist/TierManage.vue`、`web/src/views/client/ArtistHome.vue`、`web/src/views/client/templates/ArtistHomeClassic.vue`、`web/src/views/client/templates/ArtistHomeGallery.vue`、`web/src/views/client/templates/ArtistHomeFolio.vue`、`web/src/views/client/templates/ArtistHomeAtelier.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`、`web/src/api/index.js`、`web/src/router/index.js`（预览路由）

### 验收标准

见 REQ-009（R48 四条 + R49 五条 + R50 五条）+ REQ-010 R55（六条）。

### 提交规范

`feat(settings): R48 头像 + R49 取色器 + R50 预览 + R55 示例图拖拽`

完成后 `npm run build` 通过再通知我。
