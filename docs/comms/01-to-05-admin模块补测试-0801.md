# 一号 → 五号：admin 模块补测试（高优先级）

> 日期：2026-08-01
> 优先级：P1（admin 模块零测试，含不可逆操作）

## 任务

你的测试覆盖审计指出 admin 模块是最大风险（35 路由 + 3 service 函数零测试）。补测试。

### 范围

1. **admin.service.js**：getGlobalStats / listRecycleBin / emptyRecycleBin（6~8 用例）
   - 重点：emptyRecycleBin 不可逆，测清空后数据真没了
   - 统计逻辑含多表 JOIN，测边界（空库/单画师/多画师）

2. **admin.routes.js 核心路由**（12~15 用例）
   - transfer（管理员更换）：两码验证流程、第一码消耗后第二码失败不回滚（P2-6）
   - deleteArtist：路由层鉴权 + 软删除响应
   - recycle-bin：列表/恢复/清空
   - profile 代理：以画师身份操作

### 授权文件

- server/tests/admin.service.test.js（新建）
- server/tests/admin.routes.test.js（新建）

### 分支

`fix/admin-tests`（从 master 拉，独立 worktree）

### 验收

- 新增用例全部通过
- 全量回归通过
- 零业务代码改动
- 发现 bug 写 comms 转交，不自己修
