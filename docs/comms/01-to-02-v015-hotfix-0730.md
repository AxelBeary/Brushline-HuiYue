# 一号 → 二号：v0.15 收尾小批次

> 日期：2026-07-30（晚间）
> 分支：`feat/v015-ux-batch`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02`

---

## 两项任务（合计 1h，一个 commit）

### 1. R57 约稿页表单防丢失（45min）

`web/src/views/client/OrderForm.vue`：

- **beforeunload**：表单有内容时（任意字段非空），`window.addEventListener('beforeunload', handler)` 弹浏览器原生确认
- **sessionStorage 草稿**：表单变化时 debounce 写入 `sessionStorage.setItem('orderForm_draft_' + subdomain, JSON.stringify(form))`
- **恢复**：onMounted 时检查 sessionStorage，有草稿则恢复 + ElMessageBox 提示"检测到未提交的草稿，是否恢复？"（恢复/丢弃）
- **清除**：提交成功后 `sessionStorage.removeItem(...)`
- **组件卸载**：onUnmounted 移除 beforeunload 监听

### 2. UI-9 图库闪烁修复（15min）

`web/src/views/artist/OrderDetail.vue` 图库区 el-image：

- 加 `#placeholder` slot：灰色骨架动画（`<div class="img-skeleton" />`）
- `.ref-img` 加 `background: var(--bg-secondary)`（空白时不是纯白）
- 备注附图 `.note-thumb` 同理加背景色（顺手）

### 3. R52 前端补 N 单（5min，等三号合入后）

`web/src/views/artist/Dashboard.vue` 今日统计行：

```
今日新增订单 ¥XXX（N 单） · 今日收入 ¥XXX（N 单）
```

用 `stats.todayNewOrderCount` 和 `stats.todayRevenueCount`（三号会加）。

**注意**：这个依赖三号先合入。如果三号还没合，先做 R57 + UI-9，R52 前端等我通知。

## 授权文件

`web/src/views/client/OrderForm.vue`、`web/src/views/artist/OrderDetail.vue`、`web/src/views/artist/Dashboard.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`

## 提交规范

`feat(client): R57表单防丢失 + UI-9图库闪烁修复`（R52 前端如果一起就追加到 message）

完成后 `npm run build` 通过再通知我。
