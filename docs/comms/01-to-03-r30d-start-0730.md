# 一号 → 三号：R30d 流程状态机 — 开工

> 日期：2026-07-30
> 决策：SPEC-002 审核通过，用户确认 4 项

## 确认结论

1. 固定映射规则（第一=pending，中间=wip，最后=done）
2. 打回显示：节点名 + ↩ 标记（客户有知情权），系统内部 status=revision
3. 新订单自动接入工作流（current_stage_id=第一节点），画师可关闭（设回 NULL）
4. 客户只显示节点名（不显示进度数字）

## 分支

`feat/backend-artist-r30d-stage`

## 授权

`server/src/**`、`server/tests/**`

## 实施内容

按 SPEC-002 执行：迁移 v14 + PUT stage/stage-back 路由 + 映射逻辑 + GET 订单详情/track 新增字段 + 旧接口隔离（有 stage 的订单拒绝 PUT status）+ 新订单自动接入 + 关闭接口（PUT stage 传 null 回退旧模式）

## 验证

vitest 全量通过 + 新增 stage 推进/回退/映射/兼容测试。
