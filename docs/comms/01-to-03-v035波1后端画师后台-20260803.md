# 派工：v0.35 波 1 —— 后端 + 画师后台（三号）

> 分支：`feat/v035-style-unify-backend`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-03`
> 需求文档：`docs/requirements/REQ-024-画风档位统一.md`（全文已审核通过，F1/F2/F5 是你的，F3/F4/F6 前端归二号）
> 验收基线：`docs/specs/SPEC-025-价格管理后台规划.md`（一主四辅结构 + 五条动线 + 六条交互原则，合入前后台整体走查依据）
> 风险等级：**高**（迁移 v37 翻转全量画师数据结构）
> 下一版本号已核实：**v37**（当前最大 v36 multi_style_model，init.js L1288）

## 一号已核实的代码现状（不用重查）

1. `style_sizes` 表（init.js L383-389）：仅 art_style_id/name/base_price/sort_order，**无图/描述/天数字段**
2. `artworks` 表（init.js L63-74）：**无 description 字段**
3. `artists` 表：无多画风开关字段
4. 后台入口：`TierManage.vue`（路由 `/tiers`）一个页面 7 个 tab——档位(tiers)/增项(addons)/倍率(multipliers)/流程(workflow)/折扣码(discount)/画风(artStyles→ArtStyleManager)/增项库(addonTemplates)。**F2 = 把「档位」tab 并入「画风」体系，不是新建页面**
5. 菜单：`ArtistLayout.vue` L170「档位」菜单项 → `/tiers`
6. `orders.tier_id` 外键 `ON DELETE SET NULL`（init.js L120）——F5 删旧档位历史订单自动置空，TrackOrder 有 `tierName || '定制'` 兜底，无需特殊处理
7. 画风 API：`style.routes.ts` PUT /api/artist/art-styles/:id schema 已有 cover_image；`style.service.ts` updateArtStyle 支持部分更新
8. 公开接口：GET /api/public/styles/:subdomain（style.routes.ts L275）返回画风+尺寸，客户端 ArtistHome.vue 消费

## 任务 1：迁移 v37（一次建全，避免二次迁移）

schema 设计（一号预判，你确认或微调，微调在交付说明）：
- `style_sizes` 加 3 字段：`image TEXT`（独立上传路径）、`image_artwork_id INTEGER REFERENCES artworks(id) ON DELETE SET NULL`（从作品集挑路径，删图自动置空）、`description TEXT`、`work_days INTEGER`
- `artists` 加 `multi_style_enabled INTEGER DEFAULT 0`
- `artworks` 加 `description TEXT`（F6 自由描述）
- 新表 `artwork_size_tags`（F6 档位标注多对多）：artwork_id + style_size_id 联合主键，双向 CASCADE（删作品/删尺寸标注自动失效——F6 验收 8）

**F1 两条路径的预判**：独立上传存 style_sizes.image；从作品集挑存 image_artwork_id。渲染优先级：image_artwork_id 有值 → 用作品图（实时引用），否则 image。与 F6 的 artwork_size_tags 是不同语义（一对一 vs 多选标注），分开建不合并。你若认为更简方案可行，交付说明。

迁移要求（大型迁移清单，合入前我逐项审）：
- up() 开头 `copyFileSync(dbPath, dbPath + '.bak.v37')`，try/catch 不阻塞
- CREATE/ALTER 幂等守卫
- **F5 旧模型迁移放 v37 up() 内**（保证每个环境恰好执行一次）：对 art_styles 为零的画师——建「默认」画风（sort_order=0, is_active=1）→ 旧 price_tiers 中 **visible 项**转 style_sizes（只搬 name/price/sort_order）→ **showcase/hidden 直接丢弃**（用户拍板）→ 图/描述/天数不搬。幂等：画师已有 art_styles 则跳过该画师
- 迁移后**不删 price_tiers 表**（orders.tier_id 外键仍指向它）；旧数据留着，客户端 TplTierGrid 兜底去留归二号

## 任务 2：后端 API

1. **尺寸 CRUD 扩展**：POST/PUT style_sizes 接受 image/image_artwork_id/description/work_days（routes schema 声明 + service 处理，**别漏 additionalProperties:false 的三处同步**——schema/keyMap/service 白名单）
2. **公开接口扩展**：GET /api/public/styles/:subdomain 的 sizes 带出 image/image_artwork_id 对应的作品图路径/description/work_days；**多画风开关关闭时只返回默认画风**（排序最前的启用画风，动态顺延——F2 验收 5）
3. **开关端点**：PUT /api/artist/profile 加 multiStyleEnabled 字段（走现有 updateArtist 链路，注意 snake_case 映射）
4. **F6 数据端点**（供二号波 2 消费）：GET /api/public/styles/:subdomain 返回里给 artworks 附带 tags（尺寸 id 数组）+ description——或独立端点，你定，**端点+字段名写进交付 comms**（二号照契约开发）
5. **作品标注 CRUD**：PUT /api/artist/artworks/:id/tags（多选尺寸 id）+ artworks 编辑接口加 description 字段（这是 F6 波 3 的后台 UI 要消费的 API，本波把 API 做掉，波 3 只做 UI）

## 任务 3：画师后台 F2 合并入口 + 开关 + F1 尺寸编辑

1. **TierManage.vue 重构**：删「档位」tab；「画风」tab 升级为「画风与价格」——画风为容器卡片，每个画风下列尺寸行（名字/价格/图缩略/描述/天数摘要），尺寸行可编辑（弹窗加：图片上传 or 从作品集挑 + 描述 + 天数，全部可选）
2. **多画风开关**：入口顶部。关=只有默认画风（最前启用）可编辑、其他灰色不可点；开=全部可编辑。开关状态存 multiStyleEnabled
3. **「从作品集挑」选择器**：弹窗内列该画师作品缩略图供选择（数据走现有 artworks 列表接口）
4. **删旧「增项」tab**（REQ-024 F2 补丁，用户 2026-08-04 拍板）：删 el-tab-pane(name="addons") + AddonManager import（TierManage.vue L81-84、L161），AddonManager.vue 文件可一并删除（唯一消费端就是此 tab，已核实）。旧增项 API（getAddons 等 6 个方法）前端不再调用，后端路由本次**保留不删**（归 v0.36 清账）。旧增项数据废弃不迁移（用户拍板）
5. **菜单文案**：ArtistLayout.vue L170「档位」菜单项文案改为「价格」（i18n 键同步 zh-CN/en；主 tab 文案「画风与价格」）
6. 其他 tab（倍率/流程/折扣码/增项库）**一字不动**（F2 验收 7）

## i18n 分工（防冲突）

locales 文件你这次**可以改**，但只动 `styleManage.*` / `tiers.*` 命名空间（你重构的区域）；`artistHome.*` / `orderForm.*` / `gallery.*` 归二号（他在并行波次改）。两人 rebase 时 locales 若冲突保留双方。

## 验证标准

1. `cd server && npx tsc --noEmit` 0 错 + `npx vitest run` 全绿（666+，新增用例覆盖：开关关闭时公开接口只返回默认画风、尺寸新字段 CRUD、F5 迁移幂等复跑）
2. `cd web && npx eslint . && npm run build` 通过（用 `node node_modules/vite/bin/vite.js build`）
3. 迁移验证：容器内重建跑 v37，carol（旧模型）出现「默认」画风 + 3 尺寸（showcase 若有则丢弃）；alice 开关关→客户端只见默认画风
4. **交付 comms 必须写清给二号的 API 契约**（端点 + 字段名 + 开关行为）

## 交付

comms：`docs/comms/03-to-01-v035波1后端-{日期}.md`。F5 迁移是高风险项，交付里写明回滚方案（DROP/ALTER 清单 + bak.v37 恢复路径）。
