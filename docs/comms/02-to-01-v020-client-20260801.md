# v0.20 客户前端批次交付

- **角色**：二号（客户页面前端）
- **工作分支**：`feat/v020-client`（worktree: `workspace/artist-commission-wt-02`）
- **任务编号**：v0.20 客户前端批次（01-to-02-v020-client-20260801.md）

## 修改模块

EP 按需引入 / vue-i18n 升级 / vuedraggable 核实 / Bug 3 重做 / P2-11 / P2-12

## 修改内容

| # | 项 | Commit | 结果 |
|---|---|---|---|
| 1 | EP 按需引入 | `7b0c57f` | unplugin-vue-components 自动注册 el-* 组件 + v-loading 手动注册 + 移除 `app.use(ElementPlus)`。main JS **1,292.94 → 396.37 kB**（gzip 434 → 147 kB），EP 组件拆入按路由懒加载的独立 chunk |
| 2 | vue-i18n v9→v11 | `999596f` | 11.4.8，测试 87/87 通过，main JS 396 → 388 kB（v11 更轻） |
| 3 | vuedraggable 动态引入 | —（无需改动） | **核实已完成**：179kB umd chunk 在基线构建中已是独立懒加载 chunk（QueueBoard/TierManage 等页面级代码分割自动拆出），不在主包内。main chunk 中的 "vuedraggable" 字符串仅为 Rollup preload 清单的文件名引用 |
| 4 | Bug 3 ThemeToggle 位置重做 | `15d21b5` | OrderForm + TrackOrder 的 `.page-prefs` 从"内容区顶部右对齐"改为**右下角固定 FAB**（`position: fixed; right/bottom: 16px`），与 4 模板主页 ThemePicker 的 R25/C37 FAB 模式完全一致（胶囊容器 + 阴影 + hover 增强）。OrderForm 摘要卡是 `top: 24px` 顶部粘性、TrackOrder 无固定元素，均不与底部 FAB 冲突 |
| 5 | P2-11 404 渲染首页 | `1b83ddd` | catch-all 路由本已渲染 LandingPage（router/index.js:41），缺的是 404 提示。LandingPage 新增 `isNotFound`（`route.name === 'NotFound'`）检测 + 警告色提示条（404 + "你访问的页面不存在，已为你返回首页"），正常首页不显示 |
| 6 | P2-12 SEO meta | `70b24f4` | index.html 补 description + og:title/description/type/site_name（中文静态兜底，页面标题由路由守卫动态更新） |

## 涉及文件

- `web/vite.config.js` — Components 插件 + ElementPlusResolver（importStyle: false，样式仍走全量 CSS）
- `web/src/main.js` — 移除全量注册，`app.directive('loading', ElLoading.directive)`
- `web/package.json` / `web/package-lock.json` — +unplugin-vue-components（devDep），vue-i18n ^9.14.5→^11.4.8
- `web/src/views/client/OrderForm.vue` / `TrackOrder.vue` — Bug 3 FAB CSS
- `web/src/views/client/LandingPage.vue` — 404 提示条（模板 + computed + CSS）
- `web/src/locales/zh-CN.js` / `en.js` — +`landing.notFoundHint`（纯新增，未改既有键）
- `web/index.html` — SEO meta

## 是否修改非客户前端文件

- `web/src/locales/zh-CN.js` / `en.js`：隐含授权（纯新增键，i18n 最小 diff 只加不改成文）
- `web/vite.config.js`：派工授权范围为"web/ 全目录"
- 未改 `server/`、`docs/`（本 comms 除外）、`.env`

## 接口依赖

无。本批次纯前端，零后端改动。S5 额度池客户端展示等三号后端完成后另起任务。

## 自测情况

| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告（每步验证 + 最终验证） |
| 前端测试 | ✅ 87/87（5 文件，每步验证 + 最终验证） |
| Vite build | ✅ 通过，main JS 388.64 kB / gzip 145.50 kB |
| console.log / TODO / v-html 残留 | ✅ 改动文件零残留 |
| i18n 中英同步 | ✅ notFoundHint × 2 语言 |
| 改动文件范围 | ✅ 仅授权文件（10 个文件全部在 web/ 内） |
| 分支状态 | ✅ 5 commits 均在 feat/v020-client，基于 master 8390b2e |

**构建产物对比**：

| 产物 | 基线 | 最终 | 变化 |
|------|------|------|------|
| main JS | 1,292.94 kB (gzip 434.10) | 388.64 kB (gzip 145.50) | **-70%** |
| main CSS | 470.87 kB (gzip 92.85) | 470.87 kB（未变） | 全量 CSS 保留 |
| vuedraggable chunk | 178.97 kB（独立懒加载） | 同（未变） | 已是最优 |

**需人工验证项**（无浏览器环境）：
- EP 按需引入后各页面 el-* 组件渲染正常（重点：el-dialog/el-drawer 等弹出层、el-table、el-form 校验提示、v-loading 遮罩）
- 暗色模式下 EP 组件样式正常（dark/css-vars.css 仍全量引入）
- OrderForm / TrackOrder 右下角 FAB 位置视觉确认（不遮挡内容、不与移动端键盘冲突）
- 访问不存在 URL（如 /abc123）时 404 提示条显示，正常首页不显示
- 中英文切换后 404 提示条文案正确
- vue-i18n v11 下语言切换、`{参数}` 插值、`$tm()` 复数形式正常

## 可能影响

- EP 按需引入是全局性改动，理论上所有页面都受影响（但组件解析是编译期自动的，运行时行为不变）
- CSS 未做按需（保留全量 470kB）——JS 按需已达成主要目标，CSS 按需（importStyle: 'css'）有样式覆盖顺序风险，建议 v0.21 单独评估
- `unplugin-vue-components` 设为 `dts: false`，不生成 components.d.ts（项目无 TS）

## 待确认问题

1. EP CSS 按需是否排 v0.21？（当前全量 CSS 470kB，gzip 93kB，优化空间约 60%）
2. S5 额度池客户端展示：等三号后端合入后开工，展示位置建议画师主页状态徽章附近（"本月还剩 X 席"）

## 申请

申请一号审核合并。
