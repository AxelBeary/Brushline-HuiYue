# 04-派工：前端纪律性扫 bug/屎山（只读深度审计）

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：四号（requirements）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w9`
> 分支：`beta/audit-frontend`
> 交付报告：`docs/comms/04-to-01-前端审计-20260808-交付.md`（写完 commit 进分支）

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w9'
git merge master --no-edit
```

## 铁律（最重要）

- **只读审计**：发现 bug/屎山 → 写报告，**不改任何代码**（改代码 = 打回）
- 每条发现带 **文件:行号** 证据 + 现象 + 触发条件 + 影响 + 建议修复
- 拿不准的标「🟡 需验证」，不硬判
- **对照已知正确模式别误报**：客户端埋点 track.js 的 localStorage 存匿名凭证 = 既有设计（标注「已知」不算问题）；check-i18n 豁免的 9 条存量硬编码中文 = 已知
- 报告 commit 到分支（纯报告零代码）

## 审计范围（web/src/ 全量，5 区 20 项）

### A Vue 响应式
1. ref/reactive 误用（该 ref 用 reactive / 深响应改浅）
2. **异步竞态**（慢请求覆盖快请求 / 卸载后 await set state——重点查列表页切换）
3. watch 死循环 / 监听大对象（deep:true 大数组）
4. v-for 无 key 或 key 不稳（index 当 key + 列表会变序）
5. v-model 绑定只读 computed（会报错或静默失败）

### B 交互数据
6. 前端校验 ≠ 后端子集（**v0.36 教训**：前端自行收紧 = bug。查金额/日期/长度校验是否比后端严）
7. 金额 / 日期（分元混用、toFixed 精度、日期时区偏移）
8. 静默 404 / loading 卡死（请求失败无提示且 loading 不结束）
9. 分页越界 / 页码不重置（切筛选后页码仍旧）
10. localStorage 读写无 try-catch（隐私模式抛错 → 白屏）

### C 内存性能
11. onMounted 加监听/定时器无 onUnmounted 移除（**全库 grep**：setInterval / addEventListener / IntersectionObserver / setTimeout 泄漏）
12. 大列表无虚拟滚动（全量渲染千条级）
13. 无节流防抖的滚动 / 输入监听

### D 安全
14. token 明文存 localStorage（查现状——既有设计标注「已知」，只记不改）
15. 用户输入未转义 v-html（XSS——重点查留言/回复/须知渲染）
16. 路由守卫缺失（需登录页无 requiresAuth）

### E 死代码 / i18n
17. 未用 import / 组件 / 样式（eslint 抓不到的）
18. 模板用了不存在 i18n key（**双向核对**：模板引用路径 a.b.c 在 locale 里存在，不是只在别的父路径下有同名后缀——08-08 管理后台教训）
19. 硬编码中文残留（对照 check-i18n 豁免清单区分新增/存量）
20. 重复代码（同逻辑多份实现）

## 输出格式（交付报告）

```
# 前端审计报告
## 一、发现汇总表
| 级别 | 数量 | 摘要 |
|------|------|------|
| 🔴 真bug/安全 | n | ... |
| 🟡 隐患/边界 | n | ... |
| 🟢 轻微 | n | ... |
| ✅ 已查无问题 | n | 分区 |

## 二、🔴 逐条详情（文件:行号 + 现象 + 触发 + 影响 + 建议）
## 三、🟡 逐条详情（同上）
## 四、🟢 轻微列表
## 五、✅ 已查无问题分区（分文件列「查了哪些，确认无误」）
```

## 审计重点文件（按价值排序）

- **最新批次重点**：`web/src/views/artist/ClientsPage.vue` / `ReturningClients.vue` / `Watermark.vue` / `PuzzlePage.vue` / `ScheduleSharePage.vue` / `web/src/utils/watermark.js` / `components/artist/SliderSwitch.vue`（如已合入）
- `web/src/views/artist/QueueBoard.vue` + `components/artist/queue/`（时间条/月历）
- `web/src/views/artist/ManualOrder.vue` + `components/artist/order/`
- `web/src/views/artist/Dashboard.vue` + `components/artist/dashboard/`
- `web/src/views/client/TrackOrder.vue`（客户查单）
- `web/src/components/ArtistLayout.vue` / `App.vue` / `router/index.js` / `stores/`

> 已知正确模式（不算问题）：track.js localStorage 存匿名凭证（既有设计）；check-i18n 存量 9 条豁免；客户端 html.dark vs 后台 data-artist-theme 双系统（既有设计）。

## 交付要求

1. commit 描述中文（如 `docs(audit): 前端只读深度审计报告`）
2. 交付报告写 `docs/comms/04-to-01-前端审计-20260808-交付.md`
3. 结束前：`git status --short` 确认干净（无代码改动）
4. **不推送、不合并、不改 STATUS**
