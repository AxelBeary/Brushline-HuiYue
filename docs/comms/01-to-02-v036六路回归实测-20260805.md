# 派工：二号 v0.36 六路回归实测（零代码改动）

> 一号派工 · 2026-08-05 · 风险等级：低（纯实测，不改代码）
> 工作目录：`D:\Hermes Agent CN Desktop\workspace\artist-commission-webguard`（worktree，分支 `v036-web-guard`，备用——发现 bug 也不要在本轮修，只报告）
> **开工第一步：`git merge master` 再读本文件**

## 任务：替用户预验收 v0.36 六路修复

用户终验还没做。你把六路修复的验收路径全部实测一遍，产出实测报告——通过的让用户终验时放心点，不通过的立刻报一号（别自己修，可能与其他在途任务冲突）。

### 实测路径清单（对照六路修复）

1. **收款区归零（B1）**：订单详情 → 改价 → 收款区数据保持；加增项 → 收款区数据保持；删增项 → 收款区数据保持
2. **收款三连（P 批）**：收款弹窗录负数顺畅（输入框不卡）；负数不带原因提交 → 报错提示；负数超已收 → 报错；正数多收 → 显示「多收 ¥X」橙色
3. **约稿表单跳步（F1）**：客户下单流程 → 增项步骤点下一步 → 「写需求」步骤不丢（客户端页面实测）
4. **拖拽守卫 + 录单价格（G1/G2）**：录单页选档位 → 加增项 → 价格跟随计算；手动输价 → 再加增项 → 手输价保留；页内图片拖向上传区 → 被拦截（至少测录单页和订单详情两处）
5. **看板交付防手滑（H1）**：看板「已交付」操作 → 弹交付窗，不直接改状态
6. **订单详情四项（T 批）**：日期 picker 改日期；备注输入 Enter 连敲不重复；推进按钮连点不重复提交

### 实测通道（技能已验证的最短路径）

1. 后端用容器（commission-web 占 3000），本地 vite 起 worktree 的 web/（`npm run dev`，5173），proxy 自动通容器
2. 登录：/login → QQ 10001（Alice）→ 获取登录码 → `docker logs commission-web --tail 5 | Select-String 登录码`
3. 客户端路径用无痕窗口访问画师主页
4. 合成拖拽测试先 patch setPointerCapture（playwright-ui-diagnosis 技能）
5. **实测改了演示数据（拖日期/录单等）必须在报告里逐项披露**，能恢复的恢复

### 交付

实测报告写本 worktree 的 docs/comms/，格式：每条路径 → ✅/❌ + 证据（截图路径或现象描述）+ 演示数据改动披露。发现的 bug 单独列「问题清单」段，标注复现步骤，**不修**。完成后转交一号。

## 授权

只读 + 浏览器操作。worktree 里不改任何代码文件（git status 交付时必须干净）。
