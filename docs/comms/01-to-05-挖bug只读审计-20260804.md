# 派工：存量代码挖 Bug（五号）——只定位不修

> 日期：2026-08-04（用户口头安排，五号已开工，本文件补方向与禁区）
> 模式：**只读审计 + 定位报告**，不改代码，不建分支
> 交付：`docs/comms/05-to-01-挖bug报告-{日期}.md`（可直接 commit 到 master，docs-only 模式同四号）

## 挖掘方向（按价值排序，都在 v0.35 不碰的稳定区）

1. **订单/收款链路**：payments 分期三态、额度池 paid_total_cents、workflow 推进与 installment 联动——此前 T3 确认时发现过 installment status 永远 pending 的 BUG 模式，同类再扫一遍
2. **snake_case 映射缺口**：新 API 端点返回值是否有 camelCase 映射（对照 GET /api/artist/orders 模式），重点查 v0.29 之后的新路由
3. **公开路由守卫一致性**：`/api/public/*` 新路由是否都有 hidden 画师 404 过滤（对照 artist.routes.ts）
4. **i18n 缺键**：后端 errors.ts 全部错误码 vs locales errors.* 键逐一对照（五号你刚修过 STAGES_RESET_BLOCKED 缺键，同类可能还有）
5. **种子/演示数据 vs 表结构**：demo-data.ts INSERT 列 vs 表全列对照（width/height/deadline/queue_position 都栽过，v37 迁移会再改表，现在先把存量缺口找全）

## 禁区（波 1/波 2 正在重写，挖了也被覆盖）

- 三号波 1 域：style.service / style.routes / TierManage.vue / ArtStyleManager.vue / init.js 迁移段
- 二号波 2 域：OrderForm.vue / ArtistHome.vue / gallery 组件 / TplTierGrid / useOrderForm
- 发现这些区域的 bug 也记下来（标「v0.35 重构区，合入后复查」），但不花时间深挖

## 交付格式（你的标准结构）

每个 bug：现象 → 根因（代码证据，行号）→ 修复方案 → 风险等级 → 涉及文件。末尾按风险分级汇总。定位完等我派授权再动手修。
