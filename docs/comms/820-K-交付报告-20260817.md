# 820-K 交付报告：作品管理页重新优化

> worktree：`d:\Hermes Agent CN Desktop\workspace\820-k`（分支 `feat/820-artwork-polish`）
> 日期：2026-08-17 ｜ 状态：改动留在工作区，门禁与截图待一号收口补跑

## 一、现状读码结论

- 目标页 `web/src/views/artist/ArtworkManage.vue` 在 818-H 已具备「批量管理 / 图片上传」两张分组卡片与「说明在左、控件在右」行结构，但上传区仍是 EP 默认 dragger（灰色虚线、无纸墨感），主图区与作品网格是裸图列表（无卡片、无标题行、无呼吸间距），空态是 EP 默认 el-empty，批量栏是素白胶囊，编辑弹窗是 label-top 表单。
- 页面没有筛选区（作品列表无筛选项），故本批不做 tab/筛选重构；主图与作品列表是互补视图（F7 去重），不适用 TierManage 式互斥 tab，保留「分区标题」结构并在方案中说明。
- 存量测量阻塞：`border-radius: 999px` ×2、间距 `10px`/`2px`（tag 与批量栏），本批一并清零。

## 二、方案与改动

只动 `ArtworkManage.vue`、其弹窗（批量删除/编辑）与两处 locale 文件（zh/en 成对新增键，仅 `artworks` 块内）。

1. **工具条/上传区分组卡片**：组头「管理工具」+ 批量管理行、图片上传行保持「说明在左、控件在右」；上传 dragger 纸墨化——paper2 底、line2 虚线、花青 hover 边框，圆图标（hq-t 底 50% 圆）+ 上传文案 + Ctrl+V 粘贴提示居中。
2. **作品网格卡片化**：每张作品成为卡片（card 底 + line 描边 + r-m 圆角 + sh-1 阴影 + 8px 内距 + 图片 1:1），下方加标题行（空标题显示「未命名作品」），hover 只动边框颜色（花青），克制动效；主图卡同款但以藤黄描边表达主图语义。
3. **分区标题**：主图 / 作品列表用「朱砂小印点 + 文楷标题」分区头（与 QuickNote/Watermark 组头同语言）。
4. **空态/加载态纸墨化**：空态改自绘卡片（Picture 图标 + 标题 + 功能陈述句，虚线边框）；加载遮罩改 paper 色半透明 + 花青 spinner，加载中有 min-height 防白屏。
5. **批量操作栏**：卡片底 + line2 描边 + sh-2 阴影 + 胶囊（r-pill token），已选计数为花青软底徽章。
6. **弹窗**：两个弹窗加 `artwork-dialog` 类（r-l 圆角、标题加粗）；编辑表单改 label-left 一行一事（标题/自由描述/档位标注三行，说明在左控件在右）。

## 三、新增/修改文案（i18n zh/en 成对）

| 键 | zh-CN | en |
| --- | --- | --- |
| `artworks.manageGroup` | 管理工具 | Manage |
| `artworks.galleryTitle` | 作品列表 | Artworks |
| `artworks.untitled` | 未命名作品 | Untitled artwork |
| `artworks.emptyHint` | 上传图片后，作品会展示在这里 | Uploaded images will appear here |
| `artworks.empty`（改） | 暂无作品 | No artworks yet |

新增文案均为功能性陈述（人话一句），无说教腔。

## 四、自检与门禁

### measure.mjs（已跑，全绿）

```
node ...\measure.mjs <worktree> --files web/src/views/artist/ArtworkManage.vue
== 圆角族 == 族: token角、圆
== 间距栅格 == 全部落在 4px 倍数栅格
== 需人工核验 == 无
== 汇总 == {"圆角族数":2,"野生字面圆角数":0,"离栅值数":0,"需人工核验":0,"文件数":1,"阻塞":false}
```

存量阻塞（999px/10px/2px）已清零，无需人工核验项。

### 前端门禁（本 worktree 实测）

| 项 | 结果 |
| --- | --- |
| `cd web && npm run lint`（含 vue-tsc typecheck + eslint） | ✅ 通过（eslint 零错误） |
| `cd web && npm run check:i18n` | ✅ OK — 存量 13 条豁免，无新增硬编码中文 |
| `cd web && npm run test:web` | ⛔ 沙箱拦：`Error: spawn EPERM`（esbuild service 无法 spawn，vitest 启动即挂） |
| `cd web && npm run build` | ⛔ 沙箱拦：同上 `spawn EPERM`（vite 加载配置阶段挂） |
| dev server（截图前置） | ⛔ 沙箱拦：同上 `spawn EPERM` |

EPERM 证据：`at ensureServiceIsRunning ... node_modules/esbuild/lib/main.js:1978:29`；与 STATUS v119 记录现象一致（沙箱拦 vitest/build）。已按任务书「勿改配置勿恋战」，由一号在主仓态补跑。

### huiyue-layout-audit 自检循环说明

- **修改前截图**：无法产出——本沙箱无运行中的服务、无 DB/环境文件，且 vite dev server 被 spawn EPERM 拦截（见上），页面需登录态，screenshot.mjs 无鉴权能力。已实测尝试，如实记录，非静默跳过。
- **VL 评审**：因截图无法产出未执行；待一号在可运行环境补截图（shot-before / shot-round1）后按清单跑 C1-C4 / S1-S5。
- **确定性测量**：已跑且零阻塞（见上），这是本批最硬的自检依据。

## 五、范围与红线核对

- 未触碰：Preferences/SlotManage/QueueBoard、ArtistLayout、客户端主页、留言统计、其余页面、greeting、money.js、STATUS.md。
- 未改作品数据契约（API/字段零改动）；相关测试 `ArtworkManage.race.test.js` 仅用 `wrapper.vm` 断言，无 DOM 选择器依赖，模板结构调整不破坏测试（门禁待一号补跑坐实）。
- 无 `any`/`ts-ignore` 新增；未执行 npm install。
- locale 文件只改了 `artworks` 键块（zh/en 成对），属本页文案纪律的必要落点。

## 六、留给一号收口的清单

1. 主仓态补跑：`web` 全量 `lint` / `test:web` / `build`（当前 worktree 均被 EPERM 拦）。
2. 可运行环境补 huiyue-layout-audit 截图序列与 VL 评审（shot-before.png 必补）。
3. 收口提交/合入（本 worktree 未 commit，改动留在工作区），STATUS 更新由一号执行。
