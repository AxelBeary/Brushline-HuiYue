# 交付：低垂果实批（屎山 TOP10 低成本高收益项）

> 分支：`beta/low-hanging-fruit` · worktree：`artist-commission-w16`
> 依据：派工 `docs/comms/01-audit-低垂果实批.md` + 审计报告 `docs/comms/审计-屎山与不优雅代码-20260806.md`
> 完成日期：2026-08-06 · 验证：server vitest 930/930 ✓ · web vitest 215/215 ✓ · eslint 相关文件 0 错误 ✓

---

## 任务 1：P0-1 裸 catch 吞错（server 14 处清单位）✅

**改动**（commit `502a354`，5 文件 17+/10-）：
- `server/src/app.ts`：GC 移入回收站失败（L97）、SENTRY DSN 解析失败（L146）、Sentry release 版本读取失败（L214）→ `catch (err)` + `app.log.warn`；L105 `rmdirSync` 目录非空为预期行为，保留并已有注释
- `server/src/index.ts`：L18 `db.close()` 失败 → `app.log.warn`（此前静默）
- `server/src/shared/file-sign.ts`：L52 token payload JSON.parse 失败、L72 decodeURIComponent 失败——**两者原本已 fail-closed**（失败 → null/false → 拒绝访问），按施工图"已 fail-closed 则只补日志"，补 `console.warn`
- `server/src/features/platform/platform.service.ts`：L36/L104/L182 脏数据 JSON.parse → `console.warn`（L182 补日志后仍 continue）
- `server/src/shared/utils/platform.ts`：L142 脏数据 → `console.warn`；L68/L134 为"throw 业务错误/return null 归其他平台"的非吞错设计，保留并补注释说明

**残留统计**：全局 `catch {` 残留 13 处 = 3 处有正当理由保留（app.ts:105、platform.ts:68/134，均已注释）+ **10 处审计清单外**（health.service.ts:139、artist.service.ts:243/422/564/583、upload.routes.ts:98/128、order-gallery.service.ts:265/289、style-pricing.service.ts:152）——不在本批审计位置清单内，未动，建议下批处理。审计清单 14 处全部处理完毕。

## 任务 2：P0-6 队列全量拉取无分页 ✅

**改动**（commit `b0551e4`，order-queue.service.ts 24+/4-）：
- `getArtistQueue(artistId, { limit, offset } = {})`：`o.*` → **显式列清单**（orders 全部业务列，去掉 `quote_snapshot` 大字段——已 grep 确认 QueueBoard 不消费；`description` 保留，QueueBoard L107 截断展示订单描述，不能去）
- 可选 `LIMIT ?`/`OFFSET ?`，**默认全量**——调用方仅 `order.routes.ts:394`（QueueBoard 拖拽排序需全量），分页不破坏现有 UI
- 内部 `reorderQueue` 调用不受影响

## 任务 3：P0-7 v-for 无 key ×4 ⚠️ 审计过期（无需改动）

**核查结论**：四处全部**已带稳定 key**（应为此前批次修复，审计报告行号过期）：
- `TplTierGrid.vue:8` `:key="tier.id"` ✓
- `TplStyleGrid.vue:9` `:key="style.id"` ✓
- `TplGallery.vue:141` `:key="art.id"` ✓
- `TplHero.vue:75` `:key="art.id"` ✓
本批零改动，eslint 复查 4 文件 0 错误。

## 任务 4：P0-9 硬编码中文 i18n ✅

**改动**（commit `0b18104`，6 文件 61+/29-）：
- `MultiplierManager.vue` 整组件模板+script 文案 → `$t()`（22 个文案点：用途/加急倍率标题与提示、编辑/删除确认、空态、添加按钮、弹窗标题、表单 label/placeholder/hint、取消/保存/创建、4 条 ElMessage），新增 `useI18n`
- `ManualOrder.vue:38` `aria-label="上传参考图"` → `$t('manualOrder.uploadRefLabel')`
- `Login.vue:10` `alt="绘约"` → `$t('login.logoAlt')`
- `zh-CN.js`/`en.js`：新增 `multiplier.*` 命名空间（24 键）+ `manualOrder.uploadRefLabel` + `login.logoAlt`，双语齐备（en 无中文）
- 附带 `web/package-lock.json` license 字段同步（MIT→AGPL-3.0，npm install 产物，与 package.json 对齐）

## 任务 5：P0-10 迁移备份逻辑复制粘贴 ×12 ✅（实际 13 处）

**改动**（commit `9f0193c`，init.js 29+/127-，净减 ~98 行）：
- 顶部新增 `backupDbBeforeMigration(version)` 工具函数（文件名 `dbPath.bak.vN`、成功/失败日志格式与原实现逐字一致）
- 13 处复制粘贴（v11/v12/v18/v19/v20/v21/v22/v23/v24/v36/v37/v38/v39——施工图写 12，实查 13）→ 一行调用
- 验证：`Select-String '📦 迁移'` 仅剩 1 处函数定义 + 2 处非备份日志（v38 CHECK 重建日志、迁移应用日志）；迁移 v36-v43 测试全绿

## 任务 6：P1-3 SESSION_SECRET 开发默认值随机化 ✅

**改动**（commit `e3c12bd`，auth.service.ts 6+/3-）：
- 开发环境未设 `SESSION_SECRET` 时：`'dev-secret-change-in-production'` 固定值 → `crypto.randomBytes(32).toString('hex')` 每次启动生成 + `console.warn` 提示（提示语更新为"已生成随机开发密钥"）
- **生产 fail-fast 不动**（NODE_ENV=production 无 secret 仍抛错）；`SECRET` 为模块级常量，sign/verify 同实例一致
- 测试不受影响：vitest 配置已显式设 `SESSION_SECRET='test-secret-key-for-vitest'`，走已设置分支；TC-A-18 过期 token 用例保持有效

---

## 验证汇总

| 验证项 | 结果 |
|---|---|
| server vitest（w16/server） | **930/930 全绿**（59 文件，43.15s） |
| web vitest（w16/web） | **215/215 全绿**（13 文件，2.80s） |
| web eslint（9 个相关文件） | 0 错误 0 警告 |
| server eslint | server 项目无 eslint 配置（0 errors，仅 ignored 提示） |
| locales `node --check` | zh-CN.js / en.js 语法通过 |
| `catch {` 残留 | 审计清单 0 残留（3 处有正当理由保留已注释）；审计外 10 处遗留待下批 |
| init.js 备份函数 | 1 函数 + 13 调用，无复制粘贴残留 |

## Commit 清单（beta/low-hanging-fruit，均在 master 72ac499 之上）

| Hash | 内容 |
|---|---|
| `502a354` | beta: P0-1 裸catch记日志 + file-sign fail-closed 补日志 |
| `b0551e4` | beta: P0-6 队列显式列 + 可选分页 |
| `0b18104` | beta: P0-9 硬编码中文 i18n |
| `9f0193c` | beta: P0-10 迁移备份抽函数 |
| `e3c12bd` | beta: P1-3 SESSION_SECRET 随机化 |

## 卡点 / 遗留

1. **P0-7 审计过期**：4 处 v-for 均已带 key，本批无改动（报告如上）
2. **审计外 10 处裸 catch**（health/artist/upload/gallery/style-pricing 服务）不在本批位置清单内，未动——建议下批按同模式处理
3. 未推送、未合并，全部改动在 `beta/low-hanging-fruit` 本地分支
