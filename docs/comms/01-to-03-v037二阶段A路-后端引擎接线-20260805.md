# 派工：三号 REQ-025 第二阶段 A 路——后端引擎接线（高风险）

> 一号派工 · 2026-08-05 · 风险等级：**高**（计价核心接线 + 不留双轨切流）
> 工作目录：`D:\Hermes Agent CN Desktop\workspace\artist-commission-v037-backend`（worktree，分支 `v037-phase2-backend`，已建好）
> **开工第一步：`git merge master` 再读本文件 + `docs/requirements/REQ-025-动态节点计价模型.md`（R1~R13 + 第八节草案）**
> 引擎已合入 master：`server/src/features/pricing/pricing-engine.ts`（7 纯函数，签名见下），直接 import 复用，**不要重写引擎**。

## 引擎函数签名（第一阶段已交付，照此调用）

```ts
import { sumEntryDeltas, allocateInitial, computeLockedState, allocateDelta,
         deriveInstallmentProgress, applyRefund, assertConservation } from '../features/pricing/pricing-engine.js'
// EngineInstallment: { id?, label?, sortOrder, basisPoints, amountCents, paidCents? }
allocateInitial(installments, totalCents) -> number[]                       // R3 初始分配
computeLockedState(installments, paidTotalCents, completedStageIndex=-1, prevLockedFlags?) -> { lockedFlags, paidCents, reasons }  // R4
allocateDelta(installments, lockedFlags, deltaCents) -> { amountsCents, allocationsCents, extraChargeCents, extraRefundCents }  // R5/R6/R10
deriveInstallmentProgress(installments, paidTotalCents) -> { paidCents, remainingCents }[]  // R7/R8
applyRefund(installments, lockedFlags, refundCents) -> { amountsCents, extraRefundCents }   // R9
assertConservation(input) -> void  // R11，失败抛 AppError(PRICING_CONSERVATION)
```

## 一号已钉死的设计决策（照做，不要另起方案）

1. **locked 持久化 = 迁移 v40**：`order_payment_installments` 加两列 `locked INTEGER NOT NULL DEFAULT 0` + `locked_reason TEXT`（completed/paidOff/prev）。**这是 ALTER TABLE ADD COLUMN，无 DROP/RENAME，事务内安全**（对照 v38 教训：只有重建父表才需事务外）。引擎 `computeLockedState` 的 prevLockedFlags 从库里 locked 列读入，算完把新 lockedFlags 写回库。
2. **条目账本是总价真相源**：`orders.final_price_cents` 保留为冗余缓存 = Σ 条目 delta，每次变动后跑 assertConservation 断言 A1，不守恒即回滚拒绝。
3. **recalcInstallmentAmounts 退役**：现有三处调用（order.service.ts L615 updateFinalPrice / L728 addExtraItem / L773 deleteExtraItem）全部改走引擎 `allocateDelta`（只摊未锁节点），**删除 recalcInstallmentAmounts 函数本体**（R12 不留双轨）。
4. **增项双写**：order_extra_items 表保留（UI 展示层不动），但 addExtraItem/deleteExtraItem 每次同步写 order_price_entries 条目（正=extra_item，删=冲正条目 refund_item，**条目只追加不物理删**，R1）。
5. **done 守卫（R13）**：updateFinalPrice 对 done 关闭无痕改价（抛错引导走条目）；delivered/cancelled 五号已拦不动。addExtraItem 对 done 允许（半终态加钱）。
6. **守恒挂载**：createOrder/updateFinalPrice/addExtraItem/deleteExtraItem/addPayment/promoteOrder 每个变动出口前跑 assertConservation。

## 任务清单（按序，每完成一项立即 commit + 跑测试，防预算耗尽丢失）

### 必做（最小闭环，让「已收节点不再变」真正生效）
1. **迁移 v40**：installments 加 locked/locked_reason 列 + init.js schema 同步 + 测试
2. **createOrder 接引擎**：写 base 条目（type=base, delta=总价）+ 分期生成改用 `allocateInitial`（合并现有两处内联 INSERT：L286-291 画风分支 / L294-299 priceCalc 分支 → 一处）
3. **recalcInstallmentAmounts 退役**：三处调用改 `allocateDelta`（读库 locked 列→引擎→写回 amount_cents + locked）；删除函数本体；确认全仓无残留调用
4. **advanceStage 触发完成即锁**：工作流推进越过收款节点时，computeLockedState(completedStageIndex) 写 locked（order-workflow.service.ts）
5. **addPayment 触发付清即锁**：收款后重算 locked（paidOff），写库；节点已收一律从 paid_total 推导（getOrderInstallments 已是此逻辑，确认）
6. **守恒挂载**：各变动出口 assertConservation，失败抛 PRICING_CONSERVATION

### 加分（预算允许再做，做完再 commit）
7. addExtraItem schema 放开负数（order.routes.ts priceCents minimum 改 -99999999）——打通 done 减价路径（R13）
8. deleteExtraItem 改冲正条目（不物理删条目行）
9. updateFinalPrice done 无痕改价守卫（转 manual_adjust 条目引导）

## 授权文件

- `server/src/features/order/order.service.ts`、`order-workflow.service.ts`、`order.routes.ts`
- `server/src/db/init.js`（仅追加迁移 v40 + schema 加列，不动既有迁移）
- `server/tests/`（新增测试 + setup.js cleanDb 如需）
- **禁碰**：pricing-engine.ts（引擎已定稿）、前端、demo-data.ts（五号 C 路负责）

## 验收

1. 必做 6 项全绿；加分项做了要声明
2. 测试：新增覆盖 R4 完成锁/付清锁/回退不解锁 + R5 分摊只摊未锁 + R11 守恒拒绝坏变动 + 迁移 v40；server 全量绿（基线 769）
3. **守恒实证**：交付报告给一个端到端场景数值推演（改价→收定金→推进→加价，每步 locked 状态 + 节点价 + 守恒断言通过），证明「已收/已完成节点价不再变」
4. commit 格式 `feat(pricing): ...`，多项分多 commit
5. **交付后不要自行 merge**，转交一号安排合入顺序

## 已知雷区

- init.js 含中文标点模板字符串会被 patch 写坏——对齐既有字符串拼接风格，改完 `node --check` 再跑测试
- 测试隔离 DB_PATH；迁移 v40 是 ADD COLUMN 可事务内（对照 v38：仅重建父表才事务外）
- self-report 无效，一号会逐 diff 审 + 独立重跑测试 + 容器重建验证
