# 派工：约稿页实测修复批（五号）

> 日期：2026-08-06 · 派工人：一号 · 风险等级：**低**（纯前端）
> 分支：`fix/orderform-selection` · worktree：`../artist-commission-of`（已建好，基于 master `3479aa5`）
> 开工第一步：`git merge master` 再读本文件。只动自己分支，不推送不合并，交付写报告。
> **来源**：外部前端 AI 评审对约稿页的多项指控。一号静态核实发现**大部分与代码不符**（选中态 class、✓ 标记、摘要文案都已存在），故本批核心纪律 = **先实测再动手，不实测不改代码**。

## 背景：一号静态核实的疑点

评审称"4 个选项选没选上完全看不出来"——但代码里档位卡有 `tier-pick--on`（主色描边+浅底）+ ✓ 印章（OrderForm.vue:44-47, 882-887），尺寸/画风选择也有 ✓。**纸面设计是对的，可能渲染效果弱（对比度不够/✓ 太小），也可能评审看错了**。同理"摘要区显示占位文本'默认'"——locales 里是 `summaryNoTier: '选好档位后这里会显示价格'`，没有"默认"字样。

## 任务（全部先 Playwright 实测截图取证，再决定修不修）

### W1 选中态实测
起 dev server，约稿页逐组选项（档位/画风/尺寸/增项勾选）实测：选中 vs 未选中的视觉差异度。
- 若差异清晰可辨 → 报告"不实"，不改
- 若差异弱（如 ✓ 太小、浅底对比不足）→ 加强：✓ 尺寸/主色描边宽度/浅底加深，改完 before/after 截图对比

### W2 "下一步"按钮色实测
按钮当前渲染色是不是主色青绿（--color-primary #34dbcb 或访客选择的强调色）？
- 是 → 报告"不实"
- 是 EP 出厂蓝 → 定位为什么 theme.css:92-116 的 `--el-color-primary` 覆写没作用到它（大概率按需引入样式顺序），修复 + 截图

### W3 摘要区空态实测
未做任何选择时摘要卡实际显示什么？截图。若出现"默认"或类似误导占位 → 改为引导文案（i18n）；若显示的是正常引导语 → 报告"不实"。

### W4 顺手项（静态已证实，直接做）
OrderForm.vue 内 `.tier-pick:hover` 仍用 `translateY(-3px)` + `--ease-bounce`（L879-881）——对齐批 1 动画纪律：hover 位移统一 -2px、时长 0.15s、去 bounce（bounce 只留庆祝时刻）。

## 授权文件

- `web/src/views/client/OrderForm.vue`（含 scoped style 动画收敛）
- `web/src/locales/zh-CN.js` / `en.js`（仅 orderForm 块，若 W3 需新增文案）
- 相关测试适配（`__tests__/OrderForm.*.test.js`）
- W2 若定位到样式引入顺序问题，可能需动 `web/src/main.js` 或 `vite.config.js`——**先在报告里给方案，一号确认后再动**

**不要动**：theme.css（二号批 1 在改，会冲突）、NotFound/Landing（二号批 1）、画师主页模板、server。

## 自测要求

1. web `npx vitest run` 全绿（基线 215）
2. eslint 0 错
3. 每个 W 项附 Playwright 实测截图（before/after）与结论：**属实已修 / 不实未改**——诚实报告，不实项不许硬改凑工作量

交付后通知用户转告一号审核。
