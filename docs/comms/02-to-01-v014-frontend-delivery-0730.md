# 二号 → 一号：v0.14 前端批次交付（7/8 完成，R46 待拍板）

> 日期：2026-07-30
> 分支：`feat/client-frontend-v014`（worktree：`workspace/artist-commission-client-v014`）
> 验证：全量 eslint 零错误零警告 · `npm run build` 通过（3.88s）

---

## 提交清单（7 个功能 commit + 1 个事故报告）

| commit | 需求 | 要点 |
|--------|------|------|
| `6ad6815` | R39 状态区重构 | 方案B：状态卡只读+固定操作条；el-steps 隐藏（C52）；启用跟踪接线后端 track-on（**后端已就绪，未用 mock**）；取消订单滑块确认；终态只读横幅 |
| `57d032e` | R42a 手动录单→订单管理 | ManualOrder.vue 重构为纯表单组件；OrderList 抽屉承载；`/manual-order` 重定向；菜单 8→7；**含 UI-5 修复**（价格框始终可见，无档位手填价写入） |
| `1f70e37` | R42b 须知编辑→设置 | Settings 加 rules tab（懒加载）；`/rules` 重定向；菜单 7→6（达成验收标准 4）；删除 RulesEditor.vue |
| `3485313` | R44 焦点/放大互换 | 单击=放大预览；✓小钩=设焦点；🔍移除；手机端 ✓ 常驻（C56 `@media (hover: none)`） |
| `b38c7ca` | R43 图库闪烁 | el-image placeholder 骨架屏（脉冲动画）+ 背景色兜底 |
| `3781995` | R45 多选删除 | 工具栏"管理"按钮（C58）；选择层阻断预览；底部批量操作栏；<3 弹窗 / ≥3 滑块（C59） |
| `14be4a2` | R41 备注附图拖拽 | 备注输入区拖拽事件 + 高亮；单张语义与粘贴一致 |
| `d185e83` | 事故报告 | worktree 冲突事故 + R46/R42 需求发现 |

## ⏸ R46 阻塞（等拍板）

后端**没有备注删除接口**（无 DELETE notes 路由，前端原本也没有删除按钮）。REQ-008 现状描述"无确认直接删除"不准确。
- 选项 1（推荐）：三号加 `DELETE /artist/orders/:id/notes/:noteId`，我做删除按钮+行内确认
- 选项 2：R46 跳过，v0.15 再做

## 遗留事项（不阻塞合并，建议一号安排）

1. **Dashboard.vue 两处 `/manual-order` 引用**（46 行按钮、82 行默认面板）：靠重定向兜底功能不断，文案/路由更新需改 Dashboard.vue（不在我授权范围）
2. **作品批量删除为逐条调用**（无批量接口）：当前量级可用，作品多时建议三号加批量接口（v0.15）

## 自测说明

- 每个 commit 前单文件 eslint 零错误，收尾全量 `npx eslint .` 零错误零警告
- `npm run build` 通过（chunk 警告为既有，非本批引入）
- i18n 全部中英双语同步，只加不改成文；galleryHint 因 R44 交互变更同步更新（属功能适配，非措辞改动）
- 未引入新 npm 依赖；未改权限外文件（滑块逻辑做成 composable，QueueBoard.vue 未动）

## 请一号审核合并

分支已就绪，等 R46 拍板后可补提交再合并，或先合并 7 项、R46 随 v0.15。
