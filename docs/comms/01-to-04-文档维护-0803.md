# 给四号：文档维护（README + CONTEXT + REQ 归档）

> 派工日期：2026-08-03｜派工人：一号
> **直接在 master 提交**（docs 低风险，四号惯例）。commit message：`docs: 文档维护——{具体改了什么}`
> **交付**：做完在聊天里说一句即可，不需要写 comms 文件（纯文档 ≤ 惯例阈值）。

## 背景

v0.32（REQ-023 多画风模型）已全部合入，文档未同步。一号已核实的过时点如下。

## 任务 A：CONTEXT.md 更新（一号已核实过时项）

| 行 | 现状（错） | 应改为 |
|----|-----------|--------|
| L63 迁移 | "迁移当前 v29（order_start_date）" | **v36（multi_style_model 多画风 5 表）** |
| L65 测试 | "后端 567 + 前端 87" | **后端 666（39 文件）+ 前端 106（6 文件）**（一号 2026-08-03 实跑数字，直接用，不需要你跑） |
| 功能清单 | 无多画风 | 补：多画风模型（art_styles/style_sizes/style_addons/size_addon_overrides/addon_templates，v0.32 REQ-023） |

其余行若发现过时一并修。

## 任务 B：README.md 检查

功能列表是否含多画风（v0.32）。缺则补一句话级别描述，不展开。

## 任务 C：REQ 归档（docs/requirements/ → docs/archive/requirements/）

8 个 REQ 文件，逐个对照 STATUS.md 的 v0.32 合入清单 + git log 判断实施状态：

- **已实施** → `git mv` 到 `docs/archive/requirements/`
- **未实施/部分实施** → 留在原地，在文件顶部标注状态

参考：REQ-023（多画风）= v0.32 已实施；REQ-017~022 大概率在 v0.28~v0.31 实施（对照 git log 确认，别凭猜）；REQ-014（桌面端伴侣应用）若未启动则留原地。

## 授权范围

README.md + docs/ 全部。**不改任何代码文件**。不碰 docs/comms/STATUS.md（一号维护）。

## 注意

- 测试数字直接用一号给的（666/106），不要自己跑 vitest
- CONTEXT.md 的迁移版本写 v36，这是权威数字（一号已验证容器日志）
