# 二号 → 一号：v0.28 时间条拖拽实施完成

> 日期：2026-08-02
> 分支：`feat/v028-timeline-drag`
> Commit：`b33c227`

## 做了什么

按已采纳方案实施时间条拖拽改日期：

1. 横条左右端 8px 热区 handle（`cursor: col-resize`），原生 Pointer Events（`setPointerCapture`，与 R30e 滑块同模式）
2. 右端拖改截稿日（`updateDeadline`），左端拖改开工日（`updateStartDate`）
3. 吸附到天（`Math.round(deltaX / tlDayWidth)`），拖拽中浮动日期标签（Teleport to body，fixed 跟随指针）
4. 约束：截稿日 ≥ 开工日（move 阶段吸附拦截 + up 阶段兜底 warning）
5. 终态订单（done/delivered/cancelled）不显示 handle；被窗口裁剪的端点不显示 handle（防误导）
6. 未设截稿日订单：右端 handle 在窗口末端，拖出即设真实截稿日；开工日右移不受限
7. 松手即保存，成功局部更新源订单（不全量刷新），失败回滚（loadQueue + loadBufferQueue）
8. 拖拽中禁用 tooltip（`:disabled="tlDrag != null"`）
9. 移动端：handle `touch-action: none`，@media(max-width:768px) 热区 8px→24px
10. 月历未动

## 改了哪些文件

| 文件 | 改动 |
|------|------|
| `web/src/views/artist/QueueBoard.vue` | +211/-10（模板 handle + 拖拽逻辑 ~115 行 + 样式 ~42 行；tlRows 增加 startDate/endDate/noDeadline/startClipped/endClipped 字段） |
| `web/src/locales/zh-CN.js` | +5 键（tlDragDeadline/tlDragStart/tlDragSaved/tlDragDeadlineBeforeStart/tlDragStartAfterDeadline） |
| `web/src/locales/en.js` | 同上英文 |

注：`npm approve-scripts esbuild` 意外往 package.json 写入 allowScripts 字段，已 `git checkout` 恢复，未进提交。

## 验证结果

- `npx eslint`（3 文件）：0 错误 0 警告
- `npm run build`：✅ 成功（5.29s）
- `npx vitest run`：87/87 通过（5 文件）

## 待一号审核

分支 `feat/v028-timeline-drag`，请审核合并。
