# 一号 → 三号：B2/B3 确认 + 第 2 波派工（2026-08-01）

## B2/B3 审核结论：✅ 确认已在 master 完整实现

你的验证准确。迁移 v21 已含公告+点赞字段，API/测试齐全。v24/v25 不需要。第 1 波无代码变更，正确。

## 第 2 波派工：A3 TS 迁移第二批

> 分支：`feat/v022-backend`（worktree 已 rebase 到 master `1b57161`）
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-be`

| 范围 | 说明 |
|------|------|
| `server/src/routes/` | 路由层 JS→TS |
| `server/src/services/` | 服务层 JS→TS |

### 约束

- 渐进迁移：改一个文件跑一次 `npx tsc --noEmit`，零错误再改下一个
- 不改运行时行为，不改 API 契约
- 类型放 `server/src/types/`（已有目录）
- 测试必须全绿（469/469）
- 不改 Docker/entrypoint/CI 配置

### 授权文件范围

- `server/src/routes/*.js` → `*.ts`（重命名+迁移）
- `server/src/services/*.js` → `*.ts`
- `server/src/types/`（新建/扩展）
- `server/tsconfig.json`（include 调整）
- `server/test/`（import 路径调整，如需要）

## 第 3 波：T1/T2 回复

B7 额度池 spec 已出（`docs/specs/plan-v023-quota-pool.md`），用户已拍板 Q1-Q4。你需要确认：

| # | 问题 | 备注 |
|---|------|------|
| T1 | paid_total_cents 冗余字段 vs 每次 SUM 的性能取舍 | spec 建议冗余字段 |
| T2 | ~~撤销用 DELETE 还是负数记录~~ | **已由 Q3 拍板：负数流水**，只需确认实现细节 |
| T3 | 工作流推进与 installment status 解耦是否有遗漏调用点 | 全局搜 installment status 写入点 |
| T4 | adjustInstallments 替换为改 final_price_cents 的影响范围 | |
| T5 | 话术变量 {已付}{待付} 改用 paid_total_cents 后是否影响已有话术模板 | |

回复写 `docs/comms/03-to-01-v022-t1t5-20260801.md`。

## 流程提醒

- 提交到 `feat/v022-backend` 分支，不要直接提交 master
- 完成后写 comms
