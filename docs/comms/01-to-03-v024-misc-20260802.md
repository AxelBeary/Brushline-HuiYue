# 一号→三号：快捷按钮 DB + UTC 修复 + 容器重建（2026-08-02）

## 任务 A：快捷按钮 DB 字段（~30min）

二号 v0.24-C 用 localStorage 做了 MVP。现在补 DB 持久化：

1. 迁移 v26：`artists ADD COLUMN quick_actions TEXT DEFAULT NULL`（JSON 数组）
2. `artist.service.ts` updateArtist allowed 列表加 `quick_actions`
3. `GET /api/artist/profile` 响应已含（`...artist` 展开），无需改
4. 测试：设置 quick_actions → 读取验证

## 任务 B：#16 getMonthlyUsage UTC 月初修复（~10min）

`artist.service.ts` L435-445：`getUTCFullYear/getUTCMonth` 导致 UTC+8 画师每月 1 号 08:00 才重置。

修复：改用本地时间计算月初（复用 dashboard.service.ts 的 `localMonthStartSqlite` 模式）。

## 任务 C：容器重建（~30min）

迁移 v24/v25 尚未在线上容器执行。重建容器让迁移跑起来：
```
docker compose down && docker compose up -d --build
```
验证：health 端点返回迁移版本 25。

## 授权文件

- `server/src/db/init.js`（迁移 v26）
- `server/src/features/artist/artist.service.ts`
- `server/test/`
- `docker-compose.yml`（只读，不改）

## 分支

`fix/v024-misc`，worktree 自建。基于 master。

完成后写 comms `03-to-01-v024-misc-{日期}.md`。
