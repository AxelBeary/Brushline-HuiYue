# 一号 → 三号：v0.16 技术债清理
> 日期：2026-07-30
> 分支：`fix/v016-tech-debt`
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt03`

## 任务：后端技术债清理（中风险）

STATUS.md 已列的三项：

1. **价格回退链×4**：4 处价格计算存在回退链逻辑重复，抽取为统一函数
2. **活跃过滤×8**：8 处"活跃画师/订单"过滤条件散落各处，统一为常量或工具函数
3. **ISO 日期格式化×6**：6 处日期格式化不一致（踩坑记忆：SQLite 日期比较用空格格式 `YYYY-MM-DD HH:MM:SS`，ISO 8601 的 T 分隔符会导致字符串比较错误），统一归一化

## 要求

- 每项改完跑全量测试确认不破坏
- 不改接口契约，不改数据库结构
- 抽取的公共函数放 `server/src/utils/` 下

授权文件：`server/src/**`（不含 `server/src/db/init.js`——不动迁移）

## 纪律

- 在 worktree 工作，不要碰主 worktree
- 提交前 `git diff --stat` 确认只有授权文件
- commit 格式：`type(scope): 描述`
- 完成后更新本文件底部"完成状态"

## 完成状态

✅ 已完成（2026-07-30）
- 分支：`fix/v016-tech-debt`，commit `bd398da`
- 新增 `server/src/utils/price.js`（PRICE_FALLBACK_SQL + resolvePriceCents）
- 新增 `server/src/utils/order-status.js`（ACTIVE_ORDER_SQL + COMPLETED_ORDER_SQL）
- 新增 `server/src/utils/date.js`（toSqliteDate + localDayStart/End/MonthStartSqlite）
- `order.service.js`：价格回退×4 + 活跃过滤×7 + ISO日期×6 已替换
- `admin.service.js`：活跃过滤×1 已替换
- 197/197 测试通过，ESLint 零错误
- 未改接口契约，未改数据库结构
