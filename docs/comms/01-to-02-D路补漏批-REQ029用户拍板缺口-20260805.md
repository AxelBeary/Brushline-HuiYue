# 派工：二号 D 路补漏批——REQ-029 用户拍板缺口（低风险）

> 一号派工 · 2026-08-05 · 风险等级：低（D 路核心已合入，本批补齐用户拍板功能）
> 工作目录：`D:\Hermes Agent CN Desktop\workspace\artist-commission-manualstyle`（worktree 复用，分支 v037-manual-style 已被合入 master，开工前需重建分支或让一号重建 worktree）
> **开工第一步：`git merge master` 再读本文件 + `docs/requirements/REQ-029-手动录单接画风模式.md`**

## 背景（一号已实锤，不要重新侦查）

你上一轮 D 路核心（画风→尺寸→增项三级选择）已审核合入（`f8e151e`），质量过关。但存在**派工时序缺口**：我给你派工时，四号还没和用户聊完 REQ-029；REQ-029 定稿后，用户拍板的几项功能你的实现里没有。本批补齐。**这不是你的失误，是并行时序问题，不要返工已合入的核心。**

## 缺口清单（对照 REQ-029 逐项 grep 核实过，确实缺失）

### 1. R2 自定义单提示（REQ-029 §二 R2）
画风区旁加一行提示文字：「都可以不选，直接手动填价格录入自定义单」。用户原话：「真要自定义画师直接不选就行对吧。所以我们正确的做法是直接在档位旁边提示可以不选来自定义」。**不做"自定义"卡片、不做模式开关**——只加提示。

### 2. R5 自定义增项（REQ-029 §二 R5，用户原话"很有必要 加"）
- 录入位置：价格面板区域，「＋自定义增项」按钮；**选了画风能录，不选的自定义单也能录**（两条路径通用）
- 每条结构：名称（必填，≤50 字）+ 金额（必填，**允许负数**——减项/让利场景；允许 0——留痕场景）
- 展示：进入价格面板明细，与基础价/目录增项并列
- 数量上限：20 条；提交前可删除已录条目
- **提交语义**：自定义增项随 createManualOrder 提交——查后端 createOrder 是否支持自定义增项字段（styleAddons 是目录增项引用，不是自定义条目）。**若后端不支持，本项需要后端配合：先在交付报告里说明你查到的后端现状，一号定后端方案，不要自行改 server/**（server 是三号/五号域）
- 算价语义：参与倍率计算，与目录增项一致

### 3. R6 图片展示开关（REQ-029 §二 R6，用户拍板"一起藏"）
- 位置：右栏「怎么录」顶部，「显示图片」开关，默认**开**
- 关闭效果：右栏**所有卡片图片一起隐藏**（画风卡片 + 档位卡片），卡片退化为纯文字（名称+价格）
- localStorage 本地记忆，不动后端

### 4. B2 取消选中（REQ-029 §三 B2，bug）
`selectStyle` 里 `if (selectedStyleId.value === id) return` 导致点已选画风卡无反应。REQ-029 要求：点已选中的画风卡 = 取消选中（对齐档位卡 toggle 交互），清空尺寸/增项/算价。修这个。

### 5. R4 倍率在画风模式的展示时机（自查）
你已实现倍率在画风模式选完尺寸后出现（diff 里 v-if 已加），确认 calculateStylePrice 确实吃了倍率参数（你 doStyleCalc 已传 usageMultiplierId/rushMultiplierId，自查确认后端接口接受）即可，预期零改动。

## 授权文件

- `web/src/views/artist/ManualOrder.vue`
- `web/src/locales/zh-CN.js`、`web/src/locales/en.js`
- `web/src/views/artist/__tests__/`（新增/扩展测试）
- **禁碰 server/**（自定义增项若需后端，报告里说明，一号协调）

## 验收

1. web 测试全绿 + 新增覆盖四项缺口
2. 浏览器实测：R2 提示可见 / R5 自定义增项录入+负数+删除+明细展示 / R6 开关一起藏+刷新保持 / B2 点已选画风取消
3. commit `feat(artist): ...`，**交付后不要自行 merge**，转交一号
