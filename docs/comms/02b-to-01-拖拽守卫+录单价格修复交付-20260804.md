# 二号-B 交付：G1 页内拖拽守卫 + G2 手动录单价格抹除修复

> 分支：`v036-w2-dragguard`（worktree wt-03b）· commit `91c1cc6` · 10 文件 +294/-10
> 待一号审核合入。派工：`01-to-02b-拖拽守卫+录单价格修复-20260804.md`

## G1 禁止页内图片拖入上传区

**实现**：新建共享守卫 `web/src/composables/useDropGuard.js`——判断 `dataTransfer.types` 是否含 `'Files'`（系统文件拖拽），无则页内拖拽。守卫挂**捕获阶段** + `stopImmediatePropagation`（侦察实锤：EP dragger 冒泡阶段处理 drop 且不查来源，必须抢在它之前；同元素监听 stopPropagation 拦不住）。i18n 新键 `upload.dragFromPage`（zh/en）。

**接入 7 处上传区**（比派工枚举多 2 处，均为一号"可能还有别的"覆盖）：
- OrderDetail 订单图库 + 备注附图（原生 drop 区，捕获守卫 + handler 内 guardDrop 兜底）
- **QueueBoard 焦点图区×2**（派工未列，枚举发现，已接入）
- ManualOrder 参考图 / ArtworkManage 作品 / DeliverDialog 交付文件（el-upload drag 区，@dragenter.capture/@dragover.capture/@drop.capture）
- OrderForm 参考图（无 drag 属性，防御性接入；**只动 el-upload，步骤导航未碰**）

**G2 手动录单价格脏标记**（ManualOrder.vue）：`priceTouched` 脏标记 + `priceInput` computed setter 包装——用户输入/步进经 setter 置脏，doCalc 程序写入绕过 setter 永不置脏（不依赖 EP 事件语义）。doCalc 未置脏→始终同步计算价；提交仅置脏才调 updatePrice（后端录单自动按计算价入账，无脏标记绝不调——根治 005 误判）；resetForm + onTierChange 清脏。

## 验证

- web vitest **156/156 绿**（基线 144 + useDropGuard 新增 12：来源识别/捕获拦截/节流）
- ESLint 0 · build ✓ 5.54s
- **G2 浏览器实测**（localhost:3899，AUTH_DEV_MODE，alice 全身像¥200 + 造数挂测试增项¥180）：
  选档位→价格框自动填 200 → 加增项→**价格框跟随变 380**（旧 bug 停 200）→ 提交 ALICE-001 → DB 实锤：`total_price_cents=38000`、`final_price_cents=38000`、quote_snapshot「全身像 ¥200 + 测试增项 ¥180 → 总价 ¥380」、breakdown tier 200 + addon 180 完整、activity_logs 空（无误调 updatePrice）
- **G1 浏览器实测**（ManualOrder 上传区合成 DragEvent，types 仅 text/html）：dragenter/dragover/drop **三事件全 prevented**、警告 toast「页面里的图片不能直接拖进上传区…」正确显示、上传列表零新增
- 未测项说明：系统文件真实拖入无法用合成事件注入 File（浏览器安全限制）——正向路径由单测覆盖（types 含 Files → 放行不 preventDefault），各上传区原上传链路逻辑未动

## 改动文件

`web/src/composables/useDropGuard.js`（新）· `useDropGuard.test.js`（新）· OrderDetail.vue · QueueBoard.vue · ManualOrder.vue · ArtworkManage.vue · DeliverDialog.vue · OrderForm.vue · locales/zh-CN.js · locales/en.js

测试残留已清理（seed-g2-addon.mjs / verify-g2.mjs / verify.json / headers.txt）。
