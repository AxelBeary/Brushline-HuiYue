# 五号派工：REQ-020 F2 修改加钱排查（2026-08-02）

> 分支：`fix/research-surcharge-0802`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-05`
> 交付 comms：`05-to-01-修改加钱排查-0802.md`

---

## 任务：排查"修改加钱"为什么不工作（只研究不修）

**背景**：画师原话"加了个屁"——修改加钱功能形同虚设。REQ-020 F2 实施前需要先搞清楚是 bug 还是设计缺口。

**排查方向**：
1. 搜 `surcharge|加钱|extra_charge|price_adjust` 在 server/src 和 web/src 中——功能是否存在？
2. 如果存在：追踪完整数据路径（前端按钮 → API → service → DB → 前端刷新），找断点
3. 如果不存在：确认是"从未实现"还是"实现了但入口被删了"
4. 检查订单总价计算逻辑：`recalcFinalPrice` 或类似函数——加钱是否参与总价计算？
5. 检查节点收款：加钱后应收金额是否联动？

**产出**：根因分析 + 修复/实施方案（2-3 个方案对比）写入交付 comms。不实施。

---

## 授权文件范围

- 只读排查，不改代码
- 如确认是 ≤5 行的 bug（如缺一个 API 调用），可以直接修并在 comms 中说明

## 约束

- 主要是研究，不是实施
- 产出要可操作：三号/二号拿到方案能直接写代码
