# 一号派工：v0.31 后端（2026-08-03）

## 分支与 Worktree

- 分支：`feat/v0.31-backend-0803`
- Worktree：`git worktree add ../artist-commission-wt-be feat/v0.31-backend-0803`
- 主 worktree 不动（一号专用）

## 授权文件

- `server/src/**`（路由/服务/DB/迁移）
- `web/src/views/artist/**`（画师端页面）
- `web/src/components/artist/**`（画师端组件）
- `web/src/api/**`（API 调用层）
- `web/src/locales/**`（翻译）
- `server/test/**`（测试）

## Wave 1（先做，~2 天）

1. **改价按钮（五号方案A）**：OrderDetail 加改价按钮，~40行。参考 `git show a07993e` 五号排查报告。
2. **F2 修改加钱排查修复**（REQ-020 F2）：画师原话"加了个屁"。先排查为什么不工作，再修。加钱后总价实时更新 + 节点收款联动。
3. **cover_order 迁移**：artworks 表加 cover_order 字段 + 排序交互。迁移 v31。

## Wave 2（Wave 1 合入后，~5 天）

4. **F3 总价可改 + 折扣码**（REQ-020 F3）：手动改价（需填原因）+ 折扣码（画师可开关，默认关）。3天。
5. **F4 节点收款重做**（REQ-020 F4）：每节点显示已收/应收/差额 + 快捷收款 + 收款时间线。支持部分收款。2天。
6. **留言 language 字段**（REQ-021 F8 前置）：留言表加 language 字段 + 查询参数。后端写入，不靠前端检测。0.5天。

## Wave 3（Wave 2 合入后，~1.5 天）

7. **F5 下一节点应收**（REQ-020 F5）：订单详情显著位置"下一节点应收：¥XXX"。0.5天。
8. **操作日志后端**（REQ-021 F1）：日志表 + API。记录状态变更/加钱/改价/收款/节点推进/备注修改。永久保留。1天。

## 已定决策（不讨论）

- 折扣码=全局码（v0.32 多画风后再扩展）
- 改价不要审批流
- 收款支持部分收款
- 操作日志永久保留
- 先倍率后折扣（REQ-023 已定）

## 依赖链

F2 → F4（加钱后收款联动）→ F5（下一节点应收）
F3 → F4（改价后收款联动）
操作日志 ← F2/F3（加钱/改价写入日志）

## 提交规范

`type(scope): 描述`，每个功能一个 commit。提交说明含影响范围+改动说明+自测情况。
