# 四号 → 一号：管理员导航 Bug + 约稿页隐藏需求（UI-7 / UI-8）

> 日期：2026-07-30
> 状态：待一号审核
> 来源：实际操作人体验 v0.12 反馈

---

## 本批次交付物

`docs/待修复问题清单.md` 新增 UI-7 / UI-8 两条。

## UI-7：管理员导航陷阱（Bug，P1）

**现象**：管理员登录后，从管理后台点"返回后台"会跳进画师后台，然后整个画师后台找不到回管理后台的入口，只能重新登录。

**根因**：
- `AdminDashboard.vue:3` 返回按钮 `@back="$router.push('/dashboard')"` 跳画师后台
- `ArtistLayout.vue` 侧边栏 MENU_ITEMS 只有画师功能项，**无管理后台入口**

**修复**：ArtistLayout 对 `store.isAdmin` 用户加一个"管理后台"菜单项（/admin）。20 分钟。转二号。

## UI-8：管理员无法隐藏公开约稿页（功能需求，P2）

**现象**：管理员本质是 artists 表一条记录，因此有公开约稿主页 `/artist/admin`，无法关闭。

**用户原声**："要不这样 管理员可以自己设置是否展示自己的约稿页。"→ 追加拍板："直接可以在状态功能里做~多一个隐藏功能"

**已确认方案**：在现有状态系统加第四个状态 `hidden`（隐藏），不加独立字段。所有画师统一可用。hidden 时客户访问主页看到友好提示、不可下单；画师/管理员后台不受影响。

- `artists.status` CHECK 约束扩展为 `('open','full','break','hidden')`，需迁移
- 前端 Dashboard 状态切换加第四项 + 客户主页路由守卫 + 友好提示页
- 后端公开 API 对 hidden 返回受限信息

**待确认**：
- C50：✅ 已确认——所有画师统一，在状态功能里做
- C51：关闭后显示友好提示还是 404？（建议友好提示）

1.5 小时。转三号（迁移+后端）+二号（前端）。

## 说明

这两条都记在待修复问题清单（UI-7 是 Bug，UI-8 标注为功能需求）。UI-8 如果一号认为需要正式需求化，我可以补 REQ 文档；目前判断它足够小，待修复清单的验收标准已够用，不必单开 REQ。

【申请】请一号审核 UI-7 / UI-8 并指派。
