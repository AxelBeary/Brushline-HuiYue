# 二号 → 一号：v0.22 第 3 波交付（A1 + A2 + A4）

**分支**：`feat/v022-frontend-w2`（worktree: `artist-commission-fe`）
**基准**：master `67f5a83`（已 rebase）
**Commits**：3 个，每项独立

| Commit | 项 | 说明 |
|--------|----|------|
| `119952b` | A1 | Sentry 前端 SDK |
| `43797f0` | A2 | E2E 接入 CI |
| `7caaa2c` | A4 | EP CSS 按需引入 |

**路径说明**：派工写 `main.ts`/`vite.config.ts`，实际为 `main.js`/`vite.config.js`（TS 迁移未覆盖入口）

---

## A1 Sentry 前端 SDK（119952b）

- `main.js`：`import * as Sentry from '@sentry/vue'`，DSN 取 `import.meta.env.VITE_SENTRY_DSN`，空则不 init（零开销）
- `environment` 按 `import.meta.env.PROD` 区分 production/development，`tracesSampleRate: 0`
- 在现有 S-10 errorHandler 内加 `Sentry.captureException(err, { extra: { vueInfo: info } })`——Sentry.init 未调用时此调用静默无操作，无 DSN 时行为不变
- 派工说"App.vue 挂 errorHandler"，实际 errorHandler 已在 main.js（S-10），无需动 App.vue
- `@sentry/vue` 加入 dependencies（+package-lock）

## A2 E2E 接入 CI（43797f0）

- 新建 `.github/workflows/e2e.yml`：ubuntu-latest + node 22
- 触发：push/PR to master/main（与 ci.yml 一致）
- 步骤：根 npm ci → web/server 预装（利用缓存加速）→ `npx playwright install chromium --with-deps` → `npm run test:e2e` → 失败时上传 playwright-report
- global-setup 自动完成：构建前端 → 种子 DB → 启服 → 预登录，CI 无需额外步骤

## A4 EP CSS 按需引入（7caaa2c）

- `vite.config.js`：resolver `importStyle: false` → `'css'`（el-* 组件样式随注册自动注入）
- `main.js`：删 `element-plus/dist/index.css`（全量 358KB），保留 `dark/css-vars.css`，手动补 3 个 JS API 组件样式（el-message/el-message-box/el-loading——走 JS 调用非模板标签，resolver 不覆盖）
- **关键风险已处理**：base.css（:root 变量）经 JS API 组件的 css.mjs 依赖链自动引入，位于 theme.css 之前——theme.css 的 `--el-*` 覆写顺序安全（这是 v0.20 保留全量 CSS 的原因，现已实证解决）

**体积结果**：

| 产物 | 基线 | 现在 | 变化 |
|------|------|------|------|
| main CSS | 470.87 kB / gzip 92.85 kB | 123.63 kB / gzip 46.14 kB | **-74% / gzip -46.7 kB** |
| main JS | 389.18 kB / gzip 145.74 kB | 402.66 kB / gzip 150.28 kB | +Sentry ~13 kB |

---

## 自测情况

| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过 |
| vitest | ✅ 87/87 |
| **E2E（真实 UI 路径）** | ✅ **5/5 全绿（45.1s）**——覆盖下单/查进度/推节点/加画师/配流程，大量 EP 组件 + ElMessage/ElMessageBox |
| Ad-hoc 结构验证 | ✅ 21/21 |
| console.log / TODO / v-html | ✅ 无残留 |
| 改动文件范围 | ✅ 仅授权文件（5 个） |

**是否修改非客户前端文件**：`.github/workflows/e2e.yml`（授权）、`web/package.json`+lock（授权）

**需人工验证项**：
- A4 删全量 CSS 后各页面 EP 组件视觉无异常（E2E 已覆盖 5 条核心路径，但边角页面如 HealthCheck、TierManage 的 EP 组件未覆盖）
- A1 有 DSN 时 Sentry 事件正常上报（本地无 DSN 无法验证，需部署环境）
- A2 workflow 首次运行需 GitHub Actions 实际触发验证

申请一号审核合并（3 commits @ `feat/v022-frontend-w2`）。
