# 一号 → 二号：BUG-1 + BUG-4 补修
> 日期：2026-07-31
> 分支：`fix/v016-bug1-bug4`（新建 worktree）
> 研判报告：`docs/comms/05-to-01-bug研判-0730.md`（已归档，git 历史可查）

## 上轮漏做的两个 Bug

### BUG-4（🔴 高）：比例条右拖不回弹+报错

`web/src/components/artist/PaymentBar.vue:121`

当前：`else if (newRight < ELASTIC_THRESHOLD && !rightIsFinal)`
改为：`else if (newRight < MIN_BP && !rightIsFinal)`
并加：`detachId.value = newRight < ELASTIC_THRESHOLD ? rightId : null`

### BUG-1（🟡 中）：排期看板焦点图闪烁

`web/src/views/artist/QueueBoard.vue` + `web/src/composables/useSignatureRefresh.js`

1. `.focus-large-img` / `.focus-img-wrap` 加 `background: var(--bg-card)` 防白闪
2. `refreshNow` 加 300ms 防抖
3. 加最大重试计数（2 次），超限不再刷新

### BUG-2 补充确认

OrderDetail 已修。检查 QueueBoard 的 `handleFocusDrop` 是否也有非图片静默问题，有则一并修。

## 授权文件

`web/src/components/artist/PaymentBar.vue`、`web/src/views/artist/QueueBoard.vue`、`web/src/composables/useSignatureRefresh.js`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`（仅追加）

## 纪律

- commit 格式：`fix(client): BUG-x 描述`
- 完成后更新本文件底部"完成状态"

## 完成状态

✅ 已完成（2026-07-31）
- `cf03537` fix(client): BUG-4 比例条右拖不回弹+报错——右拖弹性阈值与左拖对称
- `407f30b` fix(client): BUG-1 焦点图闪烁(背景色防白闪+refreshNow防抖300ms+重试上限2次) + BUG-2补充 拖入非图片提示
- 审核材料：`docs/comms/02-to-01-BUG补修提交-0731.md`
