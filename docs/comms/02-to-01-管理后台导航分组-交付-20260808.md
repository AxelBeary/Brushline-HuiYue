# 02-to-01-管理后台导航分组-交付-20260808.md

> 交付：二号 → 一号（client-frontend）
> 分支：`beta/admin-nav-groups`，commit `cac71ee`
> 派工：`docs/comms/02-派工-管理后台导航分组-20260808.md`
> 未推送、未合并（按派工）

## 一、改动摘要

授权文件 3 个，其余零改动：

| 文件 | 改动 |
|------|------|
| `web/src/components/admin/AdminLayout.vue` | 导航注册表改分组（NAV_GROUPS 3 组 + navItems 带 group 字段 + groupedNav computed）；桌面侧栏与移动端抽屉均按组嵌套渲染；组标题样式（--ink3 小字） |
| `web/src/locales/zh-CN.js` | 新增 `navGroupOverview: 概览` / `navGroupOps: 运营` / `navGroupConfig: 配置与监控` |
| `web/src/locales/en.js` | 新增 `Overview` / `Operations` / `Config & Monitor` |

分组结构（与施工图一致）：
- 概览 overview：管理员面板
- 运营 ops：画师管理 / 问候语管理 / 默认流程模板
- 配置与监控 config：社交平台管理 / 系统自检 / 埋点看板

实现要点：
- 折叠态（≤900px 图标条）组标题不渲染（模板 `v-if="!collapsed"`），仅图标 + tooltip，宽度保持 64px 不回归
- 移动端抽屉（≤600px）同样分组渲染，组标题始终显示
- 组标题样式严格按施工图：`padding: 14px 16px 6px; font-size: 11px; color: var(--ink3); letter-spacing: .08em;`，双主题（宣纸/墨黑）自动适配
- 无新依赖（纯 div + v-for，未用 el-menu），无硬编码中文（zh/en 同步）

## 二、门禁验证结果

| # | 门禁 | 结果 |
|---|------|------|
| 1 | `npm run test:web` | ✅ 215/215（13 文件全过，5.5s） |
| 2 | `npm run lint` | ✅ 0 error |
| 3 | `npm run build` | ✅ 成功（web/dist 产出，SPA 由隔离服务器 serve 实测加载） |
| 4 | `npm run check:i18n` | ✅ 无新增硬编码中文（存量 9 条豁免不变） |
| 5 | 截图 | ✅ 3 张（见下） |
| 6 | `git diff --stat` | ✅ 只动 AdminLayout.vue + zh-CN.js + en.js |

## 三、截图（workspace/temp/admin-nav-groups/）

浏览器实测环境：隔离实例（PORT=3100 + 独立测试库 + TOTP 真实登录管理员 10003），Playwright Chromium 截图。

| 截图 | 视口 | DOM 断言 | 说明 |
|------|------|----------|------|
| `admin-desktop-expanded.png` | 1440×900 | groupTitles=3, navItems=7 | 桌面展开态：三组标题「概览」「运营」「配置与监控」+ 7 导航项，视觉完整 |
| `admin-narrow-collapsed.png` | 800×900 | groupTitles=0, navItems=7 | 窄屏折叠态：图标条 64px，无组标题（符合铁律），tooltip 保留 |
| `admin-mobile-drawer.png` | 550×900 | groupTitles=3, navItems=14（侧栏隐藏 7 + 抽屉 7） | 移动端抽屉：汉堡打开后分组渲染，组标题可见 |

折叠/抽屉验证说明：
- 折叠态：800px 视口（600 < w ≤ 900）触发 `collapsed`，侧栏为图标条，断言组标题数量=0 → 折叠不显示组标题 ✓；7 个图标项仍可点击（tooltip 保留）✓
- 抽屉态：550px 视口（≤600）侧栏 v-show 隐藏走抽屉，汉堡打开后抽屉内 3 组标题 + 7 项按组渲染 ✓；点击项关闭抽屉路由跳转沿用原 `goDrawer` 逻辑未动 ✓
- 激活态：`activePath` 计算逻辑未改（子路由归属父级高亮、/admin 兜底不变），diff 中 `activePath` 无改动 ✓

## 四、风险与说明

- 无风险项：纯展示层改动，未触碰路由、权限、API；后端零改动
- `activePath`/`pageTitle` 依赖 `navItems` 查找，navItems 仅增加 `group` 字段，行为不变
- 临时截图脚本与测试库（e2e/test-nav.db*、temp-*.mjs）已清理，git status 干净

## 五、下一步建议（一号拍板）

- 视觉细节（组标题字号/字距是否符合纸墨视觉）可人工看图拍板，需要微调只改 `.nav-group-title` 一处
- 合入 master 后即可按派工文件即删流程清理