# 03B-派工：lint warnings 清理 + 后端顺手小项

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：三号（backend）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w15`
> 分支：`beta/cleanup-lint-warnings`
> 交付报告：`docs/comms/03B-to-01-lint清理-交付-20260808.md`（写完 commit 进分支）

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w15'
git merge master --no-edit   # 先合入最新 master 再读本文件
```

## ⚠️ 环境注意（主仓已知坑）

- 主仓 `server/node_modules` 缺 oxlint 二进制（package.json 已声明）——**`npm install --no-save oxlint@^1.77.0` 补装**（不改 package.json/lock，三号 05C 批验证过的做法）
- 或者在本 worktree 跑 eslint 部分 + 单独确认 oxlint

## 背景

STATUS 已知遗留：**lint 基线 warnings** server 6 个（pricing-engine.ts 既有）+ web 4 个（OrderDetail 3 死解构 + OrderForm onMounted 未使用）。本批清后端部分 + 顺手小项。用户铁律：「避免屎山，禁止技术债，代码要优雅！」

## 授权文件（只许动这些）

1. `server/src/features/pricing/pricing-engine.ts` — lint warnings 清理
2. 后端其他 lint warning 位置（先全量 lint 定位再改）
3. `web/src/views/artist/OrderDetail.vue` — **死解构 3 个清理**（currentStageIdx/nextStage/daysLeft，v0.40 遗留）——若顺手
4. `web/src/views/client/OrderForm.vue` — onMounted 未使用（若顺手）
5. `web/src/components/artist/order/` — 若死解构关联组件

> 只清 warnings/死代码；不重构逻辑；不改行为。

## 任务 1：定位并清理 server lint warnings

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w15\server'
npm install --no-save oxlint@^1.77.0   # 若缺
npm run lint   # eslint + oxlint
```

- 逐个定位 warnings：`eslint` 的 warning + `oxlint` 的 warning
- **pricing-engine.ts 6 个**：先读代码理解 warning 原因（可能是 unused var / 显式 any / 空 catch），再优雅修复（不是 delete 了事——若变量确实没用则删，若语义需要则改名/加下划线标注意图）
- 修复后 `npm run lint` 必须 **0 errors 0 warnings**

## 任务 2：OrderDetail 死解构清理（前端，顺手）

`web/src/views/artist/OrderDetail.vue`：`const { currentStageIdx, nextStage, daysLeft } = ...` 三处解构后未使用（v0.40 遗留）——删除未使用的解构项。

- ⚠️ 先 grep 确认确实零引用（别误删在用变量）
- ⚠️ 若这些值在模板/其他函数用到，保留并说明——**以实情为准**

## 任务 3：OrderForm onMounted 未使用（前端，顺手）

`web/src/views/client/OrderForm.vue`：onMounted import 后未使用（或使用位置 lint 报）——清理 import 或补使用。

- ⚠️ 先读代码确认：是 import 未用（删 import）还是调用缺失（补）

## 门禁（提交前必须全绿）

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w15\server'
npm run lint && npm run typecheck && npm run test
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w15\web'
npm run lint && npm run test:web && npm run check:i18n && npm run build
```

## 交付要求

1. commit 描述中文（如 `chore: lint warnings 清理——pricing-engine+OrderDetail死解构+OrderForm`）
2. 交付报告写 `docs/comms/03B-to-01-lint清理-交付-20260808.md`：定位到的 warning 清单（before）→ 修复说明 → 门禁结果（after）
3. 结束前：停本地 dev server；`git status --short` 确认无未跟踪残留
4. **不推送、不合并、不改 STATUS**
