# 01→05：打磨第二批（遗留小项 + v038 视觉瑕疵收尾）

> 日期：2026-08-05 · 派工：一号 · 执行：五号
> worktree：`../artist-commission-polish`（已建）· 分支：`polish-leftover`（基于 master `a8ef3e5`）
> 依据：你的第三方核实报告 + v038 二批审核记录的 vision 瑕疵。**D（需求描述可空过）仍待用户拍板，本批不碰 useOrderForm 校验。**

## 开工

`cd ../artist-commission-polish` → `npm install`（web）→ `git merge master`

## 任务清单（全部低风险，独立成 commit）

1. **回收站插画偏亮**（v038 二批审核记录的唯一 vision 瑕疵）：管理后台回收站区块（AdminDashboard.vue 内，自行定位插画/空态图），按 v0.38 纸墨 token 调至与周边明度协调；宣纸/墨黑双主题各截图自查。
2. **验证风格统一**（你报告里的轻微-一致性项）：留言板原生 required（TplGuestbook.vue）vs 查询页 JS 守卫（TrackOrder.vue）——统一为同一风格（选其一并说明理由），行为不变（空提交仍被拒）。
3. **画廊空状态优化**（可选，时间够再做）：「全部」筛选按钮常驻下的空态文案/引导微调，不破现有交互。

## 铁律

1. 只动 web；server 零触碰；不破 v0.38 token 与 7 色语义；不加 eslint-disable
2. 基线 web 192 全绿 + lint 0 + build 过（web/ 目录跑）；受影响组件补/改单测
3. 交付：commit 到分支 + `docs/comms/05-to-01-打磨第二批-交付-20260805.md`（逐条改动 + 前后截图放 docs/audit-screenshots/polish2/）。不推送不合并。

—— 一号
