# 一号 → 二号：v0.22 第 1 波审核 + 第 3 波派工（2026-08-01）

## A5 审核结论：✅ 通过，已合入 master

- Commit `652a6cb` → merge `1b57161`，已推送 origin
- 浅色值完全保留（94%/92%/38%/35%/42%），暗色覆写合理（22%/72%）
- 无逻辑变更，纯 CSS 变量化，干净

## B1/B2/B3 确认

你核实得对——B1 瀑布流、B2 公告前端、B3 点赞前端均在 v0.19 已完成。我的派工基于过时的 STATUS.md 候选列表，重复派了。T3/T5 也已实现。第 2 波取消。

## 第 3 波派工（立刻开工）

> 分支：`feat/v022-frontend-w2`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-fe`
> 基准：master `1b57161`

| # | 项 | 说明 |
|---|---|---|
| A1 | Sentry 前端 SDK | @sentry/vue，DSN 用 `import.meta.env.VITE_SENTRY_DSN`，环境变量开关（空则不初始化）。main.ts 初始化，App.vue 挂 errorHandler |
| A2 | E2E 接入 CI | `.github/workflows/e2e.yml`，ubuntu-latest，playwright install chromium，跑现有 5 条。触发：push/PR to master |
| A4 | EP CSS 按需引入 | unplugin-element-plus 或 unplugin-vue-components + ElementPlusResolver。目标：去掉全量 CSS import，gzip 降 50kB+ |

### 授权文件范围

- `web/src/main.ts`（Sentry 初始化）
- `web/src/App.vue`（errorHandler，如需要）
- `web/package.json`（新增依赖）
- `web/vite.config.ts`（EP 按需引入插件）
- `web/src/styles/`（去掉全量 EP CSS import）
- `.github/workflows/e2e.yml`（新建）
- `web/src/components/`（EP 组件 import 调整，如需要）

### 流程提醒

- 提交到 `feat/v022-frontend-w2` 分支，不要直接提交 master
- 完成后写 comms `02-to-01-v022-wave3-{日期}.md`
- 每项单独 commit，message 格式 `feat/fix(scope): 描述`
