# 一号 → 三号：order.service.js 职责拆分
> 日期：2026-07-31
> 分支：`refactor/v016-order-split`
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt03`
> 依据：五号审计报告 `docs/comms/05-to-01-代码文档审计-0731.md` 任务A

## 任务：852 行 → ~400 行，拆 4 个子模块

按五号建议的顺序，**先拆零风险的，再拆有依赖的**：

### 第一步：order-stats.service.js（零风险，纯读）

移出：`getArtistStats`、`getUpcomingDeadlines`
- 依赖：db, PRICE_FALLBACK_SQL, ACTIVE_ORDER_SQL, COMPLETED_ORDER_SQL, 日期工具函数
- 无写操作，无交叉调用

### 第二步：order-queue.service.js（低风险）

移出：`getArtistQueue`、`reorderQueue`、`compactQueue`、`updatePriority`
- compactQueue 需 export（deliverOrder 调用它）
- 依赖 getOrder（从 order.service.js import）

### 第三步：order-gallery.service.js（中风险）

移出：`addReference`、`removeReference`、`setFocusImage`、`addDeliverable`、`deliverOrder`
- deliverOrder 调用 compactQueue（从 order-queue.service.js import）
- addReference 含 BUG-3 去重逻辑，别丢了

### 第四步：order-workflow.service.js（中风险）

移出：`advanceStage`、`rollbackStage`、`enableTracking`、`getStageInfo`、`mapStageToStatus`
- advanceStage 写 status + completed_at，与 updateOrderStatus 有语义重叠，注意边界

### 保留在 order.service.js

createOrder, getOrder, getOrderByNo, updateOrderStatus, updateDeadline, addNote, deleteNote, updateFinalPrice, getArtistOrders, getClientQueuePosition, getClientOrdersByQq, hasClientOrders, getPlatformConfig, generateOrderNo（~14 个）

## 关键约束

1. **不改接口契约**：路由层 import 路径更新，但 API 行为零变化
2. **不产生循环引用**：order.service.js 不反向 import 子模块
3. **每拆一步跑全量测试**，确认不破坏
4. 路由层 `order.routes.js` 的 import 同步更新

授权文件：`server/src/features/order/**`、`server/src/features/admin/admin.service.js`（如有 import 变更）、`server/tests/**`

## 纪律

- 4 步分 4 个 commit，每步跑测试
- commit 格式：`refactor(server): 拆分 order-xxx.service.js`
- 完成后更新本文件底部"完成状态"

## 完成状态

（三号完成后填写）
