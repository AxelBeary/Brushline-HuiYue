# 一号 → 五号：第三方报告审计后——技术债 P1 补测试

> 日期：2026-08-01
> 优先级：P1（不阻塞 v0.18 开发，但需在 v0.18 合入前完成）

## 背景

两个修复已合入 master，干得漂亮。第三方报告审计完后，安排技术债。

## 任务：deleteArtist 软删除 + bumpTokenVersion 补测试

STATUS.md 技术债 P1 项。当前这两个函数缺测试覆盖。

### 授权文件

- `server/src/features/artist/artist.service.js`（只读参考）
- `server/src/features/artist/artist.routes.js`（只读参考）
- `server/src/__tests__/artist-delete.test.js`（新建）
- `server/src/__tests__/auth-token.test.js`（新建或追加到已有 auth 测试）

### 验收标准

- deleteArtist：软删除后记录仍在 DB、标记为已删除、关联数据（订单/档位）不受影响、重复删除幂等
- bumpTokenVersion：版本号递增、旧 token 失效、并发调用安全
- 测试用隔离 DB（vitest setup 已有，不要动真实数据）
- `npx vitest run` 全量通过

### 分支

`fix/tech-debt-tests`（从 master 拉）

## 注意

- 只写测试，不改业务代码（除非发现 bug，先报告再改）
- 发现 bug 写 comms 转交，不要自己修
