# 五号 → 一号：第三方测试报告核实结果

日期：2026-08-05 ｜ 性质：只读核实（代码对照 + 浏览器实测 localhost:3000），未改任何代码
输入：第三方研判者 10 条问题报告（首页/主题/i18n/画师页/画廊/约稿表单/留言板/查询页）

## 结论速览

10 条中：真问题 5（其中 1 条需产品拍板）｜误报 3｜数据质量/产品决策 2

## 真问题（建议派工，全部低风险）

| # | 问题 | 定位 | 建议 |
|---|------|------|------|
| A | 主题面板主色色板名硬编码中文（青/碧/蓝/靛/紫），英文界面仍显示中文 | `web/src/components/ThemePicker.vue:60-66` accents 数组 name 未走 i18n | name 改 i18n 键，locale 双语各加 5 词 |
| B | 亮色模式平台警告区对比度低（用户肉眼确认） | `web/src/components/Disclaimer.vue` 直接用 EP `el-alert type=warning` 默认浅色，无覆盖 | 亮色下加深文字色（目标 AA 4.5:1） |
| C | 查询页 placeholder 偏淡（EP 默认 #a8abb2，白底约 2.5:1） | `TrackOrder.vue` 输入框未覆盖 EP placeholder 变量 | 顺手调深，与 B 同批 |
| D | 约稿需求描述步骤可空过（无任何校验） | `useOrderForm.js:111-122` rules 只有 tierId/clientQq/agreed；后端 `order.routes.ts:103` 允许 null | **需用户拍板**：保持选填 / 加软提示 / 加最低校验。拍板前不派工 |
| E | 「返回主页」无障碍名重复（accessible name 读两遍） | el-page-header icon 自带 aria-label="返回主页" 与 title 文本叠加（EP 组件内部行为） | 低优先级；视觉重复经用户仔细确认**不存在**（DOM 单节点、无伪元素、无 text-shadow），报告猜测的 CSS 叠印不成立 |

## 误报（不建议动作）

- **留言板/查询页空提交"可成功"**（报告#2，原定严重）：不可复现。留言板三层防线——原生 required（TplGuestbook.vue:21,30）+ submit() trim 守卫（:118-120）+ 后端 minLength:1（guestbook.routes.ts:23-24，实测空串 400）。查询页空 QQ 被 JS 守卫拦截（TrackOrder.vue:272，实测 toast「请输入QQ号」）。仅存验证风格不一致（required vs JS 守卫），降级轻微-一致性，可选统一。
- **"返回主页"视觉文字重复**（报告#6）：用户实测仔细查看无重影，误报。
- **"随系统"无选中态**（报告#10）：ThemePicker.vue:16-22 三选项同一 v-for 同一 active class，逻辑对称，不存在单独缺失。

## 数据质量 / 产品决策（非代码 bug）

- **画师 bio 英文模式显中文**（报告#1）：bio 是数据库用户内容（LandingPage.vue:30 渲染 artist.bio），非 i18n 缺口。多语言 bio 属产品决策，暂不建议做。
- **作品图 alt=文件名**（报告#5）：实测 19 张图全部有 alt（noAlt:0），代码兜底正常（TplGallery.vue:149 `art.title || 兜底`）；问题是部分作品上传时 title 存了文件名。建议从画师上传引导解决，不改画廊代码。
- 报告#3 画廊空状态：「全部」筛选按钮常驻顶部，非死路，可选优化不紧急。报告#9 视觉断层：主观项无缺陷。

## 派工建议（如一号决定处理）

一批可带走：A + B + C（纯前端视觉/i18n，授权文件：ThemePicker.vue、locales/zh-CN.js、locales/en.js、Disclaimer.vue、TrackOrder.vue 样式段）。E 顺手可带。D 等用户拍板。全部低风险，无需方案预审。

—— 五号
