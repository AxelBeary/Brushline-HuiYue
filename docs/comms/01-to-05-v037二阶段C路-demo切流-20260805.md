# 派工：五号 REQ-025 二阶段 C 路——demo-data 切流（中风险）

> 一号派工 · 2026-08-05 · 风险等级：中（演示数据重建，用户终验直接面向）
> 工作目录：一号稍后建 worktree（见开工指令）
> **开工第一步：`git merge master` 再读本文件**
> **前置已满足**：A 路后端引擎接线已合入 master（`3aea46f`）——createOrder 写 base 条目 + allocateInitial 生成分期、generateInstallmentsForOrder 已走引擎、refreshInstallmentLocks/checkOrderConservation 已导出可用。

## 背景（一号已实锤）

demo-data.ts 现状（L270-305）：直插 orders 绕过 createOrder → 调 generateInstallmentsForOrder 补分期。A 路合入后，直插订单**没有条目账本**（base 条目缺失），守恒断言靠懒回填兜底——演示数据不是新模型的完整样本，用户终验时看到的订单缺条目/缺 locked 状态。

REQ-025 §8.2 切流要求：演示数据改走与真实订单相同的入口，保证演示数据本身就是新模型的端到端验证。

## 任务

### 1. demo-data.ts 切流（核心）

改造演示订单生成，使每个演示订单具备完整的新模型数据：
- **条目账本完整**：每单有 base 条目（= 总价）；有增项/改价剧情的单写对应条目
- **分期由引擎生成**：generateInstallmentsForOrder（已是引擎实现，可直接复用）
- **locked 状态推导**：按演示剧本的已收金额 + 当前阶段，调 refreshInstallmentLocks 落 locked/locked_reason（已付清节点 paidOff、已完成节点 completed）
- **守恒闭合**：生成后跑 checkOrderConservation，全量演示订单必须零报错

方案选择权在你（改 createOrder 走全流程 vs 直插后补齐条目+引擎调用），选改动小、剧本表达力强的；但**验收是硬标准**：最终 DB 里每个正式区演示订单 Σ条目delta = final_price_cents 且守恒通过。

### 2. 完整性校验扩展（L341-363 区域）

现有字段完整性断言扩展：
- 正式区有报价订单必须有 ≥1 条 base 条目
- Σ条目delta = final_price_cents（A1 数据层对账）
- Σ节点价 = final_price_cents（无额外项的演示单）
- locked=1 的节点必有 locked_reason

### 3. 验证

- demo-data 在容器内重跑成功（`docker exec -w /app/server commission-web npx tsx scripts/demo-data.ts`）
- 重跑后 server 测试全绿（demo-data 改动可能影响 setup/测试 fixture 的话一并处理，声明超授权改动）
- 交付报告附：重跑后容器 DB 守恒核查结果（每单 Σ条目/Σ节点价/locked 状态列表）

## 授权文件

- `server/scripts/demo-data.ts`（主体）
- `server/tests/setup.js`（如需 fixture 同步，声明）

**禁碰**：order.service.ts / pricing-engine.ts / init.js / 前端（A/B 路成果，零改动）。

## 演示数据改动纪律

demo-data 重建会清掉当前 DB 演示订单（含用户终验遗留的 teasttt/testtta 增项——那是 08-04 用户终验残留，重建即清理，属预期收益，报告里提一句即可）。**重建前先备份**：脚本开头已有备份逻辑则复用，没有则在交付报告声明手动备份文件名。

## 验收

1. 演示订单全量守恒零报错（交付报告附核查输出）
2. server 测试全绿（基线 786）
3. commit `feat(demo): ...`，**交付后不要自行 merge**，转交一号
