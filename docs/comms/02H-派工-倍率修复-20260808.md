# 02H-派工（重写）：REQ-036 倍率轻合并实现——加购项池统一含倍率项（用户暴怒报障）

> 派工人：一号（主理人）
> 日期：2026-08-09（重写——原版只修显示，违背 REQ-036 §四）
> 执行角色：二号（client-frontend）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w33`
> 分支：`beta/fix-multiplier-display`
> 交付报告：`docs/comms/02H-to-01-倍率修复-交付-20260808.md`

## ⚠️ 用户原话（暴怒）

「顶上的倍率没改掉，你特么我价格里不能设置倍率是吧？我和四号都已经说的那么清楚了？仔细看我和四号的交流！！！！！」

## ⚠️ 方案依据（REQ-036 §四，用户与四号拍板——一号重读原文）

> 用户问题：「数量 开关 用途 加急 这些倍率也是增项的一种可以吗？这样**倍率tab也可以和增项去合并**？还是你有其他更好的逻辑」
> 结论（用户拍板「倍率合并接受『轻合并』」）：
> - **产品层面合并**：画师在**加购项池统一看到所有加购项（加法项 + 倍率项）**，**倍率 tab 退居预置**
> - 技术层面轻合并（不物理合并表）：`addon_templates` 加 `kind` 维度（add/multiply），乘法项计价走乘法；`price_multipliers` 表保留
> - 用途单选 + 加急单选（用户拍板；现有引擎注释「取最高，不叠加」一致）；加法增项可多选共存

**02G 做错**：只把加法项放进池，倍率项显示 ¥0、倍率配置锁在独立 tab——**违背拍板**。本批实现 §四 产品层面合并。

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w33'
git merge master --no-edit
```

## 授权文件（只许动这些）

1. `web/src/components/artist/ArtStyleManager.vue` — 加购项池含倍率项（核心）
2. `web/src/components/artist/addon-utils.js` — formatAddonPrice / addonKind 支持 multiply
3. `web/src/views/artist/TierManage.vue` — 倍率 tab 退居预置
4. `web/src/locales/zh-CN.js` + `en.js` — 新键双语

> 不改后端（批B kind 已实现）；不引新依赖。

## 任务 1（核心）：加购项池统一含倍率项

**目标**：加购项池 = 加法项 + **倍率项**统一可见（用户拍板「倍率也是增项的一种」）。

1. **数据源**：池子数据 = 画风已挂增项（含 kind=multiply 的倍率项）+ 可挑选的库模板（含系统预置倍率：商用 +50% / 加急 +100%）。确认 API 返回字段名（后端返回 `kind` 或 `tpl_kind`——**先读后端 getStyleAddons/getPublicStyles 实际返回**，别猜）
2. **胶囊显示**：倍率项胶囊显示 `+50%`/`+100%`（百分比语义，批B kind 已实现）——`formatAddonPrice` 加 multiply 分支
3. **徽标**：倍率项带「倍率」徽标（与加法项视觉区分）
4. **拖拽语义**：倍率项拖到尺寸 = 该尺寸启用该倍率（与加法项一致；后端 kind 计价已支持）
5. **摘要/预览**：尺寸摘要 chip 倍率项显示 `+50%`（三形态：¥50 / ¥80/位 / +50%）；预览明细含倍率项
6. **倍率项不进加法小计**（kind=multiply 是因子，小计/计价由后端算）

## 任务 2：倍率 tab 退居预置

**REQ-036 §四：「倍率 tab 退居预置」**——用户原话「这样倍率tab也可以和增项去合并」。

- **画师日常入口**：倍率配置进「画风与价格」页加购项池（新建/编辑倍率项 = 在池内做，与增项统一）
- **倍率 tab**：从 TierManage 主导 tabs **移除/降级**——倍率项成为池内可管理的「预置资产」（系统预置 + 画师可自定义），不再独立 tab
- ⚠️ 移除 tab 前确认：`price_multipliers` 表保留（后端画师级全局倍率仍存这）；画师级自定义倍率管理入口 = 池内「新建增项」选 kind=multiply（或先确认现有倍率数据如何迁移展示——**先读 MultiplierManager 现状 + price_multipliers 数据**）
- ⚠️ 保守：若完全移除倍率 tab 影响画师既有倍率配置，**先做「池内显示倍率项 + 可配置」+ 倍率 tab 标注「已并入加购项池」过渡**——以用户「我价格里能设置倍率」为验收核心

**验收（用户视角）**：
- 画师在「画风与价格」页：加购项池能看到/新建/编辑倍率项（商用 +50% / 加急 +100% 或自定义）
- 拖倍率胶囊到尺寸 → 该尺寸启用该倍率
- 摘要/预览显示 +50%
- **画师不再需要去独立倍率 tab 才能配倍率**（倍率 tab 退居/标注）

## 门禁（提交前必须全绿）

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w33\web'
npm run lint
npm run test:web
npm run check:i18n
npm run build
```

## 交付要求

1. commit 描述中文（如 `feat(web): REQ-036倍率轻合并——加购项池统一含倍率项+倍率tab退居(用户拍板§四)`）
2. 交付报告写 `docs/comms/02H-to-01-倍率修复-交付-20260808.md`：对照 REQ-036 §四 逐条 + 门禁 + **截图**（池内倍率胶囊 +50%、摘要 chip、拖拽倍率到尺寸、倍率 tab 退居——视觉门禁）
3. 结束前：停本地 dev server；`git status --short` 确认干净
4. **不推送、不合并、不改 STATUS**
