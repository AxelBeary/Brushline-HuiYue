# 05E-派工：巡检修复批（05D 发现的真问题）

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：五号（bugfix）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w17`
> 分支：`beta/fix-inspect-findings`
> 交付报告：`docs/comms/05E-to-01-巡检修复-交付-20260808.md`（写完 commit 进分支）

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w17'
git merge master --no-edit   # 先合入最新 master 再读本文件
```

## 背景

05D 后台巡检（已合入 master `2b4e15d`）发现 0🔴 14🟡 15🟢。本批修 **Top 5 低成本高收益 + 顺手小项**（全部已独立复核属实）。用户铁律：「避免屎山，禁止技术债，代码要优雅！」

## 授权文件（只许动这些）

1. `web/src/views/artist/QueueBoard.vue` — 🟡-Q1 完成区刷新
2. `web/src/views/artist/Settings.vue` — 🟡-SE1 切 tab 丢修改
3. `web/src/views/artist/ArtworkManage.vue` — 🟡-A1 触屏按钮 + 🟡-A2 上传大小检查
4. `web/src/views/artist/OrderList.vue` — 🟡-O1 分页漏单（+ 顺手 🟢-O2 row-click）
5. `web/src/views/artist/StandaloneIncome.vue` — 🟡-I1 裸 fetch 收口
6. `web/src/views/artist/ToolsExport.vue` — 🟡-E1 裸 fetch 收口 + 🟢-E2 不可用格弱化
7. `web/src/components/artist/DiscountCodeManager.vue` — 🟢-T2 复制按钮
8. `web/src/views/artist/Watermark.vue` + `PuzzlePage.vue` — 🟡-W1/P1 订单下拉 100 条上限（如顺手）
9. `web/src/api/index.js` — 如需封装收口方法
10. `web/src/locales/zh-CN.js` + `en.js` — 如需新键

> 不改后端（除 🟡-O1 若需后端语义——**优先前端方案**，见下）；不引新依赖。

## 任务 1：🟡-Q1 排期看板完成区刷新（1 行）

QueueBoard.vue：
- `onDeliveredFromBoard`（L79-81）补 `loadCompletedQueue()`（若该函数存在——先读代码确认 completed 加载函数名）
- `refreshAll`（L173-175）补 completed 刷新
- 效果：交付后完成区立即显示刚交付的订单

## 任务 2：🟡-SE1 Settings 切 tab 未保存修改静默丢失

现状：Settings.vue save 按 tab 拆分（L512），切 tab 时未保存的修改直接丢——唯一数据丢失级问题。

**方案**（选实现优雅者）：
- 方案 A：切 tab 前脏检测——若当前 tab 有未保存修改，ElMessageBox.confirm「有未保存修改，确定离开？」（el-tabs 有 before-leave 钩子：`@tab-change` 或 `before-leave`）
- 方案 B：切 tab 自动保存当前 tab（若 save 接口独立可无感）——**优先 A**（B 有意外写入风险）
- ⚠️ 只对「有修改的 tab」拦截；无修改直接切

## 任务 3：🟡-A1 作品管理触屏按钮 + 🟡-A2 上传大小检查

ArtworkManage.vue：
- **A1**：L553-559 `.artwork-actions` 与 L545 主图区——`opacity 0 → hover 1` 在触屏不可见。改法：`@media (hover: none)` 或 `≤768px` 时按钮常显（opacity 1）；桌面 hover 保持。**注意克制动效纪律**：hover 位移不给，只改可见性
- **A2**：`handleUpload`（L332-343）开头补大小/类型检查（与粘贴路径一致：>10MB 拒绝 + 图片类型校验），超限 ElMessage.warning + 不发送

## 任务 4：🟡-O1 订单列表分页漏单

现状：复合筛选 active/completed 是客户端过滤已加载页（默认 50 条），>50 单漏单。

**优先前端方案**（不改后端）：
- OrderList.vue：`?status=active|completed` 统计卡跳转时——改为服务端筛选（后端 `?status=` 是否支持 active/completed 语义？查 order.routes.ts L348-351；若后端 status 只支持单值枚举，则前端方案 = 拉取全部页再过滤，或分页参数带 status 语义）
- **若后端支持**：`loadOrders` 传 status 给后端
- **若后端不支持**：方案 B = 前端按需拉全量（pageSize 200 循环拉完）再客户端过滤，标注「订单多时稍慢」；**推荐优先查后端能力再定**
- ⚠️ 必须验证：>50 单时统计卡跳转不漏单、total 不误导

## 任务 5：🟡-I1/E1 裸 fetch 收口

StandaloneIncome.vue（L137/180/207）+ ToolsExport.vue（L111/L145）裸 fetch 不走 axios 拦截器（401 不登出/无超时/无 i18n）。

**方案**：
- api/index.js 的 artistApi 加方法（getStandaloneIncomes/createStandaloneIncome/deleteStandaloneIncome 已有？先查——若已封装则直接替换裸 fetch；若没有则封装）
- ToolsExport 的 export.csv 下载可保留 fetch（blob 下载特殊性），但**补 401/超时处理**；standalone 汇总若裸 fetch 则收口
- ⚠️ 收口后行为与 axios 拦截器一致（401 自动登出/15s 超时/i18n 错误）

## 任务 6：顺手小项

- 🟢-T2 DiscountCodeManager 行内复制按钮（clipboard API，照 ScheduleSharePage 模式）
- 🟢-E2 ToolsExport「不可用」格弱化（隐藏或加注脚，不删后端端点预留）
- 🟢-O2 OrderList 桌面 row-click 进详情（保留详情按钮兼容）

## 门禁（提交前必须全绿）

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w17\web'
npm run lint
npm run test:web
npm run check:i18n
npm run build
```

## 交付要求

1. commit 描述中文（如 `fix(web): 巡检修复批——完成区刷新/切tab防丢/触屏按钮/分页漏单/fetch收口`）
2. 交付报告写 `docs/comms/05E-to-01-巡检修复-交付-20260808.md`：逐任务改动说明 + 门禁结果 + 关键验证（Q1 完成区刷新、O1 分页不漏单）
3. 结束前：停本地 dev server；`git status --short` 确认无未跟踪残留
4. **不推送、不合并、不改 STATUS**
