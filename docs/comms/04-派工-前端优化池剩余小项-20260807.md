# 04-派工-前端优化池剩余小项-20260807.md

> 派工：一号 → 四号（requirements）
> worktree：`../artist-commission-w4`（分支 `beta/front-opt-rem`）
> 背景：前端优化池（五号两份核实报告提取）剩余非视觉小项，四号空闲承接
> 基线：master `e3d9349`。**开工第一步 `git merge master`**。

## 授权文件

- `web/src/main.js`（仅 errorHandler 相关）
- `README.md`（L99-L100 过时修复）
- `web/index.html`（字体 preload，若做）

## 任务

### A. main.js errorHandler 去动态 import（优化池项）
- 现状：main.js 的 errorHandler 内可能有 `await import(...)` 动态引入（grep 确认）——改为静态 import（顶部），减少运行时异步
- 读 main.js 全文，定位 errorHandler 里的动态 import（若有），移到顶部静态 import
- ⚠️ 若 main.js 无动态 import（可能已被其他批处理）→ 核实后报告「无需改动」

### B. README L99-L100 过时修复（优化池项）
- 读 README.md L90-110，对照当前项目结构/命令，修正过时描述（如旧文件名/旧命令/旧路径）
- 逐条核对：命令实际可跑（package.json scripts）、路径实际存在

### C. 字体 preload（优化池项，可选）
- 读 web/index.html，看字体（wencai/noto woff2）是否 preload
- 若未 preload：给首屏字体（LXGW WenKai 或 Noto Sans SC 的 woff2）加 `<link rel="preload" as="font">`
- ⚠️ 注意：字体文件是分片（60+12 个 woff2），preload 全部无意义——只 preload 最常用 1-2 个（如 latin/basic 子集），或评估后跳过（preload 收益小就不做，报告说明）

## 红线

- 不碰视觉类（Serif 局部化等放下版本）
- 不改业务逻辑、不改 i18n 键
- 每项改动前先核实现状（可能已被前批处理）——「已处理」就报告无需改动

## 门禁

1. `cd web && npm run test:web` → 215/215
2. `cd web && npm run lint` → 0 error
3. `cd web && npm run build` → 成功
4. `git diff --stat` 只动授权文件

## 交付

- commit 到分支 `beta/front-opt-rem`，不推送不合并
- 交付报告 `docs/comms/04-to-01-前端优化池剩余小项-交付-20260807.md`：A/B/C 逐项 完成/无需改动+理由
