# 五号交付：A 类 Bug 修复批次（2026-08-02）

> 分支：`fix/a-bugs-batch-0802`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-05`
> 基线：`ce737f9`（master HEAD）

---

## 修复清单（7 commit）

| # | 问题 | 修复 | commit |
|---|------|------|--------|
| 43b | 封面 PUT 400 | 删除空 body schema | `4e23a39` |
| 54 | 可约稿+已约满矛盾 | 公开 API 新增 `effectiveStatus` 字段 | `297aa8c` |
| 35 | 开工日可比截稿日晚 | `updateStartDate`/`updateDeadline` 双向交叉校验 | `8378059` |
| 36 | 公告时间范围可倒设 | `announcement_expires_at` 不得早于今天 | `2d36433` |
| 43a | CSP 拦 Sentry | `connect-src` 动态拼接 `SENTRY_DSN` 域名 | `86a05d6` |
| 45 | 快捷按钮可添加仪表盘 | 候选池过滤 `dashboard` | `3aaca47` |
| 66 | 问候语 `{name}` 被吞 | locale 文件花括号转义 `{'{'}name{'}'}` | `c224d61` |

## #4 时间条拖拽——非代码 Bug

Playwright 自动化诊断（headless Chromium 1440×900）结论：**功能正常工作**。
- 5 个 handle--start、4 个 handle--end 全部渲染（1 个无截稿日订单不显示 end handle，设计如此）
- computed style 全部正确（width 8px, pointer-events auto, z-index 1, cursor col-resize, touch-action none）
- 模拟 pointerdown→move→up 后 API PUT 200，日期更新成功
- 用户可能未切到时间条视图（默认是列表视图），或 8px handle 难以命中（触摸热区在移动端已扩大到 24px）

**建议**：如用户仍反馈不工作，请确认是否在时间条视图（📊 图标）下操作。桌面端 8px 确实偏窄，可考虑扩大到 12-16px（C 类视觉改进，非逻辑 bug）。

## 修改文件（11 个，全在授权范围内）

| 文件 | 改动 |
|------|------|
| server/src/features/artist/artist.routes.ts | #43b 删 schema + #54 effectiveStatus |
| server/src/features/artist/artist.service.ts | #36 公告过期日校验 |
| server/src/features/order/order.service.ts | #35 双向交叉校验 |
| server/src/shared/errors.ts | 新增 INVALID_START_DATE、INVALID_ANNOUNCEMENT_DATE |
| server/src/app.js | #43a CSP 动态拼接 |
| server/tests/effective-status.test.js | #54 测试（5 用例） |
| server/tests/date-cross-validation.test.js | #35 测试（4 用例） |
| server/tests/announcement-like.test.js | #36 适配（TC-ANN-03 改用直接写 DB） |
| web/src/views/artist/Settings.vue | #45 过滤 dashboard |
| web/src/locales/zh-CN.js | #66 花括号转义 |
| web/src/locales/en.js | #66 花括号转义 |

## 验证结果

- [x] 后端测试：576/576 通过（36 文件）
- [x] 前端 ESLint：零错误零警告
- [x] 前端 build：成功（5.06s）
- [x] 原路径已修复（各 Bug 对应测试用例通过）
- [x] 无回归（全量测试通过）
- [x] 授权范围检查：11 文件全在派工授权列表内

## 待二号跟进

- #54 前端适配：TplStatusBadge 改用 `effectiveStatus` 替代 `status`
