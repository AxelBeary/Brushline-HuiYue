# 一号 → 五号：v0.17 前置审计 + 测试覆盖检查
> 日期：2026-07-31

## 背景

看板闪烁研判质量很高，已采纳并派给二号（BUG-5）。你现在空闲，做 v0.17 前置审计。

## 任务 A：测试覆盖审计

v0.17 将改动以下模块，检查现有测试覆盖是否足够：

1. `server/src/services/artist.service.js`（或等效）——R58-8 + 灵感标签将新增 API
2. `server/src/routes/artist.routes.js`（或等效）——同上
3. `web/src/composables/useSignatureRefresh.js`——BUG-5 将重构
4. `web/src/composables/useOrderForm.js`——R24 将修改校验逻辑

产出：每个模块的现有测试覆盖情况 + 缺失的关键路径 + 建议补充的测试用例清单。

## 任务 B：迁移安全预检

v0.17 将新增迁移 v17（platform_urls + inspiration_tags）。检查：
1. 现有迁移 v1–v16 是否有未解决的兼容性问题
2. v17 迁移草案（三号尚未提交）需要注意的陷阱（字段默认值、JSON 字段在 SQLite 的存储、回滚方案）

## 授权文件

| # | 文件 | 用途 |
|---|------|------|
| 1 | `docs/comms/05-to-01-v017前置审计-0731.md` | 审计报告交付 |

只读审计，不改代码。发现问题写进报告，不自行修复。
