# 05-派工-画师统计页补全-20260808.md

> 派工：一号 → 五号（bugfix）
> worktree：`../artist-commission-w5`（分支 `beta/stats-page-full`）
> 背景：用户拍板「画师统计独立页做全：总览 + 按日趋势 + 事件明细」。现状 StatsPage.vue 只有 98 行基础版（总览 + byDay 趋势），**缺事件明细（byName）区块**
> 基线：master `d54fe8a`。**开工第一步 `git merge master`**。

## 现状（一号已核实）

- `web/src/views/artist/StatsPage.vue`（98 行）：总览卡（total）+ byDay 柱状图（纯 CSS/div 实现，不引图表库）
- 后端已就绪：`GET /api/artist/tracking/summary?days=30`（tracking.routes.ts:148，返回 `{ mode, enabled, total, byDay, byName }`）
- 路由已接（/stats）

## 本批任务：补全事件明细（byName）区块

### 要做
1. **事件明细区**：读取 summary.byName（事件名分组计数，后端已返回），展示事件名 + 次数列表（表格或列表）
2. **保留**：总览卡 + byDay 柱状图（已有，不破坏）
3. **三态适配**：`mode === 'on'` 才有数据（三号后端契约：off=连采集停 / hidden=画师侧不显示 / on=显示）——页面顶部判断 mode，非 on 时显示提示而非空数据误导
4. 视觉：纸墨 token，双主题，对齐既有 StatsPage 风格

### 授权文件
- `web/src/views/artist/StatsPage.vue`（补 byName 区块）
- i18n：zh-CN.js + en.js（stats.* 新键）
- **不碰**：后端、其他页面

### 实现铁律
1. 不做假修复；不引图表库（继续纯 CSS/div）
2. i18n zh/en 同步；禁硬编码中文
3. 三态逻辑与后端契约一致（先读 tracking.routes.ts:148-161 确认 mode/enabled 返回结构）

### 验证门禁
1. `cd web && npm run test:web` → 215/215
2. lint 0 / build 过 / check:i18n 0
3. **截图**：统计页（有数据态 + 非 on 提示态），存 workspace/temp/stats-page/
4. `git diff --stat` 只动授权文件

## 交付

- commit 到分支 `beta/stats-page-full`，不推送不合并
- 交付报告 `docs/comms/05-to-01-统计页补全-交付-20260808.md`：byName 区块实现 + 三态适配 + 截图
