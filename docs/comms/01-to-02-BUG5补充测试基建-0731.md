# 一号 → 二号：BUG-5 补充——前端测试基础设施
> 日期：2026-07-31
> 补充派工：01-to-02-v017第一批派工-0731.md

## 背景

五号审计确认 web/ 目录下零测试文件、无 vitest 配置。BUG-5 重构 useSignatureRefresh.js 是建立前端测试基础设施的最佳切入点（67 行、逻辑清晰、可 mock）。

## 补充要求

在 BUG-5 修复的同时，搭建前端测试基础设施：

1. `cd web && npm i -D vitest @vue/test-utils happy-dom`
2. 新建 `web/vitest.config.js`（environment: 'happy-dom'）
3. 为 useSignatureRefresh.js 补测试（至少覆盖）：
   - 按图刷新：单图 error 只刷新该图 URL，其他图不动
   - 按图计数：errorRetries Map 独立计数，超限后不再刷新
   - 防重入：refreshing 锁生效
   - onUnmounted 清理定时器
4. mock `artistApi.refreshSignatures`，用 `vi.useFakeTimers()` 控制定时器

## 授权文件补充

| # | 文件 | 用途 |
|---|------|------|
| 11 | `web/vitest.config.js` | 新建，前端测试配置 |
| 12 | `web/package.json` | 新增 devDependencies |
| 13 | `web/src/composables/__tests__/useSignatureRefresh.test.js` | 新建，BUG-5 测试 |

## 验证标准补充

- `cd web && npx vitest run` 全绿
- 测试覆盖按图刷新 + 按图计数 + 防重入 + 清理
