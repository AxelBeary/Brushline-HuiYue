# 三号 → 一号：审计 P1 修复提交（3 项）
> 日期：2026-08-01

## 做了什么

第三方审计 P1 三项修复全部完成。

## 分支

`fix/audit-p1-batch`（worktree: `../artist-commission-p1`）

## 改了哪些文件

| 文件 | 变更 |
|------|------|
| `server/src/features/artist/artist.routes.js` | P1-1: /api/artists 列表过滤 hidden；公开 workflow 加 hidden 检查 |
| `server/src/features/pricing/pricing.routes.js` | P1-1: 公开报价加 hidden 检查 |
| `web/src/composables/useOrderForm.js` | P1-2: 粘贴参考图改用 uploaded.url（签名 URL）替代裸路径 |
| `web/src/api/index.js` | P1-3: 401 拦截器排除登录相关错误码（CODE_INVALID 等不触发登出） |
| `server/tests/routes.test.js` | 补 3 个测试（TC-RT-16e/f/g） |

## 修复详情

### P1-1：hidden 画师泄露（安全）
- /api/artists 列表：`.filter(a => ... && a.status !== 'hidden')`
- /api/artists/:subdomain/workflow：加 `|| artist.status === 'hidden'` → 404
- /api/public/pricing/:subdomain：加 `|| artist.status === 'hidden'` → 404
- 画师主页 /api/artists/:subdomain 已有 hidden 处理（UI-8，只返回最小信息），无需改

### P1-2：粘贴参考图 403
- 根因：references/ 目录不在 isPublicUploadPath 白名单（只有 images/ 公开），裸路径无签名 → 403
- 上传 API 已返回 `url: signedUrl(filePath)`，前端忽略了它手动拼裸路径
- 修复：`url: uploaded.url`（一行改动）

### P1-3：401 误登出
- 根因：登录码错误也返回 401，拦截器对所有 401 执行登出+跳转
- 修复：白名单排除 `CODE_INVALID / CODE_EXPIRED / CODE_TOO_MANY_ATTEMPTS / QQ_NOT_REGISTERED / MISSING_CREDENTIALS`

## 验证结果

- 396/396 测试全绿（19 文件）
- ESLint 零错误（3 个 warning 是五号 admin 测试既有问题，不在授权范围）

## 额外发现

无。三项修复范围清晰，未发现额外问题。
