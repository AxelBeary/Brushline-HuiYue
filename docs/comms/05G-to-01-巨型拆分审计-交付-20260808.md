# 巨型拆分审计 + 历史债务盘点

> 交付：五号（bugfix）→ 一号（主理人）
> 日期：2026-08-08
> 派工：`05G-派工-巨型拆分审计-20260808.md`
> 范围：web/src 全量巨型文件拆分可行性 + 全库历史债务（web + server）
> 方式：**只读审计**，未改任何代码
> 基线：master 已合入（`Already up to date`），分支 `beta/audit-large-files`

## ⚠️ 开工基线核实（先于审计，重要）

派工施工图基于旧基线，**以下假设已过时**，审计已按当前代码实际状态执行：

| 施工图假设 | 当前实际（核实证据） | 处置 |
|---|---|---|
| QueueBoard.vue ~1530 行待拆 | **已拆**（`322fa5d` v0.41）：主文件仅 181 行，逻辑已拆入 QueueBoardList.vue / QueueBoardCalendar.vue | 见下：拆分产物反成新巨型文件 |
| ManualOrder.vue ~1497 行待拆 | **已拆**（`e90224c` v0.42）：主文件 497 行，逻辑拆入 ManualOrderLeft/Right | 见下：ManualOrderRight 反成新巨型文件 |
| OrderDetail 拆分模式已验证（1523→1311） | 属实（`d2e8888` 抽 composable + `3dcd911` 拆 PaymentPanel/GalleryPanel），但现又长回 **1425 行** | 需二轮拆分，见 Top 施工单 |
| ArtStyleManager ~956 行 | 实际 944 行 | 属实，见施工单 |

STATUS 已知遗留「QueueBoard 1530 / ManualOrder 1497 待派」同属过时记录（v0.41/v0.42 已收编拆分）。

## 一、巨型文件清单（>300 行）

行数口径：Get-Content .Count（与 git 行数一致口径；全量 36 个文件 ≥300 行，其中 31 个组件/逻辑文件 + 2 个 locale + 3 个测试文件）。

### 组件/逻辑文件（31 个，按行数排序）

| 文件 | 行数 | template | script | style | 建议拆分 | 预估后 | 风险 | 优先级 |
|---|---|---|---|---|---|---|---|---|
| views/artist/OrderDetail.vue | 1425 | 1-599 | 600-1216 | 1217-1425 | script 再抽 composable（备注/附加项/价格弹窗/沟通文案），模板备注卡拆子组件 | 1425→~950 | 中（工作流核心页，两轮拆分模式已验证） | P1 |
| components/artist/queue/QueueBoardCalendar.vue | 1325 | 1-221 | 222-1021 | 1022-1325 | **月历 + 时间条双视图混居一文件**，拆 QueueBoardTimeline.vue（L428-1020 script + 对应模板/style） | 1325→~750+~600 | 中（共享 calOrders 数据源需先抽 composable） | P1 |
| views/client/OrderForm.vue | 1284 | 1-598 | 599-890 | 891-1284 | script 抽 useOrderFormSteps/useOrderFormTracking（埋点 L727-800 独立）；模板按 step 拆块 | 1284→~1000 | 高（客户端下单主路径，用户已拍板交互） | P3 |
| components/artist/order/ManualOrderRight.vue | 1122 | 1-384 | 385-904 | 905-1122 | 拆 ManualOrderStylePicker（画风/尺寸/增项三级，L37-350 模板 + L462-830 script）+ 价格面板 | 1122→~600+~400 | 中（录单核心，拆分模式已验证） | P1 |
| components/artist/ArtStyleManager.vue | 944 | 1-283 | 284-834 | 835-944 | 拆 StyleFormDialog / SizeFormDialog / AddonPanel（script L364-830 是三大对话框逻辑） | 944→~500+3×~150 | 中（**用户正与四号详谈增项交互优化，须协调**） | P2（协调后） |
| components/templates/TplGallery.vue | 847 | 1-264 | 265-432 | 433-867 | album 画册 / masonry 瀑布流双布局拆两个子组件（共享数据/筛选逻辑） | 847→~500+~250 | 中（4 模板共用，契约不可变） | P3 |
| views/artist/Settings.vue | 769 | 1-266 | 267-653 | 654-769 | 按 tab 拆 ProfileTab/ShowcaseTab/TemplateTab | 769→~350+3×~150 | 中（form 共享状态跨 tab） | P2 |
| components/ArtistLayout.vue | 685 | - | - | - | 侧边栏/顶栏/移动端抽屉拆 NavSidebar/HeaderBar | 685→~400 | 低 | P4 |
| views/artist/ArtworkManage.vue | 661 | - | - | - | 拆 ArtworkCard / UploadDialog / TagEditor | 661→~400 | 低 | P3 |
| composables/useOrderForm.js | 654 | - | - | - | 已是大 composable，可再细分（步骤/校验/埋点） | 654→3×~220 | 低 | P4 |
| components/artist/queue/QueueBoardList.vue | 612 | - | - | - | 拆 QueueOrderCard（行卡片渲染/拖拽） | 612→~380 | 低 | P3 |
| views/client/templates/ArtistHomeFolio.vue | 587 | - | - | - | 4 模板各自皮肤是设计（共享逻辑不共享皮肤），可抽公共逻辑 composable | 587→~450 | 低 | P4 |
| views/client/TrackOrder.vue | 568 | - | - | - | 流程/状态卡拆子组件 | 568→~400 | 低 | P4 |
| views/admin/ArtistManage.vue | 560 | - | - | - | 列表行 + 抽屉拆开 | 560→~380 | 低 | P4 |
| views/artist/ManualOrder.vue | 497 | - | - | - | 已拆过（v0.42），保持 | - | - | ✅ |
| views/artist/Watermark.vue | 497 | - | - | - | 画布逻辑抽 composable | 497→~350 | 低 | P4 |
| components/templates/TplStyleGrid.vue | 457 | - | - | - | 保持（模板共享组件） | - | - | ✅ |
| views/client/templates/ArtistHomeAtelier.vue | 454 | - | - | - | 同 Folio | - | - | P4 |
| views/client/templates/ArtistHomeGallery.vue | 418 | - | - | - | 同 Folio | - | - | P4 |
| components/admin/AdminLayout.vue | 409 | - | - | - | 保持 | - | - | ✅ |
| views/artist/PuzzlePage.vue | 389 | - | - | - | 拼图逻辑抽 composable | 389→~300 | 低 | P4 |
| views/client/templates/ArtistHomeClassic.vue | 384 | - | - | - | 同 Folio | - | - | P4 |
| views/artist/ScheduleSharePage.vue | 375 | - | - | - | 保持 | - | - | ✅ |
| components/artist/StageListView.vue | 356 | - | - | - | 保持 | - | - | ✅ |
| components/artist/order/ManualOrderLeft.vue | 351 | - | - | - | 已拆过，保持 | - | - | ✅ |
| views/client/__tests__/OrderForm.stepnav.test.js | 344 | - | - | - | 测试文件，不参与拆分 | - | - | - |
| web/src/api/index.js | 343 | - | - | - | 可考虑按域拆（auth/artist/admin/order） | 343→4×~90 | 低 | P4 |
| views/artist/PriceCalculator.vue | 335 | - | - | - | 保持 | - | - | ✅ |
| views/artist/OrderList.vue | 328 | - | - | - | 保持 | - | - | ✅ |
| components/templates/TplHero.vue | 323 | - | - | - | 保持 | - | - | ✅ |
| components/artist/PaymentBar.vue | 307 | - | - | - | 保持 | - | - | ✅ |

### 非组件大文件（不参与拆分）

- `locales/en.js` 1466 行 / `locales/zh-CN.js` 1462 行：**纯数据语言包，不建议拆**（i18n 整包加载是现状；懒加载优化属性能池 P2，见 STATUS，非拆分范畴）
- `composables/__tests__/useOrderForm.test.js` 824 行 / `ManualOrder.stylemode.test.js` 516 行：测试文件，行数大属正常（多用例）

## 二、Top 拆分施工单（每文件：组件边界/依赖/事件流/验证）

### S1. OrderDetail.vue（1425 行）— 二轮拆分

- **现状**：template 1-599（信息卡/日期卡/时间线/待收横幅/操作条/备注区/附加项/收款/图库），script 600-1216，style 1217-1425。已拆 PaymentPanel/GalleryPanel（3dcd911）+ 4 个 composable（d2e8888: workflow/gallery/deadline/payment）。
- **建议边界**：
  - `useOrderNotes`（L775-860：备注/图片上传/拖拽/预览）
  - `useOrderExtra`（L865-908：附加项弹窗）
  - `useOrderPriceDialog`（L910-956：改价弹窗）
  - `useOrderComm`（L957-1050：沟通话术复制/语音）
  - template 备注卡（含上传/拖拽）拆 `OrderNoteCard.vue`
- **依赖**：全部基于 `order` ref（单源），composable 接受 order 参数即可，事件不上行（与 d2e8888 模式一致）。
- **风险**：中。此页是订单核心页，改动面大。但前两轮拆分已建立模式（模板原样搬移 + 事件上行 + 样式 scoped + 视觉零变化），照抄即可。
- **验证**：拆分后 vitest 全绿 + `OrderDetail.duebanner.test.js` 不破坏 + 浏览器实测订单详情页全功能（备注上传/改价/附加项/收款）+ 截图对比 0 像素差。

### S2. QueueBoardCalendar.vue（1325 行）— 拆时间条

- **现状**：**一个月历 + 一个时间条视图塞在同一个文件**（template v-if calendar L3-100 / timeline L101-221；script 月历 L245-427 / 时间条 L428-1020）。时间条含 Excel 式滚动、拖拽改期、缩放档位、画布覆盖订单范围（v0.42 批F）。
- **建议边界**：拆 `QueueBoardTimeline.vue`（时间条全部：L101-221 模板 + L428-1020 script + 对应 style）。月历留在本文件。
- **依赖**：两视图共享 `calOrders` 数据源（L325-355：bandRange/parseDate/dateKey）。**必须先抽 `useQueueCalendarData` composable** 再拆，否则重复计算逻辑。
- **风险**：中。时间条交互复杂（拖拽/缩放/滚动），拆分后事件流（refresh-all emit）必须保持。
- **验证**：vitest + 浏览器实测：月历翻月/时间条拖拽/缩放档位/今天线全部功能回归 + 截图对比。

### S3. ManualOrderRight.vue（1122 行）— 拆画风选择器

- **现状**：录单右栏。档位模式（L13-36）+ 画风模式（L37-350：画风→尺寸→增项三级 + 自定义增项 + 价格预览）+ 提交逻辑（L714-900）。defineModel 双向绑定 8 个字段 + emit submit-success/dirty。
- **建议边界**：拆 `ManualOrderStylePicker.vue`（画风模式全部：L37-350 模板 + L462-830 script，含自定义增项 R5 + 图片开关 R6）。主文件留档位模式 + 提交。
- **依赖**：stylePicker 需要 props.styles/usageMultipliers/rushMultipliers + defineModel 回传 tierId/description/priority/deadline 等；价格计算留在主文件（或拆 ManualOrderPricePanel）。
- **风险**：中。defineModel 多字段 + 校验（R2 手输价放行）与父组件 form 提交耦合，需保持提交链路一致。
- **验证**：`ManualOrder.stylemode.test.js`（436 行）全覆盖样式模式断言——拆分后必须全绿 + 浏览器实测录单（画风/档位/自定义增项/提交）。

### S4. ArtStyleManager.vue（944 行）— 拆对话框（**待增项协调后**）

- **现状**：script L284-834。三个大对话框逻辑内联：画风（styleDialog L364-462）、尺寸（sizeDialog L465-700）、增项模板（addon L701-830）+ 上传逻辑。
- **建议边界**：`StyleFormDialog.vue` / `SizeFormDialog.vue` / `AddonPanel.vue`。
- **依赖**：全部基于 `styles/addonTemplates` 顶层 ref（L293-295），拆后 props+emit 上行。
- **风险**：中。**用户正与四号详谈增项交互优化（增项改造）**，若增项先改，拆分须后置，避免双重返工。施工图已注明此协调点。
- **验证**：拆分后 ArtStyleManager 全功能浏览器回归 + 与增项改造批次冲突检测。

### S5. Settings.vue（769 行）— 按 tab 拆

- **现状**：profile/showcase/template 三 tab（L16+ el-tabs）。rules 文本 + link 校验 + 表单 + 模板预览全在一个 script（L267-653）。
- **建议边界**：ProfileTab / ShowcaseTab（rules+links）/ TemplateTab。
- **依赖**：`form` reactive（L353）跨 tab 共享 + `beforeTabLeave`（L16），拆分需先定义 tab 间共享契约（form 放父或 provide/inject）。
- **风险**：低-中。共享状态改动面大但逻辑独立。
- **验证**：浏览器实测三 tab 保存/切换 + 校验提示不丢。

## 三、历史债务清单（文件:行号）

### 3.1 死代码（未使用导出/组件/API）

| 项 | 证据 | 级别 | 建议 |
|---|---|---|---|
| **画师端档位 API 整组死封装**（6 个） | `web/src/api/index.js:132-140` getTiers/createTier/updateTier/deleteTier/setTierVisibility/reorderTiers —— node 全库扫描 0 调用；画师端档位管理已迁移到画风模型（ArtStyleManager 用 createArtStyle/createStyleSize 等），TierManage.vue:36-42 仅挂载新模型管理器 | 🔴 | 删 6 个封装；**先由三号核实后端 `/api/artist/tiers*` 路由零消费再删路由**（artist.routes.ts 仍有 GET/PUT/DELETE tiers 路由） |
| 管理端 `createArtistArtwork` | `web/src/api/index.js:360` 0 调用（admin 用 getArtistArtworks/createArtistArtwork？实查仅 getArtistArtworks 在用，create 死） | 🟡 | 删封装（后端 admin.routes.ts 对应 POST 路由一并核实） |
| 管理端 `updateArtistTier` | `web/src/api/index.js:357` 0 调用（ArtistDetailDrawer.vue 用 getArtistTiers/createArtistTier/deleteArtistTier） | 🟡 | 删封装 |
| `TplCoverShowcase.vue` 死组件 | `web/src/components/templates/TplCoverShowcase.vue` 全库 0 引用（v0.25 封面轮播已由 4 模板各自实现，4 模板 import 清单均无它） | 🟡 | 删文件 |
| `HySpinner.vue` 死组件 | `web/src/components/shared/HySpinner.vue` 全库 0 引用（HySkeleton 在用但未引用 HySpinner） | 🟢 | 删文件（先确认无动态 import，已查无） |
| server `getTotpStatus`/`getBoundTotpSecret` | `server/src/features/auth/auth.service.ts` 定义处仅 1 次出现，全库无调用 | 🟢 | 删导出（保守起见可保留） |
| server `computeTotp` | `server/src/features/auth/totp.ts:116` 仅定义处 1 次（内部验证用 computeTotpAtCounter L118/138，非 computeTotp） | 🟢 | 删导出或标 deprecated |

### 3.2 TODO/FIXME/HACK/XXX

**✅ 全库 0 处**（web/src + server/src，大小写精确匹配 `\b(TODO|FIXME|HACK|XXX)\b`，排除测试文件）。无遗留 TODO。

### 3.3 过期注释

- 未发现「引用行号/行为/API 已变化」的过期注释。版本注释（v0.xx）是项目惯例（功能来源标注），抽查均与现状一致（如 api/index.js:249 注释的旧增项删除记录属实）。
- 中文遗留标记（待补/待实现/暂时/凑合/以后再说）扫描 0 处。

### 3.4 重复实现

| 项 | 证据 | 级别 | 建议 |
|---|---|---|---|
| **金额显示 toFixed(2) 内联重复** | `OrderForm.vue` 25 处 / `ManualOrderRight.vue` 19 处 / `PriceCalculator.vue` 6 处 均为 `¥{{ (x ?? 0).toFixed(2) }}`；`utils/money.js` 仅有 formatCents（分→元），无"元金额显示"助手 | 🟡 | 加 `formatYuan(n)` 到 money.js，批量替换（低风险，视觉零变化） |
| 空态组件混用 | InkEmpty（画师后台）与 el-empty（客户端/管理端）并存——分场景样式，非 bug | 🟢 | 可选统一，非必须 |
| fetch 封装 | 已统一在 api/index.js（axios 实例 + 拦截器 + i18n 错误码） | ✅ | 无重复 |
| 日期格式化 | utils/datetime.js 已统一 formatDateTime/formatDateTimeShort | ✅ | 无重复 |
| 后端重复 SQL | 抽查 order.service/order-queue.service 的 `LEFT JOIN price_tiers` 关联为正常查询非重复 | ✅ | 无 |

### 3.5 历史遗留核对（STATUS 对照）

| STATUS 记录 | 核实结果 |
|---|---|
| lint 基线 warnings：OrderDetail 3 死解构 + OrderForm onMounted 未使用 | ✅ **已清**（本审计核实：OrderDetail L682 解构已无 currentStageIdx/nextStage/daysLeft；OrderForm 无 onMounted） |
| QueueBoard 1530 / ManualOrder 1497 待派 | ✅ **已拆**（v0.41/v0.42），STATUS 记录过时，见开头基线核实 |
| OrderDetail 死解构 3 个（currentStageIdx/nextStage/daysLeft） | ✅ 已清 |
| repowiki P2 / 性能池 / 第三方报告 P2 候选 | 非本批范围，保持池内 |

## 四、✅ 已查无问题分区

- TODO/FIXME/HACK/XXX：全库 0 处
- utils 未使用导出：0（dashboard-normalize/linkValidation/message-parser/reply-templates/sanitize/simpleIcons/watermark/useCountUp 全部有引用）
- 中文遗留标记（待补/待实现/暂时/凑合）：0 处
- 编码：ArtStyleManager/QueueBoardList 中文注释正常（UTF-8 无 BOM；PowerShell 默认 GBK 显示乱码是假象，非文件问题）
- server 后端：无 TODO 无重复 SQL，健康检查/service 均为正常实现
- 模板共享组件（TplHero/TplTierGrid/TplRules/TplStickyCta/TplStatusBadge/TplStyleGrid）：行数 < 300 或已合理，无拆分必要
- locales 大文件：语言包正常，不拆

## 五、优先级建议（低成本高收益 Top 5）

1. **🔴 删画师端档位死 API 封装**（api/index.js:132-140 六件套）——纯删零风险，防新人误用旧模型；先请三号核实后端路由零消费
2. **P1 QueueBoardCalendar 拆时间条**（S2）——月历/时间条双视图同文件是当前最大维护痛点，时间条独立后可各自演进
3. **P1 ManualOrderRight 拆画风选择器**（S3）——录单页核心逻辑解耦，`ManualOrder.stylemode.test.js` 已建立完整测试网可兜底
4. **P1 OrderDetail 二轮拆分**（S1）——沿用已验证模式（d2e8888/3dcd911），零行为变化拆分最稳
5. **🟡 formatYuan 统一金额显示**（OrderForm 25 处 + ManualOrderRight 19 处）——批量替换低风险，消除 50 处重复

> 附注：S4 ArtStyleManager 拆分**须等增项交互优化拍板后**再排（用户在谈，避免双重返工）；客户端页面（OrderForm/TplGallery）拆分风险较高且视觉已拍板，建议放 Beta 后。