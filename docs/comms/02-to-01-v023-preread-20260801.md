# 二号 → 一号：v0.23 预读报告（B4 核实 + B7 预读）

**分支**：`feat/v023-frontend`（worktree: `artist-commission-fe`，已 rebase 到 master `f671961`）

---

## 第 1 波（B4 留言板）：✅ 全部 4 项已在 F4（v0.19）完成，无需改动

| 派工项 | 实际状态 | 依据 |
|--------|----------|------|
| 1. TplGuestbook.vue | ✅ 完整实现 | `components/shared/TplGuestbook.vue`（137 行）：昵称必填+200字限制+空状态+画师回复标识+分页加载+429限流处理，API 已对接（非 mock） |
| 2. 4 模板挂载 | ✅ 已挂载 | Classic L92 / Gallery L48 / Folio / Atelier 均有 `<TplGuestbook :subdomain="subdomain">` |
| 3. 画师端审核列表 | ✅ 已实现 | Dashboard.vue L36-53（area-guestbook 模块）+ API approve/reject/reply（api/index.js L109-112） |
| 4. 管理端 | ✅ 已实现 | AdminDashboard.vue L62-87（跨画师留言管理+强制删除）+ API getMessages/deleteMessage（L259-260）。注：GET /api/admin/messages 后端待三号补齐（v0.19 遗留，前端静默降级） |

**结论**：第 1 波取消。第 4 波（B4 对接 API）也无需——API 已对接。

---

## 第 2/3 波（B7 额度池前端）：预读完成，建议等后端合入后开工

### 三号后端状态

`feat/v023-backend` 无领先 master 的 commit——B7 后端尚未开工。

### 预读发现（4 项，影响实施）

**1. 🔴 PaymentBar 不在 OrderDetail——spec 描述与代码不符**

派工说"替换现有 PaymentBar 勾选交互"，但 PaymentBar 只被 `WorkflowPaymentEditor.vue` 消费（工作流收款比例编辑），消费方是 Settings/TierManage/ArtistDetailDrawer/DefaultWorkflowEditor——**与订单收款无关**。OrderDetail 里没有 PaymentBar。

B7 实际是：**OrderDetail 新增收款记录区**（§4.1 的 ASCII 布局），不是"替换 PaymentBar"。PaymentBar 本身保留（工作流比例编辑仍需要）。

**2. OrderDetail 已有价格小结区（L879-905）**

现有逻辑：后端返回优先 → 缺失时从 installments 本地计算兜底（`i.paid` 过滤累加）。B7 需将此区替换为额度池模型（已收/应收/待收 + 进度条 + 流水列表 + 记录/撤销弹窗 + 应收参考区）。

**3. TrackOrder 客户端分期展示（L103-107）**

现有：installments 列表（节点名 + 金额）。B7 第 3 波改为：进度条 + 四项数据（已付/下期应付/待付/总额），不显示节点名。需新增 `paidTotalCents`/`finalPriceCents` 字段消费。

**4. 管理端无订单详情页**

管理端只有 ArtistManage 的订单列表弹窗（el-dialog，无详情）。B7 第 3 波"订单详情加收款流水只读展示"需新建 UI 容器（建议在订单列表弹窗内加行展开或新建 drawer）。

### 开工建议

**等三号后端合入后开工**，理由：
1. B7 是替换型改动（重做收款区），非增量——契约先行并行需大量向后兼容分支，成本高
2. 后端尚未开工，等待窗口短
3. 契约已冻结（spec §3 API + §4 交互 + Q1-Q4/T1-T5 全确认），合入后可立即开工

### B7 实施计划（后端合入后执行）

| 波 | 工作 | 文件 | 预估 |
|----|------|------|------|
| 2 | 画师端收款区重做 | OrderDetail.vue + useOrderPayments.js（新）+ api/index.js + i18n | 3h |
| 3a | 客户端 track 页 | TrackOrder.vue + i18n | 1h |
| 3b | 管理端收款流水 | ArtistManage.vue（或新组件）+ i18n | 1h |

等待一号确认：(1) 第 1 波取消 (2) B7 等后端合入后开工。
