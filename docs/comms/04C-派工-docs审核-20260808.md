# 04C-派工：docs/readme 全面审核 + 部署须知更新（含 cron 备份）

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：四号（requirements）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w32`
> 分支：`beta/docs-audit`
> 交付报告：`docs/comms/04C-to-01-docs审核-交付-20260808.md`

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w32'
git merge master --no-edit
```

## 背景（用户 2026-08-08：「全部要做」「cron每日备份写进部署须知里」「找人仔细去审核我们的docs readme等」）

- **docs/readme 全面审核**：查过时/错误/缺失/矛盾内容
- **部署须知更新**：含 cron 每日备份（03E 备份脚本已合入，OPS.md 有示例——需并入部署须知）

## 授权文件（只许动这些）

1. `docs/` 全部 md（requirements/comms/archive/external-wiki 等）— 只读审核 + 修正过时
2. `README.md`（根）— 审核修正
3. `docs/OPS.md` — 部署须知（含 cron 备份）
4. `docs/comms/STATUS.md` — 状态行核对（只报告，不直接改——STATUS 由一号维护，发现过时列清单）

> 审核为主，修正限于「明显过时/错误/矛盾」；不重构文档结构。

## 任务 1：docs 全面审核（只读 + 修正过时）

**目标**：找「文档写的事实 vs 代码现实」的偏差（外部深度研判/三路核查已发现多例：REQ-035 状态行过时已修、changelog 版本、README 等）。

**审核清单**：
1. **README.md**：项目简介/功能列表/技术栈/启动方式/部署说明——对照代码现状（08-05 曾发现 README L99/L100 过时，核实是否已修）
2. **docs/requirements/ 全部 REQ**：状态行（✅/🟡/📋）+ 功能点描述——对照 master（重点 REQ-022/025/026/029/031/033/034/035/036）
3. **docs/changelog.md**：版本记录 vs 实际合入（v0.42-v0.44 对齐）
4. **docs/archive/**：归档文档是否标注「已归档」清晰（不逐篇改，抽查）
5. **docs/external-wiki/**：repowiki 是否过时（P0 重写过 3 篇，P2 未做——抽查标注）
6. **docs/OPS.md**：运维手册完整性（03E 新建，核对 + 补 cron 备份）
7. **docs/comms/STATUS.md**：状态行 vs 实际（列「待一号修正」清单，不直接改）

**修正原则**：
- 「明显过时/错误/矛盾」→ 直接改（如 REQ 状态行、README 功能列表）
- 「需要产品确认」→ 列清单交一号
- 每条修正带证据（git log / 代码位置）

## 任务 2：部署须知更新（含 cron 每日备份）

`docs/OPS.md`（或新建 DEPLOYMENT.md 如更合适）：
- **cron 每日备份**（用户点名）：
  ```
  # 宿主机 crontab（每天 02:30 备份）
  30 2 * * * cd /path/to/artist-commission && docker exec commission-web sh -c "cd /app/server && node --experimental-strip-types -e \"import('./scripts/backup-db.ts').then(()=>{})\"" >> /var/log/huiyue-backup.log 2>&1
  # 或更简单：docker exec 直接跑 npm run backup
  30 2 * * * docker exec commission-web npm --prefix /app/server run backup >> /var/log/huiyue-backup.log 2>&1
  ```
  （先读 backup-db.ts 实际运行方式 + 03E 交付报告确认命令，写准确）
- **恢复步骤**：备份文件 → cp 回 commission.db → 重启容器
- **保留策略**：最近 7 份
- **部署清单**：重建步骤（备份→build→up→Caddy restart→三层验证）
- **AUTH_DEV_MODE**：说明生产必须 false

**验收**：部署须知含 cron 备份命令（可直接复制用）、恢复步骤、重建清单。

## 门禁（提交前必须全绿）

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w32'
git status --short   # 确认改动面合理（docs 为主）
# md 无 lint；但改动后确认无残留冲突标记
```

## 交付要求

1. commit 描述中文（如 `docs: 全面审核修正 + 部署须知(cron备份/恢复/重建)`）
2. 交付报告写 `docs/comms/04C-to-01-docs审核-交付-20260808.md`：审核发现清单（修正了哪些 + 列给一号待定项）+ 部署须知改动说明
3. 结束前：`git status --short` 确认干净
4. **不推送、不合并、不改 STATUS**
