# 一号 → 二号：v0.15 前端任务

> 日期：2026-07-30
> 分支：`feat/v015-frontend-b1`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02`
> 需求文档：`docs/requirements/REQ-010-v0.15规划.md`

---

## 批次1：OrderDetail 备注区重构（R46 + R40）

**前置**：三号批次1（DELETE notes 接口）合入后开工。

### R46：备注删除前端

- 备注条目悬停显示 ✕ 删除按钮（与参考图删除交互一致）
- 点击删除 → ElMessageBox.confirm（C59 方案 C：单条用弹窗）
- 系统备注（`created_by='system'`）不显示删除按钮
- 删除成功后从列表移除（无需整页刷新）
- i18n：删除确认文案（中英）

### R40：时间线合并（纯前端）

- 将"状态卡"和"备注卡"合并为**活动时间线**视图
- 系统备注（状态变更）和画师备注按 `created_at` 混排
- 每条节点有类型标识（🔄 状态变更 / 📝 备注 / 🖼 带图备注）
- 操作条保持独立（不合并进时间线）
- 添加备注输入框移到时间线底部
- 方案 A（纯前端）：数据已在 `order.notes` 里，只改渲染方式
- 老订单（无工作流）时间线仍可用
- 操作条触发状态变更后时间线即时刷新
- i18n：时间线相关文案（中英）

**授权文件**：`web/src/views/artist/OrderDetail.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`、`web/src/api/index.js`（如需加 deleteNote 调用）

**批次1 完成后 commit + 通知一号审核。**

---

## 批次2：主页定制（R48 + R49前端 + R50）

**前置**：三号批次2（迁移 v15 + accent_color API）合入后开工。

### R48：头像上传

- Settings.vue 加 el-upload 头像上传入口
- 4 个客户模板（Classic/Gallery/Folio/Atelier）消费 `artist.avatar`
- 无头像时显示默认占位（首字母或图标）
- 验收标准见 REQ-009 R48（四条）

### R49：取色器前端

- Settings.vue 加 el-color-picker（预设色值，与 ThemePicker 5 色一致）
- 4 个客户模板通过 CSS 变量注入 `accent_color`（`--accent-color`）
- 未设置时回退模板默认色
- C61 已拍板：强调色只影响客户主页，不影响画师后台
- 验收标准见 REQ-009 R49（五条）

### R50：预览

- Settings.vue 加"预览主页"按钮
- 点击后新窗口/路由打开 ArtistHome，参数覆盖（avatar + accent_color 用当前设置值而非已保存值）
- 预览页顶部横幅"预览模式 — 修改尚未保存"
- 验收标准见 REQ-009 R50（五条）

**授权文件**：`web/src/views/artist/Settings.vue`、`web/src/views/client/ArtistHome.vue`、`web/src/views/client/templates/ArtistHomeClassic.vue`、`web/src/views/client/templates/ArtistHomeGallery.vue`、`web/src/views/client/templates/ArtistHomeFolio.vue`、`web/src/views/client/templates/ArtistHomeAtelier.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`、`web/src/api/index.js`、`web/src/router/index.js`（预览路由）

**批次2 完成后 commit + 通知一号审核。**

---

## 批次3：仪表盘（R51前端 + R52前端）

**前置**：三号批次2（deadline + todayTodo + todayStats API）合入后开工。

### R52：今日统计（紧凑行）

- Dashboard.vue 现有 4 统计卡上方加一行：`今日新增订单 ¥XXX（N 单） · 今日收入 ¥XXX（N 单）`
- 始终显示，无数据时 ¥0
- 异步加载，loading 态可接受
- i18n（中英）

### R51：截稿日 + 今日待办（两个独立卡片）

- 截稿日卡片：即将到期订单列表（7 天内 + 非终态），按 deadline 升序，每条显示订单号+客户名+截稿日+剩余天数（≤3 天红色警示），点击跳转详情，无到期显示"近期无截稿 🎉"
- 今日待办卡片：pending + revision + 今日截稿的订单清单，点击跳转，无待办显示空状态提示
- 两个卡片始终显示（不做"空间不足则隐藏"）
- i18n（中英）

### R51 补充：OrderDetail 截稿日设置

- OrderDetail.vue 基本信息区加日期选择器（el-date-picker），设置/修改/清除截稿日
- ManualOrder.vue 录单时可填截稿日

**授权文件**：`web/src/views/artist/Dashboard.vue`、`web/src/views/artist/OrderDetail.vue`、`web/src/views/artist/ManualOrder.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`、`web/src/api/index.js`

**批次3 完成后 commit + 通知一号审核。**

---

## 提交规范

- `feat(order): R46 备注删除前端 + R40 时间线合并` / `feat(settings): R48 头像上传 + R49 取色器 + R50 预览` / `feat(dashboard): R51 截稿日待办 + R52 今日统计`
- 每批次一个或两个 commit
- 提交前 `git diff --stat` 确认只有授权文件
- 构建验证：`npm run build`（在 web/ 目录）通过再通知

## 关键 UI 决策提醒

- R40 时间线：操作条**不合并**进时间线，保持独立
- R51/R52：始终显示，不做响应式隐藏
- R49：accent_color **只影响客户主页**，画师后台不变
