# 05→01：第三方垃圾检测报告核实（可能过时，逐条排查）

> 日期：2026-08-05 · 执行：五号 · 模式：只读排查（未删任何文件）
> 用户提示「可能过时 需要排查」——**确认：报告约 40% 过时/误报，且含 2 处危险建议（WAL 删除、回收站一刀切），不可直接执行**

## 逐条核实

| 报告项 | 结论 | 现状核实 |
|--------|------|---------|
| 1. WAL/SHM「可直接删除」 | ❌ **危险误报** | `server/data/commission.db` 实测 **journal_mode=WAL**，`-shm` 今日 11:36 更新=活跃运行时文件。删 WAL = 未 checkpoint 数据丢失。integrity_check=ok。**绝不能删**（Docker 内为 DELETE 模式，但宿主 DB 是 WAL） |
| 2. 测试产物可删 | ✅ **属实** | playwright-report ✓、test-results ✓、web/dist 236 文件（11.65MB，报告 227 接近）均 gitignored、可重建 |
| 3. `.recycle-bin` 建议清理 | ⚠️ **部分属实但建议危险** | 现状 **64 文件 26.38MB**（报告 56/16.96MB 过时低估）。其中 **39 个 ≤16B 损坏占位**（上传失败残留）可清；**25 个真实用户文件**（含 4 个 5.3MB 大图）= 软删除恢复保险（AdminDashboard 回收站可恢复）。**不能一刀切**——真实文件清理 = 不可恢复；应走应用层「清空回收站」功能或保留真实文件 |
| 4. DB 备份「保留 3-5 个」 | ⚠️ **路径错误 + 建议与项目策略冲突** | 备份在 **`data/` 根目录**（26 个文件，~7.9MB），非报告所说 server/data（那里 0 个）。STATUS 明确这些是**里程碑保险**（v38 迁移事故教训，bak-v037-pre-v41 等）。成本极低收益高，**不建议清理**（清理决策权在一号） |
| 5. search_results.json | ✅ **属实（重复确认）** | `git ls-files` 已跟踪（v0.8 起，AI 搜索产物，183B 空缓存）。可删 + gitignore（低风险，需一号授权 git 操作） |
| 5.2 workspace 空目录 | ⚠️ **部分过时** | `workspace/temp/` 存在但空（git 不跟踪空目录，无需处理）；**workspace-temp 不存在**（报告过时） |
| 6. notes/37 损坏 PNG | ❌ **过时** | `server/uploads/notes/37/` **已空**，5 个 13B PNG 已不存在 |
| 7. docs/待修复问题清单 重复 | ❌ **误报** | 两文件**大小不同**：根目录版 9433B vs archive 版 3310B。根目录版是活跃文档（已更新），archive 是历史归档——**非重复**，报告「大小完全一致」不实 |
| 空间回收预估 40-45MB | ⚠️ **偏高** | 最大两项（recycle-bin 26MB、WAL 5.5MB）均不建议删；实际可安全回收约 **12-13MB** |

## 结论与建议

### 可安全清理（低风险，共约 12-13MB）
1. `playwright-report/` + `test-results/`（~0.5MB，测试产物）
2. `web/dist/`（11.65MB，可 `npm run build` 重建——留着也无碍，建议随版本重建而非删）
3. 回收站内 **39 个 ≤16B 损坏占位**（上传失败残留，无恢复价值）
4. `server/search_results.json`（183B，删 + gitignore，需一号授权）

### 不可动（危险项，明确警告）
- ⚠️ **WAL/SHM**：DB 为 WAL 模式，删 = 数据丢失风险
- ⚠️ **回收站 25 个真实文件**：软删除恢复保险，清理 = 不可恢复
- ⚠️ **data/ 26 个 DB 备份**：STATUS 里程碑保险，保留策略由一号决策

### 已过时/误报（无需动作）
- server/ 下 WAL（不存在）、workspace-temp（不存在）、notes/37 损坏图（已空）、docs 重复（非重复）

## 备注
- 本次只读排查，未删任何文件、未建分支
- 回收站损坏占位清理建议**走应用层**（AdminDashboard 清空功能），避免目录级删除与 DB 记录不一致
- 若要执行清理，建议由一号派工授权，列出精确文件清单逐个操作
