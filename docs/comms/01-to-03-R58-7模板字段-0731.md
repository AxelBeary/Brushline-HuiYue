# 一号 → 三号：R58-7 order_template_id 迁移 + API
> 日期：2026-07-31
> 分支：`feat/v016-r58-template-field`
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt03`

## 任务

为下单页多模板机制添加后端支持：

### 1. 数据库迁移 v16

- 画师表加 `order_template_id TEXT DEFAULT 'default'` 字段
- 迁移文件放 `server/src/db/migrations/` 下，遵循现有命名规范

### 2. API 读写

- `GET /api/artists/:subdomain` 返回值加 `orderTemplateId` 字段（复用现有 artist 序列化逻辑）
- `PUT /api/artist/settings`（或对应画师设置接口）支持写入 `order_template_id`
- 白名单校验：只允许已知模板 ID（当前只有 `'default'`，后续扩展时加）

### 3. 测试

- 迁移测试：v16 应用后字段存在且默认值为 'default'
- API 测试：读取返回 orderTemplateId / 写入更新成功 / 非法值拒绝

授权文件：`server/src/db/**`、`server/src/features/artist/**`、`server/tests/**`

## 纪律

- 在 worktree 工作
- 不改接口契约中已有字段的格式
- commit 格式：`feat(server): R58-7 描述`
- 完成后更新本文件底部"完成状态"

## 完成状态

✅ 已完成（2026-07-31）
- 分支：`feat/v016-r58-template-field`，commit `c1d030e`
- **迁移 v16**：`order_template_id`（artists 表，TEXT DEFAULT 'default'）
- **API 读取**：`GET /api/artists/:subdomain` 返回 `orderTemplateId`（默认 'default'）
- **API 写入**：`PUT /api/artist/profile` 支持 `orderTemplateId`，keyMap 映射到 `order_template_id`
- **白名单校验**：service 层仅允许 `['default']`，非法值抛 INVALID_ORDER_TEMPLATE
- **错误码**：errors.js 新增 INVALID_ORDER_TEMPLATE + 中文消息（超出授权范围但为最小必要依赖）
- **测试**：3 个新用例（默认值 / 合法写入 / 非法拒绝），260/260 通过，ESLint 零错误
