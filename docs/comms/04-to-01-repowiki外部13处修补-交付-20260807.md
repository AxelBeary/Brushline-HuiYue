# 04-to-01 repowiki 外部 13 处修补交付

- 派工：`docs/comms/04-派工-repowiki外部13处修补-20260807.md`（一号 → 四号）
- 完成日期：2026-08-07
- 核对基准：分支 `beta/wiki-patch`（worktree `../artist-commission-w4`），开工前已 `git merge master`（Already up to date，分支 HEAD `a7a9b99` 已含 master `ba546a4`）
- 路线：**B（仓库内维护）**——13 篇修订版落盘 `docs/external-wiki/`；外部原文（`C:\Users\qly19\Desktop\repowiki\`）**一字未动**
- 核对人：四号（需求整理者）
- 纪律遵守：只动授权文件；不推送、不合并；git 只 add/commit 到 `beta/wiki-patch`

---

## 一、完成总览

13 处全部 ✅。每处修订均先经代码证据核实（Test-Path 文件存在性 + Select-String 关键符号），再落盘修订版。

| # | 外部文档 | 修订版 | 状态 |
|---|---|---|---|
| 1 | `API 接口文档\画师管理接口.md` | `wiki-画师管理接口.md` | ✅ |
| 2 | `后端开发指南\认证授权系统\会话管理.md` | `wiki-会话管理.md` | ✅ |
| 3 | `架构设计\前后端通信协议\认证协议.md` | `wiki-认证协议.md` | ✅ |
| 4 | `项目概述\核心功能特性\认证授权系统.md` | `wiki-认证授权系统.md` | ✅ |
| 5 | `架构设计\安全架构.md` | `wiki-安全架构.md` | ✅ |
| 6 | `故障排除\错误码参考.md` | `wiki-错误码参考.md` | ✅ |
| 7 | `部署运维\监控告警系统.md` | `wiki-监控告警系统.md` | ✅ |
| 8 | `数据库设计\数据迁移管理.md` | `wiki-数据迁移管理.md` | ✅ |
| 9 | `数据库设计\数据访问模式.md` | `wiki-数据访问模式.md` | ✅ |
| 10 | `核心功能模块\仪表盘与分析.md` | `wiki-仪表盘与分析.md` | ✅ |
| 11 | `项目概述\项目结构说明.md` | `wiki-项目结构说明.md` | ✅ |
| 12 | `故障排除\日志分析.md` | `wiki-日志分析.md` | ✅ |
| 13 | `knowledge\zh\环境变量与运行时配置系统.md` | `wiki-环境变量与运行时配置系统.md` | ✅ |

---

## 二、逐条修订证据（以 master 代码实测为准）

### 认证系 6 处

**#1 画师管理接口**：cite 与正文 `server/src/app.js`/`index.js` 全部替换为 `.ts`（含 mermaid 图 `index.ts`/`app.ts`）。
证据：Test-Path `server/src/app.ts`=True、`server/src/app.js`=False；`index.ts`=True、`index.js`=False。

**#2 会话管理**：图例 `POST /login (用户名/密码/TOTP)` → `POST /api/auth/verify (QQ号+TOTP)`；删「刷新接口」表述（含 mermaid 类图 `+refresh(req,res)`、流程图「Token 刷新」整段、性能段、故障排查、附录）。
证据：auth.routes.ts 仅 3 端点（`POST /api/auth/verify` L26 / `GET /api/auth/me` L85 / `POST /api/auth/logout` L95），**全库无会话刷新接口**（refresh 命中均为订单锁价 `refreshInstallmentLocks` 与文件签名 `refresh-signatures`）。
澄清：`useSignatureRefresh.js` 是**文件签名 URL 刷新**（受保护文件访问签名 15 分钟过期），不是会话刷新——已在文档中显式注明。

**#3 认证协议**：图例 `POST /login` + `POST /verify-totp` → 唯一 `POST /api/auth/verify`；删「自动续期」「滑动过期」描述，明确会话固定 7 天、过期需重新登录。
证据：同 #2；auth.service.ts `createSession`（L180-181）payload 含 `t: Date.now()`、`v: tokenVersion`。

**#4 认证授权系统**：「登录视图：处理用户名/密码与TOTP输入交互」→「仅 QQ 号 + 6 位 TOTP 动态口令单步登录（REQ-027）」；mermaid 序列图 `POST /login {username,password}` → `POST /api/auth/verify {qqNumber, code}`。
证据：web/src/views/artist/Login.vue L17/L21 仅 `qqNumber` + `code` 两输入框，无用户名/密码字段。

**#5 安全架构**：简介删「JWT/HMAC Token」→「HMAC 签名会话 Token」；「上传与静态资源由 app.js 统一处理」→ `app.ts`；mermaid 图 app.js→app.ts。
证据：`server/src/**` 全库 `jwt` 零命中（仅 health.service.ts 健康检查**显示名**叫 JWT_SECRET、实际查 SESSION_SECRET——P0 批已核实）；会话为 HMAC 签名（auth.service.ts createSession）。

**#6 错误码参考**：附录「NOT_LOGGED_IN/SESSION_EXPIRED/TOKEN_REVOKED：跳转登录页或刷新 Token」→「重新登录（无刷新令牌机制）」；正文 app.js→app.ts（5 处含 mermaid）。
证据：同 #2（无刷新端点）。

### 非认证 7 处

**#7 监控告警系统**：cite 5 处 `server/src/app.js` → `app.ts`（已全部替换，app.js 计数 0）。
证据：Test-Path 确认。

**#8 数据迁移管理**：`connection.js` → `connection.ts`（cite/正文/mermaid 全部）；演进列表补 **v44**（`tracking_events_anon_tokens`：events + anon_tokens 埋点表）+ **v45**（`tracking_events_artist_index`：events.artist_id 索引），「当前最新版本为 **v45**」。
证据：`server/src/db/init.js` MIGRATIONS 数组实测：L1650 `version: 44, name: 'tracking_events_anon_tokens'`；L1679 `version: 45, name: 'tracking_events_artist_index'`。

**#9 数据访问模式**：cite 4 处 `connection.js`/`seed.js`/`app.js`/`index.js` → `.ts`（init.js 保持 .js）；正文「连接管理（connection.js）」「种子数据（seed.js）」→ .ts。
证据：Test-Path 确认 connection.ts/seed.ts/app.ts/index.ts 均存在、对应 .js 不存在、init.js 仍存在。

**#10 仪表盘与分析**：删「实时数据更新机制：WebSocket」「确认 WebSocket 连接状态」→「REST 拉取（无 WebSocket）」；故障排查同步改；**补「埋点数据看板（v44 tracking）」章节**（⚪ 缺失项）。
证据：`server/src`+`web/src` 全库 `WebSocket|ws://|wss://` **零命中**；dashboard 端点实测 `/api/artist/dashboard/revenue|todo|activity`；tracking 端点实测 `/api/anon-token`、`/api/events`、`/api/admin/tracking/summary`、`/api/artist/tracking/summary`、`/api/admin/tracking-config`(GET/PUT)。

**#11 项目结构说明**：cite 3 处 `index.js`/`app.js`/`connection.js` → `.ts`；mermaid participant `index.ts`/`app.ts`/`connection.ts`；正文「查看 index.js」→ index.ts。
证据：Test-Path 确认。

**#12 日志分析**：cite `app.js` → `app.ts`；「根据 SENTRY_DSN 动态拼接 connect-src」→「`SENTRY_DSN_BACKEND || SENTRY_DSN`（app.ts:145，兼容旧名）」；附录变量清单补 DB_PATH/SESSION_SECRET/COOKIE_SECRET/ADMIN_QQ。
证据：app.ts L145 实测 `const cspSentryDsn = process.env.SENTRY_DSN_BACKEND || process.env.SENTRY_DSN`；L210 `const sentryDsn = process.env.SENTRY_DSN_BACKEND`。

**#13 环境变量与运行时配置系统**：删 `BOT_ENABLED`/`BOT_WS_URL`（REQ-028 未实施）与 `LOGIN_CODE_TTL`/`LOGIN_CODE_MAX_ATTEMPTS`（v41 已移除）→ 业务开关类改为 `AUTH_DEV_MODE`、认证参数类注明「无（已随 v41 移除）」；「SESSION_SECRET（JWT/…）」措辞 →「会话 HMAC 签名 + 文件访问签名 + Cookie 签名；.env.example 注释仍写 JWT 签名，实际无 JWT」；source_files 内 index.js/app.js → .ts。
证据：全库 `BOT_ENABLED`/`BOT_WS_URL`/`LOGIN_CODE_TTL`/`LOGIN_CODE_MAX_ATTEMPTS` 零命中；`.env.example` 权威清单实测：必填 `SESSION_SECRET`/`COOKIE_SECRET`/`ADMIN_QQ` + `NODE_ENV`/`AUTH_DEV_MODE` + 可选 `DB_PATH`/`UPLOAD_DIR`/`CORS_ORIGIN`/`DOMAIN`/`SENTRY_DSN_BACKEND`。

---

## 三、验证门禁结果

1. ✅ **引用文件 Test-Path**：16 个关键引用全部存在（app.ts/index.ts/connection.ts/seed.ts/init.js/auth.routes.ts/auth.service.ts/tracking.routes.ts/tracking.service.ts/dashboard.routes.ts/useSignatureRefresh.js/Login.vue/middleware auth.ts/errors.ts/rate-limit.ts/health.service.ts）。
2. ✅ **关键符号 Select-String**：`POST /api/auth/verify` 7 处（会话管理/认证协议/认证授权系统/画师管理接口）；`SENTRY_DSN_BACKEND || SENTRY_DSN` 1 处（日志分析）；`v44`/`v45` 已写入（数据迁移管理）。
3. ✅ **修订版落盘**：13 篇 `wiki-*.md` 全部 Test-Path=True（防"幻觉完成"）。
4. ✅ **残留扫描**：13 篇内 `有有`、`POST /login`、`/api/auth/login`、`verify-totp`、`BOT_ENABLED`、`BOT_WS_URL`、`SIGN_SECRET`、`JWT_SECRET`、`DATABASE_URL`、`RATE_LIMIT_*`、`server/src/*.js`（app/index/connection/seed）全部清零；`LOGIN_CODE_TTL` 仅保留在「已移除」说明句中。
5. ✅ **外部原文未动**：仅读取 `C:\Users\qly19\Desktop\repowiki\`，未做任何写入。

---

## 四、存疑标注（暴露不确定性）

1. **未逐行核对行号引用**：修订版中保留了大量外部原文的「章节来源」行号（如 `server/src/app.ts:17-318`），这些行号来自外部贡献者标注，本次修订只修正文件名未逐一验证行号精确性；若一号需要精确行号版本，可后续批次用 `Select-String` 逐篇校准（P0 三篇同款待办）。
2. **会话管理的 mermaid 类图**：删除了 `AuthService.refreshToken` 后类图结构保持其余方法不变；如需完全对齐代码实际导出函数名（createSession/verifySession 等），需进一步重绘——本次为修补批，未做结构级重写（与 P0 重写批分工一致）。
3. **#10 埋点章节为新增补写**：基于 tracking 路由/service 实测函数，未写前端看板页面（后端已就绪、前端待接入，STATUS v22 已注明）。
4. **README 追加行**：13 篇条目已加入归档表，与 P0 5 篇条目同格式；「P2 待办」已改为「P2 已完成」。

---

## 五、git 操作

- 分支：`beta/wiki-patch`（worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w4`）
- 新增：`docs/external-wiki/` 13 篇修订版 + 本报告
- 修改：`docs/external-wiki/README.md`（归档表追加 13 条目 + P2 状态更新）
- Commit：`docs: repowiki 外部 13 处修补批——认证 6 + 非认证 7 修订版 + README + 交付报告`
- 已遵守：只 add/commit 本次 docs 改动；**未推送、未合并**；外部 repowiki 原文一字未动。
