# 一号 → 三号：v0.19 收尾——P1-5 实施 + P2-7 修复

> 日期：2026-08-01
> 优先级：中
> 分支：fix/v019-cleanup

## 任务 A：P1-5 方案 A 实施（~30min）

方案已批准：阻止删除有活跃订单引用的 workflow stage。

### 改动

`server/src/features/artist/workflow.service.js` 的 `deleteStage`（约第 190-218 行）：

DELETE 前新增检查（事务内）：
```sql
SELECT COUNT(*) as c FROM orders
WHERE current_stage_id = ? AND status NOT IN ('delivered', 'cancelled')
```

有活跃订单 → 抛 `AppError(E.STAGE_IN_USE, 400, { count })`
错误消息：`该节点下有 N 个进行中的订单，请先完成或转移后再删除`

终态订单（delivered/cancelled）的悬挂引用不阻止删除。

### 新增

- `server/src/shared/errors.js`：`STAGE_IN_USE` 错误码
- `server/tests/workflow.test.js`：2 个测试（有活跃订单拒绝 / 无活跃订单允许）

## 任务 B：P2-7 uncaughtException 无 db.close()（~15min）

`server/src/index.js` 的 uncaughtException / unhandledRejection 处理器中，退出前调用 `db.close()`。

## 授权文件

- server/src/features/artist/workflow.service.js
- server/src/shared/errors.js
- server/src/index.js
- server/tests/workflow.test.js（或新建）

## 验收

1. `cd server && npx vitest run` 全绿
2. 有活跃订单的 stage 删除返回 400 + 友好提示
3. 无活跃订单的 stage 正常删除

## 交付

comms：`docs/comms/03-to-01-v019收尾修复-0801.md`
