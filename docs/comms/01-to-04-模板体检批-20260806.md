# 派工：客户端模板体检批（四号，只读）

> 日期：2026-08-06 · 派工人：一号 · 风险等级：**零**（只读巡检，零代码改动）
> 分支：`chore/template-check` · worktree：`../artist-commission-tpl`（已建好，基于 master `3479aa5`）
> 开工第一步：`git merge master` 再读本文件。巡检用该 worktree 起 dev server 即可，**不改任何代码**，交付报告落主仓 docs/comms（只读批无需分支交付）。

## 背景

用户请外部前端 AI 评审了客户端视觉，但**只审了 classic 模板**。其余三个模板（gallery/folio/atelier）与 classic 共享 `templates.css` 基础但各自 scoped 样式，可能有同类或独立问题。同时评审的多项指控已被一号静态核实为不实（见批 1 派工文末），说明外部评审可信度打折——本批任务就是**用实测而不是纸面推理**，给四个模板出一份可信的缺陷清单。

## 任务

1. **四个模板逐一浏览器巡检**：`ArtistHomeClassic/Gallery/Folio/Atelier.vue`，用 demo 数据起 dev server，亮/暗模式 + 有数据/空数据（无头像、无画风、无作品、无公告）各态截图
2. **按统一标准检查**（吸收评审属实项的同类问题）：
   - 动画：hover 位移是否 -2px、时长是否 0.15s、有无滥用 bounce（批 1 正在收敛全局规则，模板 scoped 样式里的漏网项记下来）
   - 空态防御：头像缺失/无画风/无作品时是否出现破图、指代不明的按钮、悬空大段留白
   - EP 出厂色泄漏：模板内 el-tag/el-button 是否渲染出与主题不符的出厂色
   - CTA 重复：同页多个一模一样的主操作按钮
   - 字体混用：展示字体（文楷/西文斜体）与正文的搭配是否有断裂
3. **分类**：① 实现走样（违反现有规范，可修）② 规范缺失（没规定这块）③ 数据态观感（空数据导致，非缺陷）④ 评审已提但一号核实不实的问题——若你在实测中复现了（说明一号静态核实漏了），**单独高亮标注**
4. 每个缺陷：截图存 `docs/audit-screenshots/template-check/`（该目录在 worktree 里建，交付时随报告一起给一号），记录页面/症状/定位（组件+行号）/建议优先级

## 交付

`docs/comms/04-to-01-模板体检清单-20260806.md`：缺陷清单 + 分类 + 截图索引 + 修复建议（按"批 1 合入后顺手修 / 独立小批 / 需设计拍板"三档分组）。

**铁律**：零代码改动；不改 STATUS；不派别人活。交付后通知用户转告一号。
