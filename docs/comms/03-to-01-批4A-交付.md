# 批4A 交付报告（三号 → 一号）

> 角色：三号（后端执行）
> 分支：fix/b4a（基于 master 9a90ac7）
> 日期：2026-08-09
> 施工图：docs/comms/批4-结构批-施工图-20260809.md
> 本批范围：项2（workflow 模板变更守卫，savePayment 按方案 b）+ 项3（server/scripts 纳入编译）
> 禁止项：项1（order_payment_installments 停写/迁移）、order.service.ts 收款写路径、init.js 迁移——本批未触碰

## 0. 结论摘要

- 项2 落地：§2.4 八处改动全部完成，三处挂守卫（updateStage 仅收款开关分支 / reorderStages / savePayment），savePayment 按用户拍板方案 b（放行 + 活跃订单存在时响应附 `appliesToNewOrdersOnly: true`，路由与前端透传 toast）。
- 项3 落地：demo-data 三个绝对导入改相对、orders INSERT 删 `tier_id`、seedCarol 旧 `price_tiers` 改为 `art_styles/style_sizes` 默认画风、补写 `order_payments` 流水 + 双源断言；新增 `tsconfig.scripts.json`，`typecheck` 改双跑。
- 验收四项全绿：`tsc --noEmit` 0 错、`tsc -p tsconfig.scripts.json --noEmit` 0 错、`npm test` 69 文件 1020 用例全绿、`oxlint src tests` 0 错（仅 2 条 HEAD 存量警告，本批未引入）。
- 提交：项2 一 commit、项3 一 commit、本交付报告一 commit，共 3 个，均在本分支，未 push/合并/碰 master。

## 1. 项2：workflow 模板变更守卫

### 1.1 改动清单（对应 §2.4）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `server/src/shared/errors.ts` | `E` 流程组新增 `WORKFLOW_PAYMENT_IN_USE`（`STAGES_RESET_BLOCKED` 旁） |
| 2 | `server/src/shared/errors.ts` | `ERROR_MESSAGES` 流程组新增含 `{count}` 插值的中文文案 |
| 3 | `server/src/features/artist/workflow.service.ts` | 新增 `countActivePaymentOrders`（§2.3 SQL 原样：orders JOIN order_payment_installments，非终态） |
| 4 | 同上 | `updateStage` 事务内 `takesPayment` 变更分支守卫（改名/描述/话术/随机开关零风险放行） |
| 5 | 同上 | `reorderStages` 事务内校验后、写 sort_order 前守卫 |
| 6 | 同上 | `savePayment` 事务内、首个写之前调用 countActivePaymentOrders（方案 b：不拦，仅标注） |
| 7 | `web/src/locales/zh-CN.js` / `web/src/locales/en.js` | errors 流程组新增 `WORKFLOW_PAYMENT_IN_USE`（`STAGES_RESET_BLOCKED` 旁） |
| 8 | 路由 + 前端 | `artist.routes.ts:484` / `admin.routes.ts:529` 透传 `appliesToNewOrdersOnly`；`WorkflowPaymentEditor.vue` 成功 toast + `workflow.paymentNewOrdersOnly` 双语键 |

### 1.2 savePayment 方案 b 说明

- guard 不拦：`savePayment` 事务内、首个 `UPDATE` 之前执行 `countActivePaymentOrders(artistId)`，>0 时不抛错，返回值改为 `{ stages, appliesToNewOrdersOnly: boolean }`。
- 路由口径：活跃订单存在时响应体附 `appliesToNewOrdersOnly: true`；无活跃订单时**不带**该字段（服务层布尔恒返回，路由层条件展开）。
- 前端透传：`WorkflowPaymentEditor.savePayment` 成功且 `res.appliesToNewOrdersOnly` 为真时 `ElMessage.info('比例已保存，仅影响新订单（已有订单按下单时快照不变）')`。
- 兼容性：`savePayment` 返回形状（数组 → 对象）已全仓核对，唯一消费方为两个路由与测试，无遗漏调用点。

### 1.3 测试（TC-WF-G1~G7）

- 追加至 `server/tests/workflow.service.test.js`，共 7 个用例：
  - G1 切换收款开关被拒（`WORKFLOW_PAYMENT_IN_USE`，detail.count=1）；
  - G2 reorder 被拒、无活跃订单放行；
  - G3 按 b 口径：savePayment 返回成功 + `appliesToNewOrdersOnly=true` + 新订单按新比例（2000/8000）生成 + 既有订单快照不变（3000/7000）；
  - G4 改名/话术放行；G5 缓冲/0 价单（无分期行）三处放行且无标注；G6 delivered/cancelled 终态不拦；G7 被拒前后 `refreshInstallmentLocks` 输出一致（防摊错节点）。
- 回归：`npm test` 1020 通过（基线 1013 + 新增 7）。

## 2. 项3：server/scripts 纳入编译

### 2.1 改动清单（对应 §3.2~3.5）

- `server/scripts/demo-data.ts`：
  - 三个 `/app/server/src/...` 绝对导入改 `../src/...` 相对导入（TS7 已移除 baseUrl/paths 绝对映射，§3.2 结论）；
  - orders INSERT 删除 v50 已清退的 `tier_id` 列（22 列 / 18 绑定参数，占位符与实参已逐位核对）；
  - `seedCarol` 旧 `price_tiers` INSERT 改为 `art_styles/style_sizes` 默认画风（3 个尺寸各带示例图，对齐 SPEC-PRICE-2 / v51 库）；
  - 头注释更新容器运行姿势（废弃 /tmp 方案）。
- 新增 `server/tsconfig.scripts.json`（§3.2 配置原样：ES2022 / bundler / strict / types node+better-sqlite3+qrcode）。
- `server/package.json`：`typecheck` 改为 `tsc --noEmit && tsc -p tsconfig.scripts.json --noEmit`（CI 单命令同源）。

### 2.2 双源补全与断言

- 先补流水后加断言（顺序满足 §3.4）：`seedDemoOrders` 每单插入后按 `paidCents = Math.round(cents * s.paidRatio)` 补一行 `order_payments`（`installment_id=NULL` 额度池语义、`note='演示收款'`、`created_by='demo'`）；orders 的 `paid_total_cents` 与流水共用同一 `paidCents` 变量，保证双源严格相等。
- `assertFieldIntegrity` 正式区有报价订单追加：`COALESCE(SUM(order_payments.amount_cents),0) === paid_total_cents`，不等则 `problems.push`（真实库 ALICE-001~004 曾不一致，A5 实锤）。
- 幂等：脚本开头 `DELETE ... ALICE-%` 时 FK CASCADE 已清旧流水，复跑不重复。

### 2.3 容器运行姿势（更新后）

- 新姿势（已写入脚本头注释）：
  - `docker cp server/scripts/demo-data.ts commission-web:/app/server/scripts/demo-data.ts`
  - `docker exec -w /app/server commission-web npx tsx scripts/demo-data.ts`
  - 生产镜像 `COPY server/` 已含脚本，重建后直接执行第二条即可。
- 旧 `/tmp` 姿势因相对导入会 `MODULE_NOT_FOUND`，废弃。

## 3. 验收记录

| 项 | 命令 | 结果 |
|---|---|---|
| main typecheck | `cd server; npx tsc --noEmit` | 0 错 |
| scripts typecheck | `cd server; npx tsc -p tsconfig.scripts.json --noEmit` | 0 错 |
| 全量测试 | `cd server; npm test` | 69 files / 1020 tests 全绿 |
| lint | `cd server; npx oxlint src tests` | 0 错（2 条存量警告：`artist.service.ts` Tier 导入、`admin.routes.ts` intIdTid，HEAD 9a90ac7 已存在并经 HEAD 版复核，本批未引入） |

附加验证（不入库）：demo-data 以 `DB_PATH=:memory:` 冒烟——模块加载/相对导入运行时解析通过（空库在首次查表处按预期失败）；改造后 SQL 片段（orders 22 列 / 流水双源 / seedCarol 画风 / price_tiers 已 DROP）对 v51 内存库全量执行通过（临时脚本已删，未提交）。

## 4. 提交记录

| commit | 说明 |
|---|---|
| `1fea328` | `fix(server): 批4A workflow 模板变更守卫（savePayment 方案 b：放行 + appliesToNewOrdersOnly）` |
| `84ecdc2` | `fix(server): 批4A scripts 纳入编译（demo-data 相对导入 + v51 对齐 + 双源断言）` |
| 本次 | `docs(comms): 批4A 交付报告` |

## 5. 风险与回滚

- 项2：纯代码守卫，无迁移、无数据变更，`revert` 即回滚；`savePayment` 返回形状变化已全仓核对。
- 项3：相对导入依赖容器新运行姿势（`-w /app/server`），头注释已同步；`tsconfig.scripts.json` / `package.json` 改动纯编译期，不影响运行时 DB。
- 边界确认：本批对 `order_payment_installments`（项1 范围）、`order.service.ts` 收款写路径、`init.js` 迁移零改动；未 push、未合并、未触碰 master。
