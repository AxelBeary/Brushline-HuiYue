# 一号 → 五号：REQ-022 F5 留言管理筛选 · 安全范围批（不依赖用户拍板部分）

> 日期：2026-08-05 · 分支：`f5-guestbook-filter` · worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-f5`（基于 master `d22f471`）
> 风险等级：低（管理后台低频页，只加筛选不加字段）
> 依据：`docs/requirements/REQ-022-B类低优先需求.md` F5 + `docs/plan-req022-实施拆解.md` §四。

## 范围界定（先读这条，防越界）

F5 的「未读/已读」语义有 a/b/c 三个方向**待用户拍板，本批一律不碰**——不加 read_at 字段、不改文案映射。本批只做 REQ F5 中零语义争议的部分：

1. **按画师筛选**（REQ 原文「按画师分组（下拉选画师）」）
2. **按审核状态筛选**（现有 status 枚举 pending/approved/rejected，纯展示筛选）
3. **按是否已回复筛选**（现有 artist_reply 字段，replied_at 非空 = 已回复）
4. 最新优先排序（现有 created_at DESC，确认保持）
5. 后端 query 参数化（拆解 §4.2 建议：与全站列表一致性，后端加参数而非纯前端筛）

## 任务

### 后端（server/src/features/guestbook/）
- `getAdminMessages()` 加可选参数 `{ artistId?, status?, replied? }`：WHERE 动态拼装（artistId 数值、status 限枚举白名单、replied=1 → `artist_reply IS NOT NULL`、replied=0 → `artist_reply IS NULL`）。
- `GET /api/admin/messages` 路由透传 query 参数（status 非法值忽略或 400，与全站惯例一致即可）。

### 前端（AdminDashboard.vue 留言区）
- 留言卡头部加筛选行：画师下拉（数据源 adminApi.getArtists 已在页面加载）+ 审核状态下拉（三态）+ 是否已回复下拉（全部/已回复/未回复）。
- 筛选变更 → 重新请求后端（不在前端过滤全量）。
- i18n 双语新增键（admin.guestbook.* 下）。
- 状态列 el-tag 显示改为 i18n 文案（现在直接显示英文枚举值 `row.status`，顺手修掉）。

### 测试
- server：筛选参数组合测试（artistId 过滤、status 枚举、replied 两态、非法 status 处理）。基线 **server 831/831 + web 192/192**。

## 授权文件

- `server/src/features/guestbook/guestbook.service.ts`、`guestbook.routes.ts`、`server/tests/*`（guestbook 相关）
- `web/src/views/admin/AdminDashboard.vue`（仅留言区）、`web/src/api/index.js`（仅 getMessages 签名）、`web/src/locales/zh-CN.js`+`en.js`（仅新增键）

**黑名单**：web 其余（views/artist/ 二号视觉批进行中）、server 其余 features、迁移文件（本批零 schema 变更）、docs/requirements/。

## 交付

- commit 单行 `feat(admin): ...`，禁止 git add -A，不推送不合并。
- 报告 `docs/comms/05-to-01-F5留言筛选-交付-20260805.md`。
- 返回摘要：commit hash、测试结果、报告路径、遗留。
