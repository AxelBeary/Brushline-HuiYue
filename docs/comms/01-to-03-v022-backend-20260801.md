# 三号 v0.22 派工（2026-08-01）

> 分支：`feat/v022-backend`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-be`
> 基准：master `6164c2c`

---

## 任务清单（按优先级排序）

### 第 1 波（后端 API，二号前端等你）

| # | 项 | 说明 |
|---|---|---|
| B2-BE | 小公告后端 | artists 表加 announcement TEXT + announcement_expires_at DATETIME。迁移 v24。画师设置 API 扩展（PUT 含公告字段）。公开 API 返回公告（含过期判断：expires_at 非空且 < now 则不返回） |
| B3-BE | 点赞后端 | artworks 表加 like_count INTEGER DEFAULT 0。迁移 v25。`POST /api/public/artworks/:id/like`（+1）+ `DELETE`（-1，不低于 0）。不存访客身份，前端 localStorage 控制 |

### 第 2 波（工程）

| # | 项 | 说明 |
|---|---|---|
| A3 | TS 迁移第二批 | 路由层（server/src/routes/）+ 服务层（server/src/services/）。保持渐进，不改运行时行为。tsc --noEmit 零错误 |

### 第 3 波（为 v0.23 准备）

| # | 项 | 说明 |
|---|---|---|
| T1 | 点赞数据模型确认 | 草案建议方案 A（计数字段），你确认或提替代。回复写 comms |
| T2 | 留言板限流策略 | v0.23 留言板用，建议同 IP 每分钟 N 条。你定 N 和实现方式（现有 rateLimit 插件扩展？）。回复写 comms |

---

## 迁移注意事项

- v24 = 公告字段，v25 = 点赞字段（按顺序，别跳号）
- 迁移文件：`server/src/db/migrations/v24_announcement.sql`、`v25_like_count.sql`
- init.js 注册迁移
- 迁移必须可回滚（DROP COLUMN 或注释说明）

---

## 授权文件范围

- `server/src/db/migrations/`（新建 v24、v25）
- `server/src/db/init.js`（注册迁移）
- `server/src/routes/`（公告/点赞 API + TS 迁移）
- `server/src/services/`（TS 迁移）
- `server/src/types/`（新建/扩展类型）
- `server/package.json`（如需新依赖）
- `server/tsconfig.json`（如需调整 include）
- `server/test/`（对应测试）

不在授权范围的文件不要动。需要改其他文件先写 comms 说明。
