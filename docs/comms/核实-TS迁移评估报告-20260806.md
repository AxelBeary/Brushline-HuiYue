# 核实：TS/JS 统一迁移评估报告（中立 AI，2026-08-06）

> 来源：`C:\Users\qly19\WorkBuddy\2026-08-06-13-08-44\TS-JS统一迁移评估报告.md`（中立 AI 全量克隆实测，已归档 workspace/temp/ts-migration-report/）
> 核实人：一号 · 逐条对照 master `56a86e7` 代码实测
> 结论：**报告断言全部成立，方案采纳**（P0 开门禁 → P1 修空指针 → P2 后端收尾 → P3 拆 any → P4 前端类型岛）

---

## 一、断言核实表（外部报告 vs 代码实测）

| 报告断言 | 实测 | 结论 |
|----------|------|------|
| TS 版本 `^7.0.2`，typescript-eslint 不兼容 | server/package.json `"typescript": "^7.0.2"`；eslint.config.js 仅 `@eslint/js` 无 TS 解析器 | ✅ 成立 |
| ESLint 完全跳过 .ts（82% 代码漏检） | eslint.config.js 无 `files: ['**/*.ts']`（ESLint 9 flat config 默认只处理 .js） | ✅ **成立（最严重发现）** |
| CI 不执行 typecheck | ci.yml server job 只有 `npx eslint .` + `npm test`，无 `npm run typecheck` | ✅ **成立** |
| strict:false + checkJs:false | tsconfig.json 确认 | ✅ 成立 |
| strictNullChecks 报 18 个错误（含登录 7 处空指针） | 实测 `tsc --noEmit --strictNullChecks` 报错（admin.routes.ts null 赋值、err unknown 等） | ✅ **成立（真实雷）** |
| 后端 82.6% TS（42 .ts / 5 .js） | server/src 确为 42 ts + 5 js（app/index/connection/init/seed） | ✅ 成立 |
| any/@ts-ignore 193 处，order.routes.ts 独占 90 | 待 P3 阶段细核（报告给出路径，滚动验证） | 存疑待核 |
| 260 个 db 查询零类型标注 | 待 P3 阶段细核 | 存疑待核 |

## 二、采纳方案（P0-P4 分层）

| 阶段 | 内容 | 量级 | 风险 | 派工 |
|------|------|------|------|------|
| **P0 开门禁** | oxlint 补 TS 覆盖 + CI 加 typecheck + 开 `strictFunctionTypes`/`noImplicitThis`（实测 0 错） | ~1h 零业务改动 | 零 | 三号 |
| **P1 修空指针** | 开 `strictNullChecks` + `useUnknownInCatchVariables`，修 31 处（登录 7 + 配额 4 等） | ~半天 | 低（修真实雷） | 三号 |
| **P2 后端收尾** | connection/index/seed/app 4 文件转 TS（475 行）；init.js 豁免保留 .js + `// @ts-check`；tsconfig 加 `checkJs:true` | ~1 天 | 中低（930 测试 + incremental-ts-typing 配方兜底） | 三号 |
| **P3 拆 any** | db 查询泛型封装（queryOne/queryAll）+ Fastify 路由 body 泛型 + entities.ts 覆盖率 + noImplicitAny 棘轮 | 滚动 | 低 | 三号滚动 |
| **P4 前端类型岛** | 不做全量迁移；web 加 tsconfig + api/types.ts + api/index.js JSDoc + 新增 composable 用 TS | 按需 | 零 | 二号按需 |

## 三、约束

- **TypeScript 7.0 与 typescript-eslint 不兼容**（Go 移植 tsgo 不再暴露 programmatic API）——lint 走 oxlint（Rust，不依赖 TS 编译器 API），等 TS 7.1 再评估回归
- `tsc --noEmit` 在 TS 7 下完全正常（报告全部实测基于 7.0.2）
- 用户已拍板：确定迁移（2026-08-06）
- 方案：先 P0 后 P2（"先关门再搬家"），每步独立验证（typecheck + 930 测试全绿）
- ⚠️ 报告建议锁定 TS 精确版本（`^7.0.2` caret 会吞 7.x 快速演进）

## 四、用户决策记录

- 2026-08-06：用户确定迁移（"我确定想迁"），先存档，刷新容器页面后开工
- 待用户确认：方案 A（P0+P1+P2 一个批全派三号）vs 方案 B（先 P0 看效果）
