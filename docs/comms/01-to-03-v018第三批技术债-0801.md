# 一号 → 三号：v0.18 第三批技术债——schema 补齐 + 工具函数补测试

> 日期：2026-08-01
> 分支：`fix/tech-debt-b3`（从 master 拉，独立 worktree）

## 任务

### A：init.js schema 字符串补齐（P2，~1h）

STATUS.md 技术债 P2：init.js 的 schema 字符串缺 8 张表。补齐使 `PRAGMA table_info` 与代码一致。

### B：工具函数补测试（中风险，~1.5h）

测试覆盖审计指出：
- utils/date.js（toSqliteDate / localDayStartSqlite / localMonthStartSqlite）：时区敏感，零测试，5~6 用例
- utils/price.js（resolvePriceCents）：价格回退逻辑，零直接测试，3~4 用例

## 授权文件

- server/src/db/init.js
- server/tests/date-utils.test.js（新建）
- server/tests/price-utils.test.js（新建）

## 验收

- 全量测试通过
- 零业务逻辑改动（schema 补齐只改字符串描述）
