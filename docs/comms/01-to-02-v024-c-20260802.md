# 一号→二号：v0.24-C 留言管理 + 按钮自定义（2026-08-02）

## 任务

Spec：`docs/requirements/REQ-013-整理.md` #1 + #3 节。

### 波 1：#1 画师端留言管理独立页面（~3h）

后端 API 已齐（`GET /api/artist/messages` + approve/reject/reply），纯前端。

1. 新建 `web/src/views/artist/GuestbookManage.vue`：
   - 状态筛选标签（全部/待审核/已通过/已拒绝）
   - 留言列表：昵称、内容、时间、状态
   - 操作按钮：通过/拒绝/回复（复用现有 API）
   - 分页（后端已支持）
2. ArtistLayout.vue 侧边栏加"留言管理"菜单项（作品管理下方）
3. 侧边栏角标：有待审核留言时显示数量（onMounted 调一次 messages?status=pending 取 total）
4. 路由注册

### 波 2：#3 快捷按钮自定义（~3h）

1. DB：artists 表加 `quick_actions TEXT DEFAULT NULL`（JSON 数组，null=默认 6 个）
   - **等等，这涉及后端**。你只做前端部分：QuickActions.vue 读配置 + Settings 里加勾选 UI
   - 后端存储：先用 localStorage（`huiyue_quick_actions`），后续三号补 DB 字段
   - 不对——用户要求"设置里勾选"，应该持久化。**你先用 localStorage 做 MVP**，我让三号补 DB 字段后你再切
2. QuickActions.vue 改版：
   - 候选池 ~10 个按钮（手动录单/留言管理/排期看板/全部订单/作品管理/价格管理/主页设置/主页预览）
   - 读 localStorage 配置，无则默认 6 个
   - 3 列网格，N 个按钮自适应行数
   - 命名与侧边栏完全一致
3. Settings.vue 加"快捷按钮"配置区（接稿设置标签页下方或基本资料底部）：
   - 候选列表 + 勾选（最少 3 最多 9）
   - 保存写 localStorage

**授权文件**：
- `web/src/views/artist/GuestbookManage.vue`（新建）
- `web/src/components/ArtistLayout.vue`
- `web/src/components/artist/dashboard/QuickActions.vue`
- `web/src/views/artist/Settings.vue`
- `web/src/router/`
- `web/src/api/index.js`
- `web/src/i18n/`
- `web/test/`

**分支**：`feat/v024-c-guestbook`，worktree `../artist-commission-fe`（pull master 后建分支）。

完成后写 comms `02-to-01-v024-c-{日期}.md`。
