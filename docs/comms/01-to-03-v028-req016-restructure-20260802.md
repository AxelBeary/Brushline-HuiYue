# 一号 → 三号：v0.28 REQ-016 后台界面逻辑重组

> 日期：2026-08-02
> 分支：`feat/v028-req016-restructure`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-03`
> 需求文档：`docs/requirements/REQ-016-后台界面逻辑重组.md`（6 项决策用户已确认按四号建议）

## 任务（按顺序）

### A. 设置页 4 Tab 重划（~3h）

现有 Tab：基本资料（12 字段混装）/ 接稿设置（空壳）/ 主页模板 / 须知编辑

改为：

| 新 Tab | 装什么 |
|--------|--------|
| **基本资料** | 头像、昵称、编码、简介、联系QQ（5 字段） |
| **主页展示** | 接稿状态、公告、外链、平台链接、灵感标签、须知 |
| **模板与风格** | 模板选择、配色、强调色、封面图、预览 |
| **偏好** | 通知开关、默认面板、快捷按钮 |

- 删除"接稿设置" Tab（v0.26 搬走后的空壳）
- 须知编辑并入"主页展示"（不单独 Tab）
- 字段只是搬位置，不改后端 API（PUT /api/artist/profile 已全支持）

### B. 接稿状态归位（~1h）

- 仪表盘 StatusSwitch：**保留**（高频操作）
- 开稿管理 SlotManage.vue：状态从只读改为**可操作**（加切换按钮，调已有 API）
- 设置页：删除接稿状态单选框（不再重复）

### C. 侧边栏瘦身（~1h）

- 手动录单 `/orders/new` **移出菜单**（订单管理页已有按钮）
- 菜单加分隔线分组：
  - 工作：仪表盘 / 排期看板 / 订单管理
  - 经营：开稿管理 / 价格管理 / 作品管理 / 留言管理
  - 门面：设置

### D. app.js Windows 路径修复（~5min）

`server/src/app.js` 第 268 行附近：`filePath.startsWith(WEB_DIST + '/')` 在 Windows 上永远 false（resolve 产生反斜杠）。改为兼容写法（`path.sep` 或同时检查 `/` 和 `\`）。五号发现的，Docker/CI 不受影响但本地 E2E 全挂。

## 授权文件

- `web/src/views/artist/Settings.vue`
- `web/src/views/artist/SlotManage.vue`
- `web/src/views/artist/Dashboard.vue`（如需调整 StatusSwitch 位置）
- `web/src/components/ArtistLayout.vue`
- `web/src/router/index.js`
- `web/src/locales/zh-CN.js`
- `web/src/locales/en.js`
- `server/src/app.js`（仅 D 任务 1 行修复）

## 验证标准

- eslint 0 错误 + build 成功
- 设置页 4 Tab 切换正常，字段保存正常
- 侧边栏分组显示，手动录单不在菜单中
- 开稿管理可切换接稿状态
- app.js 修复后本地 `npm run dev` 静态资源正常加载

## 交付

comms `03-to-01-v028-req016-{日期}.md`
