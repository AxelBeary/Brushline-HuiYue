# 02-派工-画师统计页+埋点三态前端-20260807.md

> 派工：一号 → 二号（client-frontend）
> worktree：`../artist-commission-w2`（分支 `beta/artist-stats-ui`）
> 蓝本：v0.42 版本计划 Step 5 + 产品拍板（2026-08-07：埋点三态=关/不显/开，默认「不显」；画师统计独立页做全：总览+按日趋势+事件明细）
> 基线：master `a2b3b52`。**开工第一步 `git merge master` 再干活**。
> 门禁：web `npm run test:web` 全绿 + oxlint 0 + build 过 + **截图**（统计页/三态开关）

## 授权文件（只许动这些）

- `web/src/components/ArtistLayout.vue`（BASE_MENU_ITEMS 加 stats 项）
- `web/src/router/index.js`（加 /stats 路由）——实际路由文件路径 grep 确认
- `web/src/views/artist/StatsPage.vue`（新建，独立统计页）
- `web/src/views/artist/Dashboard.vue`（移除「我的数据统计」区块 L23-33 区域）
- `web/src/views/admin/TrackingAnalytics.vue`（开关改三态控件）
- `web/src/api/index.js`（若需加 stats 接口函数）
- i18n：`web/src/locales/zh-CN.js` + `web/src/locales/en.js`（zh/en 同步）

## 红线

- **不碰**：`web/src/styles/*.css`（动画批/UI 批刚合入，样式已有 token 体系——本批只做功能，样式用现有变量 `--sp-*`/`--fs-*`/纸墨 token）
- 不碰后端（statsMode 后端已就绪，仅前端消费）
- 不删 Dashboard 其他区块，只移除 tracking 区块
- i18n 键 zh/en 同步；禁新硬编码中文
- 前端校验只能是后端规则的子集

---

## 改动清单

### A. 画师统计独立页（用户拍板：侧边栏「门面」组导航项 + 独立页做全）

1. **ArtistLayout.vue**（BASE_MENU_ITEMS，L258-273 区域）：门面组加 stats 项：
```js
// before
  { index: '/settings', icon: Setting, labelKey: 'menu.settings', group: 'front' },
  { index: '/preferences', icon: Document, labelKey: 'menu.preferences', group: 'front' }
// after
  { index: '/stats', icon: TrendCharts, labelKey: 'menu.stats', group: 'front' },
  { index: '/settings', icon: Setting, labelKey: 'menu.settings', group: 'front' },
  { index: '/preferences', icon: Document, labelKey: 'menu.preferences', group: 'front' }
```
> ⚠️ `TrendCharts` 图标从 @element-plus/icons-vue import（grep 确认 ArtistLayout 现有 import 方式）。labelKey `menu.stats` 需在 i18n 加。

2. **路由**（grep 实际路由文件）：加：
```js
{ path: '/stats', name: 'Stats', component: () => import('../views/artist/StatsPage.vue') }
```
> 按现有路由模式（懒加载 + 放 ArtistLayout 子路由下）。

3. **StatsPage.vue 新建**（核心，三区块全做）：

```vue
<template>
  <ArtistLayout>
    <div class="stats-page">
      <h2 class="od-page-title">{{ $t('stats.title') }}</h2>

      <!-- 总览卡：总事件数 + 活跃天数 -->
      <div class="stats-overview" v-if="data">
        <div class="stats-overview-item">
          <div class="stats-overview-num">{{ data.total }}</div>
          <div class="stats-overview-label">{{ $t('stats.totalEvents') }}</div>
        </div>
      </div>

      <!-- 按日趋势：柱状图（纯 CSS/div 实现，不引图表库） -->
      <div class="stats-section" v-if="byDay.length">
        <h3 class="od-section-title">{{ $t('stats.byDay') }}</h3>
        <div class="stats-bars">
          <div v-for="d in byDay" :key="d.day" class="stats-bar-col">
            <div class="stats-bar" :style="{ height: barHeight(d.count) }" :title="`${d.day}: ${d.count}`"></div>
            <div class="stats-bar-label">{{ shortDay(d.day) }}</div>
          </div>
        </div>
      </div>

      <!-- 事件明细：byName 列表 -->
      <div class="stats-section" v-if="byName.length">
        <h3 class="od-section-title">{{ $t('stats.byName') }}</h3>
        <div class="stats-list">
          <div v-for="e in byName" :key="e.name" class="stats-list-row">
            <span class="stats-list-name">{{ $t(`events.${e.name}`, e.name) }}</span>
            <span class="stats-list-count">{{ e.count }}</span>
          </div>
        </div>
      </div>

      <div v-if="mode !== 'on'" class="stats-disabled">
        {{ $t('stats.disabledHint') }}
      </div>
    </div>
  </ArtistLayout>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useI18n } from 'vue-i18n'
import ArtistLayout from '../../components/ArtistLayout.vue'
import { artistApi } from '../../api/index.js'

const { t } = useI18n()
const data = ref(null)
const mode = ref('')
const byDay = ref([])
const byName = ref([])

const MAX_BAR = 80

function barHeight(count) {
  const max = Math.max(...byDay.value.map(d => d.count), 1)
  return Math.max(4, Math.round((count / max) * MAX_BAR)) + 'px'
}
function shortDay(day) {
  return String(day).slice(5) // 'YYYY-MM-DD' → 'MM-DD'
}

onMounted(async () => {
  try {
    const res = await artistApi.getTrackingSummary(14)
    mode.value = res.mode
    if (res.enabled) {
      data.value = { total: res.total }
      byDay.value = res.byDay || []
      byName.value = res.byName || []
    }
  } catch (e) {
    // 静默失败，展示 disabled 提示即可
  }
})
</script>

<style scoped>
.stats-page { padding: 24px; }
.stats-overview { display: flex; gap: 16px; margin: 16px 0; }
.stats-overview-item {
  background: var(--card, #fff); border: 1px solid var(--line, #e5e5e5);
  border-radius: 10px; padding: 20px 24px; min-width: 140px;
}
.stats-overview-num { font-size: 32px; font-weight: 700; color: var(--ink, #222); font-variant-numeric: tabular-nums; }
.stats-overview-label { font-size: 13px; color: var(--ink3, #888); margin-top: 4px; }
.stats-section { margin-top: 24px; }
.stats-bars { display: flex; align-items: flex-end; gap: 4px; height: 110px; padding: 8px 0; overflow-x: auto; }
.stats-bar-col { display: flex; flex-direction: column; align-items: center; justify-content: flex-end; height: 100%; flex: 1; min-width: 18px; }
.stats-bar { width: 12px; border-radius: 3px 3px 0 0; background: var(--hq, var(--el-color-primary)); min-height: 4px; }
.stats-bar-label { font-size: 10px; color: var(--ink4, #aaa); margin-top: 4px; transform: rotate(-30deg); white-space: nowrap; }
.stats-list { display: flex; flex-direction: column; gap: 6px; }
.stats-list-row { display: flex; justify-content: space-between; padding: 8px 12px; background: var(--card, #fff); border-radius: 6px; font-size: 13px; }
.stats-list-name { color: var(--ink, #222); }
.stats-list-count { color: var(--hq, var(--el-color-primary)); font-weight: 600; font-variant-numeric: tabular-nums; }
.stats-disabled { margin-top: 24px; padding: 16px; border: 1px dashed var(--line, #ccc); border-radius: 8px; color: var(--ink3, #888); text-align: center; }
</style>
```

> ⚠️ **先读 api/index.js** 确认 artistApi 现有函数结构（grep `tracking`），若 `getTrackingSummary` 不存在则加：
> ```js
> getTrackingSummary(days = 14) { return request({ url: '/api/artist/tracking/summary', params: { days } }) }
> ```
> 按项目实际 request 封装风格写。

### B. 埋点三态前端（用户拍板：关/不显/开，默认「不显」）

**TrackingAnalytics.vue**：开关控件（当前可能是 el-switch 或 radio）改三态 el-radio-group：

```html
<!-- before（grep 现有开关控件，可能是 <el-switch v-model="visible"> 之类） -->
<!-- after -->
<el-radio-group v-model="statsMode" @change="onModeChange">
  <el-radio value="off">{{ $t('tracking.modeOff') }}</el-radio>
  <el-radio value="hidden">{{ $t('tracking.modeHidden') }}</el-radio>
  <el-radio value="on">{{ $t('tracking.modeOn') }}</el-radio>
</el-radio-group>
```

```js
// script 内
const statsMode = ref('hidden') // 默认「不显」
onMounted(async () => {
  const cfg = await adminApi.getTrackingConfig() // grep 现有接口函数名
  statsMode.value = cfg.statsMode || 'hidden'
})
async function onModeChange(v) {
  await adminApi.updateTrackingConfig({ statsMode: v })
  ElMessage.success(t('tracking.saved'))
}
```
> ⚠️ 先读 TrackingAnalytics.vue 现状（grep `artistStatsVisible|tracking-config|el-switch`），按现有接口函数名对接（后端 GET/PUT `/api/admin/tracking-config` 已就绪，body `{statsMode: 'off'|'hidden'|'on'}`）。保留旧字段兼容。

**Dashboard.vue**：L23-33 的 tracking 区块（`v-if="trackingStats?.enabled"`）——**移除**（统计页独立后 Dashboard 还给清爽）。删除整个 `area-tracking` div + 相关 script 逻辑（grep `trackingStats` 清理干净）。

### C. i18n 新键（zh/en 同步）

```js
// zh-CN.js
menu: { ...stats: '数据统计' },  // 或放 menu 对象里
stats: {
  title: '数据统计',
  totalEvents: '总事件数',
  byDay: '按日趋势',
  byName: '事件明细',
  disabledHint: '统计未开启，请在管理后台开启'
},
tracking: {
  modeOff: '关闭',
  modeHidden: '不显示',
  modeOn: '开启',
  saved: '已保存'
}
// en.js 对应英文
```
> ⚠️ 新键命名按现有 i18n 结构组织（先读 zh-CN.js 看 menu/stats/tracking 是否已有对象，避免覆盖既有键）。

---

## 验证门禁（交付前必做）

1. `cd web && npm run test:web` → 215/215 全绿
2. `cd web && npm run lint` → oxlint 0 error
3. `cd web && npm run build` → 成功
4. `cd web && npm run check:i18n` → 无新增硬编码中文
5. **截图**：`workspace/temp/stats-page/`
   - 统计页三区块（总览/趋势柱状/明细）375px + 桌面
   - 三态开关（off/hidden/on 各一）
   - Dashboard 移除 tracking 后布局
6. 写交付报告 `docs/comms/02-to-01-画师统计页+埋点三态-交付-20260807.md`（commit 到分支）

## 交付形态

- commit 到自己分支 `beta/artist-stats-ui`，不推送不合并
- 交付报告抬头写「二号转交一号，文件：docs/comms/02-to-01-画师统计页+埋点三态-交付-20260807.md」
- 若工具调用接近上限：先 commit 已完成改动再报告
