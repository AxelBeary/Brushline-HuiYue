# 三号 → 一号：REQ-022 F2 外链重做 · 后端交付（第二轮）

> 日期：2026-08-05 · 分支：`f2-social-backend` · worktree：`artist-commission-f2`
> 第一轮锁定 `4f64d82`（纯函数+平台服务+迁移 v42+错误码），本轮续路由+主体改造+测试。
> **合并门控：本分支暂不合入 master**（等二号 F2 前端批，v0.38 后统一合）。

## 一、改动清单

### 主体改造
| 文件 | 变更 |
|---|---|
| `server/src/features/artist/artist.service.ts` | `updateArtist` custom_links 分支重写：新结构 `[{platformId, url}]`；归一化（normalizeLinkUrl）+ 长度（assertLinkLengthLimits）+ 条数 ≤8 硬校验；platformId 一律后端重推导（rederivePlatformId，忽略前端传值）。删 platform_urls 写入分支与 allowed 白名单项。getCustomLinks 删 weibo_url/bilibili_url 回退。删 getPlatformUrls |
| `server/src/features/artist/artist.routes.ts` | PUT profile schema customLinks 改新结构（items 仅 `{url}`，maxItems 8，无 pattern——裸链补全交给 service 层）；删 platformUrls schema 与 keyMap 项；GET /api/artists 与 GET /api/artists/:subdomain 响应删 weiboUrl/bilibiliUrl/platformUrls；新增 GET /api/platforms（公开，仅启用） |
| `server/src/features/admin/admin.routes.ts` | 新增平台 CRUD 段：GET/POST/PUT/DELETE /api/admin/platforms（requireAdmin，schema 全 additionalProperties:false，snake_case 与 admin 端一致）；POST 返回 201；DELETE 返回 `{success, reattributed}` |
| `server/src/utils/platform.ts` | **已删除**（旧正则识别 KNOWN_PLATFORMS/identifyPlatform/parsePlatformUrls，白名单外删除，一号已批准） |
| `server/tests/setup.js` | cleanDb 补 `DELETE FROM social_platforms`（防测试间种子污染） |

### 测试
- `server/tests/platform.test.js`：重写为防投毒纯函数向量（TC-FN-01~28）。
- `server/tests/platform-routes.test.js`：重写为 F2 路由测试（TC-PL2-01~28）+ 保留灵感标签用例（TC-IT-01~04）。
- `server/tests/artist.service.test.js`：改写 TC-R-06 系列（新结构/上限8/platformId忽略）+ TC-R-07 系列（不回退旧列）；新增 TC-R-06e（platform_urls 冻结）。
- `server/tests/routes.test.js`：改写 TC-RT-12 系列（新结构/service层拒绝LINK_URL_INVALID/不回退旧列/字段移除断言）。

## 二、防投毒向量测试结果（派工 F2-7 全过）

| 向量 | 期望 | 结果 |
|---|---|---|
| `weibo.com` | ✓ 命中 | ✅ TC-FN-16 |
| `m.weibo.com` | ✓ 命中 | ✅ TC-FN-17 |
| `weibo.com:8080` | ✓ 命中（hostname 天然去端口） | ✅ TC-FN-03/TC-PL2-11 |
| `weibo.com.evil.com` | ✗ 归其他 | ✅ TC-FN-18/TC-PL2-10 |
| `xweibo.com` | ✗ 归其他 | ✅ TC-FN-19/TC-PL2-10 |
| 大小写混写 | ✓ 命中 | ✅ TC-FN-20 |
| `javascript:` | ✗ 拒绝 | ✅ TC-FN-04/TC-PL2-05 |
| `ftp://` | ✗ 拒绝 | ✅ TC-FN-05/TC-PL2-06 |
| `data:` | ✗ 拒绝 | ✅ TC-FN-06 |
| 裸链补全 https | ✓ | ✅ TC-FN-01/TC-PL2-02 |
| 域名 254 字符 | ✗ 拒绝（253 通过） | ✅ TC-FN-10/11 |
| 路径+查询 1501 | ✗ 拒绝（1500 通过） | ✅ TC-FN-12/13 |
| 总长 1801 | ✗ 拒绝（1800 通过，hash 构造） | ✅ TC-FN-14/15/TC-PL2-07 |
| 9 条 | ✗ 拒绝（上限 8） | ✅ TC-PL2-04 |
| 未知域名 | ✓ 归其他（platformId=null） | ✅ TC-PL2-09 |
| DELETE 平台后链接归其他 | ✓（不级联删链接） | ✅ TC-PL2-23（reattributed=1） |
| 前端传 platformId 投毒 | ✗ 被忽略，按 URL 重推导 | ✅ TC-PL2-03 |

## 三、验证结果

- **vitest：877/877 全绿**（merge master 后重跑；master 基线 845 + 本批新增/改写 32）
- **tsc --noEmit：0 错误**
- **eslint server：0 错误 0 警告**
- merge master（至 `b73c6fe`）无冲突。

## 四、commits（3 批）

1. `8cff23d` refactor(backend): artist custom_links 保存/读取链路改新结构
2. `00a4954` feat(backend): 平台接口 + PUT schema + 响应新结构 + 删旧 platform.ts
3. `f1ebb7e` test(server): 防投毒向量测试 + 改写受影响既有测试

## 五、icon_key 清单（24 平台，供二号 F2-4 图标打包）

simple-icons slug（20 个）：
`sinaweibo` `bilibili` `xiaohongshu` `pixiv` `x` `kuaishou` `douban` `youtube` `instagram` `twitch` `artstation` `tiktok` `deviantart` `zcool` `afdian` `weasyl` `threads` `tumblr` `behance` `neteasecloudmusic`

fallback_char 单字兜底（4 个，simple-icons 无图标）：
LOFTER=`L` · 抖音=`抖` · QQ空间=`空` · 米画师=`米`

## 六、响应结构变化（供二号前端批对照）

**GET /api/artists/:subdomain / GET /api/artists**：
- 删：`weiboUrl`、`bilibiliUrl`、`platformUrls`
- 改：`customLinks: [{ platformId: number|null, url: string }]`（platformId=null 即「其他」，前端按 platformId 查 GET /api/platforms 拿 name/iconKey/fallbackChar 渲染）

**PUT /api/artist/profile**：
- 删：`platformUrls` 字段
- 改：`customLinks: [{ url }]`（maxItems 8；只传 url，platformId 后端推导）

**新增接口**：
- `GET /api/platforms`（公开）→ `[{ id, name, iconKey, fallbackChar, matchDomains, sortOrder, enabled }]`（仅启用项）
- `GET /api/admin/platforms`（含停用）/ `POST`（201）/ `PUT /:id` / `DELETE /:id`（返回 `{success, reattributed}`）
- 管理端 body 用 snake_case（name/icon_key/fallback_char/match_domains/sort_order/enabled）

## 七、备注

- 迁移 v42 与种子 24 平台在第一轮 `4f64d82` 已交付（幂等守卫：仅空表插入）。
- platform_urls / weibo_url / bilibili_url 三列保留在 DB（无 DROP，REQ 注明不写迁移），仅写入与读取路径全部移除。
- errors.ts 新增 5 个 PLATFORM_* 错误码在第一轮已批准；本轮无新增超白名单文件（删旧 utils/platform.ts 属一号已批准项）。

三号转交一号，文件：docs/comms/03-to-01-F2外链后端-交付-20260805.md
