# 五号交付：P 批收款三连（P1 负数卡死 + P2 多收 + P3 跟踪提示文案）

> 分支：v036-w2-bugfix @ `ee05b85`（已 merge master 至 58799f2，含 B1）
> 风险：中。铁律遵守：前端校验 = 后端 addPayment 规则的子集（0 拒/负数带 note/负数不超已收；正数无上限）。

## P1 负数超上限卡死

**复现记录**（容器 3000 = master 真实状态，订单 805 已收 ¥86）：
1. 打开收款弹窗，input-number 带 `:min="-86"` 硬钳制
2. 输入 -100（低于 min），blur → **EP 静默清空为空串，无任何提示，值丢失**，确认按钮状态与用户预期错位——即用户所说"卡死"
3. 期望的 payRefundExceed 提示根本没机会出现（值在提交前就被钳制吞掉）

**修复**：去掉 `:min/:max` 硬钳制，输入顺畅；提交时 submitPayment 校验（原有 payRefundExceed 路径接管），提示含上限金额。

**修复后实测**（dev 5173）：输入 -100 blur 后值保留 → 点确认 → 弹「退款金额不能超出已收金额 ¥86.00」→ 弹窗保留、值保留、可改正重提。✅

## P2 多收

- 删除 submitPayment 的 `cents > poolRemainingCents` 拦截 + 去掉 input-number `:max`（与 P1 同一处 :min/:max 一并移除）
- 收款区：paidTotal > finalPrice 时第三项显示「多收 ¥X」（橙色，新增 payOverpaid 键 + pool-overpaid 样式）替代「待收 ¥0」
- 新增测试 TC-INST-06（quota-pool.test.js）：累计多收 60000>50000 → 全节点 paid、paidCents 封顶无负数；退款 20000 后节点正确回退
- TrackOrder.vue：实测多收后显示「已付 ¥436 / 下期 ¥0 / 待付 ¥0 / 100%」无负数无异常——按"坏了才改"未动
- 全局搜索确认无"小费"字样存在

**实测**（dev 5173，订单 805）：录入 +¥350（超待收 ¥294）→ 提交成功 → 收款区「已收 ¥436.00 / 应收 ¥380.00 / 多收 ¥56.00」→ 三节点全 ✓ 已收无负数。✅
（实测产生的假流水+操作日志已从共享容器 DB 清理，订单 805 恢复原状）

## P3 TRACK_ALREADY_ON 文案

zh-CN：「请勿重复启用流程跟踪，请刷新网页」；en：「Please refresh the page — tracking is already enabled for this order」。

## 验证结果

| 项 | 结果 |
|----|------|
| server vitest | 713/713 全绿（基线 699 + B1 新增 13 + TC-INST-06） |
| web vitest | 144/144 全绿 |
| eslint | 0 错误 0 警告 |
| build | ✓ |
| P1 浏览器实测 | 负数全流程顺畅（复现+修复后双记录） |
| P2 浏览器实测 | 多收录入/显示/节点状态全通过 |

## 修改文件（4 个，全在授权列表内）

| 文件 | 改动 |
|------|------|
| web/src/views/artist/OrderDetail.vue | 弹窗去 :min/:max 钳制；删正数拦截；poolOverpaidCents + 多收显示 + 样式 |
| web/src/locales/zh-CN.js | payOverpaid 键 + TRACK_ALREADY_ON 文案 |
| web/src/locales/en.js | 同上英文 |
| server/tests/quota-pool.test.js | TC-INST-06 多收节点推算测试 |

## 备注

1. **派工授权写的 `server/src/features/order/__tests__/` 目录不存在**，测试实际落在 `server/tests/quota-pool.test.js`（与 TC-INST 系列同文件同主题），请一号知悉。
2. 中途 worktree 混入 B1 的残缺版 order.routes.ts（调用在、定义丢，致 32 测试 500），按一号指令还原该文件 + merge master 后基线恢复正常；已备份脏 diff 于 workspace/temp/wt05-orphan-order.routes.ts.patch。
3. useOrderPayments.js 授权内但无需改动（收款提交逻辑在 OrderDetail.vue）。
4. 回滚：`git revert ee05b85`。
