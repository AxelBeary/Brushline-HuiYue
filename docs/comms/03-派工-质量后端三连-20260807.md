# 03-派工-质量后端三连-20260807

> 派工：一号 → 三号（backend）
> worktree：`artist-commission-w3`（分支 `beta/quality-backend`，HEAD=159ee76）
> 开工第一步：**`git merge master` 再读本文件**。
> 三连任务：①源头防屎门禁 ②作品分页后端 ③埋点三态后端。按顺序做，每项独立 commit。

## 一、任务①：源头防屎门禁（用户红线，最高优先）

读 `docs/comms/03-派工-源头防屎门禁-20260807.md`（已有施工图），执行：
- **no-explicit-any: error**（server eslint）——any 已清零（266→0），现在开规则
- **no-console**（server error / web warn）
- **check-i18n 脚本**（`web/scripts/check-i18n.js`，扫 .vue/.js 用户可见中文，排除 locales/注释/$t 参数/白名单）
- **CI 追加**（ci.yml 加 check:i18n）
- 验证：故意塞违规 → 被拦 → 恢复全绿
- ⚠️ 存量硬编码中文**不修**（巡检修复批处理），脚本先列白名单/报告

## 二、任务②：作品分页后端

读 `docs/comms/03-派工-作品分页-20260807.md`（已有施工图），执行：
- `getArtworksPaged` + `getPublicArtworksPaged`（artist.service.ts，LIMIT/OFFSET 参数化）
- 画师端新接口 `GET /api/artist/artworks/paged?page&pageSize`（默认 20，clamp 1-50）
- 公开端 `GET /api/public/artworks/:artistId?page&pageSize`（默认 10，clamp 1-30，**限流 30/分**）
- 老接口保留；测试（分页/排序/边界/权限/限流）

## 三、任务③：埋点三态后端（用户拍板：关/不显/开，默认不显）

- `platform_config` key `stats_mode`（'off'/'hidden'/'on'），默认 'hidden'；兼容旧 `artist_stats_visible`（迁移或读取回退）
- `GET/PUT /api/admin/tracking-config` 改三态（body `{ statsMode: 'off'|'hidden'|'on' }`）
- `GET /api/artist/tracking/summary` 响应 `mode` 字段（替代 enabled，保留 enabled 兼容）
- **POST /api/events 在 off 时静默丢弃**（返回 ok 不落库）
- 测试：三态切换/off 不落库/默认 hidden
- ⚠️ 禁止 any（FastifyRequest + as 断言）；迁移如需要 v46 必须幂等

## 四、门禁
```bash
cd server && npm test && npm run typecheck && npm run lint   # 基线 953/953
cd web && npm run lint && npm run check:i18n                 # 门禁脚本
```
## 五、交付
- 报告 `docs/comms/03-to-01-质量后端三连-交付-20260807.md`（三项各自改动+测试+验证证据）
- 不推送、不合并、不改 STATUS；合入由一号执行
