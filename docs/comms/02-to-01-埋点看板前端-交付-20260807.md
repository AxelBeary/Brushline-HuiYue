# 02-to-01-埋点看板前端-交付-20260807

> 交付：二号 → 一号（client-frontend）
> 分支：`beta/tracking-frontend`（worktree w2）
> 对应派工：`docs/comms/02-派工-埋点看板前端-20260807.md`（REQ-033 收尾，前后端契约以派工文档为准）

## 一、改动清单

| 文件 | 改动 |
|------|------|
| `web/src/api/index.js` | artistApi 追加 `getMyTrackingSummary(days=14)`；adminApi 末尾追加 `getTrackingSummary(days=30)` / `getTrackingConfig()` / `setTrackingConfig(artistStatsVisible)`（均按派工契约） |
| `web/src/views/admin/TrackingAnalytics.vue` | **新建**管理员埋点看板页：顶部三卡（总事件数 / 画师门面可见开关 / 天数选择 7·14·30·90）、下单漏斗表（事件名/次数/占比）、事件分布表 + 按日趋势表（el-progress 比例条，不引新图表库）；onMounted 并行拉 summary+config，v-loading 加载态，失败 ElMessage.error；开关切换调后端 PUT，失败回滚 |
| `web/src/router/index.js` | admin 子路由追加 `analytics → AdminAnalytics`（meta.titleKey = admin.tracking.title, requiresAdmin） |
| `web/src/components/admin/AdminLayout.vue` | 顶部 Tab 菜单追加 `/admin/analytics` |
| `web/src/views/artist/Dashboard.vue` | 左栏 area-todo 之后新增 `.area-tracking` 区块（宽屏 grid 左栏 row 5），`v-if="trackingStats?.enabled"`（管理员关=完全不渲染）；onMounted 独立 try/catch 静默加载（失败不阻塞其他模块）；顶部大数字 + 按事件名 top6 小列表（未知事件名回退原文） |
| `web/src/locales/zh-CN.js` / `en.js` | 新增 `admin.tracking.*`（title/total/visibleLabel/daysLabel/funnelTitle/byNameTitle/byDayTitle/colName/colCount/colRatio/colDay/days7·14·30·90/visibleSaved/empty）与 `dashboard.tracking*`（trackingTitle/trackingTotal/trackingNames 11 个事件名映射）；跟随现有 admin/dashboard 键风格，只加不改 |

纪律核对：纯新增 + 少量追加，未动现有页面逻辑；画师区块 v-if 严格绑定 `trackingStats?.enabled`；未引新依赖（图表手写 el-table/el-progress）；全部文案走 `$t`，中英双语同步。

## 二、截图（4 张，已存 workspace/temp/shots/）

| 文件 | 内容 |
|------|------|
| `D:\Hermes Agent CN Desktop\workspace\temp\shots\01-admin-analytics-on.png` | 管理员埋点看板（开关=开，漏斗/事件分布/按日趋势数据渲染） |
| `D:\Hermes Agent CN Desktop\workspace\temp\shots\02-admin-analytics-off.png` | 管理员看板（开关=关，PUT 已生效） |
| `D:\Hermes Agent CN Desktop\workspace\temp\shots\03-artist-dashboard-off.png` | 画师工作台（门面统计隐藏态：enabled=false 区块完全不渲染） |
| `D:\Hermes Agent CN Desktop\workspace\temp\shots\04-artist-dashboard-on.png` | 画师工作台（门面统计开启态：累计事件 234 + top6 事件列表） |

> 截图来自 Playwright 真实登录（TOTP）+ route mock 契约接口（后端 summary/config 接口由三号并行实现，本分支尚未合入，故按派工契约 mock 验证前端渲染与开关联动；全链路联调等三号合入后由一号安排）。

## 三、测试结果

1. `cd web && npm run test:web`：**13 files / 215 tests 全过**
2. `npm run lint`：**0 errors**；4 个存量 warning（OrderDetail.vue ×3 / OrderForm.vue ×1，非本次改动引入，基线已有，未动这两个文件）
3. `npm run build`：**成功**（vite build 5.57s）

浏览器自测（临时脚本已删，环境已清理）：
- A1 管理员看板渲染：total=1234 ✓；开关初始=开（class=el-switch is-checked, input checked=true）✓
- A2 切换开关 → mock PUT 收到 `{artistStatsVisible:false}`，看板刷新为关 ✓
- A3 恢复开关 → PUT `{artistStatsVisible:true}` ✓（开关走后端接口，不本地存）
- B1 画师门面 enabled=false → `.area-tracking` 数量 0（v-if 不渲染）✓
- B2 画师门面 enabled=true → 区块渲染，total=234，列表 6 行（slice(0,6)）✓

## 四、自测步骤（可复现）

前置：后端 summary/config 接口已合入（或按契约 mock）。
1. 起隔离实例：`$env:PORT=3998; $env:DB_PATH=<测试库>; $env:WEB_DIST=web/dist; $env:ADMIN_QQ=10003; cd server; npm start`
2. 浏览器登录管理员（QQ 10003）→ 访问 `/admin/analytics`：验证三卡/漏斗/两表渲染、天数切换 7·14·30·90、开关切换后画师侧联动
3. 登录画师（QQ 10001）→ `/dashboard`：管理员开=显示「我的数据统计」区块；关=完全隐藏（刷新验证）
4. 门面事件名展示：已知 11 个事件名走翻译（dashboard.trackingNames.*），未知回退原文

## 五、待一号跟进

- 后端三号合入后安排全链路联调（真实接口替换 mock）
- lint 存量 4 warning 建议另派（非本次范围）
- 不推送、不合并、未改 STATUS，合入由一号执行