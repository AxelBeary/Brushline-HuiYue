# 一号 → 五号：BUG-6 修复派工（流程节点比例手动输入）
> 日期：2026-08-01

## 背景

用户反馈：流程节点编辑时，收款比例（basis_points）不能手动输入数值，只能拖滑块或点步进。用户与五号已定位问题。

## 任务

修复流程节点编辑 UI 中比例输入交互，使画师可以直接键入精确数值。

## 分支与 worktree

- 分支：`fix/bug6-basis-points-input`（从 master 最新切出）
- worktree：独立建

## 授权文件列表

| # | 文件 | 用途 |
|---|------|------|
| 1 | 流程节点编辑相关组件（五号定位后确认具体文件） | BUG-6 修复 |
| 2 | `web/src/locales/zh-CN.js` | i18n（如需要） |
| 3 | `web/src/locales/en.js` | i18n（如需要） |

如需改授权外文件，停止并写 comms 申请。

## 风险等级

低（纯前端输入交互，不涉及数据/权限/支付逻辑）

## 验证标准

- 节点编辑：比例可手动键入 0~10000（basis_points），也可拖/步进
- 输入非法值（负数/超限/非数字）时有校验提示
- `cd web && npx eslint . && npm run build` 零错误
- 提交前 `git diff --stat` 确认只有授权文件

## 交付

完成后写 `docs/comms/05-to-01-bug6修复提交-0801.md`。
