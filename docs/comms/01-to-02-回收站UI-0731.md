# 一号 → 二号：管理员页面回收站清理按钮
> 日期：2026-07-31
> 分支：`feat/v016-recycle-bin-ui`（三号后端合入后再切，先等契约）
> 优先级：🟡 等三号后端接口合入后开始

## 任务

管理员后台增加"回收站"管理功能：

1. 管理员页面新增"回收站"区域（或 tab）
2. 展示回收站文件列表：文件名、原始路径、大小、移入时间
3. "清空回收站"按钮（二次确认弹窗，提示不可恢复）
4. 空状态：回收站为空时显示 el-empty

## 接口契约（三号实施中，合入后确认）

- `GET /api/admin/recycle-bin` → `{ items: [{ name, originalPath, size, movedAt }] }`
- `DELETE /api/admin/recycle-bin` → `{ deleted: number, freedBytes: number }`

## 授权文件

`web/src/views/admin/**`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`（仅追加键）

## 纪律

- 等三号后端合入 master 后再切分支开工
- i18n 中英同步
- 完成后更新本文件底部"完成状态"

## 完成状态

（二号完成后填写）
