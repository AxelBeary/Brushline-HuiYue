# 派工：四号 — v0.28~v0.35 changelog 补全 + docs 归档盘点

> 来自：一号 | 2026-08-04
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w4`（分支 `docs/v035-changelog`，已建好）
> **授权说明**：changelog.md 与 docs 归档本不在你的默认文件权限内，此派工为一号特批授权，仅限本次两项任务。
> **开工第一步**：cd 进 worktree 先 `git merge master`（本派工在 master 上，你 worktree 建得早），再读本文件。

---

## T-1：changelog 补全 v0.28 → v0.35（八个版本）

现状：`docs/changelog.md` 停在 v0.27，master 已到 v0.35。缺八个版本。

素材：master 的 `git log --oneline`（可加 `--stat` 看文件范围），逐版本提炼。

要求：
1. 沿用现有格式：`## vX.XX — 日期` + `✨ 功能` / `🐛 修复` / `📊 测试` 小节，倒序排列（最新在最上，v0.35 在 v0.27 前面）
2. **用户视角描述**，面向非程序员（画师/客户），不堆 commit hash、不写内部实现细节。参考现有 v0.25~v0.27 条目的语气
3. 每版本 2~6 条要点，合并琐碎 commit（一次修 5 个 i18n 键 = 一条"补全界面文案"）
4. 版本主题参考（从 STATUS/待修复清单/git log 交叉核对）：v0.28 时间条拖拽+审计修复、v0.31 分期节点、v0.32 画风/增项模型、v0.33~v0.34 下单表单/草稿、v0.35 REQ-024 画风档位统一。**以 git log 实据为准，这只是线索**
5. 事故不隐瞒但写法克制：v0.38 迁移事故已修复且数据完好，如需提及写"修复"条目即可

## T-2：docs 归档盘点（只出清单，不动文件）

盘点 `docs/` 顶层所有文件，列出：
1. 已完成/过时的需求文档 → 建议移 `docs/archive/requirements/`
2. 已实施完的 spec/plan → 建议移 `docs/archive/specs-done/`
3. 仍在用的（CONTEXT.md/待修复问题清单/视觉提案等）→ 标注保留理由

**只写清单不移动**——归档动作等一号审核后由你执行或一号代劳。清单写进交付报告。

---

## 授权文件

`docs/changelog.md`（T-1 唯一改动文件）

## 验证

changelog 补完后自查：版本号连续无跳档、日期与 git log 对得上、语气与旧条目一致。

## 完工

commit（`docs: v028-v035变更日志补全` 单行）→ `docs/comms/04-to-01-changelog-交付-20260805.md` 写交付报告（含 T-2 归档清单）并 commit。不推送、不合并——合并权在一号。返回摘要：commit hash、报告路径、归档清单要点。
