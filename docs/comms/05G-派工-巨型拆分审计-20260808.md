# 05G-派工：巨型文件拆分审计 + 历史债务盘点（只读）

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：五号（bugfix）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w21`
> 分支：`beta/audit-large-files`
> 交付报告：`docs/comms/05G-to-01-巨型拆分审计-交付-20260808.md`（写完 commit 进分支）

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w21'
git merge master --no-edit   # 先合入最新 master 再读本文件
```

## ⚠️ 环境注意

主仓 server node_modules 缺 oxlint——`npm install --no-save oxlint@^1.77.0` 补装（不改 package.json/lock）。

## 背景（用户 2026-08-08：「token 管够，派分身去扫屎山 大文件 隐患 bug 和历史债务 历史遗留」）

已做：前后端深度审计（0🔴）+ 05D 后台巡检（32 条）+ 05E 修复。**本批补两专项：巨型文件拆分可行性 + 历史债务盘点**。**只读**：发现 → 报告，不改代码。

## 铁律

- 只读：改代码 = 打回
- 每条带 **文件:行号** 证据 + 现象 + 建议
- 分级：🔴 高优先级（性能/维护性严重）· 🟡 中等 · 🟢 轻微 · ✅ 已查无问题
- 对照已知正确模式别误报（拆分模式已验证 = OrderDetail 1523→1311 行，0% 像素差异）

## 任务 1：巨型文件拆分审计（web/src 全量）

**目标**：找 >300 行的前端组件，评估拆分可行性与收益，产出施工单。

1. **文件规模盘点**：扫描 `web/src/` 全部 .vue/.js，列出 >300 行文件（含行数）
2. **Top 候选逐文件分析**（重点）：
   - `QueueBoard.vue`（~1530 行）——已知待拆（05D 巡检也提到）；列拆分方案（组件边界：列表/月历/时间条/操作栏）
   - `ManualOrder.vue`（~1497 行）——已知待拆；列拆分方案
   - `ArtStyleManager.vue`（~956 行）——**注意：用户正跟四号详谈增项交互优化**，拆分方案需与增项改造协调（若增项先改，拆分后做）
   - `OrderDetail.vue` / `Settings.vue` / `AdminLayout.vue` / `AdminDashboard.vue` 等 >300 行候选
3. **每文件输出**：当前行数 → 建议拆分边界（哪些逻辑抽哪些子组件）→ 预估拆分后行数 → 依赖关系 → 风险（共享状态/事件流/样式）
4. **优先级排序**：按「维护价值 ÷ 拆分风险」排序（参考 OrderDetail 拆分先例：模板原样搬移 + 事件上行 + 样式 scoped + 视觉零变化）

## 任务 2：历史债务盘点（全库）

**目标**：扫屎山/死代码/遗留 TODO/过期注释/重复实现，产清单。

1. **死代码**：未使用导出/未使用 import/未使用组件/死分支（grep 交叉验证；eslint 已抓的不用重复）
2. **遗留 TODO/FIXME/HACK**：全库 grep `TODO|FIXME|HACK|XXX`，逐个评估（是活 TODO 还是已过时）
3. **过期注释**：注释引用的行号/行为/API 已变化（如「v0.25 起」但代码已大改）
4. **重复实现**：同逻辑多份（如金额格式化、日期处理、fetch 封装、空态组件）
5. **历史遗留**：STATUS 已知遗留里未清的（lint warnings 已清✅ / 死解构已清✅ / repowiki P2 / 性能池 / 第三方报告 F2 等）
6. **后端**：server/src 同样扫（死导出/遗留 TODO/重复 SQL）

## 输出格式（交付报告）

```
# 巨型拆分审计 + 历史债务盘点
## 一、巨型文件清单（>300 行 + 行数 + 拆分建议）
| 文件 | 行数 | 建议拆分 | 预估后 | 风险 | 优先级 |
## 二、Top 拆分施工单（每文件：组件边界/依赖/事件流/验证）
## 三、历史债务清单（死代码/TODO/过期注释/重复实现，文件:行号）
## 四、✅ 已查无问题分区
## 五、优先级建议（低成本高收益 Top 5）
```

## 交付要求

1. commit 描述中文（如 `docs(audit): 巨型拆分审计+历史债务盘点`）
2. 交付报告写 `docs/comms/05G-to-01-巨型拆分审计-交付-20260808.md`
3. 结束前：`git status --short` 确认干净（无代码改动）
4. **不推送、不合并、不改 STATUS**
