# 三号 → 一号：v0.18 第三批技术债提交（schema 补齐 + 工具函数补测试）
> 日期：2026-08-01

## 做了什么

技术债 B3 两项全部完成。

## 分支

`fix/tech-debt-b3`（worktree: `../artist-commission-td`）

## 改了哪些文件

| 文件 | 变更 |
|------|------|
| `server/src/db/init.js` | schema 字符串补齐 8 张缺失表 + artists 补 order_template_id 列 |
| `server/tests/date-utils.test.js`（新建） | 8 用例（TC-DU-01~08） |
| `server/tests/price-utils.test.js`（新建） | 6 用例（TC-PU-01~06） |

## A：schema 补齐详情

补齐的 8 张表（均为 `CREATE TABLE IF NOT EXISTS`，幂等无副作用）：

| 表 | 来源迁移 |
|----|----------|
| artist_workflow_stages | v5 + v20（含 speech_template） |
| default_workflow_template | v5 |
| order_payment_installments | v5 |
| greeting_templates | v6 |
| price_addons | v9 |
| addon_tiers | v9 |
| price_multipliers | v9 |
| order_price_breakdown | v9 |

另补：artists 表缺 `order_template_id TEXT DEFAULT 'default'`（v16 迁移添加的列）。

**零业务逻辑改动**——schema 字符串仅用于新库初始化的描述性 DDL，迁移逻辑未动。

## B：工具函数测试详情

| 文件 | 用例数 | 覆盖 |
|------|--------|------|
| date-utils.test.js | 8 | toSqliteDate 格式、无 T 分隔符、nowSqlite、localDayStart/End、localMonthStart、跨年边界、跨月边界 |
| price-utils.test.js | 6 | resolvePriceCents 三级回退、全 null、字段缺失、PRICE_FALLBACK_SQL 结构 |

## 验证结果

- 410/410 测试全绿（21 文件）
- ESLint 零错误（3 warning 是五号 admin 测试既有问题）
