# 二号 → 一号：v0.35 联调交付——删 mock 换真实 API（F3/F6）

> 分支：`feat/v035-client-gallery-link` · commit `32e8fdf`（10 文件 +136/-246，净删 110 行 mock）
> worktree：`artist-commission-wt-02`（按派工命令重建）· 日期：2026-08-04

## 做了什么（对照派工两处 mock 注入点）

1. **`applyV035MockFields()` 整函数删除**（useArtistData.js）+ ArtistHome.vue 的 mock import/调用块删除，`styles.value = res || []` 恢复直读
2. **F6 数据源切换**：新增 `getPublicGallery` API 方法；ArtistHome 拉 gallery 端点存入 `galleryData` 传给模板；**TplGallery 签名变更**：新增 `gallery` prop（`{ artworks, filterSizes }`），原 `styles` prop 移除——`artworks` prop 保留为兜底（端点失败时回退旧行为：无筛选行、纯作品流，向后兼容）

## 3 处契约差异的适配

- **差异 1**：`resolveSizeImagePath(size)` 简化为 `size.artwork_image_path || size.image || ''`（不再需要 artworks 参数查 id）→ **TplStyleGrid 的 artworks prop 已裁剪**（displayImageUrl 是唯一消费点，4 模板传参同步移除）
- **差异 2**：gallery 独立端点 → ArtistHome 单点拉取，TplGallery 吃 `gallery` prop
- **差异 3**：`buildGalleryFilters(filterSizes)` 直接吃后端 filterSizes（多画风判定改为 style_name 种类 > 1，前端拼「画风 · 尺寸」前缀）；`filterArtworksBySize` 按 `size_tags[].style_size_id` 匹配。删除档位的失效由后端 CASCADE + 前端 tagIndex 查不到双重兜底

## 改动文件

`api/index.js`（+getPublicGallery）· `composables/useArtistData.js`（纯函数重写）· `composables/__tests__/useArtistData.test.js`（按新契约重写 13 用例，mock 用例删除）· `views/client/ArtistHome.vue` · `components/templates/TplGallery.vue`、`TplStyleGrid.vue` · 4 模板（props 加 gallery、TplGallery/TplStyleGrid 传参调整）

## 验证结果

- `npx vitest run`：**144/144**（153 − 22 mock 用例 + 13 新契约用例）· `npx eslint .`：**0 错 0 警** · build ✓
- 浏览器实测（vite 5173 + Docker 后端，容器已含三号代码）：
  - **契约实测**：curl 过 `/api/public/gallery/alice` 与 `/api/public/styles/alice`，字段与派工契约一致（sizes 带 image/image_artwork_id/artwork_image_path/description/work_days；gallery 带 size_tags/filterSizes）
  - **真实数据 F3**：临时 SQL 种子模拟画师配置后实测——尺寸 1 引用作品图（artwork_image_path）✓、尺寸 2 独立上传图 ✓、尺寸 3 无图兜底封面 ✓、描述/天数联动 ✓、取消选中回封面 ✓（**种子已在联调后清理**，DB 恢复原状，tags 表 0 行）
  - **真实数据 F6**：筛选行单画风无前缀 ✓、各档位筛出正确 ✓、无标注作品只在「全部」✓、空档位空态提示 ✓、lightbox 双标签+自由描述 ✓、点标签跳下单带 styleId+sizeId 直落增项步+banner ✓、端点未标注时全部混编不报错 ✓
  - carol（F5 迁移后画师）：新模型展示柜 + 筛选行正常 ✓
- locales：本次零改动（上轮已建 gallery.* 命名空间，本轮无新文案）

## 遗留观察（非本任务范围，报告一号）

- **gallery 模板布局疑点**：ArtistHomeGallery.vue 挂 TplGallery 未传 `layout`，实际渲染 masonry（组件默认值）而非 editorial。查证 git 历史：波 2 前后均未传该 prop，属**历史遗留**非本次引入，未动。如需 editorial 请一号定夺派工归属。
- alice 尺寸/作品标注目前是空的（真实状态：画师还没配置）。用户体验 F3/F6 真实效果需先由画师在后台「画风与价格」+「作品管理」配置数据。
