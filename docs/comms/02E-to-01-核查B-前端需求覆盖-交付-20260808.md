# 02E-to-01：核查B·前端需求覆盖核查交付（只读，零代码改动）

> 交付人：二号（client-frontend）
> 日期：2026-08-09
> 派工：`docs/comms/02E-派工-核查B前端-20260808.md`
> 分支：`beta/audit-coverage-2`（base: master @ 371deff）
> 铁律遵守：只读核查，零代码改动，纯报告 commit；未推送、未合并、未改 STATUS

---

## 一、汇总

| REQ | 功能点 | 前端实现 | 状态 |
|-----|--------|---------|------|
| REQ-022 F1 | 作品绑定档位 + 完稿转作品链路 | OrderDetail 发布为作品 + ArtworkManage 档位标注 | ✅ 已实现 |
| REQ-022 F2 | 外链重做（统一输入区 + 平台可扩展 + 防投毒） | Settings 合一编辑器 + PlatformManage + 客户端页脚合一 | ✅ 已实现 |
| REQ-022 F3 | 约稿表单回显 | OrderForm 摘要卡 | ✅ 已实现 |
| REQ-022 F4 | 回收站分页 | ArtistManage 回收站 el-pagination（20/页） | ✅ 已实现 |
| REQ-022 F5 | 留言分组/筛选 | AdminDashboard 管理端留言三维筛选 | ✅ 已实现 |
| REQ-025 | 动态节点计价（前端展示/操作） | 待收横幅/负数退款已做；锁定/抵扣/额外应收展示与 done 入口收敛未做 | ⚠️ 部分实现 |
| REQ-026 | 画师后台视觉重设计 | 纸墨 token + 双主题 + 日期卡二合一 | ✅ 已实现 |
| REQ-029 | 手动录单接画风模式 | ManualOrder 画风三步走 + 自定义增项 + 图片开关 | ✅ 已实现 |
| REQ-033 | 业务埋点（前端上报/看板） | track.js + 三处埋点 + 6 类动作 + 看板 | ✅ 已实现（1 处口径差异） |
| REQ-034 | 快捷入口改造 | QuickActions 动作型 + 去镜像 + status 滑块 | ✅ 已实现（1 处交互差异） |
| REQ-035 | 工具类目（9 项工具 + 四件套） | 12 个 /tools/* 路由全部落位 | ✅ 已实现（文档状态严重过时） |
| REQ-031 | 候选项 A1/A2/B1/C4 四件套 | 导出/收据/分享/时区全落位 | ✅ 已实现 |

**结论**：施工图列出的 8 份 REQ 前端功能点，**无「需求有但代码无」的硬漏做项**；发现 1 处 ⚠️ 部分实现（REQ-025 前端展示五要素 + done 入口收敛，属二阶段剩余）、2 处文档/实现口径差异（REQ-033 页面浏览事件名、REQ-034 状态切换交互形态）、1 处文档严重过时（REQ-035 状态仍写"未做"，实际批A/B/C/D/E + 后置 4 项全部已合入）。

---

## 二、✅ 已实现（带证据）

### REQ-022 F1 作品绑定档位 + 完稿转作品
- `web/src/views/artist/OrderDetail.vue` L395-424：发布为作品入口（仅 delivered 显示）+ 弹窗（勾选图片默认全选，非图片置灰）
- OrderDetail.vue L997 / L1120：publishArtwork API 调用（一图一作品，发布不锁订单可重复）；L1387 弹窗样式
- `web/src/views/artist/ArtworkManage.vue` L77/107/185：作品编辑入口；L454-513 档位标注多选（启用画风×尺寸展平，多画风「画风 · 尺寸」防歧义）+ 自由描述，保存即时 PUT
- 客户端档位展示：ArtistHome.vue L138（tiers 加载）+ 4 模板 TplTierGrid（有画风 → TplStyleGrid，无画风 → 档位兜底）

### REQ-022 F2 外链重做
- `web/src/views/artist/Settings.vue` L77-103：链接编辑器（外链/平台链接合一，一行 = 平台下拉+URL+↑↓✕，上限 8 条，空态"还没有添加链接"）；L352 detectLinkPlatform（粘贴/输入即时识别）
- `web/src/utils/linkValidation.js`（防投毒全实现）：
  - normalizeLinkUrl：裸链自动补 https://；`javascript:`/`ftp:`/`data:` 等危险协议拒绝（L21-50）
  - checkLinkLength：域名 ≤253 / 路径+查询 ≤1500 / 总长 ≤1800，超限 tooLong（L56-74）
  - matchDomain：hostname === 域名 或以 `.域名` 结尾才算命中，`weibo.com.evil.com`/`xweibo.com` 一律不认（L81-96）
  - validateLink：组合校验 + 平台识别，识别不出 → null（其他）（L105-127）
- `web/src/views/admin/PlatformManage.vue`：平台管理后台（名称/匹配域名列表/图标白名单 iconKey+fallbackChar/排序/启用开关/增删改，L13-58）
- 客户端页脚合一：4 模板全部 `footerLinks` 单排渲染（ArtistHomeAtelier L62-76 / Classic L18-21 / Folio L106-109 / Gallery L72-86）

### REQ-022 F3 约稿表单回显
- `web/src/views/client/OrderForm.vue` L439-451：粘性摘要卡（昵称/描述回显，空值隐藏，描述 3 行截断）；L455-457 画风模式摘要（档位/增项/总价）
- 测试：`web/src/views/client/__tests__/OrderForm.summary.test.js`

### REQ-022 F4 回收站分页
- `web/src/views/admin/ArtistManage.vue` L202-220：回收站弹窗（REQ-022 F4 分页）；L217-220 el-pagination（current-page/page-size）；L363-376 分页逻辑（recyclePageSize = 20，翻页重新请求 adminApi.getRecycleBin({ page, pageSize })）

### REQ-022 F5 留言分组/筛选
- `web/src/views/admin/AdminDashboard.vue` L83-125：管理端留言管理（跨画师，强制删除）；L90-100 三维筛选（画师 filterArtistId / 审核状态 pending-approved-rejected / 是否已回复 filterReplied）；L144 注释明确 REQ-022 F5

### REQ-026 画师后台视觉重设计
- `web/src/styles/artist-tokens.css`（13.8KB）：纸墨 7 色 token（花青 #33526E/朱砂 #BC3A2B/石绿 #2F7D54/藤黄 #966C0A/赭石 #8A5A33）+ 双主题（paper/ink 全套变量）+ 圆角 4/7/11px + 阴影三级 + 字体（霞鹜文楷 LXGW WenKai / 思源黑体 Noto Sans SC）+ 旧变量兼容映射；全部挂在 `html[data-artist-theme]` 条件属性（客户端零影响）
- `web/src/stores/theme.js`：artistTheme 宣纸/墨黑（localStorage `huiyue-artist-theme` 持久化，默认宣纸）+ toggleArtistTheme + enter/leaveArtistScope（路由守卫统一管理）
- `web/src/components/ThemeToggle.vue`：右上角主题切换按钮（artist-theme-btn--ink 形态 + toast）
- OrderDetail.vue L43-77：日期卡二合一（开工日+截稿日一卡，卡头剩余天数 chip；L1248-1252 样式）；L44 注释明确 REQ-026 §四
- AdminDashboard.vue L199：管理后台重设计（纸墨 token + 布局分层）

### REQ-029 手动录单接画风模式
- `web/src/views/artist/ManualOrder.vue` L186-187：isStyleMode（有画风即启用）/ isMultiStyle（多画风才选画风步）
- `web/src/components/artist/order/ManualOrderRight.vue`：
  - L37-61 画风三步走（画风卡片→尺寸→增项）；L58 R2 自定义单提示（"都可以不选，直接手动填价格录入自定义单"）
  - L465 单画风自动选中退化；L536-538 切换画风清空 + B2 点已选卡片取消选中
  - L616 实时算价（300ms 防抖，calculate-style-price）
  - L480-518 R5 自定义增项（允许负数/0，上限 20，可删除，两条路径通用）
  - L524-527 R6 显示图片开关（localStorage `manualOrder_showImages` 记忆，画风+档位一起藏）
- 测试：`web/src/views/artist/__tests__/ManualOrder.stylemode.test.js`

### REQ-033 业务埋点
- `web/src/utils/track.js`：完整骨架（匿名凭证 POST /api/anon-token + localStorage；攒批 5s/10 条；pagehide sendBeacon 带走剩余；网络错误回队重试；400 INVALID_ANON_TOKEN 静默重取重试；其余 4xx 丢弃——埋点永不打断业务）
- ThemePicker.vue L32：`theme_accent_change`（换色率）
- OrderForm.vue L744-746：`order_form_step_view` / `order_form_step_back`；L771 `order_submit_success`；L775 `order_form_abandon`（漏斗四事件齐全）
- ArtistLayout.vue L252-260：trackPageView 页面浏览（路由变化统一收口，一次进入一条）
- 6 类关键操作 artist_action：ManualOrder L246 `order_create`；useOrderWorkflow L60/84 + OrderDetail L746 `order_status_change`；ArtworkManage L385/390 `artwork_set_cover`、L347/508 `artwork_publish`；Settings L336/551 `settings_save`；GuestbookManage L216 `guestbook_reply`
- 看板：`web/src/views/admin/TrackingAnalytics.vue` + 路由 `/admin/analytics`（router 已注册）

### REQ-034 快捷入口改造
- `web/src/components/artist/dashboard/QuickActions.vue`：
  - L10-26 QUICK_ACTION_POOL：type=route（跳转）/ action（执行动作）/ link（新窗口）三类
  - L30 QUICK_ACTIONS_DEFAULT：`['manual', 'preview', 'rules', 'share', 'quickconfig', 'status']`——queue/orders/artworks/tiers/settings 6 个导航镜像全部移出默认（验收 1）
  - L68-75 status 动作：三态滑块（open/full/break）；L118-128 onStatusChange → PUT /artist/profile → store 同步 → 成功提示；L119 statusBusy 防连点
  - L134-135 preview 无 subdomain 兜底：ElMessage.warning「未设置主页域名」+ 引导（验收 4）

### REQ-035 工具类目（9 项 + 四件套全部已合入）
路由已注册 12 个 `/tools/*` 页面（router/index.js）：
| 工具 | 视图 | 合入证据 |
|------|------|---------|
| 收入导出 CSV（A1） | ToolsExport.vue（9.1KB，含收入概览区） | 709e3d1（基础四件套） |
| 订单收据（A2） | locales receipt* 键 + 收据入口 | 709e3d1 |
| 完稿分享（B1） | OrderDetail.vue L403-472 分享按钮+弹窗（F2 域名校验复用） | 709e3d1 |
| 时区显示（C4） | TrackOrder.vue L275-276 + utils/datetime.js | 709e3d1 |
| 今天吃什么 | FoodMenu.vue（菜谱 495 条） | afa0a7e + 3a00bf0 |
| 散单记账（批C） | StandaloneIncome.vue（表单/列表/删除） | 9cd7428 |
| 图片水印（批D） | Watermark.vue（canvas 文字/图片水印/透明度/位置/导出） | d70d567 |
| 进度拼图（批E） | PuzzlePage.vue（勾选 2~6 图 + canvas 拼图） | 91a3b0d |
| 排期公示（批E） | ScheduleSharePage.vue | 91a3b0d |
| 客户标记（批A） | ClientsPage.vue | eaf4bba |
| 老客召回（批A） | ReturningClients.vue | eaf4bba |
| 跨平台解析（批B） | utils/message-parser.js + 测试 + ManualOrder 粘贴解析按钮 | c9aeb77 |
| 稿价计算器（后置） | PriceCalculator.vue（14.5KB） | 3769175 |
| 社恐回复（后置） | SocialReply.vue | 3769175 |
| 速记剪切板（后置） | QuickNote.vue | 3769175 |
| 截稿日建议（后置） | DeadlineAdvice.vue | 3769175 |

### REQ-031 候选项四件套
- A1 收入导出：`/tools/export`（ToolsExport.vue）
- A2 订单收据：locales receipt*（zh-CN.js L693+）+ 收据入口
- B1 完稿分享：OrderDetail.vue L403/407/409/472（delivered 显示，弹窗含平台+文案模板，占位符 {orderNo}/{homepage}）
- C4 时区显示：TrackOrder.vue L275-276（Intl.DateTimeFormat().resolvedOptions() 取客户时区；验收 2 同时区只显示一行由代码分支处理）

---

## 三、❌ 漏做（需求有但代码无）

**核查结论：无硬漏做项。**

8 份 REQ 的全部前端功能点均有代码实现或处于明确排期/重派中：
- REQ-035 唯一未完成项是「时间条拖拽改期/左右滑动」——文档明确标注已重派五号 `beta/timeline-redo` 进行中（非漏做，是进行中）
- REQ-025 二阶段展示类缺口见下节（部分实现，非零实现）

---

## 四、⚠️ 部分实现（缺口说明）

### REQ-025 动态计价（前端展示/操作）—— 6 项缺口

**已做**：
1. 订单级共待收总横幅：OrderDetail.vue L126-133（`remainingCents` 订单级总待收 + `nextDueInstallment` 当前节点副信息，点击跳转收款区）——5b2388b
2. 负数退款 label 切换：OrderDetail.vue L518（金额 <0 时 label 变「退款原因（必填）」）——5b2388b
3. 负数收款支持：composables/useOrderPaymentPanel.js submitPayment（负数校验：原因必填 + 不超已收金额）
4. PaymentPanel 多收展示（P2 早于 REQ-025，非抵扣标签语义）

**缺口（REQ-025 §8.3 前端交互稿 / 验收 11/13，二阶段剩余）**：
| # | 缺口 | 证据 |
|---|------|------|
| 1 | 已锁节点「锁定价 + 锁定标记」视觉：收款区无任何 locked 展示（迁移 v40 已有 locked 列，98db120，但前端未渲染） | PaymentPanel.vue 仅节点状态 paid/partial/remaining 展示，全仓搜 `locked/锁定价` 无收款区命中 |
| 2 | 超付抵扣标签「已抵扣 ¥X」：无 | 全仓搜 `已抵扣/抵扣` 无命中；PaymentPanel 的 poolOverpaidCents 是「多收」语义（P2），非 R7 超付抵扣标签 |
| 3 | 额外应收/应退独立展示（「额外待收 ¥X」/「应退 ¥X」）：无 | 全仓搜 `extraCharge/extraRefund/额外应收` 无命中 |
| 4 | done 状态无痕改价按钮未隐藏：OrderDetail.vue L344 `v-if="!isTerminal"`，isTerminal = [delivered, cancelled]（useOrderWorkflow.js L19），done 时改价按钮仍显示（后端已关闭该入口，6bb200c，前端未收敛） | L340-344 |
| 5 | done 状态工作流回退按钮未隐藏：canBackStage（useOrderWorkflow.js L48）只拦 delivered/cancelled，done 时「打回」仍可点（后端 rollbackStage done 守卫是否已补需三号侧确认） | L166 |
| 6 | 负增项入口前端仍被 min=0 拦死：附加项弹窗 `extraForm.priceYuan :min="0"`，后端 schema 已放开负数（6bb200c 负增项写 refund_item 条目）——前端收紧后端规则，R13「减价/退款条目」入口未放开 | OrderDetail.vue L531-538 |

> 备注：REQ-025 验收 11 的「当前节点待收高亮」已有基础（nextDueInstallment 横幅 + PaymentPanel 待收），「未来节点预估价」与缺口 1 绑定未做。缺口 4/5/6 均属「done 半终态操作入口」收敛，建议与三号后端守卫批次协同收口。

---

## 五、已做未记录（代码有但文档/STATUS 没标）

### 1. REQ-035 状态严重过时（最重要）
REQ-035 文档头部状态仍写「未做：批A 客户标记+老客召回 / 批B 跨平台解析 / 批C 散单记账 / 批E 拼图+排期公示 / 水印全面版 / 后置 4 项」，**实际已全部合入 master**：
- 批A：eaf4bba（+ merge 1152980）
- 批B：c9aeb77（+ merge 743a532）
- 批C：9cd7428（+ 文档 06f68f8）
- 批D：d70d567（+ merge e8bd0e4）
- 批E：91a3b0d（+ merge 84fa0c4）
- 后置 4 项：3769175（+ merge 124a8c8）
- 今天吃什么：afa0a7e + 菜谱扩张 3a00bf0（124→495 条）
- 基础四件套（A1/A2/B1/C4）：709e3d1

### 2. REQ-033 页面浏览事件名口径差异
REQ-033 §4.2 定义 `artist_page_enter`；实际实现（ArtistLayout.vue L242-250 PAGE_VIEW_EVENT_MAP）用 9 个 `*_view` 事件（dashboard_view/queue_view/orders_view/manual_view/tiers_view/artworks_view/guestbook_view/settings_view/preferences_view）。后端白名单（tracking.service.ts EVENT_WHITELIST）同时含 `*_view` 系列与 `artist_page_enter`，前后端一致、可跑通——属施工图对 REQ 的细化（更细粒度页面统计），但 REQ-033 文档未更新。另：/slots、/admin 无白名单事件名，前端注释明确不埋。

### 3. REQ-034 状态切换交互形态差异
REQ-034 §五.2 要求「状态切换二次确认（el-popconfirm）」；实际实现为 QuickActions.vue L68-75 三态滑块显式选择（选即改，statusBusy 防连点，无 popconfirm）。交互形态不同但「显式选择」本身即防误触，目标一致；REQ-034 文档未标注此差异。

---

## 六、风险与建议（给一号）

1. **REQ-025 缺口 6 值得立即排**：前端 `:min="0"` 收紧后端已放开的负数条目，是「前端校验必须是后端子集」的违规实例（v0.36 负数退款教训同型），建议批内修复，与后端负条目机制对齐。
2. **REQ-025 缺口 1/2/3 建议等三号引擎端到端联调后再补**：锁定/抵扣/额外应收展示依赖后端 enrich 字段返回，前端先做展示骨架成本低但验收依赖后端字段，排期可随 REQ-025 收尾批次。
3. **REQ-035 文档过时建议四号更新**：STATUS/REQ-035 状态与 master 实际差异大，后续排期会被误导。
4. **本报告为纯只读核查**：除 `docs/comms/02E-to-01-...` 报告文件外零改动，未推送未合并。

---

## 附：核查方法
- 分支：beta/audit-coverage-2（master @ 371deff 已 merge）
- 核查对象：docs/requirements/ 8 份 REQ（022/025/026/029/031/033/034/035）前端功能点 ↔ web/src 实际代码
- 证据形式：文件:行号 + 合入 commit；每条均已 grep/Select-String 实锤
- 拿不准项：REQ-025 后端 rollbackStage done 守卫归属三号侧，标注「需三号确认」，未硬判