# 第三方报告核实记录（2026-08-06）

> 报告来源：用户桌面「新建文件夹」7 文件（00 总纲 / 01-PM / 02-架构 / 03-设计 / design-tokens-spec / design-tokens.json / OPEN-DECISIONS），已归档 `workspace/temp/third-party-report-20260806/`（不入库，gitignore 区）。
> 核实人：一号 · 核实方法：报告主张逐条对照当前 master 代码（`df26d7d`）。
> 结论先行：**报告质量高、多数主张与内部研报/体检交叉验证一致；但基于 2026-08-06 8:12 前的分支，部分项已过时**（吸底 CTA 恰在 8:12 由五号 `496e082` 修复，报告仍列为待做）。

---

## 一、与在途批重叠（已派，勿重复）

| 报告项 | 报告主张 | 当前状态 | 结论 |
|--------|----------|----------|------|
| A7 吸底 CTA | 「三模板吸底 CTA 从上线起就没触发过」 | **已修复**（`496e082` 8:12 合入，4 模板 Playwright 实测 PASS） | **报告过时**，不派 |
| C2 封面上限 6 张 | 前端后端双双未实施 | 属实——已派三号批（`01-to-03`） | 重叠，已派 |
| A3 OrderForm 6 处硬编码中文 | 属实 | 已派二号批 F1（键已存在） | 重叠，已派 |
| A8 Folio 导航死链 | 属实 | 已派五号批 T2（导航动态化） | 重叠，已派 |
| F5/A6 查无订单 3 秒锁 | 属实 | 已派二号批 F5（3→2 秒） | 重叠，已派 |

## 二、属实且有新增价值（入待办池）

| 报告项 | 主张 | 代码实证 | 建议 |
|--------|------|----------|------|
| **A1 订单号找回** | `/orders/my` 后端已存在、前端 API 已封装（`web/src/api/index.js:236` myOrders），但全 UI 零调用——查单页强制要求订单号 | 后端 `order.routes.ts:254` ✓；前端封装 ✓；TrackOrder.vue 无调用 ✓ | **高价值**：查单页补「只填 QQ 列出我的订单」。A 测前批次 |
| **A10 EP 蓝残留** | `TrackOrder.vue:113` 走 JS 内联 `#409eff`，绕过 CSS 变量覆写 | 实测 L113 `:color="... ? '#67c23a' : '#409eff'"` ✓ | 顺手项：改走 token（进度条颜色）。随视觉批 |
| D1 五色对比度 | accent 1/2/3 号白字对比度 1.73/2.12/3.15:1 不达 AA；4/5 号 4.77/6.95 已达 | theme.css:79-83 五色确为同一条青→蓝色相带 ✓ | **需用户拍板**（OD-01 换色：改动已上线可选色） |
| D2 藤黄双语义 | `--th` 同时承担「待确认状态」+「封面/主图标识」，暗色白字 1.98:1 | artist-tokens.css:41 + ArtworkManage.vue:541/505/546 ✓ | 语义拆分，A 测前可选 |
| B2 订单筛选缺 revision | 筛选无返修档 | QueueBoard 有 revision 处理（L98/144）但筛选枚举待核 | 待核，低优 |
| U1 批量上传体验 | N 次弹窗 + N 次全量重拉，无进度重试 | ArtworkManage.vue 上传逻辑待核 | 结构性，A 测后排 |
| A2 步进指示器浮动 | step=2 双语义碰撞 | 待核 | 与二号批 U4 邻近，可并入 |

## 三、属实的工程事实（报告自证，已接受）

- ElMessage 325 处 / ElMessageBox 43 / el-skeleton 0（与报告校正一致）
- 图标四套并行 + 64 处 Unicode 字符当图标（`★☆✓` 非 emoji 码点——报告已自行校正，不按 P0）
- faux bold：Noto Sans SC 无 700 字面，浏览器合成粗体发糊
- OrderDetail 1731（报告 1898 为旧统计）/ QueueBoard 1530 / ManualOrder 1302 巨型组件
- 「测试只测功能、审核只看 diff 不看截图」验收盲区——与 STATUS 视觉熵增教训同源

## 四、需用户拍板（OD-01/OD-05，报告已列）

1. **OD-01 五色主色换色**：是否接受改变已上线的可选主色（低饱和自然色替代霓虹青蓝，验收铁律「新值不得低于旧值」）。⚠️ 涉及 ThemePicker 用户可见功能。
2. **OD-05 功能色边界**：STATUS 规则 11 已拍板「功能色不收 token」，但后台纸墨盘已用国画色重定义 success/warning/danger——需澄清边界是「客户端不收」还是「全站不收」。

## 五、报告提出的工程改进（框架级，暂不派）

- 三层 token（primitive/structure/semantic）+ 双轨对齐 + client-flow-skin.css —— 与本批「流程页 palette 挂接」方向一致但工程量大，A 测后评估
- 8 道门禁（图标扫描/视觉回归/行数棘轮/对比度校验…）—— 价值高，但需先有视觉基线；A 测后
- 巨型组件拆分蓝图（OrderDetail≤250 行等）—— 已列入 STATUS 前端性能池，A 测后

## 六、处置建议

1. **本批不追加派工**——报告价值项已被内部研报覆盖大半，剩余（A1/A10/D2）随 Beta 冲刺第二批消化。
2. **A1 订单号找回优先入第二批**（高价值、后端能力已就绪、纯前端接线）。
3. OD-01/OD-05 两拍板项汇报用户。
4. 报告文件不入库（保持 git 干净），工作副本在 workspace/temp。
