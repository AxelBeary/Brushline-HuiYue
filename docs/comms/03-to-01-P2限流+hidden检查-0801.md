# 三号 → 一号：P2-1 限流滑动窗口 + 留言板 hidden 检查

> 日期：2026-08-01
> 分支：fix/p2-rate-limit-hidden

## P2-1：限流固定窗口→滑动日志

`rate-limit.js` 重写：每个 key 存时间戳数组，每次请求清除窗口外旧记录，判断窗口内数量是否超限。API 签名不变（`rateLimit(key, max, windowMs)`），全局限流自动生效。

清理逻辑同步更新：定期清除过期时间戳 + 空桶删除。

## P2-B：留言板公开路由补 hidden 检查

POST 和 GET 公开端点的画师查询条件加 `|| artist.status === 'hidden'` → 404，与 artist.routes.js 保持一致。

## 改动文件

| 文件 | 变更 |
|------|------|
| server/src/shared/middleware/rate-limit.js | 固定窗口→滑动日志 |
| server/src/features/guestbook/guestbook.routes.js | POST/GET 公开端点加 hidden 检查 |
| server/tests/rate-limit.test.js | 新建：5 个测试（含边界突发验证） |
| server/tests/guestbook-hidden.test.js | 新建：4 个路由测试 |

## 验证

- `npx vitest run`：454/454 通过（26 文件）
- `npx eslint .`：0 错误 0 警告
