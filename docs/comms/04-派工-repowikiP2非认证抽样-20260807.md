# 04-派工-repowikiP2非认证抽样-20260807.md

> 派工：一号 → 四号（requirements）
> worktree：`../artist-commission-w4`（分支 `beta/wiki-p2`）
> 蓝本：`docs/comms/04-to-01-repowiki-P2-交付-20260807.md` 第五节建议（已核实：认证系 14 篇全部新鲜，过时风险集中在非认证主题）
> 基线：master `3afe5c7`。**开工第一步 `git merge master` 再干活**。
> 门禁：无代码测试；文档自检 = 每处引用文件 Test-Path + 关键符号 Select-String

## 授权文件（只许动这些）

- `docs/external-wiki/` 下新建的修订版（命名 `wiki-<主题>.md`，与既有 P0/13 处同模式）
- `docs/external-wiki/README.md`（若追加条目）
- 交付报告 `docs/comms/04-to-01-repowiki-P2非认证抽样-交付-20260807.md`

## 红线

- **不碰** `C:\Users\qly19\Desktop\repowiki\`（外部原文一字不动）
- 不碰既有修订版（P0 三篇 + 13 处修补批）
- 修订内容逐条以 master 代码为准；禁止虚构变量/端点

---

## 任务：非认证主题抽样核对（P2 收尾）

**背景**：P2 原假设「认证系文档过时」被否定（14 篇抽样全部 TOTP 时代）。过时风险集中在**非认证主题**——部署/CSP/Sentry/前端与近期改动（`SENTRY_DSN_BACKEND` 环境变量陷阱、CSP connect-src、Caddyfile 压缩缓存、TS 迁移 .js→.ts）高度相关。

**动作**：从剩余 112 篇中抽**非认证主题 10-12 篇**核对，重点：

1. **部署主题**（高价值）：
   - `部署运维\*` 目录下文档（Docker 部署/compose/Caddyfile 相关）
   - 核对点：entrypoint.sh 指向 `src/index.ts`（非 index.js）、compose 3000 端口策略、Caddyfile `encode zstd gzip` 压缩
2. **监控/Sentry 主题**：
   - `故障排除\日志分析.md` 相关（已修）+ 其他 Sentry/监控文档
   - 核对点：`SENTRY_DSN_BACKEND` 环境变量（非裸 `SENTRY_DSN`）、CSP connect-src 含 ingest 域名
3. **前端主题**：
   - 前端路由/组件/构建相关文档
   - 核对点：`main.js`/`i18n` 结构、vite 构建、客户端 4 模板

**每篇核对方法**（同 P0 批模式）：
1. 读外部原文 → 列出「过时段落/虚构变量/错误文件名」
2. 对照 master 代码逐条核实（引用文件 Test-Path + 关键符号 Select-String）
3. 分类：🔴 严重过时（需修订）/ 🟡 轻微过时（可修订）/ 🟢 新鲜（无需动）
4. 修订版落盘 `docs/external-wiki/wiki-<主题>.md`（🟡 以上才写修订版；🟢 只记结论不产修订）

**交付报告**：`docs/comms/04-to-01-repowiki-P2非认证抽样-交付-20260807.md`——抽样清单表（文档/结论/引用证据）+ 修订版列表 + 存疑标注（哪些只做了关键行抽样未逐句比对）。

## 验证门禁

1. 修订版引用文件 Test-Path 确认存在
2. 关键符号 Select-String 抽查（`SENTRY_DSN_BACKEND`、`entrypoint.sh`、`index.ts`、`encode zstd gzip` 等）
3. 修订版在 `docs/external-wiki/` 真实落盘（写后 Test-Path，防幻觉完成）
4. 外部原文一字未动（不改 Desktop\repowiki）

## 交付形态

- commit 到分支 `beta/wiki-p2`，不推送不合并
- 交付报告抬头写「四号转交一号，文件：docs/comms/04-to-01-repowiki-P2非认证抽样-交付-20260807.md」
- 若工具调用接近上限：先 commit 已完成部分再报告
