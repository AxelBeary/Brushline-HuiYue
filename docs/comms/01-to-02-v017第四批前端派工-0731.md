# 一号 → 二号：v0.17 第四批前端派工（SPEC-003 前端 + UX-1 修复）
> 日期：2026-07-31

## 背景

SPEC-003 后端已合入（迁移 v18 + CRUD API + 尾款重算）。现在做前端适配。

## 任务 A：画师端 OrderDetail——附加工作项卡片

按 SPEC-003 §6：
1. 备注区下方新增"附加工作项"卡片
2. 每条显示：名称 + 金额 + 删除按钮（悬停显示）
3. 底部"添加附加项"按钮 → 弹窗（名称 + 说明 + 金额）
4. 添加/删除后刷新订单数据（final_price_cents 已重算）
5. 有附加项且画师手动改过价时，提示"最终价格由附加项自动计算"

API：
- POST /api/artist/orders/:id/extra-items（body: {name, description?, priceCents?}）
- DELETE /api/artist/orders/:id/extra-items/:itemId
- getOrder 已返回 extraItems 数组

## 任务 B：客户进度页——附加项展示

按 SPEC-003 §5.5（硬约束）：
1. 价格区域显示附加项明细（名称 + 金额）
2. 尾款金额使用 finalPriceCents
3. 付款节点展示（installments 数组：name + amountCents + paid）
4. **不显示** description、id、created_at

track API 已返回：extraItems [{name, priceCents}] + finalPriceCents + installments [{name, amountCents, paid}]

## 任务 C：UX-1 图片预览修复（五号审计，4 文件各加一行）

以下 4 个组件有 `:preview-src-list` 但缺 `preview-teleported`，图片预览可能被 transform 截断：
1. TplGallery（客户主页图库）
2. TplTierGrid（档位网格）
3. ArtworkManage（画师作品管理）
4. ArtistDetailDrawer（画师详情抽屉）

修复：每个组件的 el-image 加 `preview-teleported` 属性（一行 × 4 文件）。

## 分支与 worktree

- 分支：`feat/client-frontend-v017-b3`（从 master 最新切出）
- worktree：独立建

## 授权文件列表

| # | 文件 | 用途 |
|---|------|------|
| 1 | `web/src/views/artist/OrderDetail.vue` | 任务 A：附加项卡片 |
| 2 | `web/src/views/client/TrackOrder.vue`（或等效进度页） | 任务 B：附加项 + 付款节点展示 |
| 3 | `web/src/locales/zh-CN.js` | i18n |
| 4 | `web/src/locales/en.js` | i18n |
| 5 | `web/src/views/client/templates/TplGallery.vue`（或等效） | 任务 C：preview-teleported |
| 6 | `web/src/views/client/templates/TplTierGrid.vue`（或等效） | 任务 C |
| 7 | `web/src/views/artist/ArtworkManage.vue`（或等效） | 任务 C |
| 8 | `web/src/views/artist/ArtistDetailDrawer.vue`（或等效） | 任务 C |

如需改授权外文件，停止并写 comms 申请。

## 验证标准

- `cd web && npx eslint . && npm run build` 零错误零警告
- OrderDetail：附加项添加/删除/列表正常，金额重算正确
- 客户进度页：附加项 + 付款节点展示正确，无画师内部信息泄露
- 4 个组件图片预览不被截断
- 提交前 `git diff --stat` 确认只有授权文件

## 交付

完成后写 `docs/comms/02-to-01-v017-b3前端提交-0731.md`。
