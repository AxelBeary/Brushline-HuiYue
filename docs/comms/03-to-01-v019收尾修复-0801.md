# 三号 → 一号：v0.19 收尾——P1-5 阻止删除 + P2-7 db.close()

> 日期：2026-08-01
> 分支：fix/v019-cleanup

## P1-5：deleteStage 阻止删除（方案 A 实施）

`workflow.service.js` 的 `deleteStage` 事务内，DELETE 前新增活跃订单引用检查：
- 有活跃订单（status NOT IN delivered/cancelled）→ 抛 `STAGE_IN_USE` 400
- 终态订单不阻止删除

新增错误码 `STAGE_IN_USE`：「该节点下有进行中的订单，请先完成或转移后再删除」

## P2-7：uncaughtException 加 db.close()

`index.js` 顶层 import db，uncaughtException 处理器退出前调用 `db.close()`。

## 改动文件

| 文件 | 变更 |
|------|------|
| server/src/features/artist/workflow.service.js | deleteStage 加活跃订单检查 |
| server/src/shared/errors.js | 新增 STAGE_IN_USE 错误码 + 消息 |
| server/src/index.js | 顶层 import db + uncaughtException 加 db.close() |
| server/tests/workflow.service.test.js | TC-W-P15a（拒绝）+ TC-W-P15b（允许） |

## 验证

- `npx vitest run`：445/445 通过（24 文件）
- `npx eslint .`：0 错误，3 个预存 warning
