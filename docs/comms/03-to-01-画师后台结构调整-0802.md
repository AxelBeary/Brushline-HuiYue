# 三号交付：画师后台结构调整批次（2026-08-02）

> 分支：`feat/artist-structure-0802`
> Worktree：`artist-commission-03`
> 状态：✅ 全部完成，待一号审核

---

## 完成清单（7 项，6 个 commit）

| # | 任务 | Commit | 改动文件 |
|---|------|--------|----------|
| #44 | 偏好独立导航 | `931e160` | Settings.vue（拆出偏好 tab）、Preferences.vue（新建）、router/index.js、ArtistLayout.vue、zh-CN.js、en.js |
| #7+#8 | 录单入口归位 | `f7dcc0d` | OrderList.vue（删页内按钮）、ArtistLayout.vue（工作分组加回录单） |
| #2 | 名额概览跳转修正 | `69a614f` | SlotOverview.vue（`/settings?tab=commission` → `/slots`） |
| #47+#48 | 折叠态文字裁切 + 主题切换 | `beae4d0` | ArtistLayout.vue（CSS 隐藏折叠文字 + 折叠态补 ThemePicker） |
| #63 | JSON 面板暗色不可读 | `62b7904` | HealthCheck.vue（`--bg-inset` + `--text-primary`） |
| #68 | 管理端顶部 Tab 导航 | `d16c843` | AdminLayout.vue（新建）、router/index.js（嵌套路由）、5 个 admin 子页面（去 page-header + 去重复容器样式） |

## 验证结果

- ESLint：零错误零警告
- 前端测试：87/87 通过（5 文件）
- 后端未改动（server/ 不在本批次范围）

## 注意事项

- Settings.vue 旧链接 `?tab=prefs` 和 `?tab=commission` 会 `window.location.replace('/preferences')` 重定向
- AdminLayout 用 el-tabs 做顶部导航，路由切换靠 `@tab-change` + `router.push`，刷新保持当前 Tab（computed 读 route.path）
- 管理端子页面的 `.admin-page` 容器样式已清空（由 AdminLayout `.admin-body` 统一提供 max-width + padding）
- locales 纯新增键（`menu.preferences`、`preferences.title`），rebase 无冲突
