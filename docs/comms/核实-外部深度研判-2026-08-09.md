# 五号核实报告：外部深度研判（Brushline-HuiYue 审计 2026-08-09）

> 收件人：一号
> 核实人：五号
> 核实对象：用户转来的外部深度审计报告（全量克隆+源码核验，六维度）
> 核实方式：对照当前 master（`3df4077`）逐条静态核实，P0/P1 全查、P2 抽查、数据画像重统计
> 结论先行：**报告整体可信，勘察质量显著高于过往外部报告；P0/P1 全部属实，偏差集中在数据画像数字与个别数量级**
> 本报告仅陈述核实事实与风险定级，**处置与派工请一号审阅定夺**。

---

## 一、逐条判定表

### P0 级（报告定级：致命）

| 条目 | 判定 | 证据（抽查核实） | 五号风险定级 |
|---|---|---|---|
| P0-1 无数据备份方案 | ✅ 属实 | server/scripts 仅 4 脚本（gc-uploads.js / backfill-artwork-dimensions.js / totp-rebind.ts / demo-data.ts），无备份脚本；docker-compose.yml 仅 web+caddy 两服务，无备份服务/定时任务；connection.ts:20-21 `isDocker ? journal_mode = DELETE : WAL` 降级逻辑真实存在 | 致命（记账系统单点文件无备份） |
| P0-2 孤儿文件 GC 白名单机制 | ✅ 属实 | app.ts:37-118 完全对应：8 字段 collect（L54-64）；R19 备注附图事故注释（L59）；v0.35 画风封面事故注释（L61-62）；回收站仅 admin.routes.ts:224-228 一个手动清空接口，无自动 TTL；grep 无黑名单/引用计数模型 | 致命（已两次数据丢失事故，机制依赖人肉登记） |

### P1 级（报告定级：严重）

| 条目 | 判定 | 证据 | 五号风险定级 |
|---|---|---|---|
| P1-1 TOTP 无重放防护 | ✅ 属实 | totp.ts:129-144 verifyTotp 纯函数校验，无已用计数器；auth.service.ts:146-149 校验成功后仅清零失败计数；grep `last_counter/used_code/replay` 全仓零命中；artists 表仅 4 个 totp 字段（init.js:40-43） | 严重（90 秒窗口内码可复用；但攻击面需拿到码，属登录链路） |
| P1-2 无集中状态机 | ✅ 属实 | order-status.ts 全文仅 12 行 2 个 SQL 片段；grep `canTransition/assertTransition/ALLOWED_TRANSITIONS` 零命中；order.service.ts:930-932 手写终态拒绝（报告引 L912 行号略偏，逻辑同）；状态字面量散落全仓（后端精确引号匹配 62 处 / 前端 119 处；宽松匹配更多——报告口径 118+149 方向成立） | 严重（转移合法性无人保证，每加状态靠人肉） |
| P1-3 金额存储双轨制 | ✅ 属实 | price.ts:10-14 PRICE_FALLBACK_SQL 三级回退（final→total→snapshot×100），SQL 侧无 ROUND（JS 侧 L23 有 Math.round）；init.js 中 REAL（price_snapshot/price/default_price/base_price/price_override）与 INTEGER cents 并存 | 严重（对账浮点误差风险 + 认知负担；存量老单走 snapshot 路径） |
| P1-4 限流器进程内存 | ✅ 属实 | rate-limit.ts:9 `buckets = new Map`；已 LRU 化（注释 P1-2 已实现，L5-6）；多实例/重启失效成立；timestamps.shift() O(n)（L41） | 严重→一般（单实例部署下影响有限；若扩容则失效） |
| P1-5 架构单进程天花板 | ✅ 属实 | better-sqlite3 同步驱动；单文件 SQLite + DELETE 模式；GC setInterval 在应用进程内（app.ts:119）；限流内存态；会话无状态 HMAC（auth.service.ts:180-184） | 结构性限制非 bug，属已知设计取舍 |
| P1-6 .env.example 漏变量 | ⚠️ 部分属实 | 代码读取 10 个变量全部确认（CORS_ORIGIN×1/TRUST_PROXY×1/DB_PATH×3/UPLOAD_DIR×8/WEB_DIST×1/PORT×2/SENTRY_DSN×2/SENTRY_DSN_BACKEND×2/DOCKER×1/KUBERNETES_SERVICE_HOST×1）；但 .env.example **已有** CORS_ORIGIN（L35-38）、DB_PATH（L29-30）、UPLOAD_DIR（L32-33）、SENTRY_DSN_BACKEND（L47）说明——真正完全缺失仅 6 个（TRUST_PROXY/WEB_DIST/PORT/SENTRY_DSN/DOCKER/KUBERNETES_SERVICE_HOST），其中 TRUST_PROXY 确为安全相关 | 一般（文档缺口，非运行时 bug） |

### P2 级（报告定级：中等/安慰剂）

| 条目 | 判定 | 证据 | 五号风险定级 |
|---|---|---|---|
| P2-A npm@latest 安慰剂修复 | ✅ 属实 | Dockerfile:24-25 在 `npm install --omit=dev`（L22）之后执行；entrypoint.sh 运行时用 npx tsx 不用 npm；升级的是 npm 自带依赖树非项目依赖 | 低（可复现性 + 假安全感） |
| P2-B allowScripts 死配置 | ✅ 属实 | server/package.json:43-46 存在 allowScripts；全仓 grep lavamoat 零命中（未安装） | 低（纯装饰字段误导） |
| P2-C npm install 非 npm ci | ✅ 属实 | Dockerfile:9、L22 均 npm install（lockfile 被 COPY 但允许更新）；.dockerignore 15 项属实（报告已自我更正） | 低（构建可复现性） |
| P2-D compose 时区注释错误 | ✅ 属实 | docker-compose.yml:35 注释"确保 CURRENT_TIMESTAMP 为北京时间"——错误（SQLite CURRENT_TIMESTAMP 恒 UTC）；date.ts 实现正确（代码对、注释错） | 低（注释误导风险） |
| P2-1 时区双口径 | ✅ 属实 | dashboard.service.ts:64-65 `strftime('%d', o.completed_at, 'localtime')` 与 date.ts UTC 口径并存；tools.service.ts:159 注释原文存在（"toSqliteDate 是 UTC 会差一天"） | 一般（统一口径约定问题） |
| P2-2 E2E 单薄 | ✅ 方向属实 | e2e/tests 下 5 个 spec（e1-client-order 25 行 / e2-client-track 25 / e3-artist-stage 23 / e4-admin-add-artist 23 / e5-admin-workflow 17，合计 113 行）；钱链路（报价→定金→尾款→退款→对账）无 E2E 覆盖成立 | 一般（钱链路无端到端验证） |
| P2-3 巨型组件 | ✅ 属实 | 实测含空行：OrderDetail.vue 1425 / OrderForm.vue 1284 / QueueBoardCalendar.vue 1325 / ManualOrderRight.vue 1122 / ArtStyleManager.vue 944；onUnmounted 28 处、v-html 6 处绑定 sanitized 变量（与报告一致） | 一般（大但不脏，副作用清理到位） |
| P2-4 无 down 迁移 | ✅ 属实 | init.js MIGRATIONS 47 条（L478+），`down(` 零命中；schema_migrations 表存在（L303） | 一般（单机小项目可接受，需写进运维手册） |
| P2-5 compose 缺日志轮转/资源限制 | ✅ 属实 | docker-compose.yml 全文无 logging/mem_limit/cpus 配置；Fastify logger:true 默认每请求打日志 | 一般（长期运行磁盘风险） |
| P2-6 health 不查库 | ✅ 属实 | app.ts:274 `app.get('/api/health', async () => ({ status: 'ok', ... }))` 仅返回进程存活 | 一般（DB 故障时健康检查误报） |
| P2-7 空 catch 15 处 | ⚠️ 数量夸大 | 全仓严格空 catch 0 处、含注释空 catch 8 处（前后端合计）——方向成立数量不符 | 轻微 |

### 第八节"做得好的"部分（报告自评正面项）

抽查 15 项全部命中：CSP 无 unsafe-eval（app.ts:150-152）；timingSafeEqual（totp.ts:141）；签名 URL 精确路径相等（app.ts:180-184）；路径穿越先解码（file-sign.ts:68-79）；500 不透传 message（app.ts:236-242）；fail-fast（init.js:1811 ADMIN_QQ / auth.service.ts:23-24 SESSION_SECRET）；开发密钥每次启动随机（auth.service.ts:26-29）；foreign_keys ON（connection.ts:21）；TOTP 防爆破 5 次锁 15 分钟（auth.service.ts:14-15,130-139）；token_version 会话撤销（auth.service.ts:178-184）；checkOrderConservation 守恒断言（order.service.ts:879-883）；迁移 noTransaction 特例（init.js:1783-1795）；上传三重校验（upload.routes.ts:24,48,157 ALLOWED_MIME_TYPES + safeExt + checkFileType）；MIME 白名单不含 SVG（upload.routes.ts:26,37）。**报告此项无夸大。**

## 二、数据画像偏差（方向成立，数字过时或口径不同）

| 报告数字 | 五号实测 | 说明 |
|---|---|---|
| md 文档 365 | **406** | docs/ 递归统计；soul/skills/software-development 185 与报告完全一致 |
| comms 63 个 | **106** | 报告统计时点较早或口径不同 |
| 近 300 次提交 docs 66/fix 11/feat 9（7.3:1） | **docs 151/fix 24/feat 38/other 87（约 4:1）** | 方向成立（文档提交远超功能），比例有出入 |
| init.js 1883 行 | 1810 | 统计口径差异；**59 CREATE TABLE / 72 ALTER / 44 INDEX / 54 FK 与实测完全一致** |
| 状态字面量 267（118+149） | 精确引号匹配 62+119=181；宽松匹配更多 | "散落全仓"成立，绝对数取决于口径 |
| E2E 154 行 | 113（25/25/23/23/17） | 方向成立（极薄） |
| 测试 1215 用例/2926 断言 | 后端 1016 用例 2442 断言 + 前端 262 用例 621 断言 = 1278/3063；**0 skip 属实**；弱断言 73/3063≈2.4%（报告 82/2926≈2.8%） | 基本属实，口径略不同 |

## 三、核实中发现的补充事实（报告未提或需修正）

1. **P0-2 补充**：GC 已有"画师表为空跳过回收"保护（app.ts:46-50），报告未提——白名单机制仍是主风险，但非完全裸奔。
2. **P1-1 补充**：TOTP 有防爆破锁定（5 次/15 分钟）+ 未注册 QQ 与错码同响应防枚举（auth.service.ts:120-139），重放防护缺失是唯一缺口。
3. **报告未提的正面项**：init.js 迁移默认包事务且区分 noTransaction 特例（L1783-1795）；.dockerignore 排除 docs（构建上下文不含 370 万字符）。
4. **P1-3 修正**：报告称"SQL 侧 PRICE_FALLBACK_SQL 没有 ROUND 直接参与 SUM 聚合"属实；但 JS 侧 resolvePriceCents 有 Math.round（price.ts:23），存量订单误差实际集中在 SQL 聚合路径。

## 四、结论

- 报告 P0（2 项）、P1（5 项属实 + 1 项部分属实）、P2（安慰剂 4 项全属实 + 中等问题 7 项基本属实）——**核心论断无虚假指控**，勘察质量显著高于过往外部报告（此前约 1/3 不实）。
- 最大偏差：P1-6 数量（10→6）、P2-7 数量（15→8）、数据画像数字（comms 63 vs 106 等）——不影响结论方向。
- 报告第七节对多角色流程的批判属**管理决策范畴**，五号不判定，请一号/操作人处理。

—— 五号，核实完毕。处置与派工请一号审阅。
