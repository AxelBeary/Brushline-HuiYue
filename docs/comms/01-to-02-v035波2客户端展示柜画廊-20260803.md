# 派工：v0.35 波 2 —— 客户端展示柜 + 画廊筛选（二号）

> 分支：`feat/v035-client-gallery`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02`
> 需求文档：`docs/requirements/REQ-024-画风档位统一.md`（F3/F4/F6 客户端是你的；F1/F2/F5 后端归三号波 1 并行）
> 风险等级：中（纯前端，但与三号并行，契约以三号交付 comms 为准）
> **并行关系**：三号同时在波 1 做迁移 v37 + API。你的 API 依赖部分**先按本派工的预判契约 mock**，三号交付 comms 里会写最终契约，届时替换。**F3/F4 交互模型不依赖新字段，立即开工**。

## 一号已核实的代码现状（不用重查）

1. **TplStyleGrid.vue**（v0.34 已改）：尺寸行已可点选 toggle + ✓ 高亮 + query 预选跳转 `goOrder()` 带 styleId/sizeId；封面 `fit="contain"` 不裁切；单画风退化路径在 `.tpl-style-single`
2. **useOrderForm.js**：`applyQueryPreselect()`（L310 附近）读 query.styleId/sizeId 预选，query > 草稿；OrderForm.vue `stepDefs`（L629）多画风链 style→size→addon→detail→contact，`watch(loading)` 已按 queryPreselect 跳步（L652）
3. **TplGallery.vue**：瀑布流/网格/editorial 三布局，`ratioStyle()` 用 width/height 占位，点赞 ArtworkLikeButton
4. **ArtistHome.vue**：`artistPublicApi.getPublicStyles(subdomain)` 拉画风 → 传给模板；`getProfile` 拉 artworks
5. 4 模板 ArtistHomeClassic/Gallery/Folio/Atelier.vue 都挂 TplStyleGrid + TplGallery，改共享组件 4 模板自动继承

## 任务 F3：展示柜「点档位切大图」（交互模型，立即开工）

REQ-024 F3（方案 1 用户已拍板）：
1. 点击尺寸行 → 上方大图切换成该尺寸的图
2. 未点任何尺寸 → 大图显示画风封面
3. 点的尺寸没图 → 大图保持画风封面兜底，不留空白
4. 选中带描述/工作天数的尺寸 → 展示区显示该尺寸描述+天数；未选中显示画风描述
5. 单画风与多画风行为一致；移动端滑动切画风不受影响

**数据依赖**：尺寸的 image/description/work_days 字段（三号波 1 迁移后 API 带出）。**先 mock**：`activeStyle.sizes[i]` 里假装有 `image`/`description`/`work_days`，交互逻辑写全；三号 API 就绪后替换数据源。尺寸图渲染优先级（三号预判）：`image_artwork_id` 对应作品图 > `image` 独立上传——以三号契约为准。

## 任务 F4：下单页预选「入口区分」（现有基建上扩展，立即可做）

REQ-024 F4（v0.34 query 预选已打底）：
1. **入口 A（带预选）**：展示柜区域下单。点过尺寸行 → 预选画风+尺寸 → **直接跳第三步「勾增项」**（现有 `watch(loading)` 已实现跳 addonStep，验证+补强）；没点尺寸 → 只预选画风，停选尺寸步
2. **入口 B（不带预选）**：约稿按钮（hero CTA / classic 侧栏 / sticky CTA）、直接输 URL → **清空 query，无预选**，画风尺寸完整再显示
3. 预选**可见可改**：下单页明确显示已预选画风名+尺寸名，可回上一步修改，价格随之重算

**关键实现点**：hero/侧栏/sticky 的「我要约稿」按钮目前 `router.push('/artist/:subdomain/order')` 不带 query（=入口 B，天然无预选）✅ 已符合；展示柜 goOrder 带 query（=入口 A）✅ 已符合。**本任务核心 = 验证入口 A 跳第三步链路 + 补「已预选」可见摘要 + 回退可改**。

## 任务 F6 客户端：画廊档位筛选 + 大图标签跳下单（mock-first）

REQ-024 F6（用户拍板：筛选方案，非橱窗分区）：
1. **画廊卡片保持干净**：网格小图不叠加任何标签/文字
2. **点开大图或 hover** 才显示档位标签 + 自由描述
3. **档位标签可点击** → 跳下单页预选该档位（复用 F4 预选逻辑，含所属画风，跳第三步）；一张作品多标签显示多个，点哪个预选哪个
4. **画廊筛选**：上方一排标签 = 该画师对外档位 + 「全部」；默认「全部」混编；点档位标签只显示标注该档位的作品；再点回「全部」；没标档位的作品只在「全部」下出现；切换平滑过渡不整页刷新

**数据依赖**：artworks 的 tags（尺寸 id 数组）+ description——三号波 1 的公开接口会带出，**端点+字段名以三号交付 comms 为准，先 mock**。筛选标签列表 = 该画师启用尺寸（从 styles 数据派生）。

## i18n 分工（防冲突）

locales 文件你可以改，但只动 `artistHome.*` / `orderForm.*` / `gallery.*` 命名空间；`styleManage.*` / `tiers.*` 归三号（他并行重构后台）。rebase 冲突保留双方。

## 验证标准

1. `cd web && npx vitest run` 全绿（125+，补 F3 切图/F4 入口区分/F6 筛选用例）+ `npx eslint .` 0 + build 通过
2. F3：点尺寸行大图切换、无图兜底封面、描述/天数联动（mock 数据可演示）
3. F4：展示柜点尺寸下单→直落增项步+摘要可见可改；约稿按钮下单→无预选从头选
4. F6：筛选标签切换、大图标签跳下单预选（mock）
5. 4 模板 + 亮暗主题抽查无破版

## 交付

comms：`docs/comms/02-to-01-v035波2客户端-{日期}.md`，**标注哪些是 mock 占位待三号 API 替换**。三号波 1 交付后你做替换联调，联调完申请审核。
