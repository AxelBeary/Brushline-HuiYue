# 01→05：安全加固批（你自己核实的 F1 补全版 + F4/F6/F9/F3）

> 日期：2026-08-05 · 派工：一号 · 执行：五号
> worktree：`../artist-commission-sec` · 分支：`security-hardening`（基于 master `25e410c`）
> 依据：你的核实报告 `docs/comms/05-to-01-安全审计报告核实-20260805.md` §四（对方 dto-patch 为基础，按你指出的 3 处缺陷补齐）

## 开工

`cd ../artist-commission-sec` → `npm install`（server + web）→ `git merge master`

## 任务清单（按你报告 §四，独立 commit）

1. **F1 补全版（本批核心，中风险）**：publicArtistDTO 剔除 totp_secret 等敏感列，应用到 **4 个端点**（me / admin/artists 列表 / admin 画师详情 / **artist/profile**——对方 patch 漏的第 4 处）；getAllArtists 改显式列清单，**补 quick_actions**（前端 Preferences/QuickActions 消费，你已搜证）+ custom_page_path 可留。**getArtistById 内部认证用保留完整行**（中间件需 token_version）。补回归测试：4 端点响应断言不含 totp_secret + 前端消费字段（含 quick_actions）不缺失。
2. **F4**：AUTH_DEV_MODE 生产环境 fail-fast（isDevAuth 加生产守卫，参照三号 ADMIN_QQ fail-fast 同模式 + 测试）。
3. **F6**：Dockerfile USER node + 卷 chown（node:22-slim 默认 root）。本地 docker build 验证启动正常。
4. **F9**：app.js 补 COOP/CORP/X-Permitted-Cross-Domain-Policies 三头。
5. **F3**：CSP 去 unsafe-eval——**先隔离实例实测 Vite 生产构建无 eval 依赖**（你报告已预判 Vue 3 生产一般不依赖），实测通过才删；不通过保留并报告。

## 明确不做

- **F14**（lookup 返回 adminQq）：产品决策项，转用户拍板，不碰 order.routes.ts L310 与 TrackOrder 联系管理员弹窗
- F2/F5/F7/F8/F10/F13：P1/P2 排期项不入本批

## 铁律

1. 基线 server 897 + web 215 全绿 + 本批新增；lint 0；tsc 0
2. F6 改 Dockerfile 后必须 build + 启动实测（容器内文件权限、uploads 卷可写）
3. 交付：commit + `docs/comms/05-to-01-安全加固批-交付-20260805.md`（逐任务 + 测试结果 + F3 实测结论）。不推送不合并。

—— 一号
