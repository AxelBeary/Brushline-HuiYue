# 02-to-01-巡检A顺手修复批-交付-20260807.md

> 交付：二号 → 一号
> 分支：`beta/inspect-fix-batch`（worktree artist-commission-w2）
> 基线：merge master 至 `f7f1e6b`（STATUS v25）后开工
> 门禁：lint 0 error 0 warning / test:web 215/215 / build 成功 / check-i18n OK / server oxlint 0 error

## 改动总览（git diff --stat）

```
 web/src/api/index.js                  |  2 ++
 web/src/main.js                       |  1 +
 web/src/views/client/DeliveryPage.vue |  3 +--
 web/src/views/client/LandingPage.vue  |  2 +-
 web/src/views/client/OrderForm.vue    | 18 ++++++++++++++----
 web/src/views/client/TrackOrder.vue   |  3 +--
 6 files changed, 20 insertions(+), 9 deletions(-)
```

只动施工图授权文件，未碰 styles/后端/其他。

## 逐项执行结果

### 1. 🔴-1 OrderForm 画风模式复制缺明细 ✅

`copyOrderSummary()` 增加 `isStyleMode` 分支：
- 画风模式：`选画风：{selectedStyle.name}` + 选中尺寸名（` · 尺寸`）+ `stylePricePreview.addonItems` 逐条明细（`- {name}: ¥{amount}`）
- 旧模式分支保留原逻辑（tierLabel + pricePreview.breakdown）
- 总价行、clipboard 复制、ElMessage 提示均保留
- 字段名与摘要卡/小票模板一致（`it.name`/`it.amount`，与 summary-card 画风模式模板 L462-464 相同来源）
- i18n 全部走既有键（styleStep/tierLabel/receiptTotal/summaryOrderNo/summaryCopied），未新增键

### 2. 🔴-2 DeliveryPage 双卡片矛盾 ✅

- 文件卡 v-if：`verified && delivery` → `verified && delivery && delivery.deliverables?.length`
- 删除文件卡内的 `el-empty noFiles` 空态（无文件时整卡不渲染，由「未交付」卡承担空态）
- 未交付卡（L40）未动。效果：有文件只显示文件卡，无文件只显示未交付卡

### 3. 🔴-3 LandingPage import 顺序 ✅

`import { ARTIST_STATUS_TYPE } from '../../constants/order.js'` 从 script 中部（L70）移至顶部 import 区（Disclaimer 之后），修复 import/first。

### 4. 🔴-4 TrackOrder loading 恒 false ✅

- 删除 `const loading = ref(false)`（L223）
- 删除模板 `v-loading="loading"`（L4）
- 已 grep 确认全文件除这两处外无 loading 引用；页面实际由 searching / myOrdersLoading 各自控制

### 5. OrderDetail 死解构 3 个 —— ⚠️ 施工图过时，零改动

- 施工图/STATUS 说 OrderDetail.vue 有 `currentStageIdx/nextStage/daysLeft` 3 个死解构
- 实测：全库 grep 三个名字，仅存在于 composables（useOrderWorkflow.js / useOrderDeadline.js）内部定义与返回值；OrderDetail.vue 当前解构（L649-652）为 `hasWorkflow/isTerminal/workflowStages/stageProgress/nextStageName/canAdvanceStage/canBackStage/advanceStage/backStage/turnOffStageTracking/trackOnLoading/enableTracking/loadWorkflowStages`，已无这三个
- 证据：lint 对 OrderDetail.vue 0 warning（此前基线 4 warnings 现只剩 3 个 console，且已被第 6 项清零）
- 结论：该问题已被此前批次（OrderDetail 拆分批）顺手清掉，本项无需动作

### 6. lint warnings 清理

**web 3 个 console** ✅ 全部处理（lint 0 warning）：
- `api/index.js:41` i18n 翻译失败兜底、`api/index.js:73` 401 软跳转失败兜底、`main.js:34` Vue 全局错误边界
- 三者均为**有意的错误处理兜底日志**（代码注释明确「避免静默吞错」；main.js 配合 Sentry 上报保留本地可见性），非调试残留，删除会损失排障能力
- 按施工图授权「调试必需 console 加 eslint-disable-next-line 并注明原因」处理，每处均附原因注释；项目已有 3 处同款先例（TplRules/Settings/OrderForm 的 vue/no-v-html）
- ⚠️ 权衡说明：红线「不新增 eslint-disable」与施工图此条冲突，我按施工图优先（一号书面授权 + 注明原因防滥用）；若一号裁定应删 console 换 0 disable，下一批可再议

**server 6 个 oxlint warning** ⚠️ 未动，请一号定夺：
- 实测清单：pricing-engine.ts `no-new-array` ×2（L186/L232）+ order.service.ts `no-unused-vars`（deriveInstallmentProgress 未用导入）+ 其余 3 个非 no-console 类
- 理由：① 均非 no-console 类，超出施工图「只清 no-console 类」范围；② server/** 属三号域，不在二号授权文件清单内（施工图「其他 lint warning 文件」我按不越权原则未扩展）；③ 施工图门禁第 4 条要求「oxlint 0 error」——当前已满足（0 error / 6 warning，均为既有基线）
- 建议：派三号顺手清（尤其 order.service.ts 未用导入是零风险清理）

## 门禁验证结果

| 门禁 | 结果 |
|------|------|
| `npm run lint` | ✅ 0 error 0 warning（基线 3 warnings → 0） |
| `npm run test:web` | ✅ 215/215（13 文件） |
| `npm run build` | ✅ 成功（chunk >500kB 警告为既有，非本次引入） |
| `npm run check:i18n` | ✅ 无新增硬编码中文 |
| `npx oxlint src`（server） | ✅ 0 error（6 既有 warning 未动，见上） |

## 待一号注意

1. **第 5 项施工图过时**：OrderDetail 死解构已被前批清除，本次零改动（lint 证据见上）
2. **eslint-disable ×3 使用说明**：全部为错误处理兜底日志 + 原因注释，属施工图明确授权的例外场景
3. **server 6 warnings 建议派三号**：非 no-console 类且越我权限，仅 0 error 门禁已过
4. 本批为代码级修复，按用户拍板未附截图
