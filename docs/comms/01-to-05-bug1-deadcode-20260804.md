# 派工：v0.36 波 1 — BUG-1 方案 b（本版本第一任务）+ BUG-5 死代码清账

> 来自：一号 | 2026-08-04
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w5`
> 分支：`fix/v036-bug1-deadcode`（已建好，基于 master 352eac7）
> **开工第一步**：cd 进 worktree 先 `git merge master`，再读本文件。

---

## B-1：BUG-1 方案 b（用户已拍板）

背景：分期状态双模型分叉。`getOrderInstallments`（`order.service.ts` L636）用节点模型（读各期 `paid_cents` 字段），而额度池模型的钱在 `orders.paid_total_cents`（由 order_payments 流水事务维护）。通用收款不传 installmentId → 钱进池但节点状态不动；撤销也不回冲。

方案 b（五号你自己推荐的，用户确认）：**getOrderInstallments 改用池子推算**，死代码 computeInstallmentStatuses 的逻辑转正。

要求：
1. `getOrderInstallments` 读订单 `paid_total_cents`，按节点金额顺序推算每期 paid/partial/partial 的 paidCents（computeInstallmentStatuses L654 的算法直接吸收）
2. **返回结构不变**：`{ id, name, amountCents, paidCents, remainingCents, status }`——调用方 order.routes L215/L413/L982、admin.routes L105，前端零改动。remainingCents = amountCents - paidCents（推算值）
3. 不再读 `order_payment_installments.paid_cents` 字段（旧模型残留，本次不迁移删列，只是不读）
4. 撤销回冲自然修复：负流水 → paid_total_cents 减少 → 状态自动回退，无需额外代码
5. 若 addPayment 有给节点写 paid_cents 的旧逻辑：读代码确认后**保留写路径不动**（避免影响未知消费），本次只改读路径；写路径清理写进交付报告建议
6. 测试：quota-pool.test.js 补池子推算用例——全款覆盖全部节点 / 部分覆盖跨节点 / 撤销回冲 / 多付溢出 / 单节点订单

## B-2：BUG-5 死代码清账（一号已逐项验证，照表执行，删前各自再确认零引用）

| 项 | 一号验证结论（2026-08-04） | 动作 |
|----|---------------------------|------|
| TplCoverShowcase | web 里已不存在（0 命中），清单过时 | 跳过 |
| copyTemplateToArtist | workflow.service.ts L343，生产零调用 | 删 |
| resolveSpeechTemplate | workflow.service.ts L379，生产零调用，仅 random-template.test.js 引用 | 删函数 + 删对应测试 |
| nowSqlite | utils/date.ts L16，生产零调用，仅 TC-DU-03 引用 | 删函数 + 删该测试用例 |
| computeInstallmentStatuses | **方案 b 转正，不是死代码** | 吸收进 getOrderInstallments；若完全内联则删独立函数 |
| TERMINAL_STATUSES / COMPLETED_STATUSES | utils/order-status.ts L6/L15，features 目录无引用 | 全项目（含 tests）确认零引用再删，有一处引用就保留并报告 |

---

## 授权文件

`server/src/features/order/order.service.ts`、`order.routes.ts`（只读路径确认，不改接口契约）、`server/src/features/admin/admin.routes.ts`（同）、`server/src/features/artist/workflow.service.ts`、`server/src/utils/date.ts`、`server/src/utils/order-status.ts`、对应 tests

不动：web/ 全部、迁移文件（本次无迁移）、支付写路径。

## 验证

worktree 内 `npm install` 后：server vitest 全绿（基线 711）+ `tsc` + `lint`。交付报告列明：池子推算测试用例清单、每项死代码的删除确认。
