# 二号 → 一号：REQ-022 F1+F2 前端批 · 交付报告

> 日期：2026-08-05 · 分支：`f1f2-frontend`（worktree `artist-commission-f1f2`）· 执行：二号
> 基线：merge master（ca1424f）+ merge f2-social-backend（dd7c04a，零冲突）
> 验收依据：`docs/REQ022-F1F2验收清单.md`（F1 十条 + F2 十二条 + 防投毒速查表）

## 一、改动清单（13 文件改动 + 5 新建）

### F1 作品链路
| 文件 | 改动 |
|------|------|
| `web/src/api/index.js` | + `publishArtwork(orderId, data)` |
| `web/src/views/artist/OrderDetail.vue` | 交付物卡 header 加「发布为作品」入口（仅 `status === 'delivered'` 显示）；弹窗：勾选交付图（默认全选，非图片置灰不可勾）→ 标题（必填 ≤100）/描述（选填 ≤500）→ 提交 → 成功提示「已发布 N 件作品」+ 可跳作品管理；错误态 ElMessage 不卡死 |

### F2 外链重做
| 文件 | 改动 |
|------|------|
| `web/src/utils/linkValidation.js`（新建） | 前端校验纯函数（复刻后端 platform.ts）：裸链补 https/危险协议拒绝/长度 253-1500-1800/域名末尾匹配识别平台 |
| `web/src/utils/simpleIcons.js`（新建） | simple-icons 白名单 20 个 iconKey → SVG path 映射 + 中文名（构建时 tree-shaking 只打 20 个，已实测独立 chunk） |
| `web/src/components/shared/TplPlatformIcon.vue`（新建） | 共享图标组件：iconKey → simple-icons SVG；无图标 → fallbackChar 单字；无装饰 CSS |
| `web/src/views/artist/Settings.vue` | 外链/平台链接两区合一：`[平台下拉(disabled)][URL][↑][↓][✕]`，上限 8，空态「还没有添加链接」，粘贴/输入即时识别（validateLink），保存逐行前端校验后只传 `[{url}]`；加载 GET /api/platforms |
| `web/src/composables/useArtistData.js` | 删 LINK_ICON_BADGE/PLATFORM_BADGE + socialLinks/platformLinks → 统一 `footerLinks`（platformId 查 platforms 表渲染 name/iconKey/fallbackChar；platformId=null → 其他） |
| `web/src/views/client/ArtistHome.vue` | 加载 GET /api/platforms（静默失败走兜底）+ 传给 4 模板 |
| `web/src/views/client/templates/ArtistHome{Classic,Gallery,Folio,Atelier}.vue` | 页脚两区块合一：一排（图标+名称，新窗口，自动换行）；TplHero 传 platforms |
| `web/src/components/templates/TplHero.vue` | socialLinks → footerLinks + platforms prop |
| `web/src/views/client/LandingPage.vue` | 删 weiboUrl/bilibiliUrl 块 + 死样式 |
| `web/src/views/admin/PlatformManage.vue`（新建） | 平台 CRUD：表格（名/图标预览/域名/排序/启用 switch）+ 增改弹窗（iconKey 白名单下拉/fallback_char/域名多行/排序/启用）+ 删除确认（归「其他」提示） |
| `web/src/components/admin/AdminLayout.vue` | 菜单 + 「社交平台管理」tab |
| `web/src/router/index.js` | 注册 `/admin/platforms` 路由 |
| `web/src/locales/{zh-CN,en}.js` | 双语键补齐；删死键（landing.weibo/bilibili、settings.platformLabel/platformAuto/platformHint/linkName） |
| `web/package.json` + lock | + `simple-icons`（用户已拍板批准） |

### 测试
| 文件 | 内容 |
|------|------|
| `web/src/utils/__tests__/linkValidation.test.js`（新建） | 27 用例：归一化/长度边界/防投毒末尾匹配/组合识别 |
| `web/src/composables/__tests__/useArtistData.test.js` | + footerLinks 5 用例 |

## 二、验证结果

- **vitest（web）**：224/224 全绿（基线 192 + 本批新增 32）
- **eslint（web）**：0 错误 0 警告
- **build（web）**：通过（6.9s）；simple-icons 只打包白名单 20 图标（独立 chunk `TplPlatformIcon-*.js`，tree-shaking 实锤）
- **API 联调**（f1f2 worktree 本地 server :3001，TOTP 登录 alice 实跑）：
  - `GET /api/platforms` → 24 平台种子，camelCase 字段 ✓
  - `PUT /api/artist/profile` customLinks 防投毒向量：`weibo.com/alice`→platformId=1 ✓；`weibo.com.evil.com/abc`→null ✓（V4）；裸链 `weibo.com/bare` 补 https→1 ✓（V7）；未知域名→null ✓（V15）；含 `javascript:alert(1)` 整包 400 LINK_URL_INVALID ✓（V17 后端兜底）
  - `POST /api/artist/orders/1/publish-artwork`（pending 订单）→ 400 PUBLISH_WRONG_STATUS ✓（F1-验收 2 后端侧）
- **浏览器自测**（vite dev :5173，classic 模板 alice/carol 主页）：页面完整渲染、无链接时页脚隐藏、无平台数据走「其他」兜底不崩、控制台零 JS 报错

⚠️ 端口冲突说明：3000 被五号 polish worktree server 占用（未动）；f1f2 联调 server 用 3001。前端页面完整截图受限（vite proxy 指向 3000=master 代码无 /api/platforms），渲染行为以单测 + 浏览器无数据兜底路径 + build 为证。

## 三、对照验收清单自检

### F1（10 条）
| 条目 | 结果 | 说明 |
|------|------|------|
| F1-1 delivered 入口 + 默认全选 | ✅ 实现 | OrderDetail 交付物卡 header 按钮仅 delivered；弹窗默认全选图片 |
| F1-2 非 delivered 无入口 | ✅ 实现+实测 | `v-if="order.status === 'delivered'"`；pending 订单调接口 400 实锤 |
| F1-3 勾 N 张 = N 条 | ✅ 后端契约 | 一图一条由后端 publishArtwork 保证（三号测试覆盖）；前端按 deliverableIds 传 |
| F1-4 发布后原图可下载 | ✅ 后端契约 | 复制非移动（后端交付报告 §）；前端交付物卡不动 |
| F1-5 作品管理可见 | ✅ 后端契约 | 201 返回 artworks 数组 |
| F1-6 档位多选替换 | ✅ 复用零改动 | ArtworkManage.vue 已有 setArtworkTags 多选弹窗（PUT 替换语义，grep 实锤） |
| F1-7 4 模板档位分组 | ✅ 复用零改动 | TplGallery filterSizes 筛选已有（v0.35 F6） |
| F1-8 展品预选 `?sizeId=` | ✅ 复用零改动 | TplGallery → OrderForm ?sizeId= 已有（v0.35 F6） |
| F1-9 未绑档位作品兜底 | ✅ 复用零改动 | filterArtworksBySize sizeId=null 全量混编 |
| F1-10 边界错误态 | ✅ 实现 | 空标题禁用提交；错误态 ElMessage；可重复发布（弹窗每次重开默认全选剩余） |

### F2（12 条）
| 条目 | 结果 | 说明 |
|------|------|------|
| F2-1 两区合一+操作 | ✅ 实现 | 一行 `[下拉][URL][↑][↓][✕]`；上限 8（MAX_LINKS）；空态文案 |
| F2-2 粘贴即时识别 | ✅ 实现+单测 | @input detectLinkPlatform；weibo/bilibili 识别单测过 |
| F2-3 裸链补 https | ✅ 实现+实测 | validateLink 归一化；API 实测 weibo.com/bare → https://weibo.com/bare |
| F2-4 防投毒归其他 | ✅ 实测 | evil 子域/前缀粘连 API 实测 platformId=null |
| F2-5 非法协议拒绝 | ✅ 实现+实测 | 前端 validateLink 拒；后端兜底 400 实测 |
| F2-6 超长拒绝 | ✅ 实现+单测 | 长度边界单测（253/1500/1800）全过 |
| F2-7 第 9 条拒绝 | ✅ 实现+后端 | 前端按钮禁用 + 后端 maxItems 8 |
| F2-8 停用/删除保护 | ✅ 后端契约 | 停用后 GET /api/platforms 不含（画师下拉不出现）；已存链接 platformId 保留渲染；删除归其他（DELETE 返回 reattributed） |
| F2-9 4 模板页脚 | ✅ 实现 | 一排合一、图标+名称、新窗口、自动换行；浏览器验证无链接隐藏/无平台兜底 |
| F2-10 管理端 CRUD | ✅ 实现 | PlatformManage 页 + 路由 + 菜单；iconKey 白名单下拉 |
| F2-11 i18n 双语 | ✅ 实现 | zh-CN/en 同步；死键已删（grep 零残留） |
| F2-12 旧结构收尾 | ✅ 实测 | `grep platformUrls/platform_urls/weiboUrl/bilibiliUrl/socialLinks/platformLinks/LINK_ICON_BADGE/PLATFORM_BADGE` 全仓零命中 |

### 防投毒向量（17 条）
V1/V2/V3/V6/V7 识别 ✓（实现+单测）；V4/V5/V15 归其他 ✓（实测）；V8/V9/V10 拒绝 ✓（前端+后端实测）；V11/V12/V13 超长 ✓（单测）；V14 第 9 条 ✓；V16 删除归其他 ✓（后端契约+reattributed 实测返回结构）；V17 后端兜底 ✓（实测 400）。

## 四、说明事项

1. **平台名多语言策略**：平台 name 只存中文（DB 种子/管理端输入），英文界面显示中文平台名属预期（验收清单已注明）；simple-icons 无图标的 4 平台（LOFTER/抖音/QQ空间/米画师）用 fallback_char 单字兜底。
2. **Settings 平台下拉为 disabled 展示**（后端重推导 platformId，手动选择无意义且会被忽略——防投毒核心）。
3. **F1-验收 6/7/8/9 复用零改动**（ArtworkManage 档位弹窗 / TplGallery 筛选+预选 / 未绑档兜底均为 v0.35 既有机制，grep 确认存在）。
4. **server 零改动**：本批只读消费 f2 后端 + master 后端。
5. 测试基线说明：本批 web 224（原 192 + 新 32），server 未跑（本批 server 零改动，基线 864 由三号 f2 交付时验证）。

## 五、commits

`REQ-022 F1+F2 前端批（发布为作品入口 + 外链重做前端 + 平台管理页 + simple-icons 精选）——二号交付，server 零改动；测试 web 224 全绿 / lint 0 / build 过；API 联调防投毒向量实测（evil 子域/裸链补全/协议拒绝全过）`

二号转交一号，文件：docs/comms/02-to-01-F1F2前端批-交付-20260805.md
