# 二号 v0.22 派工（2026-08-01）

> 分支：`feat/v022-frontend`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-fe`
> 基准：master `6164c2c`

---

## 任务清单（按优先级排序）

### 第 1 波（零依赖，立刻开工）

| # | 项 | 说明 |
|---|---|---|
| B1 | 瀑布流统一 | TplGallery 默认 layout 改 masonry，4 模板统一。folio 已有实现可复用。验收：竖图/横幅/混排不裁切，4 模板一致 |
| A5 | PaymentBar 暗色适配 | 浅色 94% 亮度在暗色下偏亮，调 HSL。小修 |

### 第 2 波（等三号后端 API 合入后）

| # | 项 | 说明 |
|---|---|---|
| B2-FE | 小公告前端 | TplAnnouncement.vue + 4 模板各自决定位置（约束：首屏可见）。📢 图标 + 底色区分。画师设置页编辑入口 |
| B3-FE | 点赞前端 | ArtworkLikeButton.vue（心形动画 + localStorage 去重 + 计数），嵌入 TplGallery 作品卡片 |

### 第 3 波（工程收尾）

| # | 项 | 说明 |
|---|---|---|
| A1 | Sentry 前端 SDK | DSN 构建注入已就绪（VITE_SENTRY_DSN ARG），接入 @sentry/vue，环境变量开关 |
| A2 | E2E 接入 CI | GitHub Actions workflow，playwright install chromium，跑现有 5 条路径 |
| A4 | EP CSS 按需引入 | 全量 470kB→按需。用 unplugin-element-plus 或手动按需 import |

---

## 待你确认（回复写 comms）

| # | 问题 |
|---|---|
| T3 | 公告在 4 个模板的具体位置（classic/gallery/folio/atelier 各放哪） |
| T5 | 点赞计数为 0 时是否显示数字 |

---

## 授权文件范围

- `client/src/components/Tpl*.vue`
- `client/src/components/ArtworkLikeButton.vue`（新建）
- `client/src/components/TplAnnouncement.vue`（新建）
- `client/src/views/artist/ArtistHome*.vue`
- `client/src/views/artist/settings/`（公告编辑入口）
- `client/src/main.ts`（Sentry 初始化）
- `client/src/composables/useArtistProfile.js`
- `client/package.json`（新增依赖）
- `client/vite.config.ts`（EP 按需引入插件）
- `.github/workflows/`（CI 配置）
- `client/src/styles/`（暗色适配）

不在授权范围的文件不要动。需要改其他文件先写 comms 说明。
