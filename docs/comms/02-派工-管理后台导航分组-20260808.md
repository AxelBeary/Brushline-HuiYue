# 02-派工-管理后台导航分组-20260808.md

> 派工：一号 → 二号（client-frontend）
> worktree：`../artist-commission-w2`（分支 `beta/admin-nav-groups`）
> 背景：用户拍板「管理后台左侧导航可以专门出二级导航吗？还是有别的更好的办法？」——一号方案：**分组标题**（非折叠子菜单），与画师后台 MENU_GROUPS 纸墨视觉一致；7 项不多，折叠反而增加点击成本
> 基线：master `e7c45f5`。**开工第一步 `git merge master`**。
> 门禁：web 215/215 + oxlint 0 + build 过 + check:i18n 0 + **before/after 截图**（桌面 1440，workspace/temp/admin-nav-groups/）

## 授权文件（只许动这些）

- `web/src/components/admin/AdminLayout.vue`（导航注册表 + 侧栏渲染 + 移动端抽屉）
- `web/src/locales/zh-CN.js` + `en.js`（zh/en 同步）
- **不碰**：其他任何文件；后端零改动

## 施工图

### 1. 导航注册表改分组（AdminLayout.vue 行 ~113-122）

现状 `navItems` 是 7 个平铺项。改为：

```js
// ─── 导航注册表（侧栏与抽屉共用，分组渲染） ───
const NAV_GROUPS = [
  { key: 'overview', labelKey: 'admin.navGroupOverview' },
  { key: 'ops', labelKey: 'admin.navGroupOps' },
  { key: 'config', labelKey: 'admin.navGroupConfig' }
]
const navItems = [
  { path: '/admin', icon: Management, labelKey: 'admin.panelTitle', group: 'overview' },
  { path: '/admin/artists', icon: User, labelKey: 'admin.artistManage', group: 'ops' },
  { path: '/admin/greetings', icon: ChatLineSquare, labelKey: 'admin.greetingManage', group: 'ops' },
  { path: '/admin/default-workflow', icon: SetUp, labelKey: 'admin.defaultWorkflow', group: 'ops' },
  { path: '/admin/platforms', icon: Share, labelKey: 'admin.platformManage', group: 'config' },
  { path: '/admin/health', icon: Monitor, labelKey: 'admin.health.title', group: 'config' },
  { path: '/admin/analytics', icon: TrendCharts, labelKey: 'admin.tracking.title', group: 'config' }
]
```

### 2. 侧栏渲染分组（行 ~19-36 桌面 + 行 ~76-87 抽屉）

两处 `v-for="item in navItems"` 改为**按 NAV_GROUPS 嵌套渲染**：

```
for group in NAV_GROUPS:
  渲染组标题（小字 label，如「运营」「配置与监控」）
  for item in navItems where item.group === group.key:
    渲染 nav-item（现有样式不动）
```

- 组标题样式：`padding: 14px 16px 6px; font-size: 11px; color: var(--ink3); letter-spacing: .08em;`（参照 ArtistLayout 分组标题风格）
- **折叠态（collapsed）**：隐藏组标题，只显示图标（保持现状宽度）
- **移动端抽屉**：同样分组渲染

### 3. i18n 新增 3 键（zh/en 同步）

```
zh: navGroupOverview: '概览', navGroupOps: '运营', navGroupConfig: '配置与监控'
en: navGroupOverview: 'Overview', navGroupOps: 'Operations', navGroupConfig: 'Config & Monitor'
```

### 4. 视觉铁律

- 组标题用 `--ink3` 小字（低饱和），与激活项花青软底不抢
- 分组间隔 `--sp-2/--sp-3` 统一
- 双主题自动适配（用现有 CSS 变量）

## 实现铁律（用户原话，违反打回）

> 「我们实现起来一定要避免屎山，禁止技术债，代码要优雅！」

1. 不做假修复；不引新依赖（不用 el-menu，分组标题是纯 div + v-for）
2. i18n 键 zh/en 同步；禁硬编码中文
3. 折叠态/抽屉态不回归（保持现有响应式行为）

## 验证门禁

1. `cd web && npm run test:web` → 215/215
2. `cd web && npm run lint` → 0 error
3. `cd web && npm run build` → 成功
4. `cd web && npm run check:i18n` → 0 新增硬编码中文
5. **截图**：桌面 1440 展开态（分组可见）+ 折叠态（图标条无组标题），存 workspace/temp/admin-nav-groups/
6. `git diff --stat` 只动 AdminLayout.vue + locales

## 交付

- commit 到分支 `beta/admin-nav-groups`，不推送不合并
- 交付报告 `docs/comms/02-to-01-管理后台导航分组-交付-20260808.md`：分组方案 + 截图 + 折叠/抽屉验证说明
