# 03-派工-管理后台仪表盘-20260808.md

> 派工：一号 → 三号（backend 兼前端轻活）
> worktree：`../artist-commission-w3`（分支 `beta/admin-dashboard`）
> 背景：**用户发火点名**：「管理后台都专门独立了 不能像画师页面那样专门做个仪表盘吗？各种功能挤在一起 乱七八糟 还有回收站也塞主页？能不能用点心！」——AdminDashboard 重排 + 回收站移出主页
> 基线：master `4b23caf`。**开工第一步 `git merge master`**。
> 门禁：web 215/215 + lint 0 + build 过 + check:i18n 0 + **before/after 截图（必交）**

## 现状（一号已核实）

- `web/src/views/admin/AdminDashboard.vue`：统计卡（3 张）+ 快捷操作条 + 画师列表 + **回收站卡片（行 72-73 塞在主页）**——确实「乱七八糟」
- 管理后台有独立 AdminLayout（左侧导航分组 概览/运营/配置与监控）

## 授权文件

- `web/src/views/admin/AdminDashboard.vue`（重排）
- `web/src/views/admin/ArtistManage.vue`（回收站迁入：画师管理页加回收站 tab 或入口，先读现状确认 ArtistDetailDrawer/ArtistManage 哪个适合）
- `web/src/locales/zh-CN.js` + `en.js`
- **不碰**：其他页面、后端、AdminLayout（导航分组已完成）

## 施工图

### 1. 主页只留「仪表盘」内容（对标画师后台 Dashboard 的清爽）
- 顶部：页头 + 副标题（保留）
- 统计卡区：3 卡（画师数/总订单/活跃订单）→ 可加「今日新增」等运营指标（若后端有，先 grep admin summary 接口返回；无则不加）
- **回收站卡片从主页移除**（用户点名）→ 迁到画师管理页（ArtistManage 加「回收站」入口/tab，或独立小卡片）
- 快捷操作条保留（用户高频：管理画师/问候语/默认流程/系统自检）
- 画师列表保留（但可压缩高度，或改为「最近活跃画师」Top5 + 「查看全部」按钮——**先看现有数据够不够，不够就保留全列表**）

### 2. 布局对齐画师后台 Dashboard 风格
- 参考 `web/src/views/artist/` 下 Dashboard 的间距/卡片节奏（--sp-*/--fs-*/--r-l 纸墨 token）
- 区块之间留白、分组清晰：统计卡一行 → 操作条 → 画师列表（或最近活跃）
- 深色/亮色双主题自适应（用 token，不硬编码）

### 3. 回收站迁入后的行为
- 现有回收站功能（孤儿文件可恢复）完整保留，只是位置从主页移到画师管理
- 入口文案清晰（「回收站」按钮 → 列表/恢复弹窗）

## 实现铁律（用户原话「能不能用心！」）

1. 布局要「用心」：信息密度适中、对齐一致、间距统一（--sp-*），不挤不空
2. 不做假修复；i18n zh/en 同步；禁硬编码中文
3. 功能零变化（只挪位置 + 重排），不删数据不改 API

## 验证门禁

1. `cd web && npm run test:web` → 215/215
2. `cd web && npm run lint` → 0 error
3. `cd web && npm run build` → 成功
4. `cd web && npm run check:i18n` → 0 新增硬编码中文
5. **截图**：AdminDashboard before/after（桌面 1440 深色+亮色）+ 回收站新位置，存 workspace/temp/admin-dashboard/
6. `git diff --stat` 只动授权文件

## 交付

- commit 到分支 `beta/admin-dashboard`，不推送不合并
- 交付报告 `docs/comms/03-to-01-管理后台仪表盘-交付-20260808.md`：重排方案 + 回收站新位置 + 截图 + 布局说明
