# 用户侧接力开工指令（2026-08-05 下午）

> 用法：在外部新会话窗口中，复制对应角色段落全文粘贴即可开工。三段可同时开三个窗口（各自独立 worktree，不冲突）。
> 一号会话（本会话）保持 master 门禁：交付回来我审核合并。

---

## 指令一 → 二号（v0.38 视觉批第四轮）

你是「绘约/artist-commission」项目的二号（画师后台视觉重设计）。全程中文输出（思考、注释、commit message、交付报告），代码标识符用英文。这是第四轮接力——前三轮已完成 token/骨架/仪表盘/QueueBoard 并 commit 锁定（`158250f`），本轮收尾剩余三页+验证。

开工流程：
1. 读角色 soul：D:\Hermes Agent CN Desktop\workspace\artist-commission\docs\soul\soul-02-client-frontend.md
2. cd worktree：D:\Hermes Agent CN Desktop\workspace\artist-commission-v038（分支 v038-visual，已 merge master 至 cc6535c）
3. git log 确认 HEAD 含 `158250f`（阶段成果）+ merge commit
4. 读派工文件 docs/comms/01-to-02-v038视觉重设计第一批-20260805.md（§三核心页要求 + §五验证要求）+ REQ-026 §四（日期卡行为规则）

本轮任务（前几轮断点清单）：
1. **OrderDetail.vue 换肤+日期卡二合一**（最难，要点）：
   - 现有日期逻辑在 L876-939 附近（deadlinePicker/startDatePicker 两个 ref + watcher + changeDeadline/changeStartDate 即时保存 + 错误回弹 + disableDeadlineDate/disableStartDateDate 校验）——逻辑一行不动，只动模板与样式
   - 模板：把两个 el-descriptions-item 日期项移入独立「日期」el-card（键 orderDetail.dateCardTitle，locales 已补）；保存成功 toast 键换 deadlineSavedSync/startDateSavedSync（已补）
   - 加 daysLeft computed：截稿日−今天；模板 chip——未来「剩 N 天」(daysLeft 键)、逾期「逾期 N 天」朱砂(daysOverdue)、当天(daysToday)
   - 整页样式区换 artist-tokens token（注意 tests/OrderDetail.duebanner.test.js stub 了 el-date-picker，改后跑它）
2. **ManualOrder.vue**：只换视觉（样式区 L1182 起换 token；模板/功能/测试类名 .mo-field/.tier-card/.next-due-banner 不动——有测试断言）；跑 tests/ManualOrder.stylemode.test.js
3. **Preferences.vue** 套 token（129 行小页）
4. 全量验证：web vitest（基线 192，注意 OrderForm.summary 测试在 master 已合入）+ eslint 0 错
5. npm run dev 浏览器实测截图：两主题（宣纸/墨黑）×（仪表盘/排期/订单详情日期卡/手动录单/侧栏）≥10 张 + 客户端抽查 LandingPage/ArtistHome 模板/OrderForm 3 张 + 逾期单朱砂 + 日期卡改日期行为。截图路径写报告
6. 语义 commit（禁止 git add -A，逐个加）+ 交付报告 docs/comms/02-to-01-v038视觉第一批-交付-20260805.md 并 commit。**不推送不合并**——合并权在一号

纪律：授权/黑名单见派工文件 §四（theme.css/templates.css/palettes.css 只读；views/client/、views/admin/、server/、components/templates/ 不碰）；ThemePicker 被客户端使用不碰。若撞迭代上限：先 commit 已写部分（注明进行中）再诚实报告。

---

## 指令二 → 三号（F2 外链后端第二轮）

你是「绘约/artist-commission」项目的三号（后端）。全程中文输出，代码标识符用英文。这是第二轮接力——第一轮已完成防投毒纯函数/平台服务/迁移 v42/24 平台种子并锁定（`4f64d82`），本轮续路由+主体改造+测试。

开工流程：
1. 读角色 soul：D:\Hermes Agent CN Desktop\workspace\artist-commission\docs\soul\soul-03-backend-artist.md
2. cd worktree：D:\Hermes Agent CN Desktop\workspace\artist-commission-f2（分支 f2-social-backend，已 merge master 至 cc6535c，HEAD 含 `4f64d82`）
3. 读派工文件 docs/comms/01-to-03-F2外链后端-20260805.md（断点清单在派工外，以本指令为准）
4. 读第一轮成果：server/src/shared/utils/platform.ts（normalizeLinkUrl/assertLinkLengthLimits/matchDomain/derivePlatformId）、server/src/features/platform/platform.service.ts、server/src/db/init.js（迁移 v42）

本轮任务（断点清单，顺序执行）：
1. **artist.service.ts 主体改造**：updateArtist 的 custom_links 分支重写（新结构 [{platformId,url}]、归一化+长度+条数≤8 硬校验、platformId 一律后端重推导）；删 platform_urls 写入分支与 allowed 白名单项；getCustomLinks 删 weibo_url/bilibili_url 回退；删 getPlatformUrls
2. **artist.routes.ts**：PUT schema customLinks 改新结构（maxItems 8）；删 platformUrls schema/keyMap；GET /api/artists/:subdomain 响应改新结构
3. **平台 CRUD 路由**：GET /api/platforms（公开）；管理端 GET/POST/PUT/DELETE 挂 admin.routes.ts（仅平台 CRUD 段）
4. **删旧 server/src/utils/platform.ts**（若存在；白名单外删除，一号已批准——报告里注明）
5. **测试（防投毒向量先行）**：weibo.com ✓ / m.weibo.com ✓ / weibo.com:8080 ✓ / weibo.com.evil.com ✗ / xweibo.com ✗ / javascript: ✗ / ftp: ✗ / 裸链补全 ✓ / 超长（域名254/总长1801）✗ / 9条 ✗ / 未知归其他 ✓ / DELETE 平台后链接归其他 ✓；改写受影响的既有测试（platform/artist 链接相关）
6. 验证：server vitest 全绿（基线 831）+ tsc 0 + lint 0 → merge master 重跑 → 分批 commit（禁 add -A）→ 交付报告 docs/comms/03-to-01-F2外链后端-交付-20260805.md（icon_key 清单 24 平台：sinaweibo/bilibili/xiaohongshu/pixiv/x/kuaishou/douban/youtube/instagram/twitch/artstation/tiktok/deviantart/zcool/afdian/weasyl/threads/tumblr/behance/neteasecloudmusic + 4 个 fallback：LOFTER=L、抖音=抖、QQ空间=空、米画师=米）

⚠️ 合并门控：本分支交付后**暂不合入** master（前端批在 v0.38 后）。errors.ts 与删旧文件超白名单，一号已批准，报告注明即可。

---

## 指令三 → 五号（F5 留言筛选安全范围批）

你是「绘约/artist-commission」项目的五号（小任务执行者）。全程中文输出，代码标识符用英文。全新批次，无断点。

开工流程：
1. 读角色 soul：D:\Hermes Agent CN Desktop\workspace\artist-commission\docs\soul\soul-05-bugfix.md
2. cd worktree：D:\Hermes Agent CN Desktop\workspace\artist-commission-f5（分支 f5-guestbook-filter，已 merge master 至 cc6535c）
3. 读派工文件 docs/comms/01-to-05-F5留言筛选-20260805.md，逐条执行

范围红线：「未读/已读」语义待用户拍板，本批一律不碰——不加 read_at 字段、不改语义映射。只做：按画师筛选、按审核状态筛选（pending/approved/rejected）、按是否已回复筛选（artist_reply IS NOT NULL）、后端 query 参数化、前端三个下拉筛选器、状态列 el-tag 改 i18n 文案（现在直接显示英文枚举，顺手修）。

验证：server vitest（基线 831）+ web vitest（基线 192）+ tsc 0 + lint 0。完工 commit 单行 feat(admin):，禁止 add -A，报告 docs/comms/05-to-01-F5留言筛选-交付-20260805.md。**不推送不合并**。
