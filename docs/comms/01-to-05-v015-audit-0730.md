# 一号 → 五号：v0.15 审计任务

> 日期：2026-07-30
> 分支：`chore/v015-audit`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-05`

---

## 任务1：文档一致性审计

- `docs/soul/` 5 个 soul 文件：检查角色职责描述是否与当前实际分工一致（v0.14 后有更新）
- `docs/requirements/` 所有 REQ 文件：检查状态标注（已审核/已关闭/待确认）是否准确
- `README.md`（如有）：检查技术栈描述、启动命令、目录结构是否与当前代码一致
- `docs/comms/STATUS.md`：仅检查格式和规则提醒部分是否完整（内容一号维护，不改）
- 输出：`docs/audit/v015-docs-audit.md`，列出问题 + 建议修复

## 任务2：代码质量审计

- ESLint 全量扫描（`npx eslint server/src web/src`），记录 warning/error 数量
- 测试覆盖率：`npx vitest run --coverage`（server/），记录未覆盖的关键路径
- 检查 `web/src/api/index.js`：所有 API 调用是否有对应的后端路由（无孤儿调用）
- 检查 i18n 完整性：`zh-CN.js` 和 `en.js` 的 key 是否一一对应（无缺失/多余）
- 输出：`docs/audit/v015-code-audit.md`，列出问题 + 严重等级

## 任务3：安全快扫

- 检查所有路由的 auth 中间件覆盖（requireAuth / requireAdmin）
- 检查 CSP 配置（app.js）是否有遗漏
- 检查文件上传校验（类型/大小/路径遍历）
- 输出：纳入 `docs/audit/v015-code-audit.md` 安全章节

---

## 授权文件

- 只读审计，不改代码
- 输出文件：`docs/audit/` 目录
- 如发现必须修的问题（如 ESLint error），在审计文档中标注，由一号决定是否派人修

## 提交规范

- `docs(audit): v0.15 文档一致性审计` / `docs(audit): v0.15 代码质量+安全审计`
- 提交前 `git diff --stat` 确认只有 `docs/audit/` 下文件
