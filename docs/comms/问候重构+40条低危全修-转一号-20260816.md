# 转交一号：问候重构 + 40 条低危全修 + 本轮反馈统筹（2026-08-16）

> 来源：四号过堂终稿《815过堂核对-第1轮-20260816.md》（用户逐条拍板，结论已核对代码/实测）
> 优先级：问候重构（B 测观感核心）> 封禁统一入口/5 条反馈/科学计数法（用户已撞到）> 40 条低危全修（下次开工排）

## 一、问候系统重构（用户拍板终稿，立即排期）

### 时段表（7 档全覆盖）
清晨 4:00~6:59 ｜ 上午 7:00~11:59 ｜ 午后 12:00~13:59 ｜ 下午 14:00~17:59 ｜ 夜晚 18:00~21:59 ｜ 深夜 22:00~3:59 ｜ 全天（万能兜底）

### 抽取逻辑（用户原话：「节日>（时段40%、画师时段专属40%、全天20%）随机>兜底」）
1. 节日（特别日）命中 → 抽该日专属文案
2. 加权随机：时段池 40%（系统通用+当前时段）｜画师时段专属 40%（该画师+当前时段）｜全天池 20%（time_slot=全天）
3. 全空 → 默认「你好，{name}」

### 回落规则（用户确认）
画师专属池空 → 时段池 → 全天池 → 默认问候

### 配置入口（用户确认）
仅管理员后台（全局问候管理页 + 画师详情抽屉，均带时段下拉）；**画师端不加自助入口**（用户原话「不给画师自己设置 只能我设置」）。数据模型已支持画师+时段双维度，无需改表。

### 附带动作
- 消除「午夜/深夜」双档（latenight 优先池概念删除，合并为深夜一档）
- 迁移 v67：旧 evening→夜晚、night/latenight→深夜、morning→上午、afternoon→下午、any→全天；专属/全局归属不变
- 同步：schema CHECK、greeting.service.ts 抽取、管理端下拉、i18n zh/en、greeting 相关测试

### 验收标准（可测试）
1. 时段无空档：4:00、6:59、7:00、12:00、14:00、18:00、22:00、3:59 边界各时刻归属正确
2. 加权可验证：注入随机数，0~39/40~79/80~99 各落对应池；权重分布统计近似 40/40/20
3. 回落链：画师无专属→用系统时段；系统无时段→用全天；全空→默认问候
4. 节日命中优先于一切
5. 管理端两入口 7 档下拉可选；画师端无配置入口（路由不存在）
6. 迁移 v67 回读：旧文案行全部搬家到正确档位，归属（专属/全局）不变
7. i18n 新键 zh/en 一致，旧键（slotLatenight 等）清理；server/web 门禁全绿

## 二、40 条低危全修（用户拍板：「让一号下次开工安排全修」）

来源：桌面《拾绘项目深度审计报告-20260815.md》P2/P3 剩余项约 40 条，分 5 组：
- 交互细节约 15（速记/计数删除无确认、算价器/水印失败静默、批量上传一败全停、回收站 NaN MB 等）
- 冷门边界约 10（匿名参考图永不回收、回收站同名恢复不确定、0 元单改价不补分期等）
- 性能约 5（画廊/订单列表 N+1、埋点丢数据）
- 技术债约 10（注释失实、测试只认 .js、端口硬编码等）
- 安全纵深约 8（TOTP 明文、签名 URL 不绑身份、OG 投毒等）

处置：分批执行（交互→边界→性能→技术债/安全），每批独立门禁验收，按审计编号逐条销账。**建议一号开工时自审计报告提取最新剩余清单**（避免凭记忆）。

## 三、本轮其他已拍板待施工项（用户已撞到，优先）

| # | 项 | 说明 |
|---|---|---|
| 1 | 封禁/移除统一入口 | 画师管理页加封禁按钮；封禁/解封/移除放一个操作入口（用户原话「封禁入口，加。已封禁和移除放在一个入口去处理」） |
| 2 | 快捷入口删「状态切换」 | 快捷入口设置去掉状态切换项，已设置的也不再显示（web/src/locales/zh-CN.js quickAction.status） |
| 3 | 后台导航刷新闪烁 | 价格管理/作品管理点进点出，左侧导航跟着刷新（前端） |
| 4 | 开店不载入节点模板 | 开箱设置后管理员自己小店没正确引入节点模板（默认工作流模板） |
| 5 | 偏好设置整页重构布局 | 含开稿管理名额设置后按钮滚到页面底部问题（升级为整页布局重构） |
| 6 | 排期看板闪烁 | 每次点东西所有内容消失一下再刷出来（前端整块重载） |
| 7 | 科学计数法消毒 | money.js 三函数（formatYuanValue/formatAddonPrice/formatYuanTrimmed）统一消毒，约 20 处调用面一处修全好 |

## 四、⚠️ 一号手里存量反馈提醒（用户叮嘱：不要忘）

用户原话：「一号手里还有上次和之后的反馈 你comms里提醒一号不要忘」

一号排任务清单时，请对照以下全量核对，**勿只处理本 comms**：
1. STATUS.md 待办池（v95 起）所有未销项
2. docs/comms/ 历史转交件（E清单拍板、815审计拍板12项、前端质量战役、CodeQL 处置等）中未闭环项
3. 用户此前各轮口头反馈（含 A 测内测反馈已落档的 docs/comms/核实-内测反馈-20260806.md 批 4 候选等）
4. 本 comms 第三节 7 项

## 五、挂起项（不施工，用户验后再说）
- 一次性下载（终验）：用户「还没人用过 再等段时间」
- 多付提示：用户「还没人用到」
