# 交付：v0.36 六路回归实测报告（二号）

> 二号交付 · 2026-08-05 · 派工：docs/comms/01-to-02-v036六路回归实测-20260805.md
> 环境：容器后端（3000）+ worktree web vite（5173，分支 v036-web-guard 已合 master ab6b4db）
> 通道：前 3 路用 Hermes 浏览器实测，后 3 路因浏览器服务不稳切本地 Playwright（chromium，合成事件 + API 断言），零代码改动

## 总结：六路全过 ✅，无 bug，2 条非阻塞小观察

## 逐路结果

### 路径 1 B1 收款区归零 ✅（订单 ALICE-006，基线 已收 ¥176.40 / 应收 ¥310.00）
- 改价 310→280：收款区「已收 ¥176.40」保持，应收跟随 → ¥280.00 ✅
- 加增项「回归实测增项」+¥15：已收保持，应收 ¥295.00 ✅
- 删增项（有确认弹窗）：已收保持，应收回 ¥280.00 ✅
- 证据：每步后读页面收款区文本，三场景已收值恒为 ¥176.40 不归零
- 数据已还原：改回 ¥310.00，收款区 176.40/310.00/133.60 与基线一致

### 路径 2 P 批收款三连 ✅（同 ALICE-006）
- 负数顺畅：输 -5，blur 后输入框值 **-5.00 保留不清空**（P1 硬钳制已去除的实证）✅
- 负数无原因提交：toast「录入负数（退款/撤销）时必须填写原因」，弹窗保留未提交 ✅
- 负数超已收（-200，已填原因）：toast「退款金额不能超出已收金额 ¥176.40」✅
- 正数多收（+200 > 待收 133.60）：提交成功，收款区显示「**多收 ¥66.40**」橙色（实测 rgb(230,162,60) = el-color-warning）✅
- 数据已还原：¥200 流水已撤销，收款区回基线（撤销时第一次点错行，点到 ¥84 的撤销弹窗，已取消未生效）

### 路径 3 F1 约稿表单跳步 ✅（客户端 /artist/alice/order，多画风 5 步链）
- 选画风（厚涂插画）→ 选尺寸（半身像）→ **增项步点「下一步」→ 准确落在第 4 步「写需求」**（需求描述/参考图/约稿流程均渲染），未跳去联系方式 ✅
- 反向：写需求点「上一步」→ 正确回到增项步 ✅
- 零数据提交（未走到联系方式步）
- 证据：步骤条高亮 + h3 标题快照（「增项与加急」↔「描述你的需求」）

### 路径 4 G1/G2 拖拽守卫 + 录单价格 ✅（/orders/new + /orders/806，Playwright）
- **G2a 价格跟随**：选半身像 → 价格框自动 120.00 → 增项 +1（+30%）→ 自动跟随 156.00 ✅
- **G2b 手输价保留**：手输 999 → 增项 −1 触发重算 → **价格仍为 999.00**（priceTouched 脏标记生效）✅
- **G1 页内拖拽拦截**：合成页内拖拽（dataTransfer 无 Files）→ 录单页和订单详情（.ref-upload-tile）两处 dragenter/dragover/drop 全部 defaultPrevented + toast「页面里的图片不能直接拖进上传区…」✅
- **对照组**：系统文件拖拽（含 Files）两处均放行无警告 ✅

### 路径 5 H1 看板交付防手滑 ✅（Playwright，/queue）
- 造未接流程 done 测试单（ALICE-008）→ 卡片下拉「交付」→ 点击 → **弹出「上传交付文件」弹窗，状态保持 done 未变** ✅
- 关弹窗后状态仍 done ✅（测试单已取消）
- 证据：dialog 可见性 + 点击前后两次 GET status 均为 done + 截图 h1-dialog.png（已随临时文件清理）

### 路径 6 T 批订单详情四项 ✅（Playwright，测试单 ALICE-009）
- **T1 日期 picker**：拦截 deadline PUT 返回 500 → 点选日期后输入框**回滚原值**；放行后改期 → 显示 2026-08-17 且 PUT 仅 1 次 ✅
- **T2 备注 Enter 连敲**：连敲 3 次 Enter → POST /notes 仅 1 个请求、页面仅 1 条 ✅
- **T3 推进连点**：「✔ 标记完成」连点 3 次 → PUT /status 仅 1 个请求，状态 done ✅
- （T4 滑块 pointercancel 属代码层修复，本轮未构造场景，建议用户终验略过或五号补）

## 问题清单

**无 bug。** 2 条非阻塞观察（供一号排期参考，不影响发版）：
1. 收款弹窗录负数时备注 label 仍为「备注（可选）」，但提交强制要原因——文案轻微误导，可考虑负数时 label 变「退款原因（必填）」
2. Hermes 浏览器环境下订单列表「详情」按钮点击不跳转（改用 URL 直达）；本地 Playwright 未复测此路径，疑似自动化环境问题，建议用户终验时顺手点一下

## 演示数据改动披露

| 改动 | 恢复情况 |
|------|----------|
| ALICE-006 改价 2 次（310→280→310） | ✅ 数值还原 ¥310.00；活动日志留改价记录 2 条 |
| ALICE-006 增项加/删「回归实测增项」 | ✅ 增项已删；日志留加/删记录各 1 条 |
| ALICE-006 收款 +¥200（备注「回归实测超收退款」） | ✅ 已撤销（净零）；日志留收/撤记录各 1 条 |
| ALICE-006 系统备注 2→6 条 | 操作自动产生的系统备注，未删（内容均如实记录实测操作） |
| 造单 ALICE-007（H1 首测失败品） | ✅ 报告完成后已补取消 |
| 造单 ALICE-008（H1 测试单） | ✅ 已取消 |
| 造单 ALICE-009（T 批测试单） | ✅ 已取消 |
| ALICE-009 deadline 2026-08-17、备注 1 条 | 随单取消，不可见 |

**git status 干净**：临时脚本/截图已全部删除，worktree 无任何代码改动。

## 验证方式说明（供一号复核）

- B1/P 批/F1：Hermes 浏览器手工操作 + 页面文本断言
- G1/G2/H1/T 批：本地 Playwright（chromium），API 登录 + 合成事件 + 请求计数断言（PUT/POST 次数即防重证据）
- 全部证据为运行时实测值，非代码推断
