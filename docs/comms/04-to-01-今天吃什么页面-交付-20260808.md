# 04-to-01-今天吃什么页面-交付-20260808.md

> 交付：四号 → 一号
> 分支：`beta/food-menu-page`（commit afa0a7e，未推送未合并）
> 派工：docs/comms/04-派工-今天吃什么页面-20260808.md
> 门禁：全部通过（见下）

## 完成内容

| 文件 | 动作 | 说明 |
|------|------|------|
| web/src/views/artist/FoodMenu.vue | 新建 | 工具页（四类模式 + 随机推荐 + 换一个 + 免责提示 + 双主题卡片） |
| web/src/components/ArtistLayout.vue | 修改 | 导入 ForkSpoon 图标；BASE_MENU_ITEMS 加工具组菜单项（/tools/food） |
| web/src/router/index.js | 修改 | 加路由 /tools/food → FoodMenu.vue（requiresAuth + titleKey: menu.foodMenu） |
| web/src/locales/zh-CN.js | 修改 | menu.foodMenu + foodMenu 文案块 |
| web/src/locales/en.js | 修改 | 同步英文 |

## 页面结构（FoodMenu.vue）

- 标题区：od-page-title 标题 + 一行副标题（对齐既有后台页面写法）
- 四大类模式选择：radio 按钮组（健康版/糖尿病版/痛风版/外卖版），key 顺序取自 FOOD_CATEGORIES 导出顺序，激活态花青软底 + 边框（--hq token）
- 免责提示：仅糖尿病/痛风版显示「本推荐仅供参考，具体饮食请遵医嘱。」一行小字
- 操作按钮：单按钮双文案（无结果=「随机推荐」，有结果=「换一个」）
- 结果卡片：菜名 + 当前模式徽标 + 全部标签（tag 映射 i18n 键）+ 一句话点评
- 初始空态：虚线框引导文案
- 样式全部走纸墨 token（--ink/--card/--line/--hq/--r-m/--sh-1 + font-scale 字号档位），亮暗双主题自动适配

## 数据源引用方式（只读消费，未修改）

```js
import { FOOD_MENU, FOOD_CATEGORIES } from '../../utils/food-menu.js'
const MODES = Object.keys(FOOD_CATEGORIES)  // 页面四类 key，顺序与数据文件一致
const pool = FOOD_MENU.filter(d => d.tags.includes(currentMode.value))  // 按 tags 过滤当前类
```

- 数据文件注释明确：tags 是分类 key 数组、同菜可多标签（共用池子），故用 tags.includes 过滤而非按分组字段
- 换一个逻辑：池子>1 时排除当前条再随机，避免连续重复

## 验证门禁（全部通过）

1. npm run lint → 0 error
2. npm run check:i18n → 无新增硬编码中文（存量 9 条豁免不变）
3. npm run build → 成功（12.0s），FoodMenu 独立 chunk 已生成
4. npm run test:web → 215/215 通过（13 files，不降）
5. git diff --stat → 仅 5 个授权文件（4 改 1 新）

## 逻辑冒烟（node 直跑数据文件验证）

- 四类池子：healthy 48 / diabetes 45 / gout 46 / takeout 48（共用标签，充足）
- 换一个逻辑连抽 100 次：连续重复 0 次

## 备注

- 纯功能工具页，未截图（派工允许文字描述）
- 未新增测试文件（页面为纯前端交互，215/215 门禁已覆盖既有回归）；如需组件级测试可后续补
- 未推送未合并，等待一号审核