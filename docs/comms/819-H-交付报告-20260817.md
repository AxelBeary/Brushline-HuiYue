# 819-H 交付报告：画师端控件右置扫平批（用户拍板⑨）

> 分支：`feat/819-right-align-artist`（基于 master 3183d548）｜日期：2026-08-17
> 范围：画师端设置/控制类页面按「一行一事——说明在左、控件在右、栅格对齐」拉齐；同类设置收进分组卡片（组头朱砂小印点）。视觉基准对齐 818-B 样板页 QuickNote / Watermark。

## 门禁结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| `web npm run lint`（typecheck + eslint） | ✅ 零错误 | vue-tsc 全过，eslint 无告警 |
| `web npm run check:i18n` | ✅ OK | 无新增硬编码中文，存量 13 条豁免 |
| `web npm run test:web` | ⛔ 沙箱拦截 | vite/vitest 内部 `spawn EPERM`（esbuild 服务无法拉起），与既往批次同因；请一号在主仓态复验 |
| `web npm run build` | ⛔ 沙箱拦截 | 同上（vite 启动/构建必经 esbuild 子进程） |
| `measure.mjs`（21 个改动文件） | ✅ 新增行零违规 | 存量违规均位于未改动行，明细见下 |
| 截图 / VL 评审 | ⛔ 未执行 | 沙箱拦 vite 子进程，dev server/构建均无法启动；如实记录，不静默跳过 |

## 改动总览（23 个文件，+1824 / −832）

### 重点页：主页设置

- **SettingsProfileTab.vue**：el-card + 顶部 label 表单 → 分组卡片（组头朱砂小印点）+ 五行（头像/昵称/身份码/简介/联系QQ），说明在左、控件在右，栅格对齐；保存按钮右对齐。顺手清零头像 hover `transform: scale(1.05)`（克制动效：只动边框/阴影）。
- **SettingsShowcaseTab.vue**：拆成两个分组卡片——「主页展示」（小店展示/公告/外链/灵感标签四行 + 保存）与「须知编辑」（说明左、textarea 右 + 预览 + 保存）。公告日期选择器收进控制列副行。
- **SettingsTemplateTab.vue**：el-card → 分组卡片 + 五行（页面布局/页面配色/强调色/封面图 + 预览保存操作行）。新增 `templates.layoutDesc` 拆分长 hint 的职责。
- **Settings.vue**：Tab 壳页，本身无控制行，未动（保持 tab 切换与加载失败横幅契约）。
- **ShopVisibilitySwitch.vue**：子组件自身已是「说明 + 开关」行式布局，且测试锁定其提示文本（ShopVisibilitySwitch.test.ts），未动。

### 账号安全 / 开稿 / 作品 / 留言

- **AccountSecurity.vue**：三张卡片改为三个分组卡片；账号信息、动态口令、Passkey 各一行一事（说明左、状态/操作右）；重绑流程与凭据表保留在行下原交互。
- **SlotManage.vue**：四个 el-card → 四个分组卡片；接稿状态/正式名额/缓冲名额/月度额度各一行，队列行为四个开关各自一行（说明左、开关右）；保存按钮右对齐。
- **ArtworkManage.vue**：工具条 → 分组卡片一行（批量管理说明左、按钮右）；上传区 → 分组卡片一行（上传说明左、拖拽区右）；作品网格/分页/批量删除栏本体未动。
- **GuestbookManage.vue**：筛选区 → 分组卡片一行（状态筛选 + 语言筛选控件右置）；留言列表与审核操作未动。

### 统计与导出页

- **ReturningClients.vue**：天数筛选 → 分组卡片一行（统计区间说明左、radio 右）；表格未动。
- **StandaloneIncome.vue**：记一笔表单 el-form 顶部 label → 四行（金额/日期/客户/备注，说明左控件右），校验规则与 `formRef` 契约不变；列表未动。
- **ToolsExport.vue**：导出区 → 分组卡片两行（时间段 / 导出按钮），收入概览与提示未动。
- **StatsPage.vue**：纯展示页，无控制区，**无需改动**。

### 工具页控制区（先核实职能，仅拉齐控制区）

- **DeadlineAdvice.vue**：计算参数收进分组卡片两行（工期天数 / 按队列顺延）+ 操作行。顺手修正 `queueHint` 插值：原传 `BUFFER_DAYS_PER_ORDER`（恒为 1），现传真实 `queueCount`，文案「当前正式队列 N 单」不再误导。
- **DeliveryChecklist.vue**：添加自查项 → 行结构（说明左、输入+按钮右）；清单本体未动。
- **DepositLedger.vue**：记一笔表单网格 → 四行（单名/金额/状态/日期，说明左控件右）+ 操作行；台账明细未动。
- **FoodMenu.vue**：模式选择与随机推荐收进分组卡片两行；顺手清零模式按钮 hover 缩放与 `gap 10px`（克制动效 + 4px 栅格）。
- **PuzzlePage.vue**：四个步骤全部改为行结构（选订单/勾图/排序/预览导出，说明左控件右）；拼图 canvas 与导出逻辑未动。
- **ScheduleSharePage.vue**：导出操作 → 行结构（说明左、复制/下载按钮右）；预览卡未动。
- **PriceCard.vue**：编辑区四行（标题/档位/联系方式/例图，说明左控件右）+ 操作行；档位行编辑、canvas 预览与导出逻辑未动。

### 看板与订单列表工具条（本体交互不动）

- **QueueBoard.vue**：视图切换 → 分组卡片一行（说明左、三视图滑块右）。
- **QueueBoardList.vue**：焦点图显示工具条 → 行结构（说明左、滑块右）；看板卡/拖拽/交付等本体未动。
- **QueueBoardCalendar.vue**：时间条工具栏 → 行结构（时间范围说明左、缩放 radio + 今天右）；月历/时间条本体未动。
- **OrderList.vue**：搜索 + 状态筛选 → 分组卡片两行（说明左、控件右）；进度条/表格/分页未动。

## i18n

新增/调整约 46 键（zh/en 成对）：settings、slots、account、artworks、guestbookManage、returning、standaloneIncome、toolsExport、deadlineAdvice、foodMenu、puzzle、schedule、orderList、priceCard、deliveryChecklist、deposit、queue、templates。文案均为功能性陈述。顺手修正 `account.passkeyEmpty` 按钮方位描述（上方 → 右侧，与行结构一致）。

## measure 明细

`measure.mjs --files <21 个改动文件>`：圆角族 2 族（token角/圆），**新增行零违规**，需人工核验 0。

存量违规（均在未改动行，行外不动）：

- 野生字面圆角 9 处：ArtworkManage 主图标签/批量栏 999px×2、GuestbookManage 语言徽标 999px、OrderList 移动卡 10px/6px、QueueBoardList 胶囊 999px、QueueBoardCalendar 看板本体 4px×3/3px/1px、FoodMenu 结果卡 999px×2、ScheduleSharePage 状态胶囊 14px、ReturningClients/ToolsExport/PuzzlePage/DeadlineAdvice 的 `var(--r-*, 6/8px)` 兜底字面。
- 离栅值 10 处：全部位于看板本体、列表行、收入概览、结果卡、移动端卡片等未改动展示区（2/3/5/6/9/10/14/18/22/1px）。

## 截图 / VL 说明

尝试按 huiyue-layout-audit 流程准备截图基线：vite 构建/dev 均在 `child_process.spawn` 处被沙箱以 EPERM 拦截（esbuild 服务无法启动），E2E 自建服务器亦依赖 `web/dist` 构建产物，故改前/改后截图与 VL 评审均无法产出。已如实记录，非静默跳过；视觉终验由一号主仓态复验 + 用户公网复验兜底。

## 测试选择器回归

改结构前已核对相关测试，保留全部依赖选择器：PriceCard（`.pc-tier/.pc-btn--ghost/.pc-mini-btn/#pc-title`）、DeliveryChecklist（`.cl-item/.cl-checkbox/.cl-input/.cl-btn/.cl-mini-btn/.cl-done`）、DepositLedger（`.dp-row/.dp-stat-value/.dp-switch/.dp-mini-btn/.dp-empty/button.dp-btn`）、OrderList（`.fetch-progress/input`）、GuestbookManage（radio mock 契约）、QueueBoardCalendar / ArtworkManage / QueueBoard / Settings.dirty（均走组件 vm/事件，不依赖 DOM class）。接口契约、props/emits 均未变更。

## 遗留与交接

- 由一号在主仓态补跑 `web npm run test:web` 与 `build`（本沙箱 spawn EPERM 拦截）。
- 工作区改动 23 文件 + 本报告，未 commit（按任务书留工作区由一号收口）。
