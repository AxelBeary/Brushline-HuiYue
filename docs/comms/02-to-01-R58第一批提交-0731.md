# 二号 → 一号：R58 第一批提交审核（R58-1 + R58-6）

> 日期：2026-07-31

---

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/v016-r58-order-page`（worktree: `artist-commission-wt02`）

【任务编号】
R58-1：表单逻辑核心 composable 抽取 / R58-6：QQ 跳转 + 复制

【修改模块】
客户约稿页 / 共享 composable / 画师订单详情页 / i18n

【修改内容】

**Commit 1** `e8935a6` — `refactor(client): R58-1 约稿表单逻辑抽取 useOrderForm composable`

1. 新建 `web/src/composables/useOrderForm.js`（415 行），封装约稿表单全部业务逻辑：
   - 数据加载：画师资料/档位/须知/流程/计价数据（增项+倍率）
   - 档位选择 + 实时计价（300ms 防抖调 calculate-price）
   - 表单校验规则（档位必填/QQ 必填/须知同意必勾）
   - 参考图上传（文件选择 + Ctrl+V 粘贴）
   - 订单提交（API + 错误处理 + loading + 成功弹窗状态）
   - R57 表单防丢失（sessionStorage 草稿 + beforeunload 拦截 + 恢复询问）
2. 顺带消除一处重复逻辑：计价与提交两处各有一份"构建已选增项列表"循环，合并为 `buildSelectedAddons()` 单一来源
3. OrderForm.vue 从 658 行降至 ~300 行，script 只剩 composable 调用——页面纯布局化，模板与样式零改动，行为不变
4. 为 R58-2 分步引导 + 下单页多模板铺路：三步表单和未来模板壳都共享此逻辑核心

**Commit 2** `2076298` — `feat(client): R58-6 QQ 跳转 + 复制`

1. 客户侧：提交成功弹窗显示画师 QQ（`artist.contactQq`，后端已返回），"跳转QQ"（`tencent://message/?uin=`）+ "复制QQ"（`navigator.clipboard`）两个按钮，"查看进度"保留为 primary
2. 画师侧：订单详情客户 QQ 行加"跳转QQ"+"复制QQ"文字按钮
3. 复制成功 toast 提示；剪贴板不可用时降级为 warning 直接展示 QQ 号供手动复制
4. i18n 新增 7 键 × 2 语言（orderForm: artistQqLabel/jumpQq/copyQq/qqCopied；orderDetail: jumpQq/copyQq/qqCopied）

【涉及文件】
- `web/src/composables/useOrderForm.js`（新建，415 行）
- `web/src/views/client/OrderForm.vue`（script 重构 + R58-6 成功弹窗 QQ 区）
- `web/src/views/artist/OrderDetail.vue`（R58-6 客户 QQ 跳转+复制）
- `web/src/locales/zh-CN.js`（+7 键，仅追加）
- `web/src/locales/en.js`（+7 键，仅追加）

【是否修改非客户前端文件】
`OrderDetail.vue` 属画师后台页面，但派工文件 R58-6 明确授权。locales 仅追加键。

【接口依赖】
无新接口。`artist.contactQq` 已由 `GET /api/artists/:subdomain` 返回（artist.routes.js:58，`contact_qq || qq_number` 回退逻辑）。

【自测情况】

| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告（3 次：R58-1 后/R58-6 后/最终） |
| Vite build | ✅ 通过（3 次） |
| console.log / TODO / v-html | ✅ 新增行零残留 |
| i18n 中英同步 | ✅ 7 键 × 2 语言，search_files 逐键确认 |
| 改动文件范围 | ✅ 5 文件均在授权内 |
| Rebase master | ✅ 基于 ba48a89 |
| 结构验证 | ✅ 12 项内联检查全 PASS（composable 导出/计价/提交/草稿/页面解耦/tencent 协议/clipboard/i18n 键） |

**需人工验证项**（无浏览器环境）：
- 约稿全流程回归：选档位→计价展开→增项/倍率→上传参考图→粘贴上传→草稿恢复→提交→成功弹窗（R58-1 是纯重构，行为应与之前完全一致）
- 成功弹窗画师 QQ 展示 + 跳转 QQ 是否唤起 QQ（桌面端 tencent:// 协议）
- 复制 QQ 后 toast + 粘贴板内容
- 订单详情客户 QQ 按钮在 el-descriptions 内的排版
- 画师未设 contactQq 时成功弹窗不显示 QQ 区（v-if 兜底）

【可能影响】
- R58-1 是纯重构，理论上零行为变化；但约稿页是核心下单路径，建议合并前人工走一遍全流程
- R58-6 画师侧改动仅订单详情 QQ 行，不影响其他功能

【待确认问题】
- 无

【申请】
申请一号审核合并。
