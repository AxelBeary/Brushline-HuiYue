# 04-派工-repowiki外部13处修补-20260807.md

> 派工：一号 → 四号（requirements）
> 路线：**B（仓库内维护，四号建议、一号采纳）**——13 处 🟡 按「修补批」在 docs/external-wiki/ 维护修订版（与 P0 三篇同模式）；外部原文（C:\Users\qly19\Desktop\repowiki\）一字不动。
> 蓝本：`docs/comms/04-to-01-repowiki-P0重写-交付-20260807.md` 第三节 13 处汇总表（已逐条核实，行号引用以 master 代码为准）
> 基线：master `8e35a19`。**开工第一步 `git merge master` 再干活**。
> 门禁：无代码测试；文档自检 = 每处修订后对照 master 代码 grep 验证（引用文件存在性 Test-Path + 关键符号 Select-String）

## 授权文件（只许动这些）

- `docs/external-wiki/` 下新建/修订的 13 篇修订版（命名与 P0 三篇同模式：`<原文档名>-修订版.md` 或原目录下同名修订，先看 P0 三篇的命名惯例）
- `docs/external-wiki/README.md`（若已有目录清单，追加新修订版条目）
- 交付报告 `docs/comms/04-to-01-repowiki外部13处修补-交付-20260807.md`

## 红线

- **不碰** `C:\Users\qly19\Desktop\repowiki\`（外部原文一字不动）
- 不碰 `docs/external-wiki/` 下 P0 三篇已修订版（本批 13 处与之互补，不重复改）
- 修订内容**逐条以 master 代码为准**，禁止凭记忆/旧文档写——每处引用文件先 Test-Path + 读源码确认
- 不引入虚构变量/虚构端点（本批第 13 项就是要删虚构变量，别边删边造）

---

## 修订清单（13 处，逐条）

> 以下「当前正确版本」列直接抄自核实报告第三节，均为 master 代码实测结论。修订版正文按此写，引用处补代码证据。

### 认证系 6 处

| # | 文档（外部） | 过时段落 | 当前正确版本 | 修订动作 |
|---|---|---|---|---|
| 1 | `API 接口文档\画师管理接口.md` | cite 与正文引用 `server/src/app.js`、`index.js`（约 5 处，含 mermaid 图） | `app.ts` / `index.ts`（100% TS，.js 不存在） | 全局替换旧文件名 |
| 2 | `后端开发指南\认证授权系统\会话管理.md` | 图例 `POST /login (用户名/密码/TOTP)`；正文「暴露登录、登出、刷新等接口」 | 唯一登录端点 `POST /api/auth/verify {qqNumber, code}`（TOTP）；**无会话刷新接口** | 改图例 + 删「刷新」字样 |
| 3 | `架构设计\前后端通信协议\认证协议.md` | 图例 `POST /login` + `POST /verify-totp`；正文「自动续期策略」 | 同上；会话无自动续期，过期需重新 verify 登录 | 改图例 + 删自动续期描述 |
| 4 | `项目概述\核心功能特性\认证授权系统.md` | 「登录视图：处理用户名/密码与TOTP输入交互」 | 登录页仅 QQ 号 + 6 位 TOTP 码 | 删旧描述一句 |
| 5 | `架构设计\安全架构.md` | 「TOTP 双因素、JWT/HMAC Token」「上传与静态资源由 app.js 统一处理」 | 无 JWT，会话为 HMAC-SHA256 签名 token（auth.service.ts）；`app.ts` | 删 JWT + 文件名替换 |
| 6 | `故障排除\错误码参考.md` | 「刷新 Token」字样 | 无刷新接口；登出 = bumpTokenVersion + clearCookie | 删/改措辞 |

### 非认证 7 处

| # | 文档（外部） | 过时段落 | 当前正确版本 | 修订动作 |
|---|---|---|---|---|
| 7 | `部署运维\监控告警系统.md` | cite 引用 `server/src/app.js`（5 处） | `app.ts` | 全局替换旧文件名 |
| 8 | `数据库设计\数据迁移管理.md` | cite 引用 `connection.js`；演进列表止于 v43 | `connection.ts`；补 **v44**（events+anon_tokens 埋点）+ **v45**（init.js MIGRATIONS 实为 v45） | 改文件名 + 补迁移版本（v44/v45 内容以 `server/src/db/init.js` MIGRATIONS 数组为准，读源码确认后写） |
| 9 | `数据库设计\数据访问模式.md` | cite 引用 index.js/app.js/connection.js/seed.js（4 处） | index.ts/app.ts/connection.ts/seed.ts（init.js 仍为 .js） | 全局替换旧文件名 |
| 10 | `核心功能模块\仪表盘与分析.md` | 「实时数据更新机制：WebSocket」及故障排查「确认 WebSocket 连接状态」 | **无 WebSocket**（全库零命中）；数据更新为 REST 拉取；且未覆盖埋点看板（v44 tracking） | 删 WebSocket + 补埋点章节（⚪ 缺失项，用核实报告第 11 项「埋点看板全链路联调」的事实补） |
| 11 | `项目概述\项目结构说明.md` | cite 引用 index.js/app.js/connection.js（3 处） | .ts | 全局替换旧文件名 |
| 12 | `故障排除\日志分析.md` | cite 引用 `app.js`；「根据 SENTRY_DSN 动态拼接 connect-src」 | `app.ts`；app.ts:145 实为 `SENTRY_DSN_BACKEND \|\| SENTRY_DSN` | 改文件名 + 变量名 |
| 13 | `knowledge\zh\环境变量与运行时配置系统.md` | BOT_ENABLED/BOT_WS_URL（REQ-028 未实施）、LOGIN_CODE_TTL/LOGIN_CODE_MAX_ATTEMPTS（v41 已移除）；「SESSION_SECRET（JWT/…）」措辞 | 均不存在；.env.example 为权威清单 | 删虚构变量 + 措辞（先读 `.env.example` 列真实变量清单，再写「权威清单」表述） |

---

## 验证门禁（交付前必做）

1. 每处修订的引用文件 Test-Path 确认存在（如 `server/src/app.ts` 真存在，`app.js` 真不存在）
2. 关键符号 Select-String 抽查（`SENTRY_DSN_BACKEND`、`POST /api/auth/verify`、`bumpTokenVersion` 等）
3. 修订版文件在 `docs/external-wiki/` 真实落盘（写文件后 Test-Path 确认，防"幻觉完成"）
4. 交付报告 `docs/comms/04-to-01-repowiki外部13处修补-交付-20260807.md`：13 处逐条 ✅/⏭️ + 每处引用证据 + 存疑标注

## 交付形态

- commit 到分支 `beta/wiki-patch`（worktree：`../artist-commission-w4` 需一号先建——若未建，用主仓 docs/ 直接改 + commit 到新分支由一号收）
- 交付报告抬头写「四号转交一号，文件：docs/comms/04-to-01-repowiki外部13处修补-交付-20260807.md」
- 若工具调用接近上限：先 commit 已完成部分再报告
