# 三号→一号：v0.24-A 档位三态后端完成（2026-08-01）

## 做了什么

REQ-013 #10 档位三态后端，6 项全部完成：

1. **迁移 v25**：`price_tiers ADD COLUMN visibility TEXT DEFAULT 'visible'`（幂等，PRAGMA 检测）
2. **公开 API 过滤**：`/api/artists/:subdomain` + `/api/public/pricing/:subdomain` 过滤 `hidden`，保留 `showcase`
3. **画师端 API**：`GET /api/artist/tiers` 返回全部档位（含 `visibility` 字段）
4. **三态切换**：`PUT /api/artist/tiers/:id/visibility`，JSON Schema 枚举校验（visible/showcase/hidden），归属校验
5. **下单校验**：`POST /api/orders` 中 tierId 非空时校验 visibility，showcase/hidden → 400 `TIER_NOT_AVAILABLE`
6. **错误码**：`TIER_NOT_AVAILABLE`（"该档位暂不接受下单"）

## 改了哪些文件

| 文件 | 变更 |
|------|------|
| `server/src/db/init.js` | 迁移 v25 |
| `server/src/shared/errors.ts` | +TIER_NOT_AVAILABLE 错误码+消息 |
| `server/src/features/artist/artist.routes.ts` | 公开主页过滤 hidden + PUT visibility 端点 |
| `server/src/features/artist/artist.service.ts` | updateTier allowed 加 visibility |
| `server/src/features/pricing/pricing.service.ts` | getPublicPricing 过滤 hidden |
| `server/src/features/order/order.routes.ts` | 下单 visibility 校验 |
| `server/tests/tier-visibility.test.js` | 新建，12 用例（TC-TV-01~12） |

## 分支

`feat/v024-tier-visibility`，worktree `../artist-commission-v024`，commit `16a7446`

## 验证结果

- 全量测试 **501/501 通过**（29 文件），含新增 TC-TV-01~12
- 迁移 v25 幂等性已验证（TC-TV-02）
- 无接口破坏性变更（纯新增字段+新增端点）

## 二号前端契约

| 端点 | 说明 |
|------|------|
| `GET /api/artist/tiers` | 每条含 `visibility: 'visible'\|'showcase'\|'hidden'` |
| `PUT /api/artist/tiers/:id/visibility` | body: `{ visibility }` → 返回完整档位对象 |
| `GET /api/artists/:subdomain` | tiers 不含 hidden，showcase 正常返回 |
| `POST /api/orders` | showcase/hidden → 400 `{ code: 'TIER_NOT_AVAILABLE' }` |
