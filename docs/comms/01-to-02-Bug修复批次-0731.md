# 一号 → 二号：Bug 修复批次（插入 R58 第二批之前）
> 日期：2026-07-31
> 分支：`feat/v016-r58-batch2`（同一个分支，先修 Bug 再做 R58）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt02`
> 研判报告：`docs/comms/05-to-01-bug研判-0730.md`

## 优先级：先修完 Bug 再做 R58 第二批

### BUG-4（🔴 高）：比例条右拖不回弹+报错

`web/src/components/artist/PaymentBar.vue:121` 右拖弹性条件 `< ELASTIC_THRESHOLD(150)` 应为 `< MIN_BP(500)`，150~499 区间漏处理。

修法（五号已给）：
```js
else if (newRight < MIN_BP && !rightIsFinal) {
    elasticId.value = rightId
    detachId.value = newRight < ELASTIC_THRESHOLD ? rightId : null
    newRight = Math.max(0, newRight)
    newLeft = startLeftBp + startRightBp - newRight
}
```

### BUG-1（🟡 中）：排期看板焦点图闪烁

`web/src/views/artist/QueueBoard.vue` + `web/src/composables/useSignatureRefresh.js`

1. `.focus-large-img` / `.focus-img-wrap` 加 `background: var(--bg-card)` 防白闪
2. `refreshNow` 加 300ms 防抖
3. 加最大重试计数（2 次），超限不再刷新

### BUG-2（🟢 低）：非图片拖到加号无反馈

`web/src/views/artist/OrderDetail.vue` + `QueueBoard.vue`

`handleGalleryDrop` / `handleFocusDrop`：过滤后 files.length === 0 且原始有文件时，`ElMessage.warning(t('...notImage'))`。i18n 加键。

### BUG-3 前端部分（等三号错误码合入后跟进）

`uploadAndAttachReference` 捕获 `REFERENCE_DUPLICATE` 错误码，提示"该图片已在图库中"。**此项等三号后端合入后再做，不阻塞本批次。**

## 授权文件

`web/src/components/artist/PaymentBar.vue`、`web/src/views/artist/QueueBoard.vue`、`web/src/views/artist/OrderDetail.vue`、`web/src/composables/useSignatureRefresh.js`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`（仅追加）

## 纪律

- Bug 修复和 R58 分批 commit，不混在一起
- commit 格式：`fix(client): BUG-x 描述`
- 修完 Bug 后继续做 R58 第二批（01-to-02-R58第二批-0731.md）

## 完成状态

（二号完成后填写）
