# 817-D 客户端域批（web）交付报告

> 分支：`feat/817-client`（基于 master 67a372ab）｜Worktree：`D:\Hermes Agent CN Desktop\workspace\817-d`
> 沙箱说明：git commit 被拦（worktree 索引位于主仓 `.git/worktrees/817-d`，Permission denied），按任务书不再重试，**全部改动留在工作区**由一号收口提交；`npm run test:web` / `npm run build` 同样被沙箱拦（esbuild spawn EPERM，见文末门禁）。

## 逐条裁决（12/12）

### 1. 7-2 TrackOrder 已保存清单连点串单 —— 已修销账
- 核实：当前 `TrackOrder.vue` 已含 K1-1（提交 40e6ed38）两重防护：已保存清单按钮 `:disabled="searching"`；`search()` 内 `searchSeq` 序号守卫（晚到旧响应丢弃，`finally` 只清最新序号）；`querySaved` 对 null 竞态不误标失效。
- 未改代码。

### 2. 7-3 OrderDetail 确认取消双入口无防重锁 —— 已修销账
- 核实：确认/推进走 `statusAction` 共享锁（`changeStatus` 首行 `if (statusAction.value) return`，按钮 `:disabled` 联动）；取消双入口（滑块 + 键盘替代按钮）统一走 `confirmCancelOrder`，`cancelSubmitting` 锁 + 滑块 start/move/end 三处 handler 守卫 + 按钮 `:disabled="cancelSubmitting"`。与 v113 销账口径一致。
- 未改代码（QueueBoardList 侧未动，A 路领地）。

### 3. 7-4 重新生成追踪链接剪贴板失败误报整体失败 —— 已修销账
- 核实：K1-2 已改 `regenerateAndCopyLink`：令牌重生成成功即成功，剪贴板失败单独弹 `regenerateTokenManual*` 手动复制提示，不再回滚/重复作废令牌。
- 未改代码。

### 4. 10-1 埋点重试把网络失败哨兵当 token 发出 —— 已修销账
- 核实：K1-7 已改 `track.js` 400 重试分支为 `newToken && newToken !== TOKEN_NETWORK_FAIL` 才重发 `/api/events`，不再把 Symbol 交给 `JSON.stringify`。
- 未改代码。

### 5. 10-2 同页三处「合计/折扣」行序与口径 —— 真修
- 核实后端：`server/src/features/pricing/style-pricing.service.ts` 的 `applyMultipliersAndDiscount` 返回 `totalCents = afterMultipliersCents - discountCents`，即 **totalCents 是折后价**。
- 现状：AddonStep 把「合计」行放在「折扣」行之前（假折前顺序）；OrderSummaryCard、下单小票弹窗均为折扣行→合计行，口径一致。
- 修法：`AddonStep.vue` 合计行移到折扣行之后，仍展示 `preview.totalCents`（折后）；无接口契约/i18n 变更。
- 测试：新增 `web/src/views/client/__tests__/AddonStep.priceorder.test.js`（有折扣行序 + 无折扣合计 2 例）。

### 6. 10-3 AddonStep 用途/加急 chip 硬编码 `+{{a.price}}%` —— 已修销账
- 核实：K1 已把用途/加急 chip 改为 `{{ priceText(a) }}`，普通增项同源；`styleAddonPriceText` 走 `formatAddonPrice(a.price, a.price_mode, ...)` 单一来源（money.js 未改）。
- 未改代码。

### 7. 10-4 pagehide 只 sendBeacon 前 50 条 + 注释失实 —— 真修
- 修法：`track.js` 新增 `buildBeaconBodies(events, token)`（每片 ≤ `SEND_BATCH_MAX` 50 条且 ≤ 60KB，单条超限仍整条尽力发出），pagehide 改为分片带走**全部积压**（不再只发前 50 条、关页即丢 150 条）；注释同步更新。
- 测试：新增 `web/src/utils/__tests__/track.pagehide.test.js`（pagehide 分片 2 例 + 纯函数契约 1 例）；另用 Node 直测 55/120/1/超限单条 4/4 通过。

### 8. 10-6 举报表单 targetId 前端数值校验 —— 已修销账
- 核实：K1-10 已加 `targetId` validator（`/^\d+$/` + `Number.isSafeInteger` + `>0`），拒负数/小数/科学计数法；zh/en 双键 `targetIdInvalid` 在位。
- 未改代码。

### 9. 7-7 types.ts 漏标 focus_image_path —— 真修
- 修法：`web/src/api/types.ts` 的 `ArtistOrderItem`（列表/队列行类型）补 `focus_image_path?: string | null`（后端 orders 表列 nullable；前端 OrderList/QueueBoard 直接读取）。`OrderDetail` 原有该字段，未重复。
- 验证：`vue-tsc --noEmit` 零错误。

### 10. 7-8 参考图上限「5」三处硬编码 —— 已修销账
- 核实：K1 已抽 `web/src/constants/upload.js` 的 `MAX_IMAGE_COUNT = 5` 为单源；`DetailStep.vue`/`ManualOrderLeft.vue`/`useOrderForm.js`/`usePasteUpload.js` 等全部引用常量。`rg` 复核无残留 `:limit="5"` / `>= 5` 硬编码。
- 未改代码。

### 11. 体验：参考图上传模块移到需求描述板块下面 —— 现状已满足，销账
- 核实：客户端下单页 `DetailStep.vue` 结构已是「需求描述 → 参考图上传 → 流程/收款预览」，模块换位在结构债清偿批（e11e044d）已落地，样式与验证逻辑未受影响。
- 备注：若该反馈指画师端手动录单 `ManualOrderLeft.vue`（参考图目前仍在描述上方），该文件属画师端视图（A/C 路领地），本批红线不动，建议转交对应路。

### 12. 体验：下单粘贴解析钳制非法日期 —— 真修
- 入口核实：`web/src/utils/message-parser.js`（REQ-035 粘贴消息解析，ManualOrder 调用），`DEADLINE_DATE_RE = /(\d{1,2})\s*月\s*(\d{1,2})\s*日/g` 会把「22月31日」当线索。
- 修法：`collectDeadline` 增加合法性过滤——月 1-12、日按该月上限（2 月按 29 天宽松，因文本无年份不可判闰年），非法命中跳过继续找后续合法日期；「x号前」同步加日 1-31 校验。沿用「解析不出不猜」原则，不做假钳制。
- 测试：`message-parser.test.js` 新增 6 例（22月31日/13月1日/2月30日/4月31日/2月29日宽容/非法在前取后续合法/32号前）；Node 直测 9/9 通过。

## 改动文件

| 文件 | 类型 |
| --- | --- |
| `web/src/views/client/order-form/AddonStep.vue` | 修改（10-2 行序） |
| `web/src/views/client/__tests__/AddonStep.priceorder.test.js` | 新增 |
| `web/src/api/types.ts` | 修改（7-7 补字段） |
| `web/src/utils/track.js` | 修改（10-4 分片） |
| `web/src/utils/__tests__/track.pagehide.test.js` | 新增 |
| `web/src/utils/message-parser.js` | 修改（体验12 日期校验） |
| `web/src/utils/__tests__/message-parser.test.js` | 修改（体验12 回归） |

## 门禁实测

```text
> npm run typecheck（web）
> vue-tsc --noEmit
（零错误，退出码 0）

> node node_modules/eslint/bin/eslint.js .
（0 error / 0 warning，退出码 0）

> npm run check:i18n
[check-i18n] OK — 存量违规 13 条豁免，无新增硬编码中文

> npm run test:web
（沙箱拦截，非代码失败）vitest 加载配置时 esbuild 服务 spawn 报 EPERM：
Error: spawn EPERM at ensureServiceIsRunning (esbuild/lib/main.js:1978)
复现：主仓 artist-commission/web 下同一命令同样失败 → 环境对 node 管道型子进程 spawn 的拦截，
与 junction/worktree 无关。

> npm run build
（沙箱拦截，同因）vite build 在 bundleConfigFile 阶段同样 spawn EPERM。
```

替代验证（可执行部分）：
- typecheck / eslint / check:i18n 全绿（上述）。
- 纯 Node 直测 message-parser 非法日期 9/9 PASS、track buildBeaconBodies 4/4 PASS。
- `test:web` / `build` 需在非沙箱环境（常规通道）补跑后方可视为完整门禁；改动均不涉新增依赖、不涉 i18n 新键、不涉接口契约，风险面收敛在前端纯函数/模板顺序。
