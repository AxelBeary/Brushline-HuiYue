# 二号派工：封面模板预读（2026-08-02）

> 不建分支，不写代码。产出写入交付 comms。
> 交付 comms：`02-to-01-封面模板预读-0802.md`（在主 worktree 写即可）

---

## 任务：4 模板封面区预读报告

**背景**：四号正在写封面重设计 REQ（#9），核心约束是"不同模板对封面的处理逻辑不同"。你作为客户端前端负责人，先读现有代码产出预读报告，为后续实施做准备。

**读什么**：
1. 4 个模板（ArtistHomeClassic/Folio/Gallery/Atelier）的 Hero/门面区域代码——当前结构是什么？有没有封面渲染逻辑？
2. `useArtistData.js` 的 `coverArtworks` computed——数据怎么来的？
3. `ArtistHome.vue` 传给模板的 props——coverArtworks 是否已传入？
4. 当前 Settings.vue 的封面管理区——星标操作调的什么 API？

**产出格式**：

```
## 现状
- 模板 X 的 Hero 区域：[结构描述，行号]
- coverArtworks 数据流：[从哪来，传到哪]
- 封面管理 UI：[在 Settings 哪里，调什么 API]

## 4 模板封面适配方案（初步）
| 模板 | 门面区域现状 | 封面展示方式 | 改动量 |
|------|-------------|-------------|--------|
| Classic | ... | 单图 Hero | ... |
| Gallery | ... | 轮播 | ... |
| Folio | ... | 大图首图 | ... |
| Atelier | ... | 拼图入口 | ... |

## 去重逻辑
封面作品从瀑布流排除的实现位置：[哪个组件，怎么过滤]

## 风险/注意
- ...
```

**约束**：只读不改。产出写入 comms 文件，一号转交四号作为 REQ 参考。
