# 04-to-01 repowiki 非认证主题交付：部署/CSP/Sentry/前端/数据库抽样核对报告

- 派工：`docs/comms/04-派工-repowiki非认证主题-20260807.md`（一号 → 四号，P2 改派）
- 核对日期：2026-08-07
- 核对基准：master HEAD（当前 `240a302`，含埋点看板 tracking/events/anon_tokens、OrderDetail 拆分、STATUS v20）；本机复核迁移 v44、app.ts SENTRY/CSP、theme.css natural-v2 五色均与派工文件一致
- 核对人：四号（需求整理者，纯只读审计：零代码改动、无 worktree、无 git 写操作；本报告不 commit，由一号合入）
- 结论口径：🔴 严重过时 / 🟡 轻微 / 🟢 正确 / ⚪ 缺失（=代码已有、文档未覆盖）
- 说明：外部贡献文档（C:\Users\qly19\Desktop\repowiki\）原样未改；判定以 master 代码为准，不信文档自我描述；引用的「代码实际」均经 grep/Test-Path 实测

---

## 〇、最重要结论

**本轮抽样 16 篇（content 区 13 篇 + knowledge 区 3 篇），🔴 3 篇 / 🟡 7 篇 / 🟢 6 篇，另 ⚪ 缺失 2 项。**

与上轮（认证系 0 🔴）不同，非认证主题存在 3 篇严重过时，根因高度集中：

1. **TS 迁移后的文件引用名漂移**（`.js` → `.ts`）：server/src 下 app.ts/index.ts/connection.ts/seed.ts 已改名（init.js 仍是 .js，前端 main.js 仍是 .js 未 TS 化）。11 篇文档中 8 篇 cite/正文仍引用旧 `.js` 名——这是贯穿性 🟡 条目，单篇修 1-3 处即可。
2. **虚构环境变量**：`SIGN_SECRET`（3 篇）、`JWT_SECRET`、`RATE_LIMIT_WINDOW_MS`/`RATE_LIMIT_MAX_REQUESTS`、`BOT_ENABLED`/`BOT_WS_URL`、`LOGIN_CODE_TTL`/`LOGIN_CODE_MAX_ATTEMPTS`——**全库 grep 零命中**。其中 SIGN_SECRET 是部署文档「必须修改清单」的核心项，误导性最强。
3. **部署命令指向不存在文件**：快速开始指南的 entrypoint（index.js）与种子命令（seed.js）实际是 index.ts/seed.ts，照做必失败。

另注意：**上轮判 🟢 的 knowledge/zh 环境变量与运行时配置系统.md 本轮发现 4 个不存在变量**（BOT_*/LOGIN_CODE_*），需修正为 🟡——上轮只验证了 SESSION_SECRET/COOKIE_SECRET/ADMIN_QQ/AUTH_DEV_MODE 四项，未扫 BOT/LOGIN_CODE 残留。

---

## 一、核对表（16 篇）

| # | 文档 | 判定 | 依据（对照代码） |
|---|------|------|------|
| 1 | `部署运维\环境配置管理.md` | 🔴 | 生产 DB 写 PostgreSQL/MySQL（实际纯 SQLite）；附录虚构 JWT_SECRET/RATE_LIMIT_WINDOW_MS/RATE_LIMIT_MAX_REQUESTS；SENTRY_DSN 命名（正式名 SENTRY_DSN_BACKEND）；cite 引用 index.js/app.js/connection.js/seed.js 旧名 |
| 2 | `部署运维\监控告警系统.md` | 🟡 | Sentry（SENTRY_DSN_BACKEND/sendDefaultPii=false/tracesSampleRate=0）、健康检查 8 项、auth 流程、滑动窗口限流全部正确；仅 cite 引用 app.js（应 app.ts） |
| 3 | `架构设计\部署架构\生产部署.md` | 🔴 | 「必须修改 SIGN_SECRET」虚构（签名 URL 实为 SESSION_SECRET，file-sign.ts:11）；「不直接映射宿主机端口」与 compose 现状不符（仍 ports 3000:3000，注释要求生产手动注释）；「泛解析」与 Caddyfile 单一 {$DOMAIN} 不符（C48 已放弃子域名）；cite 旧文件名 |
| 4 | `架构设计\部署架构\环境配置.md` | 🟢（附 🟡 引用） | SENTRY_DSN_BACKEND/VITE_SENTRY_DSN、COOKIE_SECRET>SESSION_SECRET 回退链、AUTH_DEV_MODE 仅 .env 控制、DB_PATH/UPLOAD_DIR/TRUST_PROXY/TZ 覆盖点——全部与代码一致；仅 cite 旧文件名 |
| 5 | `部署运维\持续集成与部署.md` | 🟢 | CI 两 job 并行、E2E 生命周期（TOTP 预登录/端口 3999/workers=1）、Dockerfile 多阶段+非 root、引用的 9 个配置文件全部存在；仅未提 ci.yml 中另有 oxlint+typecheck（非错误，属未覆盖） |
| 6 | `架构设计\部署架构\CI_CD 流水线.md` | 🟢 | 与 #5 内容重叠，机制描述一致；「server：Fastify + tsx」正确；无旧文件名引用问题 |
| 7 | `前端开发指南\国际化 (i18n).md` | 🟢 | i18n/index.js、locales/en.js、zh-CN.js 全部存在；detectLocale/setLocale/Element Plus 联动描述正确；懒加载明确标注为「建议」=未实施，与 main.js 静态 import 现状一致（诚实） |
| 8 | `数据库设计\数据迁移管理.md` | 🟡 | MIGRATIONS/schema_migrations/noTransaction/幂等守卫/.bak.vN/v38/v40/v43 描述全部正确；**演进列表停在 v43，缺 v44（events+anon_tokens 埋点）** ⚪；cite 引用 connection.js（应 connection.ts） |
| 9 | `数据库设计\数据访问模式.md` | 🟡 | better-sqlite3 单连接+短事务、WAL/DELETE 描述正确；大量概念性内容；cite 引用 index.js/app.js/connection.js/seed.js 旧名 |
| 10 | `核心功能模块\仪表盘与分析.md` | 🟡 + ⚪ | 引用 10 个文件全部真实存在；**「WebSocket 连接与数据同步策略」为虚构**（全库无 WebSocket 引用）；**埋点看板（tracking/events/anon_tokens/TrackingAnalytics.vue，v44 合入）完全未覆盖** ⚪ |
| 11 | `项目概述\快速开始指南.md` | 🔴 | 「tsx 运行 index.js」——实际 index.ts；「.env 填写 SIGN_SECRET」虚构；「node server/src/db/seed.js」——实际 seed.ts，命令会失败；cite 旧文件名 |
| 12 | `项目概述\项目结构说明.md` | 🟡 | 架构/中间件/E2E/部署描述正确；cite 引用 index.js/app.js/connection.js 旧名 |
| 13 | `故障排除\日志分析.md` | 🟡 | Sentry/错误处理器/CSP 描述基本正确；cite 引用 app.js（应 app.ts）；「根据 SENTRY_DSN 动态拼接 connect-src」应为 SENTRY_DSN_BACKEND||SENTRY_DSN（app.ts:145） |
| 14 | `knowledge\zh\环境变量与运行时配置系统.md` | 🟡（上轮 🟢 修正） | SESSION_SECRET/COOKIE_SECRET/ADMIN_QQ/AUTH_DEV_MODE/TRUST_PROXY/TZ/CORS_ORIGIN/SENTRY_DSN_BACKEND/VITE_SENTRY_DSN 全部真实；**BOT_ENABLED/BOT_WS_URL（REQ-028 备案未实施）与 LOGIN_CODE_TTL/LOGIN_CODE_MAX_ATTEMPTS（旧登录码已随 v41 移除）全库零命中**；「SESSION_SECRET（JWT/…签名）」措辞沿用 .env.example 注释 |
| 15 | `knowledge\zh\构建与部署系统（Docker + Vite + Fastify + GitHub Actions）.md` | 🟢 | Vite 6、tsx 混编、多阶段、Caddy 2、CI/E2E 描述与代码一致 |
| 16 | `knowledge\zh\画师约稿管理平台前端样式体系.md` | 🟢 | artist-tokens.css/palettes.css/theme.css、data-accent 1..5、data-palette paper/ink/dusk/moss、data-artist-theme paper|ink、--paper/--ink/--hq/--zs/--sl/--th/--zhe/--buf 全部与代码一致 |

**汇总：🔴 3 篇 / 🟢 6 篇 / 🟡 7 篇；🔴 条目 9 项 / 🟡 条目 13 项 / ⚪ 缺失 2 项。**

---

## 二、🔴 严重过时清单（供一号决策是否重写）

| # | 文档 | 过时段落 | 当前正确版本（代码实测） |
|---|------|----------|--------------------------|
| 1 | 环境配置管理.md | 「生产环境：PostgreSQL 或 MySQL」（数据库连接与初始化一节） | 纯 SQLite（better-sqlite3），connection.ts 仅 SQLite/:memory:，无 PG/MySQL 支持 |
| 2 | 环境配置管理.md | 附录「JWT_SECRET：签名密钥（必须存在）」「RATE_LIMIT_WINDOW_MS / RATE_LIMIT_MAX_REQUESTS」 | 变量不存在（grep 零命中）；密钥类实际为 SESSION_SECRET/COOKIE_SECRET（.env.example 权威清单） |
| 3 | 环境配置管理.md | 「SENTRY_DSN：错误追踪 DSN」 | 正式名 SENTRY_DSN_BACKEND（app.ts:210；CSP 处有 ||SENTRY_DSN 兼容 app.ts:145，但 .env.example 仅列 BACKEND） |
| 4 | 生产部署.md | 「必须修改：DOMAIN、SESSION_SECRET、SIGN_SECRET、COOKIE_SECRET…」「SESSION_SECRET/SIGN_SECRET：未更换会导致会话伪造或链接泄露」 | **SIGN_SECRET 不存在**；文件签名 URL 密钥 = SESSION_SECRET（file-sign.ts:11 getSecret，生产 fail-fast） |
| 5 | 生产部署.md | 「Web 容器暴露 3000 端口供内部网络访问，不直接映射宿主机端口」 | docker-compose.yml 当前仍 `ports: 3000:3000`（注释注明生产需手动注释，仅 expose）；文档写成已成事实 |
| 6 | 生产部署.md | 「自动 HTTPS 与泛解析」 | Caddyfile 仅 `{$DOMAIN} { … }` 单主域；C48 决策已放弃子域名方案（文件内注释明确） |
| 7 | 快速开始指南.md | 「entrypoint.sh 进入 server 目录并通过 tsx 运行 index.js」 | entrypoint.sh 实为 `exec npx tsx src/index.ts`；index.js 不存在 |
| 8 | 快速开始指南.md | 「创建 .env 文件，填写必要变量（DOMAIN、SESSION_SECRET、SIGN_SECRET、COOKIE_SECRET、TZ 等）」 | SIGN_SECRET 不存在；.env.example 无该项 |
| 9 | 快速开始指南.md | 「首次种子数据：docker compose exec web node server/src/db/seed.js」 | seed.js 已改名 seed.ts；照此命令必失败 |

## 三、🟡 轻微过时清单（修补即可）

| # | 文档 | 过时段落 | 当前正确版本 |
|---|------|----------|--------------|
| 1 | 监控告警系统.md | cite/图表引用 `server/src/app.js`（5 处） | app.ts |
| 2 | 数据迁移管理.md | cite 引用 `connection.js`；演进列表止于 v43 | connection.ts；补 v44（events + anon_tokens 埋点，init.js 已含 version 44） |
| 3 | 数据访问模式.md | cite 引用 index.js/app.js/connection.js/seed.js（4 处旧名） | index.ts/app.ts/connection.ts/seed.ts（init.js 仍为 .js） |
| 4 | 仪表盘与分析.md | 「实时数据更新机制：WebSocket」及故障排查「确认 WebSocket 连接状态与重连机制」 | 无 WebSocket（全库 grep 零命中）；数据更新为 REST 拉取 |
| 5 | 项目结构说明.md | cite 引用 index.js/app.js/connection.js（3 处旧名） | .ts |
| 6 | 日志分析.md | cite 引用 `server/src/app.js`；「根据 SENTRY_DSN 动态拼接 connect-src」 | app.ts；app.ts:145 实为 SENTRY_DSN_BACKEND \|\| SENTRY_DSN |
| 7 | knowledge 环境变量文档 | 「BOT_ENABLED、BOT_WS_URL」「LOGIN_CODE_TTL、LOGIN_CODE_MAX_ATTEMPTS」 | 均不存在（BOT=REQ-028 备案未实施；LOGIN_CODE 已随 v41 TOTP 移除）；「SESSION_SECRET（JWT/…）」措辞建议删 JWT |
| 8 | 环境配置.md（🟢 篇内条目） | cite 引用 index.js/app.js/connection.js | .ts |

## 四、⚪ 缺失（代码已有、文档未覆盖）

1. **埋点看板**：`核心功能模块\仪表盘与分析.md` 未覆盖 tracking 模块（server/src/features/tracking/tracking.routes.ts + tracking.service.ts、v44 events/anon_tokens 表、web/src/views/admin/TrackingAnalytics.vue + 画师门面统计区块）。2026-08-07 已合入 master，是「分析」主题下最值得补的内容。
2. **迁移 v44**：`数据库设计\数据迁移管理.md` 演进列表停在 v43。

## 五、建议（交一号决策）

| 优先级 | 动作 | 范围 | 理由 |
|--------|------|------|------|
| P0 | **重写 3 篇** | 快速开始指南.md / 生产部署.md / 环境配置管理.md | 部署命令指向不存在文件、虚构核心密钥变量（SIGN_SECRET/JWT_SECRET）、生产 DB 描述错误——照做会失败或漏配，误导性最高 |
| P1 | **修补 6 篇 + knowledge 1 篇** | 监控告警 / 数据迁移管理 / 数据访问模式 / 仪表盘与分析（删 WebSocket + 补埋点）/ 项目结构说明 / 日志分析 / knowledge 环境变量 | 全部为文件引用名 .js→.ts + 删虚构变量 + 补 v44；1-3 处/篇，可与上轮认证系 6 处 🟡 合并成同一修补批 |
| P2 | **补埋点章节**（可选并入 P1） | 仪表盘与分析.md 或新增 tracking 专篇 | 埋点看板是 v44 新功能，分析主题下空白 |
| 无需动 | 其余 6 篇 🟢 | — | 内容与 master 一致（含 knowledge 构建与部署、前端样式体系） |

**不建议**按 P0 模式重写 🟢/🟡 篇——机制描述正确，重写成本 > 收益。

## 六、存疑标注（暴露不确定性）

1. 16 篇未全文逐句比对，判定基于关键信号扫描（SIGN_SECRET/JWT/PostgreSQL/MySQL/index.js/app.js/seed.js/WebSocket/BOT_ENABLED/LOGIN_CODE/SENTRY_DSN/迁移版本/五色值）+ 与代码关键点对照；若一号需全量逐句核对可再派一轮。
2. `部署运维\持续集成与部署.md` 与 `架构设计\部署架构\CI_CD 流水线.md` 内容高度重叠（外部贡献者重复编写），未逐行比对差异，仅分别对照代码。
3. knowledge/zh 环境变量文档上轮判 🟢 但本轮发现 4 个不存在变量——上轮验证范围有限（4 个变量），本轮已全变量扫描；建议后续核对该文档时以 .env.example 为权威清单。
4. 本报告不 commit；master 仅只读（HEAD 已确认 240a302 代码基线，派工文件所载一致；当前仓库 HEAD f70c998 仅含派工文档提交，不影响核对结论）。

---

*纯只读审计完成：零代码改动、无 worktree、无 git 写操作、未 commit。repowiki 原目录未改一字。*
