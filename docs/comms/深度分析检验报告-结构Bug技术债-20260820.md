# 深度分析检验报告：结构 / Bug / 技术债（只读取证）

> **性质**：只读分析，全程零产品代码修改（唯一写入 = 本报告 + STATUS 落档 + 分析期临时工具，临时脚本已删）。
> **工作区状态**：HEAD `9f202f9c`；未提交变更 12 个文件 = v141 粘贴录单批 + OCR 第二批探索（ocr.ts/ocr.test.ts 新增、package.json、locales、message-parser、ManualOrder.vue、THIRD-PARTY-NOTICES 等，用户拍板方向在做）。
> **取样时间**：2026-08-20 22:0x 一轮扫描；容器 DB 快照同刻。
> **方法**：完全按 `docs/comms/深度分析计划书-结构Bug技术债-20260820.md`——proactive-scan-checklist 十一模式静态扫描、容器内查库（docker exec 只读探针）、基线门禁实跑、三态裁决。

---

## 一、Phase 0 门禁基线实测（当前工作区实跑，非引用旧数）

| 门禁 | 实测结果 | 结论 |
| --- | --- | --- |
| server typecheck（src+scripts+tests 三配置） | EXIT 0 | ✅ |
| server lint | **0 warnings 0 errors**（291 files / 98 rules，133ms） | ✅ |
| server vitest | **Test Files 137 passed / Tests 1608 passed**（368.98s） | ✅ 与已提交基线 1608 一致 |
| web eslint | EXIT 0 | ✅ |
| web lint（eslint + vue-tsc + scripts tsc） | **首跑 1 error**：`src/utils/__tests__/ocr.test.ts(4,30) TS6133: 'OcrError' declared but never read`；**复跑 EXIT 0** | ⚠️→✅ 详见旁注 |
| web test:web | **Test Files 106 passed / Tests 721 passed**（14.30s） | ✅ |
| web check:i18n | OK（存量豁免 13 条，零新增硬编码中文） | ✅ |
| web build | OK 16.07s（main 313.49kB/gzip 112.09 + vendor 210.76/79.26 + Chart.js 独立分包 208.28/71.53） | ✅ |
| E2E | 未在本报告重跑（分析期不起服务），引用既有基线 13/13（v139） | ➖ |

**旁注（如实记录，Phase 2 仲裁口径）**：web lint 首跑抓到的 TS6133 出现在**在途未提交批**正在编辑的文件上；约一小时后复跑 vue-tsc 已绿（ocr.test.ts 第 4 行已变为不含 `OcrError` 的导入——扫描期间该批次被用户继续编辑）。同一现象还有：STATUS v141 登记 `712/712`，实测已 `721/721`（+9，即 OCR 批新增测试）。结论：**在途批门禁数字处于漂移常态，提交时以 accept.ps1 实测为准**；本报告不作产品缺陷定性。

---

## 二、结构类发现（S）

### S-01 巨型文件 Top 榜重建（含在途批，2026-08-20 实测）
**server/src**（排除 tests）：admin.routes 970 / style.service 938 / artist.service 695 / order-pricing 574 / artist.routes 564 / schema 516 / auth.routes 510 / app 464。
**web/src**（排除 locales 与 tests）：**api/types.ts 1506（新晋头名）** / ArtistManage.vue 1040 / QueueBoardCalendar.vue 972 / PriceCard.vue 930 / OrderDetail.vue 925 / ArtistLayout.vue 902 / Login.vue 879 / ArtStyleManager.vue 867 / ManualOrderRight.vue 865 / TplGallery.vue 854 / QueueBoardList.vue 836 / ArtworkManage.vue 814。

**再生长证据（🟡 中风险）**：changelog 05G 三拆基线「OrderDetail 1450→628 / ManualOrderRight 1049→868 / QueueBoardCalendar 1326→756」，现为 925 / 865 / 972——**OrderDetail 回胀 +47%、QueueBoardCalendar 回胀 +29%**，拆分红利正在回吐；ManualOrderRight 基本稳住。api/types.ts 1506 行是类型契约仓（纯 interface，天然膨胀，属「可接受的聚集」但与组件混排仍建议后续专项治理）。

### S-02 any 退化 / ts-ignore / 蒙混（🟢）
- `@ts-ignore` / `@ts-expect-error`：server 0、web 0。
- 裸 `any` / `<any>` / `: any`：**双端零命中**。
- `as unknown as` 断言：web 生产代码 13 处（ArtStyleManager 763、WorkflowPaymentEditor 401、ManualOrder 44/59/265/549、OrderDetail 474/475/480、Settings 263、IncomeTrendCharts 131/132），**全部带注释的局部类型收窄**，非蒙混；另有 12 处在测试夹具（惯用模式）。与 v132 迁移纪律「零 any、缺口一律局部 interface + as 断言」一致。

### S-03 死代码 / 双模型分叉（🟢，零实证死链路行）
- 启发式全仓导出引用计数：候选零引用符号 60 个，抽验 8 个（`listRecycleBin`、`signFilePath`、`feedUrl`、`clearOgCache`、`cleanupExpiredBuckets`、`getPlatformById`、`addDeliverable`、`drawWatermark` 系）→ **全部为「同文件内部调用 / 测试钩子 / 导出常量」**，无一实锤死代码。启发式工具自身的系统误差（自文件引用、命名空间调用）已量化为该方法论上限。
- 已备案死链路一条：后端 `changePercent`（dashboard.service 165-182）→ 前端 `normalizeRevenue` 兜底字段（dashboard-normalize.ts 59）→ **无页面消费**。属 v139 已拍板销账项（「钱不进日报」有决策锚点），不算隐匿债务。

### S-04 模块划分 / 循环依赖（🟢）
- 自写静态图扫描（474 文件，相对导入解析）：**CYCLES = 0**。server / web 各自无环，双端无跨仓相对引用。
- 风格债（🟢 低风险）：web/src 残留 **534 处 `from '.../x.js'` 带后缀导入**（v132 迁移批量遗留，moduleResolution=bundler 下可解析、无害、可机械统一）。计划书附录 A4 预检坐实。

### S-05 迁移链 / Schema 一致性（🟢）
- 容器内实测 `schema_migrations MAX(version)=69`，与 schema.ts 迁移索引、STATUS 口径一致；
- 43 张表全列快照已取证（temp/db-schema-snapshot.txt），无孤儿表/幻影列。

### S-06 审计脚本覆盖缺口（🟡 中风险工具债）
- `audit-i18n-keydiff.mjs`（soul skill 目录）**已腐烂**：import `web/src/locales/zh-CN.js` → 实测 `ERR_MODULE_NOT_FOUND`（v132 后语言文件已 .ts，脚本未跟进）。修复或下架二选一。
- `audit-demo-schema.cjs` 可用（容器内可达真实表），但只覆盖 demo-data.ts 单源——本报告用扩展探针补齐了全部 INSERT 源（见 B-07）。

---

## 三、Bug 类发现（B）

| 编号 | 扫描项 | 取证结果 | 裁定 |
| --- | --- | --- | --- |
| B-01 | `.toFixed()` 空值崩溃 | 全仓 16 处：15 处为除法/文件大小上下文（NaN 不崩）；唯一直调用 money.ts:16 经 `toFiniteNumber` 兜底 | ✅ 安全 |
| B-02 | parseInt 无基数 | server 25+ 处**全部带 `, 10`**；web 零命中 | ✅ 安全 |
| B-03 | 静默空 catch | `catch {}` / `.catch()` 双端**零命中** | ✅ 安全 |
| B-04 | v-html 注入 | 生产仅 1 个渲染入口 `SanitizedRichText.vue`（统一消毒，README 契约 + contract.test 钉住）；调用方（TplRules/SettingsShowcaseTab）入参均已 sanitizeHtml | ✅ 安全 |
| B-05 | SQL 注入面 | `${}` 命中面抽查：app.ts:110 动态列名=元数据来源+转义；order-list 排序/筛选走白名单；结算/下单均占位符。无用户输入直接进 `${}` 证据 | ✅ 安全 |
| B-06 | snake_case 映射断点 | 后端 43 表全 snake 列 + 前端 api/types.ts **186 个 snake 字段声明** + 全仓**零 camelize/snakeize 转换器** = 全链直通自洽，无「后端 snake 前端 camel 漏映射」式 bug 的土壤 | ✅ 安全 |
| B-07 | seed/demo INSERT 漏列 | 容器内对 8 张关键表 NOT NULL 约束实测 vs demo-data.ts（10 处 INSERT）+ db/seed.ts（2 处）逐一比对：**幻影列 0、NOT NULL 漏写 0**。关键证据：orders 严格三列 order_no/artist_id/client_qq 全写，version 有默认 1 无须写；artworks width/height 已写入（时间条/画廊占位链路无缺列） | ✅ 安全 |
| B-08 | v117 四条报障终验 | 见下方三态裁决表 | 四选三「已修」+一「已修」 |
| B-09 | 路由鉴权覆盖 | admin.routes **56 个路由定义全挂 requireAdmin**（1 处批量 step-up 钩子 + 逐路由 preHandler）；order-client 匿名订单走 anon token 恒定时间比对；上传路由 requireAuth + x-anon-token；前端单 beforeEach 守卫（setup 初始化/主题作用域/鉴权/反向守卫/开放重定向白名单五合一，router/index.ts 173-255） | ✅ 安全 |
| B-10 | 六轴组合场景矩阵抽检 | 取消（Passkey 取消护栏不碰 store）· 重试（anonUpload 失效换新一次重试）· 并发（version 乐观锁 + ORDER_CONFLICT + 幂等键双保险，audit-batch-d-version 测试）· 权限切换（step-up 二次验证 + theme 作用域守卫防闪白）· 空值（money/JSON.parse 兜底链）· i18n（键成对 + check:i18n 门禁）——**为抽检口径非穷举**，深层时序组合（如取消与支付并发交叉）无实测载体 | ✅ 抽样全绿 |

### B-08 v117 四条报障三态裁决表
| # | 报障 | STATUS 轨迹 | 本报告代码级终验 | 三态裁决 |
| --- | --- | --- | --- | --- |
| ① | Passkey 取消后 QQ 行变「-」 | v117① → v118 A 路 | `usePasskeyCreate.ts` 单源护栏：取消（NotAllowedError / null）只发人话提示并 finally 复位 busy，**全程零 store.profile 写操作**；AccountSecurity 注册改走此流 | ✅ 已修待公网复验 |
| ② | 暗色模式骨架屏白色 | v117② → v117 修复（817） | `HySkeleton.vue` 33-37：骨架底色/描边接 `var(--card/--line)` 链式回退（旧 `var(--bg-color)` 无映射回退 #fff 即白闪根因，注释在案）；GalleryPanel 骨架走 `var(--paper2)` | ✅ 已修待公网复验 |
| ③ | 客户端上传参考图「缺少有效匿名凭证」 | v117③ → v118 B 路根治 | 后端 upload.routes 240-252 强制 x-anon-token；前端 `useOrderGallery.ts:61` 失效换新重试一次 + QueueBoardList 530 / useOrderForm 618-647 全链路带 token；anontoken.test 含报障原文断言 | ✅ 已修待公网复验 |
| ④ | 截图右上角直显原始键 common.artist | v117④（一号附赠项）→ v118 A 路 | zh-CN.ts 219 / en.ts 220 双键在案；`stores/artist.ts:41` 顶栏回退 `t('common.artist')` 合法渲染 | ✅ 已修待公网复验 |

**结论**：计划书预录的「已报未修 4 项」——**四选零未修**，全部处于「已修待公网复验」（公网尚未跑 update.sh 同步 v118 之后批次）。

---

## 四、技术债发现（T）

| 编号 | 债务项 | 取证结果 | 裁定 |
| --- | --- | --- | --- |
| T-01 | formatYuan 统一 | `web/src/utils/money.ts` 单模块收口五函数（formatCents/formatYuan/formatYuanValue/formatAddonPrice/formatYuanTrimmed）；全仓视图层**零手工 `¥{{ (x/100).toFixed }}` 残留** | ✅ 已还 |
| T-02 | 手动录单幂等键消费 | server `withIdempotency` 三消费点（order-client 94-95 / order-list 158-161 手动录单 / order-action 283-284 收款，含 I6-d 注释）；前端三处带 UUID header（useOrderForm 646 / useOrderPayments 53/74 / ManualOrderRight 505）+ 双测试锚定 | ✅ 已还 |
| T-03 | snake_case 全链自洽 | 见 B-06——全链 snake 直通，无转换层断点 | ✅ 已还 |
| T-04 | P1-2 集中状态机 | `order-status.ts` 单一 `STATUS_TRANSITIONS`（21-63），撤销为回滚不走前向断言（282 注释在案）；测试多组（order.service.test / audit-batch-d-version 乐观锁非法路径）；web 零本地状态机拷贝 | ✅ 已还 |
| T-05 | 前端守卫双写 | `router/index.ts` 唯一 beforeEach 五责合一；ArtistLayout/AdminLayout 已不自行摘挂主题（301/135 注释「统一由路由守卫管理」） | ✅ 已还 |
| T-06 | 05G 巨型拆分 Top5 | 三拆完成但**两条回胀**（OrderDetail +47% / QueueBoardCalendar +29%），详见 S-01 | 🟡 拆过但需第二轮 + 防阀 |
| T-07 | comms 清理 / repowiki | docs/comms 根目录仅 2 文件（STATUS + 本计划书），历史已滚动归档 archive-20260819/ | ✅ 已还 |
| T-08 | 巨型再生长防阀 | 无任何机械拦截（无行数上限 lint / 无季度复拆清单），changelog 仅事后登记 | 🟡 建议上防阀 |
| T-09 | 审计资产覆盖缺口 | audit-i18n-keydiff 腐烂（S-06）；proactive-scan-checklist 11 模式全部本报告实测仍有效 | 🟡 修/下架二选一 |

**摘要**：九项债务 **6 项已还清**（T-01~05、07）；3 项 🟡 挂起（巨型回胀二轮拆分、再生长防阀、i18n 审计脚本修复），无 🟡 以上主链债务。

---

## 五、四张清单落账

### 🚨 上线前必做（🔴）
**当前为 0 项。** 主链数据正确性 / 崩溃 / 鉴权绕过方向零红线。唯一注意：当前在途未提交批（v141+OCR）本身须在提交前跑 accept.ps1 全绿——属常规流程非缺陷。

### 🔧 待修复清单（🟡，按收益排序）
1. **巨型回胀二轮拆分**：OrderDetail 925 / QueueBoardCalendar 972 / api/types.ts 1506——回胀 +29%~47%，05G 开局三拆只剩一条稳定。
2. **audit-i18n-keydiff.mjs 修复或下架**：`.js→.ts` 导入跟进（v132 后失效）。
3. **再生长防阀**：把「800 行上限」从口头变成机械（lint 插件或 accept 附加检查），或季度复拆清单。
4. **534 处 `.js` 后缀 import 清理**（🟢 风格债，一条 codemod 可清）。

### ❌ 误报表
- **v117 四条报障「未修」之说不成立**——四选零未修，均为「已修待公网复验」（证据：B-08 裁决表）。公网试新功能前请先跑 update.sh 同步（v118~v139 已增十批）。

### 📦 可接受暂缓（🟢）
- changePercent/normalizeRevenue 死链路（有 v139 拍板销账锚点，非隐匿债）；
- admin.routes 970 行（路由注册器聚集惯例，对照 style.service 938 行服务逻辑聚集，拆解收益偏抒发组织而非降风险）；
- api/types.ts 作为纯类型契约仓的膨胀（无害但建议与业务类型分层）。

---

## 六、证据链索引（file:line，均 2026-08-20 实测）

- 门禁：temp/server-gate.log（typecheck/lint/vitest 1608 原文）、temp/web-gate.log（test:web 721 + check:i18n + build 原文）；web lint TS6133 两次跑录见控制器输出。
- 容器 DB：temp/db-schema-snapshot.txt（MIGRATION_VERSION=69、43 表全列）；temp 探针二输出（8 表 NOT NULL 约束清单，Probe2 实录于控制器输出）。
- 循环依赖：临时脚本 cycle-detect.mjs（474 文件 CYCLES=0，已删）；死代码启发式：deadcode-reference.mjs（60 候选抽验 8，已删）。
- 巨型文件：S-01 表为本报告 PowerShell 实测（排除 tests/locales 口径）。
- 其余逐项引用见各表「取证结果」列内 file:line。

*本报告只读交付，未派工修复；是否对 🟡 五项立项施工，待用户拍板。*