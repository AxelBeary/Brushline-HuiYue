# 02-to-01-基础四件套前端-交付-20260808.md

> 交付：二号 → 一号
> worktree：`artist-commission-w2`（分支 `beta/base-four-kit`，已 merge master `5a79d80`）
> 状态：A1 完成 / A2 完成（含数据缺口说明）/ B1 完成（F2 校验复用）/ C4 完成（落在下单时间位，截稿日字段后端未暴露）
> 门禁：web 215/215 ✓ lint 0 ✓ build ✓ check:i18n 0 新增 ✓ 截图 6 张 ✓

## 一、四项逐条

### A1 收入导出 CSV（工具页 /tools/export）— 完成
- 新建 `web/src/views/artist/ToolsExport.vue`：日期范围（el-date-picker daterange，默认本月 1 号→今天）+ 导出按钮
- 调 `GET /api/artist/tools/export.csv?from=&to=`，fetch + credentials:'include'（token 在 httpOnly cookie），blob 下载
- 文件名从 Content-Disposition 解析（后端返回 income-YYYYMMDD-YYYYMMDD.csv），解析失败兜底固定名
- 空数据：后端空 CSV 仅表头 → 前端按行数检测（<=1 行）→ 页面内提示「该时间段无收款记录」，不下载空文件（REQ-031 验收 2）
- 入口：ArtistLayout BASE_MENU_ITEMS 工具组加 `{ index: '/tools/export', icon: Download, labelKey: 'menu.toolsExport', group: 'tools' }`（参照 FoodMenu 模式）；router 加 `/tools/export`
- 截图：`temp/base-four-kit/A1-tools-export.png`

### A2 订单收据（客户端 delivered）— 完成（数据缺口已核实）
- TrackOrder.vue：delivered 状态显示「收据」入口按钮（v-if order.status === 'delivered'）
- 收据弹窗（只读）：订单号 / 画师 / 付款节点（installments 有则列）/ 合计金额 / 已付 / 待付 + 诚实说明行
- 实测守恒：ALICE-005 合计 ¥100.00 = 已付 ¥100.00 = 定金 ¥50 + 尾款 ¥50 ✓（验收 2）
- **契约缺口（报告必读）**：客户端查单接口 `GET /api/orders/track/:orderNo` **不返回 order_payments 逐笔实收明细**（只有 finalPriceCents / paidTotalCents / installments；installments 也无日期字段）。按派工「若缺→报告说明缺口，不硬造」执行——收据展示现有可获取字段，弹窗内注明「逐笔实收流水明细待后端开放客户可见字段后补充」。逐笔收款（日期/金额/类型）需后端在 track 接口补 `order_payments` 客户可见子集（金额+日期+类型，不含画师备注/成本），前端再渲染成完整对账表。
- 只呈现事实流水，不出现画师私有数据（成本/备注）✓
- 截图：`temp/base-four-kit/A2-receipt-dialog.png`

### B1 完稿分享（画师端 delivered）— 完成（F2 校验复用）
- OrderDetail.vue 交付卡 header「发布为作品」按钮旁加「分享」按钮（仅 delivered 显示）
- 分享弹窗：平台选择（`GET /api/platforms` 启用列表）+ 文案模板 textarea（默认模板含占位符 {orderNo} {homepage}，可编辑，localStorage 持久化 `huiyue_share_template`）
- 生成发布页 URL：微博（weibo.com 域名匹配）→ `https://weibo.com/intent/post?text=`（文案预填，新窗口打开）；**B 站等无公开预填发布 URL 的平台 → 复制文案 + 打开画师主页**（用户手动粘贴，发布动作仍在第三方完成）
- **F2 校验复用**：`web/src/utils/linkValidation.js` 的 `validateLink` + `matchDomain` 原样复用（未造新轮子）——画师主页链接从 profile.customLinks 取，经 validateLink 校验（域名匹配所选平台 matchDomains，防投毒）后才嵌入文案；文案含 {homepage} 但无合规主页链接时弹窗内警示
- 实测：弹窗正常渲染（平台「微博」+ 文案 + 占位符提示），种子平台数据可用
- ⚠️ 说明：文案模板无后端存储（需求原案为「画师预设模板」，未合入后端字段）——本次用 localStorage 前端持久化，不引后端改动；若需跨设备/后台统一管理模板，需后端加字段（超出本派工范围）
- 截图：`temp/base-four-kit/B1-order-delivered-buttons.png` + `B1-share-dialog.png`

### C4 时区显示（客户端查单页）— 完成（截稿日字段后端未暴露，落在下单时间位）
- TrackOrder.vue 下单时间（orderTime）展示处加双行：第一行北京时间（显式 timeZone:'Asia/Shanghai'）+ 第二行客户当地时间（仅浏览器时区 ≠ 北京时显示）
- 实测：北京时区 → tz-local=0 只显示一行（验收 2 ✓）；America/New_York → tz-local=1 双行（北京时间 + 你的当地时间）（验收 1 ✓）；夏令时由 Intl API 天然处理（验收 3 ✓）
- **契约缺口**：订单表有 deadline 字段（画师端录单可设），但客户端 `GET /api/orders/track/:orderNo` **未返回 deadline**——查单页无「截稿日」可展示。本项落地在现有「下单时间」位实现双时区能力；若需对截稿日做双时区，需后端 track 接口补 `deadline` 字段（非本派工范围，未改后端）
- 截图：`temp/base-four-kit/C4-track-order-beijing.png`（单行）+ `C4-track-order-newyork.png`（双行）

## 二、验证门禁（全过）

| 门禁 | 结果 |
|------|------|
| npm run test:web | 215/215（13 files）✓ |
| npm run lint | 0 error ✓ |
| npm run build | 成功（12.5s）✓ |
| npm run check:i18n | 0 新增硬编码中文 ✓ |
| git diff --stat | 仅授权文件 ✓ |

## 三、改动文件（授权范围内）

- `web/src/views/artist/ToolsExport.vue`（新建：A1 工具页）
- `web/src/components/ArtistLayout.vue`（工具组菜单项 + Download 图标）
- `web/src/router/index.js`（/tools/export 路由）
- `web/src/views/client/TrackOrder.vue`（A2 收据 + C4 时区）
- `web/src/views/artist/OrderDetail.vue`（B1 分享）
- `web/src/locales/zh-CN.js` + `en.js`（toolsExport / track.tz* / track.receipt* / orderDetail.share* 键，zh/en 同步）

## 四、契约确认

1. **A2 数据源**：track 接口无 order_payments 明细 → 前端不硬造，收据展示现有字段 + 缺口说明；待后端补客户可见收款流水字段
2. **B1 F2 校验**：复用 `utils/linkValidation.js`（validateLink/matchDomain），未重造轮子；画师主页链接需先在「主页设置」F2 链接编辑器配置
3. **C4 截稿日**：track 接口无 deadline 字段 → 时区能力落在下单时间展示位；截稿日双时区待后端补字段
4. 未触碰后端；未引新前端依赖（@playwright/test 仅实测临时安装，已还原 package.json/lock 并清理）

## 五、演示数据说明

- 本地独立实测环境（3998/vite 5174，独立 DB commission.db）造单 ALICE-001~005 推进 delivered + 收款 + 交付文件，用于截图；实测结束已停服务、删临时脚本与 vite.local.config.mjs；截图存 `temp/base-four-kit/`（temp/ 不入 git）
- 测试数据仅存在于本地实测 DB，不影响生产
