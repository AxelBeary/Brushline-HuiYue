# 03-to-01-any全库清零-交付-20260807

> 交付：三号 → 一号（backend）—— 两连批返工 + 全库 any 清零，全部完成
> 分支：`beta/any-cleanup`，HEAD `4f3b809`（含 master 309b2c0 合并）
> 派工依据：`03-派工-any清零返工-20260807.md` + `03-派工-any全库清零-20260807.md`
> 性质：strict 模式下 server/src 全库显式 any 清零，无任何蒙混

## 一、总览

| 批次 | Commit | 文件 | 处数 | 说明 |
|------|--------|------|------|------|
| 批1 返工 | `02da231` | admin.routes / artist.routes / dashboard.routes | 82 | handler `request/reply: any` → FastifyRequest/FastifyReply + 体内 as 断言 |
| 批2a | `f3a9749` | order.routes | 90 | FastifyRequest + OrderDetail/OrderRow 具体类型 |
| 批2a 补充 | `79a49ed` | order.routes | — | 复用 entities.OrderDetail，删本地重复接口 |
| 批2b | `8f0b319` | style.routes / pricing.routes / upload.routes / fastify.d.ts / entities.ts | 56 | 路由 any 清零 + request 挂载属性类型化（order→OrderDetail、multiplier→Multiplier、targetArtist→Artist，新增 artStyle/addonTemplate） |
| 批2c-1 | `3cb11a6` | guestbook / activity-log / order-gallery / order-queue / order-stats / order-workflow service | 20 | SQL params `any[]`→`Array<string|number>`；返回类型 any→OrderDetail/ArtistOrderRow/具体行类型 |
| 批2c-2 | `735afcf` | order.service / entities.ts / utils/price.ts | 18 | order.service 返回类型具体化；resolvePriceCents 参数放宽 |
| 批2c-3 | `4f3b809` | admin.routes / order.routes | — | map 回调复用 ArtistOrderRow 收尾 |

**合计：266 处显式 any 全部清零。**

## 二、替换模式（无 any 蒙混）

### 类别 A：路由 handler 参数（大头）
```ts
import type { FastifyInstance, FastifyRequest, FastifyReply } from 'fastify'

fastify.get('/api/...', { preHandler: requireAuth }, async (request: FastifyRequest, reply: FastifyReply) => {
  const id = Number((request.params as { id: string }).id)
  const body = request.body as { qqNumber?: string }
})
```
- preHandler 具名函数（requireAdmin/requireExistingArtist/requireOwnOrder/requireOwnStyle 等）同样改
- `request.artist` / `request.order` / `request.multiplier` / `request.targetArtist` / `request.artStyle` / `request.addonTemplate` 均为 fastify.d.ts 扩展类型，直接可用

### 类别 B：SQL 行断言（合理保留 + 更精确）
- `(row as { c: number }).c` 等具体类型断言全部保留（SQL 结果无静态类型，非 any）
- `as any[]` → `as ArtistOrderRow[]` / `as DeadlineRow[]` / 具体行类型

### 类别 C：`as any` 逃生舱 → 全部替换
- `request.body as any` → 按 schema 的具体 body 接口
- `(request.body as any).xxx` → `(request.body as { xxx: T }).xxx`
- `catch (err: any)` → `catch (err)` + `err as AppError` / `err as Error`
- `(artist as any).id` → 直接 `artist.id`（类型守卫已收窄）

### 类别 D：fastify.d.ts
- `order: any` → `OrderDetail`（新增 entities.ts 导出，extends Order）
- `multiplier: any` → `Multiplier`
- `targetArtist: any` → `Artist`
- 新增 `artStyle: ArtStyle`、`addonTemplate: AddonTemplate`（style.routes preHandler 挂载）
- entities.ts 新增导出：`OrderDetail`（订单详情：基础字段+关联数组+画师/档位字段）、`ArtistOrderRow`（订单列表/队列行，带 index signature）

## 三、门禁结果（HEAD 4f3b809）

```
npx tsc --noEmit --strict   → 0 errors
npm test                    → 61 files / 953 tests passed
npm run lint                → 0 errors（6 warnings 均为存量：
                              pricing-engine.ts no-new-array×5 + order.service.ts 未用 import×1，
                              stash 前后对比确认非本批引入）
```

## 四、全库 grep 证据

```
Select-String -Path server\src -Recurse -Include *.ts -Pattern ":\s*any\b|\bas\s*any\b|<any>|\bany\[\]"
→ 0 命中（0 hits）

补充检查：
- request: any / reply: any      → 0
- as unknown as any              → 0
- @ts-ignore / @ts-expect-error  → 0
- any[] 裸数组                    → 0
```

## 五、改动明细（按文件）

| 文件 | 处数 | 修法 |
|------|------|------|
| admin.routes.ts | 43 | A（request/reply 参数 + params/query/body 断言 + `(o: any)`→ArtistOrderRow + `err: any`→AppError） |
| artist.routes.ts | 36 | A + `(t: any)`→`{ visibility: string }` + `request.body as any`→具体 body |
| dashboard.routes.ts | 3 | A |
| order.routes.ts | 90 | A + OrderDetail/ArtistOrderRow + deliverables `|| []` + getOrder 非空断言 |
| style.routes.ts | 24 | A + `request.body as any`→具体 body + `as Parameters<typeof svc.fn>[N]` 断言 |
| pricing.routes.ts | 20 | A + 同上 |
| upload.routes.ts | 9 | A + `opts: { uploadDir?: string }` + `err: any`→Error |
| order.service.ts | 18 | 返回类型 any→OrderDetail/ArtistOrderRow/具体行；`params: any[]`→`Array<string\|number>`；getOrder 关联数组显式列+行类型断言 |
| order-queue.service.ts | 6 | 返回 any[]→ArtistOrderRow[]；updatePriority→OrderDetail |
| order-workflow.service.ts | 6 | advanceStage/rollbackStage/enableTracking→OrderDetail；getStageInfo/replaceSpeechVars/getSpeechInfo 参数 order: any→OrderDetail |
| order-gallery.service.ts | 4 | deliverOrder 等返回类型具体化（{ order, statusChanged } / OrderDetail） |
| guestbook.service.ts | 2 | params any[]→Array<string\|number> |
| order-stats.service.ts | 1 | getArtistStats 返回类型具体化（10 字段） |
| activity-log.service.ts | 1 | params any[]→Array<string\|number> |
| types/fastify.d.ts | 3 | order→OrderDetail / multiplier→Multiplier / targetArtist→Artist + 新增 artStyle/addonTemplate |
| types/entities.ts | 新增 | OrderDetail（extends Order）、ArtistOrderRow 导出 |
| utils/price.ts | 1 | resolvePriceCents 参数放宽为可选字段（兼容 OrderDetail 可选列） |

## 六、说明与边界

1. **无豁免清单**：266 处全部清零，无一处保留 any，无 @ts-ignore/@ts-expect-error。
2. **非空断言 `!` 的使用**：事务内 `return getOrder(orderId)!` 等（订单刚创建/更新，逻辑上必存在）——非 any，属具体类型下的合理断言，lint 无 no-non-null-assertion 规则报错（已确认 0 errors）。
3. **resolvePriceCents 参数放宽**：`{ final_price_cents?: number | null; total_price_cents?: number | null; price_snapshot?: number | null }`——只放宽为可选，语义不变，向后兼容。
4. **lint 存量 6 warnings**：pricing-engine.ts `new Array(n)`×5（unicorn no-new-array）+ order.service.ts 未使用 import `deriveInstallmentProgress`×1，均为历史代码，非本批引入；按最小变更原则未顺手清理，如需可在后续技术债批处理。
5. 未推送、未合并、未改 STATUS；合入由一号执行。