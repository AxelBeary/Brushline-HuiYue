# 二号交付：v0.31 Wave 3 前端（2026-08-03）

## 分支

`feat/v0.31-frontend-0803`（已推送 origin，已 rebase 到最新 master）

## 改动

### F8 留言语言过滤（GuestbookManage.vue）

- 状态筛选同行右侧加语言筛选下拉（el-select）
- **动态选项**（REQ-021 拍板"根据实际数据动态生成"）：从留言数据聚合语言代码，按数量降序
- 语言标签用原文显示（中文/English/日本語），未知语言直接显示代码
- 卡片头部加语言徽标（primary 色胶囊）
- 与状态筛选组合过滤（两个条件 AND）
- 边界处理：数据刷新后当前筛选值已不存在时自动重置（watch languageOptions）
- 本地过滤（与现有 statusFilter 模式一致——画师端 API 返回全量数组）
- i18n: `guestbookManage.languageAll`

### F1 操作日志前端——阻塞中

- master 无 GET /logs API（搜索 `/logs|order_logs|operation_log` 为 0 匹配），契约未定
- 等三号 Wave 3 操作日志后端合入后实施（时间线面板 + 类型/时间筛选）

## 改动文件

| 文件 | 改动 |
|------|------|
| `web/src/views/artist/GuestbookManage.vue` | F8 语言筛选 |
| `web/src/locales/zh-CN.js` / `en.js` | F8 键 |

## 验证

- ESLint：零错误零警告
- 前端测试：106/106 通过（6 文件）
- vite build：成功（5.45s）

## Commit

1. `d52aa47` feat(client): F8留言语言过滤

## 待办

- F1：等三号 GET /logs API 合入 master 后实施（订单详情操作日志时间线 + 筛选）
