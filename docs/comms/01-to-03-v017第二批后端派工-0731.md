# 一号 → 三号：v0.17 第二批后端派工（R58-8 + 灵感标签）
> 日期：2026-07-31

## 背景

v0.17 第二批是全栈任务（R58-8 URL 识别 + 灵感标签自定义 + R58 多模板设置页）。后端部分无前端依赖，可先行。二号正在做第一批（R24+R25），前端部分等第一批合入后再派。

## 分支与 worktree

- 分支：`feat/backend-v017-b2`（从 master 最新切出）
- worktree：独立建，禁止在主 worktree 操作

## 任务 A：R58-8 URL 平台识别（后端）

用户拍板：自动识别 + 后备手动选。

1. 画师表加 `platform_urls` 字段（JSON，存各平台 URL）或独立 `artist_platforms` 表——你定，写清理由
2. 平台识别逻辑：从 URL 提取平台（Pixiv/Twitter/X/微博/Lofter/B站/小红书/其他），写识别工具函数
3. API：画师设置页读写平台 URL（GET/PUT）
4. 迁移文件：v17
5. 客户主页 API 返回平台链接列表（含识别后的平台名 + 原始 URL）

## 任务 B：灵感标签自定义（后端）

当前标签硬编码在前端。

1. 画师表加 `inspiration_tags` 字段（JSON 数组）或复用现有字段——你定
2. API：画师设置页读写自定义标签（GET/PUT）
3. 客户下单页 API 返回画师自定义标签
4. 迁移文件：与任务 A 合并为 v17

## 授权文件列表（只许改这些）

| # | 文件/目录 | 用途 |
|---|-----------|------|
| 1 | `server/src/db/migrations/` | 新增 v17 迁移 |
| 2 | `server/src/db/schema.js`（或等效） | 表结构更新 |
| 3 | `server/src/services/artist.service.js`（或等效） | 画师设置读写 |
| 4 | `server/src/routes/artist.routes.js`（或等效） | API 路由 |
| 5 | `server/src/utils/` | 平台识别工具函数（新文件） |
| 6 | `server/tests/` | 对应测试 |

如需改授权外文件，**停止并写 comms 申请扩展授权**。

## 验证标准

- `cd server && npx vitest run` 全绿
- 迁移 v17 可正常应用
- 平台识别覆盖至少：pixiv, twitter/x, weibo, lofter, bilibili, xiaohongshu, 其他
- 提交前 `git diff --stat` 确认只有授权文件

## 交付

完成后写 `docs/comms/03-to-01-v017-b2后端提交-0731.md`，含分支名 + 改动文件 + 验证结果 + 迁移说明。
