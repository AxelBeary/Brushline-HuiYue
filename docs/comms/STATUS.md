# 全局状态（一号维护，其他角色只读）

> 最后更新：2026-08-02 v0.28 收工
> 维护者：一号（主理人）

---
## master 状态

- **HEAD**：`cab1dad`（收工提交后更新），与 origin 同步
- **后端测试**：567/567 通过（34 文件）
- **前端测试**：87/87 通过（5 文件）
- **E2E 测试**：5/5 通过
- **迁移**：v29
- **容器**：✅ Healthy

---
## v0.28 完成清单

| 项 | 角色 | 状态 |
|---|---|---|
| REQ-016 后台界面逻辑重组（设置页4Tab+侧边栏分组+接稿状态归位+app.js修复） | 三号 | ✅ |
| 画师后台现状审计（60+问题+11截图基线+回归清单） | 五号 | ✅ |
| 时间条拖拽设计评估 + 实施（原生Pointer Events，右端截稿日/左端开工日） | 二号 | ✅ |
| 文档维护（changelog+REQ-016状态） | 四号 | ✅ |
| 审计bug修复（OD1 #undefined + T3 TierManage i18n化） | 五号+一号 | ✅ |
| 画师后台视觉规范 v1 + 完整设计书 | 一号 | ✅ |

---
## 明天开工指南

### 唯一候选：画师后台视觉统一

> **前置条件**：用户带设计稿回来（已拿设计书去外部 AI 生成）
> **参考文档**：
> - `docs/画师后台设计书-完整版.md`（完整设计书，7 章，含每页详细规格）
> - `docs/画师后台视觉规范-v1.md`（从 6 份参考稿提炼的规范，待设计稿定稿后更新）
> - `docs/audit-画师后台现状.md`（五号审计，60+ 问题，改前基线）
> - `docs/audit-screenshots/`（11 张截图基线）

**实施路径**（设计稿回来后）：
1. 用户确认设计方向 → 一号定稿视觉规范（更新 v1 → v2）
2. 三号建共享样式层（`artist-ui.css` 或 scoped 共享 class）：配色 token、卡片、按钮、Badge、表格、空状态、统计卡
3. 三号逐页改（按权重：仪表盘 → 排期 → 录单 → 订单详情 → 其余）
4. 五号回归（对照审计文档逐项确认）

**如果设计稿没回来**：全员空闲，无其他不阻塞工作。

### 各角色状态

全部空闲，等设计稿。

---
## 分支状态

| 分支 | Worktree | 状态 |
|------|----------|------|
| master | 主 worktree（一号专用） | 收工提交后更新 |

---
## 重要规则提醒

- 合并到 master 后**立即推送**
- 操作 master 前 `git log --oneline -5` 确认 HEAD
- 禁止对 master 执行 `git reset --hard` / `git rebase`
- 禁止 `git add -A`
- 并行角色必须在独立 worktree
