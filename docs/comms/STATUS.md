# 全局状态（一号维护，其他角色只读）

> 最后更新：2026-08-05 夜 v0.37 轮开局——四号文档轮 + 三号 REQ-025 引擎第一阶段 两路并行已派工，worktree 已建
> 维护者：一号（主理人）
> **刷新后自包含**：新会话只读本文件即可完全恢复，不依赖任何对话记忆。
---
## master 状态

- **HEAD**：`3aea46f`（三号 A 路合入 + 收尾），与 origin 同步
- **工作树**：干净（四号需求深聊批在途产物除外：REQ-027/028 草稿 + REQ-014/待修复清单修改，未提交，勿动）
- **测试基线**：server 786/786 · web 173/173 · tsc 0 · lint 0
- **容器**：A 路合入后已重建，healthy；迁移 v40 实跑通过（locked/locked_reason 列就位，订单 14 条完好，A 路核心函数在镜像）
- **备份**：`data/commission.db.bak-v037-pre-v40`（A 路合入前）+ `bak-v037-pre-v39`（引擎合入前）+ `bak-v036-w2-verify`（六路合入前）+ `bak-v036-verify`（波 1）
- **迁移**：v40
- **comms**：只剩本文件（本轮全部派工/交付已合入，按"合入即删"清理）

---
## 当前阶段：v0.37 轮收官——TOTP 上线门槛解除 + D 路补漏批全合入，全员空闲待命

| 角色 | worktree/分支 | 内容 | 状态 |
|------|--------------|------|------|
| 三号 | — | **REQ-027 TOTP 动态口令登录（P0 上线门槛）**：TOTP 核心零依赖 + 迁移 v41 + 登录/绑定/重置/CLI + R7 旧登录码一刀切移除 | ✅ 已合入 `ac44a79`（server 824/824，RFC 6238 官方 6 向量一号独立验证全过，容器重建 v41 实跑通过） |
| 二号 | — | D 路补漏批：REQ-029 四项用户拍板缺口（R2/R5/R6/B2） | ✅ 已合入 `8461251`（web 186/186，浏览器实测四项全过） |
| 五号 | — | C 路：demo-data 切流 | ✅ 已合入 `eb0b4d0`（空闲） |
| 四号 | — | 需求深聊批：REQ-026/027/028/029/030 + REQ-022 回写 + 传播修改 | ✅ 已落库（空闲） |

**重大里程碑（2026-08-05）**：REQ-027 TOTP 上线门槛解除——登录改 TOTP 动态口令，QQ Bot 推迟为 REQ-028 备案，AUTH_DEV_MODE 关闭不再依赖任何消息渠道。
**TOTP 审核验证链**（一号独立执行，不信 self-report）：逐行审 totp.ts RFC 6238 实现（HMAC-SHA1/动态截断/timingSafeEqual）+ **RFC 官方 6 条测试向量独立跑全过** + 防爆破（防枚举同响应/5 次锁 15 分钟/锁定期间正确码也拒）+ 迁移 v41（DROP login_codes 无反向 FK 确认）+ CLI 兜底脚本 + transfer 双码改造 + 旧登录码残留清除（仅剩 2 死键建议项）+ server 824 + web 179 + tsc/lint 全绿 + 容器重建 v41 实跑通过。
**新依赖**：qrcode@1.5.4（二维码生成，唯一依赖例外，已声明）+ @types/qrcode（dev）。
**D 路补漏批审核**：对照 REQ-029 逐项核实四项全补齐；R5 自定义增项走创建后 addExtraItem 补写（对齐 deadline 模式，零后端改动，实测 final=30000 入账正确）；合并态 web 186/186 绿（locales 自动合并后重跑确认）。

**重大决策变更（2026-08-05 用户拍板，四号深聊批）**：登录主方式改为 **TOTP 动态口令**（REQ-027，P0），QQ 机器人推迟为通知渠道备案（REQ-028）。上线门槛易主：原"QQ Bot 发码"消解，REQ-027 成为唯一上线门槛，AUTH_DEV_MODE 关闭不再依赖消息渠道。
**用户追加确认（2026-08-05）**：REQ-027 Q1 恢复方案只做"管理员重置"一条——认可；REQ-026 Q1 v0.38 视觉迁移"一次性全量切换"——认可（不留双轨，token 层建成后全页面统一套用）。
**D 路补漏批根因**（一号复盘）：派工早于 REQ-029 定稿，用户拍板的 R2/R5/R6/B2 未进派工。教训已入账：并行需求深聊与开发派工时，需求稿未定稿的功能点不进开发派工，或派工注明"以需求稿定稿为准"。

**A 路合入验证链**（一号独立执行）：4 commit 逐 diff 深审 + 双重记账修复确认（收口逻辑互斥分支）+ 存量订单漂移查库排查（13 单零漂移，A1 严格相等不误杀）+ ManualOrder 跨模块风险排除（录单状态到不了 done）+ 存量测试适配核实（语义变更合理适配非掩盖）+ 合并态重跑 786/786 + tsc/lint 全绿 + 容器重建 v40 实跑 + 引擎核心函数镜像存在性验证。
**用户 v0.36 终验**：✅ 通过。「已收节点不再变」从算法变为真实生效（A 路核心承诺）。
**拆路依据**：C（demo-data.ts）与 D（ManualOrder.vue + locales）零文件交集可并行；均不碰 A 路成果文件。

**测试基线**：server **786/786**（46 文件）· web 173/173 · tsc 0 · lint 0

**引擎合入验证链**（一号独立执行，不信 self-report）：
- 逐行读 440 行引擎实现：锁价"先到先锁"、floor+尾差归最后未锁、负 delta R8 封顶、镜像退款、守恒三断言全部与 REQ-025 R4~R11 对齐
- 49 测试逐条读断言：边界案例 1~8 纠正值全覆盖 + 守恒破坏用例（A3 用例专门构造 A1/A2 自洽仅 A3 破坏的数据）
- worktree 独立重跑 762/762 + tsc + `npm run lint` 全绿；merge master 后合并态重跑 769/769
- 容器重建后：迁移 v39 实跑（schema_migrations 有记录）、表/索引就位、订单 9 条完好、引擎文件 MD5 与 master 一致

**三号上报的 A2 断言修正已确认合理**：REQ-025 R11.2 原文「总价−已收=Σ待收」在关单后场景不成立，实现为「+额外应收−额外应退」，关单前等价。REQ-025 文档 R11.2 表述可在第二阶段派工时一并修订（不阻塞）。

### 下一步：REQ-025 第二阶段（待用户拍板开工）

草案已就位（REQ-025 第八节：10 端点接入清单 + 切流方案 + 交互稿 + 测试映射）。第二阶段拆法待一号派工：端点接线（createOrder base 条目 / updateFinalPrice / addExtraItem 负数放开 / deleteExtraItem 冲正 / addPayment 填充推导 / advance 触发锁 / recalcInstallmentAmounts 退役）+ demo-data 切流 + 前端收款进度展示 + done 无痕改价守卫。

### 用户终验重点路径（v0.36 六路，二号已全数预验通过）

改价/加项后收款区不归零；负数退款顺畅+多收橙色；约稿表单不跳步；录单价格跟随；看板交付弹窗；备注 Enter 不重复。另顺手点一下订单列表「详情」按钮（二号 Hermes 环境疑似自动化问题，本地 Playwright 未复现）。

### 本轮踩坑记录（下轮派工前必读）

1. **A2 守恒表述**：REQ-025 R11.2 原文在关单后场景不严格成立，实现按「含额外项」版本落地，文档待修订
2. **eslint 直调 vs npm run lint**：直调单文件会报 "no matching configuration"（flat config 路径匹配问题），必须用 `npm run lint` 全量跑
3. **容器验证文件一致性用 MD5**：grep -c 按行计数与本地 Select-String 按匹配行计数口径不同，哈希一致才是铁证
4. **setup.js 一行改动超授权**：三号 cleanDb 加 DELETE order_price_entries 属测试隔离必要，已声明——此类最小必要超授权可接受，但交付报告必须声明

### 一号裁决：done 改价守卫的时序（2026-08-05，写入五号派工）

done 订单当前唯一减价路径是 updateFinalPrice——负增项被 schema 拦死（`priceCents minimum: 0`），负收款只退钱不调总价。因此 done 改价守卫**必须等 REQ-025 第二阶段的负条目机制一起上**，否则画师减价无门。五号本批只拦 delivered/cancelled。

### 本轮候选核销记录（派工前已逐项验证代码现状）

1. ❌ **createOrder 分期去重**：REQ-025 第二阶段重写分期生成时自然覆盖，不单独立项
2. ❌ **addons 表/addPayment 旧写路径评估**：同上，REQ-025 收款分摊重写时必然评估，并入第二阶段派工
3. ⏸ **ManualOrder 接画风模型**：现状属实（无画风入口）但会生成分期撞 REQ-025 新引擎，推迟到 v0.37 引擎切流后

### 本轮踩坑记录（下轮派工前必读）

1. **候选清单过时**：波 2 候选里「四号归档清单」实际早已完成（文件已在 archive/）。派工前必须 search_files 验证代码现状，盲信列表 = 重复劳动。
2. **worktree 交叉污染**：五号 worktree 曾混入 B1 的残缺版 order.routes.ts（调用在、定义丢），致 32 测试 500。恢复：`git checkout -- <file>` 还原单文件 + `git merge master` 拉完整版。
3. **合入顺序约定生效**：五号（P 批）与三号-B（T 批）同改 OrderDetail.vue，约定五号先合、三号-B 交付后 merge master 重跑测试再放行——零冲突收工。
4. **三号-B 上报的「deleteNote 响应缺 currentStageId」**：已被 B1 enrich 覆盖修复（他测的是旧容器镜像），无需再处理。

### 待用户拍板（不阻塞派工）

1. ~~done 状态是否归入终态~~：✅ 2026-08-05 用户拍板 **done = 半终态**（详见下方「已拍板待实施」）。
2. ~~Classic 模板画廊位置~~：✅ 2026-08-05 用户确认现状正确（Classic 画廊在价格区后是有意设计），关闭。
3. 时间条「开稿/截稿日二合一」：属 v0.38 视觉重设计范围（设计 brief 已列），已答复用户。

### 已拍板待实施：done 半终态规则（REQ-025 第二阶段派工约束）

用户 2026-08-05 拍板（场景：画完后客户要多版本光影加钱 / 十张改一张退钱）：

- **done = 半终态，双向留痕**：
  - ✅ 允许：加附加项（加钱）、加负条目（减钱/退）、收款、负数收款
  - ❌ 禁止：无痕改总价、绕过条目直接删改价格结构、工作流回退
- **钱的去向引擎自动判定**（画师零决策）：客户未付全 → delta 冲抵未付节点（R5/R9）；已付全 → 进额外应收 `extra_charge_after_close` / 额外应退 `extra_refund_after_close`（R10）
- **delivered 保持完全终态不变**
- **顺手补漏洞**：`updateFinalPrice` 后端目前无状态校验（delivered 也能通过 API 改价，仅前端藏按钮）——第二阶段补后端守卫（前端校验只是子集，v0.36 L3 教训）
- 实施归属：REQ-025 第二阶段（接端点切流）派工约束，第一阶段纯函数引擎不受影响

### 下一轮候选（REQ-025 第二阶段之后）

- ~~ManualOrder 接新画风模型~~：✅ **2026-08-05 用户确认需要，已排期**——A 路合入后作为 D 路派工（与 C 路 demo 切流并行：ManualOrder.vue 前端 vs demo-data.ts，零文件交集）。需求细节待用户与四号对齐后出需求稿。
- 二号/五号空闲：视 REQ-025 第二阶段拆分情况派前端 PaymentBar 改造（额外应收展示、已锁节点锁定价展示）

### 待用户与四号交流的话题（2026-08-05 一号盘点）

1. **ManualOrder 接画风需求细节**：档位模式与画风模式如何共存（多画风开关下的录单 UI）、录单算价预览、初始节点状态在画风模式下的交互
2. **REQ-022 四个待确认**：① 完稿转作品允许选部分图片？② 作品可绑多个档位？③ 外链平台列表硬编码 5 个+其他？④ 回收站用分页（建议）？
3. **REQ-014 桌面端伴侣应用**（v1.0+ 远期）：细节迭代中，不急

### 已知遗留

| 项 | 归属 |
|----|------|
| AUTH_DEV_MODE=false + QQ Bot 接入 | 上线前必做清单 |
| done/delivered 终态订单也生成了分期 | 待用户拍板终态定义后处理 |
| addons 表是否 drop + addPayment 旧写路径 | 下一轮同批评估 |

---
## 版本计划（用户拍板）

| 版本 | 内容 | 状态 |
|------|------|------|
| v0.35 | REQ-024 画风档位统一 F1-F6 | ✅ 全合入 |
| v0.36 | 清账版 + 终验反馈轮六路修复 | ✅ 全合入，等用户终验 |
| v0.37 | REQ-025 动态节点计价模型（已审核通过备案） | v0.36 终验后 |
| v0.38 | 画师后台视觉重设计（纸墨颜料盘；含开稿/截稿日二合一 picker） | v0.37 后 |

**v0.38 之后**：上线安全前置 + REQ-022 剩余链路（交付→发布→绑档位，水印已砍）+ 真实画师反馈批次。

---
## 各角色状态

| 角色 | 状态 |
|------|------|
| 二号 | 开工中：D 路 ManualOrder 接画风（worktree: artist-commission-manualstyle）；B 路已合入 `5b2388b` |
| 三号 | 空闲；A 路已合入 `3aea46f`（server 基线 769→**786**） |
| 四号 | 开工中：需求深聊批（REQ-027/028 草稿在途，主仓 docs/，无 worktree） |
| 五号 | 开工中：C 路 demo 切流（worktree: artist-commission-demo） |

新开工角色需重新建 worktree（用 `git worktree add`，一号统一分配）。

---
## ⚠️ v38 迁移事故记录（一号自查发现并已修复）

迁移运行器把迁移包在事务里，`PRAGMA foreign_keys` 事务内是 no-op，DROP artists 触发子表 CASCADE。修复：运行器 noTransaction + 事务外 12 步 + 双保险 + TC-MIG-38。教训：**任何 DROP/RENAME 父表的迁移必须事务外执行并显式关 FK**。

---
## 重要规则提醒

- 合并到 master 后**立即推送**；操作前 `git log --oneline -5` 确认 HEAD
- 禁止对 master `git reset --hard` / `git rebase`；禁止 `git add -A`
- 并行角色必须在独立 worktree（用完即删，本轮已清理）；Docker 环境 SQLite 用 DELETE 模式
- **开工第一步 `git merge master` 再读派工文件**；交付合入前再 merge 一次重跑测试
- **一号 commit 前逐行核对 git status 暂存区**——防误带他角色改动（e04f2f5 事故教训）
- **前端校验只能是后端规则的子集**（v0.36 L3 教训）；审入校验前对照后端完整能力设计
- **派工前验证代码现状**（候选清单可能过时，v0.36 波 2 教训）
- **self-report 不可信**：角色声称完成必须实测验证（跑测试/读 diff/grep 关键改动）
