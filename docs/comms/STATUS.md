# 全局状态（一号维护，其他角色只读）

> 最后更新：2026-07-30 深夜（重启前最终状态）
> 维护者：一号（主理人）

---

## master 状态

- **HEAD**：`97caf3d`，与 origin 同步
- **测试**：151/151 通过（7 文件）
- **构建**：通过
- **容器**：commission-web 运行中（v0.13 + UI-7/UI-8 完整）

---

## 各角色任务状态

| 角色 | 当前任务 | 状态 | 备注 |
|------|----------|------|------|
| 二号 | 待命 | ⚪ 空闲 | R34/R30/R33 前端已合入 |
| 三号 | 待命 | ⚪ 空闲 | v0.13 后端 + UI-7/UI-8 已合入，SPEC-002 已提交 |
| 四号 | 待命 | ⚪ 空闲 | REQ-007 已通过，新反馈已整理 |
| 五号 | 待命 | ⚪ 空闲 | v0.12 回归 11/11 通过 |

---

## 下一步（按优先级）

1. **SPEC-002 审核**：三号已提交 R30d 流程状态机技术方案（`docs/specs/SPEC-002-R30d-流程状态机.md`），一号审核 → 用户确认 → 实施
2. **v0.13 收工**：R30d 是 v0.13 最后一项，完成后 v0.13 关闭
3. **v0.14 规划**：R36 嵌入白名单 + 其他候选

---

## v0.13 已合入内容

迁移 v13（login_codes 重建）+ CVE Dockerfile + P1-5 关闭（Caddyfile 清理）+ R33 签名刷新（后端+前端）+ R34 三模板外链 + R30a/b/c/e 看板增强 + R37 关闭（CSRF 已有 SameSite=Lax，SRI 不适用）+ UI-7 管理员导航 + UI-8 hidden 状态。测试 145→151。

---

## 重要规则提醒

- 合并到 master 后**立即推送**（同一命令链）
- 操作 master 前**必须 `git log --oneline -5` 确认 HEAD**
- 禁止对 master 执行 `git reset --hard` / `git rebase`
- 提交前 `git diff --stat` 确认只有授权文件（禁止 `git add -A`）
- 开工前读本文件了解当前状态
- **代码必须在 git 里，未 commit 不算完成**
- **实际操作人不是中继，交接写 comms 文件**
