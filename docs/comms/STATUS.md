# 全局状态（一号维护，其他角色只读）

> 最后更新：2026-07-30（P0 批次合入后）
> 维护者：一号（主理人）

---

## master 状态

- **HEAD**：`58720ef`（P0-5 合入），与 origin 同步
- **测试**：145/145 通过（7 文件）
- **构建**：通过
- **容器**：commission-web 运行中（含 UI-3 修复 + v0.12 后端 + P0 批次）
- **迁移**：v12 已在容器重建时自动执行

---

## 各角色任务状态

| 角色 | 当前任务 | 状态 | 分支/备注 |
|------|----------|------|-----------|
| 二号 | v0.12 前端（R15→R18→R19 + Settings camelCase） | 🔵 开工 | `feat/client-frontend-v012`（worktree: artist-commission-client） |
| 三号 | 待命 | ⚪ 空闲 | v0.12 后端已合入 |
| 四号 | 待命 | ⚪ 空闲 | UI-3 已闭环 |
| 五号 | P0-3/P0-4/P0-5 已合入，待命 | ⚪ 空闲 | 等签名矩阵回归（二号前端完成后） |

---

## 下一步（按优先级）

1. **二号 v0.12 前端**（R15→R18→R19，含 Settings camelCase 适配）
2. **容器重建**（二号前端完成后）
3. **五号回归**：签名矩阵逐项核对（容器重建后）
4. **v0.13 规划**（签名刷新机制 / 嵌入白名单 / SRI / CSRF）

---

## 今日合入内容

| 提交 | 内容 |
|------|------|
| `eae0ae3` | v0.12 后端（迁移 + R15/R18/R19，三号） |
| `c5960a8` | UI-3 订单列表焦点图签名修复（四号发现，一号修） |
| `78a56df` | P0-3 转让防爆破（五号） |
| `3e01ec1` | P0-4 时间格式（五号） |
| `58720ef` | P0-5 嵌入脚本 CSP（五号） |

---

## 重要规则提醒

- 合并到 master 后**立即推送**（同一命令链）
- 操作 master 前**必须 `git log --oneline -5` 确认 HEAD**
- 禁止对 master 执行 `git reset --hard` / `git rebase`
- 提交前 `git diff --stat` 确认只有授权文件（禁止 `git add -A`）
- 开工前读本文件了解当前状态
- **代码必须在 git 里，未 commit 不算完成**
- **实际操作人不是中继，交接写 comms 文件**
