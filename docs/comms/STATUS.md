# 全局状态（一号维护，其他角色只读）

> 最后更新：2026-08-04 深夜 v0.35 三波全部合入 + 交付卡死修复合入（等用户终验）
> 维护者：一号（主理人）
> **刷新后自包含**：新会话只读本文件即可完全恢复，不依赖任何对话记忆。

---
## master 状态

- **HEAD**：`54e4c25`（comms 清理），与 origin 同步
- **v0.34 关键 merge**：二号客户端 `81a39aa`、三号后端画师 `5459ce4`
- **测试**：server 666/666 · web 125/125（二号 +7 query 预选用例）· tsc 0 · eslint 0 · build ✓
- **迁移**：v36（v0.35 将上 v37）
- **容器**：已重建 Healthy，alice 6 作品全带 width/height

---
## 当前阶段：v0.34 收工待体验，v0.35 需求已就绪

### v0.34 合入清单（用户 8 项反馈全修）

| # | 反馈 | 修复 |
|---|------|------|
| 1 | ghost 按钮框看不见 | 半透明底 + text-dim 边框（亮暗均可辨） |
| 2 | 封面图被切掉 | contain 不裁切 + 点击放大（对齐 TplTierGrid） |
| 3 | 空间浪费 + 款式不能点 | 尺寸行可点选 ✓ 高亮 + padding 收紧 |
| 4 | 流程模块没优化 | 重做：编号节点 + 连接线 + 收款百分比徽章（横竖双形态） |
| 5 | emoji 全删 | locales 132 处 + 客户端 + 画师/管理后台（换 EP SVG；✓✕等功能符号保留） |
| 6 | 404 丑 + 主题按钮逻辑不一致 | 独立 NotFound.vue + 复用右下角浮窗 ClientFloatingActions |
| 7 | 作品顶位置 | demo-data 用 sharp 写 width/height + 存量回填 |
| 8 | 档位图不是后台设置的 | 封面/例图上传即时保存（编辑→立即 PUT，失败回滚；新建→提示确定后生效） |

**附带**：URL query 预选（主页选画风/尺寸 → 下单页直接预选，query > 草稿）+ 7 个新测试。

### 用户待体验清单（v0.34）

1. **客户端 alice**：价格表点尺寸行 → ✓ 高亮 + 引导提示 → 点「选择此画风约稿」→ 下单页已预选（选齐直接进增项步）
2. 封面图完整显示不裁切，可点击放大
3. 「约稿流程与收款」新视觉：编号圆点 + 连接线 + 30%/70% 徽章
4. hero「查询进度」按钮轮廓可见（亮暗主题都试）
5. **404**：随便访问一个不存在的路径
6. 全站无 emoji（客户端 + 画师后台；画师后台图标换成线条 SVG）
7. **画师后台**：编辑画风上传封面 → **不用点确定立即生效**（你 22:14 上传的 3 张图在磁盘上但没关联，需重新设一次；新图上传即保存）
8. 客户端作品展示加载不跳动（尺寸已回填）

**注意**：alice 目前只有 1 个画风「默认」——「厚涂插画」在你白天体验时被停用了（is_active=0，公开接口只显示启用画风）。想看多画风展示柜，进后台画风管理把它重新启用。

### 已知遗留

- 浅色主题 hero 标题压画作暗部对比度低（二号观察，排小修）
- 编辑画风上传封面后点「取消」不回滚封面（与 R48 头像一致，预期行为）
- 用户 22:14 的 3 张孤儿图（无 DB 引用）等 gcUploads 自动清理，未手动删

---
## 各角色状态

- **三号**：空闲。v0.35 全部完成（波 1 `3e2a1af` + 补漏 A1-A5 + 波 3 `06a8ef6`）
- **二号**：空闲。v0.35 全部完成（波 2 mock `8598729` + 联调 `06a8ef6`，净删 110 行 mock）。遗留观察：ArtistHomeGallery.vue 未传 layout（masonry 非 editorial，历史遗留非本次引入），待用户定夺
- **四号**：空闲。REQ-025 动态计价模型审核通过排 v0.37（`ddb3cc5`）。v0.36 负责 changelog 补写 + docs 归档
- **五号**：空闲。交付卡死修复已合入（`06a8ef6`，方案 B：deliver-no-file 端点 + DeliverDialog 共享组件 + 看板直接弹窗，708/708）。遗留观察待拍板：看板下拉 delivered 项是否统一走 deliver-no-file。**待用户拍板只剩 BUG-1 分期双模型方案 a/b（倾向 b）**

---
## 当前阶段：v0.35 执行中（波 1+波 2 并行）

| 波 | 角色 | 分支/worktree | 内容 |
|----|------|--------------|------|
| 1 | 三号 | **已合入 `3e2a1af`** | 迁移 v37（生产库副本实跑验证，carol 逐画师 NOT EXISTS 守卫兜住 v36 全局守卫漏洞）+ F1/F2/F5/F6 数据层 API + 画师后台 F2 合并入口 + gcUploads 补收 cover_image。703/703 |
| 1补+3 | 三号 | **已合入 `06a8ef6`** | 补漏 A1-A5（删旧增项 tab + AddonManager 删除；A2 菜单文案 master 已改 no-op；双层 vuedraggable 拖拽排序；+导入增项入口；增项即时保存防抖+回滚）+ 波 3 F6 后台 UI（作品编辑弹窗：标题+自由描述+档位标注多选） |
| 2 | 二号 | **已合入 `8598729`** | F3/F4/F6 mock-first（含一号审核补 hover 点击死区 `6bca653`） |
| 2联调 | 二号 | **已合入 `06a8ef6`** | 删 mock 换真实 API（净删 110 行）：F3 直读 styles 扩展字段（artwork_image_path）、F6 走 gallery 独立端点（size_tags 对象数组/filterSizes 后端门控）、TplStyleGrid artworks prop 裁剪。浏览器实测全过 |
| 插队 | 五号 | **已合入 `06a8ef6`** | 工作流订单最后节点交付卡死修复（方案 B，用户报障）：deliver-no-file 端点 + DeliverDialog 共享组件 + 看板直接弹窗 + 详情页 done 补入口，TC-RT-24~27，708/708 |

**v0.35 状态**：F1-F6 全部合入，master `06a8ef6`，server 708/web 144 全绿。容器重建后等用户终验（体验清单见下）。

**⚠️ v38 迁移事故（2026-08-04 深夜，一号自查发现并已修复）**：一号为 BUG-8 第三项写的迁移 v38（重建 artists 表改 CHECK）首次执行时**清空了所有子表数据**——根因：迁移运行器把迁移包在事务里，而 `PRAGMA foreign_keys` 在事务内是 no-op，导致 FK 关闭防护失效，DROP artists 触发子表 CASCADE。数据已从 bak.v38 自动备份完整恢复（6 画风/11 尺寸/21 作品/4 订单，端到端验证通过）。修复：运行器加 `noTransaction` 支持 + v38 事务外 12 步重建 + fkState 前置校验 + foreign_key_check 双保险 + TC-MIG-38 回归测试（`75f1b5c`，710/710）。教训：**任何 DROP/RENAME 父表的迁移必须事务外执行并显式关 FK**。

**并行契约**：locales 三号只动 styleManage/tiers 命名空间，二号只动 artistHome/orderForm/gallery。F5 迁移高风险，合入前按大型迁移清单逐项审。

**2026-08-04 四号两轮补丁处置**：① 用户拍板旧「增项」tab 随旧档位退役（旧增项数据废弃不迁移）——已 patch 波 1 派工任务 3 第 4 条 + 菜单「档位」→「价格」；SPEC-025 通过审核为 v0.35 后台验收基线。② 四号 UX 直觉审计 4 项（行号全核实）：前 3 项（画风/尺寸拖拽排序、已有画风追加导入增项、增项保存统一即时保存）已并入波 1 派工任务 7/8/9；第 4 项（订单筛选缺 revision 档，OrderList.vue L20-28）为小修，随下批。**ManualOrder.vue（手动录单）仍消费旧 tiers+addons 计价**——F5 后旧表数据保留，功能不坏，归 v0.36 清账版，不扩波 1 范围。

v0.34 待体验清单见上一版 STATUS git 历史（容器已重建，8 项：尺寸预选/封面不裁切/流程新视觉/ghost按钮/404/无emoji/封面即时保存/作品不跳动）。

**五号交付已处理（2026-08-03 深夜）**：时间条「拖不动」根因 = 演示订单 deadline 全 NULL → 整条平移被禁用（REQ-019 设计要求有截稿日才能整条拖）。一号已在 master 直接修复 `1d11f37`：demo-data.ts 给 ALICE-001~003 补 deadline（start_date +7/+10/+14 天，done 终态不补），容器内已重跑生效——排期看板现在抓横条中段可整条平移，左右端 handle 也正常。非代码回归，是演示数据缺口。

---
## 版本计划（用户 2026-08-03 拍板 + 一号定版本数）

| 版本 | 内容 | 状态 |
|------|------|------|
| v0.34 | 8 项体验反馈 | ✅ 全部合入，容器已重建待体验 |
| **v0.35（最后功能版）** | REQ-024 画风档位统一 **F1-F6 全量**（档位带图/后台合并+多画风开关/点档位切大图/下单预选/旧模型迁移 v37/作品档位标注+画廊筛选 F6） | 需求全文审核通过，**待用户体验 v0.34 后派工** |
| **v0.36（清账版）** | 全部旧账：changelog/docs 归档/snake_case 统一/P2 剩余/测试补缺/4 模板走查调整 | v0.35 后 |

做不完加 v0.37，不硬撑（用户授权加班但不压缩质量）。

**v0.35 三波拆解（预排，派工时细化；F6 并入后体量增大用波次化解，不砍范围）**：
- **波 1（三号）**：v37 迁移一次建全——style_sizes 加图/描述/天数字段、artists 加多画风开关字段、artworks↔style_sizes 多对多关联表（F1 从作品集挑图 + F6 档位标注共用，避免二次迁移）、artworks 加自由描述字段；+ 画风带图 API + 后台「画风与价格」合并入口 + 多画风开关 + F5 旧模型迁移脚本（showcase 直接丢弃）
- **波 2（二号）**：客户端展示柜 F3 点档位切大图 + F4 下单预选（在 v0.34 query 预选基础上扩展入口区分与跳第三步）+ F6 画廊筛选标签 + 大图档位标签跳下单
- **波 3（三号）**：后台作品管理扩展——档位标注多选 + 自由描述字段编辑（F6 后台部分）
- F5 迁移为高风险项，合入前按大型迁移清单逐项审（自动备份/幂等/老数据映射/级联）
- **关键依赖**：v0.34 已把 query 预选基建合入，v0.35 波 2 在其上扩展，不返工

---
## 重要规则提醒

- 合并到 master 后**立即推送**；操作前 `git log --oneline -5` 确认 HEAD
- 禁止对 master `git reset --hard` / `git rebase`；禁止 `git add -A`
- 并行角色必须在独立 worktree；Docker 环境 SQLite 用 DELETE 模式
- 幂等脚本「清理」与「种子」显式区分归属（v0.33 教训）
- **上传类交互即时保存模式**（v0.34 确立）：编辑已有实体时上传成功立即 PUT，失败回滚预览；新建时提示「确定后生效」。后续新增上传交互一律照此模式

---
## 分支状态

| 分支 | Worktree | 状态 |
|------|----------|------|
| master | 主 worktree（一号专用） | ✅ 干净 |
| feat/v035-style-unify-backend | wt-03 | 三号波 1 在途 |
| feat/v035-client-gallery | wt-02 | 二号波 2 在途 |

comms 只剩 STATUS.md + 两份在途派工（01-to-03-v035波1 / 01-to-02-v035波2）。

残留：`artist-commission-02`、`artist-commission-p0` 磁盘目录为旧 worktree 残留（git 记录已清），无害。根目录 `tmp-diag*.cjs` 疑似五号排查产物，保持关注。

