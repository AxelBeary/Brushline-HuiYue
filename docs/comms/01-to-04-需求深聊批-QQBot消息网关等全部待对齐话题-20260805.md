# 派工：四号需求深聊批——全部待对齐话题（与用户逐题深入交流）

> 一号派工 · 2026-08-05 · 风险等级：低（纯需求整理，零代码）
> 用户指示（原话）：「还有qqbot（或者说消息网关），所有要办的全部写comms丢四号 让他和我深入交流」
> **工作方式**：与用户逐题深入交流（四号工作原则：能内部解决的技术核实别转交一号，只把真正需要用户拍板的决策项呈现），每题聊透后输出/更新需求稿到 docs/requirements/，转交一号审核排期。
> **不需要 worktree**：本任务产出是需求稿，直接在主仓 docs/ 工作；需求稿文件写完转交一号，由一号审核合入（四号不自行 commit 推送）。

## 话题 1：QQ Bot / 消息网关（用户点名，优先聊）

**现状实锤（一号已查证，不用重新侦查）**：
- `auth.service.ts` L63：`// TODO Phase 2: 接入 QQ Bot (NapCat/OneBot) 发送登录码`——**全项目唯一 TODO**，上线前必做清单（待修复问题清单 L4）
- `AUTH_DEV_MODE=*** 时登录码直接显示在网页/日志——上线前必须关（H1），关掉的前提就是 QQ Bot 先接上（否则画师拿不到登录码）
- `orders.client_notify` 字段已就位（客户下单时勾选「是否允许QQ排队提醒」），**但无任何消费方**——通知链路缺失
- REQ-014 桌面版登录设计依赖 QQ Bot 发码（NapCat, OneBot 11 WebSocket）

**要和用户深入对齐的点**：
1. **定位**：就叫「QQ Bot」还是抽象成「消息网关」（未来可能接微信/Telegram/邮件等渠道）？用户原话是「qqbot（或者说消息网关）」——倾向先做网关抽象还是 QQ 直连够用？
2. **通知场景范围**：① 登录码发送（必做）② 客户排队提醒（client_notify 消费）③ 客户进度变更提醒（订单状态推进时）④ 画师侧提醒（新订单/催收款/截稿临近）——哪些进 v1？
3. **技术路线**：NapCat/LLOneBot（OneBot 11 协议，开发自参考已备案）+ 部署形态（独立 Docker 容器？用户本机跑？）——QQ Bot 需要挂一个 QQ 号，用谁的号、怎么托管，是产品问题也是运维问题
4. **风控与合规**：QQ 个人号 Bot 有封号风险，用户对风险的容忍度；消息频率限制
5. **版本归属**：登录码发送是上线门槛（AUTH_DEV_MODE 关闭的前提），必须排进上线前；其他通知场景可以分批

**产出**：需求稿 docs/requirements/REQ-0XX-消息网关.md（含场景清单、优先级、技术方案选型、部署方案、风控说明）

## 话题 2：ManualOrder 接画风需求细节（用户已确认要做，已排期）

用户 2026-08-05 确认「手动录单接画风是需要的」，一号已排期：REQ-025 二阶段 A 路合入后作为 D 路开发。**本话题聊需求细节，聊完立即出稿开工**：
1. 档位模式与画风模式如何共存：多画风开关开启后，录单页的 UI 形态（选画风→选尺寸→增项，对齐客户下单页 OrderForm 的结构？）
2. 录单算价预览：选了画风尺寸后是否实时算价（对齐 OrderForm 的 doCalc 逻辑）
3. 初始节点状态（F4 已有功能）在画风模式下的交互是否一致
4. 手动录单的 source='manual' 订单走引擎后，base 条目语义与客户自助下单一致（无特殊处理？还是有差异？）

**产出**：更新 REQ-015 归档文档的补充需求 或 新建需求稿，供一号派 D 路

## 话题 3：REQ-022 四个待确认（F1 作品链路开工前必须定）

1. 完稿转作品时，是否允许画师选择部分图片（不是全部完稿图）？
2. 作品绑定档位：一个作品能绑多个档位吗？
3. 外链平台列表：硬编码 5 个主流平台（微博/B站/小红书/推特/其他）+ 可配置？
4. 回收站分页还是折叠？

**产出**：把拍板结果写回 REQ-022 文档的「待确认问题」节

## 话题 4：v0.38 视觉重设计需求稿（设计 brief → 开发需求）

设计方向用户已认可（纸墨颜料盘，docs/画师工作台视觉提案-v2.html）。现有三份文档：
- docs/画师工作台视觉提案-v2.html（视觉方向）
- docs/画师后台视觉规范-v1.md（7 色语义规范）
- docs/design-brief-画师后台视觉重设计.md（开发 brief）

**任务**：整理成完整的 REQ-026（v0.38 需求稿）——把视觉规范翻译成开发可执行的功能/交互清单：每个页面/组件的改造点、双主题（宣纸/墨黑）切换交互、开稿/截稿日二合一 picker 的行为定义、霞鹜文楷/思源黑体的应用边界、迁移策略（一次性全量换还是分页面灰度）。**注意用户原则：外部设计提案=视觉语言参考，不是功能规格——功能以现有系统+REQ 为准。**

**产出**：docs/requirements/REQ-026-画师后台视觉重设计.md

## 话题 5（备案，不急）：REQ-014 桌面端伴侣应用

v1.0+ 远期，细节迭代中。仅当用户主动提时再深聊，不主动展开。

## 交付与验收

1. 每个话题聊透后落一份/更新一份需求稿，写清：用户拍板记录（日期+原话要点）、规则条款、边界案例、验收标准
2. 全部需求稿整理完后统一转交一号审核（不要边聊边合入）
3. 优先级建议：话题 1（QQ Bot，上线门槛）→ 话题 2（ManualOrder，已排期等稿）→ 话题 3（REQ-022）→ 话题 4（v0.38）
