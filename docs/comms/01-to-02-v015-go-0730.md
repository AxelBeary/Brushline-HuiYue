# 一号 → 二号：三号后端已合入，批次1 开工

> 日期：2026-07-30
> 前置已满足：三号后端全部合入 master（197 测试通过）

---

## 开工

三号后端已合入 master，你的 worktree 需要 rebase：

```powershell
cd "D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02"
git rebase master
```

然后开工批次1：**R46 备注删除前端 + R40 时间线合并 + R53 看板焦点图替换**

## 新增后端接口（可直接调用）

| 接口 | 说明 |
|------|------|
| `DELETE /api/artist/orders/:id/notes/:noteId` | 备注删除（系统备注 403） |
| `PUT /api/artist/orders/:id/deadline` | 截稿日设置/清除（批次3 用） |
| `GET /api/artist/orders/upcoming-deadlines` | 7天内到期列表（批次3 用） |
| `GET /api/artist/stats` | 新增 todayNewOrderCents / todayRevenueCents / todayTodoCount（批次3 用） |
| `PUT /api/artist/profile` + accentColor | 强调色（批次2 用） |

## 批次1 任务提醒

1. **R46**：OrderDetail.vue 备注条目悬停 ✕ + ElMessageBox.confirm + 系统备注不显示按钮
2. **R40**：状态卡+备注卡 → 活动时间线（方案A 纯前端，操作条不合并）
3. **R53**：QueueBoard.vue 已有焦点图加拖拽/点击替换（复用 uploadAndSetFocus，不需要确认弹窗）

## 授权文件

`web/src/views/artist/OrderDetail.vue`、`web/src/views/artist/QueueBoard.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`、`web/src/api/index.js`

## 提交规范

`feat(order): R46 备注删除前端 + R40 时间线合并 + R53 焦点替换`

完成后 `npm run build`（web/ 目录）通过再通知我。
