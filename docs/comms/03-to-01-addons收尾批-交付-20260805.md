# 03→01：addons 清理收尾批交付（schema 删除 + 死码清理 + DROP 迁移 v43）

> 日期：2026-08-05 · 执行：三号 · 分支：`addons-final` · commit：`802af46`（基于 master `25e410c`）
> 派工：`docs/comms/01-to-03-addons收尾批-20260805.md` · 用户已拍板 DROP

## 任务清单完成情况

**1. schema 删除**
- POST /api/orders（order.routes.ts）：删 addons 字段 + 解构 + createOrder 传参
- 手动录单（order.routes.ts L450 区）：同上三处
- POST /api/public/calculate-price（pricing.routes.ts，实际路径非派工写的 /api/pricing/calculate）：删 addons 字段 + 解构 + 传参

**2. createOrder 参数清理**
- order.service.ts：CreateOrderParams 删 addons、函数解构删 addons、档位模式 calculatePrice 调用删 addons
- pricing.service.ts：CalculatePriceOpts 删 addons 字段，注释更新（v43 已 DROP）

**3. 死码清理**（grep 确认零业务引用后删）
- errors.ts：ADDON_NOT_FOUND/ADDON_MAX_QTY/ADDON_NOT_FOR_TIER 错误码 + 中文消息
- entities.ts：Addon interface
- fastify.d.ts：requireOwnAddon 死注释 + addon: any 字段

**4. DROP 迁移 v43**（守 v38 事故规则）
- `noTransaction: true` + `PRAGMA foreign_keys = OFF` → 回读校验 =0 才继续（不为 0 抛错中止）→ DROP addon_tiers + price_addons → `foreign_key_check` 零悬空验证 → finally 恢复 FK
- 迁移注释写明冻结依据（零写路径/存量 1 行垃圾/0 订单引用/无下游 FK）
- **关键修复**：initDatabase 每次重跑都会执行 `schema` 常量，原 schema 含 `CREATE TABLE IF NOT EXISTS price_addons/addon_tiers`——v43 DROP 后重跑会把表**重建回来**（测试 TC-MV-02 实测抓到）。已从 schema 常量删除两表定义。全新库由 v36 历史迁移建表 → v43 DROP，顺序正确；已发布迁移 v36 内容未动

**5. 迁移前备份**
- `data/commission.db.bak-pre-v43`（331,776 字节，better-sqlite3 只读打开 + backup API，WAL 一致性，2026-08-05 18:04）
- 容器部署时一号重建前自行确认备份

## 与派工预期的差异（技术判断修正）

派工预期"删后 ajv additionalProperties:false 天然拒收 addons → 400"**不成立**：项目 Fastify 默认 `removeAdditional=true`，未知字段**静默剥离而非 400**（routes.test.js L272 / style.test.js L669 / publish-artwork.test.js L256 多处现有测试已实证该配置）。按派工授权"按实际行为定断言"，回归测试改为断言「请求成功 + addons 被剥离、价格不含增项（totalPriceCents=20000 / addonTotal=0）」。

## 测试基线对比

| 项 | 基线 | 本批 | 说明 |
|----|------|------|------|
| server 测试 | 897/897（53 文件） | **903/903（55 文件）** | 新增 addons-reject 3 例 + migration-v43 3 例；TC-P-16/17 改写（契约已无 addons） |
| lint | 0 | 0 | `npm run lint` 零输出 |
| tsc | 0 | 0 | `npm run typecheck` 零输出 |

新测试：
- `addons-reject.test.js`：TC-AR-01 POST /api/orders 带 addons → 200 且价格 20000 分（无增项）；TC-AR-02 calculate 带 addons → 200 且 addonTotal=0；TC-AR-03 不带 addons 正常（回归）
- `migration-v43.test.js`：TC-MV-01 v43 已应用且两表不存在；TC-MV-02 幂等（重跑 initDatabase 不报错且两表仍不存在——抓出 schema 重建 bug 后通过）；TC-MV-03 price_tiers 活表不受影响

## 验证证据

- `npx vitest run`：55 文件 903/903 通过，87.20s
- `npm run lint`：exit 0 零输出；`npm run typecheck`：exit 0 零输出
- commit `802af46` 已落分支，未推送未合并
- 备份 `data/commission.db.bak-pre-v43` 已生成（主仓 data/）

—— 三号
