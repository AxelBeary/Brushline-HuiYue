# 三号 → 一号：v0.28 REQ-016 后台界面逻辑重组

> 日期：2026-08-02
> 分支：`feat/v028-req016-restructure`
> Worktree：`artist-commission-03`

## 做了什么

### A. 设置页 4 Tab 重划

旧：基本资料（12 字段混装）/ 接稿设置（空壳）/ 主页模板 / 须知编辑
新：

| Tab | 内容 |
|-----|------|
| 基本资料 | 头像、昵称、编码、简介、联系QQ（5 字段） |
| 主页展示 | 公告、外链、平台链接、灵感标签、须知（独立卡片+独立保存） |
| 模板与风格 | 模板选择、配色、强调色、封面图、预览 |
| 偏好 | 通知开关、默认面板、快捷按钮（独立卡片+独立保存） |

- 删除「接稿设置」空壳 Tab
- 须知编辑并入「主页展示」（独立卡片，保留独立 saveRules）
- save() 按当前 tab 拆分提交（后端 PUT /api/artist/profile 为部分更新语义，已读后端确认）
- 清理 Settings.vue 中已归 SlotManage 的死字段（status/slots/quota/queue 相关 10 个）
- 旧 tab 名兼容映射：`?tab=rules` → showcase、`?tab=commission` → prefs（SlotOverview.vue 的跳转不断链）
- `/rules` 路由重定向改指 `?tab=showcase`

### B. 接稿状态归位

- 仪表盘 StatusSwitch：**保留不动**
- 开稿管理 SlotManage.vue：只读卡片 → radio-button 即时切换（与仪表盘同逻辑，含失败回滚）
- 设置页：删除接稿状态单选框（不再重复）

### C. 侧边栏瘦身

- 手动录单 `/orders/new` 移出菜单（路由保留，订单管理页按钮+快捷操作区入口不变）
- 菜单分三组：工作（仪表盘/排期/订单）· 经营（开稿/价格/作品/留言）· 门面（设置）
- 管理员后台入口归入「门面」组
- 侧边栏 + 移动端抽屉同步分组
- 删除 DocumentAdd 图标导入（不再使用）

### D. app.js Windows 路径修复

- `filePath.startsWith(WEB_DIST + '/')` → `filePath.startsWith(WEB_DIST + sep)`
- 新增 `sep` 到 path 具名导入
- 五号发现：Windows 下 resolve() 产生反斜杠，原写法永远 false，本地 E2E 全挂

## 改了哪些文件

| 文件 | 变更 |
|------|------|
| `server/src/app.js` | D: sep 导入 + startsWith 兼容（2 行） |
| `web/src/views/artist/Settings.vue` | A: 4 Tab 重划 + save 拆分 + 死字段清理 |
| `web/src/views/artist/SlotManage.vue` | B: 状态可操作（radio + updateStatus + 回滚） |
| `web/src/components/ArtistLayout.vue` | C: 菜单分组 + 手动录单移出 + 分组标题样式 |
| `web/src/router/index.js` | A: /rules 重定向改指 showcase |
| `web/src/locales/zh-CN.js` | 新增 tabShowcase/tabPrefs/groupWork/groupBiz/groupFront/statusSection；删 tabCommission/goDashboard/movedHint |
| `web/src/locales/en.js` | 同上英文版 |

## 接口变更

无。纯前端 UI 重组 + 1 行后端路径修复。PUT /api/artist/profile 未改动。

## 数据库变更

无。

## 验证结果

- ESLint：0 错误 0 警告
- Build：✅ 成功（10.87s）
- 前端测试：87/87 通过（5 文件）
- 后端测试：567/567 通过（34 文件）
- git diff --stat：7 文件，全部在授权范围内

## 注意事项

- `settings.tabWorkflow` 保留（ArtistDetailDrawer.vue 管理后台在用，不在三号授权范围）
- `settings.statusLabel/statusOpen/statusFull/statusBreak` 保留（ArtistDetailDrawer + SlotManage 在用）
- SlotOverview.vue（仪表盘组件，不在授权范围）的 `?tab=commission` 跳转通过别名映射兼容，无需改动
