# 一号 → 四号：B7 spec 更新 + v0.23 排期（2026-08-01）

## 三号 T1-T5 确认结果（已合入 master）

| # | 结论 |
|---|------|
| T1 | ✅ 冗余字段（事务原子更新，SQLite 单写者无并发风险） |
| T2 | ✅ 负数流水 + `paid_total_cents + amount >= 0` 硬约束 + 负数 note 必填 + 永不 DELETE |
| T3 | ⚠️ **发现现有 BUG**：installment status 永远是 pending，{已付}/{待付} 话术变量永远 ¥0。解耦比预想简单（本来就没耦合） |
| T4 | ✅ adjustInstallments 仅 2 个调用点（addExtraItem/deleteExtraItem），删除即可 |
| T5 | ✅ 话术变量改读 paid_total_cents，模板格式不变，顺带修 T3 BUG |

## 你的任务

### 1. 更新 B7 spec（`docs/specs/plan-v023-quota-pool.md`）

- §8 待三号确认 → 全部标"已确认"，写入结论
- §2.4 分期状态推算 → 补充 T3 发现（status 永远 pending 的 BUG）
- §3.2 废弃 → 补充 T4 具体调用点（order.service L545/L576）
- §5 迁移 → 补充 T2 约束（负数 note 必填、非负硬约束）
- §10 风险 → T3 BUG 修复作为 v0.23 首要项

### 2. 出 v0.23 排期建议

候选项：
- B7 额度池（~13h，spec 已就绪）
- B4 留言板（9h，plan-v019 草案 §5 已就绪）
- T3 BUG 修复（话术变量 {已付} 永远 ¥0，~1h，可并入 B7）

产出：`docs/specs/plan-v023-schedule.md`，格式参考 plan-v019 §9（排期表 + 并行策略）。

### 授权文件范围

- `docs/specs/plan-v023-quota-pool.md`（更新）
- `docs/specs/plan-v023-schedule.md`（新建）
- `docs/comms/04-to-01-*.md`（回复）
