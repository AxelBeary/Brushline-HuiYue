# 四号 → 一号：交互直觉审计（UX-20260804）——4 项发现，3 项建议并入波 1

> 日期：2026-08-04
> 来源：用户要求"排查哪些操作不够好、不够直觉"，四号读代码逐项核实（行号为证），非猜测
> 落盘：`docs/待修复问题清单.md` UX-20260804 节 + `docs/specs/SPEC-025-价格管理后台规划.md` 第四节勘误/第五节缺口

## 4 项发现

| # | 问题 | 证据 | 建议归属 |
|---|------|------|---------|
| 1 | **画风/尺寸无法排序**：后端 sort_order 更新 API 已存在（style.service.ts L253/L338），但 ArtStyleManager.vue 无 draggable，前端没有排序入口 | ArtStyleManager.vue 全文无 draggable | **波 1 必须补**——REQ-024 F2"默认画风=排序最前的启用画风"，画师不能排序就无法控制谁是默认画风 |
| 2 | **已有画风无法追加导入增项**：importAddons 勾选只在新建弹窗显示，画风卡片增项区只能对已导入项 启用/禁用/改价 | ArtStyleManager.vue L158 `v-if="!editingStyleId"` | 波 1 增项区补"＋导入增项"入口。不补则 SPEC-025 动线 5（加新料→在画风里导入）断头 |
| 3 | **增项保存心智不统一**：勾选/改价要点"保存增项"按钮，尺寸覆盖每行又是独立保存按钮——同卡片两套心智，且与 v0.34 确立的"上传即时保存"原则不一致 | ArtStyleManager.vue L97（保存增项按钮）、L125（覆盖行内保存） | 波 1 重构时统一为即时保存（同封面/例图模式） |
| 4 | **订单筛选缺"修改中"档**：DB 状态有 7 个（含 revision），筛选条只有 6 档，revision 订单只能靠搜索找 | OrderList.vue L20-28 筛选按钮 vs L128 ACTIVE_STATUSES 含 revision | 小修，随下个批次（不必波 1） |

## 申请

1. 第 1-3 项请一号评估并入波 1 派工（都在 ArtStyleManager 重构范围内，边际成本低；第 1 项与 F2 默认画风规则强相关，强烈建议必补）
2. 第 4 项排入后续小修批次
3. SPEC-025 第四节第 4 条已加勘误（原文"拖拽排序现状已有"不属实，已改为"后端有前端无"），请一号知悉
