# 05-to-01-用户反馈批-交付-20260807

> 交付：五号 → 一号（bugfix）——**一号代提交**（五号会话撞迭代上限，BOM 事故由一号修复后收尾）
> 分支：`beta/user-feedback`（worktree w5）
> 对应派工：`docs/comms/05-派工-用户反馈批-20260807.md`

## 一、BOM 事故说明（诚实披露）

五号执行期间尝试"去 BOM"时把 5 个文件的换行符破坏（首行 `<` 丢失 + 2 个文件被压成单行 + 2 个文件标签错位）。会话撞迭代上限后交付中断。**一号接手修复**：从 master 恢复损坏文件 + 按五号已验证的改动内容重新应用（QuickActions/ArtistLayout/SlotOverview/StatCards/Dashboard 5 文件），lint 0 errors + web 215/215 全绿后提交。

## 二、改动清单（修复后最终版）

| 文件 | 改动 |
|------|------|
| `web/src/components/artist/dashboard/QuickActions.vue` | **任务1+2**：状态卡去「状态切换」四字直接显示状态名（偏好设置保留）；删除常驻虚线块，拖图/粘贴能力并入「快速发作品」卡片（点击跳转 /artworks、拖图直接发布+发布中高亮态）；v-model:is-gallery-drag-over 等价保留 |
| `web/src/components/ArtistLayout.vue` | **任务4**：克制动效——collapse/lang/mobile-menu/nav-item/logout 按钮按压 scale(0.98) + 0.15s ease-out；移动端抽屉淡入+上移 0.18s（替代 EP 侧滑）；`.artist-scope` 前缀提升特异性（修复全局规则覆盖 scoped transition 的存量问题） |
| `web/src/components/artist/dashboard/SlotOverview.vue` | **任务4**：slot-card hover 微浮起 -2px + 阴影、按压 scale(0.98) |
| `web/src/components/artist/dashboard/StatCards.vue` | **任务4**：stat-card hover -2px + 阴影（原 -3px 收紧）、按压 scale(0.98) |
| `web/src/views/artist/Dashboard.vue` | **任务3+4**：留言审核按钮按压动画；布局微调（QuickActions 区压缩 240→162px，消除常驻块冗余） |

## 三、验证（五号实测 + 一号复跑）

- 任务1：状态卡 DOM 文字仅剩状态名，切换循环正常（点击后 已排满→可约稿 来回）✅
- 任务2：常驻块 DOM 消失（quickPublishExists=false）；快速发作品卡点击跳转 /artworks；拖图直接发布（合成 DataTransfer+File 实测 0→1，随后清理）；发布中显示「发布中…」+ 高亮 ✅
- 任务4：hover/按压/抽屉动画实测生效；无弹跳旋转；时长 ≤0.2s；prefers-reduced-motion 兜底存在 ✅
- **一号复跑**：lint 0 errors · web test 215/215 ✅

## 四、说明

- 偏好设置里「状态切换」字样**保留未动**（用户要求）
- 截图：五号实测期间截图存 `workspace/temp/`（用户反馈批相关），一号代提交未再补截图——如需补 visual 门禁截图可再执行
- 未推送、未合并、未改 STATUS；合入由一号执行
