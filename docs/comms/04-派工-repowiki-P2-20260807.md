# 04-派工-repowiki-P2-20260807

> 派工：一号 → 四号（requirements）
> 工作方式：**纯只读审计，零代码改动，不需要 worktree**——在主仓目录只读（不许 git 写操作），输出报告落 `docs/comms/`。
> 开工第一步：先读 `docs/comms/STATUS.md` 确认现状，再读 `docs/external-wiki/README.md` 与 `docs/external-wiki/repowiki-核对报告-20260806.md` 了解背景。

## 一、任务总览

repowiki P2：**其余 112 篇抽样检查认证系文档**（P0 已重写认证接口.md + 数据库模式设计.md，见 README）。核对报告建议：抽查 `API 接口文档\画师管理接口.md`、`管理员接口.md`、`认证授权系统\认证授权系统.md`、`TOTP 双因素认证.md` 等是否仍描述旧登录码/旧 JWT 流程。

## 二、文档位置与基准

- 原文（外部贡献，只读）：`C:\Users\qly19\Desktop\repowiki\`（约 118 篇，**原目录保留不删不改**）
- 仓库内已重写版本（对照基准）：`docs/external-wiki/wiki-认证接口.md`、`docs/external-wiki/wiki-数据库模式设计.md`
- 代码基准：master HEAD（当前 `2449b6b`，v0.40 收官）——认证已 TOTP 化（REQ-027）、后端 100% TS、数据库迁移 v43

## 三、检查方法

1. 在 `C:\Users\qly19\Desktop\repowiki\` 下定位认证/授权相关文档（文件名含 auth/认证/TOTP/登录/令牌/会话/管理员/画师管理 的 .md）
2. 每篇对照 master 代码核实（重点 grep）：
   - `POST /api/auth/verify`（TOTP 登录唯一端点，auth.routes.ts）vs 旧 `login`/JWT 描述
   - cookie 会话（httpOnly artist_token）vs JWT Bearer/刷新令牌描述
   - TOTP 绑定走管理员路由（admin.routes.ts bind-init/bind-confirm/reset）vs 客户端直绑描述
   - 错误码（NOT_LOGGED_IN/SESSION_EXPIRED/TOKEN_REVOKED）与锁定策略（TOTP_MAX_ATTEMPTS=5，15 分钟）
   - 文件引用名 .js vs .ts（TS 迁移）
3. 输出核对表：文档名 / 描述内容 / 代码实际 / 判定（🔴严重过时 / 🟡轻微 / 🟢正确 / ⚪缺失）

## 四、输出

- 报告写 `docs/comms/04-to-01-repowiki-P2-交付-20260807.md`：
  - 抽样了哪几篇、每篇判定
  - 🔴 严重过时清单（文档+过时段落+当前正确版本）——**供一号决策是否照 P0 模式重写**
  - 建议：哪些值得重写（P0 级）、哪些修补即可、哪些无需动
- 零代码改动，不 commit 任何仓库文件；报告由一号合入

## 五、纪律

- **只读**：不 `git add/commit/push/merge`、不碰 master、不建 worktree
- 外部贡献文档不直接改——只报告，重写由一号派单
- 判断以 master 代码为准，不信文档自我描述
- 20 分钟无产出停下报卡点
