# 一号 → 二号：R58 第二批（回收站 UI + R58-2 分步引导）
> 日期：2026-07-31
> 分支：`feat/v016-r58-batch2`
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt02`

## 任务A：回收站 UI（小任务，先做完）

三号后端已合入（`GET/DELETE /api/admin/recycle-bin`）。管理员页面增加回收站管理：

1. 管理员页面新增"回收站"区域
2. 展示文件列表：文件名、原始路径、大小、移入时间
3. "清空回收站"按钮（二次确认，提示不可恢复）
4. 空状态 el-empty

授权文件：`web/src/views/admin/**`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`（仅追加）

## 任务B：R58-2 分步引导布局

基于 R58-1 的 `useOrderForm` composable，将约稿页从单页表单改为三步引导：

1. **步骤一：选档位**——档位卡片列表，选中态弹性动画
2. **步骤二：写需求 + 上传**——描述输入 + 灵感标签快捷注入（R58-4 顺手做）+ 参考图上传
3. **步骤三：联系方式**——QQ + 昵称 + 须知同意
4. **粘性摘要卡**：右侧（宽屏）/ 底部（移动端）实时显示已选档位 + 价格
5. **步骤指示器**：顶部 ①②③ 进度
6. **小票二次确认**（R58-3 顺手做）：提交前弹出咖啡馆小票风格确认弹窗（锯齿边 CSS）
7. **复制约稿信息**（R58-5 顺手做）：提交成功页一键复制订单摘要
8. **移动端**：三步折叠为单栏，摘要卡移到底部

R58-4 灵感标签：画师设置页配置标签列表（先用硬编码默认标签，后续三号加字段），点击 tag 自动追加到描述输入框。

授权文件：`web/src/views/client/OrderForm.vue`、`web/src/composables/useOrderForm.js`、`web/src/styles/templates.css`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`（仅追加）

## 纪律

- 先做完任务A再做任务B，分两个 commit
- R58-2 是核心下单路径重构，提交前跑通全流程
- i18n 中英同步，只加不改
- commit 格式：`feat(client): 描述`
- 完成后更新本文件底部"完成状态"

## 完成状态

（二号完成后填写）
