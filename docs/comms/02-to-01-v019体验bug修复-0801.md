# 二号 → 一号：v0.19 体验 bug 修复交付（4 个）

> 日期：2026-08-01
> 分支：fix/v019-experience-bugs

## 修复明细

| Bug | 级别 | 文件:行 | 改动 |
|------|------|---------|------|
| 1 话术 placeholder 崩溃 | P0 | zh-CN.js:478 / en.js:478 | `{客户名}` 花括号被 vue-i18n 当 ICU 占位符解析（中文非合法标识符→SyntaxError）。用 `{'{'}` / `{'}'}` 字面量转义，渲染结果不变，中英界面保持中文原文 |
| 2 详细计价展开崩溃 | P0 | OrderForm.vue:133 | `pricePreview.installments.length` → `pricePreview?.installments?.length`（与 266/303 行对齐） |
| 2 同类排查 | — | ManualOrder.vue:255 | `pricePreview.value.totalPrice.toFixed(2)` → `(pricePreview.value.totalPrice ?? 0).toFixed(2)`（totalPrice 为 undefined 时同样崩溃） |
| 3 亮暗/中英按钮位置反直觉 | P2 | OrderForm.vue:465 / TrackOrder.vue:309 | `.page-prefs` 从 `absolute top:16px right:16px` 改为跟随页面头部布局（`max-width` 对齐内容区 + `flex justify-content:flex-end`），ThemeToggle 与 el-page-header 同行右对齐 |
| 4 看板"开始制作"报错 | P1 | QueueBoard.vue:103 | `v-else-if="nextAction(...)"` → `v-else-if="element.currentStageId == null && nextAction(...)"`，工作流订单不再穿透到固定状态按钮 |

## Bug 2 全局排查结论

OrderForm.vue 所有 `pricePreview.` 访问：133 行是唯一漏网（其余在 `v-if="pricePreview"` 守卫内或已有 `?? 0` 兜底）。ManualOrder.vue 发现 255 行同类问题（已修）。两文件无其他遗漏。

## 验证

- `npx eslint .`：零错误零警告
- `npx vitest run`：全绿（exit 0）
- `npm run build`：✓ built in 3.98s
- 浏览器实测待安排（4 个 bug 的交互路径：话术输入框 / 计价展开 / 按钮位置 / 看板工作流订单）
