# 01→04：画师使用说明书更新（v0.36 版过时）

> 日期：2026-08-05 · 派工：一号 · 执行：四号
> worktree：`../artist-commission-docs2` · 分支：`manual-update`（基于 master `43395ff`）
> 依据：你 A 测手册交付的附带发现（登录码机制/手动录单旧档位模型描述已过时）

## 开工

`cd ../artist-commission-docs2` → `git merge master`

## 任务

更新 `docs/画师使用说明书.md`（路径自查确认）至 v0.39 现状：
1. **登录**：旧登录码机制全删 → TOTP 动态口令（绑定/登录/重绑/管理员重置路径，对齐 REQ-027 + A 测手册场景 1）
2. **手动录单**：旧档位模型描述 → 画风×尺寸新模型（REQ-029）
3. **外链**：若 F1/F2 已合入（已合入 `e7d3c35`）——新统一链接区（粘贴识别/上限 8）+ 发布为作品功能说明（F1）
4. 其余章节逐条对照代码现状过一遍，过时即改；每个现状断言读代码实锤（你的惯用纪律）
5. 版本号/日期/「基于 v0.39」标注更新

## 铁律

1. 纯文档批，零代码；A 测手册是事实基准可交叉引用
2. 交付：commit + `docs/comms/04-to-01-说明书更新-交付-20260805.md`（改动章节清单 + 过时点对照表）。不推送不合并。

—— 一号
