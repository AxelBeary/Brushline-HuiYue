# 二号 → 一号：v0.15 收尾批次提交审核

> 日期：2026-07-30（晚间）

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/v015-ux-batch`（worktree: `D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02`）

【任务编号】
v0.15 收尾：R57 约稿页表单防丢失 + UI-9 图库闪烁修复

【修改模块】
客户约稿页（OrderForm.vue）+ 订单详情备注附图（OrderDetail.vue）+ i18n

【修改内容】

**R57 表单防丢失**
1. **beforeunload 拦截**：表单有内容时（档位/描述/QQ/昵称/增项任一非空），关闭/刷新页面弹浏览器原生确认
2. **sessionStorage 草稿**：表单变化 debounce 500ms 写入 `orderForm_draft_{subdomain}`（按画师隔离，多画师不串）；表单清空时自动删除草稿
3. **恢复确认**：onMounted 检测到草稿 → ElMessageBox"检测到未提交的草稿，是否恢复？"（恢复/丢弃）；丢弃即删草稿
4. **档位失效防护**：恢复时校验 tierId 是否仍在画师档位列表中（画师可能已删档位），失效则丢弃该字段及关联增项/倍率
5. **提交成功清除**：removeItem + 移除 beforeunload 监听（提交后跳转不再拦截）
6. **onUnmounted 清理**：移除 beforeunload 监听 + 清除 debounce timer（SPA 路由切换不泄漏）
7. 参考图不存草稿（已上传文件无法安全恢复）——恢复的是文字字段 + 增项/倍率选择

**UI-9 图库闪烁**
- 图库区 el-image 骨架屏 + `.ref-img` 背景色 **R43 已完成**（核实过代码），本次只补 `.note-thumb` 背景色（`var(--bg-secondary)`，与图库一致）

**i18n**
- 新增 5 键 × 2 语言：orderForm（draftTitle/draftFound/draftRestore/draftDiscard/draftRestored）

【涉及文件】
- `web/src/views/client/OrderForm.vue`（R57）
- `web/src/views/artist/OrderDetail.vue`（UI-9 仅 2 行 CSS）
- `web/src/locales/zh-CN.js` / `web/src/locales/en.js`（i18n）

【是否修改非客户前端文件】
否。仅授权文件（4 个）。Dashboard.vue 未动——R52 前端补 N 单等三号合入后按通知执行。

【接口依赖】
无新接口（纯前端）。

【自测情况】
| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告（rebase 后重跑） |
| Vite build | ✅ 通过（8.19s） |
| Rebase master | ✅ 无冲突 |
| console.log / TODO | ✅ 无残留 |
| v-html | ✅ 仅既有 1 处（须知预览，已消毒） |
| i18n 中英同步 | ✅ 5 键 × 2 |
| 改动文件范围 | ✅ 仅 4 个授权文件 |
| ad-hoc 验证脚本 | ✅ 33/34 PASS（唯一 FAIL 为 master 漂移导致的文件范围误报，rebase 后 diff 已干净；脚本后被审批拦截未重跑，改用内联 ESLint+build 验证） |

**需人工验证项**（无浏览器环境）：
- R57：填表后刷新页面 → 浏览器原生确认弹窗；返回后草稿自动恢复提示 → 恢复/丢弃行为正确
- R57：提交成功后刷新页面 → 无草稿提示
- R57：画师删除档位后客户恢复草稿 → 档位字段为空不报错
- UI-9：备注附图加载时无白闪（灰色底 → 图片）

【可能影响】
- 客户约稿页：填表后离开会多一步浏览器确认（设计意图）
- 不影响画师后台、管理后台、嵌入脚本

【待确认问题】
无。R52 前端补 N 单等三号合入通知。

【申请】
申请一号审核合并。
