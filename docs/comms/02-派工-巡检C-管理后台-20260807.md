# 02-派工-巡检C-管理后台-20260807

> 派工：一号 → 二号C（client-frontend，全页面巡检第 C 路）
> 工作方式：**只读巡检 + 浏览器实测**，零代码改动、不建 worktree、不 git 写操作。输出报告落主仓 `docs/comms/`。
> 目的：用户要求「全页面巡检，找布局/美观/操作逻辑的问题」。本路负责**管理后台**。

## 一、巡检范围（本路专属）

管理后台（管理员登录后 /admin/*）全部页面：
1. **AdminDashboard 总览**（views/admin/AdminDashboard.vue）
2. **ArtistManage 画师管理**（views/admin/ArtistManage.vue + ArtistDetailDrawer.vue）
3. **GreetingManage 问候语管理**（views/admin/GreetingManage.vue + components/admin/GreetingTable.vue）
4. **PlatformManage 社交平台**（views/admin/PlatformManage.vue）
5. **DefaultWorkflowEditor 默认流程**（views/admin/DefaultWorkflowEditor.vue）
6. **HealthCheck 系统自检**（views/admin/HealthCheck.vue）
7. **TrackingAnalytics 埋点看板**（views/admin/TrackingAnalytics.vue——⚠️ 刚合入的新页面，重点查）
8. **AdminLayout 布局**（components/admin/AdminLayout.vue）

## 二、检查点（逐页过）

1. **布局**：错位/重叠/溢出/空白异常/窄屏/宽屏
2. **美观**：与 AdminDashboard 既有风格一致（管理后台从简，纸墨 token 换肤）；新页面 TrackingAnalytics 与旧页面风格是否统一
3. **操作逻辑**：按钮可点性/禁用态/加载态/空态/错误态；表单校验；表格分页；删除确认；开关联动（TrackingAnalytics 的开关→画师侧显隐）
4. **数据展示**：数字格式（金额/日期/百分比）；空数据处理
5. **i18n**：中英键完整性

## 三、方法

1. **静态分析**（先做）：读代码找结构问题
2. **浏览器实测**（后做）：起本地 vite dev **端口 5176**（`cd web && $env:PORT=5176; npm run dev`），访问各页面**截图留证**
   - 需管理员登录：可用开发态/AUTH_DEV_MODE 或直接构造会话（参考 api 层）
   - 每发现一个问题：截图 + 文件:行号 + 描述
3. 产出报告，不修代码

## 四、输出

- 报告写 `docs/comms/02-to-01-巡检C-管理后台-交付-20260807.md`：
  - 🔴 真实问题（页面 + 截图路径 + 文件:行号 + 描述 + 建议修法）
  - 🟡 改进建议 / ✅ 健康项
  - 严重度标注：高/中/低
- 截图存 `workspace/temp/inspect-c/`（若实测）

## 五、纪律

- 只读：不 git add/commit/push/merge、不碰 master
- **不夸大不编造**：截图为准；判断以用户拍板过的设计规范为准
- TrackingAnalytics 是刚合入新页面，重点看有没有明显问题（但不与后端联调，数据 mock 或真实都行）
- 20 分钟无产出停下报卡点
