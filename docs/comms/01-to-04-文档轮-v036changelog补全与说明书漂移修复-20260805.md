# 派工：四号文档轮（低风险）

> 一号派工 · 2026-08-05 · 风险等级：低（纯文档，零代码）
> 工作目录：`D:\Hermes Agent CN Desktop\workspace\artist-commission-docs`（worktree，分支 `v036-docs`，已建好）
> **开工第一步：`git merge master` 再读本文件**（本文件在 master 上）

## 任务 1：changelog.md 补 v0.36 整段

changelog.md 目前停在 v0.35，**v0.36 整段缺失**。在 `## v0.35` 之前插入 `## v0.36 — 2026-08-05` 段落。以下是经一号核实的完整合入清单（来源：git log 实锤，非凭记忆），按你的 changelog 既有风格组织：

### 波 1（清账批）
- **时间条重做**：四档缩放（月/季/半年/年）+ 拖拽撤销 toast 提示；顺带修 L0/L1/L3 三处小问题
- **画廊布局按模板区分**（用户拍板）：Classic/Folio 恢复瀑布流，Gallery/Atelier 保持画册翻页；TplGallery 增加 layout prop 双分支并存
- **主图区修复**：cover_order 序号恢复 + 上下排序按钮（F7 主图去重时排序入口漏迁）
- **负数收款回归修复**：L3 校验收紧时误拦 `cents<=0`，恢复退款/撤销录入（与后端 addPayment 规则对齐：允许负数、须填原因、不超已收）
- **画廊灯箱两处热修**：弹窗飘出窗口（el-dialog 加 append-to-body，画廊祖先 transform 劫持 fixed 定位）；去掉第三层 EP 内置预览（preview-src-list 叠出全屏遮罩被弹窗截断）
- **演示订单分期缺口修复**：demo-data 直插订单绕过了 createOrder 导致分期为空，复用 generateInstallmentsForOrder 补齐
- **需求瘦身**：REQ-022 砍掉水印联动（用户拍板，代码本无实现）

### 波 2（用户终验反馈轮，六路并行修复）
- **收款区归零根因修复**：订单变更端点（改价/加项/删增项等）统一走 enrichOrderForArtist 响应增强，修「加项/改价后收款区显示归零」
- **收款三连**：收款弹窗负数输入修复（去 :min/:max 硬钳制）+ 多收支持（后端正数无上限，显示「多收 ¥X」）+ 跟踪文案
- **约稿表单跳步修复**：OrderForm 增项步骤点「下一步」不再跳过「写需求」步骤（附步骤导航组件测试）
- **页内拖拽守卫**：7 处上传区拦截页内图片拖入（useDropGuard 捕获阶段）
- **手动录单价格脏标记**：修「录单选档位+加增项后价格跟随被手输旧价抹掉」（005 订单事故根因）
- **看板「已交付」防手滑**：统一走交付弹窗，不再直接改状态
- **订单详情四项健壮性**：日期 picker 失败回滚 + 备注 Enter 防重复提交 + 推进按钮防连点 + 进度滑块 pointercancel 处理

### 测试
- 后端 711 → **713** · 前端 144 → **166**

### 迁移
- 无（v38 迁移已记入 v0.35）

**注意**：以上是你的素材清单，行文按 changelog 既有格式与口吻组织（功能/修复分节、加粗标题、一句话说清用户可感知的变化），不要照抄本文件。拿不准的条目可读对应 commit 的 diff 核实（波 2 六路合入 hash：42fe432 / 6f57b3b / 55b0cd1 / c3cabd0 / 3179351 / b795f6b）。

## 任务 2：画师使用说明书漂移修复

`docs/画师使用说明书.md` 与本轮修复对照，重点核对：
1. **收款章节**：负数退款录入方式、多收显示——波 2 P 批改了交互，说明书若描述了旧的 :min/:max 限制或"不支持多收"即为漂移
2. **看板章节**：「已交付」现在弹交付窗，若说明书写"点击直接标记交付"即为漂移
3. **画廊章节**：布局已按模板区分（瀑布流/翻页两种），波 1 时同步过一次，本轮确认无新漂移
4. **手动录单章节**：价格跟随/手输覆盖的语义（G2 脏标记修复后行为：没手动改过就跟随计算价，改过才锁定）
5. 全局搜一遍 changelog 波 1/波 2 各条目的关键词，确认没有其他漂移点

修复原则：只改与代码实际行为不符的描述，不重构文档结构，不加未实现的功能描述。

## 授权文件（只许动这些）

- `docs/changelog.md`
- `docs/画师使用说明书.md`

其他文件一律不碰。完成后 commit（格式 `docs: ...`）+ 交付报告写入本 worktree 的 docs/comms/，转交一号。

## 验收

1. changelog v0.36 段落完整覆盖上述清单，无虚构条目，格式与既有版本一致
2. 说明书所有漂移点修正，每条修正能在代码/commit 里找到依据
3. 交付报告列明：changelog 新增条目数 + 说明书修正点清单（每条附依据）
