# 二号 → 一号：前端测试扩充 + P2 审计预排查 交付

> 日期：2026-08-01
> 分支：feat/frontend-test-expand

## 任务 A：前端测试扩充

新增 4 个测试文件，70 个用例（原 17 → 总 87）：

| 文件 | 用例数 | 覆盖 |
|------|--------|------|
| composables/__tests__/useOrderForm.test.js | 40 | 数据加载、档位选择、计价（防抖/失败回退/增项构建）、增项初始化、草稿保存/恢复/丢弃/损坏处理、校验规则、提交流程、参考图上传 |
| composables/__tests__/usePasteUpload.test.js | 13 | 粘贴拦截、图片/非图片识别、数量/大小限制、enabled 开关、错误处理、卸载清理 |
| utils/__tests__/datetime.test.js | 7 | UTC→本地转换、空格/T 格式、非法输入、options 覆盖 |
| utils/__tests__/sanitize.test.js | 10 | 空输入短路、白名单配置（危险标签/事件属性排除）、链接加固 hook |

说明：
- sanitize.test.js mock 了 DOMPurify——happy-dom 的 DOMParser 与 DOMPurify 不兼容（实测白名单标签被误删、script 反而保留），测试改为验证集成契约（白名单配置 + hook 行为），DOMPurify 自身过滤由其测试套件保证
- useOrderForm 测试覆盖了派工要求的"价格计算、增项初始化、草稿保存/恢复"全部路径，包括刚修完的计价崩溃相关逻辑（增项默认值初始化防 undefined）

验证：
- `npx vitest run`：87/87 全绿
- `npx eslint .`：零错误零警告
- `npm run build`：通过

## 任务 B：P2 审计预排查

对照 docs/archive/待修复问题清单.md 逐条验证当前 master（50e5cf0）：

### 已修复（7 项，清单可标 ✅）

| 编号 | 问题 | 验证依据 |
|------|------|----------|
| P2-3 | 用户枚举 | auth.routes.js:47 统一返回"若该QQ已注册，登录码已发送" |
| P2-5 | GC 启动不执行 | app.js:121 启动时立即 gcUploads() |
| P2-6 | GC 子进程 | app.js:49 内联函数，不再 execFile |
| P2-8 | 停机无超时 | index.js:31-36 有 10s 超时强退 |
| P2-9 | CORS 文档不符 | .env.example:34 已修正为"留空 = 禁止跨域" |
| P2-13 | 禁止缩放 | index.html 无 user-scalable/maximum-scale |
| P2-14 | token localStorage | stores/artist.js 已改 httpOnly cookie |

### 仍存在（5 项）

| 编号 | 问题 | 归属 | 改动量 | 风险 |
|------|------|------|--------|------|
| P2-11 | 404 catch-all 渲染首页 | 前端 | ~30min（新建 NotFound.vue + router 改指向；router 需一号协调） | 低 |
| P2-12 | index.html 缺 SEO meta | 前端 | ~10min（index.html 需确认授权） | 低 |
| P2-7 | uncaughtException 无 db.close() | 后端 | ~15min | 低（WAL 自动恢复） |
| P2-1 | 限流固定窗口边界突发 | 后端 | ~1h（改滑动窗口） | 低 |
| P2-2 | 限流内存存储重启清零 | 后端 | 需 Redis，生产前处理 | 单实例可接受 |

### 建议修复顺序

1. P2-11 + P2-12（前端一批，~40min，二号可执行）
2. P2-7（后端，~15min，三号可执行）
3. P2-1（后端，~1h）
4. P2-2（生产部署前，需 Redis 基建）
