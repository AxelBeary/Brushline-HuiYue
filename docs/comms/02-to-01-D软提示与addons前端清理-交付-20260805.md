# 二号 → 一号：D 软提示 + addons 前端清理 · 交付报告

> 日期：2026-08-05 · 分支：`softhint-addonsfe`（worktree `artist-commission-d2`）· 执行：二号
> 基线：merge master（43395ff）零冲突
> 合入顺序：**本批先合（前端停传 addons），三号批后合（后端删字段）**——已按此交付

## 一、任务一：D 项软提示（用户拍板 = 软提示）

**改动**：`web/src/views/client/OrderForm.vue`
- 需求描述步骤「下一步」改走 `goNextFromDetail()`：`description` 留空 → ElMessageBox.confirm 软提示「需求描述为空，确定继续吗？」→ **确认后放行**（不拦截、不加必填校验）；取消留在本步
- 后端 schema 零改动（description 保持可空）
- i18n：`orderForm.descSoftTitle / descSoftMsg / descSoftContinue` 中英双语（zh-CN.js + en.js）

**测试**（OrderForm.stepnav.test.js +1 例）：空描述点下一步 → confirm 被调 + step 不前进；mock 取消 → 留下；mock 确认 → 放行到联系方式步骤 ✓
- 顺带修正 3 个正向链路用例：填 description 后放行（模拟真实用户）

## 二、任务二：addons 旧模型前端清理（与三号后端同批联动）

### 2.1 删除清单（price_addons 旧链路，前端先停传防 ajv 400）

| 文件 | 删除内容 |
|------|---------|
| `web/src/composables/useOrderForm.js` | `availableAddons`（tier.addons 读路径）、`addonGroups`、`addonSelections/addonToggles`、`buildSelectedAddons`、`formatAddonPrice`、`CATEGORY_META`；doCalc/提交 payload 的 addons 字段；草稿保存/恢复/监听/初始化全部旧增项分支；`hasPricingExtras` 改定义（去掉 addons 条件，仅倍率+折扣） |
| `web/src/views/client/OrderForm.vue` | 旧增项 UI 块（`availableAddons/addonGroups/addonSelections/addonToggles/formatAddonPrice` 渲染区）整个删除；解构清理 |
| `web/src/views/artist/ManualOrder.vue` | 旧档位增项块（模板 + `addonSelections/addonToggles/availableAddons/addonGroups/CATEGORY_META/formatAddonPrice/buildAddonList`）；提交 payload addons 字段；onTierChange/重置的旧增项清理 |

### 2.2 保留（一字未动，新体系）
- **画风 style_addons**：`availableStyleAddons / styleAddonSelections / buildStyleAddons / formatStyleAddonPrice`（OrderForm + ManualOrder + ArtStyleManager + api.setStyleAddons）
- **自定义增项 customAddons**（REQ-029 R5，ManualOrder）
- `TplTierGrid` addons slot：纯透传无数据消费（grep 确认）

### 2.3 测试同步
- `useOrderForm.test.js`：删旧模型 addons 用例 10 例（formatAddonPrice 3 / availableAddons 3 / addonGroups 1 / buildSelectedAddons 2 / 增项默认值 1），改 onTierChange/hasPricingExtras/草稿恢复用例断言
- `OrderForm.stepnav/summary.test.js`：mock 删旧增项字段
- `ManualOrder.stylemode.test.js`：提交断言 `addons` → `toBeUndefined()`；旧档位回归用例改为断言旧增项 UI 不再出现

## 三、验证结果

- **vitest（web）**：215/215 全绿（基线 224 − 删 10 旧用例 + 加 1 D 软提示 = 215）
- **eslint（web）**：0 错误 0 警告
- **build（web）**：通过
- **server 零改动**（grep 确认，schema 删除归三号）

## 四、grep addons 残留清点（web/src，派工要求）

| 位置 | 语义 | 处置 |
|------|------|------|
| `useOrderForm.js` L55/299-311/327/447 | 画风 style_addons | ✅ 保留（新体系） |
| `ManualOrder.vue` L195/606/716-728/761 | 画风 style_addons | ✅ 保留 |
| `ArtStyleManager.vue` L114-147/658/680 | 画风增项管理 | ✅ 保留 |
| `api/index.js` L218/226 | 画风增项接口 | ✅ 保留 |
| `TplTierGrid.vue` L56 + 4 模板透传 | slot 透传无消费 | ✅ 保留（无害） |
| `manualOrder.addons` locale 键 | 画风增项步骤标题在用 | ✅ 保留 |
| 测试 mock 数据 `tier.addons` / `styleState.addonSelections` | 测试 fixture | ✅ 保留（旧草稿兼容性验证数据） |
| **旧模型 price_addons 链路**（availableAddons/addonGroups/addonSelections/addonToggles/buildSelectedAddons/buildAddonList/CATEGORY_META/formatAddonPrice） | 旧模型 | ✅ **已清零**（grep 全仓零命中旧语义消费） |

## 五、说明事项

1. **发现并修复一个真 bug**：初版 `goNextFromDetail` 里 `step.value = contactStep` 缺 `.value`（把 computed 对象赋给 step），组件测试抓出，已修为 `contactStep.value`。
2. 草稿里的旧 `addonSelections/addonToggles` 字段（历史 sessionStorage 残留）新代码不再读取，兼容性无害；测试保留旧草稿 fixture 验证不崩。
3. 浏览器联调受端口限制（3000 被 polish worktree server 占用未动）——D 软提示行为与 addons 清理均由组件/单测覆盖（215 全绿）。

## 六、commits

`feat(client): D 软提示（需求描述可空过仅弹确认不拦截，双语）+ addons 旧模型前端清理（useOrderForm/OrderForm/ManualOrder 停传 addons，画风 style_addons 一字不动） (一号审核要点:server零改动/前端先停传防ajv400/旧增项残留grep清零/215全绿含D软提示组件测试/lint0/build过)`

二号转交一号，文件：docs/comms/02-to-01-D软提示与addons前端清理-交付-20260805.md
