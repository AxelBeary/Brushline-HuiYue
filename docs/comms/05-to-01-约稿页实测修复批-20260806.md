# 交付：约稿页实测修复批（五号）→ 一号

> 日期：2026-08-06 · 分支：`fix/orderform-selection` · 提交：`72a5e68`（3 文件，13+/8-）
> 核心纪律执行：**全部 Playwright/浏览器实测取证后才下结论**，不实项未硬改。W2 属实但动 main.js/vite.config.js 需一号确认，**已给方案未动手**。

## 各项结论

| 项 | 结论 | 证据 |
|----|------|------|
| **W1 选中态** | **不实，未改** | 尺寸卡选中 = 主色描边 `rgb(77,232,217)` + 浅底 `rgba(77,232,217,0.12)` + ✓（16px 主色）；未选 = 灰边深底。档位/画风卡同机制（`.tier-pick--on`/`.style-pick--on` + ✓ 印章，OrderForm.vue L44-47/882-890/1072-1089）。差异清晰可辨 |
| **W2 按钮色** | **属实（全站性问题）** | 「下一步」按钮实测 `rgb(64,158,255)` = EP 出厂蓝 #409eff。主页「我要约稿」等所有主色按钮同受影响。**根因**：EP base.css 的 `:root{--el-color-primary:#409eff}` 以独立 chunk `_plugin-vue_export-helper-*.css` 晚于 theme.css（main.css 内）加载，同特异性 `:root` 后者赢 → 覆盖 theme.css:93 覆写。亮暗模式均受影响 |
| **W3 摘要空态** | **部分属实 → 已修** | 评审「默认」字样**不实**（locales 无此文案）；但单画风未选尺寸时摘要显示「日系 合计 ¥0.00」缺引导 = 真问题。修复：摘要卡画风模式加 `selectedSize` 空态判断 → 显示引导「选好尺寸后这里会显示价格」（新 key `summaryNoSize`，zh-CN/en.js） |
| **W4 动画收敛** | **已修** | OrderForm.vue 内 5 处 hover/过渡收敛：tier-pick/style-pick `translateY(-3px)→(-2px)`、时长 `0.3s→0.15s`、`--ease-bounce→ease`；size-pick/step-dot/inspire-tag 同步收敛。✓ 印章入场动画（tier-stamp-in）为庆祝时刻**保留 bounce** |

## 修改文件清单

| 文件路径 | 修改类型 | 改动 |
|----------|----------|------|
| web/src/views/client/OrderForm.vue | 修复 | W3 摘要空态 v-if/v-else + W4 5 处过渡收敛 |
| web/src/locales/zh-CN.js | 新增 | `summaryNoSize: '选好尺寸后这里会显示价格'` |
| web/src/locales/en.js | 新增 | `summaryNoSize: 'Pick a size to see the price here'` |

## 验证结果

- [x] web vitest **215/215** 全绿（含既有 OrderForm.summary 测试）
- [x] eslint 0 错 0 警（web 全量）
- [x] npm run build 通过
- [x] 浏览器实测：空态显示引导语 / 选中尺寸后恢复完整摘要（半身像 ¥120）
- [x] git diff 仅 3 授权文件，无越权、无格式化噪音

## W2 修复方案（等一号确认后动手）

**方案 A（推荐，动 vite.config.js）**：`ElementPlusResolver` 增加 `importStyle: 'css'` 时显式保证 EP base.css 在 theme.css 之前。实测不可靠（动态 chunk 顺序难控）。

**方案 B（推荐，动 main.js）**：将 `import 'element-plus/theme-chalk/dark/css-vars.css'` 移到 theme.css 导入**之后**？不行——问题不在 dark css-vars，而在动态注入的 base.css。

**方案 C（最稳，动 theme.css，需与二号协调避免冲突）**：theme.css:92 的 `:root` 覆写改为 `:root:root`（双 :root 特异性 (0,2,0) > EP :root (0,1,0)）。一行改动，根治全站按钮色。**但 theme.css 正被二号批 1 修改，派工禁动**——需一号协调。

**倾向方案 C**，改动最小、全站根治。请一号裁决后再派工。

## 截图

`D:\Hermes Agent CN Desktop\workspace\temp\w-20260806-shots\`（w3-empty-after.png / w3-filled-after.png 等）

## 备注

- 测试 server 停用前请知会：`e2e/test-05.db` 隔离测试库 + 3999 端口进程 `proc_db963dd58f18` 仍挂着（仅本 worktree 用，不影响生产）
- 本批未动 theme.css / NotFound / Landing / 画师主页模板 / server，与二号批 1 无冲突面
