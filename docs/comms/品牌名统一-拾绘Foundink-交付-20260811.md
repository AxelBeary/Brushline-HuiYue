# 交付：品牌名统一「拾绘 + Foundink」（原 绘约/Brushline-HuiYue）

> 交付人：四号（借一号窗口）｜日期：2026-08-11
> 分支：`feat/rebrand-shihui-foundink`（worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w-rebrand`）
> 请一号审核后合入 master

---

## 一、任务背景

用户拍板（2026-08-11）：品牌名定为 **中文「拾绘」+ 英文「Foundink」**。
- 「绘约」弃用：谐音「毁约」负面（约稿平台致命联想）
- 「约绘」弃用：撞名漫友文化 2011 年同名绘本杂志（同赛道知名品牌）

## 二、改动清单

### 代码（用户可见，11 文件）

| 文件 | 改动 |
|------|------|
| `web/src/locales/zh-CN.js` | footer/logo/排期/登录页 brandTitle 5 处「绘约」→「拾绘」 |
| `web/src/locales/en.js` | 对应 5 处 HuiYue/Huiyue → **Foundink** |
| `web/index.html` | meta description / og:title / og:site_name →「拾绘 Foundink」 |
| `server/src/features/auth/totp.ts` | `buildOtpAuthUri` issuer 默认值「绘约」→「拾绘」（**TOTP 二维码显示名**） |
| `web/src/components/ArtistLayout.vue` | 品牌副标 `BRUSHLINE` → `FOUNDINK` + 注释 |
| `web/src/components/admin/AdminLayout.vue` | 注释 |
| `web/src/views/artist/Login.vue` | 注释 |
| `web/scripts/check-i18n.js` | 白名单「绘约」→「拾绘」 |
| `web/src/styles/theme.css` / `templates.css` / `palettes.css` | 头部注释 |

### 文档（12 文件）

README.md（标题「拾绘 Foundink（原 Brushline-HuiYue）」）、THIRD-PARTY-NOTICES.md、docs/CONTEXT.md、docs/specs/接口契约清单-v1.md、docs/纸墨设计语言提案-v1.md（3 处）、docs/soul/soul-01~05（5 文件各 1 处）、REQ-014（路径设计 `我的文档\拾绘\` 等 7 处）、REQ-028（「拾绘平台号」）

## 三、验证（独立复跑，非 self-report）

- `web`：vitest **350/350 通过**（42 文件）✅
- `web`：check:i18n **OK**（无新增硬编码中文）✅
- `server`：tests/totp.test.js **14/14 通过** ✅（TC-TOTP-14 传自定义 issuer，不依赖默认值，不受影响）

## 四、未改（合理保留，说明理由）

| 位置 | 理由 |
|------|------|
| `docs/comms/STATUS.md`、`docs/changelog.md` | 历史状态/日志，不改历史 |
| `docs/soul/skills/**` | 内部知识库，量大，不影响用户；需要时后置批改 |
| `server/tests/totp.test.js` | 传参测试（`'绘约/测试'` 是自定义 issuer 的特殊字符用例，与品牌默认值无关） |
| `docs/OPS.md` 2 处 | `gh api repos/AxelBeary/Brushline-HuiYue/...`——**真实仓库名，改了命令失效** |
| REQ-038~043 已交付文档 | 用户指示：一号已收，不再回头改（品牌名决策已记录在 REQ-039 决策 6） |

## 五、风险与注意

1. **纯显示名/文案变更，零功能行为变化**——无迁移、无接口变更
2. TOTP issuer 是**生成二维码时写入**：已绑定画师的二维码不变，新绑定显示「拾绘」
3. 容器重建后生效（前端构建产物烘焙）
4. 仓库名 `Brushline-HuiYue` 未改（GitHub 改名有重定向，1.0 后再定）——README 已标注「原 Brushline-HuiYue」

## 六、待办

- [ ] 一号审核本分支 → 合入 master
- [ ] 容器重建（下次部署动作）
- [ ] 后续 1.0 实施按「拾绘/Foundink」品牌名统一（域名/logo/物料）
