# 一号 → 三号：v0.17 第四批后端派工（SPEC-004 名额与缓冲系统）
> 日期：2026-07-31

## 背景

SPEC-004（docs/specs/SPEC-004-名额与缓冲系统.md）已由四号与用户三轮交流后产出，用户已拍板。按 SPEC 实现后端。

## 任务

按 SPEC-004 实现：
1. 迁移 v19：artists 表加 batch_limit / buffer_limit 字段（或按 SPEC 定义）
2. 队列逻辑：正式区满后新订单进缓冲区
3. 递补逻辑：正式区订单完成/取消后，缓冲区最早一单自动递补
4. API：画师设置名额上限（GET/PUT）
5. API：看板返回正式区 + 缓冲区分组数据
6. 客户进度页：排队位置（"您在缓冲区第 N 位"）
7. 测试：覆盖 SPEC 中的边界情况

## 迁移要求（五号检查清单）

| # | 检查项 |
|---|--------|
| 1 | schema 字符串同步 |
| 2 | 迁移幂等 |
| 3 | 自动备份（copyFileSync） |
| 4 | cleanDb 同步 |

## 授权文件列表

| # | 文件/目录 | 用途 |
|---|-----------|------|
| 1 | `server/src/db/init.js` | 迁移 v19 + schema |
| 2 | `server/src/features/order/`（order 模块） | 队列/递补逻辑 |
| 3 | `server/src/features/artist/`（artist 模块） | 名额设置 API |
| 4 | `server/src/shared/errors.js` | 新错误码 |
| 5 | `server/tests/` | 测试 |

如需改授权外文件，停止并写 comms 申请。

## 验证标准

- `cd server && npx vitest run` 全绿
- 迁移 v19 幂等 + 备份
- 递补逻辑正确（完成/取消触发，FIFO）
- 提交前 `git diff --stat` 确认只有授权文件

## 交付

完成后写 `docs/comms/03-to-01-v017-b4后端提交-0731.md`。
