# 05-派工：后端纪律性扫 bug/屎山（只读深度审计）

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：五号（bugfix）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w8`
> 分支：`beta/audit-backend`
> 交付报告：`docs/comms/05-to-01-后端审计-20260808-交付.md`（写完 commit 进分支）

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w8'
git merge master --no-edit
```

## 铁律（最重要）

- **只读审计**：发现 bug/屎山 → 写报告，**不改任何代码**（改代码 = 打回）
- 每条发现带 **文件:行号** 证据 + 现象 + 触发条件 + 影响 + 建议修复
- 拿不准的标「🟡 需验证」，不硬判
- **对照已知正确模式别误报**：requireAuth+JSON Schema+参数化 SQL=正确姿势（tools.routes.ts 等），不算问题
- 报告 commit 到分支（纯报告零代码）

## 审计范围（server/src/ 全量，5 区 20 项）

### A 错误处理
1. catch 吞错（空 catch / 不抛不记录）
2. 假成功（200 但没落库 / 业务失败返回 ok）
3. `reply.send` 后继续执行（重复响应）
4. await 缺失（promise 未捕获 / 未 await 的异步写）
5. 定时器/监听未清理

### B 边界空值
6. 除零（金额比例 / 分页分母）
7. parseInt·Number 无校验（NaN 进 SQL）
8. 空数组 reduce / [0] / find 后直接取字段
9. 日期 UTC·本地混用（对标 TC-TR-15 教训：断言与分组口径同源）
10. 金额负数 / 溢出 / 分元混用

### C 并发事务
11. check-then-act 竞态（库存 / 名额 / 额度扣减）
12. 多步写缺事务（部分失败留脏数据，对照 v38 迁移教训）
13. 同步 SQLite 大查询阻塞 async（性能）

### D 安全
14. 模板字符串拼 SQL（非参数化）
15. 越权（artist_id 过滤缺失 / requireAdmin 覆盖不全）
16. 敏感信息日志 / 错误泄露（token / totp_secret / 内部路径）
17. 用户输入拼路径（路径穿越）

### E 清理
18. 未使用导出 / 死分支
19. console.log 泄漏
20. 重复代码（同逻辑多份实现，如金额回退层级）

## 输出格式（交付报告）

```
# 后端审计报告
## 一、发现汇总表
| 级别 | 数量 | 摘要 |
|------|------|------|
| 🔴 真bug/安全 | n | ... |
| 🟡 隐患/边界 | n | ... |
| 🟢 轻微 | n | ... |
| ✅ 已查无问题 | n | 分区 |

## 二、🔴 逐条详情（文件:行号 + 现象 + 触发 + 影响 + 建议）
## 三、🟡 逐条详情（同上）
## 四、🟢 轻微列表
## 五、✅ 已查无问题分区（审计严谨性证据——分文件列「查了哪些，确认无误」）
```

## 审计重点文件（按价值排序）

- `server/src/features/order/order.service.ts`（1222 行，金额/事务核心）
- `server/src/features/order/order.routes.ts`（1046 行）
- `server/src/features/artist/tools.service.ts` + `tools.routes.ts`（**最新批次**——重点查新代码）
- `server/src/features/tracking/tracking.service.ts` + `routes.ts`（最新批次）
- `server/src/features/artist/artist.routes.ts` / `admin.routes.ts`（权限边界）
- `server/src/features/order/order-workflow.service.ts` / `pricing-engine.ts`（流程/算价）
- `server/src/shared/`（中间件/错误/工具）

> 已知正确模式（不算问题）：tools.routes.ts 的 requireAuth + schema additionalProperties:false + 参数化 SQL；tracking 的 `${since}` 由 days clamp(1..90) 拼字面量（已审计过，安全）。

## 交付要求

1. commit 描述中文（如 `docs(audit): 后端只读深度审计报告`）
2. 交付报告写 `docs/comms/05-to-01-后端审计-20260808-交付.md`
3. 结束前：`git status --short` 确认干净（无代码改动）
4. **不推送、不合并、不改 STATUS**
