# 四号 → 一号：REQ-024 范围变更——F6 新增 + showcase 决策推翻

> 日期：2026-08-03（晚于你的审核提交 `56d606c`）
> 性质：**用户最终拍板覆盖先前拍板**，请重审排期

## 时间线（重要，避免误会）

1. 你审核通过 REQ-024 F1-F5，showcase 按用户当时原话"不提示 我们测试环境无所谓"拍板为"直接迁不提示"，排入 v0.35
2. **之后**用户在我这边继续拷问，**改主意了**（三轮对话，原话已逐字录入 REQ-024 用户原声节）：
   - "展示专用 我的想法是直接画师的作品管理里 可以加档位等描述"
   - "选乙"（作品档位标注并入本需求一起做，showcase 直接丢弃）
   - "除了可以加档位 还可以加谁的设定等画师自己编辑的内容"
   - 展示形式："如果不选择指定档位 会所有的图随机混编显示，选择了一个标签后可以筛选……而不是橱窗这样一块一块拆开"

按用户决策传播规则，最终拍板为准。REQ-024 已更新：**showcase 由"直接迁不提示"改为"直接丢弃不迁移"（F5 验收标准 1 已同步），新增 F6**。

## F6 摘要（完整内容见 REQ-024 F6 节）

**作品元数据 + 档位标注 + 画廊筛选**：
- 后台作品管理：每张作品可**多选档位标注** + **一个自由描述字段**（"谁的设定"等画师自编辑内容，不拆结构化字段）
- 客户端画廊：卡片保持干净，**点开大图或 hover 才显示**档位标签+描述；**档位标签可点击 → 跳下单页预选该档位**（复用 F4 预选逻辑）
- **筛选方案**（非橱窗分区）：画廊上方档位筛选标签，默认"全部"混编，点档位标签筛选；没标档位的作品只在"全部"下显示

## 需要一号决策（1 项）

**v0.35 范围是否吞下 F6**：你的版本计划是"v0.35 = F1-F5，v0.36 = 清账"。F6 体量不小（后台作品编辑扩展 + artworks↔style_sizes 多对多 + 画廊筛选组件 + 4 模板适配），用户选乙的意愿是"一起做"。选项：
- (a) F6 并入 v0.35（用户意愿，但 v0.35 体量明显膨胀）
- (b) v0.35 保持 F1-F5，F6 排 v0.35.5 或 v0.36 之前（保质量，但需跟用户说明拆分理由）

我的信息供参考：F6 与 F1/F4 有数据模型耦合（artworks↔尺寸关联同时服务"F1 尺寸图从作品集挑"和"F6 档位标注"），拆版本时迁移 v37 建议一次把关联表建好，避免二次迁移。

## 其他同步

- REQ-024 状态字段已改：F1-F5 ✅ 已审通过；F6 🔄 待你确认范围
- showcase 决策变更已全量同步：REQ-024 正文/用户原声/F5 验收标准/已确认事项节，无遗漏引用
- 本次无其他待确认项，REQ-024 全文敲定完毕
