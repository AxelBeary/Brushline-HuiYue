# 五号 → 一号：v0.28 画师后台审计交付

> 日期：2026-08-02
> 分支：`audit/v028-artist-ui-audit`
> 性质：只读审计，未改任何代码

## 做了什么

### 任务 A：画师后台现状审计

逐页审计 11 个画师后台页面（登录/仪表盘/排期看板/订单管理/手动录单/订单详情/开稿管理/价格管理/作品管理/留言管理/设置），产出：

- **审计文档**：`docs/audit-画师后台现状.md`（全局问题 8 项 + 逐页问题 60+ 项 + 正面发现 7 项 + 设计稿对照建议）
- **截图基线**：`docs/audit-screenshots/`（11 张全页 PNG）

核心发现：
1. 配色体系分裂（青绿/蓝/薄荷绿/状态色四套并存，无 design token）
2. emoji 当图标（跨平台不一致，廉价感）
3. EP 默认组件零定制
4. 设置页信息架构混乱（与 REQ-016 诊断一致）
5. 大文件风险：OrderDetail 1449 行、QueueBoard 1271 行

### 任务 B：REQ-016 回归测试清单

三号实施 REQ-016 后，按以下清单回归：

#### 设置页（4 Tab 重组）

- [ ] 新 Tab 结构：基本资料 / 主页展示 / 模板与风格 / 偏好
- [ ] 旧"接稿设置" Tab 不残留（DOM 中无 `name="commission"` 的 tab-pane）
- [ ] 基本资料仅含：头像、昵称、编码、简介、联系QQ（≤5 字段）
- [ ] 主页展示含：接稿状态、公告、外链、平台链接、灵感标签、须知
- [ ] 模板与风格含：模板、配色、强调色、封面图、预览
- [ ] 偏好含：通知开关、默认面板、快捷按钮
- [ ] 每个 Tab 的保存按钮独立可用，保存后刷新数据不丢
- [ ] `/settings?tab=rules` 重定向仍工作（须知归入主页展示后路径可能变）
- [ ] `/settings?tab=commission` 旧链接不 404（重定向或友好提示）

#### 侧边栏

- [ ] 菜单分组正确：工作（仪表盘/排期/订单）· 经营（开稿/价格/作品/留言）· 门面（设置）
- [ ] 手动录单不在菜单中（`MENU_ITEMS` 无 `/orders/new` 项）
- [ ] 订单管理页内"手动录单"按钮仍可达 `/orders/new`
- [ ] 所有菜单链接点击后路由正确、页面渲染正常
- [ ] 折叠态（64px）分组分隔线仍可见
- [ ] 移动端抽屉导航同步更新
- [ ] 管理员"管理后台"入口仍在末尾

#### 接稿状态

- [ ] 仪表盘 StatusSwitch 切换 → 刷新后状态保持
- [ ] 开稿管理页状态变为可操作（不再是只读卡 + "去仪表盘切换"链接）
- [ ] 设置页基本资料中无接稿状态 RadioGroup
- [ ] 三处（仪表盘/开稿管理/客户主页）状态同步一致
- [ ] 切换后侧栏底部状态点颜色/文字同步更新

#### 路由兼容

- [ ] `/settings?tab=rules` → 正确定位到须知所在 Tab
- [ ] `/settings?tab=commission` → 重定向或友好提示（不 404）
- [ ] `/orders/new` 直接访问仍可用（仅菜单移除，路由保留）
- [ ] 浏览器前进/后退在上述重定向后行为正常

## 改了哪些文件

| 文件 | 类型 |
|------|------|
| `docs/audit-画师后台现状.md` | 新建（审计文档） |
| `docs/audit-screenshots/*.png`（11 张） | 新建（截图基线） |
| `docs/comms/05-to-01-v028-audit-20260802.md` | 新建（本文件） |

未改任何代码文件。

## 验证

- 截图脚本 11/11 成功
- 审计文档覆盖全部 11 页面 + 侧栏 + 移动端 + 代码质量
- 回归清单覆盖 REQ-016 四项变更（设置页/侧栏/接稿状态/路由）

## 备注

- 截图脚本 `e2e/audit-screenshots.mjs` 为临时产物，合入后可删
- vision 分析对 >100KB 截图超时，04-orders / 05-manual-order / 08-tiers / 09-artworks 四页的视觉分析基于源码精读而非截图 AI 分析，结论可靠但描述粒度略粗
- 订单详情页 `/orders/1` 截图为无数据态（显示 #undefined），这是种子数据无 id=1 订单所致，非代码 bug（但 undefined 拼入 UI 确实是 bug，已记录 OD1）
