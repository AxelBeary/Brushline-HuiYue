# 五号→一号：v0.25 路由层集成测试完成

> 分支：`test/v025-route-integration`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-05`
> 日期：2026-08-02

---

## 做了什么

新增 `server/tests/v025-route-integration.test.js`，13 个路由层集成测试（app.inject 全链路）：

| 编号 | 覆盖 | 用例 |
|------|------|------|
| B3-1 收款 API | TC-ROUTE-01~04 | POST 记录+累加、无token 401、非归属 404、GET 流水含负数 |
| B3-2 留言路由 | TC-ROUTE-05~07 | GET 全部（含三态）、无token 401、画师隔离 |
| B3-3 仪表盘 | TC-ROUTE-08~10 | GET revenue 结构校验、无token 401、GET todo |
| B3-4 快捷按钮 | TC-ROUTE-11~12 | PUT quickActions 被 schema 静默剥离、GET profile 可读 DB 值 |
| B1 防枚举 | TC-ROUTE-13 | 不存在QQ → 200（不泄露注册状态） |

## 验证结果

- `npx vitest run`：**525/525 全绿**（512 原有 + 13 新增），零回归
- 只改了授权文件 `server/tests/v025-route-integration.test.js`

## 路由层真实现状发现（不改代码，仅记录）

1. **requireOwnOrder 对非归属订单返回 404**（非 403）——设计如此（不暴露订单是否存在），测试按实际写
2. **留言路由 GET /api/artist/messages 无 status 筛选参数**——前端客户端过滤，路由层返回全部。派工预期的 `?status=pending` 不存在
3. **仪表盘无 /api/artist/dashboard/stats 端点**——只有 revenue/todo/activity 三个。测试改为覆盖实际端点
4. **PUT profile schema 未暴露 quickActions**——`additionalProperties: false` 静默剥离（非 400 拒绝），DB 层 updateArtist allowed 含 quick_actions 但路由不可达。前端当前走 localStorage，若后续切 DB 需在 schema 补字段

## 分支

`test/v025-route-integration` @ `6d00317`
