# 二号 → 一号：v0.25 前端四件完成，申请审核

> 分支：`feat/v025-frontend`
> Commit：`c57546d`（基于 master `3c5b374`）
> 日期：2026-08-02

---

## 做了什么

| 任务 | 内容 | 状态 |
|------|------|------|
| C | 快捷按钮 localStorage→DB：保存走 `PUT /api/artist/profile`（quickActions），加载 DB 优先 + localStorage 回退，首次非默认值自动迁移到 DB | ✅ |
| E | 月历翻月手势：touchstart/touchend 水平滑动 >50px 触发（参考 TplTierGrid 实现） | ✅ |
| D | 时间条视图：第三 tab，横轴日期（两周/一月/两月缩放）+ 纵轴订单（按确认日排序），实心/半透明/斜纹与月历一致，今天参考线，点击跳详情，共享 calOrders 数据源切换不重请求 | ✅ |
| A | 封面图轮播：Settings 模板 tab 星标管理 + TplCoverShowcase 共享组件（只输出内容不带皮肤）+ 4 模板各自独立视觉（classic 圆角展带 / gallery 展墙直角框 / atelier 拍立得微旋转 / folio 编辑式序号标注），el-carousel 自动轮播 + 手动滑动 | ✅ |

## 改了哪些文件

```
web/src/api/index.js                          +3   (setArtworkCover/unsetArtworkCover)
web/src/components/artist/dashboard/QuickActions.vue  +31/-10  (parseQuickActions + DB 优先)
web/src/components/templates/TplCoverShowcase.vue     新增（共享封面组件，无装饰 CSS）
web/src/composables/useArtistData.js          +4   (coverArtworks computed)
web/src/locales/en.js / zh-CN.js              各 +18  (14 新键中英对齐)
web/src/views/artist/QueueBoard.vue           +266  (时间条视图 + 翻月手势 + 三 tab)
web/src/views/artist/Settings.vue             +138  (快捷按钮 DB 保存 + 封面管理区)
web/src/views/client/templates/ArtistHome*.vue  4 个模板各 +42~72  (封面区 + 独立视觉)
```

## 验证结果

- `npx eslint .`：零错误零警告 ✅
- `npm run build`：成功（10.04s）✅
- i18n：14 个新键中英全对齐（脚本逐键校验）✅
- XSS：无新增 v-html（既有 3 处均走 sanitizeHtml）✅
- console.log：零 ✅
- 授权范围：全部文件在派工授权内（api/index.js 按隐含授权规则，已注明）✅

## 需要一号知晓

1. **后端缺口（C）**：`PUT /api/artist/profile` 的 JSON schema 缺 `quickActions` 属性 + keyMap 缺 `quickActions→quick_actions` 映射（service 白名单已有该列）。当前后端会 400 拒绝该字段，前端已做降级：catch 后回退 localStorage + 提示"已保存到本地"。**三号合入封面 API 时建议顺手补上这两行**，前端无需再改。
2. **任务 A 未联调**：对着派工契约实现（PUT/DELETE `/api/artist/artworks/:id/cover` + 公开主页 `is_cover` 排前），等三号 API 合入 master 后 rebase 联调。字段缺失时封面区不显示（向后兼容）。

## 自测说明

纯静态验证（ESLint/build/i18n/XSS），未起服务跑页面。建议一号审核时重点看：
- QueueBoard 时间条的 sticky 标签列 + 绝对定位横条布局
- 4 模板封面区视觉差异化是否符合"共享逻辑不共享皮肤"
