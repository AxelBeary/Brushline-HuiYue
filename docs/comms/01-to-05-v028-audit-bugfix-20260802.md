# 一号 → 五号：v0.28 审计 bug 修复

> 日期：2026-08-02
> 分支：`fix/v028-audit-bugs`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-05`
> 来源：你的审计文档 `docs/audit-画师后台现状.md` 中发现的 bug

## 任务

从你的审计文档中挑出**确认是代码 bug 的项**（不是视觉问题、不是信息架构问题），逐个修复。

已知至少包含：
- **OD1**：订单详情页 `/orders/:id` 无数据时 `#undefined` 拼入 UI（order_no 为 undefined 时显示 `#undefined`）

其余 bug 项从审计文档中自行筛选。判断标准：
- ✅ 修：代码逻辑错误、undefined/null 拼入 UI、字段名不匹配
- ❌ 不修：视觉丑、信息架构不合理、EP 默认样式（这些等视觉统一批次）

## 授权文件

- `web/src/views/artist/**`（画师端页面）
- `web/src/components/artist/**`（画师端组件）
- `server/src/**`（如涉及后端字段映射）
- `server/tests/**`（补测试）

## 验证标准

- 每个修复有对应验证（eslint + build + 相关测试）
- 后端改动跑 vitest
- 不改视觉样式、不改信息架构

## 交付

comms `05-to-01-v028-audit-bugfix-{日期}.md`，列每个 bug 的根因 + 修复 + 验证
