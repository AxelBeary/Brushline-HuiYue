# 三号 v0.23 派工（2026-08-01）

> 分支：`feat/v023-backend`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-be`
> 基准：master `f83b8bb`

---

## 第 1 波：B7 额度池后端（5.5h，关键路径起点，立刻开工）

参考 spec：`docs/specs/plan-v023-quota-pool.md`（T1-T5 已确认）

| # | 工作 | 说明 |
|---|------|------|
| 1 | 迁移 v24 | orders 加 paid_total_cents INTEGER DEFAULT 0 + 存量换算（SUM paid installments）+ 建 order_payments 表 + 索引 |
| 2 | 收款 API | POST /api/artist/orders/:id/payments（正数收款/负数撤销，负数 note 必填，paid_total_cents + amount >= 0 硬约束）+ GET 流水列表 |
| 3 | 分期状态推算 | computeInstallmentStatuses（三态：paid/partial/pending），getOrderInstallments 改用 paid_total_cents |
| 4 | 话术变量修复（T3 BUG） | replaceSpeechVars / getSpeechInfo 改读 order.paid_total_cents（不再 SUM installments WHERE status='paid'） |
| 5 | 删 adjustInstallments | 仅 2 个调用点（order.service addExtraItem L545 / deleteExtraItem L576），改为只调 recalcFinalPrice |
| 6 | 订单详情 API 扩展 | GET /api/artist/orders/:id 响应加 paidTotalCents / remainingCents / installments 带推算 status |
| 7 | 客户端 API 扩展 | GET /api/public/orders/:trackId 响应加 paidTotalCents / finalPriceCents |

### 约束

- 迁移 v24 可回滚（paid_total_cents 可空，order_payments 独立表）
- order_payments 永不 DELETE（审计完整性）
- 事务原子：INSERT 流水 + UPDATE paid_total_cents 同一事务
- 469/469 测试全绿 + tsc 零错误 + eslint 零错误
- 新增测试覆盖：收款/撤销/非负约束/存量换算/话术变量

## 第 2 波：B4 留言板后端（3h）

参考：`docs/specs/plan-v019-artist-homepage-draft.md` §5

| # | 工作 |
|---|------|
| 1 | 迁移 v25：建 guestbook_messages 表 + 索引 |
| 2 | 7 个 API（POST 公开提交 / GET 公开已审核 / GET 画师全部 / PUT approve / PUT reject / PUT reply / DELETE 管理员强删） |
| 3 | 限流：公开 POST 同 IP 每分钟 3 条（用现有 rateLimit 插件扩展） |

## 第 3 波：测试 + 回归

- B7 测试补充（1h）
- B4 测试补充（1h）
- 全量回归 469+ 全绿

---

## 授权文件范围

- `server/src/db/migrations/`（v24、v25）
- `server/src/db/init.js`（注册迁移）
- `server/src/features/order/`（收款 API + 推算 + 话术 + 删 adjustInstallments）
- `server/src/features/guestbook/`（留言板 API）
- `server/src/shared/middleware/rate-limit.ts`（限流扩展）
- `server/src/types/`（新增类型）
- `server/test/`（对应测试）

不在授权范围的文件不要动。需要改其他文件先写 comms 说明。
