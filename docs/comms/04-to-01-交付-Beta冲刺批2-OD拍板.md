# 交付：四号 · Beta 冲刺批 2 —— OD-01/OD-05 拍板记录

> 派工：`docs/comms/01-to-04-Beta冲刺批2-OD详谈.md`
> 分支：`docs/beta-sync`（worktree `../artist-commission-w4`）
> 拍板日期：2026-08-06（四号会话直接与用户详谈，原文逐字记录）
> 提交：`docs: OD-01客户端五色只换1-3号+埋点 + OD-05功能色边界选乙+藤黄调深`（本次 commit）

---

## 一、OD-01 客户端五色主色是否换色 → **选 b（只换 1/2/3 号，4/5 保留）**

### 用户原文（逐字）

> **1 b)**

（对应选项 b：只换 1/2/3 号不达标的，4/5 保留）

### 拍板结论

- **数据槽位**：`data-accent="1..5"` 结构不变（5 槽位）。
- **1/2/3 号换新值**（第三方 §2.2.4 低饱和自然色方案）：
  - 1 号：`#34dbcb`（1.73:1 ❌）→ **月白青 `#356B69`**（6.07:1 ✅）
  - 2 号：`#34c2db`（2.12:1 ❌）→ **雾蓝 `#3F5E80`**（6.72:1 ✅）
  - 3 号：`#3498db`（3.15:1 ❌）→ **藤紫 `#5E5494`**（6.62:1 ✅）
- **4/5 号保留现状**（已达标，不回归）：
  - 4 号：`#346edb`（4.77:1 ✅）不动
  - 5 号：`#3445db`（6.95:1 ✅）不动
- **验收铁律**：新值对比度 ≥4.5:1 且不低于旧值（1/2/3 号实测 6.07/6.72/6.62 全部过线，无回归）。

### 补充拍板：埋点（用户原文「可以 埋。」，2026-08-06）

- **做埋点**：`theme_accent_change` 事件 + `palette_version` 字段（`neon-v1` / `natural-v2`），**埋点 ≤ 值替换**同批上线。
- **目的**：A 测后凭「换色率」区分「功能没用」vs「旧霓虹色太丑没人换」——换色率 <5% 且无主动需求→下线固化单一主色；5%~20%→保留不投入；>20%→需求真实可扩展（第三方 PM 预置判据）。
- **说明**：用户选 b（4/5 保留）不影响埋点，照常做。

### 落档建议（给一号/后续派工）

1. **实施文件**：`web/src/styles/theme.css:79-83`（亮色）+ `:86-90`（暗色）。⚠️ 暗色值沿用第三方 §2.2.4 的 v2 建议（`#8FBDBA`/`#90A9C9`/`#A9A0D6`），若 4/5 号暗色保留现状则需一并确认暗色混搭是否可接受（见下）。
2. **风险提示（已告知用户，用户接受）**：换后 1/2/3 号为低饱和自然色、4/5 号仍为霓虹深蓝/蓝紫，**五色视觉可能不连贯**。若实施后观感差，可再议 4/5 是否跟进换色（本次拍板不含）。
3. **与 STATUS 规则 12 的关系**：已向用户讲清——换色属无障碍修复（对比度），不等同于页面重设计，本次拍板即边界确认。
4. **埋点**：`theme_accent_change` + `palette_version` 已拍板做（见上），随换色同批派工；A 测后凭换色率决策功能去留（第三方 PM 判据）。

---

## 二、OD-05 功能色边界澄清 → **选乙（客户端不收、后台可收）**

### 用户原文（逐字）

> **选乙**

（对应选项乙：客户端功能色不收 token、后台纸墨盘可收）

### 拍板结论

- **边界**：STATUS 规则 11「功能色保持 EP 出厂色不跟随主色」**仅指客户端**（访客页面的徽章绿「可约稿」/橙「已排满」等功能色，不跟画师五色主色走）。
- **后台不受此限**：纸墨盘 `web/src/styles/artist-tokens.css:96-104` 已用国画色重定义 `--color-success/warning/danger`（石绿/藤黄/朱砂，v0.38 已拍板设计），**维持现状，不回退**。

### 补充拍板：藤黄调深（用户原文「2调深」，2026-08-06）

- **后台藤黄警告色调深**：`--warning #A8790B` → **`#966C0A`**（白字对比度 3.89:1 → 6.28:1 达标），同属「后台可收功能色」轨道。
- **实施范围**：`web/src/styles/artist-tokens.css` 中 `--warning` 值（浅色宣纸主题）；暗色 `#D9B36A` 保持（§2.2.2 第三方已列，不在本次调深范围）。
- **验收**：调深后白字对比度 ≥4.5:1（实测 6.28:1 ✅）。

### 落档建议（给一号直接复制进 STATUS 规则 11）

> 11. **功能色保持 EP 出厂色不跟随主色（2026-08-06 边界补充，四号落档）**：徽章绿"可约稿"/橙"已排满"等功能色沿用 EP 出厂值（theme.css:28-31 既有设计），不收进平台五色体系——功能色跨平台一致是惯例。**边界（OD-05 拍板）：此规则仅指客户端**（访客页面）；**画师后台不受此限**，纸墨盘国画语义色（`artist-tokens.css`：石绿=完成/藤黄=待确认/朱砂=逾期/危险，v0.38 拍板设计）维持现状，外部评审"收进 token 体系"建议继续打回。

---

## 三、验证情况

- ✅ merge master 成功（fast-forward `7f19962` → `b6a5063`），批 1 文档改动（维护说明书/开发自参考）已确认保留在 master
- ✅ 拍板前事实核实：theme.css 五色现状、artist-tokens.css 功能色重定义、第三方报告原文件（`C:\Users\qly19\Desktop\新建文件夹\` OPEN-DECISIONS.md + 03-设计 §2.2.4）均已实测/实读
- ✅ 零代码改动；用户原文逐字记录
- ⏳ 未跑测试（纯文档批，无代码变更）

## 四、待一号处理

1. STATUS 规则 11 补充文本见上（写清由四号补充，一号最终合并）。
2. OD-01 换色实施派工（theme.css 亮暗色值替换 + 可选埋点）由一号排期，建议排入 Beta 冲刺视觉批。
3. 交付报告合入后按 comms 清理惯例删除本批派工文件（01-to-04-Beta冲刺批2-OD详谈.md）。

---

*四号 2026-08-06*
