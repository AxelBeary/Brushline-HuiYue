# 交付：二号 D 路补漏批——REQ-029 四项用户拍板缺口（完成）

> 二号交付 · 2026-08-05 · 分支 `v037-manual-style-fix` · commit `4280fef` · 请一号审核，勿自行 merge

## 做了什么

四项缺口全部完成（对照派工文件 01-to-02-D路补漏批-REQ029用户拍板缺口-20260805.md）：

1. **R2 自定义单提示**：画风区旁提示「都可以不选，直接手动填价格录入自定义单」（多/单画风都显示，单画风也能自定义单）。同时调整提交拦截语义（REQ-029 §四验收3）：画风模式未选尺寸时，**手输过价 = 自定义单放行**；未手输 = 半途状态拦截（防误触 0 元单）。
2. **R5 自定义增项**：价格面板「＋添加」按钮（选了画风可录、自定义单也可录）；名称必填 ≤50 + 金额必填（允许负数=减项/让利、0=留痕）；上限 20 条可删；明细与基础价/目录增项并列、总价含合计（桌面 + 移动端双布局）。提交走 **创建后 addExtraItem 补写**（对齐 deadline/startDate 的 postCreate 补写模式）。
3. **R6 图片显示开关**：右栏「怎么录」顶部，默认开；关闭后画风卡 + 档位卡图片一起藏（含无封面首字占位），卡片退化纯文字；localStorage 记忆（`manualOrder_showImages`），不动后端。
4. **B2 取消选中**：`selectStyle` 点已选画风 = 取消选中（对齐档位卡 toggle），清空尺寸/增项/算价/脏标记。
5. **R4 自查**：calculateStylePrice 已吃倍率参数（前端 doStyleCalc 传 usageMultiplierId/rushMultiplierId + 后端 schema 接受），零改动 ✓

## R5 后端现状说明（派工要求报告）

**createOrder/createManualOrder 不支持提交时携带自定义增项字段**（schema 只有 styleSizeId/styleAddons 目录引用）。但现有 **`POST /api/artist/orders/:id/extra-items` 补写链路完全满足 REQ-029 语义**：`{name(≤100), priceCents(允许负数 -99999999~99999999)}` 上限 20，入账 `order_extra_items`（REQ-029 §二R5 自己写明"对应 extra_item 条目类型"），前端 `artistApi.addExtraItem` 已存在。**结论：零后端改动**，采用创建后逐条补写（对齐截稿日/开稿日既有模式）。实测验证：手输价 200 + 自定义增项 100 提交后，后端 addExtraItem 将条目并入 final_price_cents → 30000，账本一致。

## 改动文件

- `web/src/views/artist/ManualOrder.vue`（四项缺口：提示/自定义增项 UI+逻辑/图片开关/B2 toggle + 提交拦截调整 + 补写链）
- `web/src/locales/zh-CN.js`、`web/src/locales/en.js`（新增 11 键，只加不改）
- `web/src/views/artist/__tests__/ManualOrder.stylemode.test.js`（+7 用例）

## 验证结果

| 项 | 结果 |
|---|---|
| web vitest 全量 | **186/186 通过**（基线 179 + 新 7：R2 提示多/单画风、B2 取消选中+自定义单提交、R5 负数录入+补写、0 金额+上限20+删除、R6 关闭+localStorage 重挂载保持、什么都不选手输价提交、画风并存三路透传 styleSizeId+styleAddons+extraItems） |
| ESLint | 零错误零警告 |
| 浏览器实测（Playwright·Alice） | R2 提示可见 ✓；R6 默认 3 图 → 关 0 → **刷新仍 0** → 开 3 ✓；B2 选厚涂插画→尺寸 1→再点→0 ✓；R5 让利优惠 -¥50.00 + 加急包装 ¥100.00 → 删除 → 提交 ALICE-006 → **final=30000（手输 200 + 自定义 100 入账）** + extraItems 加急包装 10000 正确 ✓；测试订单已取消 |

## 已知说明

- 提交失败消息拼接沿用既有中文模式（postCreateFailed 链），未引入新 i18n 键（既有硬编码先例，最小 diff 原则）
- 测试订单 ALICE-006 已取消；临时脚本已删（git 干净）
