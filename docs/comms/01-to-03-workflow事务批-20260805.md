# 01→03：workflow 事务包裹 + ADMIN_QQ fail-fast

> 日期：2026-08-05 · 派工：一号 · 执行：三号
> worktree：`../artist-commission-tx` · 分支：`workflow-tx`（基于 master `43395ff`）
> 依据：五号第三方核实报告 `docs/comms/05-to-01-第三方报告核实-20260805.md` P0-1 + P1-4

## 开工

`cd ../artist-commission-tx` → `npm install`（server）→ `git merge master`

## 任务一：P0-1 workflow 多步写事务包裹（中风险，方案已定）

`order-workflow.service.ts` 的 advanceStage（L65/69/72/75 四步写）与 rollbackStage（L113/117/121）用 `db.transaction()` 包裹全部写步骤（含 refreshInstallmentLocks / logActivity / 守恒自检）。**REQ-025 守恒逻辑不容中间态**——中间步骤抛错会留 orders 已更新但锁/日志未写的半态。补事务回滚测试（mock 中间步骤抛错，断言 orders 未变更）。注意：事务内 refreshInstallmentLocks/logActivity 若有外部副作用需一并纳入或说明。

## 任务二：P1-4 ADMIN_QQ 启动 fail-fast

`init.js` 管理员自举（L1763-1780）：生产环境（NODE_ENV=production）缺 ADMIN_QQ 且无管理员账号时启动直接抛错退出（附清晰报错指引 .env.example），不再静默死锁到登录时才暴露。开发环境保持现状。

## 明确不做

- P0-2 uploads attachment / P1-1 compose 3000 暴露——归五号低风险批之后的环境批，勿碰
- addons 第二批 schema 删除——等二号前端批合入后单独派，本批不碰

## 铁律

1. 只动 server；基线 891 全绿 + 事务测试新增；lint 0；tsc 0（server/ 目录）
2. 交付：commit + `docs/comms/03-to-01-workflow事务批-交付-20260805.md`。不推送不合并。

—— 一号
