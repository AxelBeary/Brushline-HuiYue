# 二号 → 一号：v0.16 前端批次1 提交审核

> 日期：2026-07-30

---

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/v016-frontend-batch1`（worktree: `artist-commission-wt02`）

【任务编号】
任务A：工艺CSS升级 / 任务B：R54 档位卡片

【修改模块】
客户前端样式 / 模板组件 / 画师后台档位页 / i18n

【修改内容】

**Commit 1** `ae0a762` — `style(client): 工艺CSS升级`

1. **弹性缓动曲线全局化**：`theme.css` 新增 `--ease-bounce: cubic-bezier(.34, 1.56, .64, 1)`，`templates.css` 滚动渐入/CTA 过渡改用该变量（带 fallback）
2. **按钮三态模型**：`theme.css` 全局 `.el-button` hover=translateY(-3px)+阴影、active=translateY(-1px)+scale(.98)，排除 disabled/loading 态
3. **clamp() 流式字号**：TplHero 铭牌标题 `clamp(30px,5vw,40px)`、移动端 `clamp(24px,6vw,30px)`；TplTierGrid 名称/价格；LandingPage h1；Folio 标题/CTA 标题
4. **minmax(0,1fr) 防溢出**：TplHero split 双栏、ArtistHomeClassic 侧栏+内容栏
5. **prefers-reduced-motion 全局兜底**：`theme.css` 末尾 `*, *::before, *::after` 关闭动画/过渡
6. **TplTierGrid 卡片悬浮**：transition 改用 `--ease-bounce`

**Commit 2** `b8f0c1d` — `feat(client): R54档位页表格→卡片布局`

1. el-table 6列 → 响应式卡片网格 `repeat(auto-fill, minmax(240px, 1fr))`
2. 卡片结构：示例图区(160px高，保留R55拖拽/点击直传+确认弹窗) → 信息区(名称+价格+描述+工期) → 操作区(编辑/删除)
3. 空状态：`el-empty` + `tiers.empty` 键
4. i18n 修复：编辑/删除按钮从硬编码中文改为 `$t('common.edit')`/`$t('common.delete')`；删除确认改用 `$t('tiers.confirmDelete')`；工期改用 `$t('tiers.daysUnit')`
5. 新增 i18n 键：`tiers.empty`（中：还没有档位 / 英：No tiers yet）

【涉及文件】
- `web/src/styles/theme.css`（+27行：缓动变量+按钮三态+reduced-motion）
- `web/src/styles/templates.css`（2处缓动替换）
- `web/src/components/templates/TplHero.vue`（clamp+minmax）
- `web/src/components/templates/TplTierGrid.vue`（clamp+缓动）
- `web/src/views/client/LandingPage.vue`（clamp）
- `web/src/views/client/templates/ArtistHomeClassic.vue`（minmax）
- `web/src/views/client/templates/ArtistHomeFolio.vue`（clamp×2）
- `web/src/views/artist/TierManage.vue`（表格→卡片，+114/-56）
- `web/src/locales/zh-CN.js`（+2行：tiers.empty）
- `web/src/locales/en.js`（+2行：tiers.empty）

【是否修改非客户前端文件】
`locales/zh-CN.js` 和 `locales/en.js` 属于"需要一号协调"清单，但新增 i18n 键是前端功能必需（R54 空状态），且仅追加不修改既有内容。`theme.css` 同理——派工文件授权了 CSS 升级。

【接口依赖】
无。纯前端改动，不涉及新接口。

【自测情况】

| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过（9.57s） |
| console.log / TODO / v-html | ✅ 新增行零残留 |
| i18n 中英同步 | ✅ tiers.empty × 2 语言 |
| 改动文件范围 | ✅ 10 文件均在授权内 |
| Rebase master | ✅ 基于 ff73941 |

**需人工验证项**（无浏览器环境）：
- 按钮 hover 浮起/active 按下效果是否自然（全局生效，所有页面）
- 档位卡片布局在不同档位数量下的排列（1/3/6/10+）
- R55 拖拽/点击直传在卡片布局下是否正常（拖入高亮、确认弹窗、上传刷新）
- 暗色模式下卡片背景/边框/文字对比度
- prefers-reduced-motion 开启后动画是否完全关闭
- clamp() 字号在 375px / 768px / 1440px 三个宽度下的表现

【可能影响】
- 按钮三态是全局 `.el-button` 覆写，影响所有页面（画师后台+管理后台+客户端）的按钮交互
- `--ease-bounce` 变量全局可用，后续组件可直接引用
- 档位卡片仅影响画师后台 TierManage 页面，不影响客户端 TplTierGrid（客户侧档位展示是独立组件）

【待确认问题】
- 按钮三态的 translateY(-3px) 幅度是否需要调整（当前参考 H5 原型报告推荐值）
- 档位卡片示例图高度 160px 是否合适（原表格仅 56px 缩略图）

【申请】
申请一号审核合并。
