# 04-派工-docs修补批-20260808.md

> 派工：一号 → 四号（requirements/docs）
> worktree：`../artist-commission-w4`（分支 `beta/docs-fix-batch`）
> 背景：docs 核实子代理报告（`docs/comms/subagent-docs-核实-交付-20260808.md`）发现 **8 项过时误导 + 12 项轻微**。STATUS 顶部已由一号刷新（v39），本批修其余
> 基线：master `b8866cf`。**开工第一步 `git merge master`**。
> 门禁：纯文档批——核对要点逐条引用代码证据 + 交付报告

## 施工步骤（按子代理报告逐条修，先读报告全文）

### 1. REQ 状态行批量标记已实施（6 份）
对照报告 + git log --grep 核实后，在下列需求文档顶部状态行标注「✅ 已实施 + 合入 commit」：
- REQ-031（4 项候选 A1/A2/B1/C4 已全部合入 `709e3d1`）
- REQ-035（后端包 `77eee7c` / 四件套 `709e3d1` / 今天吃什么页 `4a89555`+扩张 `f526b2f` / 时间条月历 `0ecf281`；时间条已重派 `beta/timeline-redo`）
- REQ-033 / REQ-034 / REQ-026 / REQ-027 / REQ-029（按 git log 核实各自实施状态，标「已实施」或保留待办）
- ⚠️ 只改状态行，不改需求正文；每条标注核实依据（commit 号）

### 2. external-wiki README 失效引用修正（3 处）
`docs/external-wiki/README.md` 引用的文件不存在（文件名已变）：
- :23 `docs/comms/02-to-01-repowiki重写-交付.md` → 查实际文件名（grep docs/comms/ 下 repowiki 交付报告），改正确引用
- :63 / :67 类似（P2 非认证抽样交付报告文件名已变）
- 修复 = 引用改到实际存在的文件；若原报告已合入即删，改用「内容已并入 STATUS/外部 wiki 归档表」表述

### 3. changelog 补 v0.42/v0.43（若有 changelog 文件）
- 先查 `docs/` 下 changelog 文件名（grep changelog）
- 若存在：补 v0.42（QueueBoard/ManualOrder 拆分+strict+any清零+质量三连+repowiki P0）和 v0.43（布局批+菜谱+hover+重设计+后端包+时间条+四件套+审计）条目，按 git log 提取
- ⚠️ npm 版本口径（0.41.0 vs 0.43.0）**不要改**——待一号 v0.44 收编时裁决，changelog 只记业务版本 v0.x

### 4. 开发自参考测试基线修正
- 查 `docs/开发自参考.md`（或实际文件名）:24 测试基线「925/58」→ 实际 server **1000/1000（64 文件）**（vitest 实测）· web 215/215（13 文件）
- 顺带 grep 其他文档是否有同类过时基线，一并修

### 5. 交付报告
- `docs/comms/04-to-01-docs修补批-交付-20260808.md`：逐项 已修/跳过+理由 + 每处代码/文件证据 + 与子代理报告逐条对照表

## 铁律

1. 只改文档，不碰代码
2. 每条修正必须有依据（git log / Test-Path / 读文件）
3. 外部 repowiki 原文（C:\Users\qly19\Desktop\repowiki\）一字不动
4. 拿不准的标「🟡 需一号裁决」不硬改

## 验证门禁

1. 无代码改动（`git diff --stat` 只动 docs/）
2. 每条修正可溯源（报告里写证据）
3. 报告 commit 到分支 `beta/docs-fix-batch`，不推送不合并

## 交付

- commit 到分支 `beta/docs-fix-batch`，不推送不合并
- 交付报告 `docs/comms/04-to-01-docs修补批-交付-20260808.md`
