# 派工：二号 F1 — OrderForm"写需求"步骤被跳过修复

> 分支：v036-w2-client（worktree wt-02）。**开工第一步 `git merge master` 再读本文件。**
> 风险等级：低

## 根因（一号已实锤）

OrderForm.vue 349 行：增项+倍率步骤（addonStep）的"下一步"按钮
`@click="step = contactStep"` —— 直接跳到联系方式，把中间的写需求步骤（detailStep）整个跳掉。

用户实测路径（多画风）：选画风(1) → 选尺寸(2) → 增项+倍率(3) →【下一步】→ 直接到联系方式(5)，写需求(4) 消失，只能手动返回。单画风同理（步骤 2 → 4）。

## 任务

1. 349 行改为 `@click="step = detailStep"`
2. **全模式回归**：旧模型（无画风）/ 单画风 / 多画风三条步骤链逐步走一遍，确认每个"下一步""上一步"都按 stepDefs 顺序移动，无其他跳步（对照 640-665 行的步骤定义）
3. 加组件测试或 useOrderForm 测试覆盖"增项步骤下一步应到写需求"

## 授权文件

web/src/views/client/OrderForm.vue + web/src/composables/__tests__/（或组件测试）。
**只改步骤导航区域**。上传区（el-upload，381 行附近）二号-B 要加拖拽守卫，你别碰，防冲突。

## 验收

web 全量测试绿 + 三模式步骤链人工走查记录写进提交说明。
提交格式：`fix(client): 约稿表单增项步骤下一步跳过写需求修复`
