# 02-to-01 交付报告：批3 前端清理（死文件 / 死引用 / i18n 死键 / composables / fetchProfile）

> 执行：二号（前端）｜分支：fix/batch3-web（基于 master 4e3eae2）｜日期：2026-08-09
> 依据：`%TEMP%\audit-D\report-d.md`（完整读取后按序执行）

## 一、任务理解

按派工 6 项顺序执行，每项一次 commit；i18n 死键删除按要求拆成多次小步提交，每次提交后跑 `npm run check:i18n` 确认无新增违规；locales 保持原编码（UTF-8 BOM）；最后全量验收并交付本报告。

## 二、改动清单（按执行顺序 + commit hash）

### 1) 死文件删除 + SlotManage 注释修正 —— `d778308`

- 删除 `web/src/components/artist/dashboard/StatusSwitch.vue`（全库零引用，逻辑已内联 SlotManage）
- 删除 `web/src/components/shared/HySpinner.vue`（全库零引用）
- 删除 `web/src/components/templates/TplCoverShowcase.vue`（全库零引用，不影响 TplHero/TplGallery/TplStyleGrid 对 useArtistData 的使用）
- `web/src/views/artist/SlotManage.vue` 第 6/112 行注释：「与仪表盘 StatusSwitch 同逻辑」→「与开稿管理内联逻辑一致」

### 2) App.vue 冗余导入删除 —— `ca5c1bf`

- `web/src/App.vue:6` 删除 `import { ElConfigProvider } from 'element-plus'`；模板 `<el-config-provider>` 由 unplugin-vue-components 的 ElementPlusResolver 自动注册解析。删除后 `npm run build` 验证通过。

### 3) i18n 死键删除（A 类 182 + B 类 2 + C 类 1 + D 类 9 = 194 键，zh+en 同步，10 次小步提交）

| commit | 批次 | 键数 | 范围 |
|--------|------|------|------|
| d508060 | batch1 | 23 | admin 4 + artistHome 11 + landing 2 + toolsExport 3 + watermark 2 + quickAction 1 |
| 9287f17 | batch2 | 20 | dashboard 18 + queue 1 + delivery 1 |
| 327a817 | batch3 | 24 | multiplier 整组（v50 清退 price_multipliers） |
| 41b71b1 | batch4a | 20 | tiers 前 20 |
| 02e24f8 | batch4b | 19 | tiers 后 19 |
| 9a47b85 | batch5 | 23 | manualOrder 15 + orderForm 8 |
| ea5f892 | batch6 | 19 | styleManage（v0.36 改版前旧文案） |
| e6b3491 | batch7 | 25 | orderDetail 10 + track 6 + embed 7 + templates 2 |
| 441bfda | batch8 | 12 | settings 6 + rules 1 + pref 2 + common（B 类 2 + C 类 1） |
| d2970c4 | batch9 | 9 | errors.* D 类死定义（ADDON_* 6 + MULTIPLIER_* 2 + PRICING_TIER_REQUIRED） |

说明：

- 删除前用 AST 全量比对（两语言各 1942 键）：A 类 183 个中 **182 个**实际存在并删除；`manualOrder.selectSizeFirst` 两侧均不存在（见第三节差异说明）。
- 删除仅精确移除目标键行/键片段，不动相邻存活键、注释与结构；每批删除后 `npm run check:i18n` 均输出 `OK — 存量违规 9 条豁免，无新增硬编码中文`。
- 测试独占键 `manualOrder.tier`（report-d 3.7，ManualOrder.stylemode.test.js 断言）按清单保留未删。
- B 类 `common.orderStatus.unknown` / `common.status.unknown`：服务端 OrderStatus 枚举与画师状态枚举（artist.routes.ts:123 `open/full/break/hidden`）永无 unknown，已删。
- C 类 `common.status.hidden`：删除前人工验证不可达——`artist.routes.ts:32` 公开列表过滤 `status !== 'hidden'`；`:48-50` 隐藏画师公开 profile 仅返回最小信息；LandingPage.vue 数据源已过滤、NotFound.vue:30 仅推荐 `status === 'open'`；公开主页（ArtistHome）不渲染 `common.status.*`。已删。

### 4) i18n 缺键补齐（16 个 errors.*，zh+en）—— `12c73b0`

按 report-d 3.2 补齐：`TOTP_NOT_BOUND` / `TOTP_INVALID` / `TOTP_LOCKED` / `TOTP_BIND_INVALID`、`PLATFORM_NOT_FOUND` / `PLATFORM_NAME_EMPTY` / `PLATFORM_ICON_REQUIRED` / `PLATFORM_DOMAIN_INVALID` / `PLATFORM_DOMAIN_TAKEN`、`PUBLISH_WRONG_STATUS` / `DELIVERABLE_NOT_FOUND`、`PRICE_CHANGE_AFTER_DONE` / `PRICING_CONSERVATION`、`STYLE_SIZE_NOT_AVAILABLE`、`ADDON_SELECTION_MUTEX`、`ORDER_NOT_OWNED`。

- zh 文案对照 `server/src/shared/errors.ts` 的 `ERROR_MESSAGES` 语义写成用户可读文案；en 对应翻译。
- 按语义分组插入 errors 块对应区段（认证 / 订单 / 发布作品 / 增项选择 / 计价引擎 / 社交平台 / 多画风）。
- 补键后 zh/en 键集完全一致（各 1764 键，diff 为空），BOM 保持。

### 5) fetchProfile 误登出修复 —— `433f3f1`

- `web/src/stores/artist.js`：`fetchProfile` 的 catch 改为**仅当 `err.status === 401` 才 `logout()`**，其余异常原样抛出（api 拦截器 05D-I1/E1 已为错误对象附加 `status` 字段，判断有效）。
- `web/src/views/artist/Dashboard.vue`：`onMounted` 中 `await store.fetchProfile()` 包本地 `try/catch`，失败 `console.warn` 记录（带 eslint-disable 注释，与 api/index.js 惯例一致），不白屏、不额外踢人，统计卡/留言模块照常独立加载。

### 6) composables 死导出删除 —— `0aa4dc8`

- `web/src/composables/useActivityLog.js`：删除全库 0 导入且内部无引用的 `ACTION_TYPES` 导出（连同注释整体删除，避免改为非导出常量后触发 `no-unused-vars`）。
- 测试专用导出 `isSystemFileDrag`（useDropGuard）、`mapStageToStatus` / `findStageForStatus`（useStageStatus）按报告保留。

## 三、与审计报告的实际差异（以本分支代码为准）

1. **report-d 3.2 的 🔴 `orderForm.selectSizeFirst` 命名空间错位在本分支不存在**：AST 全量比对确认 `orderForm.selectSizeFirst` 在 zh:610 / en:611 已定义，`OrderForm.vue:585/589` 的 `t('orderForm.selectSizeFirst')` 可正常解析；`manualOrder.selectSizeFirst` 两侧均无定义，故 A 类清单中该项无键可删。未做任何额外修改。
2. 报告行号基于 `artist-commission` 仓库，本分支（w-b3w worktree）除上述 1 键外其余行号/键全部吻合，删除清单逐一以实际键树校验。

## 四、验收证据（全量）

```
npm run lint        ✅ 0 errors 0 warnings
npx vitest run      ✅ 17 files / 254 tests passed
npm run check:i18n  ✅ OK — 存量违规 9 条豁免，无新增硬编码中文
npm run build       ✅ built in 7.91s
```

## 五、清理与纪律确认

- `web/src/locales/zh-CN.js` / `en.js` 均保持 UTF-8 BOM（EF BB BF 字节级验证）。
- 未引入任何新依赖；未格式化无关行；未动报告清单外的键。
- 未 push、未 merge、未触碰 master；本分支仅含上述 15 个 commit + 本交付文档。
- 临时工具脚本（i18n 删除脚本等）均在系统 %TEMP% 下，未落入仓库。

二号 2026-08-09
