# 05-to-01-UI优化批P0-交付-20260807.md

> 五号转交一号，文件：docs/comms/05-to-01-UI优化批P0-交付-20260807.md
> 分支：`beta/ui-opt-p0`（worktree ../artist-commission-w5），已 merge master（a7a9b99），不推送不合并
> 施工图：docs/comms/05-派工-UI优化批P0-20260807.md

## 结论

4/4 全部完成 ✅。门禁全过：web 测试 215/215 · eslint 0 error · build 成功 · diff 只动授权文件 · before/after 截图齐。

## 改动清单

### P0-1. OrderForm 移动端底部粘性操作条 ✅
- 模板：`.step-layout` 结束后新增 `.mobile-cta-bar`（v-if="artist"），含价格区（`displayPrice.toFixed(2)`，已核实 computed 存在）+ 主按钮（contactStep 显示「提交」，其余「下一步」）
- CSS（scoped）：桌面 `display:none`；`@media (max-width: 860px)` sticky bottom + blur 背景 + safe-area；`.step-nav { padding-bottom: 64px }` 防遮挡
- 脚本：`onMobileNext()` 复用既有流转——contactStep→`openReceipt()`、detailStep→`goNextFromDetail()`、旧模型 step1→detailStep、多画风 step1→sizeStep、sizeStep→addonStep、addonStep→detailStep
- 顺手修：OrderForm:904 `.preselect-banner-btn` 的 `transition: all 0.2s` → `border-color, background`（动画批纪律，施工图授权）
- 桌面原「下一步/提交」按钮不动，双路径并存

### P0-2. OrderForm 步骤指示器移动端保留当前步名 ✅
- CSS：860px 下 `.step-label` 全隐藏后追加 `.step-label--on { display: inline }` + `.step-item:has(.step-label--on) { order: -1 }`（父容器 .step-indicator 是 flex，order 有效）
- 模板：el-form 内首个步骤块前加 `.step-eyebrow`（`第 {cur} / {total} 步`，stepDefs.length 动态）
- i18n：zh-CN.js / en.js 同步新增 `orderForm.stepProgress`（4 空格缩进对齐）

### P0-3. OrderList 移动端卡片视图 ✅
- 模板：`.order-table-wrap` 前新增 `.order-cards` v-for displayedOrders（已核实存在），卡片含缩略图（focusImageUrl）/订单号/档位·客户/状态 tag/时间/优先级 tag，点击跳 `/orders/{id}?from=orders`
- CSS（scoped）：`@media (max-width: 768px)` 隐藏表格 + 显示卡片列；变量用后台纸墨 token（--card/--line/--ink/--ink3，已 grep artist-tokens.css 确认存在）；额外加 `cursor: pointer`
- 分页/空态（InkEmpty）在容器外，窄屏仍可用

### P0-4. 间距 token 体系 ✅
- theme.css `:root` 内追加（grep 确认无同名变量）：`--sp-1..7`（4/8/12/16/24/32/48px）+ `--fs-page-title/section/body/aux/eyebrow`
- 只建立 token，不动旧值，不替换存量

## 验证门禁（逐条）

1. `cd web && npm run test:web` → **215/215 全绿**（13 文件）
2. `cd web && npm run lint` → **0 error**（3 个 warning 为既有：api/index.js:41/73、main.js:34 console，非本批引入）
3. `cd web && npm run build` → **成功**（11.5s，chunk 大小警告为既有）
4. `git diff --stat` → 仅 5 个授权文件：OrderForm.vue / OrderList.vue / theme.css / zh-CN.js / en.js（+100 −1）
5. 截图（workspace/temp/ui-opt-p0/，375px 视口，Playwright + Edge，隔离实例）：
   - P0-1：`orderform-after-cta.png` / `orderform-before-cta.png`
   - P0-2：`orderform-after-indicator.png` / `orderform-before-indicator.png`（+ full 整页各一张）
   - P0-3：`orderlist-after-cards.png` / `orderlist-before-table.png`
   - P0-4：代码 diff 即证据（无需截图）
6. 实测断言（同实例执行）：CTA 条 `display:flex` 文本「合计 ¥50.00 下一步」；眉题「第 1 / 3 步」；当前步名「选档位」；OrderList 卡片 3 张（TEST-001 待确认 赭/藤/灰优先级标签渲染正常）

## 实测环境说明

- 隔离测试库 `workspace/temp/ui-opt-p0/test-p0.db`（seed alice/bob + 3 条测试订单）+ 隔离 server（PORT=3100, WEB_DIST=web/dist），**未碰生产容器 3000**
- before 基线 = 同一实例 CSS 注入隐藏 P0 新增元素（`display:none!important` + order 还原），与 master 渲染等价（P0 改动均为新增类/覆盖 display，无结构重排），保证前后唯一差异是目标功能
- TOTP 登录走真实链路（固定测试密钥注入隔离库，RFC 6238 现算），测完即删库

## 自修说明（与施工图差异）

1. `onMobileNext` 中旧模型 step1 目标写 `detailStep.value`、多画风 step1 目标写 `sizeStep.value`（施工图蓝图写硬编码 2，语义等价、按源码变量对齐更稳）
2. OrderList 卡片加 `cursor: pointer`（施工图 CSS 未列，点击跳转是模板明确行为，可用性补充）
3. i18n 新键缩进修正为 4 空格（首次写入 2 空格，已对齐既有键）
4. `transition: all` 顺手修（施工图明确授权）

## 待一号确认

- 视觉验收（截图 + 用户口述）；P0-4 token 替换存量是后续批，本批不碰
- 分支未推送未合并，交一号审核