# 后台导航页巡检报告（05D → 01）

> 巡检人：五号（bugfix）· 日期：2026-08-08
> 范围：画师后台全部导航页（ArtistLayout 四组 19 页 + /tiers 五个子模块）
> 性质：只读巡检（零代码改动），发现 → 报告
> 依据：施工图 docs/comms/05D-派工-后台巡检-20260808.md
> 基线：master @ f3724bf（已 merge 后巡检）

## 〇、执行概要

- 19 个导航页逐页过完，三查维度（逻辑/操作/布局）全覆盖，共记 **32 条**：🔴 0 · 🟡 14 · 🟢 15 · 页级 ✅ 2
- 无「明显错误/错位/操作不通」级问题（🔴=0）；现有页面质量整体良好（上一轮批修复大多已生效：F4 上传区、B7 表格横滚、滑块确认、空态统一等）
- 最值得做的 3 件事：①排期看板交付后「完成区」不刷新（1 行改动，数据一致性）；②Settings 多 tab 未保存修改静默丢失（防数据丢失）；③作品管理触屏设备操作按钮不可见（移动端可用性）
- 共性主题：**工具组 4 个页面用裸 fetch 不走统一 API 拦截器**（401/超时/i18n 全部缺失）——建议收口；**订单下拉 100 条上限**在 3 个工具页重复出现——建议统一解决

## 一、汇总表

| 页 | 逻辑 | 操作 | 布局 | 级别摘要 |
|----|------|------|------|---------|
| /dashboard | ✅ | 🟡×1 | 🟢×1 | 🟡1 🟢1 |
| /queue | 🟡×1 | 🟢×1 | ✅ | 🟡1 🟢1 |
| /orders | 🟡×1 | 🟢×1 | ✅ | 🟡1 🟢1 |
| /orders/new | 🟡×1 | 🟢×1 | ✅ | 🟡1 🟢1 |
| /slots | 🟢×1 | ✅ | 🟢×1 | 🟢2 |
| /tiers | 🟡×1(需验证) | 🟢×1 | ✅ | 🟡1 🟢1 |
| /artworks | 🟡×1 | 🟡×1 | 🟢×1 | 🟡2 🟢1 |
| /guestbook | 🟡×1 | 🟢×1 | ✅ | 🟡1 🟢1 |
| /tools/food | ✅ | ✅ | ✅ | 页级 ✅ |
| /tools/watermark | 🟡×1 | 🟢×1 | ✅ | 🟡1 🟢1 |
| /tools/export | 🟡×1 | 🟢×1 | ✅ | 🟡1 🟢1 |
| /tools/income | 🟡×2 | 🟢×1 | 🟢×1(需验证) | 🟡2 🟢2 |
| /tools/puzzle | 🟡×1 | 🟢×1 | ✅ | 🟡1 🟢1 |
| /tools/schedule | ✅ | ✅ | 🟢×1 | 🟢1 |
| /tools/clients | ✅ | ✅ | 🟢×1 | 🟢1 |
| /tools/returning | ✅ | 🟢×2 | ✅ | 🟢2 |
| /stats | ✅ | 🟢×1 | ✅ | 🟢1 |
| /settings | 🟡×1 | ✅ | ✅ | 🟡1 |
| /preferences | ✅ | ✅ | ✅ | 页级 ✅ |

合计：🔴 0 · 🟡 14 · 🟢 15 · 页级 ✅ 2

## 二、逐条详情（按页分组）

### /dashboard 仪表盘
- 🟡-D1 `web/src/views/artist/Dashboard.vue` L48-73：留言审核卡片内**每条留言（含已 approved/rejected）都常驻一个回复输入框**，已回复过的留言也占一块输入区，列表信息密度高；且 480px 滚动区内没有「去留言管理」入口，留言多时只能在此滚动处理。建议：已处理（approved/rejected）留言折叠回复框、只在编辑时展开；卡片底部加「管理全部留言 → /guestbook」链接。
- 🟢-D2 `web/src/views/artist/Dashboard.vue` L208：`.gb-mod-list { max-height: 480px; overflow-y: auto }` 内部滚动，滚动条细且无渐隐提示，用户不易察觉下面还有内容。建议：卡底加淡出渐隐或「查看更多」提示。

### /queue 排期看板
- 🟡-Q1 `web/src/views/artist/QueueBoard.vue` L79-81（onDeliveredFromBoard）与 L173-175（refreshAll）：**交付成功后只 loadQueue()，refreshAll 也只刷 queue+buffer，completed「完成区」不刷新**——刚交付的订单不会出现在完成区（最近 7 天已交付），必须整页刷新才看得到，用户会以为完成区没更新。建议：onDeliveredFromBoard 与 refreshAll 补 `loadCompletedQueue()`（1 行改动，收益直接）。
- 🟢-Q2 `web/src/components/artist/queue/QueueBoardList.vue` L287-291（onMounted 只调一次 getWorkflow）：工作流节点只在进页时拉一次，后端改动节点后看板「推进到下一节点」按钮的下一节点信息会过期（点推进才报错）。建议：refresh-queue 时顺带重拉 workflowStages。

### /orders 订单管理
- 🟡-O1 `web/src/views/artist/OrderList.vue` L154-160 + L208：**复合筛选 active/completed 是「客户端过滤已加载页」**（默认 pageSize 50，后端 `server/src/features/order/order.routes.ts` L348-351 确认分页，上限 200）。统计卡跳转 `?status=active|completed` 时只显示前 50 条订单的子集，但分页 total 显示全量 → **订单 >50 条时漏单且总数误导**。建议：复合值走后端筛选（后端加 active/completed 语义）或前端按需拉全量再过滤。
- 🟢-O2 `web/src/views/artist/OrderList.vue` L54-80：桌面端表格**整行不可点击进详情**，须点右侧「详情」按钮（2 步）；移动端卡片已整卡可点。建议：桌面端加 row-click 进详情（保留按钮兼容）。

### /orders/new 手动录单
- 🟡-M1 `web/src/components/artist/order/ManualOrderLeft.vue` L173-177：参考图上传 >10MB 时**只 ElMessage.warning + return，未 throw**——el-upload 的 http-request 无异常会认为上传「成功」并显示缩略图，但实际文件未上传，画师易误以为传上了（提交时 uploadedRefs 无该路径）。建议：超限分支 `throw` 或 return rejected，让 EP 标记失败。🟡 需实测确认 EP 对 return undefined 的处理。
- 🟢-M2 `web/src/views/artist/ManualOrder.vue` L218：QQ 历史每次 `getOrders(pageSize 200)` 全量拉回再客户端过滤——订单量大时每次输 QQ 都拉 200 条。建议：后端 q 参数已支持，可直接 `getOrders(undefined, { q: qq })` 服务端过滤。

### /slots 开稿管理
- 🟢-S1 `web/src/views/artist/SlotManage.vue` L203：`.slot-manage { max-width: 640px; margin-top: 16px }` 未水平居中（只有 margin-top），卡片贴左，与 /preferences（pref-card 也是 max-width 600 无居中，风格一致）但视觉上偏侧。建议：加 `margin: 0 auto` 或统一布局约定。
- 🟢-S2 `web/src/views/artist/SlotManage.vue`（整页）：四个卡片分散修改、仅底部一个「保存」按钮，**离开页面无未保存提示**（对照 WorkflowPaymentEditor 有 beforeunload 拦截）。改一半切走即丢。建议：加脏检测/离开提示（低成本）。

### /tiers 价格管理（5 子模块）
- 🟡-T1 `web/src/components/artist/MultiplierManager.vue` L46：倍率 `:min="1.0"`——若业务存在「优惠/折扣倍率」（<1，如老客 0.9 折）则录不了；若倍率仅作加价（加急 ×1.2）则合理。**需确认产品语义**，标 🟡 需验证。
- 🟢-T2 `web/src/components/artist/DiscountCodeManager.vue` L17：折扣码表格**无「复制」按钮**，画师要把码发给客户需手动选中复制（码 20 位内，容易错）。建议：行内加复制按钮（clipboard API，同 ScheduleSharePage 模式）。

### /artworks 作品管理
- 🟡-A1 `web/src/views/artist/ArtworkManage.vue` L553-559（.artwork-actions opacity 0 → hover 1）与 L545（主图区同款）：**触屏设备无 hover，编辑/删除按钮实际不可见**（:focus-within 在 touch tap 上不保证触发；tap 图片会先触发 el-image 预览）。这是移动端核心操作（删图/编辑）不可达。建议：≤768px 常显或改为点击展开操作层。
- 🟡-A2 `web/src/views/artist/ArtworkManage.vue` L332-343（handleUpload）+ L232-233（paste maxSizeMB 10）：**drag/点击上传无前端大小限制**（仅粘贴路径有 10MB 检查），大文件直接发后端被拒才知道。建议：handleUpload 开头补与粘贴一致的大小/类型检查。
- 🟢-A3 `web/src/views/artist/ArtworkManage.vue`（doBatchDelete 逐条 deleteArtwork）：批量删除无后端批量端点，删 N 张 = N 个请求，多选 20+ 张时明显慢。建议：后端加批量删除端点（前端已具备 ids 列表）。

### /guestbook 留言管理
- 🟡-G1 `web/src/views/artist/GuestbookManage.vue` L178 + L150-157：**后端返回全量留言，前端本地分页**——留言量数百条时首屏拉全量慢。公开留言接口已有 page/pageSize，建议管理接口对齐后端分页。
- 🟢-G2 `web/src/views/artist/GuestbookManage.vue`（筛选区）：pending 角标在筛选 tab 上，但无「一键审完/全部已读」快捷操作。留言多时逐条点通过较累。建议：可选加批量操作。

### /tools/food 今天吃什么
- ✅ 已查无问题：四模式（健康/糖尿病/痛风/外卖）选择、糖尿病/痛风固定免责提示、随机推荐防连续重复（池子>1 换一条）、初始空态引导、双主题 token 适配、键盘可达（button 原生）。未见优化空间。

### /tools/watermark 图片水印
- 🟡-W1 `web/src/views/artist/Watermark.vue` L295：**完稿图订单下拉一次只取 100 条**（getOrders pageSize 100 无翻页/搜索）——订单 >100 时选不到更早订单的完稿图。建议：下拉加搜索（filterable 已有）或分页加载。
- 🟢-W2 `web/src/views/artist/Watermark.vue`（exportImage 下载名）：导出文件名 `watermarked-时间戳.png` 不带来源名，下载多张后难区分。建议：带原图名或订单号。

### /tools/export 收入导出
- 🟡-E1 `web/src/views/artist/ToolsExport.vue` L111 + L145：**裸 fetch 不走 axios 拦截器**（对照 `web/src/api/index.js` L11-61：401 自动登出/15s 超时/i18n 错误翻译全部缺失）——工具页 401 时只提示错误不登出，慢请求无超时。建议：standalone-incomes 汇总封装进 artistApi（export.csv 下载可保留 fetch，但补 401/超时处理）。
- 🟢-E2 `web/src/views/artist/ToolsExport.vue` L72-79（income-grid）：订单/总收入两格显示「不可用」占位——代码注释已承认「后端区间汇总端点缺口」。两个永久「不可用」格对用户是噪音。建议：弱化（隐藏/注脚）或补后端端点后再亮出。

### /tools/income 散单记账
- 🟡-I1 `web/src/views/artist/StandaloneIncome.vue` L137/180/207：三处裸 fetch（POST/GET/DELETE），与 E1 同问题（401 不登出/无超时/无 i18n）。建议统一封装进 artistApi。
- 🟡-I2 `web/src/views/artist/StandaloneIncome.vue`（列表）：记账明细**全量加载无分页/月份筛选**——长年记账后列表越来越长。建议：加月份筛选或分页。
- 🟢-I3 `web/src/views/artist/StandaloneIncome.vue` L273：`.si-row-amount { color: var(--hq) }` 金额用花青上色，与「统计数字墨色不上色」铁律（REQ §1.1）及其他页金额墨色（OrderList/ReturningClients）不一致。对照 ToolsExport 的 income-standalone 同用花青，疑为「收入强调」的有意设计。**需确认**，标 🟡 需验证；若无意则统一墨色。

### /tools/puzzle 进度拼图
- 🟡-P1 `web/src/views/artist/PuzzlePage.vue` L111：同 W1，订单下拉 100 条上限。
- 🟢-P2 `web/src/views/artist/PuzzlePage.vue`（loadImage）：图片加载无超时——单张挂起时预览/导出一直 busy。建议：加超时（同 Watermark 的 loadImage 问题，可抽公共 util）。

### /tools/schedule 排期公示
- 🟢-SC1 `web/src/views/artist/ScheduleSharePage.vue`（drawCard 字体）：导出图字体固定 "Microsoft YaHei"（canvas 可用字体限制），与后台纸墨设计系统（文楷标题/思源正文）不一致。建议：可接受（导出图是独立分享品）；若追求一致可尝试加载主题字体。
- ✅ 其余：复制文本含 execCommand 兜底、图片导出两遍测量高度、状态推导（≥10/3-9/<3）、数据并行加载齐全。

### /tools/clients 客户标记
- 🟢-C1 `web/src/views/artist/ClientsPage.vue`（表格无分页）：客户标记全量加载。标记客户通常有限，当前可接受；量大后建议后端分页（已有 qq 搜索兜底）。🟢 观察项。

### /tools/returning 老客召回
- 🟢-R1 `web/src/views/artist/ReturningClients.vue` L71-77：复制话术仅 clipboard API，**无 execCommand 兜底**（对照 ScheduleSharePage L- 有兜底）——非安全上下文/低版本浏览器复制失败无降级。建议：补兜底（可抽公共 clipboard util）。
- 🟢-R2 `web/src/views/artist/ReturningClients.vue`（表格行）：老客列表无「标记客户」快捷入口——召回后常想顺手补标记/备注，需切到 /tools/clients 再搜索 QQ。建议：行内加「标记客户」按钮（跳转或直接打开标记弹窗，两页数据同源）。

### /stats 数据统计
- 🟢-ST1 `web/src/views/artist/StatsPage.vue` L73：时间范围硬编码 14 天（getMyTrackingSummary(14)），无切换。后端 days 参数已支持。建议：加 7/14/30 天切换（低成本）。
- ✅ 其余：总览卡、纯 CSS 柱状图（不引图表库）、三态（off/hidden/on）提示齐全。

### /settings 主页设置
- 🟡-SE1 `web/src/views/artist/Settings.vue` L512-523：save() **按当前 tab 拆分提交**，无脏检测——在「基本资料」改昵称/简介后直接切到「主页展示」点保存，只提交 showcase 字段，**之前未保存的 profile 修改静默丢失**（用户毫无提示）。建议：切 tab 时检测脏状态提示，或统一「保存全部」。这是本批唯一「数据丢失」级体验问题。
- ✅ 其余：BUG-7 加载失败防默认值覆盖、头像即时上传（10MB 限制）、链接粘贴自动识别平台、灵感标签去重（≤20）、须知独立保存 + 预览消毒（XSS 防护）、模板/配色/强调色（5 预设+清除）、封面预览跳作品管理、R50 新窗预览、旧 tab 别名重定向齐全。

### /preferences 偏好设置
- ✅ 已查无问题：字号档位（localStorage 即时生效，normal 清 dataset）、通知/默认面板（DB）、快捷按钮配置（3~9 校验、DB+localStorage 回退、localStorage→DB 迁移静默重试）齐全。未见优化空间。

## 三、✅ 已查无问题页（页级）
- /tools/food：四模式/免责/随机防重复/空态/双主题（见上）
- /preferences：字号/通知/快捷配置（见上）

## 四、优先级建议（低成本高收益 Top 5）

| # | 编号 | 事项 | 改动量 | 理由 |
|---|------|------|--------|------|
| 1 | 🟡-Q1 | QueueBoard 交付后完成区不刷新 | 1-2 行（补 loadCompletedQueue） | 数据一致性，每次交付都踩，用户可感知 |
| 2 | 🟡-SE1 | Settings 切 tab 未保存修改静默丢失 | 脏检测 + 切 tab 提示（或统一保存） | 唯一「数据丢失」级问题，防用户财产损失 |
| 3 | 🟡-A1 | 作品管理触屏操作按钮不可见 | ≤768px 常显/点击展开 | 移动端核心操作（删图/编辑）可达性 |
| 4 | 🟡-E1/I1 | 工具页裸 fetch 收口进 artistApi | 封装 3 个端点 + 2 处引用替换 | 统一 401 登出/超时/i18n，防「登出失效」类隐患 |
| 5 | 🟡-O1 | 订单复合筛选漏单（统计卡跳转 active/completed） | 后端加 active/completed 语义或前端拉全量 | 数据准确性，订单 >50 即触发 |

## 五、验证方法与边界说明

- 全部结论基于 master @ f3724bf 静态代码阅读；未起 dev server 实测（本批只读）。
- 标「🟡 需验证」项（M1 超限上传 EP 行为 / T1 倍率语义 / I3 金额上色意图 / E2 后端缺口）：代码可读出行为，但产品意图/运行表现建议实测或向用户确认。
- 已知设计不误报：纸墨 token 语义色（花青/朱砂/石绿/藤黄/赭石）、克制动效纪律（≤0.2s 按压）、后台/客户端双主题、文楷仅标题/数字/印章、统计数字墨色铁律——以上均视为既有设计，仅在「同一体系内不一致」时记录（如 I3 金额花青 vs 其他页墨色）。
- 共性问题登记（不在单页重复展开）：①订单下拉 100 条上限（W1/P1 + Watermark/Puzzle 两页）；②裸 fetch 无统一拦截器（E1/I1）；③hover-only 操作（A1 + StageListView del-btn 半透明、stage-desc 空态 hover 提示，同族问题可一起处理）。

— 五号 2026-08-08