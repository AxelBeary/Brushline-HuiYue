# 05C-to-01：审计修复批交付报告

> 交付角色：五号（bugfix）
> 日期：2026-08-08
> worktree：D:\Hermes Agent CN Desktop\workspace\artist-commission-w10
> 分支：beta/batch-audit-fixes
> 派工文档：docs/comms/05C-派工-审计修复-20260808.md
> 范围：授权文件 12 个（后端 1 + 前端 11）；未推送、未合并、未改 STATUS

## 一、执行概要

- 开工先 `git merge master --no-edit`：Already up to date（分支已含 master 最新）
- 施工图全部任务完成；门禁全绿
- 竞态 seq 范式 3 场景最小逻辑验证 PASS

## 二、逐任务改动说明

### 任务 1：异步竞态修复（🟡2/🟡3/🟡4 + OrderList 同族，统一 seq 模式）

**1a. ManualOrder.vue（🟡2 qqHistory 竞态）**
- 新增 `let qqSeq = 0`；watch 回调开头 `const mySeq = ++qqSeq`（每次输入/重置递增，旧请求立即失效）
- Promise.all 返回后 / catch / finally 均加 `mySeq !== qqSeq` 守卫再赋值
- ⚠️ 比施工图略强：施工图写「setTimeout 内 ++qqSeq」，本实现改为「watch 开头 ++qqSeq」——否则输入变无效/重置时旧请求仍会渲染覆盖清空态（watch 无效分支不经过 setTimeout）；交付前已确认此增强为必要闭环
- 顺手修复：无效 QQ 分支补 `qqHistoryLoading.value = false`（原代码该分支漏清 loading，输入非法 QQ 时骨架会一直转）

**1b. ClientsPage.vue（🟡3 搜索无防抖+竞态）**
- `@input="loadClients"` → `@input="onSearchInput"`（300ms 防抖，unmount 清 timer，照 OrderList.vue 现有模式）
- `loadClients` 加 `searchSeq` 守卫（成功 / catch / finally）
- import 补 `onUnmounted`

**1c. useOrderForm.js（🟡4 价格预览竞态）**
- `doCalc` 加 `calcSeq`、`doStyleCalc` 加 `styleCalcSeq`；请求返回后校验再赋值 `pricePreview` / `stylePricePreview`；catch 同样守卫
- 赋值方式从 `await` 直赋改为先取 res 再校验后赋值（守卫需要返回值）

**1d. OrderList.vue（同族搜索竞态）**
- `loadOrders` 加 `loadSeq` 守卫（已有 300ms 防抖，只补序号；搜索/翻页快速切换时慢请求不再覆盖新结果）

### 任务 2：Settings.vue index key（🟡1，customLinks）

- 模板 `:key="index"` → `:key="link.__k ?? index"`
- 新增自增计数器 `let linkKey = 1`；`addLink` push 补 `__k: linkKey++`
- `loadProfile` 解析后端 custom_links 后统一补 `__k`（`.map(l => ({ ...l, __k: linkKey++ }))`）
- moveLink/removeLink 走 splice 保持对象引用，key 不漂移；保存循环只取 `l.url`，`__k` 不提交后端
- 备注：施工图该段注释行原文与文件存在细微字符差异（首轮批量替换未命中），改为函数体精确替换完成，结果与施工图一致

### 任务 3：i18n 补键（🟡5）

- `zh-CN.js` + `en.js` settings 域补 `noChanges: '没有修改'` / `'No changes'`（saved 键旁，同族）
- `ArtistDetailDrawer.vue` 无需改代码：该处已写 `t('settings.noChanges') || '没有修改'` 兜底，补键后自动走翻译
- 注：施工图写 `components/admin/ArtistDetailDrawer.vue`，实际文件在 `views/admin/ArtistDetailDrawer.vue`（目录笔误，授权意图一致，按真实路径处理）

### 任务 4：后端 parseInt 严格校验（🟡-1，guestbook.routes.ts）

- `/api/admin/messages` 的 artistId 解析改为严格数字串校验：`/^\d+$/.test(...) ? Number(...) : NaN`
- 效果：`'12abc'`→NaN（原 parseInt 得 12）、`'1.9'`→NaN（原 parseInt 得 1）；非法值忽略，与全站列表惯例一致

### 任务 5：金额格式化统一（🟢4）

- 先读 `formatCents` 实现确认：返回两位小数字符串（无 ¥ 前缀），与原 `(x/100).toFixed(2)` 语义一致 → 可安全统一（零 UI 变化）
- `ManualOrderLeft.vue:95`（原无 ¥）→ `formatCents(clientSummary.totalPaidCents)`
- `ReturningClients.vue:20` / `StandaloneIncome.vue:192` / `ToolsExport.vue:102`（¥ 在外层模板串）→ `¥${formatCents(...)}`
- 附带收益：formatCents 有 `(cents||0)` 空值保护（原写法 null/undefined 会 NaN）

## 三、门禁结果（全绿）

| 项目 | 命令 | 结果 |
|------|------|------|
| 前端 lint | npm run lint | 0 errors ✓ |
| 前端单测 | npm run test:web | 17 files / 262 tests passed ✓ |
| 前端 i18n | npm run check:i18n | OK，无新增硬编码中文 ✓ |
| 前端构建 | npm run build | built in 7.22s ✓ |
| 后端单测 | npm run test | 67 files / 1014 tests passed ✓ |
| 后端类型 | npm run typecheck | 0 errors ✓ |
| 后端 lint | npm run lint（eslint+oxlint） | 0 warnings 0 errors ✓ |

备注：server 目录初始无 node_modules，已 `npm ci` 安装后跑通（未改动 package-lock.json / package.json）。

## 四、竞态修复验证

修复采用库内既有正确样板（PuzzlePage.vue renderSeq）同范式。另做了 3 场景最小逻辑验证（node 模拟，脚本在 worktree 外 workspace/temp/verify-seq.js）：

- 场景1 慢请求 A(100ms) 后发快请求 B(10ms)：渲染结果仅 ["B"]，A 被丢弃 → PASS（慢请求不覆盖快请求）
- 场景2 过期请求失败路径：catch 内守卫同样丢弃，不渲染 → PASS
- 场景3 finally/loading 语义：过期请求不碰 loading，最新请求正常收尾 → PASS（对应 no-unsafe-finally 修正后的 if 包裹写法）

浏览器级快速输入实测未做（需起全链路 dev server + 登录态）；因改动为标准 seq 范式 + 262 前端测试全绿，风险低。如需浏览器回归可后续补（huiyue-browser-regression-testing 流程就绪）。

## 五、范围与合规

- 改动文件 12 个：ManualOrder.vue / ClientsPage.vue / useOrderForm.js / OrderList.vue / Settings.vue / zh-CN.js / en.js / ManualOrderLeft.vue / ReturningClients.vue / StandaloneIncome.vue / ToolsExport.vue / guestbook.routes.ts（全部在授权清单内）
- 未引入新依赖；未推送、未合并、未改 STATUS
- 结束时 git status --short 无未跟踪残留；本 worktree 无 dev server 在跑（仅系统级 node 进程，非本项目）

## 六、遗留建议（非阻塞）

1. 前端单测暂未覆盖「快速输入/切换」竞态用例；可将 verify-seq 场景固化为 vitest 用例，防回归
2. ManualOrder.vue 无效分支 loading 清理为顺手小项，已随批合入（如需独立追溯可拆出）
