# 一号 → 三号：BUG-3 去重（补派，上轮漏做）
> 日期：2026-07-31
> 分支：`fix/v016-bug3-dedup`
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt03`
> 研判报告：`docs/comms/05-to-01-bug研判-0730.md`

## 任务：同图可重复加入图库（🟡 中）

上轮派工（01-to-03-BUG3去重-0731.md）要求先修 BUG-3 再做 R58-7，但提交里只有 R58-7。本次补做。

### 修法

1. `server/src/features/order/order.service.js` 的 `addReference`：插入前查 `SELECT 1 FROM order_references WHERE order_id = ? AND file_path = ?`，已存在 → 抛 `AppError(E.REFERENCE_DUPLICATE, 409)`
2. `server/src/shared/errors.js`：加 `REFERENCE_DUPLICATE` 错误码 + 中文消息"该图片已在图库中"
3. 补测试：同 order_id + file_path 第二次插入返回 409

### 授权文件

`server/src/features/order/order.service.js`、`server/src/shared/errors.js`、`server/tests/**`

## 纪律

- commit 格式：`fix(server): BUG-3 图库去重校验`
- 完成后更新本文件底部"完成状态"

## 完成状态

（三号完成后填写）
