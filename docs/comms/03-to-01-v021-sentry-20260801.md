# 三号 → 一号：v0.21 Sentry 后端接入完成

> 分支：`feat/v021-sentry`（1 commit）

## 变更清单

| 文件 | 改动 |
|------|------|
| `server/src/app.js` | `@sentry/node` 条件初始化（DSN 空/development → 不 init）；错误处理器 500 分支加 `Sentry.captureException`；release 从 package.json version 读取 |
| `server/package.json` | 新增依赖 `@sentry/node ^10.69.0` |
| `server/package-lock.json` | 锁文件更新 |
| `.env.example` | 新增 `SENTRY_DSN_BACKEND` 占位 + 说明注释 |

## 验收标准覆盖

| # | 状态 | 说明 |
|---|------|------|
| S-AC2 | ✅ | 500 错误 → `Sentry.captureException(error)` |
| S-AC3 | ✅ | DSN 空/不设 → 不调 `Sentry.init()`，零网络请求 |
| S-AC5 | ✅ | `NODE_ENV=development` → 跳过 init |
| S-AC6 | ✅ | `sendDefaultPii: false` |
| S7 | ✅ | release 从 `package.json` version 读取 |

## 验证

- 后端测试：469/469 通过（27 文件）
- ESLint：零错误零警告
- 测试环境无 DSN → Sentry 完全不激活，零副作用

## 备注

- `tracesSampleRate: 0`：只做错误捕获，不做性能追踪（免费版额度有限）
- 用户注册 sentry.io 后，在 `.env` 填入 `SENTRY_DSN_BACKEND=https://...` 即启用
- 前端部分由二号在 wt-02 实施（`VITE_SENTRY_DSN`）
