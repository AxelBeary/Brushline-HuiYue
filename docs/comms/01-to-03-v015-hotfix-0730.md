# 一号 → 三号：v0.15 收尾小批次

> 日期：2026-07-30（晚间）
> 分支：`fix/v015-hotfix-batch`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-03`

---

## 三项小任务（合计 35min，一个 commit）

### 1. R56 倍率排序（10min）

`server/src/features/pricing/pricing.service.js` 两处 ORDER BY 改为按数值从高到低：
- 第 194 行附近：usage multipliers 查询
- 第 285 行附近：rush multipliers 查询

`ORDER BY value DESC`（或对应字段名，看实际列名）。

### 2. ENV-1 uploads 目录自动创建（10min）

`server/src/app.js`：在 `@fastify/static` 注册之前，加 `mkdirSync(uploadDir, { recursive: true })`。

需要 `import { mkdirSync } from 'node:fs'`（或 `node:fs/promises`，看现有 import 风格）。

### 3. R52 补 N 单计数（15min）

`server/src/features/order/order.service.js` 的 `getArtistStats`：
- 新增 `todayNewOrderCount`：今日新增订单数（COUNT，与 todayNewOrderCents 同条件）
- 新增 `todayRevenueCount`：今日完成订单数（COUNT，与 todayRevenueCents 同条件）
- 返回对象加这两个字段

前端 Dashboard.vue 由二号改（你不用管前端）。

## 授权文件

`server/src/features/pricing/pricing.service.js`、`server/src/app.js`、`server/src/features/order/order.service.js`

## 测试

跑 `npx vitest run` 通过即可（现有测试不覆盖排序方向，不强制加新测试，但 ENV-1 可以顺手验证）。

## 提交规范

`fix(server): R56倍率排序 + ENV-1目录创建 + R52补N单计数`
