# 派工：五号-B H1 — 看板「已交付」统一走 DeliverDialog 交付弹窗

> 分支：新建 worktree。工作目录：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-05b`（不存在则自建：在主仓 `git worktree add D:\...\artist-commission-wt-05b -b v036-w2-board-deliver master`）。**开工第一步 `git merge master` 再读本文件。**
> 风险等级：低（纯前端交互收敛，无 API 变更）
> 用户已拍板（STATUS 波 2 候选）：看板下拉「已交付」统一走 DeliverDialog 交付弹窗，防手滑。

## 现状（一号已核实）

QueueBoard.vue：
- 134-141 行：下拉菜单 `command="delivered"` 项（done 状态可见），经 `quickAction` 处理
- 1073-1087 行：`quickAction` 对非 cancelled 命令**直接调 `artistApi.updateStatus(order.id, command)`**——done→delivered 跳过交付弹窗，一步点错直接变已交付
- 对比：116-123 行工作流订单的「去交付」按钮已经走 `openDeliverFor(element)`（120 行）→ DeliverDialog（431-435 行）
- DeliverDialog 组件已引入（458 行）、`deliverDialogVisible`（468）/`openDeliverFor`（470）/`onDeliveredFromBoard`（474）全部现成

## 任务

1. `quickAction` 里 `command === 'delivered'` 时改走 `openDeliverFor(order)`（与 cancelled 走滑块的分支同款写法），不再直调 updateStatus
2. 检查是否还有其他直调 delivered 的入口（grep `updateStatus` 全调用点），一并收敛；工作流订单的「去交付」按钮路径不动
3. 全模式回归：工作流订单（有 currentStageId）/ 非工作流订单（done 状态）两条交付路径都走 DeliverDialog

## 授权文件

- web/src/views/artist/QueueBoard.vue
- web/src/locales/zh-CN.js + en.js（如需新文案）
- 测试文件（如需）

**不许动** OrderDetail.vue / ManualOrder.vue / OrderForm.vue（本轮多路在改）。

## 验收

- 浏览器实测：看板下拉点「已交付」→ 弹交付弹窗（可上传文件/无文件交付），取消则订单状态不变
- web 全量测试绿（基线 144）
- 提交格式：`fix(artist): 看板已交付操作统一走交付弹窗防手滑`，说明里写清实测步骤
