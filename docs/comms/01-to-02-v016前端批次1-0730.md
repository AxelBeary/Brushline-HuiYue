# 一号 → 二号：v0.16 前端批次1
> 日期：2026-07-30
> 分支：`feat/v016-frontend-batch1`
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt02`

## 任务A：工艺层CSS升级（低风险，不增功能只升质感）

参考：`docs/comms/04-to-01-H5设计原型完整分析报告-0730.md` 第三节

1. **弹性缓动曲线全局化**：在 `client/src/assets/` 主 CSS 中定义 `--ease-bounce: cubic-bezier(.34, 1.56, .64, 1)`，替换现有交互动画中的默认 ease（只用于 transform/box-shadow，opacity 单独用 ease）
2. **按钮三态模型**：全局按钮 hover=translateY(-3px)+阴影加深，active=translateY(-1px)+scale(.98)
3. **`clamp()` 流式字号**：页面标题/间距从固定 px 改为 clamp()（如 `clamp(34px, 5.5vw, 46px)`）
4. **`minmax(0, 1fr)` 防溢出**：检查现有 grid 布局，纯 `1fr` 列全换 `minmax(0, 1fr)`
5. **`prefers-reduced-motion` 兜底**：全局加一行媒体查询关闭动画

授权文件：`client/src/assets/**`、`client/src/components/**`（仅 CSS 部分）、`client/src/views/**`（仅 CSS 部分）

## 任务B：R54 档位页表格→卡片（C63/C64 已确认）

将画师后台档位管理从 el-table 改为卡片布局。用户已确认方向。

授权文件：`client/src/views/artist/TiersView.vue`（或对应档位页组件）

## 纪律

- 在 worktree 工作，不要碰主 worktree
- 提交前 `git diff --stat` 确认只有授权文件
- commit 格式：`type(scope): 描述`
- 完成后更新本文件底部"完成状态"

## 完成状态

（二号完成后填写）
