# 02-to-01-画师后台UI巡检-交付-20260807.md

> 交付：二号 → 一号
> 派工：`02-派工-画师后台UI巡检-20260807.md`（只读巡检，零代码改动）
> 基线：master `7d202ec`（本分支已 merge master，fast-forward）
> 方法：隔离实例（server 4002 + vite 5175）+ Playwright 逐页截图 + DOM 量化 + 阿里云 VL 看图（26 张）
> 范围：画师后台 12 页 + 管理后台 7 页，桌面 1440×900 + 移动 375×812 双视口

## 一、环境与数据基础（重要前置说明）

- 隔离实例：`server/data/commission.db`（seed 重建，v45 迁移），alice(10001) 兼管理员（测试库 platform_config.admin_qq=10001，仅测试环境配置）
- **seed 数据量**：price_tiers=5 / artist_workflow_stages=14 / greeting_templates=8 / social_platforms=24 / commission_rules=1 / artists=3；**orders / artworks / guestbook_messages / 增项 / 倍率 / 折扣码 = 0**
- ⚠️ **订单详情页（/orders/:id）无法巡检**：seed 无订单数据，派工红线「不建数据」→ 空态之外无内容可看。建议补巡路径：生产容器（有真实订单，需 TOTP）或带订单数据的测试库
- 本批截图基于**空态 + 少量 seed 数据**：多处「大段空白」是空数据下的页面表现，不等于有数据时布局也空；但**空态本身的观感（插画/文案/留白）是真实可改进项**（A 测教训：界面=门面）
- 全部截图存 `D:\Hermes Agent CN Desktop\workspace\temp\ui-audit-artist\`（主仓外，不入库）

## 二、量化检查结果（DOM 实测，全部页面）

| 指标 | 结果 |
|------|------|
| 横向溢出（scrollWidth > innerWidth） | 19 页 × 2 视口全部 **无** |
| 破图（img naturalWidth=0） | 全部 **0** |
| console error / pageerror / requestfailed | 全部 **0**（登录态页面） |
| 主题 | 全部 paper（亮色） |
| 明显空洞（相邻区块 gap>220px） | 自动化扫描 0（VL 仍发现视觉留白，见问题清单——大空白多为 flex 拉伸容器内，非相邻块间距） |

## 三、问题清单（按严重度）

### 🔴 明显错位 / 布局缺陷（5 条，建议优先拍板）

1. **偏好页「保存快捷按钮」+ 提示文字悬浮于勾选列表中部**
   - 页面：/preferences（桌面 1440 + 移动均可见）
   - 截图：`preferences-1440.png`
   - 描述：快捷按钮配置卡内，`form-hint` 与「保存快捷按钮」渲染在 checkbox 列表**中部右侧**，未落到列表下方（VL 两次独立确认：单张复查 + 批量）
   - 定位：`web/src/views/artist/Preferences.vue` 快捷配置区（`<el-checkbox-group class="quick-config">` 之后紧跟 `form-hint` + button，约 L123-131，CSS `.quick-config` 布局导致）
   - 来源：VL 截图确认 + 静态分析

2. **订单列表表头未撑满整行，右侧留浅色块**
   - 页面：/orders（桌面）
   - 截图：`orders-1440.png`
   - 描述：空态表格表头固定列宽合计约 744px（64+100+100+120+100+80+100+80），容器更宽时 el-table 不拉伸 → 右侧留浅色块，视觉错位
   - 定位：`web/src/views/artist/OrderList.vue` el-table 固定 width 列
   - 来源：VL 截图确认 + 静态分析（列宽求和）
   - 备注：有数据行时同问题应同样存在（列宽固定）

3. **默认流程页卡片宽度与上方提示条错位**
   - 页面：/admin/default-workflow（桌面）
   - 截图：`admin-workflow-1440.png`
   - 描述：提示条全宽、卡片右侧大片空白，宽度不一致（VL：「卡片右侧大片空白，与提示条宽度错位」）
   - 定位：`web/src/views/admin/DefaultWorkflowEditor.vue`（卡片容器 max-width 与提示条不一致）
   - 来源：VL 截图确认

4. **统计页左侧选中指示条显示不全**
   - 页面：/stats（桌面）
   - 截图：`stats-1440.png`
   - 描述：左侧导航选中项左缘深色指示条只显示一半，疑似被裁切（VL）
   - 定位：`web/src/views/artist/StatsPage.vue` / ArtistLayout 导航选中态样式
   - 来源：VL 截图确认（静态层面待修复批核实）

5. **移动端埋点看板漏斗表事件名列过窄，英文断词 3-4 行**
   - 页面：/admin/analytics（移动 375）
   - 截图：`admin-analytics-m-375.png`
   - 描述：漏斗表事件名英文长词换行三、四行，可读性差；表格右侧留白偏多
   - 定位：`web/src/views/admin/TrackingAnalytics.vue` 漏斗表列宽（min-width 不足）
   - 来源：VL 截图确认

### 🟡 观感差 / 空态设计问题（按页面列出，均为 VL 截图确认）

6. **登录页（桌面+移动）**：右上主题控件与 EN 按钮拥挤未对齐，绿色圆点无按钮外框、意图不明；页面四周留白大 — `login-1440.png` / `login-375.png`
7. **仪表盘**：收入图仅一条平虚线显空；中、右栏底部大片空白；空态集中页面冷清 — `dashboard-1440.png`；移动端收入图 **X 轴数字拥挤重叠**、问候语末行孤词「手腕」 — `dashboard-m-375.png`
8. **排期看板**：空态下方大段空白、插画过淡、缺操作引导按钮（桌面+移动）；移动端**标题重复** — `queue-1440.png` / `queue-m-375.png`
9. **订单列表**：空态仅一行文字缺插图单调 — `orders-1440.png`
10. **手动录单**：右列下方大片空白；**日期框宽度与旁边输入框不一致** — `orders-new-1440.png`
11. **开稿管理**：内容列偏左、右侧大片空白；卡片说明文字行距过宽（桌面+移动，移动「单/月」拆两行、`启用月度额度`换行生硬） — `slots-1440.png` / `slots-m-375.png`
12. **价格管理**：空态下方大段空白、空态图单调 — `tiers-1440.png`
13. **作品管理**：空态下方大片空白、插画对比低显灰暗；「管理」按钮在空态下孤立冗余 — `artworks-1440.png`
14. **留言管理**：空态下方大片空白、插画浅灰与背景融合对比低；筛选下拉框与 Tab 组间距偏大 — `guestbook-1440.png`
15. **设置**：资料卡片右侧大片空白、整体偏左 — `settings-1440.png`
16. **偏好**：「特大」文字与说明文字粘连；右侧大段空白 — `preferences-1440.png`
17. **管理后台首页**：两处空态灰纸箱插画暗淡难看、区块过高留白大；画师列表头部按钮拉满整行排布松散（桌面+移动） — `admin-1440.png` / `admin-m-375.png`
18. **画师管理**：页面底部大片空白；Alice 行「移除」按钮浅粉底白字对比度低；表头与背景色接近区分弱 — `admin-artists-1440.png`
19. **埋点看板**：底部大片空白、空态仅「暂无数据」单调、右表表头略挤（桌面+移动） — `admin-analytics-1440.png` / `admin-analytics-m-375.png`
20. **每日一句管理**：页面下方大片空白；「添加」按钮呈灰色似禁用，与配色不协调 — `admin-greetings-1440.png`
21. **默认流程**：比例条边界有被裁切的「(`」残影；底部空白偏大 — `admin-workflow-1440.png`
22. **系统自检**：卡片下方大片空白；插图灰暗沉闷；按钮左对齐与说明居中不一致 — `admin-health-1440.png`

### 🟢 建议（可选打磨，非缺陷）

23. 登录页主题控件（ThemePicker 展开面板按钮 随系统/亮/暗/✓ 在 DOM 但默认隐藏）——设计如此，但右上入口拥挤感可优化（并入 #6）

## 四、已排除项（VL 误判 / 截图 artifact，DOM 实测修正）

- **管理后台社交平台图标「未渲染」＝ VL 误读**：`admin-platforms` 图标列实测 **20 个 SVG 正常渲染**（path 完整）+ 4 平台（LOFTER/抖音/QQ空间/米画师）按设计用单字兜底（`simpleIcons.js` 注释明确）。VL 把第二列 `pm-code`（显示 iconKey 文本）误当图标列
- **手动录单移动端「悬浮栏遮挡」＝ fullPage 截图 artifact**：`.mo-mobile-bar` 是 fixed 底部吸底栏（y=745/812），真实滚动到底后与最后表单元素 **无重叠**（DOM 实测 overlap=false，页面有 padding 补偿）
- 登录态全部页面无横向溢出/破图/JS 错误（量化全绿，见第二节）

## 五、交互实测（Playwright 真实点击）

| 交互 | 结果 |
|------|------|
| 登录（TOTP 动态码真实表单） | ✅ 成功（隔离库 alice） |
| 价格管理 5 tabs（画风与价格/倍率/流程与比例/折扣码/增项库）逐个点击 | ✅ 全部正常切换，无报错（截图 `tiers-tab0~4-1440.png`） |
| 作品管理「管理」模式切换 | ✅ 正常 |
| 管理后台 7 tab 导航（画师管理/问候语/默认流程/社交平台/系统自检/埋点看板） | ✅ 全部可达 |
| 登录页 ThemePicker 面板（随系统/亮/暗） | 面板默认收起（设计如此），展开按钮不可见属正常 |

## 六、未覆盖与后续建议

1. **订单详情页**（/orders/:id）：seed 无订单 + 红线不建数据 → 未覆盖。建议生产容器（真实订单 + 管理员 TOTP）或带订单测试库补巡
2. 有数据场景（订单行/作品网格/留言列表）的布局巡检需带数据环境，本批仅空态
3. 暗色主题（ink）未全量巡检：本批登录态默认 paper；主题切换在登录页实测可用（设计面板），建议下批补暗色全页面
4. 修复优先级建议：🔴 5 条（偏好悬浮按钮 / 订单表头 / 默认流程宽度 / 统计指示条 / 移动漏斗列宽）→ 🟡 空态插画与留白（批量统一处理更高效，可参考客户端空态批先例）

## 门禁自检

- [x] 零代码改动（本分支仅新增本交付报告）
- [x] 截图数量 26 页次（19 页 × 桌面 + 20 页 × 移动 = 39 张主截图）+ 交互截图，≥ 页面数
- [x] 报告落盘（本文件）
- [x] 每条结论注明来源（截图确认 / DOM 量化 / VL / 静态分析）
