# 一号 → 三号：v0.38 并行批 · addons 表与 addPayment 写路径评估（只读研究，不改代码）

> 日期：2026-08-05 · 分支：`assess-addons` · worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-assess`（基于 master `a22f15e`）
> 风险等级：低（纯只读调研 + 报告，零代码改动）
> 性质：STATUS「已知遗留」第 3 项「addons 表是否 drop + addPayment 旧写路径——待评估批」的落地。

## 背景

v0.37 REQ-025 动态计价上线后，价格结构改为条目账本（order_items）+ 锁价分摊引擎。旧体系的两处残留是否已死，需要一次彻底评估，决定下批是否清理。

## 评估问题（逐条给出结论 + 证据：文件:行号）

### Q1：addons 表能否 drop？
1. 谁还在**写** addons 表？（createOrder 档位模式的 `addons` 参数是否仍活跃——注意 REQ-029 画风模式与档位模式并存，档位模式可能仍在用）
2. 谁还在**读** addons 表？（订单详情展示、价格计算 calculatePrice、demo-data、测试）
3. 存量数据：生产/测试库中 addons 关联的订单有多少？（用只读 SQL 查 `data/commission.db` 统计，不改数据）
4. 结论三选一：**可以 drop**（所有链路已迁条目账本）/ **保留但冻结**（仅存量订单展示需要）/ **仍是活表**（档位模式还在用，不能动）。

### Q2：addPayment 是否存在绕过引擎的旧写路径？
1. `order.service.ts` addPayment（约 L1151）之外，还有没有其他写 order_payments 的路径（grep 全部 INSERT INTO order_payments）？
2. 每条写路径是否都走了引擎联动（installmentId 关联 / locked 判定 / 冲抵逻辑）？
3. 结论：列出所有写路径清单 + 各自是否合规。

## 纪律

- **只读**：不改任何代码、不写迁移、不动数据库（SELECT 统计可以）。
- 不碰 docs/requirements/（需求稿是四号的）。
- 报告落 `docs/comms/03-to-01-addons评估报告-20260805.md`，含：Q1/Q2 逐条结论、证据（文件:行号）、存量数据统计、清理建议（若建议 drop，给出迁移步骤草案供一号评审，但本批不执行）。
- commit 单行 `docs(comms): ...`，不推送不合并。最后返回摘要：Q1 结论、Q2 结论、报告路径。
