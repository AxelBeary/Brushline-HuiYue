# 三号 → 一号：v0.19 第 1 波后端（F3 公告 + F1 点赞）

> 日期：2026-08-01
> 分支：feat/v019-backend-wave1

## T1/T2 确认

- **T1（点赞模型）**：同意方案 A（计数字段）。匿名 + localStorage 去重，不需要独立表。
- **T2（留言限流）**：同意同 IP 每分钟 2 条。第 2 波实施时按此执行。

## 已完成

### 迁移 v21（announcement_and_like_count）

- `artists` 表：`announcement TEXT DEFAULT NULL` + `announcement_expires_at DATETIME DEFAULT NULL`
- `artworks` 表：`like_count INTEGER DEFAULT 0`
- 含自动备份（v11/v12 模式）
- schema 字符串已同步更新

### F3 小公告后端

- `artist.service.js`：`updateArtist` 白名单新增 `announcement` / `announcement_expires_at`；新增 `getAnnouncement(artist)` 函数（过期返回 null）
- `artist.routes.js`：`PUT /api/artist/profile` JSON Schema 新增 `announcement`（maxLength 500）+ `announcementExpiresAt`（maxLength 30）；keyMap 映射；公开 `GET /api/artists/:subdomain` 返回 `announcement` 字段

### F1 点赞后端

- `artist.service.js`：新增 `likeArtwork(id)` / `unlikeArtwork(id)`（上限 99999，下限 0）
- `artist.routes.js`：新增 `POST /api/public/artworks/:id/like` + `DELETE /api/public/artworks/:id/like`（公开匿名，返回 `{ likeCount }`）
- 公开 API `GET /api/artists/:subdomain` 的 artworks 数组已包含 `like_count`（SELECT * 自动带出）

## 改动文件

| 文件 | 变更 |
|------|------|
| server/src/db/init.js | schema 字符串 + 迁移 v21 |
| server/src/features/artist/artist.service.js | 白名单 + getAnnouncement + likeArtwork/unlikeArtwork |
| server/src/features/artist/artist.routes.js | Schema/keyMap/公开返回 + 点赞路由 |
| server/tests/announcement-like.test.js | 10 个测试（公告 5 + 点赞 5） |

## 验证

- `npx vitest run`：422/422 通过（22 文件）
- `npx eslint .`：0 错误，3 个预存 warning（admin 测试，非本次）

## 接口变更（新增，无破坏性）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/public/artworks/:id/like | 点赞 +1，返回 `{ likeCount }` |
| DELETE | /api/public/artworks/:id/like | 取消 -1，返回 `{ likeCount }` |

`PUT /api/artist/profile` 新增可选字段：`announcement`（string|null）、`announcementExpiresAt`（string|null）。
`GET /api/artists/:subdomain` 响应新增：`announcement`（object|null）。

## 二号前端对接说明

- 公告：画师设置页调 `PUT /api/artist/profile` 传 `announcement` + `announcementExpiresAt`；客户主页读 `GET /api/artists/:subdomain` 的 `announcement` 字段（null = 无公告或已过期）
- 点赞：客户主页作品卡片调 `POST/DELETE /api/public/artworks/:id/like`，用 localStorage 记已赞状态；`artworks` 数组每项有 `like_count` 字段
