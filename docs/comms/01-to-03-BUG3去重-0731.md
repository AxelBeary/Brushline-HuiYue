# 一号 → 三号：BUG-3 后端去重（插入 R58-7 之前）
> 日期：2026-07-31
> 分支：`feat/v016-r58-template-field`（同一个分支，先修 Bug 再做 R58-7）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt03`
> 研判报告：`docs/comms/05-to-01-bug研判-0730.md`

## BUG-3（🟡 中）：同图可重复加入图库

`server/src/features/order/order.service.js` 的 `addReference` 无 (order_id, file_path) 去重。

### 修法

1. `addReference` 插入前查 `SELECT 1 FROM order_references WHERE order_id = ? AND file_path = ?`
2. 已存在 → 抛 `AppError(E.REFERENCE_DUPLICATE, 409)`（幂等设计也可静默返回，但显式报错让前端能给提示）
3. `server/src/shared/errors.js` 加 `REFERENCE_DUPLICATE` 错误码
4. 补测试：同 order_id + file_path 第二次插入返回 409

### 授权文件

`server/src/features/order/order.service.js`、`server/src/shared/errors.js`、`server/tests/**`

## 纪律

- 先修 Bug 再做 R58-7（01-to-03-R58-7模板字段-0731.md）
- commit 格式：`fix(server): BUG-3 描述`

## 完成状态

（三号完成后填写）
