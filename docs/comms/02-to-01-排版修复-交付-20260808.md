# 02-to-01 排版修复-交付-20260808

> 交付：二号（client-frontend）→ 一号
> 分支：beta/layout-fixes（worktree artist-commission-w2）
> 基线：master ecff9b1（开工已 git merge master）
> 状态：已 commit，未推送未合并

## 逐项交付

### 1. 主页卡片网格 auto-fill → auto-fit（P1）✅ 已修

- 文件：web/src/views/client/LandingPage.vue:114
- 改动：`grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))` → `repeat(auto-fit, minmax(280px, 1fr))`
- 效果：2 卡片时不再留空轨道偏左，卡片拉伸填满整行（截图对比：before 卡片 x=284 / after 卡片 x=367，网格 diff 5% 像素变化）
- 3 卡/4 卡布局：auto-fit 在卡片数 ≥ 列数时行为与 auto-fill 一致，不破坏（已验证 1280px 视口 2 卡场景）

### 2. 状态徽章对比度（el-tag dark：可约/已排满/休息中）（P2）✅ 已修

- 文件：web/src/styles/theme.css（新增 el-tag 覆盖段）
- 现状（实测 WCAG）：success #67C23A 白字 2.24:1；warning #e6a23c 白字 2.19:1；danger #f56c6c 白字 2.90:1（均 < 4.5:1）
- 修复：dark 模式改深字（只调对比度不改色相，可约仍绿/已排满仍橙/休息中仍红）：
  - success：白字 → #0E3D20 on #67C23A = 5.48:1（派工建议 #1F5E3A 实测只有 3.44:1 不达标，改用更深 #0E3D20）
  - warning：白字 → #4d320b on #e6a23c = 5.40:1
  - danger：白字 → #5c1616 on #f56c6c = 4.58:1
  - 深色主题 bg 提亮后（#95d475/#eebe77/#f89898），文字更深：6.97/6.36/5.56:1
- 覆盖范围：LandingPage.vue:38 + NotFound.vue:29 + TrackOrder.vue:56 的 el-tag dark 全部生效（经 theme.css 全局覆盖，未改 NotFound/TrackOrder 文件本身）
- ⚠️ 边界说明：el-tag light 模式未动——admin 全部 el-tag 均为 light（三号审计批地盘），且 TrackOrder 不在本批授权文件；light 模式浅色主题 success 2.08:1 仍偏低，建议后续批单独处理（需先与三号协调）

### 3. footer 文字对比度（P2）✅ 已修

- 文件：web/src/styles/theme.css（新增 --text-footer 变量）+ web/src/views/client/LandingPage.vue:153（引用）
- 现状：#c0c4cc on #f5f5f5 = 1.60:1（13px 不达标）
- 修复：新增语义变量 --text-footer：浅色 #606266（5.61:1）/ 深色 #8a8f98（5.67:1），双主题同步
- 说明：未改全局 --text-muted（它被 strip-dot/时间戳等多处使用，直接改会波及全站），单独新增 footer 专用变量，影响面最小

### 4. 流程百分比徽章字号 + 对比度（P2）✅ 已修

- 文件：web/src/components/shared/WorkflowOverviewStrip.vue（流程百分比徽章唯一实现处，被 4 模板 + OrderForm 共用）
- 现状：.strip-pay-chip font-size 12px；color: var(--color-primary) on 12% 浅底；对比度不足（accent4 浅色 4.10:1 / 深色 accent4 3.89:1 / accent5 深色 2.88:1）
- 修复：
  - 字号 12px → 13px
  - 浅色主题文字加深：color: color-mix(in srgb, var(--color-primary) 70%, #000)（全 5 accent 实测 6.94-8.91:1，色相不变仅加深）
  - 深色主题文字提亮：html.dark 下 color: color-mix(in srgb, var(--color-primary) 70%, #fff)（全 5 accent 实测 4.61-7.82:1）
- ⚠️ 文件位置说明：派工施工图写「classic 模板流程百分比」，但实际组件在 components/shared/WorkflowOverviewStrip.vue（classic 模板引用它），故改动落在共享组件，属施工图目标必要文件

### 5. classic hero 对齐（P3）❌ 未做

- 按派工施工图第 5 项「P3 可选，先不做」处理，结构性改动留后续批

## 顺带修复（说明）

- web/src/components/templates/TplStatusBadge.vue：状态徽章文字原 var(--pal-text-dim) 浅色 3.61:1 不达标（13px），改为 color-mix(in srgb, var(--pal-text) 65%, var(--pal-text-dim)) = 浅色 8.78:1 / 深色 10.27:1，色相不变仅加深
- 同属施工图第 2 项「全 grep templates/*.vue 状态徽章统一修」目标，文件在 components/templates/（模板状态徽章唯一实现处）

## 验证门禁（全部通过）

| 门禁 | 结果 |
|------|------|
| npm run test:web | 215/215 passed |
| npm run lint | 0 错误 |
| npm run build | 成功（11.35s） |
| npm run check:i18n | OK 无新增硬编码中文 |
| git diff --stat | 仅 4 个文件（均为授权/施工图目标文件） |

## before/after 截图

路径：workspace/temp/layout-fixes/（10 张，before 用 git stash 生成真实旧码截图）

- landing-grid-before/after.png：主页卡片网格（auto-fill 空轨道 vs auto-fit 拉伸）
- status-badge-before/after.png：可约徽章（白字 vs 深绿字）
- workflow-chip-before/after.png：流程百分比徽章（12px vs 13px + 文字加深）
- landing-footer-before/after.png：footer 文字（#c0c4cc vs #606266）
- tpl-status-before/after.png：模板状态徽章文字（pal-text-dim vs 加深）

像素 diff 证据：landing-grid 5% 变化 / status-badge 文字区 2% / workflow-chip 2% / footer 文字 bbox (578,39)-(701,51) / tpl-status 文字 bbox (66,54)-(107,66)，before/after 均真实存在视觉差异。

## 风险与建议

1. el-tag light 模式（TrackOrder 订单状态 tag、admin tag）对比度仍偏低——建议后续批与三号协调处理（本批避免碰 admin 地盘）
2. 所有改动均为颜色/字号/布局值，无逻辑变化；el-tag 覆盖用双类特异性（.el-tag.el-tag--dark.el-tag--success 0,3,0 > EP 0,2,0），不受加载顺序影响