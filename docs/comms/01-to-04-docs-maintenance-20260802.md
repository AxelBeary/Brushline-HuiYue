# 一号→四号：文档维护审计（2026-08-02）

## 任务

项目文档好久没维护了，全面检查一遍，过时的更新，缺失的补上。

### 检查清单

1. **README.md**（项目根目录）：
   - 技术栈描述是否准确（TS 迁移进度、测试数量、迁移版本号）
   - 功能列表是否覆盖 v0.20-v0.24 新增（额度池、档位三态、展示柜、工作流、看板标签等）
   - 开发/部署命令是否正确
   - 截图/链接是否失效

2. **docs/CONTEXT.md**（项目上下文）：
   - 版本号、测试数量、迁移版本是否最新
   - 功能清单是否覆盖近期变更
   - 技术债/遗留项是否更新（P0/P1 已修、安全债状态）

3. **docs/specs/**：
   - 已实现的 spec 标注"已实现"（SPEC-001~004）
   - SPEC-005 日历视图状态确认（待开发）
   - 过时的 plan-* 文件是否该移 archive/

4. **docs/requirements/**：
   - REQ-013 原始记录 + 整理文档保留
   - 已完成的 REQ 标注状态

5. **docs/待修复问题清单.md**：
   - P0 三项已修（标注 ✅）
   - P1 四项修复中（标注 🔵）
   - 其余项状态核实

6. **docs/soul/**：
   - 各角色 soul 文件是否有过时描述（如"二号等通知"之类的临时状态）

### 产出

直接改文件，改完提交到 master（docs 改动低风险，不需要走分支）。
commit message 格式：`docs: 文档维护——{具体改了什么}`

### 授权

- `README.md`
- `docs/`（全部）
- 不改代码

### 注意

- 不确定的数据（如"当前线上版本"）不要猜，标注"待确认"
- 不要删历史文档，只更新状态标注
- 改完写 comms `04-to-01-docs-maintenance-{日期}.md` 简述改了什么
