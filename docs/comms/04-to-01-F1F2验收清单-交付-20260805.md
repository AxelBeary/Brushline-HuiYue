# 04→01：REQ-022 F1/F2 验收清单交付

> 日期：2026-08-05 · 分支：`f1f2-acceptance-docs` · 不推送不合并

## 交付物

`docs/REQ022-F1F2验收清单.md`（F1 10 条 + F2 12 条 + 防投毒向量速查表 17 条 + 结论模板），格式对齐 REQ-026 验收清单：操作步骤 → 预期结果 → 实测记录位留空。F5 未做清单（选 c 不做，已上线）。

## 关键结论

1. **拍板全部钉死，零待确认项**：拆解 §七 8 个待确认中 F1 三项（delivered 门槛 / 一图一作品 / 仅展品带预选）+ F2 工时 + 端口处理 + 旧回退删除均按 STATUS §已拍板规则与三号派工定案写入，不再列待确认。
2. **代码现状实锤（读 master，非凭拆解）**：
   - F1：OrderDetail.vue 交付物卡在 L491（发布按钮为本批新增）；ArtworkManage.vue L408~452 已有档位标注多选弹窗（PUT 替换语义，F1-验收 6 直接验前端行为与后端一致）；TplGallery.vue 已有档位筛选 + orderByTag→`?sizeId=`（v0.35 F6，F1-验收 8 复用该机制）；DeliveryPage.vue L30 下载按钮在。
   - F2：Settings.vue 双输入区现状确认（外链 6 条 L92 / 平台链接 10 条 L110）；admin 目录现无平台管理页（F2-验收 10 验新增页面）。
3. **R-1b 逐模板点名落实**：F1-验收 7（按档位分组展示）与 F2-验收 9（页脚合一）均把 Classic/Gallery/Folio/Atelier 做成逐模板表格，不许一条带过。
4. **done ≠ delivered 写进 F1-验收 2**：done 是半终态，无发布入口——这是拍板规则 1 的边界，专门列了一条防漏。
5. **防投毒速查表**：17 条向量逐条标了对应验收条目，★ 标前端行为重点（后端 877/877 已过的前端批不必全复测，抽查为主）。

## 备注

- 清单写的是行为契约，入口按钮文案标注「以交付时界面为准」（派工允许）。
- F2-验收 10 平台 CRUD 依赖 F2 后端合入——清单前提节已注明：验收时若 f2-social-backend 未合入，先向一号确认门控状态。
