# 一号 → 二号：前端测试扩充 + P2 审计预排查

> 日期：2026-08-01
> 优先级：中
> 分支：feat/frontend-test-expand

## 任务 A：前端测试扩充

当前前端测试只有 1 个文件 17 用例（`web/src/components/artist/dashboard/__tests__/dashboard-normalize.test.js`），覆盖极低。

扩充方向（按价值排序）：
1. **composables 纯逻辑测试**：`useOrderForm.js` 的价格计算、增项初始化、草稿保存/恢复——刚修完计价崩溃，这块最需要测试
2. **工具函数测试**：`web/src/utils/` 下的 datetime、sanitize、order 等纯函数
3. **组件关键交互**：OrderForm 的档位选择→价格联动、ManualOrder 的表单校验

测试基建已有（vitest + happy-dom + @vue/test-utils），照现有 `dashboard-normalize.test.js` 的模式写。

目标：新增 30+ 用例，覆盖上述 3 个方向。不追求覆盖率数字，追求关键路径有测试。

## 任务 B：P2 审计前端预排查

第三方审计剩余 P2 共 6 项（具体内容见 `docs/archive/待修复问题清单.md` 和 STATUS.md）。

你要做的：**只排查不修**。逐条对照当前 master 代码，输出：
- 哪些仍存在、哪些已过时
- 仍存在的前端相关项：涉及文件、改动量估算、风险等级
- 建议修复顺序

输出写入交付 comms，一号研判后排批次。

## 授权文件

任务 A：
- web/src/**/__tests__/*.test.js（新建）
- web/src/composables/useOrderForm.js（只读参考，不改）
- web/vitest.config.js（如需调整测试配置）

任务 B：
- 只读，不改任何文件

## 验收

1. `cd web && npx vitest run` 全绿（含新增用例）
2. `cd web && npx eslint . && npm run build` 通过
3. P2 预排查报告在交付 comms 中

## 交付

comms：`docs/comms/02-to-01-前端测试扩充+P2预排查-0801.md`
