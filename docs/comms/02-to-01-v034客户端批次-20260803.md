# 交付：v0.34 客户端反馈批次（二号 → 一号）

> 分支：`fix/v034-client-ui-batch`（worktree wt-02），基线 5f78dff，6 个提交
> 状态：7 项任务全部完成，自动化验证全绿，手动链路验证通过，申请审核

## 提交清单

| 提交 | 任务 | 内容 |
|------|------|------|
| `bb057a6` | B/C/G | TplStyleGrid：尺寸行可点选 + 封面 contain 不裁切 + 空间收紧 |
| `beb8cf4` | B | useOrderForm/OrderForm：URL query 预选（query > 草稿）+ 7 个新测试 |
| `0034915` | A | NotFound.vue 新建 + router catch-all + 删 LandingPage isNotFound |
| `67ae0cd` | D | TplHero ghost 按钮半透明底 + text-dim 边框 |
| `c6f766e` | E | WorkflowOverviewStrip 重做（编号节点 + 连接线 + 收款徽章） |
| `88a1c64` | F | locales 132 处 + 客户端/共享组件 emoji 清理 |

## 逐项说明

**任务 B（尺寸可选 + 预选跳转）**：
- TplStyleGrid 尺寸行改 button，点击 toggle 选中（复用 `--active` 视觉语言：主色边框 + 主色浅底 + 阴影 + ✓），再点取消；切换画风（菜单/滑动）自动清空尺寸选择
- 选中后按钮下方出现引导提示（新增 i18n 键 `artistHome.styleSizeHint`，中英双语）
- goOrder 带 `?styleId=&sizeId=`；useOrderForm 新增第三参 `initialQuery`，加载画风后 `applyQueryPreselect()`：无效/已停用 ID 静默忽略、sizeId 必须属于当前画风；`queryPreselect` 命中记录导出
- restoreDraft 按 queryPreselect 跳过：query 已选的画风/尺寸不被草稿覆盖（增项以 query 为准，倍率仍从草稿恢复）
- OrderForm 加载完成后按预选跳初始步骤（双选 → 增项步骤；仅画风 → 尺寸步骤）
- 单画风退化路径同样支持 sizeId 预选
- **api/index.js 未动**（复用现有 getPublicStyles，无新接口，隐含授权未用到）

**任务 C（封面不裁切）**：多画风 + 单画风两处 `fit="contain"` + `height:auto`（对齐 TplTierGrid），加点击放大（preview-src-list）。

**任务 A（404 重做）**：NotFound.vue——描边镂空大号 404（`-webkit-text-stroke: 2px var(--color-primary)`）+ 文案 + 「回平台首页」胶囊按钮 + 画师卡片入口（`artistPublicApi.getAll()` 取前 6，失败静默隐藏）；主题/语言复用 ClientFloatingActions 右下角浮窗；新增 i18n 命名空间 `notFound.*`（中英）。router catch-all 改指向；LandingPage 删除 isNotFound 逻辑 + 提示条样式。

**任务 D（ghost 按钮）**：半透明底 `color-mix(var(--pal-surface) 70%, transparent)` + `border-color: var(--pal-text-dim)` + backdrop-blur，hover 加深。primary 未动。

**任务 E（流程模块）**：重做为编号圆点（26px）+ 连接线 + 收款徽章（主色底百分比胶囊）；收款节点主色填充、尾款金色填充 + 金色「尾款」文字（复用 `workflow.final` 键，替代原 🔒 emoji）。横向（OrderForm/后台全览）+ 竖向（4 模板）双形态。后台 WorkflowPaymentEditor 用横向模式，视觉同步升级、语义不变。

**任务 F（emoji 清理）**：
- locales：zh-CN 64 处 + en 68 处（脚本批量 + 手工复核，只删 emoji 不改措辞；保留 ✓✕✔★☆◐↩→←— 等功能性文本符号）
- 客户端/共享组件：ThemeToggle ☀️/🌙 改纯 CSS 图形（太阳=圆+8 光线 box-shadow，月亮=inset 遮罩）；OrderTimeline 💰 删除（收款节点已有百分比徽章区分）；TplAnnouncement 📢 删除（模板 :deep 孤儿样式一并清）；useArtistData 🔗→「链」；useOrderForm CATEGORY_META 5 个 emoji 删除（模板同步去 icon 引用）；4 模板 ✏️ revision-note 图标删除；ArtistHome 🔍/🙈、DeliveryPage/TrackOrder 📄、OrderForm 🎟×2/🎨 删除
- **未碰画师/管理后台 .vue**（归三号）；测试文件 emoji 保留（测试样本）

**任务 G（空间）**：classic 左栏卡片 padding 28→22/gap 14→12；TplStyleGrid 菜单项 padding 收紧、showcase gap 20→16、信息区 padding 收紧。轻度，未动布局结构。

## 改动文件（23 个，全在授权范围内）

组件：TplStyleGrid / TplHero / WorkflowOverviewStrip / OrderTimeline / TplAnnouncement / ThemeToggle
视图：OrderForm / LandingPage / NotFound（新建）/ ArtistHome / DeliveryPage / TrackOrder / 4 个 ArtistHome 模板
逻辑：useOrderForm / useArtistData / router / templates.css
i18n：zh-CN / en（新增键：`notFound.*` ×3、`artistHome.styleSizeHint`；删键：`landing.notFoundHint`——已无引用，全项目搜过）

## 验证结果

1. **vitest**：125/125 全绿（6 文件；原 118 + 新增 7 个 query 预选用例；修 1 个既有断言 `group.icon` → `group.label`）
2. **eslint**：`npx eslint .` 零错误零警告
3. **build**：`node node_modules/vite/bin/vite.js build` 成功
4. **手动验证**（vite dev 5174 + 容器 3000 的 v0.33 数据）：
   - 任务 B 链路：alice 主页选「半身像」→ ✓ 高亮 + 引导提示 → 跳转 `?styleId=2&sizeId=2` → OrderForm 直落「选增项」步骤、摘要卡「默认/半身像/¥120.00」价格已算 ✅
   - 任务 A：随机路径 404 页正常（大数字 + 文案 + 按钮 + 3 画师卡片 + 右下角浮窗）✅
   - 任务 D：亮色主题下 ghost 按钮轮廓清晰（视觉确认）✅
   - 任务 E：alice 主页 7 节点流程竖向时间线渲染正常（排期确认 30% 主色、交付 70% 尾款金色）✅

## 备注

- 容器当前 alice 只有 1 个画风「默认」（API 实测只返回 1 条）——与 STATUS 记载的 2 画风不符，疑似用户白天体验时操作过画风配置（三号任务 2 的封面上传相关）。多画风路径（左菜单 + query styleId 预选切换）已有单测覆盖，未影响交付；请一号知悉。
- 遗留观察（非本批次）：浅色主题 hero 标题「Alice」压在画作暗部对比度偏低——任务 D 只处理按钮，标题文字阴影可排后续。

完成后请审核合并。
