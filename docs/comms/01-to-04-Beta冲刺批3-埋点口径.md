# 派工：四号 · Beta 冲刺批 3 —— 三处埋点口径定义（需求文档）

> 分支：新建 `docs/beta-tracking` · worktree：`../artist-commission-w9`（一号已建）
> 开工第一步：`git merge master` 再读本文件。
> 任务类型：**需求文档**（四号本职），零代码改动，产出埋点实施口径文档。

---

## 任务摘要

用户 2026-08-06 拍板 3 处埋点全做（OD-01 落档 STATUS）：
1. **换色率 `theme_accent_change`**（带 `palette_version`）——二号批 3 已含换色埋点实现（utils/track.js），**本批只需补：远程端点接入规格**（后续接后端时用什么路径/格式/鉴权）
2. **下单流程漏斗**（各步骤到达/流失率、停留时长、返回上一步频次）——定义已在第三方 `01-PM §9.2`，本批**提取成正式 REQ/实施文档**
3. **后台功能使用率**（画师后台各功能点击/使用统计，**用户主动要求**）——口径尚未定义，本批**定义最小口径**

## 授权文件（只动这些）

- 新建 `docs/requirements/REQ-033-业务埋点方案.md`（含三处埋点完整定义）
- `docs/comms/STATUS.md`（**只读，不写**）

**不要动**：`web/`、`server/`、既有 REQ 文档（除非确需引用）。

---

## 任务 1：换色埋点远程端点接入规格

**现状**：二号批 3 已建 `web/src/utils/track.js`（console.debug + TODO 远程端点注释），换色调用 `trackEvent('theme_accent_change', { accent })` 已接。

**本批产出**（写进 REQ-033 §1）：
- 远程端点建议：`POST /api/events`（或 `/api/tracking`），body `{ name, ts, version, accent, ua?, ip? }`
- 鉴权：无需登录（访客行为）？还是需要？——**给出建议并标注待拍板**
- 批量上报：n 条合并 / 每事件即报？——建议批量（防刷），给出窗口建议
- 隐私边界：不收集 QQ 号/订单号等 PII，只收集行为事件
- 与 Sentry 的分工：Sentry = 错误监控，业务埋点 = 独立端点（已在 track.js 注释写明）

## 任务 2：下单流程漏斗正式定义

**来源**：第三方 `01-PM §9.2` 有埋点定义（各步骤到达/流失率、停留时长、返回上一步频次），`workspace/temp/third-party-report-20260806/01-PM-交互痛点与优先级.md` L1096 起。

**本批产出**（写进 REQ-033 §2）：
1. 先读第三方 §9.2 全文，提取/整理为正式定义。
2. 明确**步骤锚点**：下单页 `OrderForm.vue` 有哪些步骤（画风模式/旧档位模式步进指示器），每个步骤的「进入/离开/返回」事件名。
3. 事件命名规范：与换色埋点统一前缀（如 `order_step_enter`/`order_step_leave`/`order_step_back`）。
4. 数据表/指标：到达率、流失率、停留时长、返回频次怎么算。
5. **标注**：实施派工时哪些埋点在 OrderForm 的哪个位置加（文件/行号参考——可标注但最终施工图由一号出）。

## 任务 3：后台功能使用率最小口径定义

**背景**：用户主动要求（原话「后代功能使用率 要。」），四号落档时提醒"可先定最小口径（粗粒度功能模块级），避免过度埋点"。

**本批产出**（写进 REQ-033 §3）：
1. **范围**：画师后台（ArtistLayout 内）各功能模块——列出主要页面/操作（排期板、订单详情、作品管理、档位管理、画风管理、设置、留言板、回收站等，先读 `web/src/views/artist/*` 列全）。
2. **粒度**：模块级（进页面一次）+ 关键操作级（新建订单、改状态、设封面、发布作品等）——**最小口径**，不逐按钮埋。
3. **事件命名**：`artist_page_enter`/`artist_action`（带 action 名）。
4. **归属**：画师已登录（有 artistId），事件可带 artistId（非 PII 问题——画师是平台用户）。
5. 实施派工建议：改 `ArtistLayout.vue`（路由变化埋 page_enter）+ 关键操作点。

## 交付要求

1. 交付报告：`docs/comms/04-to-01-交付-Beta冲刺批3-埋点口径.md`，附 REQ-033 全文要点 + 待拍板项清单（如端点鉴权）。
2. commit 到 docs 分支，信息带「docs:」前缀，如 `docs: REQ-033业务埋点方案(换色端点/下单漏斗/后台使用率口径)`。
3. 产出物是**需求文档**，三处埋点的实施施工图由一号后续派工（本批只定义口径）。
