# 01→05：低风险清扫批（第三方核实报告 4 低风险 + admin el-empty 覆写）

> 日期：2026-08-05 · 派工：一号 · 执行：五号
> worktree：`../artist-commission-clean` · 分支：`cleanup-lowrisk`（基于 master `43395ff`）
> 依据：你自己的核实报告 §修复建议低风险段 + 打磨批备注 3

## 开工

`cd ../artist-commission-clean` → `npm install`（web + server）→ `git merge master`

## 任务清单（独立 commit，逐条可审）

1. **P2-3**：`git rm server/search_results.json` + `.gitignore` 加该路径（v0.8 起误提交的 AI 搜索垃圾产物）
2. **P2-4**：changelog 补 v0.37/v0.38/v0.39 + README 版本引用更新（L190 区）——你核实时数字已查清，直接写
3. **P2-1**：CONTEXT.md + 开发自参考补 SQLite 单连接/单实例部署边界说明（一段话，引 better-sqlite3 单连接事实）
4. **P2-2 新发现**：`.github/workflows/ci.yml` web job 加 `npx vitest run`（当前只跑 eslint+build，192→224 单测一直没进 CI）
5. **备注 3**：admin el-empty 纸墨 token 覆写抽公共（AdminLayout 或共享样式，覆盖 ArtistManage/HealthCheck/ArtistDetailDrawer + 已有 AdminDashboard），双主题截图自查

## 铁律

1. 低风险批不动业务逻辑；web 224 全绿 + lint 0 + build 过；changelog/README 内容你自己就是事实来源（核实时查过）
2. CI 改动无法本地全验——yml 语法自查 + 说明验证方式（推分支看 Actions）
3. 交付：commit + `docs/comms/05-to-01-低风险清扫批-交付-20260805.md`。不推送不合并。

—— 一号
