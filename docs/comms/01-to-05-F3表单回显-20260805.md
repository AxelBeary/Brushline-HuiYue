# 一号 → 五号：REQ-022 F3 约稿表单摘要回显（昵称/需求描述补齐）

> 日期：2026-08-05 · 分支：`f3-summary-echo` · worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-f3`（基于 master `131dee0`）
> 风险等级：低（纯客户端前端展示，无后端改动）
> 依据：`docs/requirements/REQ-022-B类低优先需求.md` F3 节 + `docs/plan-req022-实施拆解.md` §三。

## 现状（一号已核实，勿重复调研）

OrderForm.vue 右侧粘性摘要卡（L460-504）**已回显**：档位/画风名、尺寸、增项明细、总价、分期——价格链路完整。
**缺口**：REQ F3 要求摘要区回显「昵称、需求描述、已选档位、已选增项、总价」中的前两项——**昵称（form.clientName）与需求描述（form.description）当前不在摘要卡内**。

## 任务

1. 摘要卡内补齐两行回显：昵称 + 需求描述（在档位/价格信息之前，客户信息区）。
   - 昵称空时该行不显示（不显示占位灰字）；需求描述空时同理。
   - 需求描述超长截断：CSS `-webkit-line-clamp` 3 行 + 省略号（摘要卡是只读概览，不是全文展示；不加展开按钮，保持简单——工程量小的非核心倾向简单方案）。
   - 文案走 i18n（zh-CN + en 新增键，如 `orderForm.summaryNickname` / `summaryDescription`）。
2. 双模式都补：画风模式（isStyleMode）与旧模型两个 template 分支（L463/L484 结构平行）——客户信息回显两分支一致，可抽到两分支公共位置（摘要卡顶部、模式分支之外）。
3. 只读展示，不改任何提交逻辑；总价仍用引擎 displayPrice，不本地重算。

## 授权文件

- `web/src/views/client/OrderForm.vue`
- `web/src/locales/zh-CN.js`、`en.js`（仅新增键）
- `web/src/views/client/__tests__/*`（如补测试）

**黑名单**：web 其余全部（尤其 views/artist/——二号视觉批进行中）、server/ 全部、迁移文件、docs/。

## 验证与交付

- worktree 内 npm install；web vitest 全绿（基线 **186/186**）+ tsc 0 + lint 0。server 不受影响不用跑。
- 手测：npm run dev + 浏览器开客户端下单页（localhost），填昵称/描述/选档位，摘要卡实时回显——截图两张（填写中 + 长描述截断态）放报告。
- 交付报告 `docs/comms/05-to-01-F3表单回显-交付-20260805.md`。
- commit 单行 `feat(web): ...`，禁止 git add -A，不推送不合并。
