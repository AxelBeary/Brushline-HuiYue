# 04-to-01 repowiki P0 重写交付：修订版 3 篇 + 外部 13 处 🟡 汇总表

- 派工：`docs/comms/04-派工-repowiki-P0重写-20260807.md`（一号 → 四号）
- 完成日期：2026-08-07
- 核对基准：master HEAD `11e99e5`（含 v45 迁移 / 埋点 / 巡检修复批）；关键代码点均经 grep/Test-Path 实测
- 核对人：四号（需求整理者）
- 纪律遵守：仓库内 `docs/external-wiki/` 直接改；**外部原文（C:\Users\qly19\Desktop\repowiki\）一字未动**；不碰代码文件；git 只 add/commit 本次 docs 改动，不推送

---

## 〇、最重要结论

**三篇 P0 修订版已按 master 代码重写完成并落盘 `docs/external-wiki/`；外部 13 处 🟡（认证 6 + 非认证 7）已汇总成一张表（见第三节），供一号拍板处置路线。**

重写过程中额外发现并修正的事实（派工未点名但修订时涉及）：

1. **`JWT_SECRET` 并非全库零命中**——`server/src/features/admin/health.service.ts` 第 7 项检查的**显示名**叫 `JWT_SECRET`（`name: 'JWT_SECRET'`），但实际读取和检查的变量是 `SESSION_SECRET`。结论不变：**没有 JWT_SECRET 这个环境变量**（会话 token 为 HMAC-SHA256 签名，auth.service.ts:181-182，无 JWT 库），但三篇修订版均把这一点写准确了（健康检查显示名 vs 实际变量）。
2. **`.env.example` 的 SESSION_SECRET 注释自带「JWT 签名」措辞**——这是仓库内措辞（沿用 .env.example 注释），修订版按代码事实写「会话 HMAC 签名」，并在环境配置管理.md 的虚构变量表里解释 JWT_SECRET 显示名来源，避免读者被健康检查报告误导。
3. **迁移版本已到 v45**（非认证报告当时为 v44 基线，本批修订版统一写最新 v45）。
4. **限流、日志、云存储等一批「概念性」虚构**（RATE_LIMIT_*、LOG_LEVEL、STORAGE_PROVIDER/ACCESS_KEY/BUCKET/REGION）经全库 grep 确认零命中，已一并列入虚构变量对照表（派工只点名了 RATE_LIMIT_*，其余为修订时顺带核实）。

---

## 一、任务①：三篇修订版 diff 摘要

### 1. `docs/external-wiki/wiki-快速开始指南.md`（新建，对应外部 `项目概述\快速开始指南.md`）

| 外部原版过时点 | 修订版修正（实测依据） |
|---|---|
| 「entrypoint.sh 通过 tsx 运行 index.js」 | 入口为 **`src/index.ts`**；entrypoint.sh 实为 `cd /app/server && exec npx tsx src/index.ts`；index.js 不存在 |
| 「.env 填写必要变量（…SIGN_SECRET…）」 | **SIGN_SECRET 全库零命中**；必填项改为 .env.example 权威清单（SESSION_SECRET/COOKIE_SECRET/ADMIN_QQ/DOMAIN）；新增「不存在变量清单」 |
| 「首次种子数据：node server/src/db/seed.js」 | seed.js 已改名 **`seed.ts`**；正确命令为 `docker compose exec web npm run db:seed`（等价 tsx src/db/seed.ts） |
| 「生产：仅 expose 3000」写成默认行为 | compose 现状仍 `ports: 3000:3000`（注释要求生产手动注释），修订版明确「这是要做的操作」 |
| cite 引用 app.js/index.js/connection.js/seed.js 旧名 | 全部改 .ts（init.js 唯一保持 .js） |

其余保留并核实的内容：SQLite 单文件、Vite 5173 代理 /api+/uploads、node:22-slim、Caddy 自动 HTTPS、Docker Compose 一键启动步骤、验证清单、常见问题。

### 2. `docs/external-wiki/wiki-生产部署.md`（新建，对应外部 `架构设计\部署架构\生产部署.md`）

| 外部原版过时点 | 修订版修正（实测依据） |
|---|---|
| 「必须修改 SIGN_SECRET」「SESSION_SECRET/SIGN_SECRET 未更换会导致会话伪造或链接泄露」 | **无 SIGN_SECRET**；文件访问签名密钥 = **`SESSION_SECRET`**（file-sign.ts `getSecret()`：生产未设置 throw、开发回退 dev-secret） |
| 「Web 容器暴露 3000 端口供内部网络访问，不直接映射宿主机端口」写成已成事实 | compose 当前仍 `ports: "3000:3000"` + `expose 3000`；修订版给「生产切换步骤」：注释 ports 两行、仅保留 expose |
| 「自动 HTTPS 与泛解析」 | Caddyfile 仅 `{$DOMAIN}` 单主域（**无泛解析**，C48 决策放弃子域名，画师主页走路径 /artist/:subdomain）；自动 HTTPS 部分仍正确保留 |

其余保留并核实：Dockerfile 多阶段+非 root+chown、WAL/DELETE 模式、迁移自动备份 .bak.v<N>、/api/health 与 /api/admin/health 8 项、静态资源缓存策略、敏感路径签名（15 分钟 TTL）、SENTRY_DSN_BACKEND、TRUST_PROXY、运维命令、生产检查清单。

### 3. `docs/external-wiki/wiki-环境配置管理.md`（新建，对应外部 `部署运维\环境配置管理.md`）

| 外部原版过时点 | 修订版修正（实测依据） |
|---|---|
| 「生产环境：PostgreSQL 或 MySQL」「DATABASE_URL/DB_POOL_SIZE/DB_TIMEOUT_MS」 | **纯 SQLite**（connection.ts 仅 better-sqlite3 / `:memory:`）；数据库连接与初始化章节按 DB_PATH 重写 |
| 「JWT_SECRET：签名密钥（必须存在）」「RATE_LIMIT_WINDOW_MS / RATE_LIMIT_MAX_REQUESTS」 | 均零命中；新增「虚构变量对照表」（含 SIGN_SECRET/JWT_SECRET/RATE_LIMIT_*/LOG_LEVEL/DATABASE_URL/云存储凭据/BOT_*/LOGIN_CODE_*，共 15 个） |
| 「SENTRY_DSN：错误追踪 DSN」 | 正式变量 **`SENTRY_DSN_BACKEND`**（app.ts:210）；CSP connect-src 兼容 `\|\|SENTRY_DSN`（app.ts:145），.env.example 只列 BACKEND |
| 「云存储：通过 OSS/GCS/S3 相关环境变量接入」「邮件、短信、支付网关等」 | 全库零命中；明确「仅本地文件系统，无云存储实现」 |
| 「建议配置中心/热重载」「.env.development/.env.production 拆分」 | 不存在；明确「启动时读一次、无热重载、单 .env」 |
| cite 引用 index.js/app.js/connection.js/seed.js 旧名 | 改 .ts（init.js 保持 .js） |

额外澄清（代码核实）：健康检查第 7 项显示名 JWT_SECRET、实际查 SESSION_SECRET；限流参数在调用处硬编码（例：transfer = IP 5 次/15min + 目标 QQ 3 次/15min）；AUTH_DEV_MODE 不要写进 compose environment。

---

## 二、README 归档表同步（`docs/external-wiki/README.md`）

- 归档内容表新增 3 行（wiki-快速开始指南 / wiki-生产部署 / wiki-环境配置管理）。
- 顺手一致性修正：`wiki-数据库模式设计.md` 行「迁移 v1..v43」→「v1..v45」（README 原表过时，与已重写文档一致）。
- 处置建议更新：P0 已完成清单扩为 5 篇；P2 待办指向本报告第三节汇总表。

---

## 三、任务②：外部 13 处 🟡 汇总表（认证 6 + 非认证 7，供一号决策）

**说明**：认证系 6 处来自 `04-to-01-repowiki-P2-交付-20260807.md` 第三节；非认证 7 处来自 `04-to-01-repowiki非认证-交付-20260807.md` 第三节。「当前正确版本」均以 master 代码为准（部分经本批复核）。

### 认证系 6 处

| # | 文档（外部） | 过时段落 | 当前正确版本 | 建议修法 |
|---|---|---|---|---|
| 1 | `API 接口文档\画师管理接口.md` | cite 与正文引用 `server/src/app.js`、`index.js`（约 5 处，含 mermaid 图） | `app.ts` / `index.ts`（100% TS，.js 不存在） | 全局替换旧文件名 |
| 2 | `后端开发指南\认证授权系统\会话管理.md` | 图例 `POST /login (用户名/密码/TOTP)`；正文「暴露登录、登出、刷新等接口」 | 唯一登录端点 `POST /api/auth/verify {qqNumber, code}`（TOTP）；**无会话刷新接口** | 改图例 + 删「刷新」字样 |
| 3 | `架构设计\前后端通信协议\认证协议.md` | 图例 `POST /login` + `POST /verify-totp`；正文「自动续期策略」 | 同上；会话无自动续期，过期需重新 verify 登录 | 改图例 + 删自动续期描述 |
| 4 | `项目概述\核心功能特性\认证授权系统.md` | 「登录视图：处理用户名/密码与TOTP输入交互」 | 登录页仅 QQ 号 + 6 位 TOTP 码 | 删旧描述一句 |
| 5 | `架构设计\安全架构.md` | 「TOTP 双因素、JWT/HMAC Token」「上传与静态资源由 app.js 统一处理」 | 无 JWT，会话为 HMAC-SHA256 签名 token（auth.service.ts）；`app.ts` | 删 JWT + 文件名替换 |
| 6 | `故障排除\错误码参考.md` | 「刷新 Token」字样 | 无刷新接口；登出 = bumpTokenVersion + clearCookie | 删/改措辞 |

### 非认证 7 处

| # | 文档（外部） | 过时段落 | 当前正确版本 | 建议修法 |
|---|---|---|---|---|
| 7 | `部署运维\监控告警系统.md` | cite 引用 `server/src/app.js`（5 处） | `app.ts` | 全局替换旧文件名 |
| 8 | `数据库设计\数据迁移管理.md` | cite 引用 `connection.js`；演进列表止于 v43 | `connection.ts`；补 **v44**（events+anon_tokens 埋点）+ **v45**（init.js MIGRATIONS 实为 v45） | 改文件名 + 补迁移版本 |
| 9 | `数据库设计\数据访问模式.md` | cite 引用 index.js/app.js/connection.js/seed.js（4 处） | index.ts/app.ts/connection.ts/seed.ts（init.js 仍为 .js） | 全局替换旧文件名 |
| 10 | `核心功能模块\仪表盘与分析.md` | 「实时数据更新机制：WebSocket」及故障排查「确认 WebSocket 连接状态」 | **无 WebSocket**（全库零命中）；数据更新为 REST 拉取；且未覆盖埋点看板（v44 tracking） | 删 WebSocket + 可补埋点章节（⚪ 缺失项） |
| 11 | `项目概述\项目结构说明.md` | cite 引用 index.js/app.js/connection.js（3 处） | .ts | 全局替换旧文件名 |
| 12 | `故障排除\日志分析.md` | cite 引用 `app.js`；「根据 SENTRY_DSN 动态拼接 connect-src」 | `app.ts`；app.ts:145 实为 `SENTRY_DSN_BACKEND \|\| SENTRY_DSN` | 改文件名 + 变量名 |
| 13 | `knowledge\zh\环境变量与运行时配置系统.md` | BOT_ENABLED/BOT_WS_URL（REQ-028 未实施）、LOGIN_CODE_TTL/LOGIN_CODE_MAX_ATTEMPTS（v41 已移除）；「SESSION_SECRET（JWT/…）」措辞 | 均不存在；.env.example 为权威清单 | 删虚构变量 + 措辞 |

> 注：本批三篇 P0 修订版已将上表中同主题内容（文件名/虚构变量/SENTRY 命名）一并修正，因此**新增交付与修补批天然互补、不重复**。

---

## 四、路线建议（交一号拍板）

| 方案 | 动作 | 成本 | 适合场景 |
|---|---|---|---|
| **A：转贡献者** | 出修改单（上表 13 处 + 3 篇 P0 的 diff 摘要）发给外部贡献者，由 ta 在 repowiki 原文修订 | 沟通往返成本高、周期不可控 | 贡献者仍活跃且愿意维护 |
| **B：仓库内维护（推荐）** | 13 处 🟡 按「修补批」在 docs/external-wiki/ 维护修订版（与 P0 三篇同模式）；外部原文继续一字不动 | 每个主题 1-2 次小派工 | 与现有 P0 模式一致、无需依赖外部响应 |

**四号建议**：走 **B**。理由：①P0 三篇已示范仓库内维护模式（README 已立目录），修补批只是同一模式的延伸；②外部报告约 1/3 不实指控的历史教训下，A 方案需要先让贡献者核对我们已逐条对照代码的判定，沟通成本高且我们已「以代码为准」完成验证；③若一号仍想回馈贡献者，可在 B 之外附加「把核对结论发 ta 参考」（善意回馈，不强依赖）。

## 五、存疑标注（暴露不确定性）

1. 三篇修订版对**本文引用文件**均经 Test-Path/读源码确认存在；未逐行核对每处行号引用（如 app.ts:145/210 为实测行，其余如 `docs/开发→生产切换指南.md` 的行号未逐行验证，文档内未引用具体行号）。
2. `数据迁移管理.md` 的 🟡 建议补 v44+v45，但 **v45 迁移具体内容未核查**（本批只确认 init.js MIGRATIONS 数组存在 version 45 及对应测试文件）；如需写进修补批，建议修补时顺带确认 v45 内容（或引用已重写的 wiki-数据库模式设计.md，其头部已写最新 v45）。
3. 上表 13 处「建议修法」为修补级动作；若一号拍板 A 方案，需把本表连同 P0 diff 摘要整理成对贡献者的修改单（可复用本报告格式）。
4. 本报告与三篇修订版已 commit（见第六节），未推送。

## 六、git 操作

- 新增：`docs/external-wiki/wiki-快速开始指南.md`、`docs/external-wiki/wiki-生产部署.md`、`docs/external-wiki/wiki-环境配置管理.md`
- 修改：`docs/external-wiki/README.md`、本报告
- Commit：`docs: repowiki P0 重写批——快速开始/生产部署/环境配置管理 修订版 + 13处🟡汇总表`
- 已遵守：只在 master 操作、只 add/commit 本次 docs 改动、**未推送**；外部 repowiki 原文未改一字。