# 一号 → 四号：v0.38 并行批 · REQ-022 实施拆解 + REQ-026 验收清单（纯文档）

> 日期：2026-08-05 · 分支：直接在 master 工作（文档批，一号代提交）或另行建 worktree——**本批只写 docs/ 文档，零代码**
> 风险等级：低（纯文档）
> 背景：REQ-022 五项（F1/F2/F3/F4/F5）用户已全部拍板，是 v0.38 之后的下一大批；F2 外链 2026-08-05 升级为「重做」（范围扩大）。派工前需要实施拆解，避免 D 路补漏批式「需求稿未定稿就开工」事故。

## 任务

### T1：REQ-022 实施拆解文档 → `docs/plan-req022-实施拆解.md`

对 F1 作品链路 / F2 外链重做 / F3 表单回显 / F5 留言分组（F4 已派五号，不用拆）逐项给出：

1. **任务分解**：按角色拆分（二号客户端前端 / 三号后端 / 五号 bug 域），每项估时（对照 REQ 整体预估表校准）；
2. **API 契约草案**：新增/变更的接口列表（方法/路径/body/响应形状），重点：
   - F2：平台配置表 schema（platforms 表：名称/匹配域名列表/图标白名单键/排序/启用）、customLinks+platformUrls 两字段合并后的单一结构（上线前无真实数据，不写迁移——REQ 已注明）、粘贴识别规则的后端职责边界（校验放前端还是后端？给结论）；
   - F1：完稿转作品接口 + 作品-档位绑定关系表（多对多，用户拍板允许绑多个）；
3. **依赖与顺序**：哪些可并行、哪些有先后（如 F2 后端平台表 → 前端下拉）；
4. **风险点**：每项列「可能翻车的地方」（对照历史教训：snake_case 映射、前端校验只能是后端子集、i18n 双语同步）。

### T2：REQ-026 视觉批验收操作清单 → `docs/comms/04-to-01-REQ026验收清单-20260805.md`

一号审核二号视觉批交付时逐条执行的检查表：REQ-026 §六 10 条验收 × 具体操作（打开哪个页面、看什么元素、两主题各查一遍），7 色语义核对表（每种状态→应该出现的颜色），客户端零影响抽查路由列表。写成可勾选的 checkbox 清单。

## 纪律

- 只写上述两个文档，不改任何代码、不改需求稿（REQ 原文只读）。
- 拆解中的不确定点标注「待一号/用户确认」，不自行拍板功能决策。
- 完成后报告路径 + 两项各自的关键结论摘要（如 F2 后端校验职责结论、并行度结论）。
