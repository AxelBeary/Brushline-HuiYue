# 一号→五号：P2 审计修复 11 项（2026-08-02）

## 背景

P0/P1 已修。剩余 P2 项（排除 #16 归三号）。一个分支搞定。

## 分支

`fix/p2-audit-fixes`，worktree 自建。基于 master。

## 清单

| # | 项 | 修复方案 | 预估 |
|---|---|----------|------|
| 11 | 增项 tierIds 空数组=关联全部（语义反） | 空数组 → 不关联任何档位（改 pricing.service.ts 条件） | 10min |
| 12 | 同一增项可重复提交 | addOnToOrder 前检查 order_addons 是否已有该 addonId | 10min |
| 13 | 手动录单三步非原子 | ManualOrder.vue 改为单次 API 调用（后端已有 createOrder 接受全部字段）或前端 Promise.all 改串行+回滚 | 30min |
| 14 | 上传 timeout 15s 不够 | api/index.js axios 实例 upload 相关请求覆盖 timeout: 120000 | 5min |
| 15 | relativeTime 时区偏差 | dashboard-normalize.js `new Date(str)` 改为 `new Date(str.replace(' ', 'T'))` | 5min |
| 17 | gc-uploads.js 不收集 notes.image_path + unlinkSync | 加 order_notes 收集 + 改 unlink→promises.unlink + dry-run 模式 | 20min |
| 18 | timingSafeEqual catch 降级 === | 删除 catch 分支，length 不等直接返回 false（不降级） | 5min |
| 19 | /api/orders/my QQ 当密码 | 加 rateLimit（每 IP 每分钟 10 次），防枚举 | 10min |
| 20 | refresh-signatures 无归属校验 | 加 order_id 参数，校验 path 属于该订单的 references | 15min |
| 21 | embed 死代码清理 | 删 embed.js + Settings disabled tab + app.js CSP 分支 | 15min |
| 22 | SPA fallback startsWith 缺分隔符 | `url.startsWith('/api')` 改 `url === '/api' || url.startsWith('/api/')` | 5min |

## 授权文件

- `server/src/features/pricing/pricing.service.ts`
- `server/src/features/order/order.routes.ts`
- `server/src/features/auth/auth.service.ts`
- `server/src/app.js`
- `scripts/gc-uploads.js`
- `web/src/views/artist/ManualOrder.vue`
- `web/src/api/index.js`
- `web/src/components/artist/dashboard/dashboard-normalize.js`
- `web/src/views/artist/Settings.vue`（删 embed tab）
- `web/src/utils/embed.js`（删除）
- `server/test/`

## 风险等级

中（#19/#20 涉及安全逻辑，#21 删代码需确认无引用）。完成后写 comms `05-to-01-p2-fixes-{日期}.md`。
