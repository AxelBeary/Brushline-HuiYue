# 02-派工-动画修复批-交付-20260807.md

> 二号转交一号，文件：docs/comms/02-to-01-动画修复批-交付-20260807.md
> 派工：`docs/comms/02-派工-动画修复批-20260807.md`
> 分支：`beta/anim-fix`（worktree `artist-commission-w2`，已 merge master `a7a9b99`）
> 完成时间：2026-08-07

## 改动清单（逐条）

### H1. 滚动渐入去回弹 ✅
- `templates.css` tpl-fade-up 位移 24px → 12px
- `.tpl-reveal.tpl-visible` 动画 0.6s `--ease-bounce` → 0.42s `cubic-bezier(0.165, 0.84, 0.44, 1)`（ease-out-quart）
- 吸底 CTA 过渡去 bounce → `0.3s ease`
- receipt-btn hover 去 bounce → `0.15s ease`（hover ≤0.15s 红线）

### H2. 后台全局 `*` 0.35s 颜色过渡 → 只挂主题切换 ✅
- `artist-tokens.css:246` 常驻 `html[data-artist-theme] .artist-scope *` → `html[data-artist-theme].theme-animating .artist-scope *`（瞬态挂载）
- 豁免规则同步加 `.theme-animating`
- 配套 JS：`ThemeToggle.vue` onToggle 切换前 `classList.add('theme-animating')`，400ms 后移除
- ⚠️ 说明：ThemeToggle.vue 不在授权清单明列，但施工图 H2 明确指示"配套 JS（ThemeToggle.vue 或主题切换实际所在组件）"，为完成 H2 必改，特此标注请一号复核

### H3. 时间线脉冲 box-shadow → 伪元素 scale/opacity ✅
- `templates.css` tl-pulse keyframes：box-shadow → `transform: scale(1→1.6) + opacity`
- `OrderTimeline.vue` `.tl-node.current .tl-dot` 保留实心点，动画移至 `::after` 伪元素（position:absolute + inset:0）
- 补 `.tl-dot { position: relative }`（施工图提示确认项，原文件无则补）

### H4. 图库骨架 shimmer background-position → translateX ✅
- `TplGallery.vue` `.tpl-gallery-skeleton`：background-position 动画 → `::after` 伪元素 `translateX(-100%→100%)` 微光
- 两处模板骨架 div 加 `aria-hidden="true"`

### M1. 网格卡片滚动渐入交错 ✅
- `TplGallery.vue` 瀑布流 item v-for 加 `:style="{ '--i': index }"`
- `templates.css` `.tpl-visible` 加 `animation-delay: calc(var(--i, 0) * 60ms)`
- 区块级 tpl-reveal 不注入变量，延迟恒 0，零风险

### M2. transition: all 显式列举 ✅（12/13 处）
| 文件 | 行 | 处理 |
|------|----|------|
| TplHero.vue | 115 | transform/box-shadow/background-color 显式（0.15s cubic-bezier 保留） |
| TplTierGrid.vue | 142 | border-color/background-color/box-shadow 显式（0.2s） |
| TplStyleGrid.vue | 241/340 | 同上 |
| TplGallery.vue | 432/530/713/753 | 按各自 hover 属性显式（0.2s） |
| ManualOrderRight.vue | 1120 | opacity/transform 显式（0.25s；拆分后位置对应原 ManualOrder:1648） |
| Settings.vue | 648/664 | border-color/box-shadow 显式（0.2s） |
| ThemePicker.vue | 100 | border-color/color/background-color 显式（0.15s） |
| OrderForm.vue | 904 | ⏭️ 不碰（红线：五号并行批） |

### M3. OrderList / QueueBoardList 遮罩 → 订单卡片骨架屏 ✅
- 新建 `web/src/components/shared/HySkeleton.vue`（订单卡片骨架：缩略图 56px + 三行文字，translateX 微光，GPU 友好，微交错 `--i * 0.11s`）
- `OrderList.vue` 表格 `v-if="!loading"` + 前插 `<HySkeleton v-if="loading" count="6" />`，import 注册
- `QueueBoardList.vue` 正式区列表 `v-if="!loading"` + `<HySkeleton v-if="loading" count="6" />`（完成区/缓冲区 v-loading 保持不动）
- ⚠️ 路径说明：实际文件在 `components/artist/queue/QueueBoardList.vue`（拆分后位置，派工已提示 grep 确认）

### L1. --font-body 加 Noto Sans SC 首位 ✅
- `theme.css`：`'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', -apple-system, ...`
- 字体已由 main.js 加载（noto/font.css），零额外成本

### L2. OrderTimeline .tl-dot 状态切换 0.3s → 0.15s ✅
- `OrderTimeline.vue` `.tl-dot` transition 全部 0.15s

### 动画移植 ✅（按表执行）
| # | 动画 | 状态 |
|---|------|------|
| 1 | 骨架屏微光 translateX 版 | ✅ 并入 H4 + M3 |
| 2 | 订单卡片骨架 | ✅ 并入 M3（HySkeleton） |
| 3 | 旋转圆环 | ✅ 新建 `HySpinner.vue`（variant="ring"，0.9s linear 纯 rotate） |
| 4 | 脉冲圆点 | ✅ 并入 H3 |
| 5 | 墨滴扩散 | ⏭️ 不做（等 A 测反馈） |
| 6 | 打字指示器 | ⏭️ 不做（TplGuestbook 未授权） |
| 7 | 波浪点阵 | ✅ HySpinner variant="wave"（7 点 × 85ms） |
| 8 | 进度条纹 | ⏭️ 不做（DeliverDialog/usePasteUpload 未授权） |
| 9 | 印章落定 | ⏭️ 不做（OrderForm 未授权，五号并行批） |
- HySpinner 本批仅建组件，未接入替换点（涉及未授权文件 ArtStyleManager/Settings 等，标注待下批）

## 验证结果

### 门禁（全部通过）
- ✅ `cd web && npm run test:web` → **215/215 全绿**（13 files）
- ✅ `cd web && npm run lint` → oxlint **0 error**（3 warnings 为既有基线：api/index.js/main.js，非本批引入）
- ✅ `cd web && npm run build` → 成功（15.84s）
- ✅ `npm run check:i18n` → OK，无新增硬编码中文（存量违规 9 条豁免）
- ✅ `git diff --stat` → 14 文件改动 + 2 新建，全部在授权范围
- ✅ 全库 `transition: all` 复查 → 仅剩 OrderForm.vue:904（红线不碰）

### 运行时视觉验证（隔离实例 3100=新构建 / 3101=master 旧构建，测试库）
| 项 | 验证方式 | 结果 |
|----|---------|------|
| H1 | getComputedStyle(.tpl-reveal.tpl-visible) | ✅ animation=tpl-fade-up, duration=0.42s, timing=cubic-bezier(0.165,0.84,0.44,1) |
| H2 | MutationObserver 监听切换瞬间 | ✅ theme-animating 类出现后 400ms 移除 |
| H3 | OrderDetail 时间线 computed style | ✅ after: ::after animation=tl-pulse + position:absolute; before: .tl-dot 自身 animation + static |
| H4 | 拦截 /uploads 请求强制 loading | ✅ .tpl-gallery-skeleton 出现（骨架生效） |
| M3 | 拦截 orders API 2.5s 强制 loading | ✅ .hy-skeleton 出现（骨架生效） |

## 截图（workspace/temp/anim-batch/，不入 git）

| 截图 | 内容 |
|------|------|
| after-h4-gallery.png / before-h4-gallery.png | 客户端画廊（新 vs 旧） |
| after-h4-skeleton-loading.png | 图库骨架 loading 态（新） |
| after-h1-scroll-reveal.png / before-h1-scroll-reveal.png | 滚动进场 |
| after-h3-timeline.png / before-h3-timeline.png | track 页时间线 |
| after-h3-timeline-orderdetail.png / before-h3-timeline-orderdetail.png | OrderDetail 时间线当前节点 |
| after-m3-orderlist.png / before-m3-orderlist.png | OrderList（新 vs 旧） |
| after-m3-skeleton-loading.png | OrderList 骨架 loading 态（新） |
| after-h2-queue.png / before-h2-queue.png | 后台队列（新 vs 旧） |
| after-h2-theme-switched.png | 主题切换后（新） |

## 自修说明（超出施工图字面的必要处理）
1. `.tl-dot` 补 `position: relative`——H3 伪元素定位必需，施工图已预留"无则补一行"
2. `ManualOrderRight.vue:1120` 替代 `ManualOrder:1648`——M2 清单行号是拆分前，拆分后 transition:all 落在此文件，按意图处理
3. `ThemeToggle.vue` H2 配套 JS——见 H2 标注
4. HySpinner 仅建组件不接入——接入点涉及未授权文件，标注待下批

## 遗留/待下批
- OrderForm.vue:904 transition:all（五号并行批处理）
- HySpinner 替换接入点（ArtStyleManager:152 覆盖、Settings coverLoading）——涉及未授权文件，待一号分配
- M2 中 Settings/ThemePicker/ManualOrderRight 属画师后台文件，本批按施工图执行；如一号希望归三号领域请指正
