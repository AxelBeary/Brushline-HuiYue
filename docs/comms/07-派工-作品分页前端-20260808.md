# 07-派工-作品分页前端-20260808.md

> 派工：一号 → 子代理（leaf）
> worktree：`../artist-commission-w7`（分支 `beta/artwork-paging`）
> 需求：用户拍板作品分页（画师端 20/页 封面置顶 / 客户端 10/页 加载更多）+ 后端已就绪
> 基线：master `cdf0c3c`。**开工第一步 `git merge master`**。

## 背景

后端已就绪：`GET /api/artist/artworks/paged?page&pageSize`（画师端默认 20，clamp 1-50，封面置顶）+ `GET /api/public/artworks/:artistId?page&pageSize`（公开默认 10，clamp 1-30，封面置顶）。老接口保留。本批做前端接入。

## 授权文件

- `web/src/views/artist/ArtworkManage.vue`（画师端：老接口 → paged + el-pagination）
- `web/src/views/client/templates/T2Gallery.vue`（客户端：10/页 + 加载更多按钮，**先做 T2 一个模板**，其余模板照抄模式——若 T2 文件名不同先 grep 找 gallery 模板实际名）
- i18n：zh-CN.js + en.js
- **不碰**：后端、其他页面

## 施工图

### 1. 画师端 ArtworkManage（20/页 + el-pagination + 封面置顶）
- 读现状：现在调哪个接口（`/api/artist/artworks`？）、数据形状（mainArtworks 数组？）
- 改为调 `GET /api/artist/artworks/paged?page=1&pageSize=20`，返回 `{ items, total, page, pageSize }`（先读 artist.routes.ts:349-360 确认返回结构）
- 底部加 el-pagination（total / pageSize 20 / current-page），翻页重新拉
- **封面置顶**：后端已按 is_cover 置顶（先确认返回顺序），前端不重复排

### 2. 客户端 T2Gallery（10/页 + 加载更多）
- 读现状：现在调哪个公开接口、如何渲染作品网格
- 改为调 `GET /api/public/artworks/:artistId?page&pageSize=10`（先读 artist.routes.ts:613-630 确认返回结构 + 签名参数）
- 列表底部加「加载更多」按钮：点击 page+1 追加；无更多时按钮隐藏/禁用
- 保留封面置顶语义（后端返回顺序）

### 3. 实现铁律
1. 不做假修复；分页边界（page 越界/空结果）处理干净
2. i18n zh/en 同步（加载更多/无更多文案）
3. 视觉沿用现有样式，纸墨 token

## 验证门禁

1. `cd web && npm run test:web` → 215/215
2. lint 0 / build 过 / check:i18n 0
3. `git diff --stat` 只动授权文件
4. commit 到分支 `beta/artwork-paging`，不推送不合并
5. 交付报告 `docs/comms/07-to-01-作品分页-交付-20260808.md`：接入说明 + 分页边界处理 + 验证

## 工作方式（子代理注意）

- worktree 若不存在先 `git worktree add D:\Hermes Agent CN Desktop\workspace\artist-commission-w7 -b beta/artwork-paging`
- git merge master 后再改
- 写文件注意 UTF-8 无 BOM、LF 行尾
- 自测 `cd web && npm run test:web` 215/215
- 结束时清理临时文件，git status 干净
