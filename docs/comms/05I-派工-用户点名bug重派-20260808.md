# 05I-派工：用户点名 bug 修复（重派——原 05 批从未交付，03D 核查发现漏做）

> 派工人：一号（主理人）
> 日期：2026-08-08（重派）
> 执行角色：五号（bugfix）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w25`
> 分支：`beta/user-bugs-redo`
> 交付报告：`docs/comms/05I-to-01-用户点名bug修复-交付-20260808.md`（写完 commit 进分支）

## ⚠️ 背景（为什么重派）

原派工 `docs/comms/05-派工-用户点名bug修复-20260808.md`（凌晨发出）**从未交付**：无交付文件、无分支、代码两处均未修复（03D 核查C 已独立核实属实）。**用户点名过这两个 bug，必须补上**。

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w25'
git merge master --no-edit
```

## 授权文件（只许动这些）

1. `web/src/views/admin/DefaultWorkflowEditor.vue` — Bug 1
2. `web/src/components/artist/WorkflowPaymentEditor.vue` — Bug 1
3. `web/src/components/artist/dashboard/QuickActions.vue` — Bug 2
4. `web/src/api/index.js` — Bug 1（若需 adminApi 方法，已有 getDefaultWorkflow/updateDefaultWorkflow/resetDefaultWorkflow L342-344，应够用）
5. `web/src/locales/zh-CN.js` + `en.js` — 如需键

> 不改其他文件；不引新依赖。

## Bug 1：管理员默认流程模板只读（用户：「默认流程模板为什么不能改？怎么还是灰色的？」）

**现状**：
- `web/src/views/admin/DefaultWorkflowEditor.vue:12` 用 `<WorkflowPaymentEditor mode="template" />`
- `web/src/components/artist/WorkflowPaymentEditor.vue:9` `:readonly="mode === 'template'"` → 管理员看默认流程编辑器全部灰色只读
- 后端 PUT `/api/admin/default-workflow`（admin.routes.ts:417-440）完整可写
- adminApi 已有 `updateDefaultWorkflow(nodes)`（api/index.js:343）

**修复**：
1. 读 WorkflowPaymentEditor 完整代码：确认 `mode` 各值行为（template=只读预览 / artist=可编辑带保存按钮调 artistApi）
2. DefaultWorkflowEditor 改为**可编辑**：新增 `mode="admin"`（或 `editable` prop）——复用 artist 可编辑逻辑但保存调 `adminApi.updateDefaultWorkflow`（先读 WorkflowPaymentEditor 的保存逻辑确认如何参数化 api）
3. **不要误伤**：先 grep 谁在用 `mode="template"`（画师设置页预览默认模板要保留只读）
4. 验收：管理员打开「默认流程」页可编辑（节点/收款/话术），保存调 PUT /api/admin/default-workflow 成功；画师端预览仍只读

## Bug 2：主页快捷按钮「仪表盘」没删（用户：「主页右边快捷按钮的仪表盘怎么还没删？」）

**现状**：
- `web/src/components/artist/dashboard/QuickActions.vue:12` QUICK_ACTION_POOL 第一条 `{ key: 'dashboard', type: 'route', icon: Odometer, labelKey: 'menu.dashboard', route: '/dashboard' }`

**修复**：
1. 从 QUICK_ACTION_POOL **删除 dashboard 项**（用户已拍板不要）
2. 检查 QUICK_ACTION_POOL 其余项：确认无其他「多余导航型」按钮（若发现报告列出，不擅自删）
3. 若 `menu.dashboard` 键因此无引用——先 grep，有其他地方用则保留

## 实现铁律

1. 不做假修复（显式 any / @ts-ignore 蒙混 = 打回）
2. i18n zh/en 同步；禁硬编码中文
3. 改动最小化，不碰无关逻辑
4. 用户原话：「我们实现起来一定要避免屎山，禁止技术债，代码要优雅！」

## 门禁（提交前必须全绿）

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w25\web'
npm run lint
npm run test:web
npm run check:i18n
npm run build
```

## 交付要求

1. commit 描述中文（如 `fix(web): 用户点名bug——管理员默认流程可编辑+删快捷按钮dashboard`）
2. 交付报告写 `docs/comms/05I-to-01-用户点名bug修复-交付-20260808.md`：两 bug 改动说明 + 验收（管理员编辑默认流程保存成功 + 快捷按钮无 dashboard）+ 门禁结果
3. 结束前：停本地 dev server；`git status --short` 确认无未跟踪残留
4. **不推送、不合并、不改 STATUS**
