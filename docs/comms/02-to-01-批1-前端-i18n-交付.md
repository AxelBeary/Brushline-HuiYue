# 02-to-01 交付报告：批1 前端 i18n——修复审计 🔴 R4（orderForm.selectSizeFirst 命名空间错位）

> 执行：二号（前端执行角色）｜分支：fix/batch1-web｜日期：2026-08-09
> 依据：%TEMP%\audit-D\report-d.md §3.2（唯一 🔴：i18n 缺键/命名空间错位）

## 一、问题与修复点

### 问题

`web/src/views/client/OrderForm.vue:585、589` 调用 `t('orderForm.selectSizeFirst')`，但键实际定义在两侧 locale 的 `manualOrder.selectSizeFirst`——客户下单未选画风/尺寸点「下一步」时会直接显示原始键。

### 前置核查（任务步骤 1）

全库 grep `selectSizeFirst`：仅 4 处出现——OrderForm.vue 两处调用 + zh-CN.js:1049 / en.js:1051 两处定义。`manualOrder.selectSizeFirst` 除 locales 定义外**零调用**（含 `titleKey`/`labelKey`/模板串/字符串拼接/动态形态，均已核查），确认为死定义，可安全删除。

### 修复（任务步骤 2-4）

| 文件 | 改动 |
|------|------|
| web/src/locales/zh-CN.js | `orderForm` 段新增 `selectSizeFirst: '请先选择画风和尺寸'`（照抄原值）；删除 `manualOrder` 段 `selectSizeFirst` |
| web/src/locales/en.js | `orderForm` 段新增 `selectSizeFirst: 'Please select a style and size first'`（照抄原值）；删除 `manualOrder` 段 `selectSizeFirst` |
| web/src/views/client/OrderForm.vue | 不动（`orderForm.selectSizeFirst` 即为正确归属） |

最小 diff：每个 locale 仅改 2 行（加 1 删 1），不涉及其他键，无重排无格式化。

### 编码红线说明（⚠️ 与任务描述有出入，按实测执行）

任务描述为「zh-CN.js 带 BOM、en.js 无 BOM」，但**实测当前 worktree（HEAD=9471151）两个文件均带 BOM（EF BB BF）**（git status 干净，非本批引入）。按红线「保持原编码不变」以磁盘现状为准执行：修改后两文件首字节仍为 `EF BB BF`，git diff 中无任何 BOM 增删。如需强制 en.js 无 BOM，请一号单独派工（本批不擅自变更编码基线）。

## 二、验收证据（cd web 后执行）

```
npm run check:i18n  ✅ OK — 存量违规 9 条豁免，无新增硬编码中文
npm run lint        ✅ eslint . 通过（0 errors）
npx vitest run      ✅ 17 test files / 254 tests passed（exit 0）
```

噪音说明：vitest 输出含 `ECONNREFUSED localhost:3000` 堆栈。已用 `git stash` 在基线 9471151 复跑确认：**基线同样输出该噪音、同样 17/254 全过**——与本次改动无关（改动仅 locale 文案行），属测试环境既有行为。

## 三、commit

```
a7fb1c2198e81f7bb32d8f54f2620d8cf08e7332（代码修复：2 个 locale 文件）
```

## 四、纪律确认

- 未 push、未 merge、未触碰 master
- 未新增依赖、未顺手改其他键、未改 OrderForm.vue
- 工作树仅含本批授权文件改动

二号 2026-08-09
