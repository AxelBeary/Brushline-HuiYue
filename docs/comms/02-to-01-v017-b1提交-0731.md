# 二号 → 一号：v0.17 第一批提交（R24 + R25 + BUG-5）
> 日期：2026-07-31

## 分支

`feat/client-frontend-v017-b1`（worktree: `artist-commission-02`，commit `6b9fb2e`）

## 改动文件（10 个，全部在授权列表内）

| 文件 | 任务 | 改动 |
|------|------|------|
| `web/src/views/client/OrderForm.vue` | R24 | openReceipt 校验失败弹 ElMessageBox 列出所有未通过项，关闭后 scrollToField 定位 |
| `web/src/composables/useOrderForm.js` | R24 | agreed 校验消息改用 `order.validation.agreeRequired` |
| `web/src/locales/zh-CN.js` | R24 | 新增 `order.validation.*` 键（title/confirm/agreeRequired） |
| `web/src/locales/en.js` | R24 | 同上，英文同步 |
| `web/src/views/client/templates/ArtistHomeClassic.vue` | R25 | ThemePicker 从侧栏卡片移至右下角 fab |
| `web/src/views/client/templates/ArtistHomeGallery.vue` | R25 | ThemePicker 从页脚移至右下角 fab |
| `web/src/views/client/templates/ArtistHomeFolio.vue` | R25 | ThemePicker 从 CTA 区移至右下角 fab |
| `web/src/views/client/templates/ArtistHomeAtelier.vue` | R25 | ThemePicker 从页脚移至右下角 fab |
| `web/src/composables/useSignatureRefresh.js` | BUG-5 | 按图刷新：refreshNow(path) + Set 去重 + Map 按图计数 |
| `web/src/views/artist/QueueBoard.vue` | BUG-5 | `@error="() => refreshNow(element.focus_image_path)"` |

## 关键设计决策

1. **BUG-5 向后兼容**：OrderDetail.vue 也用 useSignatureRefresh 且 `@error="refreshNow"`（授权外，未改）。el-image 的 error 回调透传 event 对象，refreshNow 对非字符串入参回退全量刷新——OrderDetail 行为不变。
2. **R25 fab 避让吸底 CTA**：Gallery/Atelier/Folio 有 TplStickyCta（fixed bottom:0 z-index:90），fab z-index:95 且 ctaVisible 时 bottom:72px 上移避让。Classic 无 CTA 栏不需要。
3. **R24 弹窗内容安全**：ElMessageBox 用 dangerouslyUseHTMLString 渲染列表，内容全部来自 i18n 翻译文案（无用户输入），无 XSS 风险。

## 验证结果

- `npx eslint .` 零错误零警告 ✅
- `npm run build` 通过 ✅
- `git diff --stat` 仅授权文件 ✅
