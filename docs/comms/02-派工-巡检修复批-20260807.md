# 02-派工-巡检修复批-20260807

> 派工：一号 → 二号（client-frontend）
> 来源：巡检 B（画师后台 6🔴）+ C（管理后台 2🟡）+ D（全局工程 7🟡）+ 四号健康巡检——汇总可修项，按"i18n 红线 / 移动端 / 全局"分组
> 开工第一步：**`git merge master` 再读本文件**。worktree 由一号分配。

## 一、任务分组（按优先级，全部修完再交付）

### 组 A：i18n 红线（用户可见文字必须走 $t()，英文界面不许中文）

1. **ManualOrder.vue 6 处硬编码中文**（B🔴-1，最优先）：
   - `web/src/views/artist/ManualOrder.vue` L986/L1000/L1009/L1016/L1028/L1038
   - 内容：`价格写入失败：${e.message}` / `自定义增项「${item.name}」写入失败：${e.message}` / `截稿日写入失败` / `开稿日写入失败` / `初始状态设置失败` / `订单 ${order.order_no} 已创建，但...请在订单详情中补充`
   - 修法：新增 i18n 键 `manualOrder.postCreateFailed.*`（按失败项细分，zh/en 同步），文案经 `$t()`；失败项用键映射，不用模板字符串拼接中文
2. **main.js 全局错误兜底**（B🔴-5 = D2 合并）：
   - `web/src/main.js` L39-44：`ElMessage.error('页面出了点小问题，请刷新重试')` + 动态 `import('element-plus')`
   - 修法：i18n 键 `common.globalError`；顶部静态引入 ElMessage（动态 import 冗余）
3. **api 拦截器兜底文案**（D3）：
   - `web/src/api/index.js` L29：`网络错误，请稍后重试`
   - 修法：i18n 键 `errors.NETWORK_ERROR`（或由调用方兜底，选更简单者）
4. **Settings 强调色名硬编码中文**（D1）：
   - `web/src/views/artist/Settings.vue` L379-383：ACCENT_PRESETS 的 `name` 字段中文「青/碧/蓝/靛/紫」
   - 修法：改 `nameKey` 走 `t('pref.accentNames.*')`（ThemePicker.vue:62-66 同款键已存在，直接复用）
5. **en 补 2 键**（B🔴-6）：`landing.weibo` / `landing.bilibili`（zh 有 en 缺，补 Weibo/Bilibili）

### 组 B：移动端布局（375px）

6. **OrderList 表格列被裁剪无横向滚动**（B🔴-2，高）：
   - `web/src/views/artist/OrderList.vue`：el-table-column 固定 width 合计 1004px > 375px 视口，body-wrapper overflow-x:hidden 无法滚动
   - 修法（三选一，选改动小且稳者）：①移动端（≤768px）隐藏低优先级列（缩略图/来源）②el-table body-wrapper 允许横向滚动（overflow-x:auto + min-width）③响应式卡片。**推荐 ②**（最小改动，EP 自带横向滚动支持）
7. **TierManage 移动端 tabs 溢出**（B🔴-4，中）：
   - `web/src/views/artist/TierManage.vue` L14-40：5 个 el-tab-pane 在 375px 下 nav 412px 被裁，尾部「增项库」不可点，无滚动箭头
   - 修法：tabs 容器加横向滚动能力（EP el-tabs 默认应出箭头，检查是否被 overflow:hidden 拦了）或加 `stretch`；确保移动端所有 tab 可达
8. **AdminDashboard 窄屏 header 按钮溢出**（C🟡-1，中）：
   - `web/src/views/admin/AdminDashboard.vue`：卡片 header 内联 `display:flex; justify-content:space-between` 4 按钮无 wrap，375px 下第 4 个按钮 right=413px 出视口
   - 修法：header 内联 div 加 `flex-wrap: wrap; gap: 8px;`（参考同文件 `.gb-filter-header` 写法）

### 组 C：全局/细节

9. **ArtistDetailDrawer 状态缺 hidden**（C🟡-2，中）：
   - `web/src/views/admin/ArtistDetailDrawer.vue`：radio-group 仅 open/full/break，hidden 画师打开抽屉无选中项且无法改回
   - 修法：补 `<el-radio-button value="hidden">{{ $t('settings.statusHidden') }}</el-radio-button>`（先确认 i18n 键存在，无则补键 zh/en）
10. **后台 404 视觉断裂**（D4，需按口径）：
    - router/index.js + NotFound.vue：画师/管理员登录后访问未知路径落到客户端 404（无侧边栏/纸墨 token）
    - 口径（一号已定）：**按路由前缀区分**——/dashboard、/admin 前缀的未知路径重定向回各自首页（简单可靠），客户端保持 404 页
11. **admin 顶栏标题空**（D5，低）：
    - `web/src/components/ArtistLayout.vue` pageTitle computed：/admin 子路由无命中返回 ''
    - 修法：对 /admin* 前缀回退 `t('menu.admin')`
12. **TrackOrder 支付进度条硬编码色**（D6，低）：
    - `web/src/views/client/TrackOrder.vue` L146：`'#67c23a' : '#409eff'`
    - 修法：用 CSS 变量（客户端主色体系 var(--color-success)/var(--color-primary)）

### 组 D：顺手小修（四号健康巡检）

13. **formatCents 抽 utils**（Y1）：全库 7 处重复（PaymentPanel.vue:94 / useOrderPaymentPanel.js:25 / OrderDetail.vue:823 / GreetingHero.vue:64 / RevenueChart.vue:76 / ArtistManage.vue:218 / TrackOrder.vue:291）→ 新建 `web/src/utils/money.js` 导出 `formatCents`，7 处统一引用
14. **OrderDetail 死解构清理**（Y5）：`currentStageIdx` / `nextStage` / `daysLeft`（OrderDetail.vue 解构处删除，模板零使用）
15. **GalleryPanel dragleave 防闪烁**（R4）：`web/src/components/artist/order/GalleryPanel.vue` L22-24 加 relatedTarget 检查（抄 OrderDetail.vue 备注区 onNoteDragLeave 写法）
16. **QueueBoard console.warn 清理**（B🟡-1）：`web/src/views/artist/QueueBoard.vue` 现有 `.catch(err => console.warn(...))` 删除或改静默（拆分后行号变了，搜索 console.warn 定位）

## 二、纪律

- 纯前端修复：不动后端、不动 API 契约、不动 schema
- i18n 键新增必须 zh/en 同步；**禁止新硬编码中文**
- 不顺手改无关文件；每步 `cd web && npm run test:web && npm run lint && npm run build` 全绿
- 改动多，建议**按组分批 commit**（A 组一批、B 组一批、C/D 合并一批），每批门禁

## 三、交付

- 报告 `docs/comms/02-to-01-巡检修复-交付-20260807.md`：每项文件:行号改动、i18n 键清单（zh/en）、测试结果、移动端修复截图（OrderList/TierManage/AdminDashboard 前后对比）
- 不推送、不合并、不改 STATUS；合入由一号执行
