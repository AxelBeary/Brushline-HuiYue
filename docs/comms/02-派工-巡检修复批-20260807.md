# 02-派工-巡检修复批-20260807（重启版）

> 派工：一号 → 二号（client-frontend）
> worktree：`artist-commission-w2`（分支 `beta/fix-batch`，HEAD=159ee76）
> 开工第一步：**`git merge master` 再读本文件**。
> 来源：巡检四路 + 健康巡检汇总的**可修项**。这是 v0.41 收编后第一轮前端修复。

## 一、任务分组（按优先级，全修完再交付）

### 组 A：i18n 红线（用户可见文字必须走 $t()）
1. **ManualOrder.vue 6 处硬编码中文**（B🔴-1）：L986/L1000/L1009/L1016/L1028/L1038（录单后补写失败提示）→ 新增 `manualOrder.postCreateFailed.*` 键，zh/en 同步
2. **main.js 全局错误兜底**（B🔴-5 + D2）：`web/src/main.js` L39-44 `ElMessage.error('页面出了点小问题...')` + 动态 import → i18n 键 `common.globalError` + 静态引入 ElMessage
3. **api 拦截器兜底文案**（D3）：`web/src/api/index.js` L29 `网络错误，请稍后重试` → i18n 键
4. **Settings 强调色名硬编码中文**（D1）：`Settings.vue` L379-383 ACCENT_PRESETS name → nameKey 走 `t('pref.accentNames.*')`（ThemePicker 同款键已存在）
5. **en 补 2 键**（B🔴-6）：`landing.weibo` / `landing.bilibili`
6. **ArtistLayout lang-btn aria 硬编码**（B复测增-1）：3 处 title/aria-label → i18n 键

### 组 B：移动端布局
7. **OrderList 表格列裁剪无滚动**（B🔴-2）：列宽合计 1004px > 375px → 修法②（body-wrapper 允许横向滚动 overflow-x:auto + min-width）
8. **TierManage 移动端 tabs**（B🔴-4）：复测修正=箭头存在，降为信息级——仅确保可滚动即可，不改结构
9. **AdminDashboard 窄屏 header 按钮溢出**（C🟡-1）：header 加 `flex-wrap: wrap; gap: 8px`

### 组 C：全局/细节
10. **ArtistDetailDrawer 状态缺 hidden**（C🟡-2）：radio-group 补 hidden 选项（先确认 i18n 键）
11. **后台 404 视觉断裂**（D4）：/dashboard、/admin 前缀未知路径重定向回各自首页（客户端保持 404 页）
12. **admin 顶栏标题空**（D5）：pageTitle 对 /admin* 回退 `t('menu.admin')`
13. **TrackOrder 支付进度条硬编码色**（D6）：用 CSS 变量

### 组 D：顺手小修（健康巡检）
14. **formatCents 抽 utils**（Y1）：7 处重复 → `web/src/utils/money.js` 统一引用
15. **OrderDetail 死解构清理**（Y5）：currentStageIdx/nextStage/daysLeft
16. **GalleryPanel dragleave 防闪烁**（R4）：加 relatedTarget 检查
17. **QueueBoard console.warn 清理**（B🟡-1）

## 二、纪律
- 纯前端；i18n 键 zh/en 同步；禁新硬编码中文
- 按组分批 commit（A/B/C+D），每批 `cd web && npm run test:web && npm run lint && npm run build` 全绿
- 移动端修复附截图（before/after）

## 三、交付
- 报告 `docs/comms/02-to-01-巡检修复批-交付-20260807.md`
- 不推送、不合并、不改 STATUS；合入由一号执行
