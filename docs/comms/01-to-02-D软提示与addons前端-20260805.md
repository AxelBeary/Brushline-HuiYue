# 01→02：D 软提示 + addons 清理第二批（前端部分）

> 日期：2026-08-05 · 派工：一号 · 执行：二号
> worktree：`../artist-commission-d2` · 分支：`softhint-addonsfe`（基于 master `43395ff`）

## 开工

`cd ../artist-commission-d2` → `npm install`（web + server）→ `git merge master`（基线即 master）

## 任务一：D 项软提示（用户拍板 = 软提示）

约稿表单「需求描述」步骤：留空点下一步时弹一次软提示（ElMessageBox confirm 或 ElMessage warning + 二次确认，选你认为体验对的一种），**确认后可继续提交**——不拦截、不加必填校验。后端 schema 不动（description 保持可空，用户拍板的是软提示不是硬校验）。i18n 中英双键。补 1 例组件测试（空描述触发提示、确认后放行）。

## 任务二：addons 旧模型前端清理（与三号后端同批联动）

三号同批删 POST /api/orders 与手动录单 schema 的 addons 字段——前端必须先停传，否则 ajv additionalProperties:false 会 400：
1. `useOrderForm.js`：删旧模型增项逻辑——`availableAddons`（tier.addons 读路径）、`buildSelectedAddons` 的 addons 分支、提交 payload 的 addons 字段（L633-649 区）、画风接口失败回落旧模型时的 addons 处理（L681-693 区）。**注意 composable 解构验证**：删掉的变量逐个确认模板/其他组件无引用（v0.19 事故同款）。
2. `ManualOrder.vue`：旧档位模式增项块删除（L1019-1025 区）。
3. `pricing.service` 公开报价 `tiers[].addons` 后端已恒空数组（第一批合入）——前端 `tier?.addons || []` 兜底可保留（无害），旧增项 UI 渲染分支删除。
4. grep 收尾：`addons`（旧模型语义的）在 web/src 残留清点，交付报告列清单。

**风格模型增项（style_addons，v0.36 新体系）一个字不动**——只清 price_addons 旧链路。

## 合入顺序

你批先合（前端停传 addons），三号批后合（后端删字段）——交付后注明，一号按序合。

## 铁律

1. server 零改动（schema 删除归三号）；v0.38 视觉不破；不加 eslint-disable
2. 基线 web 224 全绿 + 本批新增；lint 0；build 过（web/ 目录）
3. 交付：commit + `docs/comms/02-to-01-D软提示与addons前端清理-交付-20260805.md`。不推送不合并。

—— 一号
