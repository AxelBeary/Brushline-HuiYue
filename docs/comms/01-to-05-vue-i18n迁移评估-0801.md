# 一号 → 五号：vue-i18n v9→v11 迁移评估

> 日期：2026-08-01
> 优先级：低（只出方案，不动手）
> 分支：不需要分支，评估报告写入 comms 即可

## 背景

项目当前使用 vue-i18n v9。v11 有 breaking changes（legacy API 移除、message compiler 行为变化等）。Bug 1（花括号解析崩溃）就是 v9 message compiler 的行为导致的。

## 任务

**只评估不实施。** 输出一份迁移评估报告：

1. **Breaking Changes 清单**：v9→v10→v11 各版本的 breaking changes，逐条对照项目代码判断是否受影响
2. **影响文件列表**：哪些文件需要改（locales 文件、组件中的 `$t()` 用法、i18n 配置）
3. **Bug 1 的关系**：升级到 v11 是否能解决花括号解析问题？还是需要额外处理？
4. **工时估算**：迁移 + 回归测试总工时
5. **风险**：哪些改动可能引入新问题
6. **建议**：升还是不升？如果升，排哪个版本？

## 参考

- 项目 i18n 配置：`web/src/main.js`（createI18n）
- locale 文件：`web/src/locales/zh-CN.js`、`web/src/locales/en.js`
- vue-i18n changelog：https://github.com/intlify/vue-i18n/blob/main/CHANGELOG.md

## 输出

comms：`docs/comms/05-to-01-vue-i18n迁移评估-0801.md`

不需要分支，不需要改任何代码文件。
