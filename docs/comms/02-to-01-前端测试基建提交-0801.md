# 二号 → 一号：前端测试基建提交（技术债 P1）
> 日期：2026-08-01
> 分支：`feat/frontend-test-infra`（commit 83f0394）
> worktree：`artist-commission-test-infra`

## 做了什么

1. 安装 vitest + @vue/test-utils + happy-dom（devDependencies）
2. `web/vitest.config.js`：happy-dom 环境，include `src/**/__tests__/**/*.test.js`，与后端 `server/vitest.config.js` 完全隔离
3. `web/src/test-setup.js`：全局 setup 占位（后续 mock EP/i18n 在此添加）
4. `web/src/utils/dashboard-normalize.js`：从 3 个仪表盘 SFC 提取纯函数（normalizeRevenue / normalizeTodo / normalizeActivity / tagKey / guessTag / relativeTime / prevPeriodLabel）
5. 3 个组件（RevenueChart / TodoList / ActivityFeed）改为 import 提取的纯函数，行为不变
6. 17 个示范测试（`__tests__/dashboard-normalize.test.js`）
7. `package.json` 加 `test:web` script

## 授权外文件说明

| 文件 | 原因 |
|------|------|
| `web/src/utils/dashboard-normalize.js`（新建） | normalize 函数嵌在 SFC 内部无法独立 import 测试，提取是可测试性的必要前提 |
| 3 个 `.vue` 组件（修改） | 改为 import 提取的纯函数，纯重构无行为变化 |

## 改动文件

| 文件 | 改动 |
|------|------|
| `web/package.json` | +test:web script，+3 devDeps |
| `web/package-lock.json` | 锁文件更新 |
| `web/vitest.config.js` | 新建 |
| `web/src/test-setup.js` | 新建 |
| `web/src/utils/dashboard-normalize.js` | 新建（7 个纯函数） |
| `web/src/components/artist/dashboard/__tests__/dashboard-normalize.test.js` | 新建（17 测试） |
| `web/src/components/artist/dashboard/RevenueChart.vue` | 改 import |
| `web/src/components/artist/dashboard/TodoList.vue` | 改 import |
| `web/src/components/artist/dashboard/ActivityFeed.vue` | 改 import |

## 验证结果

- `npm run test:web`：17/17 通过 ✅
- `npx eslint . --max-warnings 0`：零错误零警告 ✅
- `npm run build`：通过 ✅
