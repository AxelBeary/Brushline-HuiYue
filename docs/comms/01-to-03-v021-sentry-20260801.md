# 派工：v0.21 Sentry 错误监控

> 一号 → 三号（后端）+ 二号（前端） | 2026-08-01
> 分支：`feat/v021-sentry`（三号 worktree，二号在 wt-02 做前端部分）
> 规格：`docs/specs/plan-v021-engineering.md` §四

## 分工

| 谁 | 做什么 |
|----|--------|
| 三号 | 后端 Sentry Node SDK 接入（app.js 初始化 + 错误处理器集成）+ 环境变量开关 + .env.example 补 DSN 占位 |
| 二号 | 前端 Sentry Vue SDK 接入（main.js 初始化）+ 环境变量开关（VITE_SENTRY_DSN） |

## 关键约束

- DSN 留空/不设 = 完全禁用，零网络请求（S-AC3）
- NODE_ENV=development 时不上报（S-AC5）
- sendDefaultPii: false（S-AC6）
- release 从 package.json version 读取（S7）
- 不改任何业务逻辑，纯附加层
- 后端测试 469/469 + 前端测试 87/87 必须通过
- 告警方式：sentry.io 面板配置邮件 Alert Rule（代码侧无需处理，用户注册后在面板配）

## 授权文件

- 三号：`server/src/app.js`、`server/package.json`、`.env.example`
- 二号：`web/src/main.js`、`web/package.json`

## 注意

- 用户还没注册 sentry.io，DSN 暂空。代码写好开关逻辑，注册后填入即可
- 前端 DSN 用 `import.meta.env.VITE_SENTRY_DSN`（Vite 环境变量前缀）
- 后端 DSN 用 `process.env.SENTRY_DSN_BACKEND`
