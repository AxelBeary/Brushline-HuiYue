# 二号 → 一号：v0.38 视觉第一批（骨架 + 核心四页）交付报告

> 日期：2026-08-05 · 分支：`v038-visual` · worktree：`artist-commission-v038`
> 交付范围：派工 `01-to-02-v038视觉重设计第一批-20260805.md` 全部任务（第四~五轮接力完成）

## 一、任务完成情况（逐条对派工）

| 派工项 | 状态 | commit |
|--------|------|--------|
| token 体系 artist-tokens.css（双主题 scoped html[data-artist-theme]） | ✅（前三轮） | `158250f` |
| 骨架 ArtistLayout + 主题切换 | ✅（前三轮） | `158250f` |
| Dashboard / QueueBoard 换肤 | ✅（前三轮） | `158250f` |
| **OrderDetail 换肤 + 日期卡二合一（REQ-026 §四）** | ✅ | `240b386` |
| **ManualOrder 换肤（只换视觉，模板/类名零改动）** | ✅ | `efb8c17` |
| **Preferences 套 token** | ✅ | `efb8c17` |
| locales 补键 + 死键 sendCode/codeSent 清理 | ✅ | `158250f` + `240b386` |
| 全量验证 + 浏览器截图实测 | ✅ | 本报告 |

### OrderDetail 日期卡二合一要点（派工最难点）

- **日期保存逻辑一行未动**：deadlinePicker/startDatePicker 两个 ref + watcher + changeDeadline/changeStartDate 即时保存 + 错误回弹 + disableDeadlineDate/disableStartDateDate 校验全部原样保留，只换了模板位置与 toast 键
- 开工日/截稿日从 el-descriptions-item 移入独立「日期」el-card（键 `orderDetail.dateCardTitle`），卡内两个独立 date-picker（数据模型不变，REQ §四.1）
- toast 换 `deadlineSavedSync`/`startDateSavedSync`（"…已保存，排期已同步"）
- 新增 `daysLeft` computed（截稿日−今天，本地时区按日）+ `deadlineChip`：剩 N 天=花青(doing) / 今天截稿=藤黄(pend) / 逾期 N 天=朱砂(over)，挂卡头右侧——7 色语义一对一
- 全部 9 张卡统一 CardHead 朱砂 mark 卡头；4 处 el-empty → InkEmpty 墨线空态；进度条硬编码色 → `var(--sl)/var(--hq)`
- 样式区全 token 化，`var(--el-*)/var(--text-*)/var(--bg-*)` 旧变量残留 **0**

### ManualOrder / Preferences

- ManualOrder：**纯样式区**换 token（diff 起始于 `</script>` 之后），模板与类名 `.mo-field/.tier-card/.next-due-banner` 等零改动保测试断言；H1 文楷 28/700、分节标题朱砂 mark、价格/总价文楷墨色不上色（统计数字铁律）
- Preferences：套 token + CardHead；新增键 `settings.notifyPanelTitle`（中英）

## 二、验证结果（实跑数据）

| 项 | 结果 |
|----|------|
| web vitest（合并态，含 OrderForm.summary 6 例） | **192/192 全绿**（12 文件，含 OrderDetail.duebanner 7 例、ManualOrder.stylemode 13 例） |
| eslint（web 全量 `npx eslint .`） | **0 错 0 警**（exit 0） |
| server tsc --noEmit | 0 错（本轮未触碰 server，基线复跑留证） |
| 旧变量残留 grep（三页） | 0 |
| locales 死键 sendCode/codeSent | 0（已清） |
| 合并 master | `be9f5f6`（origin/master 至 92ad45e，无冲突，合并态重跑全绿） |

## 三、浏览器实测（16 张截图）

**环境**：worktree 隔离实例 PORT=3100 + WEB_DIST 直出（生产容器占 3000，未动）；测试库 seed + TOTP 密钥注入走真实 `/api/auth/verify` 登录链路（无开发后门）；造单 ALICE-901（截稿+5天）/ ALICE-902（逾期3天）。

截图目录：`docs/audit-screenshots/v038/`

| 文件 | 内容 | 验收对应 |
|------|------|----------|
| 01-dashboard-paper | 宣纸·仪表盘 | 验收 1/4 |
| 02-queue-paper | 宣纸·排期看板 | 验收 1 |
| 03-orderdetail-datecard-paper | 宣纸·订单详情日期卡（chip「剩 5 天」花青） | 验收 6 |
| 04-orderdetail-overdue-paper | 宣纸·逾期单（chip「逾期 3 天」朱砂，其余界面安静） | 验收 3 |
| 05-manualorder-paper | 宣纸·手动录单（H1 文楷 + 分节朱砂 mark） | 验收 1/5 |
| 06-preferences-paper | 宣纸·偏好页 | 验收 1 |
| 07-orderdetail-datesave-toast | 日期卡改期实拍：2026-08-10 → 2026-08-12，toast「截稿日已保存，排期已同步」，chip「剩 5 天」→「剩 7 天」实时更新 | 验收 6 |
| 08~11 | 墨黑·仪表盘/排期/逾期单详情/手动录单 | 验收 2（两主题 7 色语义一致） |
| 12-refresh-kept-ink | 刷新后 `data-artist-theme` 仍为 ink | 验收 2（持久化） |
| 13-mobile-dashboard-ink | 390×844 窄宽度后台可用 | 验收 9 |
| 14~16 | 客户端 LandingPage / ArtistHome(alice) / OrderForm | 验收 10 |

**脚本断言（非肉眼）**：改期 toast 与 chip 变化经 MutationObserver 实锤；主题切换 toast「已切换 · 墨黑」；**客户端路由下 `data-artist-theme` = null**（token 作用域隔离实锤，客户端零影响）；刷新保持 ink。

**视觉复核（vision 逐图审）**：宣纸/墨黑两主题下日期卡结构、chip 语义、朱砂克制度、布局对齐均通过；手动录单页视觉评分 8~8.5/10（楷体大标题成立、朱红节标齐备、无错位溢出）。

## 四、测试数据清理（演示数据纪律）

- 造单 ALICE-901/902 在独立测试库 `server/data/test-v038.db`，**已随库删除**；未污染任何共享数据
- 临时脚本 `e2e/temp-v038-prep.mjs`、`temp-v038-shots.mjs`、token 缓存已删；测试 uploads 目录已删；测试服务器已停
- 交付时 `git status` 干净（本报告 commit 后）

## 五、遗留 / 需一号知悉

1. **OrderDetail 日期卡改期实测改动了 ALICE-901 截稿日**（测试库内，已随库删除，无残留）
2. locales 死键清理（STATUS 遗留项）已确认清零
3. 视觉复核的小瑕疵（不影响验收，记录备第二批参考）：日期卡右半留白偏大；textarea 原生 resize 角标未主题化——均属吹毛求疵级，未处理
4. 第二批页面（OrderList/SlotManage/TierManage/ArtworkManage/GuestbookManage/Settings/Login + 管理后台）未动，token 兼容映射层（artist-tokens.css 旧变量映射）继续为其兜底

**交付物**：commit `240b386` + `efb8c17`（代码）、`be9f5f6`（merge master）、本报告。不推送不合并，合并权在一号。
