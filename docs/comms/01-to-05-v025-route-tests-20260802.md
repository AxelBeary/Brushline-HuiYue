# 五号 v0.25 追派：路由层集成测试补充

> 分支：`test/v025-route-integration`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-05`
> 日期：2026-08-02
> 来源：五号审计报告 B3 + B1

---

## 任务：补 5 个路由层集成测试（~1h）

你刚做完审计，代码最熟，直接上手。

### B3-1：收款 API 路由层（优先级最高）

文件：`server/tests/quota-pool.test.js`（追加）或新建 `server/tests/payments-routes.test.js`

用 `app.inject()` 验证：
1. POST `/api/artist/orders/:id/payments` — 正常记录 → 201 + paidTotalCents 累加
2. POST — 无 token → 401
3. POST — 非归属订单 → 403
4. GET `/api/artist/orders/:id/payments` — 返回流水列表（含负数记录）

### B3-2：留言筛选路由层

文件：`server/tests/guestbook.test.js`（追加）

1. GET `/api/artist/messages?status=pending` — 只返回 pending
2. GET `/api/artist/messages?status=approved` — 只返回 approved
3. GET `/api/artist/messages`（无 status）— 返回全部

### B3-3：统计卡路由层

文件：`server/tests/dashboard.test.js`（追加）

1. GET `/api/artist/dashboard/stats` — 200 + 结构校验（revenue/orders 字段存在）
2. GET — 无 token → 401

### B3-4：快捷按钮路由层

文件：`server/tests/monthly-quota.test.js`（追加）

1. PUT `/api/artist/profile` body 含 `quickActions: [...]` → 200 + 持久化
2. PUT — `quickActions: "非法JSON"` → 400（如有校验）或 200（如无校验则记录现状）
3. GET `/api/artist/profile` → 返回 quickActions 字段

### B1：send-code 防枚举路由层

文件：`server/tests/routes.test.js`（追加）

1. POST `/api/auth/send-code` body 含不存在的 QQ → 200（与存在 QQ 行为一致，不泄露是否存在）

---

## 约束

- 只加测试，不改业务代码
- 用已有 `buildApp()` + `app.inject()` 模式（对照 routes.test.js 现有写法）
- 每个 it 命名：`TC-ROUTE-XX: 描述`
- 发现路由层真有 bug（如缺鉴权）→ 记录在交付 comms 里，不自己修

## 授权文件

- `server/tests/`（新增或追加测试文件）
- `server/tests/setup.js`（如需新 seed 辅助函数）

## 验证

`npx vitest run` 全绿（512 + 新增用例）

## 交付

comms：`05-to-01-v025-route-tests-{日期}.md`
