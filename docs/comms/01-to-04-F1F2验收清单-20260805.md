# 一号 → 四号：REQ-022 F1/F2 验收清单撰写（文档批）

> 日期：2026-08-05 · 分支：`f1f2-acceptance-docs` · worktree：`../artist-commission-acc`（已基于 master 建好）· 风险：低
> **授权范围：仅 `docs/`**，不碰代码。你在 REQ-026 批的验收清单（10 条逐项操作 + 7 色核对表）质量很好，本批照那个标准来。

## 背景与拍板依据

用户已拍板（STATUS §规则 5/6/7，写清单时按此为准，别再列待确认）：
- F1：发布门槛 = `delivered`；一图一作品；「立即约稿」预选仅展品点入带（`?sizeId=`）
- F2：工时 3.5 天接受；防投毒三规则（域名末尾匹配/裸链补 https/长度 253-1500-1800）
- F5：未读语义选 c 不做（已上线三维筛选）——F5 不需要清单，跳过

你的拆解 `docs/plan-req022-实施拆解.md` 第八节 8 个待确认项已全部有解，逐条对照上述拍板核销。

## 任务：两份验收清单（合并写一个文件）

`docs/REQ022-F1F2验收清单.md`，结构对齐你 REQ-026 那份：

### 第一份：F1 作品链路（约 10 条）
- 每条格式：**操作步骤 → 预期结果 → 实测记录位（留空）**
- 覆盖：delivered 订单出现「发布为作品」入口 / 非 delivered 无入口 / 勾选 N 张 = N 条作品 / 发布后客户交付页原图仍可下载 / 作品管理页可见新作品 / 绑定档位（多选替换）/ 客户端 4 模板按档位分组展示 / 展品点入「立即约稿」带预选、其他路径不带 / 未绑档位作品的展示兜底
- ⚠️ R-1b：4 模板必须逐一点名，不许写"4 个模板同理"一条带过

### 第二份：F2 外链重做（约 12 条）
- 覆盖：设置页粘贴识别即时反馈 / 裸链保存后补 https / `weibo.com.evil.com`、`xweibo.com` 保存后归「其他」（打开 DB 或页面验证）/ `javascript:` 拒绝 / 超长拒绝 / 第 9 条拒绝 / 平台停用后下拉不出现但已存链接保留 / 删除平台后链接归「其他」不消失 / 客户端 4 模板页脚图标+名称新窗口打开 / 管理端平台 CRUD / i18n 中英
- 附「防投毒向量速查表」：把你拆解 2.5 的向量表搬进来，每条标验收条目编号

## 验证与交付

- 写清单时读 master 代码核实按钮/文案实际存在性（别凭拆解臆写入口名），拿不准的标「以交付时界面为准」
- 交付报告 `docs/comms/04-to-01-F1F2验收清单-交付-20260805.md`；commit 单行 `docs: …`；**不推送不合并**。清单将随二号 F1/F2 前端批交付时用于逐条验收。
