# 内测反馈核实记录（2026-08-06）

> 来源：内测用户 6 条反馈（用户转达，提示"可能已过时或修复"）
> 核实人：一号 · 对照 master `49c7824` 代码逐条查证

---

## 逐条核实结论

| # | 反馈 | 核实结论 | 分类 |
|---|------|----------|------|
| F1 | 后台需要字体调节（老年人看不清小字） | **真实缺失**——后台无字号调节功能（grep fontSize/字号 零命中）。画师后台字体系统一 12-14px 小字，无放大入口 | 新功能（P1，无障碍） |
| F2 | 淡色对比度太低，按钮看不清；"那个条的颜色也没了" | **真实存在**——`--ink3 #8D887A` 3.21-3.54:1、`--ink4 #B3AE9F` 2.22:1 不达 AA（与第三方报告 A3 一致）；按钮白字/辅助文字对比不足。「条的颜色没了」待实测确认指向（优先级色条？进度条？），需浏览器复核 | P1 对比度修复 + 待实测 |
| F3 | 导航栏右侧"快捷入口"与左侧导航高度重复 | **真实存在**——`QuickActions.vue` 默认 6 项（queue/manual/orders/artworks/tiers/settings）与侧边栏 `menu.*` 完全同名同路由（注释自己承认"命名与侧边栏完全一致"）。功能可自定义但默认高度重复 | 产品决策（需用户拍板：改造 or 移除） |
| F4 | 画师后台参考图上传区大小很诡异 | **待复核**——`ManualOrder.vue` L19-43 参考图上传区 + `.mo-ref-upload :deep(.el-upload--picture-card)` 样式 L1221。需浏览器实测确认"诡异"指什么（过大/过小/比例异常） | 待实测复核 |
| F5a | "高、中(默认)、低"按钮组 | **用户澄清（2026-08-06）**：非功能问题，是**观感吐槽**——"那人就看了一眼，嫌按钮不好看就不想看网页了"。连带吐槽：左下角画师头像就一个「画」字（`ArtistLayout.vue:334` `avatarChar = artistName[0]` 首字兜底，未传头像的画师显示首字母）、各种按钮控件丑、动画一点没有、左上角理应是 logo | **观感批（P1 第一印象）** |
| F5b | 换页面闪烁（尤其暗色）+ 建议 keyframes 加载动画 + prefers-reduced-motion | **真实存在**——暗色换页白屏/闪烁常见于字体/背景加载时序；当前加载用 EP `v-loading` 默认转圈。全站路由过渡无统一处理 | P2 优化（工程量大，涉及多页） |
| F6 | 后台录单没有暂时保存 | **真实缺失**——`ManualOrder.vue` grep draft/草稿/暂存/autosave 零命中。录单表单无草稿/暂存功能，误关页面数据丢失 | 新功能（P1，体验） |

---

## 处置建议

### 批 4 候选（按优先级）
1. **F2 对比度修复**（P1）：纸墨盘 ink3/ink4 调深 + 按钮对比度——与第三方报告 A3 同源，可与"藤黄调深"（批 3 五号在做）合并为纸墨盘对比度批
2. **F5a 后台观感批（P1 第一印象，用户澄清为观感吐槽）**：
   - 未传头像画师的头像兜底「画」字太丑——`ArtistLayout.vue:334` 首字兜底。可改为：默认用品牌印章/墨水瓶 SVG 占位（而非首字母），或引导画师上传头像（空态引导）
   - 左上角品牌区观感（logo 实际存在 `ArtistLayout.vue:9-15` 朱砂印章「绘」+ 绘约 + BRUSHLINE，但内测用户没认出/折叠态下只剩印章）——复核折叠态品牌区是否可辨识
   - 按钮控件观感 + 动画缺失——对照纸墨盘设计书核对 EP 默认控件覆盖度，补 hover/active/loading 动效（150ms 纪律），优先侧边栏/顶栏/主操作按钮
3. **F6 录单暂存**（P1）：ManualOrder 草稿保存（localStorage 自动暂存 + 恢复提示）——需用户确认交互
4. **F1 字号调节**（P1 无障碍）：后台加字号档位（如 标准/大/特大），画师偏好保存
5. **F5b 路由闪烁 + 加载动画**（P2）：统一路由过渡 + keyframes 加载 + prefers-reduced-motion——工程量大，需排期
6. **F3 快捷入口**：**需用户拍板**（改造为真正有用的/移除/保留可自定义）
7. **F4 上传区**：**需实测复核**后定（截图让用户口述）

### 待用户澄清（更新后）
- F3 快捷入口处置方向（改造/移除/保留）
- F4 上传区"诡异"的具体表现
- ~~F5a~~ ✅ 已澄清（观感批，见上）

### 与在途批关系
- 批 3 四路并行中（w6-w9），本批不追加派工，避免撞文件
- F2 与批 3 五号（藤黄调深）同域但不同文件（artist-tokens.css 同文件！）——**等批 3 五号交付后并入批 4**，避免同文件并行冲突
