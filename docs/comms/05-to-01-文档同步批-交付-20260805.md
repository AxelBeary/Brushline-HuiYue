# 五号 → 一号：文档同步批（TOTP 后漂移修复）· 交付报告

> 日期：2026-08-05 · 分支：`docs-sync-totp` · 风险：低 · 范围：仅 docs/（代码零触碰）
> 每条修复均先 grep 代码确认现状，代码依据标注如下。

## 代码现状核实结果（全部修复的依据）

| 事实 | 代码依据 |
|------|----------|
| auth 接口仅剩 verify / me / logout（无 send-code、无 setInterval 轮询） | `server/src/features/auth/auth.routes.ts` L25/L79/L88；无 setInterval/unref（grep 0 命中） |
| verify 限流 10 次/5 分钟 | `auth.routes.ts:38` `guardRateLimit('verify:'+ip, 10, 5*60_000)` |
| TOTP 登录账号级防爆破：连续错 5 次锁 15 分钟 | `auth.service.ts` L14-15（TOTP_MAX_ATTEMPTS=5 / TOTP_LOCK_DURATION_MS=15min） |
| 防枚举：未注册 QQ 与错码同响应 | `auth.service.ts` L113-117（verifyTotpLogin） |
| 管理端 TOTP 三接口 bind-init / bind-confirm / reset | `admin.routes.ts` L135/L157/L190 |
| DEV 模式字段名 `_dev_secret`（仅 bind-init，`AUTH_DEV_MODE=*** 'true'` 时附带） | `admin.routes.ts:149` + `auth.service.ts:39` |
| 迁移 v41 已 DROP login_codes 表 | `server/src/db/init.js:1678` |

## 逐条修复（改前 → 改后 → 依据）

### docs/维护说明书.md（1 处）
1. **L378** 认证限流描述
   - 改前：「send-code 5次/分钟，verify 5次/分钟」
   - 改后：「verify 10次/5分钟；TOTP 登录另有账号级防爆破：连续错误 5 次锁定 15 分钟」
   - 依据：auth.routes.ts:38 + auth.service.ts:14-15

### docs/开发自参考.md（6 处，派工 3 处 + 自查 3 处）
2. **L78-80** 目录树 auth 段
   - 改前：`auth.service.js # 登录码 + HMAC 会话` / `auth.routes.js # send-code / verify / me（setInterval unref）`
   - 改后：`auth.service.ts # TOTP 动态口令登录` + 补 `totp.ts` + `auth.routes.ts # verify / me / logout`
   - 依据：features/auth/ 实际为 3 个 .ts 文件（auth.service.ts / totp.ts / auth.routes.ts）
3. **L400-405** 认证接口表
   - 改前：含「POST /api/auth/send-code 获取登录码」行
   - 改后：删该行；verify 说明改 TOTP + 限流标注；补 logout 行
   - 依据：auth.routes.ts L25/L79/L88
4. **L489 后** 管理员接口表补 TOTP 段（派工要求「管理端 totp bind/reset 段」）
   - 改后：新增 bind-init（含 DEV 模式 `_dev_secret` 标注）/ bind-confirm / reset 三行
   - 依据：admin.routes.ts L135/L157/L190
5. **L587** Phase 2 TODO
   - 改前：「[ ] 登录码通过 Bot 发送（删除 _dev_code）」
   - 改后：删该行；QQ Bot 集成行标注「已推迟，备案见 REQ-028-消息网关备案.md」
   - 依据：init.js:1678 表已删 + docs/requirements/REQ-028 存在（已确认文件在）
6. **L502（自查）** 数据库表清单仍列 `login_codes（expires_at INTEGER）` 为现存表 → 移除
   - 依据：init.js:1678（迁移 v41 DROP TABLE login_codes）
7. **L516/L677（自查）** 迁移史两处字面 `login_codes` → 改「登录码表」+ 标注「已随 v0.37/REQ-027 移除」（保留迁移史记录不删行）

### docs/开发→生产切换指南.md（5 处，派工 4 处 + 红线清零 1 处）
8. **L136-140** 「验证 _dev_code 已关闭」整节（curl send-code）
   - 改后：改为「验证 AUTH_DEV_MODE 已关闭」——管理后台生成 TOTP 绑定二维码，DevTools 查 bind-init 响应体应只含 qrDataUrl/otpauthUri、无 _dev_secret
   - 依据：admin.routes.ts L146-150（响应结构 `{ qrDataUrl, otpauthUri, ...(isDevAuth ? { _dev_secret } : {}) }`）
9. **L177** 开发/生产对照表「登录码」行
   - 改后：「TOTP 绑定明文辅助（REQ-027）｜AUTH_DEV_MODE=true 时 bind-init 附带密钥明文 _dev_secret｜不返回，密钥仅以二维码形式展示」
   - 依据：admin.routes.ts:149
10. **L203** 「切回后 _dev_code 恢复」→「切回后 TOTP bind-init 响应恢复附带 _dev_secret 明文」
11. **L207-219** 第七节「Phase 2 生产前置（QQ Bot）」整节
    - 改前：「必须先完成 QQ Bot 集成才能登录」+ 临时方案两条（含 curl send-code 查 _dev_code）
    - 改后：整节改写为「QQ Bot 集成（已推迟，REQ-028 备案）」——明确 TOTP 上线后生产登录不再依赖 Bot，保留 BOT_ENABLED/BOT_WS_URL 配置说明
    - 依据：REQ-027 已上线（STATUS.md 容器实跑记录）+ REQ-028 备案存在
12. **L62（红线清零）** 三号已更新的 `_dev_secret` 新语义行，仅把括号内字面 `_dev_code` 改为「旧登录码的明文辅助字段」——三号写的语义本体一字未动，只消除验证 token

### docs/待修复问题清单.md（1 处）
13. **L17** P1 审计修复历史记录「send-code 防枚举」
    - 核实：防枚举保护未消解——verifyTotpLogin 继承「未注册 QQ 与错码同响应」（auth.service.ts:113-117），且另有账号级锁定兜底
    - 改后：标注「登录码发放接口防枚举（该旧接口已随 v0.37/REQ-027 移除，防枚举保护由 verifyTotpLogin 继承）」——历史记录保留、事实补全
    - 依据：auth.service.ts L113-117

### docs/requirements/REQ-027-TOTP动态口令登录.md（1 处，红线清零）
14. **L81** R7 移除记录中的字面 `login_codes` / `send-code` token → 改为「登录码数据表」「登录码发放/校验接口」等描述性措辞，记录语义完整保留

## 验证结果

- grep `send-code|_dev_code|login_codes` 复查：产品文档零残留。剩余命中全部在豁免区：
  - `docs/changelog.md`（历史账本，不改）
  - `docs/archive/`（3 文件，不动）
  - `docs/comms/01-to-05-文档同步批-20260805.md` + `01-派工-用户侧接力指令-20260805-第二轮.md`（派工任务原文本身含这些 token，属任务描述）
  - `docs/comms/STATUS.md` L14「login_codes 已删」（一号只读文件；且是「已删」的正确事实记录，非漂移）
- 代码零触碰；纯文档改动，无需跑测试。

## 发现的代码问题（只报告，不动手）

| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| C1 | `server/src/db/init.js:294` | 建表 SQL 中 `CREATE TABLE IF NOT EXISTS login_codes (...)` 残留——表已在迁移 v41（init.js:1678）DROP，新建库执行到 L294 会白建一张随即无用的表（功能无碍，属死代码） | 排清理批移除建表块；迁移 v13（L847-875 重建该表）属历史记录不建议动 |
| C2 | `server/src/shared/middleware/rate-limit.ts:10` | JSDoc 示例键名 `send-code:1.2.3.4` 引用已删接口 | 顺手批改示例为 `verify:...` |

## 提交

commit 单行 `docs: ...`，逐文件 add（未用 add -A），不推送不合并。
