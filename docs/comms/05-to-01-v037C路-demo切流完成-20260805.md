# 五号交付：REQ-025 二阶段 C 路——demo-data 切流完成

> 五号 → 一号 · 2026-08-05 · 分支 `v037-phase2-demo` · 请一号审核合入（五号不自行 merge）

## 做了什么

demo-data.ts 演示订单直插后补齐新模型四要素，全量守恒闭合（保留直插方式——createOrder 无法表达演示剧本的 status/stage/paidRatio/daysAgo/deadline 且 orderNo 需精确控制）：

1. **base 条目**：每单 INSERT order_price_entries `type='base'` delta=总价（与 createOrder 的 appendPriceEntry('base') 同构；FK CASCADE 保证幂等）
2. **分期走引擎**：generateInstallmentsForOrder（已是引擎 allocateInitial 实现，复用不变）
3. **locked 推导**：refreshInstallmentLocks 按剧本已收金额+当前阶段落 locked/locked_reason（付清=paidOff / 完成=completed）
4. **守恒闭合**：checkOrderConservation 每单调用，不守恒即抛错中止
5. **清理范围扩展**：删除逻辑从固定 demoNos 改为 `LIKE 'ALICE-%'`——08-04 用户终验残留的 ALICE-005~014（testa/testtas/H1实测/横幅实测临时单，直插自旧流程无条目账本）随重建清理，属派工预期收益
6. **完整性断言扩展**：正式区有报价订单须有 base 条目；Σ条目delta = final_price_cents；Σ节点价 = final_price_cents（无额外项订单）；locked=1 必有 locked_reason

## 修改文件

| 文件路径 | 修改类型 | 改动行数 |
|----------|----------|----------|
| server/scripts/demo-data.ts | 修改 | +42 / -7 |

## 验证结果

- [x] 容器内重跑成功（`docker exec -w /app/server commission-web npx tsx scripts/demo-data.ts`），完整性断言通过（订单 4 条，作品 16 张）
- [x] 幂等：连续两次重跑结果一致
- [x] server 测试全绿：46 文件 / 786 tests passed（本地 vitest，typecheck 通过）
- [x] 容器 DB 守恒核查全量零报错（见下）

```
=== 演示订单守恒核查 ===
ALICE-001 confirmed final=28000 已收=14000 base条目=1 Σ条目=28000 Σ节点=28000 增项=0 [OK/OK]
   节点: 排期确认:8400分/锁(paidOff) | 线稿确认:9240分/未锁 | 交付:10360分/未锁
ALICE-002 wip final=12000 已收=6000 base条目=1 Σ条目=12000 Σ节点=12000 增项=0 [OK/OK]
   节点: 排期确认:3600分/锁(completed) | 线稿确认:3960分/锁(completed) | 交付:4440分/未锁
ALICE-003 wip final=8000 已收=4000 base条目=1 Σ条目=8000 Σ节点=8000 增项=0 [OK/OK]
   节点: 排期确认:2400分/锁(completed) | 线稿确认:2640分/未锁 | 交付:2960分/未锁
ALICE-004 done final=5000 已收=5000 base条目=1 Σ条目=5000 Σ节点=5000 增项=0 [OK/OK]
   节点: 排期确认:1500分/锁(completed) | 线稿确认:1650分/锁(completed) | 交付:1850分/锁(completed)
=== 全量守恒通过 ===
```

## 环境操作说明（非 git 变更，已在容器生效）

- **备份**：重建前手动备份 `commission.db.bak-v037-phase2-pre-demo`（容器 /app/data/）
- **恢复文件**：alice-p03.jpg 08-04 被回收站回收导致 backfill 失败，已从 uploads/.recycle-bin 恢复到 uploads/images/2158/（宿主侧，bind mount 即时生效）
- 用户终验残留（teasttt/testtta 增项单 ALICE-005~014）已随重建清理

## 备注

- 容器内 `npx vitest run` 不可用（生产镜像无 devDependencies），测试在本地 worktree 全绿，非环境回归
- 未碰 order.service.ts / pricing-engine.ts / init.js / 前端，setup.js 无需改动（demo-data 不被测试 import）
