# 02-派工-巡检A-客户端页面-20260807

> 派工：一号 → 二号A（client-frontend，全页面巡检第 A 路）
> 工作方式：**只读巡检 + 浏览器实测**，零代码改动、不建 worktree、不 git 写操作。输出报告落主仓 `docs/comms/`。
> 目的：用户要求「全页面巡检，找布局/美观/操作逻辑的问题」。本路负责**客户端页面**。

## 一、巡检范围（本路专属）

客户端（访客可见）全部页面：
1. **Landing 落地页**（web/src/views/client/ 下 Landing 相关）
2. **画师主页 4 模板**：T1 classic / T2 gallery / T3 folio / T4 atelier（views/client/ 下 Tpl* 组件 + 模板容器）
3. **约稿流程 OrderForm**（views/client/OrderForm.vue 及其子组件）
4. **查单 TrackOrder**（views/client/TrackOrder.vue）
5. **客户端共享组件**（components/client/ 下）

## 二、检查点（逐页过）

1. **布局**：错位/重叠/溢出/空白异常/窄屏（375px 移动端）/宽屏（1440px）两端都看；栅格对齐
2. **美观**：与已拍板设计系统对照（theme.css 五色 natural-v2、纸墨 token；4 模板各自视觉）；色板一致性；间距/字号/圆角统一；视觉层级
3. **操作逻辑**：按钮可点性/禁用态/加载态/空态/错误态；表单校验反馈；跳转链路完整；返回路径；无效操作拦截
4. **响应式**：断点行为（移动端导航/CTA/表格）
5. **i18n**：中英键完整性

## 三、方法

1. **静态分析**（先做）：读代码找结构问题（模板结构/样式/绑定）
2. **浏览器实测**（后做，加分项）：起本地 vite dev **端口 5174**（`cd web && $env:PORT=5174; npm run dev`），用浏览器访问各页面，**截图留证**
   - 登录画师用 API 直接构造或 AUTH_DEV_MODE 开发态（如可）
   - 每发现一个问题：截图 + 文件:行号 + 描述
3. 产出报告，不修代码

## 四、输出

- 报告写 `docs/comms/02-to-01-巡检A-客户端-交付-20260807.md`：
  - 🔴 真实问题（页面 + 截图路径 + 文件:行号 + 描述 + 建议修法）
  - 🟡 改进建议（不紧急）
  - ✅ 健康项（确认没问题的页面，给用户安心）
  - 严重度标注：高（用户可见的错位/坏交互）/ 中 / 低
- 截图存 `workspace/temp/inspect-a/`（若实测）

## 五、纪律

- 只读：不 git add/commit/push/merge、不碰 master
- **不夸大不编造**：截图为准，标注不清的宁可略过；判断以用户拍板过的设计规范为准（外部提案=视觉参考，不认）
- 20 分钟无产出停下报卡点
