# 三号 → 一号：v0.20 画师端+后端批次完成

> 分支：`feat/v020-artist`（3 commits）

## 变更清单

### 1. S5 额度池（feat）

**改动文件：**
- `server/src/db/init.js` — schema 字符串加 `monthly_quota`；迁移 v23（幂等 + 备份）
- `server/src/features/artist/artist.service.js` — 新增 `getMonthlyUsage()`；`computeSlotDisplay()` 整合额度逻辑；`updateArtist` 白名单加 `monthly_quota`
- `server/src/features/artist/artist.routes.js` — 公开 API 响应加 `monthlyQuota` + `quotaInfo`；PUT Schema 加 `monthlyQuota`；keyMap 加映射
- `server/tests/monthly-quota.test.js` — 15 个测试（TC-S5-01 ~ TC-S5-15）
- `web/src/views/artist/Settings.vue` — 额度编辑 UI（开关 + 数字输入）
- `web/src/locales/zh-CN.js` / `en.js` — 4 个 i18n 键

**接口变更：**
- `GET /api/artists/:subdomain` 新增字段 `monthlyQuota`（integer|null）、`quotaInfo`（{used, quota, remaining}|null）——纯新增，向后兼容
- `PUT /api/artist/profile` 新增可选字段 `monthlyQuota`（integer|null, 0~999）

**数据库：** 迁移 v23，`artists` 表加 `monthly_quota INTEGER DEFAULT NULL`。幂等（PRAGMA 检查列存在）。回滚：删列即可。

**设计要点：**
- 额度按 UTC 月计算（对齐 SQLite CURRENT_TIMESTAMP）
- 已取消订单不计入额度
- 额度耗尽优先于名额显示（"本月已约满"）
- 仅额度无名额 → "开放中 · 本月剩 X 单"
- 二号客户端只需读 `quotaInfo` / `slotDisplay`，无需额外 API

### 2. QueueBoard 静默失败修复（fix）

- `web/src/views/artist/QueueBoard.vue` L482：`.catch(() => {})` → `.catch(err => console.warn(...))`

### 3. PaymentBar 美化（style）

- `web/src/components/artist/PaymentBar.vue`：段色相区分（HSL 变量）、高度 48→64px、手柄 grip 可视化、过渡动画、标尺 5 刻度、圆角修正
- 纯视觉改动，逻辑层（拖拽/键盘/弹性/脱离）未动

## 验证

- 后端测试：469/469 通过（27 文件）
- ESLint：零错误零警告
- 前端构建：通过（vite build 8.25s）

## 给二号的备注

公开 API `GET /api/artists/:subdomain` 新增 `monthlyQuota` 和 `quotaInfo` 字段。`slotDisplay` 已整合额度逻辑，客户端直接展示即可。如需独立展示额度进度条，用 `quotaInfo.used` / `quotaInfo.quota`。
