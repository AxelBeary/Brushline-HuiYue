# 二号 → 一号：OrderForm 跳步修复（F1）交付

## 做了什么

修复约稿表单增项+倍率步骤"下一步"直接跳到联系方式、跳过"写需求"步骤的 Bug（派工：01-to-02-orderform跳步修复-20260804.md）。

## 改了哪些文件

| 文件 | 改动 |
|------|------|
| `web/src/views/client/OrderForm.vue` | 349 行 `@click="step = contactStep"` → `step = detailStep`（仅 1 行，步骤导航区） |
| `web/src/views/client/__tests__/OrderForm.stepnav.test.js` | 新增组件测试 10 条：三模式步骤链正向/反向逐步走查 + F1 回归专项断言 |

## 分支与提交

- 分支：`v036-w2-client`（worktree wt-02）
- 提交：`0e37bc5` fix(client): 约稿表单增项步骤下一步跳过写需求修复（已含三模式走查记录）

## 验证结果

- web 全量测试 **154/154 绿**（8 文件，含新增 10 tests）
- `npx eslint .` 零错误零警告
- `npx vite build` 成功（5.53s）
- 三模式步骤链走查（对照 stepDefs 逐步核对，详见 commit message）：
  - 旧模型 3 步、单画风 4 步、多画风 5 步，正向/反向均无跳步
  - F1 专项：增项步下一步 → 写需求（多画风 3→4、单画风 2→3）
- 上传区（el-upload）未动，与二号-B 拖拽守卫任务零冲突

## 备注

- 测试策略：步骤导航逻辑（stepDefs/step/各步骤号 computed）在组件内、不在 useOrderForm 里，故 mock composable 控制三模式、实测按钮点击链路。测试 mock 边界仅限 vue-i18n/vue-router/i18n 单例/useOrderForm/ClientFloatingActions，不 stub Element Plus 本体。
- worktree 首次开工执行了 `npm install`（node_modules 缺失，非代码变更）。
