# 一号 → 二号：v0.14 前端批次开工

> 日期：2026-07-30
> 分支：`feat/client-frontend-v014`（单分支，按顺序提交）
> 优先级：P0（R39 用户指令）

---

## 任务顺序

### 第一步：R39 交互方案（不写代码）

出 2~3 个详情页状态区交互方案（文字描述 + 布局示意即可），写入 comms 交一号转用户拍板。

核心约束（用户已确认）：
- **C52**：有工作流时固定 el-steps **隐藏**，工作流进度条为主
- **C53**：无工作流老订单显示轻量提示"启用流程跟踪"
- 操作入口收敛到一个区域
- 取消订单保持滑块确认（R30e）
- 客户 track 页不受影响

方案确认后再动手写代码。

### 第二步：实施（方案确认后）

按以下顺序在同一分支提交：

| 序 | 需求 | 要点 |
|----|------|------|
| 1 | R39 状态区重构 | 按确认方案实施 |
| 2 | R42a 手动录单→订单管理 | OrderList 加入口，侧边栏删菜单，/manual-order 重定向 |
| 3 | R42b 须知编辑→设置 | Settings 加 tab，侧边栏删菜单，/rules 重定向 |
| 4 | R44 焦点/放大互换 | 单击=放大预览，✓小钩=设焦点（C56：手机端✓常驻） |
| 5 | R43 图库闪烁 | el-image 加 placeholder slot + 背景色 |
| 6 | R45 多选删除 | 工具栏"管理"按钮进入多选（C58），批量≥3滑块确认（C59） |
| 7 | R41 备注附图拖拽 | 复用 usePasteUpload，备注区加拖拽+粘贴（C55） |
| 8 | R46 备注删除确认 | 删除备注加轻量行内确认（C59 方案 C） |

### 授权文件

- `artist/src/views/OrderDetail.vue`
- `artist/src/views/OrderList.vue`
- `artist/src/views/Settings.vue`
- `artist/src/views/ArtworkManage.vue`
- `artist/src/views/ManualOrder.vue`（删除或重定向）
- `artist/src/views/RulesEditor.vue`（删除或重定向）
- `artist/src/layouts/ArtistLayout.vue`
- `artist/src/router/index.js`
- `artist/src/locales/zh.json`
- `artist/src/locales/en.json`
- `artist/src/composables/`（如需新增）
- `api/index.js`（如需新接口调用）

### 纪律

- 代码必须 commit 到分支，未 commit 不算完成
- i18n 只加不改成文
- 提交说明：`type(scope): 描述`，含影响范围+自测情况
- R42 旧路由必须重定向，不能 404
- R44 手机端 ✓ 按钮常驻，不依赖悬停
- R45 批量删除确认方式：≥3 条用滑块，单条用标准弹窗
