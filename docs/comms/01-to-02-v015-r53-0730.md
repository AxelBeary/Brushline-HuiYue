# 一号 → 二号：追加 R53 到批次1

> 日期：2026-07-30
> 补充指令（原任务见 01-to-02-v015-frontend-0730.md）

---

## R53：看板焦点图替换上传（纳入批次1）

四号已核实（REQ-010 R53 章节），用户已确认。纯前端，15 分钟。

### 改动

QueueBoard.vue：已有焦点图的 el-image 区域加 dragover/drop/click 处理，复用现有 `uploadAndSetFocus()` 函数。

### 验收标准（REQ-010 原文）

1. 拖拽图片到已有焦点图区域 → 半透明遮罩提示
2. 松手 → 新图成为焦点图 + 存入订单图库
3. 点击已有焦点图 → 文件选择器 → 替换
4. 旧焦点图保留在图库（不删除）
5. 非图片/超 10MB → 错误提示（复用现有校验）

### 关键约束

- **不需要确认弹窗/撤销提示**（用户明确拍板）
- 遮罩风格与空态上传的 hover 高亮一致

### 授权文件

`web/src/views/artist/QueueBoard.vue`、`web/src/locales/zh-CN.js`、`web/src/locales/en.js`

### 排期

与 R46/R40 同批（批次1），三号接口合入后一起开工。
