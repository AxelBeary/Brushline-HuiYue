# 815 过堂核对 · 第 1 轮（2026-08-16）—— 用户决策清单逐条过堂记录

> 四号整理 · 与用户直接过堂 · 结论已核对代码/实测 · 待确认项明确标注
> 状态：本文件为过堂实录；用户答复待确认项后更新，终稿转一号执行。

## 一、已拍板销账项

| # | 事项 | 用户结论（原文） | 处置 |
|---|---|---|---|
| 1 | 终验·取消撤销 | 「1取消撤销没问题」 | 销账 |
| 2 | 终验·浮层画风/尺寸 | 「6没问题」 | 销账 |
| 3 | 终验·额度满牌 | 「7没问题」 | 销账 |
| 4 | 终验·一次性下载 | 「2下载功能还没人用过 再等段时间」 | 挂起待用后验 |
| 5 | 终验·多付提示 | 「5还没人用到」 | 挂起待用后验 |
| 6 | **部署搬家** | 「已经把生产搬到海外服务器/公网机器」 | **销账（重大）**——原待办清单第 4 项完成，海外服务器+公网已就位 |
| 7 | **B 测** | 「已部署到服务器 开始b测」 | **已开启**——结合上条：生产已在海外服务器，B 测开始 |
| 8 | 增项模板管理 | 「增项模板管理看过了 设计的不错」 | 验收通过 |
| 9 | 桌面端 REQ-014 | 未再提，维持 HOLD | 不动 |

## 二、新需求（用户拍板，待施工）

### REQ-新-01：画师管理页封禁/移除统一入口
- 用户原话：「封禁入口，加。 已封禁和移除放在一个入口去处理」
- 现状核查（代码实证）：画师管理页只有「移除」（软删除：标记 deleted_at + token_version 递增，无 UI 恢复入口）和已封禁行的「解封」；主动「封禁」（is_banned 独立态，两步确认）只在举报管理页处理举报时出现
- 需求：画师管理页增加封禁入口，且**封禁/解封/移除统一放在一个操作入口**里处理（用户明确"放在一个入口"）
- 验收标准（待补充细节后定稿）：
  - 画师管理列表/详情中可对任意未封禁画师发起封禁（两步确认）
  - 已封禁画师可解封（现有入口保留）
  - 封禁/移除归入同一操作区（用户可接受的具体形态待确认）
  - 封禁即踢下线、写留痕（对齐现有 compliance 行为）

### 新反馈 5 条（Bug/需求，已立案待修）
| # | 现象（用户原话） | 定性 | 状态 |
|---|---|---|---|
| 1 | 「快捷约稿设置里把"状态切换"删掉 已设置的也不能再显示」 | 需求 | 已定位：`web/src/locales/zh-CN.js` quickAction.status='状态切换'，快捷入口设置项；删入口+清已设置 |
| 2 | 「后台的"价格管理"、"作品管理"点进去点出来左边的导航也会跟着刷新一下」 | Bug（前端） | 待修：左侧导航随路由切换重渲染闪烁 |
| 3 | 「新建的管理员画师初始化并确认开店后 流程与比例不会载入默认设置」 | Bug（后端默认值） | 待修：开店后默认工作流模板/默认价格比例未载入 |
| 4 | 「开稿管理设置名额后按钮要滚到最下面 反直觉」 | UX 改进 | 待修：设置名额后提交按钮在页面底部需滚动 |
| 5 | 「排期看板每次点东西后所有东西会消失一下再刷新出来」 | Bug（前端） | 待修：看板交互触发整块重载闪烁 |

## 三、待确认项（用户答复后更新）

1. **E5 文案（深夜/午夜）**：用户未直接选 A/B，先要求解释 6 时段代码逻辑（已答：见回复）——逻辑讲清后待拍板：
   - A 保持「深夜」=night 普通档 +「午夜」=latenight 优先池
   - B 改 latenight 标签（如「深夜特供」）避免混淆
2. **科学计数法消毒**：用户新反馈「数字疑似没有做好"科学计数法"的消毒」
   - 已定位疑点：`web/src/utils/money.js` formatAddonPrice 裸拼 `¥${n}`（无 toFixed/科学计数法消毒；percent 模式 `+${n}%` 同理）；formatYuanValue/formatYuanTrimmed 整数直出 `¥${v}` 亦无防大数
   - 待用户指认：具体在哪看到（增项模板管理价格列/输入框？画师定价？）以精确定位
3. **新反馈 #3/#4 理解**：四号按「默认工作流模板+默认价格比例未载入」「名额设置的确认按钮位置」理解立案，待用户确认无误

## 四、40 条低危打磨项（用户问"都是啥"，已答）

来源：桌面《拾绘项目深度审计报告-20260815.md》P2/P3 剩余项，分 5 组（交互细节约 15 / 冷门边界约 10 / 性能约 5 / 技术债约 10 / 安全纵深约 8）。
用户未拍板处置方向（建议：挂着，B 测反馈撞到哪个修哪个），**待确认**。

## 五、转交一号清单

1. 部署搬家销账（第 4 项完成）+ B 测已开启 → STATUS 更新
2. REQ-新-01 封禁/移除统一入口 → 排期施工
3. 新反馈 5 条 → 排期修（Bug 2/3/4/5 + 需求 1）
4. 科学计数法消毒 → 定位后按 Bug 修
5. E5 文案拍板 → 等用户答复后转交
