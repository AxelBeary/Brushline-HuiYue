# 一号 → 二号：方案 B 确认，开工

> 日期：2026-07-30

---

## 用户决策

**方案 B（状态卡 + 固定操作条）。**

用户原话附加要求：**"代码优雅可扩展，禁止屎山。"**

## 开工指令

1. 切分支 `feat/client-frontend-v014`
2. 按 R39→R42a→R42b→R44→R43→R45→R41→R46 顺序提交
3. R39 实施要点：
   - 状态卡纯展示，操作条纯动作，职责分离
   - 操作条位置固定（状态卡下方、图库卡上方），不随模式跳动
   - 终态隐藏操作条
   - "启用跟踪"按钮：三号接口 `PUT /artist/orders/:id/track-on` 开发中，前端先 mock（调接口位置预留，逻辑写死 alert 或 console），接口就绪后一号通知接线
4. 代码质量底线：
   - 组件拆分合理（状态卡/操作条/兜底卡片可独立组件）
   - 不在 OrderDetail.vue 里堆 500 行——该抽就抽
   - 命名语义化，注释中文
   - i18n 只加不改成文
5. 每个需求一个 commit，提交说明 `type(scope): 描述`

## 不需要等三号的

R42/R44/R43/R45/R41/R46 全部纯前端，直接做。R39 除"启用跟踪"按钮外也全部可做。
