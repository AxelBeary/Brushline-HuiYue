# 二号 v0.23 派工（2026-08-01）

> 分支：`feat/v023-frontend`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-fe`
> 基准：master `f83b8bb`

---

## 第 1 波：B4 留言板前端骨架（三号做 B7 后端时你并行做，不需要 API）

| # | 工作 | 说明 |
|---|------|------|
| 1 | TplGuestbook.vue | 留言墙共享组件：留言列表 + 提交表单（昵称必填 + 200 字限制）+ 空状态（"还没有留言，来说点什么吧 💬"）+ 画师回复展示（带"画师"标识） |
| 2 | 4 模板挂载 | ArtistHomeClassic/Gallery/Folio/Atelier 底部统一引入 TplGuestbook |
| 3 | 画师端审核列表 | 留言管理页：pending/approved/rejected 筛选 + 通过/拒绝/回复操作 |
| 4 | 管理端 | 管理员留言管理（查看/强制删除） |

> 组件先写 UI + mock 数据结构，API 调用留接口占位（如 `// TODO: 对接 POST /api/public/artist/:subdomain/messages`）。三号 B4 后端就绪后第 4 波对接。

## 第 2 波：B7 画师端前端（3h，等三号 B7 后端 API 合入后）

参考 spec：`docs/specs/plan-v023-quota-pool.md` §4.1

| # | 工作 |
|---|------|
| 1 | 收款记录区重做：已收/应收/待收 + 进度条 + 流水列表 + "记录收款"弹窗（金额 + 可选备注）+ 撤销（二次确认） |
| 2 | 应收参考区：分期节点只读展示（✓/○ 由 paid_total_cents 推算） |
| 3 | 替换现有 PaymentBar 勾选交互 |

## 第 3 波：B7 客户端 + 管理端（2h）

| # | 工作 |
|---|------|
| 1 | 客户端 track 页：进度条 + 四项数据（已付/下期应付/待付/总额），不显示画师内部节点名 |
| 2 | 管理端：订单详情加收款流水只读展示 |

## 第 4 波：B4 前端对接 API（三号 B4 后端就绪后）

- 把第 1 波的 mock 占位替换为真实 API 调用
- 联调测试

---

## 授权文件范围

- `web/src/components/shared/TplGuestbook.vue`（新建）
- `web/src/views/artist/ArtistHome*.vue`（挂载留言墙）
- `web/src/views/artist/`（画师端留言审核页）
- `web/src/views/admin/`（管理端留言管理）
- `web/src/components/artist/PaymentBar.vue`（B7 重做）
- `web/src/views/artist/OrderDetail.vue`（收款记录区）
- `web/src/views/track/`（客户端付款展示）
- `web/src/composables/`（新增 useGuestbook / useOrderPayments）
- `web/src/api/index.js`（新增 API 方法）
- `web/src/i18n/`（中英文案）
- `web/test/`（对应测试）

不在授权范围的文件不要动。需要改其他文件先写 comms 说明。
