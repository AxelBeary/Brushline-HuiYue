# 一号派工：v0.31 前端（2026-08-03）

## 分支与 Worktree

- 分支：`feat/v0.31-frontend-0803`
- Worktree：`git worktree add ../artist-commission-wt-fe feat/v0.31-frontend-0803`
- 主 worktree 不动（一号专用）

## 授权文件

- `web/src/views/artist/**`（画师端页面——本版本临时授权）
- `web/src/components/artist/**`（画师端组件）
- `web/src/components/templates/**`（4模板）
- `web/src/composables/**`
- `web/src/api/**`
- `web/src/locales/**`
- `web/test/**`（测试）

## Wave 1（先做，~3 天）

1. **F2 拖拽上传**（REQ-021 F2）：录单页上传区支持拖拽。el-upload 自带 drag。0.5天。
2. **F3 录单设开稿日**（REQ-021 F3）：录单表单加日期选择器（可选），用 REQ-018 的 disabled-date。0.5天。
3. **F5 增项点击勾选**（REQ-021 F5）：价格管理页增项行点击=编辑，拖拽=排序，不冲突。0.5天。
4. **F6 作品主图设置**（REQ-021 F6）：作品编辑页"设为主图"。API 已有（PUT /artist/artworks/:id/cover）。1天。
5. **F7 主图去重**（REQ-021 F7）：主图不在下方图片网格重复。useArtistData 加 galleryArtworks computed。0.5天。

## Wave 2（Wave 1 合入后，~1.5 天）

6. **F4 录单设节点状态**（REQ-021 F4）：录单表单加初始节点状态选择器（待确认/已确认/进行中）。0.5天。
7. **F8 留言语言过滤**（REQ-021 F8）：留言管理页加语言筛选下拉。需三号先完成 language 字段+查询参数。0.5天。
8. **机动**：补测试、修体验问题。

## Wave 3（Wave 2 合入后，~1.5 天）

9. **操作日志前端**（REQ-021 F1）：订单详情页操作日志时间线 + 按类型/时间筛选。需三号先完成日志 API。1.5天。
10. **联调**：与三号联调收款/改价/日志全链路。

## 注意

- 本版本临时授权画师端文件，v0.32 恢复默认分工（三号=画师端前后端）
- 主图去重注意空态：只有一张作品且设了封面时，去重后瀑布流为空——需兜底显示
- F6 作品主图 ≠ REQ-017 封面（主图=列表缩略图，封面=首页展示），UI 相邻但逻辑独立
- composable 解构：模板中引用的每个变量必须从 composable 解构列表中导出（v0.19 事故教训）

## 提交规范

`type(scope): 描述`，每个功能一个 commit。提交说明含影响范围+改动说明+自测情况。
