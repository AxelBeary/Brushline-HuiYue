# 四号 → 一号：REQ-025 动态节点计价模型（备案 + 待审核排期）

> 日期：2026-08-04
> 交付物：`docs/requirements/REQ-025-动态节点计价模型.md`
> 性质：新需求备案。用户确认"规则合理且完善"，但排期需一号定夺

## 需求一句话

工作流收款节点从"固定百分比快照"升级为**动态锁价分摊模型**：总价条目化可变、增减价按未锁节点比例分摊、完成 OR 付清先到先锁、超付向后抵扣、关单后增减走额外应收/应退。用户 2026-08-04 用一个完整场景口述（195→改价300→收定金→收线稿超付→加200分摊→关单后加50），四号复算验证自洽（纠正用户一处笔误：完稿应为 140 非 180，守恒律验证），并对照第三方 AI 方案批判吸收。

## 用户已拍板的关键决策

- **D1 锁价规则**：完成 OR 付清，先到先锁；回退不解锁
- **D2**：发生过收款后禁止无痕覆盖总价，必须走条目
- **D3 排期**：用户问"能挤 v0.35 吗"——四号核实波 1/波 2 已合入但波 3 + 联调在途，且 v0.35 是拍板过的"最后功能版"，**建议不挤**；v0.36 清账不动它，**v0.36 后独立版本**
- **D4**：跳过影子计算器，直接规则文档 + 测试用例
- 不留 legacy/dynamic 双轨：上线全切 + 演示数据重建

## 给一号的参考（文档第五节有实施映射）

现有表结构已核实，就地进化即可：`order_payment_installments`（订单级分期快照）加锁价字段 + `order_extra_items` 扩展为调整条目账本 + 复用 `order_payments`/`paid_total_cents`。不需要第三方 AI 提议的 4 张平行新表。精度规则：以分计算、向下取整、尾差归最后未锁节点（守恒前提）。8 个边界案例 + 12 条验收标准已写入文档。

## 申请

1. 审核 REQ-025 规则是否可进入待开发状态
2. 定版本归属（四号建议 v0.36 后独立版本，不挤 v0.35/不进 v0.36）
