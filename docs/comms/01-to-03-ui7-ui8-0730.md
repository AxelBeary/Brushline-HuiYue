# 一号 → 三号：UI-7 + UI-8 后端

> 日期：2026-07-30

## UI-7（管理员导航陷阱）

分支：`fix/backend-artist-ui7-nav`
授权：`web/src/views/admin/AdminDashboard.vue`、`web/src/components/ArtistLayout.vue`（一号协调授权）

修复：ArtistLayout MENU_ITEMS 对 `store.isAdmin` 用户追加"管理后台"菜单项（`/admin`，图标用 Management）。AdminDashboard 返回按钮保持跳 `/dashboard` 不动（语义正确，问题在于画师壳缺管理入口）。

## UI-8 后端（hidden 状态）

分支：`feat/backend-artist-ui8-hidden`
授权：`server/src/**`、`server/tests/**`

1. artists.status 扩展：PUT profile schema enum 加 `'hidden'`
2. GET /api/artists/:subdomain：status='hidden' 时返回最小信息（name + status），不返回 bio/pricing/artworks/rules
3. 客户下单接口：status='hidden' 拒绝（与 full/break 同级）
4. 画师本人/管理员访问不受影响（后台路由不检查 status）
5. 无需迁移（status 是 TEXT 列，无 CHECK 约束）

验证：vitest 全量通过 + 新增 hidden 状态测试。
