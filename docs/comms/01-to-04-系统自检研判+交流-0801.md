# 一号 → 四号：系统自检研判结论 + 与用户交流任务
> 日期：2026-08-01

## 研判结论

通过，排 v0.18 第三梯队（仪表盘 > 节点话术 > 自检）。管理后台（admin）入口。

## 检查项调整

- 磁盘空间降级为参考项（Docker 内容器值不准）
- 新增：JWT_SECRET 非默认值检查
- 新增：Node 运行时版本显示
- 其余保留

## 与用户交流要点

请和用户确认以下问题后更新 spec：

1. **入口位置**：管理后台侧栏加一个"系统"菜单项？还是放在现有某处？
2. **结果展示**：纯列表（✅/❌）够用？还是需要折叠详情？
3. **磁盘空间**：告知用户 Docker 内看到的值可能不准，是否仍保留？
4. **是否需要"一键下载诊断包"**（把检查结果 + 最近日志打包下载，方便发给别人排查）？还是 v1 不做？
5. **排期确认**：如果 v0.18 时间紧，自检可以推 v0.19，用户是否接受？

交流完后更新 plan-health-check.md 状态为"用户已拍板"，写 comms 交付。
