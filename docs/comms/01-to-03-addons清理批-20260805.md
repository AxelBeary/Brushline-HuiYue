# 01→03：price_addons/addon_tiers 冻结表代码清理批（第一批：算价读路径）

> 日期：2026-08-05 · 派工：一号 · 执行：三号
> worktree：`../artist-commission-addons`（已建）· 分支：`addons-cleanup`（基于 master `a8ef3e5`）
> 依据：你的评估报告 `docs/comms/03-to-01-addons评估报告-20260805.md` §Q1.5（你自己写的草案，本批执行其子集）

## 开工

`cd ../artist-commission-addons` → `npm install`（server）→ `git merge master`（基线即 master，应无变化）

## 本批范围（只做这些）

按 Q1.5 第 1 条的**算价链路部分** + 第 3 条测试清理：

1. `pricing.service.ts`：`getPublicPricing`（L102-108）移除 price_addons/addon_tiers 读取与「档位+适用增项」组装中的增项部分；`calculatePrice`（L168 addon_tiers 校验 / L180 price_addons 取价）移除增项取价逻辑——**档位基础价（price_tiers）一个字不动**。行为变化：addons 参数传入不再计价（等价忽略），生产存量仅 1 行垃圾数据、0 订单引用，风险已由你的报告实锤。
2. 测试：`pricing.service.test.js` / `style.test.js` / `setup.js` 的 price_addons/addon_tiers seed 清理，受影响断言改写；新增 1~2 例「传 addons 不影响档位算价」回归。
3. `pricing.service.ts:7-8` 注释同步更新。

## 明确不做（下批，勿越界）

- POST /api/orders 与手动录单 schema 的 addons 字段删除——**与前端旧模型兜底分支（useOrderForm.js L633-649 / ManualOrder.vue L1019-1025）耦合**，必须等二号腾出手前后端同批改，否则 ajv 拒收会打断档位模式下单
- createOrder addons 参数删除（同上耦合）
- DROP 表迁移——等 schema/前端清理全部合入后单独一批（届时版本号用 **v43**：v42 已被 F2 平台表占用；DROP 守 v38 事故规则：事务外 + 显式关 FK + 迁移前备份）

## 铁律

1. 只动 server，web 零触碰
2. 基线 server 864 全绿 + 本批新增；`npm run lint`、`tsc --noEmit` 零错（在 server/ 目录跑）
3. DB 只读纪律保持——本批不写迁移不碰真实库，测试走隔离 DB_PATH
4. 交付：commit 到分支 + `docs/comms/03-to-01-addons清理批-交付-20260805.md`（改动清单 + 测试基线前后对比 + 剩余清理项清单）。不推送不合并。

—— 一号
