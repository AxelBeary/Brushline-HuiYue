# 04-派工-repowiki非认证主题-20260807

> 派工：一号 → 四号（requirements）
> 工作方式：**纯只读审计，零代码改动，不需要 worktree**——在主仓目录只读（不许 git 写操作），输出报告落 `docs/comms/`。
> 背景：四号上一轮（repowiki P2 认证系）已交付——认证系 14 篇抽样全部 TOTP 时代，0 🔴。四号建议改派**非认证主题**，一号采纳（P2 改派）。

## 一、任务总览

repowiki P2 改派：**非认证主题抽样**（部署/CSP/Sentry/前端/数据库等主题），对照 master 代码核实过时项。重点风险面（四号上轮建议 + 近期代码改动相关）：

1. **部署/CSP/Sentry**：`SENTRY_DSN_BACKEND` vs `SENTRY_DSN` 变量名陷阱（app.js 读 SENTRY_DSN_BACKEND||SENTRY_DSN）、CSP connect-src、Caddyfile 压缩缓存
2. **前端主题**：5 色换色（natural-v2）、模板体系、i18n 懒加载等近期改动
3. **数据库/其他**：除已重写的数据库模式设计.md 外，是否有 schema 描述文档仍指旧表/旧迁移

## 二、文档位置与基准

- 原文（外部贡献，只读）：`C:\Users\qly19\Desktop\repowiki\`（约 118 篇，**原目录保留不删不改**）
- 仓库内已重写版本（对照基准）：`docs/external-wiki/wiki-认证接口.md`、`wiki-数据库模式设计.md`
- 代码基准：master HEAD（当前 `240a302`，含埋点看板/拆分/STATUS v20）
- 上轮报告参考：`docs/comms/04-to-01-repowiki-P2-交付-20260807.md`（已合入，可读）

## 三、检查方法

1. 在 `C:\Users\qly19\Desktop\repowiki\` 下定位非认证主题文档（部署/运维/CSP/Sentry/前端/样式/环境变量/数据库 相关 .md）
2. 每篇对照 master 代码核实（重点 grep）：
   - `.env` 实际变量名 vs 文档描述的变量名（grep `server/.env` 或 `.env.example`）
   - CSP/Sentry 相关描述 vs `server/src/app.ts` 实际实现
   - 前端主题/色板描述 vs `web/src/styles/theme.css`（natural-v2 五色 #356B69/#3F5E80/#5E5494/#346edb/#3445db）
   - 文件引用名 .js vs .ts（TS 迁移后 app.ts/index.ts/connection.ts/seed.ts）
   - 表名/迁移版本 vs `server/src/db/init.js`（当前迁移 v44，含 events/anon_tokens）
3. 输出核对表：文档名 / 描述内容 / 代码实际 / 判定（🔴严重过时 / 🟡轻微 / 🟢正确 / ⚪缺失）

## 四、输出

- 报告写 `docs/comms/04-to-01-repowiki非认证-交付-20260807.md`：
  - 抽样了哪几篇、每篇判定
  - 🔴 严重过时清单（文档+过时段落+当前正确版本）——供一号决策是否重写
  - 🟡 轻微过时清单（修补即可，1-2 行/处）
  - 建议：哪些值得重写（P0 级）/修补（P1）/无需动
- 零代码改动，不 commit 任何仓库文件；报告由一号合入

## 五、纪律

- **只读**：不 `git add/commit/push/merge`、不碰 master、不建 worktree
- 外部贡献文档不直接改——只报告，重写由一号派单
- 判断以 master 代码为准，不信文档自我描述
- 20 分钟无产出停下报卡点
