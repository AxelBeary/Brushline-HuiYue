# 四号 → 一号：v0.18 仪表盘需求预研交付

> 日期：2026-07-31

## 交付物

`docs/specs/plan-dashboard.md`——v0.18 仪表盘需求草案。

## 内容概要

基于 C46/C47 拍板方向 + SPEC-004 §5 + 现有 Dashboard.vue 盘点 + temp/ 用户点评提取，覆盖四个模块：

1. **收入统计**：月/季/年维度切换 + 图表 + 环比 + 汇总数字
2. **快捷操作区**：从 4 按钮升级为卡片网格，新增图库/档位入口
3. **最近活动/待办**：保留 R51 截稿日+待办，新增活动流（复用 order_notes）
4. **名额概览卡**：SPEC-004 已定义，补充进度条可视化选项

## 需要用户拍板的决策点（C48~C58）

| # | 问题 | 建议 |
|---|------|------|
| C48 | 收入图表类型（柱状/折线/双轴） | 柱状图 |
| C49 | 是否加档位收入饼图 | 否（v1 不做） |
| C50 | 快捷操作布局（卡片网格/按钮行/分组） | 卡片网格 |
| C51 | 快捷操作项（6 个：排期/手动/全部/图库/档位/设置） | 如列 |
| C52 | 快捷操作是否可自定义 | 否（v1 固定） |
| C53 | 是否加活动流 | 是 |
| C54 | 活动流条目上限 | 10 |
| C55 | 活动流刷新机制 | 进入加载一次 |
| C56 | 活动流数据源（状态变更/复用 order_notes/新表） | 复用 order_notes |
| C57 | 名额卡进度条 | 进度条+文字 |
| C58 | 整体布局（单列/双栏/标签页） | 单列流式 |

## 设计原则（从用户点评提炼）

- 页面不能太重（用户原声："感觉这个页面有点重了"）
- 不要悬浮按钮（用户存疑）
- 性能敏感，不做自动轮询（用户："有点卡"）
- 截稿日+待办用户明确认可，保留增强

## 粗估工时

~13h（后端 3.5h + 前端 8h + 测试 1.5h），图表库选型待三号评估。

## 申请

请一号审核草案结构是否完整，C 项清单是否可转交用户拍板。
