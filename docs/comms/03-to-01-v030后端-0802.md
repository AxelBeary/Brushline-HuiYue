# 三号交付：v0.30 后端+画师端（2026-08-02）

> 分支：`feat/v030-backend`
> Worktree：`artist-commission-03`
> 状态：✅ 四波全部完成，待一号审核

---

## 完成清单（4 项，4 个 commit）

| 波 | 任务 | Commit | 改动文件 |
|---|------|--------|----------|
| 1 | #15 瀑布流零跳动后端 | `e4cdc99` | init.js（迁移v30）、artist.service.ts（sharp读尺寸）、backfill-artwork-dimensions.js（回填脚本）、package.json（+sharp）、3个测试文件（async适配） |
| 1 | REQ-018 日期交互改进 | `8d63d9c` | Settings.vue（公告过期日disabled-date+shortcuts）、OrderDetail.vue（开工日/截稿日联动disabled-date）、ManualOrder.vue（截稿日不早于今天）、zh-CN.js、en.js |
| 2 | REQ-019 时间条整体平移 | `a05030a` | QueueBoard.vue（中间拖拽move模式+今天约束+grab光标+浮动标签双日期）、zh-CN.js、en.js |
| 3 | REQ-020 F1 订单搜索 | `0422773` | order.service.ts（?q LIKE模糊匹配昵称/订单号/档位名）、order.routes.ts（传q参数）、api/index.js、OrderList.vue（搜索框+300ms防抖+空结果提示）、zh-CN.js、en.js |

## 验证结果

- 后端测试：576/576 通过（36 文件），迁移 v30 正常应用
- 前端 ESLint：零错误零警告
- 前端测试：87/87 通过（5 文件）

## 注意事项

- **sharp 新增依赖**：已加到 server/package.json dependencies（非 devDependencies），Docker 构建需要
- **回填脚本**：`node server/scripts/backfill-artwork-dimensions.js`，幂等（只更新 width IS NULL 的行），部署后手动跑一次
- **createArtwork 改为 async**：两个调用方（artist.routes.ts + admin.routes.ts）已是 async handler，Fastify 自动 await，无需改动
- **订单搜索 COUNT 查询**：加了 LEFT JOIN price_tiers（搜索范围含档位名），对性能无实质影响（artist_id 索引仍在）
- **locales 纯新增键**，rebase 无冲突
