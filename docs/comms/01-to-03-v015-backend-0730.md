# 一号 → 三号：v0.15 后端任务

> 日期：2026-07-30
> 分支：`feat/v015-backend`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-03`
> 需求文档：`docs/requirements/REQ-010-v0.15规划.md`

---

## 批次1（无迁移，先做）

### R46：备注删除接口

- `DELETE /api/artist/orders/:id/notes/:noteId`
- requireAuth + 归属校验（非本画师 → 404）
- 系统备注（`created_by='system'`）拒绝删除 → 403
- 带图备注删除后，图片走现有 GC 机制（查 upload.service.js 的孤儿清理逻辑，复用即可）
- 授权文件：`server/src/features/order/order.routes.js`、`server/src/features/order/order.service.js`
- 测试：新增 DELETE notes 测试用例（正常删除 / 系统备注拒绝 / 非归属拒绝）

### R52：今日统计

- `getArtistStats` 新增 `todayNewOrderCents` + `todayRevenueCents`
- 今日新增：`created_at >= 今日零点` 的订单金额合计（final → total → snapshot 回退链，与月收入一致）
- 今日收入：`completed_at >= 今日零点` 且 `status IN ('done','delivered')` 的金额合计
- 时区处理与月收入一致（应用层算本地零点 UTC 时间戳）
- 授权文件：`server/src/features/order/order.service.js`
- 测试：新增今日统计测试（有数据 / 无数据返回 0 / 跨天边界）

**批次1 完成后 commit + 通知一号审核。**

---

## 批次2（迁移 v15）

### 迁移 v15：两个字段

```sql
ALTER TABLE artists ADD COLUMN accent_color TEXT DEFAULT NULL;
ALTER TABLE orders ADD COLUMN deadline DATETIME DEFAULT NULL;
```

- 在 `MIGRATIONS` 数组末尾追加，version: 15
- 授权文件：`server/src/db/init.js`

### R49：取色器后端

- `PUT /api/artist/profile` 支持 `accent_color` 字段
- 校验：仅允许预设色值列表（与前端 ThemePicker 的 5 色一致，查 `web/src/components/ThemePicker.vue` 确认色值）+ null（清除）
- 公开 API `GET /api/public/artists/:code` 返回 `accent_color`
- 授权文件：`server/src/features/artist/artist.service.js`、`server/src/features/artist/artist.routes.js`

### R51：截稿日后端

- `PUT /api/artist/orders/:id` 支持 `deadline` 字段（ISO 8601 或 null 清除）
- 新增 `GET /api/artist/orders/upcoming-deadlines`：返回 deadline 在未来 7 天内 + 状态非终态的订单列表（按 deadline 升序），每条含 id/order_no/customer_name/deadline/status
- `getArtistStats` 新增 `todayTodoCount`：今天截稿 + status='pending' + status='revision' 的订单数
- C62 已拍板（一号）：今日待办口径 = 今天截稿 + pending + revision，v1 固定规则不做自定义
- 授权文件：`server/src/features/order/order.routes.js`、`server/src/features/order/order.service.js`

**批次2 完成后 commit + 通知一号审核。**

---

## 并行任务（不阻塞批次1/2）

### R38：附加工作项技术方案

- 输出 `docs/specs/SPEC-003-附加工作项.md`
- 覆盖：数据模型（order_extra_items 表）+ 尾款重算逻辑 + 客户确认流程（v1 建议画师单方面加，不做确认）+ 客户进度页适配 + 与现有 price_addons/quote_snapshot 的关系
- 参考 REQ-010 R38 章节的 6 个待展开问题
- 方案写完后 commit 到 `feat/v015-backend` 分支，一号转交用户确认
- 授权文件：`docs/specs/` 目录

---

## 提交规范

- `feat(order): R46 备注删除接口` / `feat(order): R52 今日统计` / `feat(db): 迁移v15 accent_color+deadline` 等
- 每个批次一个或两个 commit，不混无关改动
- 提交前 `git diff --stat` 确认只有授权文件

## 测试

- 每批次完成后跑 `npx vitest run`（在 server/ 目录），全量通过再通知
- 新增接口必须有对应测试用例
