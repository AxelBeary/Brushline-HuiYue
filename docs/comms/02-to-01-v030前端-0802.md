# 二号交付：v0.30 客户端前端（2026-08-02）

> 分支：`feat/v030-frontend`
> 验证：ESLint 零问题 + `npm run build` 通过（两波各自验证）

---

## 第 1 波：#15 瀑布流零跳动（commit `3853a0b`）

- `TplGallery.vue`：el-image 外层加 `.tpl-gallery-img-wrap` 容器，绑定 `aspect-ratio: width/height`（后端字段就绪时精确预留高度，lazy 加载零 reflow）
- 字段缺失时返回空样式对象，骨架占位（#50 的 skeleton）兜底，行为与改动前一致
- `:deep(.el-image__placeholder)` 填满容器，有 ratio 时骨架撑满精确高度
- 4 模板共用 TplGallery，改一处全生效

**待验证**：实际零跳动效果需三号后端 width/height 字段（迁移 v30）合入后容器内验证。

## 第 2 波：REQ-017 封面重设计（commit `4ae5902`）

### 1. 去重（约束 2：封面不在图格重复）
- `useArtistData.js` 新增 `galleryArtworks` computed：`artworks.filter(a => !a.is_cover)`
- 兜底：过滤后为空则不去重（画师只有一张作品且设了封面时，主页仍有作品可看）
- 4 模板 TplGallery 改传 `galleryArtworks`，section 显隐条件同步改

### 2. Hero 显式化（约束 1：封面是门面图）
- `heroArtwork` 改为 `coverArtworks[0] || artworks[0] || null`（显式优先封面，无封面 fallback 第一张）
- `coverArtworks` 加 `cover_order` 排序（字段缺失 fallback 0，保持后端原序）

### 3. Settings 改造（约束 3：只放链接不搬列表）
- 删除封面管理区（作品网格 + 星标 + toggleCover 函数 + 旧 CSS）
- 替换为：当前封面预览缩略图（无封面时虚线占位"尚未设置封面"）+ "管理封面 →" 链接（router-link 跳 /artworks）

### 4. 作品管理页封面操作
- `ArtworkManage.vue`：每卡片右上角常驻星标按钮（★/☆，复用 PUT/DELETE /artist/artworks/:id/cover）
- 封面卡片左上角"封面"标签；多封面时显示 cover_order 序号（按 cover_order 排序后的位次）
- i18n 新增 6 键（artworks.coverSet/coverUnset/coverSetSuccess/coverUnsetSuccess/coverTag + settings.coverManageLink），中英双语

## 改动文件

| 文件 | 改动 |
|------|------|
| `web/src/components/templates/TplGallery.vue` | #15 aspect-ratio 容器 |
| `web/src/composables/useArtistData.js` | galleryArtworks 去重 + heroArtwork 显式化 + cover_order 排序 |
| `web/src/views/client/templates/ArtistHome*.vue` ×4 | TplGallery 改传 galleryArtworks |
| `web/src/views/artist/Settings.vue` | 封面管理区→预览+链接 |
| `web/src/views/artist/ArtworkManage.vue` | 封面星标+标签+序号 |
| `web/src/locales/zh-CN.js` / `en.js` | REQ-017 新增键 |

## 依赖/备注

- **cover_order 字段**：后端尚未加（三号迁移 v30/v31），前端已 fallback 兼容（缺失时按后端原序）。上下箭头调序 UI 未做——派工说"先用上下箭头调整（拖拽可选）"，但 cover_order 写入 API 未定，等三号后端就绪后再补调序交互
- **#43b 封面 400 Bug**：PUT /artworks/:id/cover 返回 400（五号排查中），修完前封面操作不可用，本批改动逻辑不受影响
- `web/package.json` 有 npm approve-scripts 改动（esbuild/vue-demi），未提交（非任务范围）
