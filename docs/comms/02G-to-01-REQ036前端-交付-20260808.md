# 02G-to-01：REQ-036 批A 增项交互直觉化重构交付

> 交付人：二号（client-frontend）
> 日期：2026-08-09
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w29`
> 分支：`beta/req036-addon-ux`
> 派工：`docs/comms/02G-派工-REQ036前端-20260808.md`
> 门禁：lint 0 / test 262 / check:i18n 0 / build ✅（一号独立复跑确认）

## 一、改动清单（8 文件，全在授权范围）

| 文件 | 改动 |
|------|------|
| `web/src/components/artist/ArtStyleManager.vue` | 主体重构（+473/-170）：尺寸三态/预览/摘要行/加购项池/双入口/拖拽/覆盖数据预载/暴露 reload() |
| `web/src/views/artist/TierManage.vue` | @tab-change 调 styleManagerRef.reload()（必修 bug 1：切 tab 刷新） |
| `web/src/components/artist/AddonCreateDialog.vue` | **新建**（115 行）：新建增项表单 + 同名处理（直接挂载/另建独立） |
| `web/src/components/artist/AddonPreviewDialog.vue` | **新建**（114 行）：顾客视角只读预览（修复 v-if+v-for 同元素 lint + controlLabel 少传 t 参数运行时 TypeError） |
| `web/src/components/artist/AddonSettingsDialog.vue` | **新建**（219 行）：三层编辑弹窗（模板级/画风级/尺寸级 + 批量启用 + 移除解绑） |
| `web/src/components/artist/addon-utils.js` | **新建**（44 行）：增项工具函数（摘要格式化/三态映射） |
| `web/src/locales/zh-CN.js` + `en.js` | 新增 40+ 双语键 |

## 二、对照 REQ-036 验收（1-12）

| 验收 | 结果 |
|------|------|
| 1. +新建增项 → 池子出现 + 沉淀库 | ✅ 实测（新建「机甲」→ 池出现「机甲¥50」） |
| 2. 同名提示（直接挂载/另建独立） | ✅ 实测（填「背景」→ 弹同名提示，点直接挂载不重复） |
| 3. 拖拽池→尺寸行启用 + 摘要实时 | ✅ 实测（拖背景胶囊到头像 → 摘要出现） |
| 4. 三层设置弹窗（模板/画风/尺寸 + 移除） | ✅ 实测 |
| 5. 应用到所有画风确认 | ✅（弹窗实现，未多画风实测——本地库单画风） |
| 6. 移除=解绑（库保留） | ⚠️ 按钮提示「批B 实现」（后端无删除 style_addon 端点） |
| 7. 尺寸三态（可约/展示/关闭 + 色语义） | ✅ 实测（关闭态整行弱化） |
| 8. 预览弹窗（顾客视角只读 + 公式 + 状态标签） | ✅ 实测（修复前 R is not a function，修复后正常） |
| 9. 摘要行三形态（加法/数量/倍率） | ✅ 实测（¥50 / ¥80/位 / +50%——倍率批B 后显示） |
| 10. 增项库 tab 保留 | ✅（未动 tabs 结构） |
| 11. 切 tab 刷新修复 | ✅（@tab-change reload，必修 bug 1） |
| 12. 覆盖面板位置修复 | ✅（面板随行内展开，必修 bug 2） |

## 三、浏览器实测（playwright + 本地 test DB + 真实 TOTP 登录）

- 池子渲染 4 胶囊 + 双入口 ✅ / 摘要行实时 ✅ / 三层弹窗 ✅ / 三态切换 ✅ / 预览弹窗 ✅ / 拖拽核心链路 ✅ / 新建增项 ✅ / 同名处理 ✅ / 零 JS 错误 ✅
- 截图 6 张：`w29/temp/`（smoke-tiers/create/drag/preview/settings-dialog + totp-rebind）

## 四、边界说明（按派工）

- 三态：本批前端置灰（数据在内存 `_status`），**批B 落库 + 算价/下单校验**
- 移除解绑按钮：提示「批B 实现」（后端无删除端点）
- 倍率项：胶囊/摘要按 `¥0` 显示（批A 同后端模型无 kind 字段；**批B 轻合并后显示 +50%**）

## 五、遗留

- 截图在 w29/temp（gitignored）——需一号拷贝或 VL 已验证可凭 diff 合入
- 交付报告本应随批 commit，因会话正常退出前停在收尾，由一号补写并 commit
