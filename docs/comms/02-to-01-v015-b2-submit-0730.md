# 二号 → 一号：v0.15 批次2 提交审核

> 日期：2026-07-30

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/v015-frontend-b1`（worktree: `D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02`）

【任务编号】
v0.15 批次2：R48 头像上传 + R49 取色器前端 + R50 预览 + R55 档位示例图拖拽

【修改模块】
画师端设置页（Settings.vue）+ 价格管理页（TierManage.vue）+ 客户主页入口（ArtistHome.vue）+ i18n

【修改内容】

**R48 头像上传**
1. Settings.vue profile tab 顶部加头像上传区：当前头像（无则首字母兜底）+ 点击上传
2. 上传流程：`uploadApi.image` → `PUT /api/artist/profile { avatar }` → 即时生效（不等 Save 按钮）
3. 前端校验：image/* + ≤10MB（与作品上传一致）
4. 无头像时 el-avatar 显示首字母占位（不显示破图）

**R49 取色器前端**
1. ⚠️ **与指令偏差**：指令说 el-color-picker，但后端白名单只有 5 色（`#34dbcb/#34c2db/#3498db/#346edb/#3445db` + null），el-color-picker 自由取色会 400。改用 **5 色色板选择器 + "默认"清除按钮**（与 ThemePicker 视觉一致），确保任何选择都在白名单内
2. 色板选择器在 Settings template tab 色板区下方，保存时随 templateId/paletteId 一起提交 `accentColor`
3. 客户侧注入：ArtistHome.vue 将 accentColor 映射到 `html[data-accent="1"~"5"]`（5 色与 theme.css 一一对应），**4 模板零改动**——所有模板已消费 `--color-primary`，data-accent 自动生效（验收标准 #5 "4 模板一致"天然满足）
4. 暗色模式免费获得：theme.css 已有 `html.dark[data-accent]` 提亮变体（验收标准 #3）
5. 画师设置优先于访客 ThemePicker 选择；离开主页时恢复访客选择（onUnmounted 清理）
6. 未设置时（null）不覆盖，行为与现在一致（验收标准 #2）
7. C61 已遵守：只影响客户主页，画师后台不变

**R50 预览**
1. Settings template tab 加"预览主页"按钮，新窗口打开 `/artist/:subdomain?_tpl=X&_pal=Y&_accent=Z`
2. ArtistHome.vue 单点分支：`_tpl` 覆盖模板选择、`_pal` 覆盖色板、`_accent` 覆盖强调色——只覆盖渲染层，不碰数据层（作品/档位/须知走公开 API）
3. 预览页顶部 sticky 横幅"🔍 预览模式 — 修改尚未保存"
4. 不传参数时行为与现在完全一致（验收标准 #5）
5. 客户拿到预览链接只看到公开数据 + 另一种渲染（验收标准 #4，无安全风险）

**R55 档位示例图拖拽**
1. TierManage 列表示例图列加 `.tier-img-wrap`：点击 → 文件选择器，拖拽 → 遮罩"拖入上传" + 松手上传
2. **无图直传，有图先 ElMessageBox.confirm 再覆盖**（旧图不可恢复——与 R53 看板焦点图"不需要确认"行为不同，一号提醒已遵守）
3. 上传流程：`uploadApi.image` → `updateTier({ exampleImage })` → 刷新列表
4. 校验：image/* + ≤10MB，非图片拖入有错误提示
5. 移除列表 el-image 的 `preview-src-list`（与 R53 同款：点击=上传，不能被预览吞掉）
6. 弹窗内"更换"按钮保持原样（无确认——用户只要求列表级拖拽加确认）

**i18n**
- 新增 18 键 × 2 语言：settings（avatarLabel/avatarHint/avatarUpdated/avatarNotImage/avatarTooBig/accentLabel/accentHint/accentClear/accentDarkHint/previewBtn）+ artistHome.previewBanner + tiers（dropToUpload/notImage/tooBig/overwriteTitle/overwriteConfirm/exampleUpdated）
- 新增 1 错误码 × 2 语言：`INVALID_ACCENT_COLOR`（对齐后端 errors.js）

【涉及文件】
- `web/src/views/artist/Settings.vue`（R48 + R49 + R50）
- `web/src/views/artist/TierManage.vue`（R55）
- `web/src/views/client/ArtistHome.vue`（R49 注入 + R50 预览覆盖）
- `web/src/locales/zh-CN.js` / `web/src/locales/en.js`（i18n）

【是否修改非客户前端文件】
否。仅授权文件（5 个）。4 个客户模板文件**零改动**——R49 通过 data-accent 在 ArtistHome 入口注入，模板已消费 --color-primary。

【接口依赖】
- `PUT /api/artist/profile` 支持 `avatar` + `accentColor`（三号已合入 master）
- `GET /api/artists/:subdomain` 返回 `accentColor`（三号已合入）
- R55 无新接口（复用 uploadApi.image + updateTier）

【自测情况】
| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过（7.87s） |
| Rebase master | ✅ 无冲突（8e84f4f） |
| console.log / TODO | ✅ 无残留 |
| v-html | ✅ 仅既有 1 处（R42b 须知预览，已 sanitizeHtml 消毒） |
| i18n 中英同步 | ✅ 18 键 + 1 错误码 × 2 |
| 改动文件范围 | ✅ 仅 5 个授权文件 |
| ad-hoc 验证脚本 | ✅ 84/84 PASS（hermes-verify-v015-b2.ps1，已删除） |

**需人工验证项**（无浏览器环境）：
- R48：设置页上传头像 → 客户主页 Classic 模板侧边栏显示新头像；非图片/超 10MB 有校验提示
- R49：选择强调色 → 保存 → 客户主页按钮/链接变色；切换暗色模式颜色仍可读；清除后回退默认
- R49：访客 ThemePicker 选色后进入设了强调色的画师主页 → 画师设置优先；离开后恢复访客选择
- R50：设置页选 gallery 模板不保存 → 点预览 → 新窗口显示 gallery 布局 + 横幅；正常访问无横幅
- R55：拖拽图片到无示例图档位 → 直传；拖到有图档位 → 确认弹窗 → 确认后覆盖、取消后不变
- R55：点击示例图区域 → 文件选择器 → 行为与拖拽一致

【可能影响】
- 档位列表示例图不再支持点击内置预览（preview-src-list 已移除）——点击改为触发上传
- 客户主页：画师设了强调色后，访客 ThemePicker 主色选择被覆盖（设计意图，C61 拍板）
- 不影响管理后台、嵌入脚本

【待确认问题】
1. **R48 模板消费 gap**：只有 Classic 模板渲染 avatar（侧边栏 el-avatar），Gallery/Folio/Atelier 的 TplHero 不渲染头像。REQ-009 验收标准 #1 说"客户访问其主页应该看到新头像"——3 个模板看不到。建议后续批次在 TplHero 加 avatar 展示（需一号确认是否追加）
2. **R49 取色器方案偏差**：用 5 色色板替代 el-color-picker（后端白名单限制），如一号/用户坚持自由取色需三号先放宽白名单

【申请】
申请一号审核合并。
