# 05→01：第三方安全审计报告核实（判断 14 项 + 修复 patch 审计）

> 日期：2026-08-05 · 执行：五号 · 模式：只读审计（未改代码）
> 材料：`security_audit_hardening_report.md`（F1-F14）+ `dto-patch.md`（F1 修复方案，未应用）
> 结论速览：对方判断 **9 属实 / 3 属实但系产品设计或已知项 / 1 部分误报 / 1 部分已缓解**；修复 patch **方向正确但不完整（漏 1 端点 + 1 字段）**

## 一、对方判断核实（F1-F14）

| # | 对方判定 | 核实结论 | 证据 / 修正 |
|---|---------|---------|------------|
| F1 | HIGH TOTP 密钥泄露 | ✅ **属实（且范围更大）** | `auth.routes.ts` L81 `{...request.artist}` / `admin.routes.ts` L542 `return a` / `artist.service.ts` L72 `SELECT *` 均含 `totp_secret`（生产库 41 列实测确认）。**对方漏了第 4 处**：`artist.routes.ts` L95-104 `/api/artist/profile` 也是 `{...artist}` 完整行 |
| F2 | HIGH 无 TLS/HSTS | ✅ **属实** | Caddyfile 仅 `reverse_proxy` 无 tls/HSTS；`{$DOMAIN}` 空时退化 :80；:3000 映射风险（上份核实已确认） |
| F3 | MEDIUM CSP unsafe-eval | ✅ **属实** | `app.js` L145 `script-src 'self' 'unsafe-eval'`。⚠️ 删除前需实测 Vite 生产构建是否依赖 eval（Vue 3 生产一般不依赖，但需验证后合入） |
| F4 | MEDIUM AUTH_DEV_MODE 误配泄露 | ✅ **属实** | `auth.service.ts` `isDevAuth` 无生产守卫。三号刚合入的 ADMIN_QQ fail-fast（f3d9668）是现成同模式参考，实现成本极低 |
| F5 | MEDIUM/LOW 签名密钥复用 | ✅ **属实** | `SESSION_SECRET` 用于会话 HMAC + 文件签名 + cookie 回退。P1 架构改动，拆分需全量回归 |
| F6 | MEDIUM 容器 root | ✅ **属实** | Dockerfile 无 USER 指令（node:22-slim 默认 uid 0）。修复简单（USER node + chown 卷） |
| F7 | MEDIUM 限流进程内 | ✅ **属实（已知延后项）** | `rate-limit.ts` 模块级 Map。v0.15 清单已评估「单实例可接受延后」+ 我方上份核实确认——**非新发现**，单实例继续延后合理 |
| F8 | LOW 匿名上传 | ✅ **属实（产品设计）** | `upload.routes.ts` L183 `/api/upload/reference` 无 preHandler，注释明确「客户下单用，公开」——匿名下单传参考图是功能需求；P2 风控建议合理（当前有 10次/10min 限流） |
| F9 | LOW 缺安全头 | ✅ **属实** | app.js 有 nosniff/XFO/CSP/Referrer/Permissions，缺 COOP/CORP/X-Permitted——补齐成本极低 |
| F10 | LOW 管理员 QQ 判定 | ✅ **设计观察合理** | `requireAdmin` 按 qq_number 相等；无公开注册所以抢注风险低。P2 role 列建模建议合理 |
| F11 | PASS .env 不进镜像 | ✅ **属实** | .dockerignore 排除 .env；docker-compose env_file 运行时注入 |
| F12 | MEDIUM 依赖扫描缺口 | ✅ **属实** | CI 无 npm audit/trivy；接入扫描流水线合理 |
| F13 | MEDIUM 前端存储型 XSS | ⚠️ **部分已缓解** | 对方明示「未覆盖 web/」——我方已核实：`v-html` 恰好 6 处全部经 DOMPurify（sanitize.js），留言 content 用 `{{ }}` 转义渲染。方向对（应复核）但缺前端证据，实际风险已低 |
| F14 | LOW-MEDIUM lookup 泄露 adminQq | ⚠️ **部分误报（产品功能）** | `order.routes.ts` L310 返回 adminQq 属实，但 **TrackOrder.vue L140-146「联系管理员」弹窗显式渲染该字段**（客户查不到订单时联系管理员）——**产品功能**，直接删除会破坏功能。建议产品决策（保留 or 脱敏）而非一刀切 |

## 二、对方修复审计（dto-patch.md）

### 方向评价
✅ **方向正确**：DTO 投影剔除敏感列 + getAllArtists 显式列——比仅改响应层更彻底，双层防御合理。

### 缺陷（3 处）
1. ❌ **漏修 `/api/artist/profile`**（`artist.routes.ts` L95-104 `{...artist}`）——F1 第 4 个泄露端点未覆盖。画师登录后 GET 自己的完整行含 totp_secret（自泄露危害低，但纵深防御应补）
2. ⚠️ **getAllArtists 显式列清单漏 `quick_actions`**（41 列中有，清单未列）——`Preferences.vue`/`QuickActions.vue` 消费该字段（画师 profile 数据源）；admin 列表当前不用，但显式列应保留防未来断裂
3. ⚠️ **清单漏 `custom_page_path`**——前端零引用（已搜证），可排除可保留，无实害

### 已验证安全的部分
- ✅ 排除 `weibo_url/bilibili_url/platform_urls` 安全——前端零引用（web/src 搜索 0 匹配，外链已走 social_platforms + custom_links）
- ✅ `totp_verified` 未排除——保留可接受（绑定状态非密钥本身，前端可能展示）
- ✅ 公开主页 `/api/artists/:subdomain`（L41-87）本就是显式字段投影——无泄露，无需改
- ✅ dto.ts 代码质量：类型标注 + void 抑制未用变量 OK，`if (!a)` 处理 null OK
- ✅ getArtistById（内部认证用）保留完整行合理（中间件需要 token_version 校验）

## 三、新发现（对方报告与 patch 均未覆盖）

1. **`/api/artist/profile` 泄露**（第 4 端点，dto-patch 漏修）
2. **getAllArtists 显式列漏 quick_actions**（前端有消费，应保留）

## 四、修复建议（供一号派工，中风险批）

以 dto-patch 为基础，补齐后实施：
1. **F1 修复（补全版）**：publicArtistDTO 应用到 **4 个端点**（me / admin/artists / admin/profile / **artist/profile**）；getAllArtists 显式列**补 quick_actions**（+ custom_page_path 可留）；建议 `/api/artist/profile` 返回经 DTO 处理
2. **F4**：AUTH_DEV_MODE 生产 fail-fast（三号 ADMIN_QQ fail-fast 同模式，5min）
3. **F6**：Dockerfile USER node + 卷权限
4. **F9**：补 COOP/CORP/X-Permitted 三头
5. **F3**：CSP 去 unsafe-eval（先隔离实例实测 Vite 生产无 eval 依赖）
6. **F14**：交产品决策（保留 or 脱敏 adminQq），不直接删
7. **F12**：CI 补 npm audit（可选）
8. **F2/F5/F7/F8/F10/F13**：P1/P2 排期（F7 已知延后、F8 产品设计、F13 已缓解）

## 备注
- 本次只读审计，未改代码、未建分支；对方 patch 未应用（git 工作区干净确认）
- F1 修复属**中风险**（改 4 个响应端点 + 1 个查询），需补回归测试（响应不含 totp_secret + 前端字段不缺失）
