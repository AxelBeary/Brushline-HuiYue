# 02-to-01：批5色板联动——交付

> 三号转交一号，文件：docs/comms/02-to-01-色板联动-交付.md
> 交付时间：2026-08-07 · 执行角色：三号（后端） · worktree：`../artist-commission-w21` · 分支：`beta/accent-sync`
> 施工图：`docs/comms/01-to-03-批5色板联动批.md`（已完整执行）

## 一、改动摘要（授权 4 文件 + 测试同步 1 文件）

| # | 文件 | 改动 |
|---|------|------|
| 1 | `web/src/views/artist/Settings.vue` L376-382 | `ACCENT_PRESETS` 1/2/3 号改新 hex（`#356B69` 青 / `#3F5E80` 碧 / `#5E5494` 蓝，展示用大写）；4/5 号不变 |
| 2 | `server/src/features/artist/artist.service.ts` L196 | `ACCENT_COLORS` 白名单改新 hex（小写 `#356b69`/`#3f5e80`/`#5e5494`，与 `String(value).toLowerCase()` 存储一致） |
| 3 | `web/src/views/client/ArtistHome.vue` L90 | `ACCENT_INDEX` 映射 key 改新 hex（小写，与 L93 `String(raw).toLowerCase()` 查找对齐） |
| 4 | `docs/待修复问题清单.md` | 新增「批5色板联动」节，记录存量 Alice 处置（见下） |
| 5 | `server/tests/artist.service.test.js`（测试同步，施工图授权） | TC-R-08 旧青→新 1 号 `#356b69`；TC-R-08b 旧蓝→新 3 号 `#5e5494`；TC-R-08d 5 色数组全换新值。**原因**：白名单收紧后旧 hex 会抛 `INVALID_ACCENT_COLOR`，3 个用例若不改必挂 |

**未动**：`theme.css`（已是最新新色）；locales；其他文件。

## 二、存量处置说明（Alice id=2158 `accent_color='#34dbcb'`）

- **未执行 UPDATE**（改生产数据需用户确认，按红线只记录）。
- **渲染兼容兜底生效**：ArtistHome 的 ACCENT_INDEX 只认新 hex，Alice 存旧 `#34dbcb` 查不到 → `null` → 走默认主色；而默认主色恰为新 1 号月白青 `#356B69`（theme.css data-accent="1"），Alice 主页实际显示符合预期（月白青）。
- **低优先级遗留已记录**：若未来默认主色变更，Alice 会脱节，届时需 `UPDATE artists SET accent_color='#356b69' WHERE accent_color='#34dbcb'`。已写入 `docs/待修复问题清单.md`。

## 三、自测真实输出

1. **oxlint**（3 个改动文件）：`Found 0 warnings and 0 errors`（96 rules, 28 threads）
2. **typecheck**（server，`npm run typecheck --prefix server`）：tsc --noEmit 通过，无错误
3. **server 测试**（`npx vitest run --root server`）：`Test Files 60 passed (60)` / `Tests 939 passed (939)`
4. **web 测试**（`npm run test:web --prefix web`）：`Test Files 13 passed (13)` / `Tests 215 passed (215)`
5. **web build**（`npm run build --prefix web`）：`✓ built in 5.68s`
6. **旧 hex 残留检查**：`#34dbcb|#34c2db|#3498db` 在 `web/src`、`server/src`、`server/tests` 全库 0 命中（Alice 的 DB 数据不在代码库，天然豁免）；docs/archive 历史文档中的旧值属归档记录，不动

## 四、浏览器实测

未实测（本批为常量同步替换 + 测试同步，无逻辑改动；且本环境无浏览器工具）。待一号/用户验收：画师设置页换色 → 保存 → 客户主页实际渲染是否同色（重点看 1/2/3 号低饱和新色）。

## 五、卡点

无。worktree 初始无 node_modules，已 `npm ci`（server + web）后全部门禁通过，未改动 lock 文件。

## 六、变更文件清单

```
server/src/features/artist/artist.service.ts
server/tests/artist.service.test.js
web/src/views/artist/Settings.vue
web/src/views/client/ArtistHome.vue
docs/待修复问题清单.md
docs/comms/02-to-01-色板联动-交付.md
```
