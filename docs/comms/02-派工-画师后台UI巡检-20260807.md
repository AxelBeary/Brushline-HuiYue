# 02-派工-画师后台UI巡检-20260807.md

> 派工：一号 → 二号（client-frontend）
> worktree：`../artist-commission-w2`（分支 `beta/ui-audit-artist`）
> 背景：用户拍板「派海量的人去大面积测网页丑陋/交互问题」——本批=**画师后台全页面巡检**（只读，出问题清单）
> 基线：master `9974be5`。**开工第一步 `git merge master`**。
> 方法：隔离环境（参考 artist-commission-local-dev-env skill）+ Playwright 逐页截图 + DOM 量化检查 + **curl VL 看图**（脚本：`D:\Hermes Agent CN Desktop\data\hermes-home\skills\software-development\huiyue-visual-verification\scripts\vl-curl.mjs`）

## 任务：画师后台 + 管理后台全页面视觉/交互巡检

**巡检范围**（登录态，画师 alice + 管理员）：
1. 登录页（亮暗切换/主色/语言）
2. 仪表盘（8 区块布局/空白/对齐）
3. 排期看板（列表/日历/拖拽）
4. 订单管理（列表/筛选/分页）
5. 手动录单（三步骤/画风模式）
6. 订单详情（时间线/付款/交付）
7. 开稿管理（流程/状态）
8. 价格管理（档位/增项/画风/倍率/折扣）
9. 作品管理（网格/上传/分页）
10. 留言管理（列表/审核）
11. 设置（资料/模板/配色）
12. 偏好（主题/字号/快捷操作）
13. 管理后台（画师管理/埋点看板/平台管理/增项库）

**每页检查项**：
- 布局：横向溢出（`document.documentElement.scrollWidth > innerWidth`）、元素重叠、大段空白（`getBoundingClientRect` 定位 >200px 空隙）
- 视觉：错位、颜色不协调（截图 + curl VL 描述）
- 交互：按钮 hover/点击反馈、弹窗打开关闭、加载态（v-loading/骨架屏）
- 控制台：JS 错误、请求失败（page.on console/requestfailed 收集）

**交付**：
- 截图存 `workspace/temp/ui-audit-artist/`（每页 1 张，桌面 1440 + 移动 375）
- 问题清单 `docs/comms/02-to-01-画师后台UI巡检-交付-20260807.md`：按严重度分级（🔴 明显错位/交互失效 / 🟡 观感差 / 🟢 建议），每条附页面 + 截图路径 + 描述 + 初步定位（组件/行号）
- 只读巡检：**零代码改动**，commit 报告到分支

## 红线

- 只读：不改任何源码，不建数据（用现有 seed/生产容器 https://localhost 或隔离实例）
- 不删不改 demo 数据
- 报告「不夸大不编造」：每条结论注明「截图确认 / DOM 量化 / 静态分析」来源

## 门禁

- 报告落盘 Test-Path 确认存在（防幻觉完成）
- 截图数量 ≥ 页面数
