# 二号 → 一号：v0.15 批次3 提交审核（最后一批）

> 日期：2026-07-30

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/v015-frontend-b1`（worktree: `D:\Hermes Agent CN Desktop\workspace\artist-commission-wt-02`）

【任务编号】
v0.15 批次3：R51 截稿日/待办卡片 + R52 今日统计行 + OrderDetail/ManualOrder 截稿日设置

【修改模块】
仪表盘（Dashboard.vue）+ 订单详情（OrderDetail.vue）+ 手动录单（ManualOrder.vue）+ API 层 + i18n

【修改内容】

**R52 今日统计（紧凑行）**
1. Dashboard 4 统计卡上方加紧凑行：`今日新增订单 ¥XXX · 今日收入 ¥XXX`
2. 金额后端返分，前端 /100 显示元（`formatCents`），无数据显示 ¥0.00
3. 始终显示，不做响应式隐藏

**R51 截稿日卡片**
1. 独立卡片"⏰ 即将到期"：调 `getUpcomingDeadlines()`（7 天内到期 + 非终态，后端已排序）
2. 每条显示订单号 + 客户名 + 截稿日 + 剩余天数；≤3 天红色警示（左边框 + 天数红色）
3. 点击跳转订单详情
4. 无到期订单显示"近期无截稿 🎉"

**R51 今日待办卡片**
1. 独立卡片"📌 今日待办"：卡头显示 `todayTodoCount` 红色徽章
2. 列表口径与后端一致（C62）：pending + revision + 今日截稿，排除终态——前端从 `getOrders()` 全量列表过滤（`todoOrders` computed）
3. 每条显示订单号 + 客户名 + 状态标签；今日截稿的显示"今天截稿"红色标记
4. 点击跳转详情；无待办显示"暂无待办，喝杯茶吧 ☕"
5. 两个卡片始终显示（600px 以下单列堆叠，不隐藏）

**R51 截稿日设置**
1. OrderDetail 基本信息区加 el-date-picker（可清除，即时保存）：`deadlinePicker` computed 从 `order.deadline` 取前 10 位（YYYY-MM-DD），change 时调 `PUT deadline`（val 或 null），后端返回完整订单替换本地状态
2. ManualOrder 录单表单加截稿日字段（可选）——手动录单接口不支持 deadline 字段，创建成功后单独调 `updateDeadline` 写入；resetForm 清空

**i18n**
- 新增 12 键 × 2 语言：dashboard（todayNewOrders/todayRevenue/deadlineCard/noDeadlines/todoCard/noTodos/daysLeft/dueToday）+ orderDetail（colDeadline/deadlinePlaceholder/deadlineUpdated）+ manualOrder（deadline/deadlinePlaceholder）
- 新增 1 错误码 × 2 语言：`INVALID_DEADLINE`（对齐后端 errors.js）

【涉及文件】
- `web/src/views/artist/Dashboard.vue`（R52 + R51 两卡片）
- `web/src/views/artist/OrderDetail.vue`（R51 截稿日设置）
- `web/src/views/artist/ManualOrder.vue`（R51 录单截稿日）
- `web/src/api/index.js`（getUpcomingDeadlines + updateDeadline，隐含授权文件）
- `web/src/locales/zh-CN.js` / `web/src/locales/en.js`（i18n）

【是否修改非客户前端文件】
否。仅授权文件（6 个）。

【接口依赖】
- `GET /api/artist/stats` 新增 todayNewOrderCents/todayRevenueCents/todayTodoCount（三号已合入）
- `GET /api/artist/orders/upcoming-deadlines`（三号已合入）
- `PUT /api/artist/orders/:id/deadline`（三号已合入，ISO 8601 或 null）

【自测情况】
| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过（7.86s） |
| Rebase master | ✅ 无冲突（e10d6f1） |
| console.log / TODO / v-html | ✅ 无残留 |
| i18n 中英同步 | ✅ 12 键 + 1 错误码 × 2 |
| 改动文件范围 | ✅ 仅 6 个授权文件 |
| ad-hoc 验证脚本 | ✅ 61/61 PASS（hermes-verify-v015-b3.ps1，已删除） |

**需人工验证项**（无浏览器环境）：
- R52：仪表盘紧凑行显示今日金额（无数据 ¥0.00），跨天后正确切换
- R51：设了截稿日（3 天内）的订单出现在"即将到期"卡片，红色警示；点击跳转详情
- R51：pending/revision/今日截稿订单出现在"今日待办"，徽章数与列表一致
- R51：订单详情 date-picker 选择/清除截稿日即时生效；手动录单填截稿日后订单详情显示

【可能影响】
- Dashboard 新增 2 个 API 调用（upcoming-deadlines + getOrders），均静默失败不阻塞页面
- 不影响客户页面、管理后台、嵌入脚本

【待确认问题】
1. **R52 紧凑行无订单数**：指令格式为"¥XXX（N 单）"，但后端 stats 只返金额不返今日订单数。当前只显示金额。如需（N 单）需三号在 stats 加 todayNewOrderCount/todayRevenueCount 两个字段
2. **今日待办列表数据源**：用 `getOrders()` 全量列表前端过滤（默认 50 条/页）。活跃订单超 50 的画师可能漏掉部分待办——当前规模无风险，规模大了建议三号加专门的 todo 列表接口

【申请】
申请一号审核合并。v0.15 前端三批全部完成。
