# 一号 → 二号：v0.18 第二批——仪表盘前端 + 嵌入跳转

> 日期：2026-08-01
> 分支：`feat/client-frontend-v018-b2`（从 master 拉，独立 worktree）
> 验收标准：`docs/specs/plan-v018-dashboard-acceptance.md`

## 任务 A：仪表盘前端（主任务）

按验收标准 §1~§8 实现仪表盘页面重构：

1. 双栏布局（>768px 双栏，≤768px 单列）
2. 收入统计区（柱状图 + 维度切换 + 汇总 + 环比）
3. "现在要干什么"合并列表（排序由后端，前端渲染标签+跳转）
4. 快捷操作区（6 卡片固定，2×3 网格）
5. 最近活动流（10 条，相对时间）
6. 名额概览卡（复用 SPEC-004 数据，batch_limit=NULL 不渲染）
7. 统计卡片 4→3（移除"本月收入"，Q4 已定）
8. 状态切换位置调整（右栏）
9. 各模块独立骨架屏 + 错误态 + 空状态
10. i18n 中英双语

### 图表库

等三号评估回复后定。如果三号还没回复，先用纯 CSS 柱状图占位，后续替换。

### 授权文件

- web/src/views/artist/Dashboard.vue（重写）
- web/src/components/artist/dashboard/（新建目录，拆分子组件）
- web/src/locales/zh-CN.js / en.js（新增键）
- web/src/api/index.js（新增 dashboard API 调用）

## 任务 B：嵌入脚本改为跳转（小任务，用户已拍板）

用户决策：嵌入功能不补完 iframe，改为最简跳转。

1. 嵌入脚本（public/embed.js 或类似）：改为 `window.location.href = 画师公开页URL`
2. EmbedOrderPage.vue：删除或标记废弃
3. README 中嵌入相关宣传：改为"跳转链接"描述
4. CSP frame-ancestors：既然不用 iframe 了，保持 'self' 即可

### 授权文件（任务 B）

- public/embed.js（或 server/src/features/embed/）
- web/src/views/client/EmbedOrderPage.vue
- web/src/router/index.js（移除嵌入路由）
- README.md（嵌入章节）

## 任务 C：slotDisplay 补 3 模板（~0.5h，用户已拍板）

看板 UI = slotDisplay 补全（Q1 拍板结论）。folio 已有，补 classic/gallery/atelier：

- 客户主页显示名额文字状态（"开放中 · 剩 X 席""可候补""已接满"）
- 复用 SPEC-004 §3 规则，不显示进度条
- 授权文件：web/src/views/client/ArtistHomeClassic.vue / ArtistHomeGallery.vue / ArtistHomeAtelier.vue

## 注意

- 任务 B/C 独立于任务 A，可以先做（小快灵）
- 仪表盘等三号 API 合入后联调
- Q3（名额概览卡错误时）：静默降级（不显示卡片），已定
- Q5（默认面板入口）：不放入新布局，已定
