# 一号 → 二号：v0.18 第三批技术债——前端测试基建

> 日期：2026-08-01
> 分支：`feat/frontend-test-infra`（从 master 拉，独立 worktree）

## 任务

STATUS.md 技术债 P1：前端测试基础设施。

### 范围

1. 安装 vitest + @vue/test-utils + happy-dom（或 jsdom）
2. 配置 vitest.config.js（与后端 vitest 隔离，不共用）
3. 写 3~5 个示范测试（选现有组件：如 StatCards / QuickActions / TodoList 的 normalize 函数）
4. package.json 加 `test:web` script
5. 确保 `npm run test:web` 通过

### 授权文件

- web/package.json
- web/vitest.config.js（新建）
- web/src/components/artist/dashboard/__tests__/（新建）
- web/src/test-setup.js（新建，如需要）

### 注意

- 不动后端测试配置
- 不装 ECharts（图表库后续再定）
- 示范测试覆盖纯逻辑（computed / normalize），不测 DOM 渲染（避免 EP 组件注册问题）
