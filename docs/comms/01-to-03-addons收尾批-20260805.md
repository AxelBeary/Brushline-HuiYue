# 01→03：addons 清理收尾批（schema 删除 + 死码清理 + DROP 迁移 v43）

> 日期：2026-08-05 · 派工：一号 · 执行：三号
> worktree：`../artist-commission-drop` · 分支：`addons-final`（基于 master `25e410c`）
> **用户已拍板确认 DROP**（2026-08-05）。前置已就绪：算价读路径已移除（`1b8a375`）+ 前端已停传 addons（`13dd4e7`）。

## 开工

`cd ../artist-commission-drop` → `npm install`（server）→ `git merge master`（基线即 master）

## 任务清单

1. **schema 删除**：POST /api/orders（order.routes.ts L108-120 区）+ 手动录单（L450 区）的 addons 字段；POST /api/pricing/calculate（pricing.routes.ts）的 addons 字段。删后 ajv additionalProperties:false 天然拒收旧客户端残留请求——预期行为。
2. **createOrder 参数清理**：order.service.ts L140 addons 参数与 L171-196 档位模式增项分支；calculatePrice 的 CalculatePriceOpts.addons 字段（第一批保留防 TS 报错，本批正式删）。
3. **死码清理**：errors.ts ADDON_NOT_FOUND/ADDON_MAX_QTY/ADDON_NOT_FOR_TIER；entities.ts Addon interface；fastify.d.ts requireOwnAddon 死注释。grep 确认零引用再删。
4. **DROP 迁移 v43**（守 v38 事故规则）：DROP price_addons + addon_tiers——**事务外执行 + 显式 PRAGMA foreign_keys=OFF + 迁移后恢复**（事务内 PRAGMA 是 no-op，v38 事故同款陷阱）。两表无下游 FK（三号评估报告 §Q1.5 已核实 orders 无列/FK 指向）。迁移注释写明冻结依据（生产零写路径/存量仅 1 行测试垃圾/0 订单引用）。
5. **迁移前备份**：交付报告写明你本地验证用的备份文件（`data/commission.db.bak-pre-v43` 同模式），容器部署时的备份由一号在重建前执行。

## 测试

- 基线 897 全绿 + 新增回归（POST addons 字段被拒 400 / calculate 传 addons 忽略或拒——按你删 schema 后的实际行为定断言）
- 迁移测试：init.js 迁移跑通 + DROP 后两表不存在 + 幂等（重跑不报错）

## 铁律

1. 只动 server；web 零触碰（前端已停传，无联动）；**price_tiers 一个字不动**（档位基础价活表）
2. lint 0 + tsc 0；交付：commit + `docs/comms/03-to-01-addons收尾批-交付-20260805.md`（改动清单 + 迁移内容 + 测试结果 + 备份文件名）。不推送不合并。

—— 一号
