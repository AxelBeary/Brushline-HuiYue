# 二号 → 一号：v0.22 第 1 波交付（B1 核实 + A5 修复）

**分支**：`feat/v022-frontend`（worktree: `artist-commission-fe`）
**Commit**：`652a6cb`（已 rebase 到 master `fce6001`）
**路径说明**：派工授权列表用 `client/src/` 简写，实际路径为 `web/src/`

---

## A5 PaymentBar 暗色适配 ✅ 已完成

**涉及文件**：`web/src/components/artist/PaymentBar.vue`（+18/-6）

**改动**：
1. 段底/文字亮度从硬编码改 CSS 变量（`--seg-light` / `--seg-text-light`），浅色值不变（94%/92% 底，38%/35%/42% 文字）
2. `html.dark` 覆写：段底 22%，文字 72%（色相/饱和度不变，保持分段辨识色）
3. 暗色下移除 bar-track 阴影 + 手柄 hover 提亮

**是否修改非客户前端文件**：否

| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过（main 389.18 kB / gzip 145.74 kB，与基线一致） |
| vitest | ✅ 87/87 |
| console.log / TODO / v-html | ✅ 无残留 |
| 改动文件范围 | ✅ 仅授权文件 |

**需人工验证项**：
- 暗色模式下 PaymentBar 段底不刺眼、百分比文字清晰可读
- 浅色模式外观不变（亮度值等价）

---

## B1 瀑布流统一 ✅ 经代码核实已完成，无需改动

B1 在 v0.19 F2 已完成：
1. `TplGallery.vue:40` layout 默认已是 `'masonry'`
2. 4 模板均无 `layout=` 传参（grep 0 命中）→ 全部走 masonry
3. masonry CSS：`columns:2` + `break-inside:avoid` + img 无固定高度（不裁切），移动端 1 列
4. 4 模板无 `:deep(.tpl-gallery)` 覆盖，styles/ 无全局覆盖 → 4 模板一致

验收标准满足：竖图/横幅/混排不裁切，4 模板一致。

---

## T3/T5 回复 + 第 2 波核实（B2/B3 前端已完成）

核实 B1 时发现第 2 波前端任务已在 v0.19 F1/F3 完成：

| 派工项 | 实际状态 | 依据 |
|--------|----------|------|
| B2-FE 小公告 | ✅ 已完成 | TplAnnouncement.vue 存在；4 模板均已挂载（Classic 侧栏 L16 / Gallery 首屏左上 L6 / Folio 左栏 L33 / Atelier 首屏右下便签 L6）；Settings.vue 有编辑入口+过期日期 L180-195 |
| B3-FE 点赞 | ✅ 已完成 | ArtworkLikeButton.vue 存在（心形动画+localStorage 去重+计数）；TplGallery L21-27 嵌入；API 方法存在 api/index.js:86-87 |
| T3 公告位置 | ✅ 已拍板已实现 | 见上表各模板位置注释 |
| T5 0 赞显示 | ✅ 已拍板已实现 | ArtworkLikeButton.vue:8 "T5（用户拍板）：0 赞不显示数字"，L23 `v-if="count > 0"` |

**建议**：一号核实 B2/B3 后端 API（点赞端点、公告字段）是否已合入——若后端也完成，第 2 波可直接标完成。

---

## 第 3 波状态

A1（Sentry 前端 SDK）/ A2（E2E 接入 CI）/ A4（EP CSS 按需引入）随时可开工，等待派工。

申请一号审核合并（A5 commit `652a6cb`）。
