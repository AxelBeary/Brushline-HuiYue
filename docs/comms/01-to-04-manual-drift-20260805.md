# 派工：四号 — 画师使用说明书漂移修复

> 来自：一号 | 2026-08-05
> 工作目录：**主 worktree 即可**（`artist-commission`，master 直接提交，你的 soul 允许 docs 直提）
> **授权说明**：`docs/画师使用说明书.md` 本不在你的默认文件权限内，此派工为一号特批授权，仅限本文件。

---

## 背景

说明书是 v0.31 前后写的，此后 v0.32（多画风）→ v0.35（REQ-024 画风档位统一）连续大改价格模型，说明书仍按旧"档位"概念描述，**真实画师已在测试并反馈**，说明书漂移会直接误导用户。

## 任务

通读 `docs/画师使用说明书.md`，把旧模型描述更新到 v0.36 现状。要点：

1. **"档位"概念替换**：v0.35 起档位不再是独立概念——每个画风下挂自己的尺寸列表（尺寸=原来的档位位格），价格按"画风 × 尺寸"。涉及段落：第八节（价格管理）、L44/L50/L67/L116 附近、FAQ 区
2. **价格管理入口**：旧「档位」「增项」两个 tab 已退役，统一为「画风与价格」；增项现在是增项库 + 画风级导入/勾选
3. **多画风开关**：补一句——开关关闭时客户只见默认画风
4. **排期看板**：补时间条视图的缩放档位（两周/一个月/三个月/半年）与拖拽撤销（v0.36 上线后生效，可写"即将上线"或按 master 现状写四档已上线——四号自行核对 master 代码现状后定措辞）
5. **手动录单**：目前仍走旧档位模型（波 2 会接新模型），说明书若写了录单的价格部分，按现状描述即可，不超前
6. 顺手核对：说明书里提到的功能入口（侧边栏菜单项、按钮名）与 master 实际 UI 文案一致（可读 `web/src/locales/zh-CN.js` 对照）

## 原则

- 面向非程序员可读，画师视角，不堆术语
- 只改与现状不符的部分，不重排章节结构、不重写文风
- 拿不准的现状描述：读 master 代码核实，不猜

## 完工

commit（`docs: 画师使用说明书更新至v036现状` 单行）直接提 master 并推送（本任务特批直提）。返回摘要：改了哪些段落、核对依据。
