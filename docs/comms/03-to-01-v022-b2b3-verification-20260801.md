# 三号 → 一号：B2/B3 后端验证结果（2026-08-01）

## 结论：B2/B3 后端已在 master 完整实现，无需新增代码

### 验证详情

逐项核实派工要求与 master 现有代码：

| 派工要求 | master 现状 | 文件位置 |
|----------|-------------|----------|
| artists 表加 announcement + announcement_expires_at | ✅ 迁移 v21 `announcement_and_like_count` 已加 | init.js L870-900 |
| artworks 表加 like_count | ✅ 同 v21 迁移 | init.js L870-900 |
| 画师设置 API 扩展（PUT 含公告字段） | ✅ PUT /api/artist/profile schema 含 announcement + announcementExpiresAt | artist.routes.js L158-159 |
| 公开 API 返回公告（含过期判断） | ✅ GET /api/artists/:subdomain 返回 `announcement: getAnnouncement(artist)` | artist.routes.js L73 |
| POST /api/public/artworks/:id/like (+1) | ✅ 已实现 | artist.routes.js L445-449 |
| DELETE /api/public/artworks/:id/like (-1) | ✅ 已实现，不低于 0 | artist.routes.js L452-456 |
| 不存访客身份 | ✅ 无身份存储，纯计数 | artist.service.js L489-504 |
| 测试 | ✅ announcement-like.test.js 10 条全绿 | tests/announcement-like.test.js |

### 迁移版本号说明

派工写"迁移 v24/v25"，但实际这些列在 **v21**（v0.19 时期）就已加入。v24/v25 不存在也不需要。我一度按派工加了 v24/v25，发现重复后已回滚，worktree 无残留变更。

### 当前状态

- worktree `artist-commission-be`（feat/v022-backend）：干净，无未提交变更
- 第 1 波无需任何代码变更

### 请示

1. 第 1 波既然无需变更，是否直接进第 2 波（A3: TS 迁移第二批——路由层 + 服务层）？
2. 第 3 波（T1 点赞模型确认 + T2 限流策略）是否现在一并回复？
