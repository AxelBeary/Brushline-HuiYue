# 二号 → 一号：v0.18-b1 前端提交（节点话术）
> 日期：2026-08-01
> 分支：`feat/client-frontend-v018-b1`（commit 66aca7c）
> worktree：`artist-commission-v018-b1`

## 做了什么

### 任务 A：节点编辑页——话术输入框 + 变量标签

- `StageListView.vue`：每个节点行下方新增话术编辑区
  - 9 个变量标签（{客户名} {客户QQ} {订单号} {档位名} {节点名} {截稿日} {总价} {已付} {待付}），点击插入光标位置（无焦点则追加末尾）
  - textarea（2~4 行自适应，500 字上限），placeholder 显示默认话术
  - 修改后出现"保存话术"按钮，PUT `speechTemplate` 字段
  - template 模式（管理员默认模板）readonly，不显示话术区
- `WorkflowPaymentEditor.vue`：新增 `onUpdateSpeech` 处理 + template 模式映射 `speech_template` 字段

### 任务 B：订单详情页——"客户沟通"小块

- `OrderDetail.vue`：附加工作项卡片下方新增"💬 客户沟通"卡片
  - QQ 号展示
  - 价格小结：总价/已付/待付（后端 `totalPriceCents/paidCents/unpaidCents` 优先，缺失时从 `installments` 本地计算兜底，再缺失显示"—"）
  - 话术预览：直接显示后端 `speechText`（已替换变量）；无当前节点时提示"未接入流程节点"，有节点但无话术时提示"暂无话术"
  - "复制文案并唤起QQ"按钮：复制 speechText → 提示"已复制节点文案，正在唤起QQ" → 1 秒后 `tencent://message/?uin=`
  - 无 clientQq 时按钮禁用显示"未设置客户QQ"；剪贴板不可用时降级为展示文本

### i18n

- zh-CN.js / en.js 各新增 11 个键（`workflow.speech*` 5 个 + `orderDetail.comm*` 6 个），只加不改成文
- 变量标签 {客户名} 等为后端契约，中英文界面均保持中文原文（已加注释说明）

## 改动文件（5 个，全部在授权范围内）

| 文件 | 改动 |
|------|------|
| `web/src/components/artist/StageListView.vue` | +129/-49 话术编辑区 |
| `web/src/components/artist/WorkflowPaymentEditor.vue` | +14/-1 onUpdateSpeech + speechTemplate 映射 |
| `web/src/views/artist/OrderDetail.vue` | +109 客户沟通卡片 |
| `web/src/locales/zh-CN.js` | +10/-1 新增键 |
| `web/src/locales/en.js` | +10/-1 新增键 |

api/index.js 无需改动（复用现有 `updateStage(id, data)`）。

## 验证结果

- `npx eslint . --max-warnings 0`：零错误零警告 ✅
- `npm run build`：通过 ✅
- `git diff --stat`：仅 5 个授权文件 ✅

## 并行期注意事项（给一号/三号）

1. **PUT workflow/:id schema 是 `additionalProperties: false`**：三号需在 schema 加 `speechTemplate: { type: 'string', maxLength: 500 }`，否则并行期保存话术会 400
2. **订单详情新字段**：`speechText`、`totalPriceCents`、`paidCents`、`unpaidCents` 缺失时前端已做回退，不会显示 undefined
3. 后端合入后需联调验证：话术保存→订单详情 speechText 显示→复制唤起 QQ 全链路
