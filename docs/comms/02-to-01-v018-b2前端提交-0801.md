# 二号 → 一号：v0.18-b2 前端提交（仪表盘重构 + 嵌入改跳转）
> 日期：2026-08-01
> 分支：`feat/client-frontend-v018-b2`（commit 762ac1b + 联调修复 a9e59b2，已 rebase master）
> worktree：`artist-commission-v018-b2`

## 任务 A：仪表盘重构

### 改动文件

| 文件 | 改动 |
|------|------|
| `web/src/views/artist/Dashboard.vue` | 重写：双栏布局容器 + 状态切换逻辑（P1-6 回滚保留） |
| `web/src/components/artist/dashboard/GreetingHero.vue` | 新建：问候区（R7 换一句）+ 今日统计行（R52） |
| `web/src/components/artist/dashboard/RevenueChart.vue` | 新建：纯 CSS 柱状图 + 月/季/年切换 + 汇总 + 环比 |
| `web/src/components/artist/dashboard/StatCards.vue` | 新建：统计卡片 ×3（移除本月收入，Q4 已定） |
| `web/src/components/artist/dashboard/TodoList.vue` | 新建："现在要干什么"合并列表（标签+跳转，排序由后端） |
| `web/src/components/artist/dashboard/QuickActions.vue` | 新建：6 卡片 2×3 网格 |
| `web/src/components/artist/dashboard/StatusSwitch.vue` | 新建：状态切换（位置调整到右栏） |
| `web/src/components/artist/dashboard/ActivityFeed.vue` | 新建：最近活动流（10 条 + 相对时间） |
| `web/src/components/artist/dashboard/SlotOverview.vue` | 新建：名额概览卡（复用 SPEC-004 队列查询） |
| `web/src/api/index.js` | +4：3 个 dashboard API（隐含授权，注明） |
| `web/src/locales/zh-CN.js` / `en.js` | 各 +22 键，只加不改成文 |

### 布局实现

- 宽屏（>768px）：CSS Grid 显式行列分配，左 60%（3fr）/ 右 40%（2fr）
- 窄屏（≤768px）：flex 单列，DOM 顺序 = 验收 6.6 顺序
- 各模块独立加载/独立失败/独立重试，互不阻塞

### 图表库

三号未回复选型，按派工指示用纯 CSS 柱状图占位。后端合入后可直接替换 RevenueChart 内部实现，接口不变。

### 后端字段兼容（已联调对齐 ✅）

三号 API 合入后 rebase master，对照 `dashboard.service.js` 实际返回修复 3 处不匹配（commit a9e59b2）：

| 组件 | 后端实际字段 | 修复 |
|------|-------------|------|
| RevenueChart | `summary.completedCount` / `summary.changePercent` | 回退链补上；prevLabel 后端未返回，前端按维度计算上一周期名称 |
| TodoList | `tag` 为中文文本（'逾期'等） | TAG_KEY_MAP 映射为英文 i18n 键，CSS class 同步英文化 |
| ActivityFeed | `content` | 加入回退链首位 |

## 任务 B：嵌入脚本改跳转

| 文件 | 改动 |
|------|------|
| `web/public/embed.js` | 重写：点击按钮 → `window.location.href = BASE_URL + '/artist/' + ARTIST`（移除 iframe 弹窗逻辑） |
| `web/src/embed/EmbedOrderPage.vue` | 标记废弃注释 |
| `README.md` | 嵌入描述改为"跳转模式" |

## 需要一号处理的事项

1. **vite.config.js 移除 embed 入口**（权限外）：`rollupOptions.input.embed` 仍指向 embed.html，移除后可整体删除 `web/embed.html` + `web/src/embed/` 目录。当前 embed.html 仍会构建但不再被 embed.js 引用，不影响功能。
2. **router/index.js 无嵌入路由**：派工中"移除嵌入路由"项实际不存在（嵌入是独立 vite 入口，不走 router），无需操作。
3. **验收标准 §3.3 路由偏差**："图库管理"写的是 /gallery，实际路由是 /artworks，已按实际路由实现。
4. **CSP frame-ancestors**：派工说保持 'self' 即可，未改动。

## 验证结果

- `npx eslint . --max-warnings 0`：零错误零警告 ✅
- `npm run build`：通过 ✅
- `git diff --stat`：仅授权文件 ✅
