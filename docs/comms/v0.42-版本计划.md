# v0.42 版本计划（下次开工执行总纲）

> 编制：一号（2026-08-07 v0.41 收官后）
> 前置：v0.41 已全部合入 master（`baee2ed`），npm 0.41.0 已对齐。**开工第一步：容器 v0.41 收官重建**（master 落后容器 8+ 批）。

## 〇、开工顺序总览

```
Step 1 容器 v0.41 收官重建（一号，runbook）
Step 2 动画修复批（二号）
Step 3 UI 优化批 P0（五号）——与 Step 2 并行（不同 worktree）
Step 4 UI 优化批 P1（二号接力/五号接力）
Step 5 画师统计页前端 + 埋点三态前端（二号）
Step 6 作品分页前端（五号）
Step 7 repowiki 外部 13 处（四号，路线 B 仓库内维护）
Step 8 v0.42 收编 + 重建 + 截图验收（一号）
```

## 一、Step 1：容器 v0.41 收官重建（前置，必须最先）

- 按 `multi-role-lead-review-workflow` 的 container-rebuild-runbook 执行：
  - 备份（VACUUM INTO，.cjs + /app/server 跑）→ 验证（integrity + 行数）
  - build + up（background + notify）
  - 三层验证：health / 新端点（anon-token）/ 迁移 v45 表 / dist 含新前端（TrackingAnalytics/ManualOrder 拆分产物）
  - **AUTH_DEV_MODE 保持开**（A 测启动才关，需用户拍板）
- 目标：容器 = master `baee2ed`（v0.41 全部）

## 二、Step 2：动画修复批（二号，最高优先）

> 蓝本：`docs/comms/04-to-01-动画核查-交付-20260807.md`（子代理逐条核实版）+ 外部 loading-animations.html（仅采用 9 个）

### H 级（4 项，先修）
- **H1** templates.css:23/39/192 滚动渐入去回弹：`tpl-fade-up 0.6s var(--ease-bounce)` → `.42s cubic-bezier(.165,.84,.44,1)` + 位移 24→12px
- **H2** artist-tokens.css:246 全局 `* 0.35s` 过渡 → 只挂容器（`html[data-theme-switching]` 方案，ThemeToggle 点击时加类 400ms 后移除）
- **H3** OrderTimeline tl-pulse box-shadow → 伪元素 scale/opacity（GPU）
- **H4** TplGallery:681-686 骨架 background-position → translateX 版

### M 级（3 项）
- **M1** 网格滚动渐入交错：`--i` CSS 变量 60ms 递增延迟（useScrollReveal 或模板注入）
- **M2** transition:all 13 处显式列举（改 transform/box-shadow 等具体属性）
- **M3** OrderList/QueueBoard/ArtworkManage 遮罩 → 订单卡片骨架屏

### L 级（2 项）
- **L1** theme.css:45 字体栈 Noto Sans SC 提到首位（+ swap 已有）
- **L2** OrderTimeline 0.3s → 0.15s

### 动画移植（9 个，克制优先）
骨架屏微光 / 订单卡片骨架（补 M3）/ 旋转圆环（按钮加载）/ 脉冲圆点（修 H3）/ 墨滴扩散（品牌）/ 打字指示器 / 波浪点阵 / 进度条纹 / 印章落定（SealStamp 已有，扩触发）
**禁止**：3D 翻转/DNA/沙漏/牛顿摆（AI 味）

### 门禁
web test 215/215 + lint 0 + build + **before/after 截图**（visual 门禁）

## 三、Step 3+4：UI 优化批（五号 P0 → 接力 P1）

> 蓝本：`C:\Users\qly19\Desktop\ui-optimization-guide.md`（已核实，勘察质量高，行号引用准）

### P0（半天）
- §2.2 OrderForm 移动端粘性操作条（总价+下一步钉底，`mobile-cta-bar` sticky bottom + safe-area）
- §2.1 步骤指示器移动端保留当前步 label（`.step-label--on` + step-eyebrow）
- §5.1 OrderList 移动端卡片视图（≤768px 隐藏表格渲染卡片，复用 status/prio 标签）
- §1.1 间距 token 体系（--sp-1..7 + --fs-* 追加 :root）

### P1（1-2 天）
- §2.3 校验失败去 alert → 内联 + scrollToField + 轻提示
- §2.4 需求留空 confirm → 内联提示 + "仍然继续"
- §6.2 TrackOrder 3 秒强制弹窗 → 内联空态
- §6.1 查单状态 Hero（状态徽章 + 进度）
- §4.1 仪表盘信息优先级（待办提第 2、门面统计沉底右栏）
- §3 后台五细节（13.5→14px / 10.5px 小字 / 折叠按钮热区 / main-content max-width / 移动端返回）
- §6.3 付款进度 2×2 网格 / §6.4 查询表单优化

### 暂缓（等 A 测反馈）
§1.2 客户端纸墨化大改 / §7 模板三条 / §8 看板头部合并

## 四、Step 5：画师统计页前端 + 埋点三态前端（二号）

- **画师统计独立页**（用户拍板：侧边栏「门面」组导航项 + 独立页做全：总览/按日趋势/事件明细）
  - ArtistLayout BASE_MENU_ITEMS 加 `{ index: '/stats', group: 'front' }`（门面组）
  - 新路由 /stats + 新页面（byDay 已后端就绪，byName 全列表）
  - Dashboard 移除「我的数据统计」区块（还给清爽）
- **埋点三态前端**（用户拍板：关/不显/开，默认「不显」）
  - TrackingAnalytics 开关控件改三态（el-radio-group：关/不显/开）调 PUT `/api/admin/tracking-config`（statsMode）
  - 画师侧按 `mode==='on'` 才显示（三号后端已返回 mode 字段）
- 注意：三号后端已把接口从 artistStatsVisible 改为 statsMode，前端契约同步

## 五、Step 6：作品分页前端（五号）

- 画师端 ArtworkManage：调 `/api/artist/artworks/paged`（20/页 + el-pagination + 封面置顶）
- 客户端 4 模板：调 `/api/public/artworks/:artistId`（10/页 + 加载更多按钮；先 T2 gallery 改通，其余模板照抄）
- 后端已就绪（三号 c3be328）

## 六、Step 7：repowiki 外部 13 处（四号）

- 按「路线 B（仓库内维护）」执行：13 处 🟡（认证 6 + 非认证 7）在 docs/external-wiki/ 维护修订版
- 清单见 `docs/comms/04-to-01-repowiki-P0重写-交付-20260807.md` 第三节
- 外部原文（C:\Users\qly19\Desktop\repowiki\）一字不动

## 七、Step 8：v0.42 收编

- 全部合入后：changelog v0.42 + npm 0.42.0（双轨对齐）
- 容器重建（v0.42 内容）+ 四场景截图验收（下单/查单/主页/后台并排）
- 待办池清理（合入即删）

## 八、版本范围（v0.42 收编内容预估）

- 动画修复批（H1-H4/M1-M3/L1-L2 + 9 动画）
- UI 优化批（P0+P1）
- 画师统计独立页 + 埋点三态前端
- 作品分页前端
- repowiki 外部修补
- 若有余力：三巨头剩余（OrderForm 客户端 59KB 拆分）

## 九、风险与注意

- **不并行拆组件**（施工单纪律）：OrderForm 拆分与 UI 优化批不同时动同一文件
- 动画/UI 批**必须附截图**（visual 门禁：无截图不通过）
- i18n 键 zh/en 同步；禁新硬编码中文（check-i18n 门禁已上线）
- 任何角色交付前 merge master（防旧基线丢修复——v0.41 教训③）
