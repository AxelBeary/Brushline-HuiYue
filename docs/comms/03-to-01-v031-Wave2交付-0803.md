# 三号交付：v0.31 Wave 2（2026-08-03）

> 分支：`feat/v0.31-backend-0803`
> Worktree：`../artist-commission-wt-be`
> 基于：`763a9cc`（rebase 后）

---

## 提交清单（3 个 commit）

| commit | 内容 |
|--------|------|
| `120adce` | F3 折扣码：迁移v32 + CRUD + 下单应用 + 画师管理UI + 改价原因必填 |
| `3245f9d` | F4 节点收款重做：迁移v33 + 节点维度已收/应收/差额 + 快捷收款 + 改价加钱联动 |
| `1b7a381` | 留言 language 字段：迁移v34 + 后端写入 + GET ?language= 过滤 |

---

## F3 总价可改 + 折扣码

### 后端
- 迁移 v32：`discount_codes` 表 + `artists.discount_enabled`（默认关）+ `orders.discount_code_id/discount_amount_cents`
- `discount.service.ts`（新文件）：CRUD + validateDiscountCode + computeDiscountCents + incrementUsage + 开关
- 画师端路由 5 条（GET/POST/PUT/DELETE + toggle），客户端验证 1 条（POST /api/public/validate-discount），全带 JSON Schema
- `createOrder` 接入折扣码：先倍率后折扣（REQ-023 已定），事务内 incrementUsage
- 客户下单 + 手动录单均接受 `discountCode` 参数
- `getPublicPricing` 返回 `discountEnabled`（客户端据此显示/隐藏折扣码输入框）
- 改价原因改必填（OrderDetail 弹窗 + 提交按钮 disabled 校验）

### 画师前端
- `DiscountCodeManager.vue`（新组件）：全局开关 + 表格列表 + 创建/编辑弹窗
- TierManage.vue 加"折扣码"Tab
- 前端 API 5 条方法
- 中英文翻译（discount 区块 + tiers.tabDiscount）

### 客户端
- 折扣码输入框归二号（OrderForm），后端 API 已就绪

---

## F4 节点收款重做

### 后端
- 迁移 v33：`order_payment_installments.paid_cents` + `order_payments.installment_id`
- `addPayment` 支持 `installmentId`：关联节点 + 更新 paid_cents + 自动标记已付清
- `getOrderInstallments` 返回节点维度：id/name/amountCents/paidCents/remainingCents/status
- `recalcInstallmentAmounts`（新函数）：总价变更后按 basis_points 比例重算节点应收
- 改价（updateFinalPrice）+ 加钱（addExtraItem/deleteExtraItem）后自动调 recalcInstallmentAmounts
- POST /payments 路由接受 installmentId，返回 installments

### 画师前端
- OrderDetail 收款区重做：每节点显示已收/应收/差额 + 状态标签 + 快捷收款按钮
- 节点快捷收款弹窗（预填差额金额）
- installmentRefs 改为直接读后端数据（不再前端推算）
- useOrderPayments 支持 installmentId
- 中英文翻译（payNode* 键）

---

## 留言 language 字段（REQ-021 F8 前置）

- 迁移 v34：`guestbook_messages.language TEXT DEFAULT 'zh-CN'`
- `createMessage` 接受 language 参数（后端写入，不靠前端检测）
- POST 路由 schema 加 language 字段
- GET 公开留言支持 `?language=` 查询参数（正则校验 `[a-zA-Z-]{2,10}`）
- 返回体加 language 字段

---

## 迁移汇总

| 版本 | 名称 | 内容 |
|------|------|------|
| v32 | discount_codes | 折扣码表 + artists.discount_enabled + orders 折扣字段 |
| v33 | installment_paid_cents | 节点 paid_cents + 流水 installment_id |
| v34 | guestbook_language | 留言 language 字段 |

全部幂等（PRAGMA 检查 / CREATE IF NOT EXISTS），带 DEFAULT。

---

## 接口变更

| 路径 | 变更 | 兼容性 |
|------|------|--------|
| POST /api/orders | 新增可选 discountCode | ✅ 向后兼容 |
| POST /api/artist/orders/manual | 新增可选 discountCode | ✅ 向后兼容 |
| POST /api/artist/orders/:id/payments | 新增可选 installmentId，返回加 installments | ✅ 向后兼容 |
| GET /api/public/pricing/:subdomain | 返回加 discountEnabled | ✅ 只增 |
| GET /api/public/artist/:subdomain/messages | 支持 ?language= 过滤，返回加 language | ✅ 向后兼容 |
| POST /api/public/artist/:subdomain/messages | 新增可选 language | ✅ 向后兼容 |
| GET/POST/PUT/DELETE /api/artist/discount-codes/* | 新增（6 条） | 新接口 |
| POST /api/public/validate-discount | 新增 | 新接口 |
| PUT /api/artist/artworks/cover-order | 新增（Wave 1） | 新接口 |

---

## 待 Wave 3

- F5 下一节点应收（依赖 F4 ✅）
- 操作日志后端（REQ-021 F1）

等一号派工。
