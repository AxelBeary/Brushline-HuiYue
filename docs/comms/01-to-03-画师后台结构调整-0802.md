# 三号派工：画师后台结构调整批次（2026-08-02）

> 分支：`feat/artist-structure-0802`
> Worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-03`
> 交付 comms：`03-to-01-画师后台结构调整-0802.md`

---

## 任务清单（按优先级）

### 1. #44 偏好独立导航（D 类，核心结构改动）

**用户拍板**：偏好从主页设置拆出，独立为一级导航。主页对外（客户看到的），偏好对内（画师自己的工作习惯）。

**实施**：
- `web/src/views/artist/Settings.vue`：拆分为两个页面/路由
  - **主页设置**（对外）：画师资料、头像、公告、外链、约稿须知、模板选择、封面管理
  - **偏好设置**（对内）：默认面板、焦点图大小、视图模式、主题、语言、通知偏好等
- `web/src/router/index.js`：新增 `/preferences` 路由
- `web/src/components/ArtistLayout.vue`：侧边栏新增「偏好」一级菜单项（图标用 Setting 或 Operation）
- 原 Settings.vue 中属于"偏好"的 Tab/区块迁移到新页面

**判断标准**（哪些归偏好）：凡是"画师自己用、客户看不到"的设置 → 偏好。凡是"客户在主页能看到的" → 主页设置。

**验证**：侧边栏出现两个入口，各自内容正确，路由切换正常，localStorage 偏好键不受影响。

### 2. #7 + #8 录单入口归位（D 类，极小）

- **#7**：`web/src/views/artist/OrderManage.vue`（或订单列表页）——删除页内「手动录单」按钮/入口
- **#8**：`web/src/components/ArtistLayout.vue` 侧边栏——「工作」分组下加回「录单」菜单项（路由指向已有的录单页 `/orders/new` 或 `/manual-order`）

搜 `manual` 或 `录单` 确认现有路由路径。

### 3. #2 名额概览跳转修正（A 类，极小）

**位置**：`web/src/components/artist/dashboard/SlotOverview.vue`——整卡点击事件。

**修复**：点击跳转目标从 `/settings`（主页设置偏好）改为 `/slots`（额度管理）或正确的额度管理路由。搜 `slots` 在 router 中确认路径。

### 4. #47 侧边栏折叠文字裁切（A 类）

**位置**：`web/src/components/ArtistLayout.vue` 折叠模式样式。

**问题**：折叠后菜单文字裁切错误，半个字漏出来。

**修复**：折叠模式下菜单文字应完全隐藏（`overflow: hidden; width: 0` 或 `display: none`），不露出半个字。检查 EP `el-menu` 的 `collapse` 属性是否正确启用（`el-menu :collapse="isCollapsed"` 应自动处理文字隐藏——如果没生效检查 CSS 覆盖）。

### 5. #48 折叠后主题切换入口消失（A 类）

**位置**：`web/src/components/ArtistLayout.vue` 侧边栏底部/hover 菜单。

**问题**：折叠后 hover 菜单只有 Alice/可约/退出，缺主题切换。

**修复**：在折叠模式的 hover 下拉菜单中补上主题切换入口（复用展开模式的主题切换逻辑）。

### 6. #63 管理端 JSON 面板暗色不可读（A 类）

**位置**：管理端自检/健康检查页面（搜 `health` 或 `json` 在 admin 目录）。

**修复**：JSON 详情面板改为 `background: var(--bg-card); color: var(--text-primary)`（跟随暗色主题），不硬编码白底灰字。

### 7. #68 管理端顶部 Tab 导航（D 类）

**用户拍板**：管理端子页面无导航栏，改为顶部 Tab 轻量导航。

**实施**：
- 搜 admin 路由（`/admin/*`）确认有哪些子页面（仪表盘、画师管理、留言管理、回收站、自检等）
- 在管理端布局壳（搜 AdminLayout 或 admin 的父路由组件）顶部加 `el-tabs`（或纯 CSS tab），路由切换
- 不用侧边栏（管理端页面少，顶部 Tab 够用）

**验证**：Tab 切换路由正常，刷新保持当前 Tab，移动端 Tab 可横向滚动。

---

## 授权文件范围

- `web/src/views/artist/Settings.vue`（拆分）
- `web/src/views/artist/Preferences.vue`（新建）
- `web/src/views/artist/OrderManage.vue`（#7 删按钮）
- `web/src/components/ArtistLayout.vue`（#8 菜单、#44 导航、#47 折叠、#48 主题）
- `web/src/components/artist/dashboard/SlotOverview.vue`（#2 跳转）
- `web/src/views/admin/`（#63 暗色、#68 Tab）
- `web/src/router/index.js`（新路由）
- `web/src/locales/zh-CN.js`、`web/src/locales/en.js`

## 约束

- 不改 server/（后端由五号负责）
- 不改客户端页面（二号所有）
- 不改 Tpl*.vue 共享组件
- #44 拆分时保持所有现有功能可用——只是搬位置，不删功能
- 偏好页面的 localStorage 键名保持不变（`queue_view_mode`、`queue_focus_display` 等），只搬 UI 不改键
- 每个任务独立 commit，message 格式 `feat(artist): #编号 描述` 或 `fix(artist): #编号 描述`
