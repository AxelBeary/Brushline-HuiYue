# 03-to-01-管理后台仪表盘-交付-20260808.md

> 交付：三号 → 一号
> 分支：`beta/admin-dashboard`，commit `a599ce9`（未推送未合并）
> 派工：`03-派工-管理后台仪表盘-20260808.md`（基线 master `4b23caf`）

## 重排方案

### AdminDashboard.vue（主页）—— 双栏仪表盘化
- **布局对标画师后台 Dashboard**（`.dash-grid` 宽屏 3fr/2fr，≤768px 单列，`--sp-4` 16px 统一间距）
- **左栏（3fr）**：统计卡 3 张一行 + 画师列表（保留全列表）
- **右栏（2fr）**：快捷操作卡（原横条改为卡片内按钮组）+ 留言管理卡（原表格 max-height=400 防撑高）
- 页头保留（标题+副标题）；回收站卡片整体移除
- 布局层次：页头 → 双栏（统计/列表 | 操作/留言），区块分组清晰、对齐一致、间距统一

### ArtistManage.vue（画师管理页）—— 回收站迁入
- 操作条新增「回收站」按钮（添加画师/更换管理员/回收站 三按钮一排）
- 点击打开回收站弹窗（720px）：分页列表（文件/路径/大小/移入时间）+ 清空按钮 + 空态 + REQ-022 F4 分页，**全部逻辑从主页原样搬运，零改动**

## 功能零变化核对
| 功能 | before | after |
|------|--------|-------|
| 统计卡 3 张 | 主页单列 | 主页左栏（逻辑同） |
| 快捷操作 4 项 | 主页横条 | 主页右栏卡片（同 4 项） |
| 画师列表 | 主页 | 主页左栏（同 API） |
| 回收站（列表/分页/清空） | 主页卡片 | ArtistManage 弹窗（同 API 同逻辑） |
| 留言管理（筛选/删除） | 主页 | 主页右栏（同 API 同逻辑） |
| i18n | — | 全部复用现有键，zh/en 无新增、无硬编码 |

## 验证结果
- `npm run test:web` → **215/215 passed**（13 files）
- `npm run lint` → **0 error**
- `npm run build` → **成功**（✓ built in 12.67s）
- `npm run check:i18n` → **OK，无新增硬编码中文**
- `git diff --stat` → 仅授权文件：AdminDashboard.vue（重排）、ArtistManage.vue（+91）
- 代码验证：AdminDashboard 无任何 recycle 功能代码残留（仅 1 处注释说明）；ArtistManage 回收站函数齐全（openRecycleBin/loadRecycleBin/handleEmptyRecycleBin/formatSize）

## 截图（workspace/temp/admin-dashboard/）
本地独立环境实测（server:3998 + vite:5174 + 独立 DB seed + TOTP 登录 admin 10003，未碰生产容器 3000）：
- `before-dashboard-light.png` / `before-dashboard-dark.png` — 旧主页（单列堆叠，回收站卡片在列）
- `before-artists.png` — 旧画师管理页（无回收站入口）
- `after-dashboard-light.png` / `after-dashboard-dark.png` — 新主页（双栏仪表盘，回收站已消失）
- `after-recycle-bin.png` — 新画师管理页回收站弹窗
- 全部 1440×900；PIL 量化：非空白（std 18~55），before/after 亮度差异 8.62（布局确实变化）

## 自评
- 用户点名痛点已消除：主页不再「乱七八糟」（5 大块 → 2 栏分组）、回收站不再塞主页（迁至画师管理页）
- 布局「用心」维度：双栏对齐画师后台节奏、卡片分组、间距统一用 token（--sp-4/--r-l/--fs-*）、深浅双主题自适应
- 风险：低——纯前端布局重排 + 位置迁移，无 API/数据改动
- 遗留：`stats` 接口仅 3 字段（无今日新增等运营指标），按派工「无则不加」未加卡
