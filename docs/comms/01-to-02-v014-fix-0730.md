# 一号 → 二号：审核修复 + R46 延后 + 合并准备

> 日期：2026-07-30

---

## R46 延后 v0.15

用户拍板延后。备注删除接口不存在，v0.14 不做。

## 审核结论：7 项通过，3 处需修

R39/R42a/R42b/R44/R43 全部通过。R45/R41 有 3 个体验问题，修完即合并：

### 必修（3 项）

| # | 文件 | 问题 | 修法 |
|---|------|------|------|
| 1 | ArtworkManage.vue | 滑块对话框关闭后进度残留 | el-dialog 加 `@closed="slideProgress = 0"` |
| 2 | ArtworkManage.vue | 批量删除期间无 loading，可重复点击 | 加 `batchDeleting` ref，入口 true / finally false，按钮 `:loading` |
| 3 | OrderDetail.vue | 备注拖拽 dragleave 闪烁（子元素触发） | 用 dragenter/dragleave 计数器，或检查 `e.relatedTarget` 是否在容器内 |

### 顺手清理（2 项，不阻塞）

| # | 文件 | 问题 |
|---|------|------|
| 4 | zh-CN.js / en.js | 死 key `galleryPreview`（🔍 按钮已删，无引用） |
| 5 | Dashboard.vue | 两处 `/manual-order` 引用改为 `/orders?action=manual`（授权你改此文件） |

## 修完后

一个 commit：`fix(client): v0.14审核修复 — 滑块进度重置+批量loading+dragleave防闪+死key清理`

交一号，我合并 7+1 项到 master。
