# 04-to-01：REQ-036 增项交互优化落档转交（2026-08-08）

> 四号转交一号。用户已拍板定稿，请拆施工图派工。

## 交付物

1. **需求文档**：`docs/requirements/REQ-036-增项交互优化.md`（对齐记录 + 8 项方案 + 内置模板 + 轻合并 + 布局 + 15 条验收标准 + 拆批建议）
2. **交互原型**：`docs/04-增项交互布局原型-20260808.html`（浏览器打开可交互演示，纸墨双主题；已实测：双主题/三态/拖拽/摘要实时/三层弹窗/移除解绑/删除尺寸确认/预览只读 全通过）
3. 讨论稿 `docs/comms/04-增项交互方案讨论稿-20260808.md` 已消费完毕，可归档删除（REQ-036 已含全部结论）

## 优先级

**P1**。画师真实反馈反直觉（「增项又必须先去增项库新建，才能在这边加」），影响日常配置体验。

## 给一号的拆批建议（详见 REQ-036 §七）

- **批 A：前端交互重构**（主体）：双入口 / 加购项池+拖拽 / 尺寸三态 / 三层编辑弹窗 / 移除解绑 / 预览只读 / 尺寸摘要行 + 布局改造（原型为准）+ **两个必修 bug**（切 tab 不刷新、尺寸覆盖面板位置错乱——均已代码证实，ArtStyleManager.vue L844 / L147）
- **批 B：后端核心**（独立批次，含迁移）：删除策略 C'（`style_addons.addon_template_id` 现为 `ON DELETE CASCADE`，需改外键行为）/ 尺寸三态后端校验（展示/关闭 → 算价+下单 400 拒单，防 F12 直调）/ 倍率轻合并（`addon_templates` 加 `kind` 维度）/ 内置模板种子（系统预置 5 个）

## 关键决策点（已拍板，拆单别改）

| 项 | 结论 |
|----|------|
| 主形态 | 尺寸主轴 + 加购项池 + 拖拽（不是表格/抽屉） |
| 三态 | 可约（石绿）/ 展示（藤黄，可见不可约）/ 关闭（朱砂，隐藏）；后端必校验 |
| 编辑弹窗 | 三层全管：模板级本身价（带应用范围确认）/ 画风级激活+价 / 尺寸级表格 |
| 删除 | 画风内移除=解绑；库删模板=C'（保留独立+解绑引用，不级联） |
| 倍率 | 轻合并（kind 维度），用途/加急单选；买断=开关一次，不可多选 |
| 内置模板 | 系统预置 5 个：加人物×80/每、背景50/每、机甲100开关、商用+50%、加急+100% |
| 价格输入 | step=1；保留即时保存（防抖 500ms） |
| 预览 | 纯只读顾客视角，无「下一步」按钮（画师已指出并拍板） |
| 增项库 tab | 保留不变，最后一个 tab，退居沉淀资产 |

## 待确认

无。已全部拍板定稿。
