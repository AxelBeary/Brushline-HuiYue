# 授权修复：挖 Bug 报告批次 A（五号）——5 项无争议修复

> 日期：2026-08-04
> 分支：`fix/v035-audit-batch-a` · Worktree：`artist-commission-wt-05`（`git worktree add` 新建，基于最新 master）
> 风险等级：中（涉及公开路由守卫 + 数据丢失防护，但无迁移无支付逻辑）
> 基线：master `1b816eb`

## 授权修复清单（按你报告的编号）

### BUG-2 i18n 缺 56 键（locales 双补）
- 双语各补 56 键（附录清单），翻译对照 errors.ts 的 ERROR_MESSAGES 中文原文，英文自行翻译保持语义一致
- STYLE_*/STYLE_ADDON_*/SIZE_OVERRIDE_* 13 键虽是波 1 域错误码，但只动 locales 不碰 service/routes，允许先行
- 验证：写个临时对照脚本确认 errors.ts 122 码 = locales errors.* 122 键（跑完即删）

### BUG-3 公开路由 hidden 过滤 ×3（波 1 禁区那处除外）
- 抽公共守卫函数（建议放 artist.service.ts 或独立 util）：`requireVisibleArtist(subdomain)` → hidden 或管理员账号时抛 ARTIST_NOT_FOUND 404（对照 artist.routes.ts:35-39 范式）
- 替换 3 处：pricing.routes.ts:227 calculate-price、:335 validate-discount、artist.routes.ts:542/552 like（likeArtwork 服务 artist.service.ts:620 加画师状态检查）
- style.routes.ts:324 calculate-style-price **不碰**（波 1 域，合入后复查清单已有）

### BUG-4 分期舍入漂移
- recalcInstallmentAmounts（order.service.ts:963-980）末节点 = 总额 − 前 N-1 个之和

### BUG-7 设置页加载失败覆盖数据
- Settings.vue:295-303 loadRules：catch 里设 loadFailed 标记 + 渲染错误态，禁用保存按钮直到加载成功
- Settings.vue:536-589 getProfile：失败时渲染错误态，禁用全部保存按钮（模板/调色板/资料同理）——未加载成功前不允许编辑保存
- 模式：错误态显示「加载失败，请刷新重试」+ 重试按钮（重新调 load）

### BUG-8 前两条 admin 存在性校验
- admin.routes.ts:589 PUT rules：补画师存在性校验（不存在 404）
- admin.routes.ts:261/364 GET greetings / GET workflow：同上
- 第三条（status enum 缺 hidden）**不修**——等用户拍板

## 不做（本轮排除，别碰）

- BUG-1 分期双模型分叉：等用户拍板修复方案（a 自动分摊 vs b 死代码转正）
- BUG-5 死代码：1-6 删除归 v0.36 清账版统一做（邻波 1 域的等合入后）
- BUG-6 时间条钳制：波 2 域（QueueBoard.vue），v0.35 合入后复查
- BUG-8 第三条：等用户拍板
- task-0 子代理其余发现：已落账待修复清单，不在此批

## 验证标准

1. `cd server && npx tsc --noEmit` 0 错 + `npx vitest run` 全绿（667+，新增用例：hidden 画师算价/验证折扣码/点赞返回 404、分期舍入尾差归末节点）
2. `cd web && npx eslint . && npm run build`
3. Settings 错误态：手动断网模拟或 mock API 失败验证保存按钮禁用
4. 交付 comms：`docs/comms/05-to-01-audit批次A修复-{日期}.md`（含测试数 + i18n 对照脚本结果）

## 文件授权范围

- `server/src/features/pricing/pricing.routes.ts`
- `server/src/features/artist/artist.routes.ts` / `artist.service.ts`
- `server/src/features/admin/admin.routes.ts`
- `server/src/features/order/order.service.ts`
- `web/src/views/artist/Settings.vue`
- `web/src/locales/zh-CN.js` / `en.js`
- `server/tests/*`（新增用例）
- 公共守卫函数放哪你定（新增文件需在交付报告说明）
