# 二号交付：客户端前端修复批次（2026-08-02）

> 分支：`fix/client-frontend-0802`
> Commit：`f6730d3`
> 验证：ESLint 零问题 + `npm run build` 通过

---

## 完成项

### #54 effectiveStatus 适配（高）
- `ArtistHome.vue` 新增 `displayArtist` computed：`effectiveStatus || status` 覆盖
- 4 模板零改动（通过 prop 传递自动生效）
- 向后兼容：字段缺失时 fallback 原始 status

### #50 瀑布流图片预留高度（中）
- `TplGallery.vue`：el-image 加 `#placeholder` 插槽 + 骨架占位 CSS（min-height 200px + shimmer 动画）
- 后端无 width/height 字段，走兜底方案

### #52 灯箱关闭方式（低）
- `TplGallery.vue` + `TplTierGrid.vue`：el-image 加 `hide-on-click-modal` 属性
- EP 2.9.0 默认支持 ESC，加此属性后点遮罩也可关闭

### #55/61 浮窗全局组件化（中）
- 新建 `web/src/components/client/ClientFloatingActions.vue`（ThemePicker + fixed 定位 + raised prop）
- `ArtistHome.vue` 统一挂载，provide `ctaRaised` ref
- Gallery/Folio/Atelier inject + watch 同步 CTA 避让
- Classic/TrackOrder/OrderForm 直接挂载（无 CTA）
- 6 处旧实现（4 模板 theme-fab + 2 页面 page-prefs）全部删除

### #17 客户沟通区去 QQ 显示（D 类）
- `OrderDetail.vue`：QQ 显示行改为"复制联系方式"按钮（复用已有 copyQq 方法）
- i18n 新增 `commCopyContact` 键（中英双语）

## 改动文件

| 文件 | 改动 |
|------|------|
| `web/src/components/client/ClientFloatingActions.vue` | 新建 |
| `web/src/components/templates/TplGallery.vue` | #50 骨架占位 + #52 hide-on-click-modal |
| `web/src/components/templates/TplTierGrid.vue` | #52 hide-on-click-modal |
| `web/src/views/client/ArtistHome.vue` | #54 displayArtist + #55 浮窗挂载 + provide |
| `web/src/views/client/OrderForm.vue` | #55 换 ClientFloatingActions |
| `web/src/views/client/TrackOrder.vue` | #55 换 ClientFloatingActions |
| `web/src/views/client/templates/ArtistHome*.vue` ×4 | #55 删旧浮窗 + inject |
| `web/src/views/artist/OrderDetail.vue` | #17 QQ→复制按钮 |
| `web/src/locales/zh-CN.js` / `en.js` | #17 新增 commCopyContact 键 |

## 备注

- #54 后端 effectiveStatus 字段尚未合入 master（五号在做），前端已向后兼容，合入后自动生效
- `web/package.json` 有 npm approve-scripts 改动（esbuild/vue-demi），未提交（非任务范围）
