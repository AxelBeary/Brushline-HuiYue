# 01→04：A 测（首轮真实画师测试）执行手册批

> 日期：2026-08-05 · 派工：一号 · 执行：四号
> worktree：`../artist-commission-v038acc`（已建）· 分支：`atest-docs`（基于 master `a8ef3e5`）
> 背景：v0.38 视觉 + TOTP + F1/F2 合入后，下一里程碑 = A 测。用户拍板：AUTH_DEV_MODE 随 A 测关闭；A 测真实反馈优先于 REQ-031 候选池。本批产出执行手册，不改一行代码。

## 交付物：`docs/A测执行手册.md`（单文件）

1. **测试剧本**：真实画师从 0 到 1 全链路——注册/管理员建号 → TOTP 登录 → 资料设置（含新外链区，若 F1/F2 前端批已合入）→ 开稿/档位 → 接单 → 工作流推进 → 交付 → 收款（含负数/退款）→ 客户侧查单/留言。每步：操作 → 预期 → 记录位。对照代码现状写（你惯用的实锤纪律），不写未合入的功能；F1/F2 章节标注「若合入」条件。
2. **AUTH_DEV_MODE 关闭检查单**：前置逐项核对（管理员 TOTP 已绑定 ✅ 2026-08-05 实跑过 / 至少一位测试画师完成 TOTP 绑定后再关 / .env 改 false + 容器重建步骤 / 回滚路径：改回 true 重建）——把 STATUS 规则 9 落成可执行步骤。
3. **反馈收集模板**：画师反馈 → 分类（bug/体验/新需求）→ 归档到哪（待修复清单 / REQ 池）的一页流程。
4. **已知限制清单**（测前告知画师，防误报）：约稿需求描述可空过（待拍板项）、addons 旧表冻结中、候选池未立项等——从 STATUS/REQ 现状汇总，逐条标来源。

## 铁律

1. 纯文档批：零代码零配置改动；引用的每个现状断言读代码/读 STATUS 实锤
2. 交付：commit 到分支 + `docs/comms/04-to-01-A测手册-交付-20260805.md`。不推送不合并。

—— 一号
