# 一号 → 五号：REQ-022 F4 回收站分页（管理后台小批）

> 日期：2026-08-05 · 分支：`f4-recycle-page` · worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-f4`（基于 master `f4f027c`）
> 风险等级：低（管理后台低频页，前后端各一处，无数据库迁移）
> 依据：`docs/requirements/REQ-022-B类低优先需求.md` F4 节——用户 2026-08-05 已拍板「**分页**，每页 20 条」（不是折叠）。

## 现状（已核实）

- 后端 `server/src/features/admin/admin.service.ts` L45 `listRecycleBin()`：文件系统 walk，一次性返回全量 `[{ fileName, originalPath, size, movedAt }]`。
- 路由 `server/src/features/admin/admin.routes.ts` L208：`GET /api/admin/recycle-bin` 返回 `{ items }`，无分页参数。
- 前端 `web/src/views/admin/AdminDashboard.vue` L33-58：el-table 全量渲染 recycleItems，无分页器。
- 清空接口 `DELETE /api/admin/recycle-bin` 语义不变（整体清空，不分页清空）。

## 实施要求

1. **后端**：`GET /api/admin/recycle-bin` 加可选 query `page`（默认 1）/ `pageSize`（默认 20，上限 100）。walk 后按 movedAt 倒序排（新删的在前），slice 返回 `{ items, total, page, pageSize }`。不传参数时行为等同 page=1/pageSize=20——**响应结构从 `{ items }` 变为 `{ items, total, page, pageSize }`，前端同批改，无第三方消费者**（已核实仅 AdminDashboard 一处调用）。
2. **前端**：AdminDashboard 回收站区加 `el-pagination`（每页 20 条，layout 与全站列表一致——参考 OrderList 的分页写法），翻页请求对应 page。清空后回到第 1 页并刷新。
3. **i18n**：新增键走 zh-CN + en 双语（如「共 N 项」等分页文案若复用现有键则不新增）。
4. **测试**：server 侧补回收站分页路由测试（造 >20 个文件条目验证 total/分页切片；参数越界 page=999 返回空 items 不报错）。测试隔离规则照旧（独立 UPLOAD_DIR/回收站根）。

## 授权文件

- `server/src/features/admin/admin.service.ts`、`admin.routes.ts`
- `server/src/features/admin/__tests__/*`（新增或现有测试文件）
- `web/src/views/admin/AdminDashboard.vue`
- `web/src/api/index.js`（仅 getRecycleBin 签名加参数）
- `web/src/locales/zh-CN.js`、`en.js`（仅新增键）

**黑名单**：server/ 其余全部、web/ 其余全部（尤其 views/client/、views/artist/——二号正在做视觉批，admin 域以外碰都不碰）、迁移文件、docs/requirements/。

## 验证与交付

- worktree 内 `npm install`；server vitest 全绿（基线 **824/824**）+ web vitest 全绿（基线 **186/186**）+ tsc 0 错 + lint 0 错。
- 交付报告 `docs/comms/05-to-01-F4回收站分页-交付-20260805.md`（改动清单 + 测试数据 + API 契约变化说明）。
- commit 单行 `feat(admin): ...`，**禁止 git add -A**，不推送不合并。返回摘要：commit hash、测试结果、报告路径。
