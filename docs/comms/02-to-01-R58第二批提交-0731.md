# 二号 → 一号：R58 第二批提交审核（回收站 UI + R58-2 分步引导）

> 日期：2026-07-31

---

【角色】
二号：客户页面前端负责者

【工作分支】
`feat/v016-r58-batch2`（worktree: `artist-commission-wt02`）

【任务编号】
任务A：回收站 UI / 任务B：R58-2 分步引导（含 R58-3/R58-4/R58-5 顺手做）

【修改模块】
管理员面板 / api 层 / 客户约稿页 / 全局模板样式 / i18n

【修改内容】

**Commit 1** `4e8274f` — `feat(client): 管理员回收站UI`

1. AdminDashboard 新增回收站卡片：文件列表（文件名/原始路径/大小/移入时间），大小 B/KB/MB 格式化，时间走 `formatDateTime`（UTC→本地）
2. "清空回收站"按钮（仅有内容时显示）：`ElMessageBox.confirm` 二次确认提示不可恢复 → `DELETE /api/admin/recycle-bin` → toast 显示删除数
3. 空状态 `el-empty`
4. api/index.js 新增 `getRecycleBin`/`emptyRecycleBin`（api/index.js 属隐含授权文件，新接口调用自动在范围内）
5. 回收站列表与统计/画师列表并行加载（Promise.all），独立 loading 态

**Commit 2** `a51c02d` — `feat(client): R58-2 分步引导布局`

1. **三步引导**：①选档位 → ②写需求+上传 → ③联系方式。顶部步骤指示器（圆点+连接线，当前步放大高亮，已完成步打勾变主色）
2. **档位卡片选择**：el-select 改为响应式卡片网格，选中态弹性动画（`--ease-bounce`）+ 右上角印章 ✓ 旋转缩放入场（`tier-stamp-in` keyframe）；计价摘要+增项+倍率保留在步骤一（原 R14 渐进展开逻辑不变）
3. **粘性摘要卡**：宽屏右侧 280px sticky 栏，实时显示档位+明细+总价+分期；移动端（≤860px）单栏，摘要卡移到底部
4. **R58-3 小票二次确认**：步骤三提交按钮 → 先跑表单校验 → 通过后弹咖啡馆小票风弹窗（锯齿边 conic-gradient、等宽字体、虚线分隔、条形码装饰、分期 chips），确认后才真正调 `submit()`；校验失败不弹窗（错误定位到对应步骤的表单项）
5. **R58-4 灵感标签**：步骤二描述框上方 6 个硬编码默认标签（角色设计/原创角色/同人二创/情侣头像/透明背景/Live2D 拆分），点击追加到描述（智能补逗号、2000 字截断）；后续三号加字段后可替换为画师自定义
6. **R58-5 复制约稿信息**：成功弹窗新增"复制约稿信息"按钮，复制订单号+档位+明细+总价多行文本
7. 小票弹窗 teleport 到 body，样式放全局 `templates.css`（已确认 main.js 全局加载）；其余新样式全部 scoped
8. composable 零改动——R58-1 抽取的逻辑核心原样复用，验证了抽取的正确性

【涉及文件】
- `web/src/api/index.js`（+2 方法：getRecycleBin/emptyRecycleBin）
- `web/src/views/admin/AdminDashboard.vue`（+回收站卡片）
- `web/src/views/client/OrderForm.vue`（三步布局重写，+606/-171）
- `web/src/styles/templates.css`（+84 行：小票弹窗全局样式）
- `web/src/locales/zh-CN.js`（+recycleBin 块 + orderForm 13 键）
- `web/src/locales/en.js`（同上）

【是否修改非客户前端文件】
`AdminDashboard.vue` 属管理员页面，派工任务A明确授权。api/index.js 为隐含授权（comms 注明）。templates.css 在授权列表内。

【接口依赖】
- `GET /api/admin/recycle-bin` → `{ items: [{ fileName, originalPath, size, movedAt }] }`（已对照 admin.service.js listRecycleBin 确认字段）
- `DELETE /api/admin/recycle-bin` → `{ success, deleted }`

【自测情况】

| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告（任务A后/任务B后各一次） |
| Vite build | ✅ 通过（10.13s / 9.77s） |
| console.log / TODO / v-html | ✅ 新增行零残留（v-html 仅既有 sanitizedRules 一处，非新增） |
| i18n 中英同步 | ✅ recycleBin 块 + orderForm 13 键 × 2 语言 |
| 改动文件范围 | ✅ 6 文件均在授权内 |
| Rebase master | ✅ 基于 d03a4d3 |
| API 方法存在性 | ✅ getRecycleBin/emptyRecycleBin 已加到 api/index.js |

**需人工验证项**（无浏览器环境，本批是核心下单路径重构，务必全流程走一遍）：
- 约稿全流程：步骤一选档位（卡片点选/切换清空增项）→ 展开计价 → 步骤二灵感标签注入+描述+上传+粘贴 → 步骤三 QQ/须知 → 提交校验失败定位 → 小票弹窗 → 确认下单 → 成功弹窗复制约稿信息
- 小票弹窗锯齿边渲染（Chrome/Edge）、弹窗内取消返回步骤三
- 摘要卡价格实时性（选增项/倍率后 300ms 防抖更新）
- 移动端 375px：单栏、摘要卡底部、步骤指示器只剩圆点
- 暗色模式下档位卡片/摘要卡/步骤指示器对比度（小票弹窗为固定浅色纸感，不随暗色变化——设计意图）
- 回收站：空态/有文件态/清空确认/清空后刷新

【可能影响】
- 约稿页布局完全重构（单页表单→三步），是核心下单路径，回归风险集中在提交流程——但提交逻辑本身零改动（composable 原样复用），风险面在布局层
- 小票弹窗样式全局定义在 templates.css，类名 `receipt-*` 有命名空间，不影响其他页面
- 回收站仅管理员可见，不影响客户/画师侧

【待确认问题】
- R58-4 灵感标签目前硬编码 6 个，等三号 `order_template_id`/画师自定义字段后替换——是否需要现在给三号提需求？

【申请】
申请一号审核合并。
