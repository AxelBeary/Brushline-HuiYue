# 转交一号：812-chores 零提交根因完整证据链（2026-08-12）

> 本文件由一号（Hermes 侧）查证后转交 Qoder 实际干活的一号。合并已交专人收口，本文件只讲根因与修复方向。

## 一句话结论

**codex 没坏，行为完全合格。** 零提交的真凶是：`workspace-write` 沙箱的可写根 = worktree 目录本身，而 git worktree 的元数据在主仓库 `.git/worktrees/<name>/` 下（**不在 worktree 目录内**）→ codex 写 README/测试文件全部成功，但 `git add/commit` 写 `index.lock` 时 **Permission denied** → 完工标记在、0 commit、3 处未提交。

## 完整时间线（Qoder 会话 task-c1d326f4 实锤）

| 本地时间 | 动作 | 结果 |
|---|---|---|
| 8/12 01:33 | 改 `~/.codex/huiyue.config.toml`：`workspace-write` → `danger-full-access`（救火，三路 codex 跑起来） | ✅ 三路施工 |
| 01:34 | **用户质疑：「danger-full-access 太危险 flash的稳定性没那么强」** | 用户拍板否决 danger-full-access |
| 01:35:27 | 改 `~/.codex/config.toml`：`[windows] sandbox = "elevated"` → `"unelevated"`（实验） | — |
| 01:35:55 | unelevated + workspace-write 冒烟 | ❌ 仍 CreateProcessAsUserW failed: 5 |
| 01:36:11 | 改回 elevated，试 `shell.path` 指系统 powershell | ❌ 失败 |
| 01:37:46 | 再次改 unelevated + **PATH 前置 `C:\Users\<user>\AppData\Local\Programs\PowerShell7\PowerShell\7`** | ✅ **SMOKE_OK** |
| 01:38+ | 把修复写进 `launch-812-{a,b,c}.ps1` 第 5 行；**15:50 派 812-chores 同样沿用** | 命令能跑、文件能写、**commit 不能** |

**config.toml 最终状态**（当前生效，LastWriteTime 01:37:53）：
```toml
[windows]
sandbox = "unelevated"
```

## 根因分层

1. **表层**：01:37:53 你改了 config.toml（elevated → unelevated）——**这是合理救火**（配合 PATH 前置修复了子进程 spawn），不是乱改。08-09 的「elevated + 用户级 pwsh」经验在商店版 pwsh 重新抢占 PATH 后失效，你现场排查出的 unelevated + PATH 前置方案是对的。
2. **真凶（你没意识到的）**：workspace-write 的可写根 = codex 启动 cwd（worktree 目录），但 `git worktree` 的元数据位于 **主仓库** `.git/worktrees/812-chores/`（`git rev-parse --git-dir` 实测）。沙箱判定该目录越界 → `git add`/`git commit` 写 `index.lock` 全部 Permission denied。**这与 elevated/unelevated 无关，workspace-write 模式下 worktree 施工必然无法 commit。**
3. **811 批为什么能提交**：811 批用的是 danger-full-access（无沙箱边界），所以 commit 正常。**workspace-write 从没在 worktree 施工上成功过 commit。**

## codex 行为评价（给 codex 正名）

812-chores 的 codex 在交付报告第 4 节**如实写明**：「git add / git commit 均报 index.lock Permission denied（worktree 的 git 元数据目录在沙箱可写根之外）」→ 三个 commit 未能落库；第 5 节给了**完整交接命令**（3 条 add+commit）。`task_complete` 在报告之后触发 = 它完成了"如实汇报"职责。**未谎报、未偷懒，处置完全正确。**

## 修复方向（合并专人收口，你确认后执行）

1. **立即收口**（专人已做）：正常环境按 codex 报告第 5 节 3 条命令提交，再补跑动态门禁（单测/build/E2E——codex 沙箱里 esbuild 起不来，静态门禁已全绿）。
2. **根治派工模式**：workspace-write + worktree 施工 = 永远 commit 不了。可选：
   - a) `[sandbox_workspace_write] writable_roots` 追加主仓库 `.git/worktrees` 路径（需验证 codex 是否支持该键，**先冒烟再全量派**）；
   - b) 派工启动器里把 cwd 指向主仓库（可写根=主仓库含 .git）但施工图限死只碰授权文件——风险大不推荐；
   - c) **接受现状**：codex 交付未提交改动 + 交接命令，commit 由一号/专人在主仓收口（本轮就是这模式，worktree 隔离仍然有效，安全边界没破）。
3. **config.toml 不要回滚**：当前 unelevated + 启动器 PATH 前置是验证过的可用组合；改回 elevated 会再次触发商店版 pwsh 的 CreateProcessAsUserW failed: 5（商店版 pwsh 又抢占了 PATH 第一）。
4. **补一条启动器注释**：launch-*.ps1 第 4 行注释下追加「⚠️ workspace-write 下 worktree git 元数据不可写，codex 无法自提交，收口由一号执行」。

## 附：本次查证用到的证据文件

- Qoder 会话：`~/.qoder-cn/projects/d-Hermes Agent CN Desktop-workspace-artist-commission/transcript/task-c1d326f4cb4e4f1f91b7.session.execution.jsonl`（01:33-01:38 段）
- codex 会话：`~/.codex/sessions/2026/08/12/rollout-2026-08-12T15-50-42-*.jsonl`（812-chores 交付报告）
- 派工启动器：`workspace/temp/launch-812-chores.ps1`（第 5 行 PATH 前置、第 9 行 workspace-write）
