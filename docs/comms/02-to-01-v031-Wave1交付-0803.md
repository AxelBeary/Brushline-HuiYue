# 二号交付：v0.31 Wave 1 前端（2026-08-03）

## 分支

`feat/v0.31-frontend-0803`（已推送 origin）

## 改动

### F2 拖拽上传（ManualOrder.vue）
- el-upload 加 `drag` + `multiple`，保留点击上传
- 加拖拽提示文案（i18n: `manualOrder.dragHint`）

### F3 录单设开稿日（ManualOrder.vue）
- 截稿日下方加"开稿日"日期选择器（可选），`disabled-date` 限今天前不可选（REQ-018）
- form 加 `startDate` 字段，提交后调 `artistApi.updateStartDate()` 写入（同截稿日模式）
- resetForm 清空 startDate
- i18n: `manualOrder.startDate` / `manualOrder.startDatePlaceholder`

### F5 增项点击勾选（AddonManager.vue）
- **已由现有代码实现**：`@click="startEdit(a)"` 打开编辑弹窗，`handle=".drag-handle"` 限定拖拽区域，点击和拖拽不冲突。无需改动。

### F6 作品主图设置（ArtworkManage.vue）
- **已由 REQ-017 封面星标实现**：星标切换 `is_cover`，API 复用 `PUT/DELETE /artist/artworks/:id/cover`。无需改动。

### F7 主图去重（ArtworkManage.vue）
- 新增 `mainArtworks` computed（is_cover=1，按 cover_order 排序）
- 新增 `gridArtworks` computed（排除主图；去重后为空兜底显示全部）
- 主图单独展示区：带"主图"标签 + 取消星标按钮 + 删除/多选操作
- 网格 `v-for` 改用 `gridArtworks`，`initial-index` 修正为全量列表索引
- i18n: `artworks.mainImages` / `artworks.mainTag`

## 改动文件

| 文件 | 改动 |
|------|------|
| `web/src/views/artist/ManualOrder.vue` | F2 + F3 |
| `web/src/views/artist/ArtworkManage.vue` | F7 |
| `web/src/locales/zh-CN.js` | F2/F3/F7 键 |
| `web/src/locales/en.js` | F2/F3/F7 键 |

## 验证

- ESLint：零错误零警告
- 前端测试：87/87 通过（5 文件）
- vite build：成功（5.29s）
- git status：干净

## Commit

1. `4fe91d1` feat(client): F2拖拽上传+F3录单设开稿日
2. `46ad329` feat(client): F7主图去重

## 备注

- F5/F6 经代码审查确认已由现有实现覆盖，无需重复开发
- api/index.js 未改动（`updateStartDate` 已存在）
- 客户端 4 模板的 galleryArtworks 去重（useArtistData）已由 REQ-017 实现，本次只补画师后台 ArtworkManage 的去重