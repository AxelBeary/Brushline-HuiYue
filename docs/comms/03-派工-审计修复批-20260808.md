# 03-派工-审计修复批-20260808.md

> 派工：一号 → 三号（backend）
> worktree：`../artist-commission-w3`（分支 `beta/audit-fixes`）
> 背景：五号/四号深度审计合入（`0196343` + `95633e6`），🔴 项修复本批
> 基线：master（开工前 git merge master 拉最新）。**开工第一步 `git merge master`**。
> 门禁：server 1000/1000 + tsc 0 + eslint 0 + oxlint 0 + 新代码 any 0
> 蓝本：`docs/comms/05-to-01-后端深度审计-交付-20260808.md` + `docs/comms/04-to-01-前端深度审计-交付-20260808.md`

## 本批 4 项（均 🔴，低成本高确定性）

### 1. adjustFinalPrice 金额回退链补 price_snapshot（后端审计 🔴-1）
- 文件：`server/src/features/order/order.service.ts:699-706`
- 现状：`const currentFinal = order?.final_price_cents ?? order?.total_price_cents ?? 0`（两级，丢 price_snapshot）
- 修复：SELECT 加 price_snapshot 列，`const currentFinal = resolvePriceCents(order) ?? 0`（utils/price.ts 已导出，三级回退）
- 测试：造 final/total 均 NULL 仅 price_snapshot 的订单 → addExtraItem → final_price_cents 正确 = snapshot×100 + delta

### 2. catch-all 伪装 400（后端审计 🟡-1，升 🔴 同族一次改齐）
- 文件：admin.routes.ts:76/727/746/757、artist.routes.ts:223/444、upload.routes.ts:176/211/245/279（先 grep 复核实际行号）
- 现状：`catch (err) { return reply.code(400).send({ error: (err as Error).message }) }`——非 AppError 伪装 400 + 泄露内部细节
- 修复：`if (err instanceof AppError) return reply.code(err.statusCode).send({ code: err.code, error: err.message }); throw err`（非业务错误向上抛走全局 500 handler）
- 测试：mock 抛非 AppError（如 `throw new Error('boom')`）→ 响应 500 结构化（非 400）；AppError → 原状态码

### 3. 节点收款前端校验收紧（前端审计 🔴-1，红线 4 违反）
- 文件：`web/src/views/artist/OrderDetail.vue`（:max 行 ~535）+ `web/src/composables/useOrderPaymentPanel.js:139-145`
- 现状：前端限制节点收款 ≤ remainingCents，后端 addPayment 不限节点金额（超额压尾款）——**前端比后端收紧**
- 修复（二选一，需保持前后端一致）：
  - 方案 A（推荐）：前端删 `:max=remainingCents` + `submitNodePayment` 的 `cents > remainingCents` 拦截，保留 `cents <= 0` 校验（与后端对齐）
  - 方案 B：后端 addPayment 加节点上限校验（改动后端语义，影响现有「多收压尾款」行为，**不推荐**除非用户拍板）
- ⚠️ 这是用户可感知行为变化（画师不再被节点入口拦超额）——**交付报告写明**，一号向用户说明

### 4. 管理后台 i18n 键路径错位（前端审计 🔴-2，二号重设计回归）
- 文件：模板 7 处（AdminDashboard.vue:7/29、ArtistManage.vue:7/13、PlatformManage.vue:7、TrackingAnalytics.vue:7）+ locales zh-CN.js/en.js
- 现状：模板用 `admin.dashboardSubtitle` 等，locale 定义在 `admin.tracking.*` 下（zh-CN.js:1131 等）→ 页面显示 key 本身
- 修复：**locale 键上提一级到 `admin.*`**（dashboardSubtitle/quickActions/artistManageSubtitle/artistActions/platformManageSubtitle/trackingSubtitle），模板引用不变
- 验证：node 扁平化逐键确认 7 键两语言均解析成功（不再显示 key）

## 实现铁律

1. 不做假修复（显式 any / @ts-ignore 蒙混 = 打回）
2. 每项带测试；金额/权限改动双确认
3. 前端改动 i18n zh/en 同步

## 验证门禁

1. `cd server && npm test` → **1000/1000 全绿 + 新增测试**
2. tsc 0 / eslint 0 / oxlint 0 / 新代码 any 0
3. `cd web && npm run test:web` → 215/215（若动前端）+ lint 0 + build 过
4. `git diff --stat` 只动授权文件

## 交付

- commit 到分支 `beta/audit-fixes`，不推送不合并
- 交付报告 `docs/comms/03-to-01-审计修复批-交付-20260808.md`：4 项逐条 修复方案 + 测试 + 行为变化说明（尤其 #3 节点收款）
