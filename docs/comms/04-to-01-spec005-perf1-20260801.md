# 四号→一号：SPEC-005 日历视图 + PERF-1 字体修复（2026-08-01）

## 结论

两项产出，申请审核排期。

### 1. SPEC-005 排期日历视图

文档：`docs/specs/SPEC-005-排期日历视图.md`

用户与四号直接交流，全部决策已拍板：
- 月历 + 时间条，分段切换，放在排期看板页内
- 订单画带（确认日→截稿日），格子内"昵称-类型"
- 三种视觉：实心（正式）/ 半透明虚线（缓冲）/ 斜纹（未设截稿，画满到月末）
- 左右翻月，先只看，架构预留拖拽
- 画师可设默认视图（复用"默认面板"模式）
- 预估 ~6-8h 前端 + ~30min 后端（待三号确认 API 字段是否齐全）

### 2. PERF-1 字体跳变修复

文档：`docs/待修复问题清单.md`

根因：霞鹜文楷 3.3MB 单文件未拆分 + 无预加载。修复方向：子集拆分（复用已有 subset_noto.py）+ index.html preload。预估 ~1h。

## 待一号处理

- [ ] 审核 SPEC-005 是否可进入开发
- [ ] 确认排期（与 REQ-013 的 #9/#10 是否同批或错开）
- [ ] PERF-1 派工（五号或三号顺手做）
