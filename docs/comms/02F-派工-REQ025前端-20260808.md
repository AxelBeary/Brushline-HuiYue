# 02F-派工：REQ-025 前端缺口修复（02E 核查发现）

> 派工人：一号（主理人）
> 日期：2026-08-08
> 执行角色：二号（client-frontend）
> worktree：`D:\Hermes Agent CN Desktop\workspace\artist-commission-w28`
> 分支：`beta/fix-req025-frontend`
> 交付报告：`docs/comms/02F-to-01-REQ025前端-交付-20260808.md`

## ⚠️ 开工第一步

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w28'
git merge master --no-edit
```

## 背景

02E 前端需求覆盖核查（已合入）发现 **REQ-025 前端 6 项缺口**。本批修其中 3 项（剩余 3 项随 REQ-025 收尾批联动）。用户铁律：「避免屎山，禁止技术债，代码要优雅！」

## 授权文件（只许动这些）

1. `web/src/views/artist/OrderDetail.vue` — 3 项缺口修复
2. `web/src/locales/zh-CN.js` + `en.js` — 如需新键

> 不改后端；不引新依赖。

## 任务 1（🔴 铁律违规，最优先）：负增项前端放开

**现状**：`OrderDetail.vue:538` 附加项价格 `el-input-number :min="0"`——后端 `6bb200c` 已放开负数（负增项=退款条目，R13 done 半终态），**前端收紧后端规则，违反「前端校验=后端子集」铁律**。

**修复**：
- `:min="0"` → `:min="-999999.99"`（与后端 `price_cents` 负数允许对齐；上限 999999.99 保留）
- ⚠️ 检查该弹窗提交逻辑：负价格提交后端是否接受（后端 schema 已放开）；若前端有 `if (price < 0) 拒绝` 类校验一并检查——**前端只做格式校验，不限制正负**（后端子集）
- ⚠️ 确认展示端（订单详情附加项列表）负价格显示正常（¥-xx 或「退款」语义——先读现状）

**验收**：附加项弹窗可输入负数价格并提交成功；订单详情展示负附加项正常；回归测试全绿。

## 任务 2（🟡）：已锁节点展示

**现状**：后端 v40 已建 `locked` 列（已收节点持久化），但前端 OrderDetail **无任何 locked 渲染**——画师看不到哪些节点已锁定（REQ-025「已收节点不再变」的视觉反馈缺失）。

**修复**：
- 读 OrderDetail 流程节点渲染（StageListView 或内联）：确认节点数据结构是否含 `locked` 字段（后端 getOrder 返回）
- 已锁节点加视觉标识：锁定图标/角标/样式（如 `el-tag`「已锁定」或锁图标），tooltip「该节点收款已确认，不可变更」
- 视觉：纸墨 token，双主题；克制动效纪律

**验收**：已收节点在订单详情流程中显示锁定标识；未锁节点无标识；双主题正常。

## 任务 3（🟡）：done 状态操作按钮隐藏

**现状**：02E 核查发现：
- done 状态「无痕改价」按钮未隐藏（`isTerminal` 不含 done）
- done 状态「回退」按钮未隐藏（`canBackStage` 只拦 delivered/cancelled）

**修复**：
- 先 grep OrderDetail 的 `isTerminal` / `canBackStage` 定义与使用点
- done 状态：无痕改价按钮隐藏（done 后改价应走条目路径，REQ-025 R13）；回退按钮隐藏（done 是半终态，不能回退到前节点——后端守卫需三号确认，**前端先隐藏**）
- ⚠️ 若后端守卫已拦（三号确认后）则前端纯隐藏是 UI 一致；若后端未拦，**本批只做前端隐藏 + 报告说明「后端守卫待三号确认」**（不越权改后端）

**验收**：done 状态订单详情无「无痕改价」/「回退」按钮；其他状态按钮保留。

## 门禁（提交前必须全绿）

```powershell
cd 'D:\Hermes Agent CN Desktop\workspace\artist-commission-w28\web'
npm run lint
npm run test:web
npm run check:i18n
npm run build
```

## 交付要求

1. commit 描述中文（如 `fix(web): REQ-025前端缺口——负增项放开+已锁节点展示+done按钮隐藏`）
2. 交付报告写 `docs/comms/02F-to-01-REQ025前端-交付-20260808.md`：三任务改动说明 + 验收 + 门禁结果 + **截图**（已锁节点标识双主题、done 状态按钮隐藏——视觉门禁）
3. 结束前：停本地 dev server；`git status --short` 确认干净
4. **不推送、不合并、不改 STATUS**
