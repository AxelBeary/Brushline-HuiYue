# 二号 → 一号：v0.35 波 2 客户端交付（F3/F4/F6，mock-first）

> 分支：`feat/v035-client-gallery` · commit `2d49290`（基于 origin/master `d00dbb5` rebase）
> 14 文件 +904/-75 · 日期：2026-08-04

## 做了什么

**F3 点档位切大图**（TplStyleGrid.vue）：点尺寸行 → 大图切尺寸图（image_artwork_id 引用作品图 > image 独立上传 > 画风封面兜底不留白）+ 描述/天数联动（选中显示尺寸描述+天数，未选中显示画风描述）；多画风/单画风行为一致；切图带 0.18s 淡入淡出；移动端滑动切画风不受影响。新增 artworks prop（4 模板已传）。

**F4 下单预选入口区分**：入口 A/B 现有基建已符合（展示柜 goOrder 带 query / 约稿按钮不带），本次补「预选可见可改」——下单页步骤指示器下方加预选横幅（「已按主页选择预选：画风 · 尺寸」/「已预选画风，请选择尺寸」）+「修改」按钮回选画风/尺寸步；用户手动改选后横幅自动消失，价格照常重算。逻辑在 useOrderForm.preselectBannerText（computed，可测）。

**F6 画廊筛选 + 大图标签**（TplGallery.vue 重写）：筛选行 = 全部 + 画师对外档位（多画风显示「画风 · 尺寸」防同名歧义）；网格小图保持干净零叠加；hover 浮层（仅桌面 hover:hover 媒体查询）与大图 lightbox（自定义 el-dialog：左右切换 + 自由描述 + 档位标签）两处显示标签；点标签 → 下单页预选「画风+尺寸」直跳第三步（复用 F4 入口 A 链路，已浏览器实测）；筛选切换 Transition 淡出淡入不整页刷新；无档位画师（旧模型）不显示筛选行，行为与现状一致。

## ⚠️ mock 占位（待三号波 1 API 交付后替换）

唯一注入点，两处：
1. `useArtistData.js` 的 `applyV035MockFields()`（含 ⚠️ 注释块标注）
2. `ArtistHome.vue` getPublicStyles 回调内的包装块（含删除指引注释）

**替换方法**：删上述两处，`styles.value = res || []` 恢复直读，前端其余零改动——纯函数层（resolveSizeImagePath/deriveGalleryFilters/filterArtworksBySize）读的字段名按派工预判契约：sizes 的 `image`/`image_artwork_id`/`description`/`work_days`，artworks 的 `tags`（尺寸 id 数组）/`description`。**若三号最终契约字段名不同，只需改 useArtistData.js 三个纯函数**。

mock 数据策略：基于真实接口返回确定性附加（尺寸 0 引作品图、尺寸 1 独立图、前 3 个带描述天数；作品按步长 2 分配双档位标签覆盖所有档位、最后一张不标演示「只在全部下出现」）。自测中发现并修复 1 个真实 bug：mock tags 需基于画廊展示列表（非封面作品）分布，否则封面占位导致某档位筛出 0 作品。

## 改动文件

- `components/templates/TplStyleGrid.vue`（F3 核心）
- `components/templates/TplGallery.vue`（F6 重写：筛选行/hover/lightbox/标签跳下单）
- `composables/useArtistData.js`（纯函数层 + mock 块）
- `composables/useOrderForm.js`（preselectBannerText）
- `views/client/ArtistHome.vue`（mock 注入单点）
- `views/client/OrderForm.vue`（预选横幅）
- `views/client/templates/ArtistHome{Classic,Gallery,Folio,Atelier}.vue`（传参）
- `locales/zh-CN.js` + `en.js`（新增顶层 gallery.* 5 键 + orderForm.preselected* 3 键，只加不改；顶层键 35=35）
- 新增 `composables/__tests__/useArtistData.test.js`

## 验证结果

- `npx vitest run`：**153/153 全绿**（基线 125 + useArtistData 22 + F4 banner 7）
- `npx eslint .`：**0 错 0 警** · `npm run build`：✓ 5.57s
- 浏览器实测（vite 5173 + Docker 后端，alice 多画风/carol 旧模型）：
  - F3：点尺寸切图 ✓（封面→尺寸图→取消回封面；image_artwork_id 路径/独立 image 路径/无图兜底封面三条全验）+ 描述天数联动 ✓
  - F4：入口 A 直落第 3 步 + 横幅可见 + 修改回退 + 改选后横幅消失 ✓；入口 B 无预选从头选 ✓
  - F6：7 筛选标签切换全部非空 ✓、无标签作品只在「全部」✓、空档位空态提示 ✓、lightbox 标签点跳下单预选直落第 3 步 ✓、网格小图零叠加 ✓
  - 4 模板（classic/gallery/folio/atelier）+ 旧模型 carol 兼容（无筛选行）✓

## 待办（三号波 1 交付后）

按上述「mock 占位」小节替换联调，完成后申请一号审核。
