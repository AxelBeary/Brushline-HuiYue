# 二号 → 一号：v0.19 第 3 波前端交付（F4 留言板 + HC 自检 + S2 进度条）

> 日期：2026-08-01
> 分支：feat/v019-frontend-wave3

## F4 留言板（3 端）

| 端 | 实现 |
|------|------|
| 客户端 | TplGuestbook.vue 共享组件（零样式，原生表单，语义 class gb-*）。4 模板底部引入，各自视觉：classic 卡片式 / gallery 展签式（细线+字距）/ folio 内联编辑感 / atelier 纸面留言条（宋体+微旋转） |
| 画师端 | Dashboard 右栏新增留言审核区（row 5）：pending 黄底醒目、通过/拒绝/回复（≤500字）、pending 计数徽章、管理员已删除留言自动过滤 |
| 管理端 | AdminDashboard 新增留言管理卡（跨画师表格 + 强制删除确认弹窗） |

- 分页：默认 20 条 + 加载更多；提交后提示"等待画师审核"；429 限流提示
- 画师回复显示在留言下方（带"画师"标识）

## HC 系统自检

- HealthCheck.vue 新页面（/admin/health，requiresAdmin）：开始检查 → 8 项列表（✅/⚠️/❌ + 摘要）→ el-collapse 展开详情（JSON）
- 磁盘项标注"仅供参考"；结果不持久化（刷新消失）
- 诊断包下载：`window.location = '/api/admin/health/download'`（httpOnly cookie 随同源请求携带）
- AdminDashboard 头部加"系统自检"入口按钮

## S2 进度条

- TrackOrder.vue（track 页）：进度条显示"节点名 X/Y" + el-progress 百分比
- **前端计算**：从已有 `workflowStages` + `currentStageId` 推导，不依赖后端新增 stageProgress 字段（零后端改动）
- 回退状态：显示"已回退到「节点名」"，不再显示泛化的"revision"文案

## ⚠️ 待三号补齐（阻塞管理端留言列表）

**后端缺 `GET /api/admin/messages` 端点**——guestbook.routes.js 只有公开/画师/DELETE 路由，管理端列表接口未实现。前端 UI 已按派工契约构建（字段 artist_name/nickname/content/status/created_at），加载失败静默降级（空状态）。三号补一个 GET 端点即可激活。

## 其他说明

- 派工写"OrderTrack.vue"，实际文件名 TrackOrder.vue，已按实际文件修改
- i18n 新增：guestbook.*（客户端）、dashboard.guestbook*（画师端）、admin.guestbook.* + admin.health.*（管理端）、track.timeline.progress/revisionAt、pageTitle.healthCheck（中英双语）

## 验证

- `npx eslint .`：零错误零警告
- `npx vitest run`：87/87 全绿
- `npm run build`：✓ built in 3.99s
- 浏览器实测待安排（留言提交→审核→可见全链路需后端 GET /api/admin/messages 就绪后联调）
