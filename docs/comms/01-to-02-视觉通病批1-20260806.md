# 派工：视觉通病批 1（二号）

> 日期：2026-08-06 · 派工人：一号 · 风险等级：**低**（纯前端）
> 分支：`fix/visual-batch1` · worktree：`../artist-commission-fix1`（已建好，基于 master `3479aa5`）
> 开工第一步：`git merge master` 再读本文件。只动自己分支，不推送不合并，交付写报告。
> **来源**：用户花钱请外部前端 AI 评审。一号逐条对照代码核实后，本批只含**属实项**，不实项已打回（见文末）。

## 任务清单

### V1 动画纪律收敛（属实：theme.css:41 bounce / hover 三档混用 / 时长混用）

现状证据：theme.css L120-127 全局 `.el-button` hover 用 bounce + translateY(-3px)；LandingPage:120 卡片 -4px；NotFound:140 卡片 -3px、NotFound:105 按钮 -2px；时长 0.15/0.2/0.25/0.3s 混用。

规矩（评审建议，一号认可）：
1. hover 位移统一 **-2px**
2. hover/交互 transition 时长统一 **0.15s**（页面级背景切换 0.3s 这类不算交互 hover，保留）
3. `--ease-bounce` 从全局按钮移除，改普通 ease 缓动；bounce 只留给"下单成功"这类庆祝时刻（若现无使用场景可留变量不用，或删变量——你研判，报告说明）

⚠️ theme.css 的 `.el-button` 规则是全局的（管理后台也用），改完必须**画师后台 + 客户端两侧各抽查 2 个页面**确认 hover 观感无回退。

### V2 EP 蓝泄漏排查（待实测，先查后修）

theme.css:92-116 有完整 EP primary 覆写且经 App.vue:19 全局引入，纸面应生效。评审称约稿页"下一步"按钮仍是 EP 出厂蓝。

步骤：起 dev server 用 Playwright 截图实测"下一步"按钮与状态徽章实际渲染色 → 若真是蓝：排查按需引入时 EP base 样式与覆写的加载顺序（vite.config.js resolver），修复并在报告附 before/after 截图与色值 → 若实测不是蓝（评审看错/看的是别处）：如实报告"不实"，不硬改。

徽章功能色（success 绿 / warning 橙）当前**有意**用 EP 出厂色（theme.css:28-31 注释"功能色固定，不跟随主色"）——这是既有设计决策，**本批不动**，报告里提一句即可，是否收进 token 由用户另拍板。

### V3 404 页推荐过滤（属实：NotFound.vue:51-52）

`getAll().slice(0, 6)` 无状态过滤，"已排满(full)/休息(break)/隐藏(hidden)"画师也进导流位。改：只保留 `status === 'open'` 的，过滤后再 slice(0, 6)；过滤后为空则整段隐藏（现有 v-if="artists.length" 已兜底）。

### V4 落款文案（属实：zh-CN.js:209 / en.js:209）

`Powered by 画师约稿平台` 自己 Power 自己。改为中性落款：`绘约 · 画师约稿平台`（zh）/ `HuiYue · Artist Commission Platform`（en）——若你有更好的中性措辞可在报告提议，先用上述方案。

### V5 Landing 卡片冗余按钮（属实：LandingPage.vue:36-38）

整卡已可点（click + keyboard + role=button），卡内"进入主页"按钮冗余。删按钮，卡片 hover/焦点态保留。

## 授权文件

- `web/src/styles/theme.css`（V1）
- `web/src/views/client/NotFound.vue`（V3）
- `web/src/views/client/LandingPage.vue`（V5）
- `web/src/locales/zh-CN.js` / `en.js`（V4）
- EP 覆写排查如需动 `web/vite.config.js` 或 `main.js` 引入顺序（V2，先报告再动）
- 相关测试文件适配

**不要动**：OrderForm.vue（五号并行批，见 `01-to-05-约稿页选中态批-20260806.md`）；四个画师主页模板（待批 1 合入后另派体检批）；server 任何文件。

## 自测要求

1. web `npx vitest run` 全绿（基线 215）
2. eslint 0 错
3. Playwright 或手动起 dev 验证：hover 位移/时长生效、404 过滤生效、Landing 卡片无冗余按钮；V2 附实测截图与色值

交付后通知用户转告一号审核。

## 附：一号已打回的不实指控（勿做）

- "avatar null 硬渲染 `<img>` 破图"——客户端全用 el-avatar 带首字兜底，无裸 img
- "价格不按 sort_order 排"——后端 ORDER BY sort_order ASC（artist.service.ts:312），疑似数据问题
- "无画风仍挂'选择此画风约稿'"——Classic:46 已 v-if 整段隐藏
