# 05-to-01-统计页补全-交付-20260808.md

> 交付：五号 → 一号
> worktree：`../artist-commission-w5`（分支 `beta/stats-page-full`）
> 任务：画师统计页补全（总览 + 按日趋势 + 事件明细 byName + 三态适配）

## ⚠️ 重要核实结论：派工描述的基线与 master 现状不符

派工文件称「现状 StatsPage.vue 只有 98 行基础版（总览 + byDay 趋势），缺事件明细（byName）区块」。
**经核实，master 上 StatsPage.vue 从 Step5（commit 7a4a92c，已合入 master 且是唯一历史）起就是完整实现**：

- 当前文件 99 行，已含：
  - 总览卡（total）✅
  - byDay 柱状图（纯 CSS/div）✅
  - **byName 事件明细区块**（事件名 + 次数列表）✅
  - 三态适配（mode !== 'on' 显示 disabledHint；mode==='on' 且无数据显示 empty）✅
  - 纸墨 token 视觉（var(--ink)/--ink3/--ink4/--hq/--card/--line），双主题自适应 ✅
  - zh-CN.js / en.js 的 stats.* 键齐全（含 events.* 事件名映射）✅
- 后端契约与前端完全一致：`GET /api/artist/tracking/summary?days=N` 返回 `{ mode, enabled, total, byDay, byName }`（tracking.routes.ts + tracking.service.ts 均已核实）

**即：派工要求的功能全部已在 master 存在，非本批新增。** 推测派工基线信息过时（或基于旧分支认知）。

## 本批实际改动（1 处真实缺口）

**补 `artist_page_enter` 事件名的 i18n 键**（zh/en 各 1 行）：
- 后端白名单含 `artist_page_enter`（画师访问自己主页上报，带 artist_id → 会进画师 byName 统计），但原 zh/en `stats.events` 都缺该键 → 事件明细会显示原始英文事件名 `artist_page_enter`
- zh-CN.js: `artist_page_enter: '主页访问'`
- en.js: `artist_page_enter: 'Homepage views'`

授权范围内（i18n 两文件），未碰后端与其他页面。

## 验证结果（全绿）

| 门禁 | 结果 |
|------|------|
| npm run test:web | ✅ 13 files / 215 tests passed |
| npm run lint | ✅ 0 错误 |
| npm run build | ✅ 通过（12.1s） |
| npm run check:i18n | ✅ OK，无新增硬编码中文 |

**实测链路（隔离环境，独立 DB stats-test.db + 独立端口 3998/5174）**：
1. TOTP 绑定 alice(10001) → fetch 登录拿 cookie ✅
2. `GET /api/artist/tracking/summary?days=14` 返回 `{mode:'on', enabled:true, total:24, byName:8项, byDay:6天}` ✅
3. 造 24 条事件（8 事件名 × 6 天）→ 浏览器打开 /stats：
   - **有数据态（mode=on）**：总览 24 / 按日趋势 08-03~08-08 / 事件明细 8 行（主页访问6、工作台访问5、订单列表4、排期看板3、快捷操作2、作品管理2、偏好1、留言1）—— `artist_page_enter` 正确显示「主页访问」✅
   - **非 on 提示态（mode=hidden）**：显示「统计未开启，请在管理后台开启」✅
   - **墨黑主题（ink）**：数据正常渲染 ✅
4. PIL 量化三张截图：宣纸主题主色 #F5F4EF（亮度 242-248）、墨黑主题主色 #171611（亮度 26-29）——双主题真实生效，非黑屏 ✅

## 截图

- `workspace/temp/stats-page/stats-page-on-data.png`（有数据态，宣纸）
- `workspace/temp/stats-page/stats-page-hidden-hint.png`（非 on 提示态）
- `workspace/temp/stats-page/stats-page-on-data-ink.png`（有数据态，墨黑主题）

## 风险与建议

1. **派工基线核对**：建议一号确认派工时是否基于旧分支/旧认知——若用户期望的「统计页做全」是已在 master 的形态，本批实际无功能缺口；若用户期望更丰富（如漏斗、占比、导出），属新需求需另立派工。
2. **事件名 i18n 覆盖**：后端白名单 22 个事件中，zh/en 已映射 12 个（客户端漏斗类 order_form_* 等 10 个不映射——因为画师统计只含带 artist_id 的后台事件，客户端事件不会出现；此设计合理，未扩键）。
3. 未推送未合并，等待一号审核。
