# 02-to-01-巡检D-全局工程-交付-20260807

> 巡检人：二号D（client-frontend，全页面巡检第 D 路）
> 范围：全局工程面（路由守卫 / 布局框架 / 主题系统 / 公共组件 / composables / api 层 / 响应式全局）
> 方式：只读静态分析（grep/脚本比对）+ 浏览器实测（vite dev 5177，截图 + DOM 采样）
> 结论概览：🔴 0 项 ｜ 🟡 7 项（D1-D7）｜ 🟢 4 项（D8-D11）｜ ✅ 健康项见各节
> 全程零代码改动：不建 worktree、不 git 写；临时脚本已删，`git status` 仅含其他路产物

---

## 一、巡检结论总表

| # | 级别 | 位置 | 问题 | 建议 |
|---|------|------|------|------|
| D1 | 🟡 中 | views/artist/Settings.vue:379-383 | ACCENT_PRESETS 强调色名硬编码中文「青/碧/蓝/靛/紫」，渲染到 `:title`（hover 可见）；en 界面显示中文，违反 i18n 红线 | 改走 `t('pref.accentNames.*')`（ThemePicker.vue:62-66 同款键已存在）；5 色值列表建议收敛单一数据源 |
| D2 | 🟡 中 | web/src/main.js:40-44 | 全局 errorHandler 的 ElMessage.error 硬编码中文「页面出了点小问题，请刷新重试」；且每次错误动态 `import('element-plus')`（模块已缓存，冗余调用） | 用 i18n 键或至少定义常量文案；顶部静态引入 ElMessage |
| D3 | 🟡 中 | web/src/api/index.js:29 | 拦截器兜底文案「网络错误，请稍后重试」硬编码中文（无 code 或非业务错误时显示，en 界面中文） | 提供 i18n 键（如 errors.NETWORK_ERROR），或由调用方兜底 |
| D4 | 🟡 中 | router/index.js + views/client/NotFound.vue | 全局 404 只有客户端版：画师/管理员登录后访问未知路径（如 /dashboard/xxx）会落到客户端 NotFound 页——无侧边栏、纸墨 token 不挂、显示客户端浮窗与「回平台首页」 | 404 页按路由前缀区分（/dashboard|/admin 前缀渲染后台版 404 或重定向回后台首页） |
| D5 | 🟡 低 | components/ArtistLayout.vue:pageTitle computed | /admin 子路由（/admin/artists 等）顶栏标题为空：pageTitle 只在 BASE_MENU_ITEMS + admin 根匹配，子路径无命中返回 '' | pageTitle 对 /admin* 前缀回退 `t('menu.admin')` 或加子路由映射 |
| D6 | 🟡 低 | views/client/TrackOrder.vue:146 | 支付进度条颜色硬编码 `'#67c23a' : '#409eff'`，蓝色不随五色 accent（客户端主色体系） | 用 `var(--color-success)` / `var(--color-primary)` 或 CSS 变量 |
| D7 | 🟡 低 | 断点全局 | 断点值分散：600（ArtistLayout 抽屉）/ 640（OrderTimeline）/ 768（App.vue el-dialog、TplGallery/Hero/StyleGrid/TierGrid）/ 900（ArtistLayout 窄屏），无统一断点常量/变量 | 抽公共断点常量（如 `BREAKPOINTS = { mobile: 600, tablet: 768, narrow: 900 }`）并逐步替换 |
| D8 | 🟢 低 | views/client/ArtistHome.vue:191 | `color: #333` 硬编码文字色（绕过 --pal-text/--text-primary） | 改 var(--pal-text) |
| D9 | 🟢 低 | 组件样式 30+ 处 | `color: #fff` 散落（多为彩底白字 badge/印章/遮罩，双主题下可接受） | 可选：纸墨体系补 --ink-pure/--paper-pure 白字 token，逐步替换 |
| D10 | 🟢 低 | locales/en.js landing 块 | en 缺 `landing.weibo`/`landing.bilibili`（zh 有；两键当前全库无引用=死键，实际影响为零） | 补键或随未来使用点一起补 |
| D11 | 🟢 低 | router/index.js | 已登录用户访问 /login 仍显示登录页（无重定向到 dashboard） | 可选：守卫加「已登录访问 /login → 跳 /dashboard」 |

## 二、路由与守卫（✅ 健康为主）

- 守卫覆盖完整：全部画师后台页 requiresAuth、/admin 父+子 requiresAdmin；未登录 → /login?redirect=；非管理员访问 /admin → /dashboard（router/index.js）
- 404 兜底独立页（v0.34 后不复用 LandingPage）；/manual-order→/orders/new、/rules→/settings?tab=showcase 重定向不断链
- titleKey 全部走 i18n（P2-C），25 个路由 titleKey 经脚本比对 zh/en 全部存在
- 🔵 前端守卫依赖 localStorage 标记（artist_logged_in）为刻意设计（注释在案：真认证在后端 httpOnly cookie），非漏洞
- ⚠️ D4 见上：后台域 404 视觉断裂

## 三、布局框架（✅ 健康）

- ArtistLayout（613 行）：折叠（手动 + ≤900px 自动）、≤600px 抽屉、三组导航（工作/经营/门面）、留言角标、语言/主题切换、埋点（PAGE_VIEW_EVENT_MAP 白名单）、mq 监听 onUnmounted 移除、主题 scope 挂/摘成对
- AdminLayout：顶部 Tab 轻量导航 + sticky；el-empty 空态统一纸墨化在布局层收敛（清扫批#5）；tab 与路由一一对应
- Login.vue 同样挂 enterArtistScope（实测 data-artist-theme=paper ✅）
- 客户端壳 ClientFloatingActions 被 ArtistHome / NotFound / OrderForm / TrackOrder 一致复用（仅 ThemePicker FAB，跨页行为一致）
- ⚠️ D5 见上：/admin 子路由顶栏标题为空

## 四、主题系统（✅ 健康，实测验证）

- theme.css：语义色 + 五色 accent（亮暗 10 组）+ EP 变量覆写（:root:root 提升特异性）完整
- palettes.css：4 配色 paper/ink/dusk/moss × 亮暗（派工提到的「五色 natural-v2」经核实为 utils/track.js 埋点版本号 EVENT_VERSION，非配色数）
- artist-tokens.css：纸墨 token 全部挂 `html[data-artist-theme]`（隔离策略正确），旧变量兼容映射齐全；7 色语义（花青/朱砂/石绿/藤黄/赭石等）一对一
- 硬编码色值扫描（全库 87 处）：绝大多数为 #fff 中性文字或变量 fallback（var(--zs, #BC3A2B) 等），未发现绕过 token 的系统性硬编码；个别见 D6/D8
- ⚠️ D1/D6 见上；数据源重复：5 色值在 theme.css / ThemePicker.vue / Settings.vue / ArtistHome.vue:90 四处各自定义（当前值一致，未来改色有漂移风险）

## 五、公共组件（✅ 健康）

- 复用面确认：OrderTimeline（画师 OrderDetail + 客户端 TrackOrder 跨端复用）、DeliverDialog（OrderDetail + QueueBoard）、UndoToast（QueueBoard）、CardHead/InkEmpty/StatusChip（dashboard 各卡 + OrderDetail 系）、WorkflowOverviewStrip（WorkflowPaymentEditor）
- v-for key 全部齐全（TplGallery/TplHero/TplStyleGrid/TplTierGrid/OrderTimeline 的 :key 在下一行——此前审计报告「templates 组件无 key」为误报，当前代码已带 key）
- 共享组件均带变量 fallback，双主题可用；无默认样式污染（Tpl* 样式 scoped/模板级）

## 六、composables（✅ 健康）

- usePasteUpload：document 级 paste 监听 + onUnmounted 移除；类型/数量/大小校验齐全；i18n 提示；纯文本粘贴不拦截
- useDropGuard：捕获阶段守卫 + stopImmediatePropagation（正确解决同元素冒泡监听）+ 1.5s 警告节流
- useSignatureRefresh：防重入、@error 按图 300ms 防抖 + 每图最大重试 2 次、定时刷新不受重试限制、onUnmounted 双清（clearInterval + clearTimeout）
- useScrollReveal：IntersectionObserver + MutationObserver（异步组件补挂）、onUnmounted 双断连
- useStickyCta：watch 哨兵就绪 + 兼容 expose proxy、onUnmounted 断连
- 🔵 useDropGuard 的 lastWarnAt 为模块级（跨实例共享节流窗口——语义上合理，避免多组件同时弹提示）

## 七、api 层（✅ 健康）

- 脚本自动比对：前端 124 个调用路径 vs 后端 167 个注册路由，**0 缺失**（含模板字符串参数归一化）
- 后端注册但前端未调用仅 5 个且全部合理：/api/health（容器自检）、/*、/api/admin/health/download（HealthCheck 下载按钮用 window.open 类跳转）、/api/anon-token + /api/events（track.js 用 fetch 直连，非 axios）
- 响应拦截器完善：错误码 i18n 翻译 + detail 命名插值（{count}）+ 401 清状态跳登录 + LOGIN_CODES 排除登录态错误（P1-3 修复在案）
- 上传超时 120s 单独覆盖（P2-#14）
- ⚠️ D3 见上：兜底文案硬编码中文

## 八、响应式全局（🟡 D7）

- 断点分散 600/640/768/900（详见 D7）；无固定定位泛滥（仅 FAB + AdminLayout sticky top）；无横向滚动条专项问题
- prefers-reduced-motion 全局尊重（theme.css / templates.css 两处）
- App.vue 移动端 el-dialog 90% 宽度兜底

## 九、i18n（✅ 健康，见 D10）

- zh 1519 键 / en 1517 键（脚本 import locale 后递归叶子统计）
- 路由 25 个 titleKey 全部存在；$t() 使用 890 键，未定义仅 18 个且全为动态模板字符串（common.orderStatus.${...} 等）——枚举值经核对全量覆盖（orderStatus 7 值 / statusShort 4 值 / priority 3 值均在 locale 定义）
- AdminLayout/ArtistLayout 的 labelKey、ThemePicker nameKey 均存在

## 十、浏览器实测（截图存 workspace/temp/inspect-d/）

页面：/（landing）、/artist/alice（画师主页）、/login（登录）、/no-such-page-xyz（404）
- 4 页全部截图成功，console.error / pageerror **零报错**
- DOM 采样（Playwright evaluate getComputedStyle）：

| 页面 | data-artist-theme | data-palette | body 背景 | 主按钮色 |
|------|------------------|--------------|-----------|----------|
| / | null（客户端零影响 ✅） | null | #f5f5f5（--bg-page） | #356B69（accent1 青 ✅） |
| /artist/alice | null ✅ | paper | #f5f5f5（容器内 --pal-* 生效，h1=#2b2622 pal-text ✅） | — |
| /login | paper ✅ | null | #f5f4ef（纸色 ✅） | #33526E（花青 ✅） |
| 404 | null ✅ | null | #f5f5f5 | #356B69 |

- 重点验证：后台 scope 内 `--color-primary` / `--el-color-primary` 均 = #33526E 花青（EP 变量跟随 token 正确）；登录主按钮实测 bg rgb(51,82,110)=#33526E ✅；客户端路由下 data-artist-theme 严格为 null（验收 10 隔离项 ✅）
- 附注：登录页首个 button（ThemePicker accent 圆点）为客户端五色青 #356B69——属 ThemePicker 数据色（客户端主题选择器），与后台 token 无关，非问题

## 十一、数据核验记录

| 结论 | 核验方式 |
|------|----------|
| 前端 124 调用 vs 后端 167 路由 0 缺失 | node 脚本正则提取 + 归一化比对（临时脚本，已删） |
| i18n 键数 / titleKey / 动态键枚举 | node import locale 递归叶子 + 路由/全库 $t 扫描 |
| 硬编码色值 87 处 | PowerShell 逐文件正则扫描 |
| v-for key 齐全 | 逐文件读 v-for 下一行（纠正同行正则误报） |
| 主题 token 生效 / scope 隔离 | Playwright DOM 采样（getComputedStyle 数值） |
| 4 页零 JS 报错 | Playwright console/pageerror 监听 |
| en 缺 landing.weibo/bilibili | node locale 对比 + 全库引用 grep（0 引用=死键） |

## 十二、给一号的处置建议

1. D1（Settings 硬编码中文）为 i18n 红线违规，建议排入下批修复（改动小：5 行 name → nameKey）
2. D2/D3（全局错误兜底中文）涉及 main.js / api/index.js（一号协调范围），建议与 D1 同批处理
3. D4（后台 404 断裂）属体验缺陷，需定产品口径：后台未知路径重定向 or 后台版 404
4. D5（admin 顶栏空标题）一行修复级，可随 D 批顺手
5. D6-D10 低优先，随代码健康巡检排期

