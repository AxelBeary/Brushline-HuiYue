# 一号→二号：SPEC-005 日历视图 + #8 话术界面（2026-08-02）

## 任务 A：SPEC-005 排期日历视图（~6h）

Spec：`docs/specs/SPEC-005-排期日历视图.md`。用户已拍板全部决策。

核心：QueueBoard.vue 顶部加视图切换（列表 / 日历），日历为月历 + 订单画带。

要点：
- 月历格子：订单画带（确认日→截稿日），格内"昵称-类型"
- 三种视觉：实心（正式）/ 半透明虚线（缓冲）/ 斜纹（未设截稿，画满到月末）
- 左右翻月，先只看（不拖拽）
- 画师可设默认视图（localStorage，复用"默认面板"模式）
- 数据源：`artistApi.getQueue()` + `artistApi.getQueue('buffer')` 合并（已有 deadline/created_at/client_name/tier_name）
- 不需要后端改动（字段已齐）

## 任务 B：#8 话术界面改进（~2h）

Spec：`docs/requirements/REQ-013-整理.md` #8 节（界面改进部分，P2）。

StageListView.vue 重构：
- 变量按钮只显示一次（顶部公共区），点击插入当前聚焦编辑框
- 节点 ≥3 时话术编辑区默认折叠（节点名 + 前 20 字预览）
- 视觉层级清晰

不做多模板随机（P3，需 DB 迁移，排后续）。

## 任务 C：SlotOverview 切 profile slotDisplay（~10min）

三号已补 profile API 返回 slotDisplay。SlotOverview.vue 删掉 `artistPublicApi.getProfile(subdomain)` 调用，改读 `store.profile.slotDisplay`。

## 授权文件

- `web/src/views/artist/QueueBoard.vue`
- `web/src/views/artist/WorkflowManage.vue` 或 `StageListView.vue`（话术）
- `web/src/components/artist/dashboard/SlotOverview.vue`
- `web/src/api/index.js`（如需新增方法）
- `web/src/i18n/`
- `web/test/`

## 分支

`feat/v024-spec005`，worktree `../artist-commission-fe`（pull master 后建分支）。

完成后写 comms `02-to-01-v024-spec005-{日期}.md`。
