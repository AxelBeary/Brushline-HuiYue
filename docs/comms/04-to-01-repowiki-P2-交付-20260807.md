# 04-to-01 repowiki P2 交付：认证/授权系文档抽样核对报告

- 派工：`docs/comms/04-派工-repowiki-P2-20260807.md`（一号 → 四号）
- 核对日期：2026-08-07
- 核对基准：master HEAD `4152fbc`（STATUS v19）——认证已 TOTP 化（REQ-027）、后端 100% TS、cookie 会话
- 核对人：四号（需求整理者，纯只读审计：零代码改动、无 worktree、无 git 写操作；本报告不 commit，由一号合入）
- 结论口径：🔴 严重过时 / 🟡 轻微 / 🟢 正确 / ⚪ 缺失（=代码已有、文档未覆盖）
- 说明：外部贡献文档（C:\Users\qly19\Desktop\repowiki\）原样未改；判定以 master 代码为准，不信文档自我描述

---

## 〇、最重要结论（与派工预期相反）

派工与 08-06 核对报告第五节 P2 建议均假设「认证系文档大概率同样过时（旧登录码/旧 JWT 流程）」。

**实测否定该假设：本次抽样的 14 篇认证/授权系文档，核心机制描述全部已是 TOTP 时代，无一 🔴 严重过时。** 连派工点名怀疑的 `TOTP 双因素认证.md` 反而是最"新鲜"的一篇（连 `npm run totp:rebind` CLI、qrcode 依赖、TOTP_* 错误码都写对了）。

**推断**：外部贡献者按当前代码分主题编写，认证主题恰好编写/更新最晚；不能把「数据库模式设计过时」的结论推广到认证系。剩余 112 篇中的过时风险集中在**非认证主题**（部署/前端/数据库等），若要完整收尾 P2 应改派其他主题抽样。

---

## 一、核对表（14 篇）

| # | 文档 | 判定 | 依据（对照代码） |
|---|------|------|------|
| 1 | `API 接口文档\画师管理接口.md` | 🟢（含 5 处 🟡 文件名） | auth 三端点 verify/me/logout 与 auth.routes.ts 逐条吻合；DTO 脱敏、错误码正确；**仅 app.js/index.js 旧文件名引用** |
| 2 | `API 接口文档\管理员接口.md` | 🟢 | TOTP 绑定路由 bind-init/bind-confirm/reset（admin.routes.ts:140/162/195）、transfer 双 TOTP + 双维限流、健康检查 8 项（db/migration/uploads/disk/integrity/backup/secret/node 与 health.service.ts 逐项吻合）、错误码全对 |
| 3 | `后端开发指南\认证授权系统\认证授权系统.md` | 🟢 | TOTP 模块（totp.ts）、会话 HMAC payload{id,t,v}、token_version 吊销、TOTP_MAX_ATTEMPTS=5/锁定 15 分钟、防枚举、SESSION_SECRET 生产 fail-fast、AUTH_DEV_MODE 生产 fail-fast——全部与 auth.service.ts 一致 |
| 4 | `后端开发指南\认证授权系统\TOTP 双因素认证.md` | 🟢 | totp.ts 算法（RFC 4226/6238、Base32、±1 窗口、timingSafeEqual）、CLI `scripts/totp-rebind.ts`、`npm run totp:rebind`、qrcode ^1.5.4、TOTP_NOT_BOUND/TOTP_INVALID/TOTP_LOCKED/TOTP_BIND_INVALID 错误码——全部属实 |
| 5 | `后端开发指南\认证授权系统\会话管理.md` | 🟡 | 正文正确；**图例残留旧登录** `POST /login (用户名/密码/TOTP)`；正文称「暴露登录、登出、刷新等接口」——实际无会话刷新接口（仅前端 useSignatureRefresh 做文件签名刷新，非会话续期） |
| 6 | `后端开发指南\认证授权系统\权限控制.md` | 🟢 | requireAuth/requireAdmin 流程、401/403 错误码、DTO 脱敏、cookie 优先 Bearer 兜底——与 middleware/auth.ts 一致 |
| 7 | `后端开发指南\认证授权系统\安全最佳实践.md` | 🟢 | 未发现旧流程描述；引用 Login.vue/totp.ts 等均存在 |
| 8 | `后端开发指南\中间件开发\认证中间件.md` | 🟢 | extractToken（cookie 优先）→ verifySession → deleted_at/token_version 检查 → 401 NOT_LOGGED_IN/SESSION_EXPIRED/ACCOUNT_NOT_FOUND/ACCOUNT_DISABLED/TOKEN_REVOKED、403 ADMIN_REQUIRED——与代码逐条吻合 |
| 9 | `架构设计\前后端通信协议\认证协议.md` | 🟡 | 正文已 TOTP/cookie 化；**图例残留旧两步登录** `POST /login (用户名/密码)` + `POST /verify-totp`；正文称「自动续期策略」——实际无自动续期 |
| 10 | `核心功能模块\认证系统.md` | 🟢 | 明写「QQ号+TOTP 动态口令作为主登录方式，替代登录码方案」——最核心结论正确 |
| 11 | `项目概述\核心功能特性\认证授权系统.md` | 🟡 | 整体正确；**残留一句**「登录视图：处理用户名/密码与TOTP输入交互」（旧描述） |
| 12 | `架构设计\安全架构.md` | 🟡 | 正文「认证与授权（TOTP 双因素、**JWT**/HMAC Token…）」——实际无 JWT，纯 HMAC 会话 token；「上传与静态资源由 **app.js** 统一处理」——实际 app.ts |
| 13 | `故障排除\错误码参考.md` | 🟢（1 处小瑕疵） | 认证错误码列表正确；仅「刷新 Token」字样（无刷新接口，小瑕疵并入修补） |
| 14 | `knowledge\zh\环境变量与运行时配置系统.md` | 🟢 | SESSION_SECRET/COOKIE_SECRET/ADMIN_QQ/AUTH_DEV_MODE 全部真实存在（grep server/src 验证：COOKIE_SECRET 回退链 app.ts、ADMIN_QQ fail-fast、AUTH_DEV_MODE 生产 fail-fast、.env.example 逐项吻合） |

**汇总：🔴 0 篇 / 🟢 9 篇 / 🟡 5 篇（均为局部残留）/ ⚪ 0 项（重要缺失）。**

---

## 二、🔴 严重过时清单

**无（0 项）。** 这是本次审计与派工预期的最大差异点——认证系文档没有发现任何「旧登录码 / JWT Bearer 主路径 / 客户端直绑 TOTP」的硬错误。

（对照：08-06 P0 报告中的认证接口.md 通篇旧流程；本次 14 篇里零篇达到该程度。）

---

## 三、🟡 轻微过时清单（修补即可，全部为 1-2 行文字/图例替换）

| # | 文档 | 过时段落 | 当前正确版本 |
|---|------|----------|--------------|
| 1 | 画师管理接口.md | cite 与正文引用 `server/src/app.js`、`server/src/index.js`（约 5 处，含 mermaid 图「应用启动 index.js / 应用构建 app.js」） | master 已 100% TS：`app.ts` / `index.ts`（已实测 .js 不存在） |
| 2 | 会话管理.md | 图例 `Client->>Routes : "POST /login (用户名/密码/TOTP)"`；正文「暴露登录、登出、刷新等接口」 | 唯一登录端点为 `POST /api/auth/verify {qqNumber, code}`（TOTP）；无会话刷新接口，无用户名/密码登录 |
| 3 | 认证协议.md | 图例 `POST /login (用户名/密码)` + `POST /verify-totp (验证码)`；正文「自动续期策略」 | 同上；会话无自动续期，过期需重新 verify 登录 |
| 4 | 项目概述\核心功能特性\认证授权系统.md | 「登录视图：处理用户名/密码与TOTP输入交互」 | 登录页仅 QQ 号 + 6 位 TOTP 码 |
| 5 | 安全架构.md | 「TOTP 双因素、JWT/HMAC Token」；「app.js 统一处理」 | 无 JWT，会话为 HMAC-SHA256 签名 token（auth.service.ts）；app.ts |
| 6 | 错误码参考.md | 「刷新 Token」字样 | 无刷新接口；登出=bumpTokenVersion+clearCookie |

> 其中 2/3/4 的残留属于**文档正文自洽但图示/措辞未同步**——正文没有声称存在 /api/auth/login，图例却画了旧流程，属局部自相矛盾，修图例即可，不构成 🔴。

---

## 四、⚪ 缺失（可选补，非必须）

1. 管理员接口.md：transfer 限流写了「IP 维度与目标 QQ 维度」但未给数值——实际为 IP 5 次/15 分钟 + 目标 QQ 3 次/15 分钟（admin.routes.ts:259-264），修补时可补一句。
2. 会话管理.md / 认证协议.md：可补一句「登录失败响应 detail 含 remainingLockMs」（auth.routes.ts 已实现，08-06 报告亦曾列为缺失项）。

---

## 五、建议（交一号决策）

| 优先级 | 动作 | 范围 | 理由 |
|--------|------|------|------|
| P1 | **轻量修补批**（不重写） | 上表 6 处 🟡 + 2 处 ⚪ 可选补 | 全部为图例/文件名/措辞级，1-2 行/处；派单可参照 08-06 P1「低成本的引用修补」同模式（`app.js→app.ts`、图例改 verify、删「JWT/用户名密码」字样） |
| P2 | **改派非认证主题抽样** | 剩余 112 篇中的部署/CSP/Sentry/前端主题 | 认证系已证实新鲜；过时风险已转移——例如部署主题与 `SENTRY_DSN_BACKEND`/`SENTRY_DSN` 环境变量陷阱、CSP connect-src、Caddyfile 压缩缓存等近期改动相关，价值更高 |
| P3 | 数据库模式设计.md 重写收尾核对（可选） | 08-06 报告存疑标注项 | 上轮报告第六节留有 `order_payments`/`order_payment_installments` 未逐列比对存疑项，可并入重写批一并复核 |
| 无需动 | 其余 9 篇 🟢 | — | 内容与 master 一致，改反而引入风险 |

**不建议**：按 P0 模式整篇重写本次 14 篇中的任何一篇——它们的核心机制描述全部正确，重写成本 > 收益。

---

## 六、存疑标注（暴露不确定性）

1. `会话管理.md` / `权限控制.md` / `安全最佳实践.md` / `安全架构.md` / `核心功能模块\认证系统.md` 等未全文逐句比对，判定基于关键行抽样（login/JWT/Bearer/验证码/密码/token/会话/TOTP 信号扫描 + 与代码关键点对照）；若一号需要全量逐句核对可再派一轮。
2. `管理员接口.md` 的 health.service.ts 行号/图表来源（`health.service.ts:157-173` 等）未逐行核对行号，仅核对检查项内容（8 项全对）。
3. 外部文档的 mermaid 图与正文偶有矛盾（见三、2/3），判定以**正文端点清单**为准；若一号认为图示错误同样误导读者，可要求修补批连图例一起改。
4. 本报告不 commit；master 仅只读（HEAD 已确认 `4152fbc`，比派工文件所载 `2449b6b` 更新——派工前已有人合入 STATUS v19，不影响核对结论）。

---

*纯只读审计完成：零代码改动、无 worktree、无 git 写操作、未 commit。repowiki 原目录未改一字。*
