# 🤖 开发自参考（AI Agent 内部文档）

> 这是给 AI 助手（我自己）看的文档。记录项目结构、设计决策、扩展点。
> 下次接手这个项目时先读这个文件。

---

## 项目位置
`D:\Hermes Agent CN Desktop\workspace\artist-commission\`

## 一句话描述
画师约稿管理平台：多画师共用一套系统，每人有独立子域名主页。客户零注册下单/查进度，画师通过后台或QQ Bot管理排期。沟通全走QQ，平台不经手钱。

---

## 技术栈
- 前端：Vue 3 + Element Plus + Pinia + vuedraggable + **vue-i18n@9**（中英双语）
- 后端：Node.js + Fastify 5
- 数据库：SQLite (better-sqlite3, WAL 模式)
- 文件存储：本地磁盘 uploads/
- 反向代理：Caddy（泛解析 + 自动 HTTPS）
- 部署：Docker Compose
- 测试：Vitest（内存数据库，42 个用例）
- QQ Bot：NapCat/LLOneBot（Phase 2，OneBot 11 协议）

---

## 目录结构（v0.3 normalized）
```
artist-commission/
├── docker-compose.yml          # 两个服务：web（healthcheck）+ caddy（depends_on healthy）
├── Dockerfile                  # 多阶段构建：node:22-slim 前端构建 → 生产运行
├── Caddyfile                   # 泛解析 *.{$DOMAIN} → web:3000
├── entrypoint.sh               # 容器启动：init db → start server（绝对路径，不依赖 CWD）
├── .env.example                # 环境变量模板
├── .gitignore                  # node_modules / dist / data / uploads / .env / *.db
├── .dockerignore               # 排除构建无关文件
├── server/
│   ├── package.json            # v0.2.0，含 vitest
│   ├── vitest.config.js        # 测试配置（内存DB、串行执行）
│   ├── tests/                  # 单元测试（32 个用例）
│   │   ├── setup.js            # 共享：内存数据库 + 显式建表 + seed 工具
│   │   ├── order.service.test.js   # TC-O-01 ~ TC-O-14
│   │   ├── auth.service.test.js    # TC-A-01 ~ TC-A-09
│   │   ├── artist.service.test.js  # TC-R-01 ~ TC-R-05
│   │   └── validate.test.js        # TC-V-01 ~ TC-V-04
│   └── src/
│       ├── index.js            # 启动入口（调用 buildApp + listen）
│       ├── app.js              # Fastify 应用工厂（路径基于 __dirname，不依赖 CWD）
│       ├── db/
│       │   ├── connection.js   # SQLite 连接（WAL + foreign_keys，:memory: 安全）
│       │   ├── init.js         # 建表 SQL（导出 initDatabase，import 无副作用）
│       │   └── seed.js         # 种子数据（2个测试画师）
│       ├── features/           # ⭐ Feature-based 目录（routes 不含裸 SQL）
│       │   ├── auth/
│       │   │   ├── auth.service.js   # 登录码 + HMAC 会话
│       │   │   └── auth.routes.js    # send-code / verify / me（setInterval unref）
│       │   ├── artist/
│       │   │   ├── artist.service.js # 画师/档位/作品/须知 CRUD
│       │   │   └── artist.routes.js  # 公开主页 + 后台 CRUD + GET /api/artists
│       │   ├── order/
│       │   │   ├── order.service.js  # 订单核心：创建/队列/拖拽/状态/统计/交付
│       │   │   └── order.routes.js   # 下单/查询/管理/交付（全部走 service）
│       │   ├── upload/
│       │   │   └── upload.routes.js  # 图片/参考图/交付文件上传（uploadDir 由 app.js 传入）
│       │   └── admin/
│       │       ├── admin.service.js  # 全局统计查询
│       │       └── admin.routes.js   # 画师 CRUD + 全局统计（走 service）
│       └── shared/             # 跨 feature 共用
│           ├── validate.js     # clamp + isValidQq
│           └── middleware/
│               └── auth.js     # requireAuth + requireAdmin
├── web/
│   ├── package.json
│   ├── vite.config.js          # 开发代理 /api → localhost:3000
│   ├── index.html
│   └── src/
│       ├── main.js             # Vue + Pinia + i18n + ElementPlus + Router
│       ├── App.vue             # ElConfigProvider(动态locale) + 全局CSS变量(亮/暗)
│       ├── api/index.js        # axios 封装 + 所有 API 函数（统一入口）
│       ├── router/index.js     # 路由表（/artist/:subdomain 参数化）+ 守卫 + 404 catch-all
│       ├── i18n/index.js       # vue-i18n 实例 + setLocale()（localStorage 持久化）
│       ├── locales/
│       │   ├── zh-CN.js        # 中文语言包（~150 条）
│       │   └── en.js           # 英文语言包
│       ├── stores/
│       │   ├── artist.js       # Pinia store：token/profile/stats
│       │   └── theme.js        # 主题 store：检测系统偏好 → localStorage → html.dark
│       ├── components/
│       │   ├── ArtistLayout.vue  # 后台布局：侧边栏+顶栏+响应式+ThemeToggle
│       │   └── ThemeToggle.vue   # 🌙/☀️ 主题切换 + EN/中 语言切换（极简按钮，无 el-dropdown）
│       └── views/
│           ├── client/         # 客户端4页：主页/下单/查进度/交付下载
│           ├── artist/         # 画师后台10页：登录/仪表盘/排期/订单/录单/设置/价格/作品/须知
│           └── admin/          # 管理员2页：面板/画师管理
├── uploads/                    # 上传文件存储（Docker volume 持久化）
└── docs/                       # 文档
    ├── 画师使用说明书.md
    ├── 维护说明书.md
    ├── 开发自参考.md            # 本文件
    ├── 开发→生产切换指南.md
    ├── tdd-spec-v0.1.md        # TDD 规格（TC 编号用例定义）
    └── changelog.md            # 变更日志
```

---

## 核心设计决策

### 1. Feature-based 目录（v0.2 重构）
- 每个业务域一个目录：`features/{auth,artist,order,upload,admin}/`
- 每个 feature 包含 `*.service.js`（业务逻辑）+ `*.routes.js`（HTTP 路由）
- 跨 feature 共用的放 `shared/`（validate、middleware）
- `app.js` 是应用工厂，`index.js` 只负责启动

### 2. 拖拽排序逻辑（重要！）
- 拖拽即绝对顺序，`priority` 仅作展示标签不影响排列
- 实现在 `features/order/order.service.js` 的 `reorderQueue(artistId, orderedIds)`
- 前端传入完整的排序后 ID 数组，后端按序分配 `queue_position`
- 前端用 vuedraggable，`@end` 事件触发后端重排

### 3. 双模式（自助 + 手动）
- 客户可以自助填表下单（source='self'）
- 画师可以手动录入（source='manual'）
- 两种进同一个队列，管理方式一致
- 画师可以把状态改为"排满/休息"来关闭自助下单

### 4. 认证方案
- 6位数字登录码，5分钟有效，**5次**错误锁定
- 开发模式：登录码输出到控制台（`🔑 [DEV]` 前缀）
- 生产模式：登录码不返回，需 QQ Bot 发送（Phase 2）
- 会话 Token：HMAC-SHA256 签名，`base64url(JSON{id,t}).base64url(sig)`，7天有效
- **v0.5**：生产环境未设置 `SESSION_SECRET` 时启动报错（不再静默回退）
- **v0.6.2**：开发模式验证码通过 `AUTH_DEV_MODE=true` 环境变量显式开启（不再依赖 NODE_ENV）
- 无密码设计
- 管理员 QQ 通过 `ADMIN_QQ` 环境变量首次引导，运行时从 `platform_config` 表读取（支持运行时更换）

### 5. 子域名方案
- **当前实现（v0.4.1+）**：前端使用参数化路由 `/artist/:subdomain`，后端 `GET /api/artists/:subdomain` 识别画师
- 生产环境可选：泛解析 DNS + Caddy 通配符证书，从 hostname 提取子域名（需额外配置）
- 开发环境直接使用 `/artist/alice` 路径访问
- 管理员后台添加画师时填写 subdomain 字段即可，无需 DNS 变更

### 6. 文件上传
- 上传路由在 `features/upload/upload.routes.js`
- **v0.5**：MIME 白名单（jpg/png/webp/gif/pdf/psd/ai/zip/rar），非白名单返回 403
- 限制：10MB/张，最多5张
- 截断时自动清理残留文件

### 7. 客户端零注册
- 客户不需要账号
- **v0.5**：凭 **QQ号+订单号** 双验证查进度（`getClientQueuePosition` 比对 `client_qq`）
- 凭交付链接下载文件
- "不知道订单号"按钮：凭 QQ 号查询在该画师处的所有订单

### 8. 订单号生成（v0.5 重构）
- 格式：`{身份码}-{序号}`，如 `ALICE-001`
- 身份码（artist_code）：2-10位大写字母/数字，画师可自定义，默认子域名大写
- 序号：从该画师最后一条订单的 `order_no` 推导（`generateOrderNo`），UNIQUE 约束防碰撞
- >999 时动态位数（`ALICE-1000`），不补零
- 状态机：pending→confirmed→wip→revision→done→delivered/cancelled，禁止跳跃

### 9. 速率限制（v0.5 新增）
- `shared/middleware/rate-limit.js`：内存桶，按 IP+端点独立计数
- 登录码发送/验证：5次/分钟
- 公开下单/订单查询：10次/分钟
- 触发返回 HTTP 429，进程重启后重置

### 10. XSS 防护（v0.5 新增）
- `web/src/utils/sanitize.js`：白名单标签+属性过滤
- 所有 `v-html` 必须经过 `sanitizeHtml()` 消毒
- 后端 `validate.js` 提供 `escapeHtml()` 用于服务端转义

### 11. 多语言（i18n）
- vue-i18n@9 Composition API 模式（`legacy: false`）
- 语言包在 `web/src/locales/`，按命名空间组织（common/menu/landing/artistHome/orderForm/track/...）
- 自动检测 `navigator.language`，手动切换存 `localStorage('huiyue-locale')`
- Element Plus locale 通过 `App.vue` 的 `ElConfigProvider` 动态切换
- 日期格式化跟随 locale（`zh-CN` / `en-US`）
- 新增语言：在 `locales/` 加文件 → `i18n/index.js` 注册 → `ThemeToggle.vue` 加菜单项

### 12. 亮暗主题
- CSS 变量体系：`:root` 8 个语义变量，`html.dark` 覆盖暗色值
- Element Plus 暗色：`element-plus/theme-chalk/dark/css-vars.css` + `html.dark` 类名
- Pinia store（`stores/theme.js`）：系统偏好检测 → localStorage 持久化 → watch 自动应用
- ThemeToggle 组件嵌入 4 处：ArtistLayout 侧边栏、LandingPage、ArtistHome、Login
- 所有 scoped 样式用 CSS 变量，不硬编码颜色

---

## API 端点速查

### 公开
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/health | 健康检查 |
| GET | /api/artists | 所有画师公开列表（首页） |
| GET | /api/artists/:subdomain | 画师公开主页 |
| POST | /api/orders | 客户自助下单 |
| GET | /api/orders/track/:orderNo?qq=xxx | 查询进度（需QQ验证） |
| GET | /api/orders/delivery/:orderNo | 交付文件 |
| GET | /api/orders/by-qq?artistId=x&qq=y | 凭QQ查订单列表 |
| POST | /api/upload/reference | 参考图上传（备用） |

### 认证
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/send-code | 获取登录码 |
| POST | /api/auth/verify | 验证登录码 |
| GET | /api/auth/me | 当前画师信息 |

### 画师后台（Bearer Token）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/PUT | /api/artist/profile | 资料读写 |
| GET/POST | /api/artist/tiers | 档位列表/创建 |
| PUT/DELETE | /api/artist/tiers/:id | 档位修改/删除 |
| GET/POST | /api/artist/artworks | 作品列表/创建 |
| DELETE | /api/artist/artworks/:id | 删除作品 |
| GET/PUT | /api/artist/rules | 须知读写 |
| GET | /api/artist/orders | 订单列表（?status=筛选） |
| GET | /api/artist/queue | 排期队列 |
| GET | /api/artist/orders/:id | 订单详情 |
| POST | /api/artist/orders/manual | 手动录单 |
| PUT | /api/artist/orders/:id/status | 改状态 |
| PUT | /api/artist/orders/:id/priority | 改优先级 |
| PUT | /api/artist/queue/reorder | 拖拽排序 |
| POST | /api/artist/orders/:id/notes | 添加备注 |
| POST | /api/artist/orders/:id/deliver | 上传交付 |
| GET | /api/artist/stats | 仪表盘统计 |
| POST | /api/upload/image | 上传图片 |
| POST | /api/upload/deliverable | 上传交付文件 |

### 管理员（ADMIN_QQ）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/admin/artists | 所有画师 |
| POST | /api/admin/artists | 添加画师 |
| DELETE | /api/admin/artists/:id | 移除画师 |
| GET | /api/admin/stats | 全局统计 |

---

## 数据库表
artists（含 artist_code）, price_tiers, artworks, commission_rules, orders（含 client_qq, artist_code）, order_references, order_notes, deliverables, login_codes

---

## 测试

### 运行
```bash
cd server
npm test              # 全部测试（vitest run）
npm run test:watch    # 监听模式
```

### 测试架构
- 使用 `better-sqlite3` 的 `:memory:` 模式（vitest.config.js 设置 DB_PATH）
- `tests/setup.js` 提供 `cleanDb()` / `seedArtist()` / `seedOrder()` 工具
- 每个测试文件 `beforeEach` 清空所有表
- 不依赖外部文件，CI 可直接运行

### 用例覆盖（42 个）
| 模块 | 文件 | 用例数 | 编号 |
|------|------|--------|------|
| 订单服务 | order.service.test.js | 18 | TC-O-01 ~ TC-O-15（含 02b/05b/09b） |
| 认证服务 | auth.service.test.js | 10 | TC-A-01 ~ TC-A-10 |
| 画师服务 | artist.service.test.js | 8 | TC-R-01 ~ TC-R-05（含 01b/01c/03b） |
| 输入校验 | validate.test.js | 6 | TC-V-01 ~ TC-V-06 |

详细规格见 `docs/tdd-spec-v0.1.md`

---

## Phase 2 待做
- [ ] QQ Bot 集成（NapCat/LLOneBot, OneBot 11 WebSocket）
- [ ] Bot 指令：排期/接单/拒单/完成/交付/状态/新单/帮助
- [ ] 登录码通过 Bot 发送（删除 _dev_code）
- [ ] 新订单 QQ 通知画师
- [ ] 状态变更 QQ 通知客户（可选）
- [ ] 画师关闭通知功能的开关
- [ ] 参考图上传完善（客户端下单时真正上传文件）

## Phase 3 待做
- [ ] 收入统计图表
- [ ] 数据导出 Excel
- [ ] 客户历史订单汇总
- [ ] 批量操作
- [ ] 更多语言（日语等）

---

## 已知注意事项
1. `server/src/app.js` 中 pino-pretty 是 dev 依赖，生产环境自动跳过（动态 import + try/catch）
2. 客户端路由使用 `/artist/:subdomain` 参数化路径（v0.4.1 起），不再使用 `?artist=` 查询参数
3. **后端 API 统一返回 camelCase**（`orderNo`、`artistName`、`tierName`、`weiboUrl`），前端模板必须匹配，不要用 snake_case
4. Docker 多阶段构建：前端在 `frontend-build` 阶段编译，生产镜像只含 `--omit=dev` 后端依赖
5. 测试中 `auth.service.js` 的 SECRET 在模块加载时固定，vitest.config.js 的 env 必须在 import 前生效
6. `entrypoint.sh` 使用绝对路径（`/app/server/src/...`），不再依赖 CWD
7. `db/init.js` import 时无副作用，CLI 执行或 `tests/setup.js` 显式调用 `initDatabase(db)` 才建表
8. 所有路由层不含裸 SQL，业务查询统一在 `*.service.js` 中
9. `upload.routes.js` 的 `UPLOAD_DIR` 由 `app.js` 通过插件选项传入，保证与静态服务路径一致
10. i18n 语言包键名按页面命名空间组织，`common.*` 为跨页面共享（状态/优先级/按钮）
11. 暗色主题依赖 `html.dark` 类名，Element Plus 组件自动适配；自定义样式必须用 CSS 变量
12. **客户端 API 分层**：公开数据用 `artistPublicApi`（getAll/getProfile），订单用 `orderApi`（create/track/delivery），画师后台用 `artistApi`（需 token）。切勿混用
13. 后端 `GET /api/artists/:subdomain` 一次返回 profile+tiers+artworks+rules，前端不需要分多次请求
14. 后端 `deliverables` 数组中 `url` 字段已含 `/uploads/` 前缀，前端 `openFile()` 直接 `window.open(url)` 即可
15. **v0.5**：`requireAuth` 中间件注入 `req.artist` 完整对象（含 id/qq_number/name/artist_code），路由层用 `req.artist.id` 做归属校验
16. **v0.5**：订单号生成从该画师最后一条订单的 `order_no` 推导序号（`generateOrderNo`），无独立计数器表
17. **v0.5**：`client_qq` 当前为明文存储（TEXT），后续可升级为 SHA-256+salt 哈希
18. **v0.5**：前端所有 `v-html` 必须经过 `sanitizeHtml()`（`web/src/utils/sanitize.js`），新增页面时注意
19. **v0.5**：`updateTier()` 同时接受 camelCase（`workDays`）和 snake_case（`work_days`），前端统一用 camelCase
20. **v0.5**：`docker-compose.yml` 默认 `NODE_ENV=production`，本地开发需手动改为 `development`
21. **v0.6.1**：SQLite `ALTER TABLE ADD COLUMN` 不支持 `UNIQUE` 约束，迁移必须先加列再 `CREATE UNIQUE INDEX`
22. **v0.6.1**：`app.js` 通过 `fastifyStatic(web/dist)` + `setNotFoundHandler` 实现 SPA fallback，`/api/` 和 `/uploads/` 前缀排除在外
23. **v0.6.1**：`getPlatformConfig()` 使用 `??`（非 `||`），空字符串是合法配置值
24. **v0.6.1**：`initDatabase()` 中 `INSERT OR IGNORE` 确保 `platform_config` 有 `admin_qq` 默认键
25. **v0.6.2**：`init.js` 使用版本化迁移（`schema_migrations` 表 + `MIGRATIONS` 数组），新增迁移只需追加 `{ version, up }` 对象
26. **v0.6.2**：`auth.js` 中 `ADMIN_QQ` 不再 export（P2-6），仅内部首次引导使用；运行时管理员从 `platform_config` 读取
27. **v0.6.2**：前端时间显示统一用 `web/src/utils/datetime.js`（`formatDateTime`/`formatDateTimeShort`），将 SQLite UTC 字符串正确转为本地时间
28. **v0.6.2**：`order.routes.js` 所有写入路由均有 Fastify JSON Schema（`additionalProperties: false`），Fastify 默认 `removeAdditional: true` 静默剥离多余字段
29. **v0.6.2**：交付上传前端校验在 `OrderDetail.vue`（50MB 限制 + 扩展名白名单），`file-list` 绑定 `deliverFileList` 确保 UI 同步
30. **v0.6.2**：登录码验证使用 `crypto.timingSafeEqual`（P0-4c），防止计时攻击
