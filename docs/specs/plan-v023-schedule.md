# v0.23 排期建议

> **编号**：plan-v023-schedule
> **作者**：四号（需求整理）
> **日期**：2026-08-01
> **状态**：建议稿，待一号审核
> **来源**：一号派工 01-to-04-v023-spec-update-20260801

---

## 1. 范围

| # | 项 | 工时 | 来源 | 状态 |
|---|---|---|---|---|
| B7 | 额度池（含 T3 BUG 修复） | 12.5h | plan-v023-quota-pool | spec 就绪，Q1-Q4 拍板，T1-T5 确认 |
| B4 | 留言板 | 9h | plan-v019 §5 | 草案就绪，用户已拍板核心规则 |
| T3 | 话术变量 {已付} 永远 ¥0 | 并入 B7 | 三号发现 | 额度池改造顺带修复 |
| **合计** | | **~21.5h** | | |

> T3 不单独排——话术变量改读 paid_total_cents 就修了，零额外成本。

---

## 2. 优先级

**B7 额度池 > B4 留言板**

理由：
1. B7 修复现有 BUG（话术变量永远 ¥0），影响画师日常使用
2. B7 是付款模型升级，后续功能（日历视图等）可能依赖收款数据
3. B4 留言板是锦上添花，不阻塞任何现有功能

---

## 3. 并行策略

| 阶段 | 三号（后端） | 二号（前端） | 说明 |
|------|-------------|-------------|------|
| 第 1 波 | **B7 后端**（5.5h）：迁移 v24 + 收款 API + 分期推算 + 话术修复 + 删 adjustInstallments | 等 B7 后端 API 就绪 | 三号先走，B7 后端是全部前端的前置 |
| 第 2 波 | **B4 后端**（3h）：建表 + 7 个 API + 限流 | **B7 画师端**（3h）：收款记录区重做 | 三号做 B4 时二号消化 B7 产出 |
| 第 3 波 | B7 测试（1h）+ B4 测试（1h） | **B7 客户端 + 管理端**（2h）：track 页四项数据 + 流水只读 | 二号收尾 B7，三号开始测试 |
| 第 4 波 | 补测试 / 回归 | **B4 前端**（5h）：客户端留言墙 2h + 画师端审核 2h + 管理端 1h | 二号做 B4 前端，三号回归 |
| 收尾 | 统一回归 | 统一回归 | 一轮覆盖 B7 + B4 全部新功能 |

---

## 4. 关键路径

```
三号 B7 后端（5.5h）→ 二号 B7 画师端（3h）→ 二号 B7 客户端+管理端（2h）
                                                                    ↘ 统一回归
三号 B4 后端（3h）──────────────────────────→ 二号 B4 前端（5h）──→ 统一回归
```

**关键路径**：~14h（三号 B7 后端 5.5h → 二号 B7 前端 5h → 回归 1h → B4 前端与 B7 部分重叠）

**实际日历时间**：两个 agent 并行，约 2-3 轮会话。

---

## 5. 依赖与风险

| 项 | 依赖 | 风险 |
|---|------|------|
| B7 前端 | B7 后端 API 就绪 | 🟢 第 1 波串行，无并行冲突 |
| B4 前端 | B4 后端 API 就绪 | 🟢 第 2 波三号做 B4 后端，二号做 B7 前端，不冲突 |
| T3 BUG 修复 | 并入 B7 | 🟢 零额外成本 |
| 迁移 v24 | 无前置 | 🟢 独立 |

---

## 6. 不在 v0.23 的项

| 项 | 原因 |
|---|------|
| 日历视图（REQ-012 F5） | 用户拍板做，但未定版本。建议 v0.24 |
| 桌面端（REQ-012 D1） | v1.0+ 评估 |
| 个人记账（REQ-012 D2） | 不做 |
| 时间统计（REQ-012 F3） | 不做 |
| Sentry 前端 SDK | 候选项，非本次 |
| E2E 接入 CI | 候选项，非本次 |

---

## 7. 待一号确认

| # | 问题 |
|---|------|
| 1 | B7 + B4 是否都排 v0.23？还是只做 B7，B4 推 v0.24？ |
| 2 | 并行策略是否可行？（第 1 波二号等三号，约 5.5h 空闲） |
| 3 | 二号第 1 波空闲期是否安排其他任务？（如 Sentry 前端 SDK ~1h） |
