# ============================================
# Artist Commission Platform - 环境变量配置
# 复制此文件为 .env 并修改实际值
# ============================================

# ── 必填 ──
# 会话签名密钥（生产环境必须设置，长度 ≥ 32 字符）
SESSION_SECRET=

# 管理员 QQ 号（首次部署时自动创建管理员账号）
ADMIN_QQ=

# ── 部署模式 ──
# 生产环境：production（默认）
# 开发环境：development（会启用详细日志等）
NODE_ENV=production

# 开发模式：true 时 API 直接返回登录码（生产环境必须为 false）
AUTH_DEV_MODE=false

# ── 可选 ──
# 数据库路径（默认 /app/data/commission.db）
# DB_PATH=/app/data/commission.db

# 上传目录（默认 /app/uploads）
# UPLOAD_DIR=/app/uploads

# CORS 来源（多域名用逗号分隔，留空允许所有来源）
# CORS_ORIGIN=https://yourdomain.com

# 主域名（Caddy 使用）
# DOMAIN=yourdomain.com